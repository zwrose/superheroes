# eval fixtures (placeholder)

Behavioral / loop eval fixtures land here as the plugins that produce them arrive
(Phase 1 onward — see [../gate.md](../gate.md) and [../README.md](../README.md)).

There are none yet: at Phase 0 there is no runnable loop to fixture. The deterministic
conformance track ([../lib/](../lib/)) is what's live today. New fixtures are **added**,
never mutated — keep historical ones frozen so past eval runs stay comparable (the rule
[`plugins/superheroes/eval/`](../../plugins/superheroes/eval/) already follows).
