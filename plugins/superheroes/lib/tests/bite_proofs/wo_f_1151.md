# WO-F (#1151) bite-proofs — skew disclosure truth + copy-register content validation

## BP-F1 — divergence reason names what diverged

**Guarded element:** `test_skew_version_skew_divergent` — axis: version-skew-only divergence reason names `lib/version_skew.py`, uses watched-semantics wording, and never says `family/registry`.

**Neutralization:** restored `family/registry semantics may differ` in `plugins/superheroes/lib/version_skew.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-f -m pytest plugins/superheroes/lib/tests/test_version_skew.py::test_skew_version_skew_divergent -q
```

**Red run (exit 1):**
```
>       assert "family/registry" not in record["reason"]
E       AssertionError: assert 'family/registry' not in 'plugin-vers...release cut.'
E         'family/registry' is contained here:
E            0.31.0 — family/registry semantics may differ (lib/version_skew.py differ between the running plugin and this repository). The guard cannot know the semantic delta, only that one may exist: apply ratified deltas by hand or wait for the release cut.
plugins/superheroes/lib/tests/test_version_skew.py:206: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_version_skew.py::test_skew_version_skew_divergent
1 failed in 0.05s
```

**Restore:** re-applied `watched review semantics differ` (inverse of neutralization).

**Restore receipt:** `"watched review semantics differ (%s differ between the running plugin and "`

**Green run (exit 0):**
```
1 passed in 0.05s
```

---

## BP-F2 — STATUSES register validates content

**Guarded element:** `test_plugin_version_skew_status_token_copy_register_content` — axis: a registered doc's skew status tokens must match the register contract.

**Neutralization:** hand-edited `checked-degraded` → `checked-degrded` in `CONVENTIONS.md` line 690.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-f -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_status_token_copy_register_content -q
```

**Red run (exit 1):**
```
E       AssertionError: plugin-version-skew status-token copy content drift — path, expected, actual: [('CONVENTIONS.md', ['checked-clean', 'checked-degraded'], ['checked-clean'])]
E       assert not [('CONVENTIONS.md', ['checked-clean', 'checked-degraded'], ['checked-clean'])]
plugins/superheroes/lib/tests/test_ssot_drift.py:426: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_status_token_copy_register_content
1 failed in 0.17s
```

**Restore:** restored ``only `checked-degraded` appends to``

**Green run (exit 0):**
```
1 passed in 0.14s
```

---

## BP-F3 — DETAIL register discovery census

**Guarded element:** `test_plugin_version_skew_detail_token_copy_register_census` — axis: every `.md` carrying a skew-scoped DETAILS token must be in the detail register.

**Neutralization:** appended `<!-- plugin-version-skew semantics-divergent -->` to `README.md` (not in register).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-f -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_detail_token_copy_register_census -q
```

**Red run (exit 1):**
```
E       AssertionError: unregistered plugin-version-skew detail-token copy — add to _PLUGIN_VERSION_SKEW_DETAIL_TOKEN_COPY_REGISTER: ['README.md']
E       assert not ['README.md']
plugins/superheroes/lib/tests/test_ssot_drift.py:452: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_detail_token_copy_register_census
1 failed in 0.18s
```

**Restore:** removed the `README.md` comment line (inverse of neutralization).

**Restore receipt:** `[MIT](LICENSE) © Zach Rose`

**Green run (exit 0):**
```
1 passed in 0.15s
```

---

## BP-F4 — DETAIL register validates content

**Guarded element:** `test_plugin_version_skew_detail_token_copy_register_content` — axis: each registered DETAILS holder's skew-scoped tokens match the register contract.

**Neutralization:** hand-edited `semantics-divergent` → `semantics-divergnt` in `auto-fix-loop.md` line 806.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-f -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_detail_token_copy_register_content -q
```

**Red run (exit 1):**
```
E       AssertionError: plugin-version-skew detail-token copy content drift — path, expected, actual: [('plugins/superheroes/skills/review-code/reference/auto-fix-loop.md', ['evidence-unreadable', 'semantics-divergent'], ['evidence-unreadable'])]
E       assert not [('plugins/superheroes/skills/review-code/reference/auto-fix-loop.md', ['evidence-unreadable', 'semantics-divergent'], ['evidence-unreadable'])]
plugins/superheroes/lib/tests/test_ssot_drift.py:471: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_detail_token_copy_register_content
1 failed in 0.19s
```

**Restore:** restored ``disclosed `plugin-version-skew` (semantics-divergent``

**Green run (exit 0):**
```
1 passed in 0.14s
```

---

## BP-F5 — `DEGRADING_DETAILS` consumer pin

**Guarded element:** `test_plugin_version_skew_degrading_details_consumer` — axis: `DEGRADING_DETAILS` equals the detail values `detect()` returns on degrading paths.

**Neutralization:** removed `DETAIL_EVIDENCE_UNREADABLE` from `DEGRADING_DETAILS` in `version_skew.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-f -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_degrading_details_consumer -q
```

**Red run (exit 1):**
```
E       AssertionError: detect() degrading-path details ['evidence-unreadable', 'semantics-divergent'] != version_skew.DEGRADING_DETAILS ['semantics-divergent']
E       assert {'evidence-unreadable', 'semantics-divergent'} == frozenset({'semantics-divergent'})
plugins/superheroes/lib/tests/test_ssot_drift.py:506: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_degrading_details_consumer
1 failed in 0.19s
```

**Restore:** re-added `DETAIL_EVIDENCE_UNREADABLE` to `DEGRADING_DETAILS`.

**Restore receipt:**
```
DEGRADING_DETAILS = frozenset({
    DETAIL_SEMANTICS_DIVERGENT,
    DETAIL_EVIDENCE_UNREADABLE,
})
```

**Green run (exit 0):**
```
1 passed in 0.15s
```

---

## BP-F6 — two-vocabulary `not-checked` discrimination

**Guarded element:** skew-scoped status/detail pins — axis: base-certification `not-checked` edits do not trip skew pins; skew-disclosure `not-checked` edits do.

**Neutralization (base half):** edited base-guard `not-checked` → `not-chekked` in `round-driver.md` line 331 (`certification.base` tri-state).

**Command (base half — must stay green):**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-f -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_status_token_copy_register_content plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_detail_token_copy_register_content -q
```

**Base-half run (exit 0):**
```
2 passed in 0.14s
```

**Restore (base half):** restored ``failed), or `not-checked` (library/eval `run_loop` — guard did not run).``

**Neutralization (skew half):** edited skew-disclosure `not-checked` → `not-chekked` in `round-driver.md` line 548 (`tri-state skew disclosure`).

**Command (skew half — must go red):**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-f -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_status_token_copy_register_content plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_detail_token_copy_register_content -q
```

**Skew-half red run (exit 1):**
```
E       AssertionError: plugin-version-skew status-token copy content drift — path, expected, actual: [('plugins/superheroes/skills/review-code/reference/round-driver.md', ['checked-clean', 'checked-degraded', 'not-checked'], ['checked-clean', 'checked-degraded'])]
E       assert not [('plugins/superheroes/skills/review-code/reference/round-driver.md', ['checked-clean', 'checked-degraded', 'not-checked'], ['checked-clean', 'checked-degraded'])]
plugins/superheroes/lib/tests/test_ssot_drift.py:441: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_status_token_copy_register_content
1 failed, 1 passed in 0.18s
```

**Restore (skew half):** restored ``disclosure: `checked-clean`, `checked-degraded`, `not-checked`, or `absent` when the seat map``

**Skew-half green run (exit 0):**
```
2 passed in 0.15s
```
