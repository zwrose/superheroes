# WO-H (#1185) bite-proofs — advance-latched legacy session refusal detail

Natural red (budget invocation 1, before fix):

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_rollback.py::test_legacy_refusal_advance_latched_names_fresh_session \
  -q --tb=line
```

```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-1a9031ea062d9ea1/plugins/superheroes/lib/tests/test_state_version_rollback.py:138: AssertionError: assert 'advance-submit-interleaved' in 'loop-state.json is schemaVersion 3; `next`/`submit` finish it'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_rollback.py::test_legacy_refusal_advance_latched_names_fresh_session
1 failed in 1.58s
```

Green after fix (budget invocation 2):

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_rollback.py::test_legacy_refusal_advance_latched_names_fresh_session \
  -q --tb=line
```

```
.                                                                        [100%]
1 passed in 1.41s
```

Whole-file run (budget invocation 3):

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_rollback.py -q --tb=line
```

```
..........                                                               [100%]
10 passed in 8.03s
```

---

## BP-H1 — `_advanceUsed` branch in `_legacy_session_refusal_detail`

**Guarded element:** `_advanceUsed` branch in `_legacy_session_refusal_detail` — axis: an advance-latched legacy session with no durable seat records at the pending slot is told to start a fresh session dir, NOT that `next`/`submit` will finish it.

**Neutralization:** removed the `_advanceUsed` check (restored pre-change branch order) in `_legacy_session_refusal_detail` at `plugins/superheroes/lib/round_driver.py`, so an advance-latched legacy state falls through to the `next`/`submit` message.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_rollback.py::test_legacy_refusal_advance_latched_names_fresh_session \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-1a9031ea062d9ea1/plugins/superheroes/lib/tests/test_state_version_rollback.py:138: AssertionError: assert 'advance-submit-interleaved' in 'loop-state.json is schemaVersion 3; `next`/`submit` finish it'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_rollback.py::test_legacy_refusal_advance_latched_names_fresh_session
1 failed in 1.42s
```

**Restore:** re-inserted the `_advanceUsed` check before `_legacy_session_has_durable_pending_records` in `_legacy_session_refusal_detail`.

**Restore receipt:**
```
    if loaded.get("_advanceUsed"):
        return ("loop-state.json is schemaVersion %r with `_advanceUsed` set — hand `submit` "
                "refuses `advance-submit-interleaved`; start a fresh session dir to continue"
                % (version,))
```

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 1.68s
```

---

Post-restore `git status --porcelain` (only the three deliverables differ from HEAD):

```
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_state_version_rollback.py
?? plugins/superheroes/lib/tests/bite_proofs/wo_h_1185.md
```
