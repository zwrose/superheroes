# WO-1311A bite-proof — foreign-instance pin gate

**Provenance:** cursor composer-2.5 / dispatch-write

## Guarded element

- **guarded element:** `plugins/superheroes/lib/launcher.py` — `_instance_pin_gate` mismatch branch (`seat_signal == "resolved"` and effective child root differs from seat root)
- **axis:** unequal seat/pin pair becomes refusal token `foreign-instance-pin`

## Neutralization

Replace the mismatch `_fail("foreign-instance-pin", …)` return with proceed:

```python
        return None, "matched"
```

(applied at the `seat_signal == "resolved"` unequal-root branch in `_instance_pin_gate`)

## Command

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_launcher.py::test_cli_launch_foreign_instance_pin_refuses -q -p no:randomly
```

## Raw red

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_cli_launch_foreign_instance_pin_refuses _________________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-1697/test_cli_launch_foreign_instan0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x105cfcfa0>

    def test_cli_launch_foreign_instance_pin_refuses(tmp_path, monkeypatch):
        ...
        assert exit_code == 1
        payload = json.loads(buf.getvalue())
>       assert payload["reason"] == "foreign-instance-pin"
E       AssertionError: assert 'settle-exit-zero-uncertain' == 'foreign-instance-pin'
E         
E         - foreign-instance-pin
E         + settle-exit-zero-uncertain

plugins/superheroes/lib/tests/test_launcher.py:4887: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_launcher.py::test_cli_launch_foreign_instance_pin_refuses
1 failed in 1.69s
```

## Restore

Reverted neutralization — restored the mismatch branch to:

```python
        return _fail(
            "foreign-instance-pin",
            seatConfigDir=seat_root or "",
            pinnedConfigDir=effective or "",
        ), None
```

## Restore receipt

Neutralized lines quoted back as the pre-neutralization `_fail("foreign-instance-pin", …)` return shown above.

## Raw green

```
.                                                                        [100%]
1 passed in 0.34s
```
