# WO-G (#1154) bite-proofs — pin fail-open closure (round-2)

## BP-G1 — the `ast` extractor (G1)

**Guarded element:** `_direct_lib_local_imports` / leg 8 — axis: *an aliased dependency is seen*.

**Neutralization:** in `lib/owner_authority.py` `allow_file_path`, changed the function-nested import and added an unprotected aliased import:
```python
def allow_file_path(cwd, root=None):
    import mode_registry as mr
    import core_md as cm
    return os.path.join(mode_registry.project_store_dir(cwd, root), ALLOW_FILENAME)
```
(Under the old regex extractor this neutralization was green — `import mode_registry as mr` was dropped and `core_md` was never collected.)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_dependencies_are_fixer_refused -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________________ test_gate_dependencies_are_fixer_refused ___________________

    def test_gate_dependencies_are_fixer_refused():
        band_roots = [PLUGIN]
        for module in sorted(_direct_lib_local_imports(_OWNER_AUTHORITY)):
            abs_path = os.path.join(PLUGIN, "lib", f"{module}.py")
>           assert ESC.is_safety_machinery(abs_path, band_roots) is True, (
                f"gate dependency {module!r} must be refused to the fixer "
                f"(is_safety_machinery({abs_path!r}, {band_roots!r}))"
            )
E           AssertionError: gate dependency 'core_md' must be refused to the fixer (is_safety_machinery('/Users/zwrose/.superheroes-worktrees/superheroes/1154-G/plugins/superheroes/lib/core_md.py', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-G/plugins/superheroes']))
E           assert False is True
E            +  where False = <function is_safety_machinery at 0x1066703a0>('/Users/zwrose/.superheroes-worktrees/superheroes/1154-G/plugins/superheroes/lib/core_md.py', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-G/plugins/superheroes'])
E            +    where <function is_safety_machinery at 0x1066703a0> = ESC.is_safety_machinery

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:339: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_dependencies_are_fixer_refused
1 failed in 0.34s
```

**Restore:** reverted `allow_file_path` to:
```python
def allow_file_path(cwd, root=None):
    import mode_registry
    return os.path.join(mode_registry.project_store_dir(cwd, root), ALLOW_FILENAME)
```

**Restore receipt (supplementary `git status --porcelain`):** (empty for `plugins/superheroes/lib/owner_authority.py`)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.12s
```

---

## BP-G2 — the routing-target leg (G2)

**Guarded element:** `test_fixer_routing_target_is_pinned` — axis: *the retired routing wording cannot come back*.

**Neutralization:** reverted `rubric/orders/dispatch-fixer.md` line ~24 to retired wording:
```markdown
   safety machinery); report it for owner escalation (see Payload contract) instead. Never
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_fixer_routing_target_is_pinned -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_____________________ test_fixer_routing_target_is_pinned ______________________

    def test_fixer_routing_target_is_pinned():
        for rel, present, absent in _FIXER_ROUTING_TARGETS:
            text = _read(rel)
>           assert present in text, (
                f"{rel}: fixer routing target must include current wording: {present!r}"
            )
E           AssertionError: rubric/orders/dispatch-fixer.md: fixer routing target must include current wording: 'report it for orchestrator escalation (see Payload contract) instead'
E           assert 'report it for orchestrator escalation (see Payload contract) instead' in 'You are the fixer for one round of an auto-fix code-review loop.\n\n## Input\n- Findings to fix: {{FIX_BATCH_PATH}} (...oaches, ambiguous intent), do NOT guess.\nReport it for owner escalation (see Payload contract) with the id and why.\n'

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:327: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_fixer_routing_target_is_pinned
1 failed in 0.27s
```

**Restore:** reverted line ~24 back to:
```markdown
   safety machinery); report it for orchestrator escalation (see Payload contract) instead. Never
```

**Restore receipt (supplementary `git status --porcelain`):** (empty for `plugins/superheroes/rubric/orders/dispatch-fixer.md`)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.30s
```

---

## BP-G3 — the normalized absence leg (G3)

**Guarded element:** `test_retired_preauthorization_rule_is_absent` — axis: *a reflowed retired sentence is still caught*.

**Neutralization:** inserted a retired sentence into `review-discipline.md` broken across two lines at a space:
```markdown
Owner authorization is required when the findings are
blocking.
```
(Under the un-normalized absence leg this reflow was green — raw substring match missed the line break.)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_retired_preauthorization_rule_is_absent -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_retired_preauthorization_rule_is_absent _________________

    def test_retired_preauthorization_rule_is_absent():
        home_text = _collapse_whitespace(_read(_HOME))
        copy_text = _collapse_whitespace(_read(_COPY_HOLDER))
        for substring in _RETIRED_BOTH_FILES:
            if substring in home_text:
>               raise AssertionError(
                    f"{_HOME}: retired rule substring must be absent, found: {substring!r}"
                )
E               AssertionError: rubric/review-discipline.md: retired rule substring must be absent, found: 'Owner authorization is required when the findings are blocking'

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:165: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_retired_preauthorization_rule_is_absent
1 failed in 0.28s
```

**Restore:** removed the two-line insertion (inverse edit — deleted the blank line and both sentence lines).

**Restore receipt (supplementary `git status --porcelain`):** ` M plugins/superheroes/rubric/review-discipline.md` (WO-G doctrine edits remain; neutralization lines absent)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.13s
```
