# WO-R1 (#1124 review round 1) bite-proofs — investigation-floor cross-boundary drift pin

## Baseline (pre-change)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py -q
```

**Output:**
```
100 passed in 0.50s
```

(Pytest emitted `PytestWarning: (rm_rf) error removing ... Directory not empty` noise from xdist temp cleanup; exit code 0, all tests passed.)

---

## N1 — home↔doc threshold comparison

**Guarded element:** `_assert_investigation_floor_threshold_matches_home` — axis: quantity threshold in `spot_check_investigated`'s docstring must match document copies; weakening home without touching docs must fail as disagreement, not parse failure.

**Neutralization:** changed docstring `at least one claimed path` to `ideally one claimed path` at `engine_adapter.py:1669` (both reference documents untouched).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated \
  -q
```

**Red run:**
```
E       AssertionError: investigation floor drift: dispatch-mechanics.md (investigated threshold) and spot_check_investigated disagree on at-least-one surviving-path threshold (home='ideally one', doc='at least one')
E       assert 'ideally one' == 'at least one'
E         
E         - at least one
E         + ideally one

plugins/superheroes/lib/tests/test_ssot_drift.py:477: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated
1 failed in 0.20s
```

**Restore:** restored docstring clause to `at least one claimed path must resolve inside the repo and exist.`

**Restore receipt:** `A spot check, not an audit: at least one claimed path must resolve inside the repo and exist.`

**Green run:**
```
1 passed in 0.15s
```

---

## N2 — regular-file clause probe

**Guarded element:** `_investigation_floor_operative_clauses_from_home` `regular-file requirement` probe — axis: `os.path.isfile(real)` guard must remain in home source.

**Neutralization:** removed the `if not os.path.isfile(real):` / `_reject(entry, "not-a-file")` block from `spot_check_investigated` at `engine_adapter.py:1718-1720`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated \
  -q
```

**Red run:**
```
E       AssertionError: spot_check_investigated floor could not be parsed — missing: ['regular-file requirement']
E       assert not ['regular-file requirement']

plugins/superheroes/lib/tests/test_ssot_drift.py:428: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated
1 failed in 0.20s
```

**Restore:** reinserted the `if not os.path.isfile(real):` / `_reject(entry, "not-a-file")` block before the generated-artifact check.

**Restore receipt:** restored lines `if not os.path.isfile(real):`, `_reject(entry, "not-a-file")`, and `continue`.

**Green run:**
```
1 passed in 0.16s
```

---

## N3 — repo-confinement clause probe

**Guarded element:** `_investigation_floor_operative_clauses_from_home` `repo-confinement check` probe — axis: realpath confinement branch must remain in home source.

**Neutralization:** removed the `if real != root_real and not real.startswith(root_prefix):` / `_reject(entry, "escapes-repo")` block from `spot_check_investigated` at `engine_adapter.py:1712-1714`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated \
  -q
```

**Red run:**
```
E       AssertionError: spot_check_investigated floor could not be parsed — missing: ['repo-confinement check']
E       assert not ['repo-confinement check']

plugins/superheroes/lib/tests/test_ssot_drift.py:428: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated
1 failed in 0.20s
```

**Restore:** reinserted the `if real != root_real and not real.startswith(root_prefix):` / `_reject(entry, "escapes-repo")` block after realpath resolution.

**Restore receipt:** restored lines `if real != root_real and not real.startswith(root_prefix):`, `_reject(entry, "escapes-repo")`, and `continue`.

**Green run:**
```
1 passed in 0.16s
```

---

## N4 — dispatch-mechanics.md leg (regression guard)

**Guarded element:** `skills/workhorse/reference/dispatch-mechanics.md` Findings-only review prompts — axis: populated `investigated` requirement cannot be deleted without drift failure.

**Neutralization:** deleted the populated-`investigated` requirement sentence (vet probe 4's exact mutation):
```
A review prompt that constrains the seat's stdout to a **single JSON object** must also require a
**populated `investigated` array** and tell the seat that an **empty `findings` array alongside a
real investigation record is a valid, welcome answer**.
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated \
  -q
```

**Red run:**
```
E       AssertionError: dispatch-mechanics.md (Findings-only review prompts): investigation floor prose drift — missing: ['populated investigated required']
E       assert not ['populated investigated required']

plugins/superheroes/lib/tests/test_ssot_drift.py:541: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated
1 failed in 0.21s
```

**Restore:** reinserted the three deleted lines immediately under `### Findings-only review prompts`.

**Restore receipt:** restored lines beginning `A review prompt that constrains the seat's stdout` through `real investigation record is a valid, welcome answer**.`

**Green run:**
```
1 passed in 0.15s
```

---

## Neutralized-surface check

**Command:**
```
git status --porcelain
```

**Output:**
```
 M plugins/superheroes/lib/tests/test_ssot_drift.py
?? plugins/superheroes/lib/tests/bite_proofs/wo_r1_1124.md
```

`dispatch-mechanics.md` and `engine_adapter.py` are absent from porcelain — all four neutralizations were restored by inverse edit.
