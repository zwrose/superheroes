# WO-L (#1122) bite-proofs

## BP20 — route liveness (`test_probe_git_fake_routes_are_live` step 3)

**Guarded element:** `test_engine_dispatch.py:test_probe_git_fake_routes_are_live` step-3 assertion — axis: **every declared fake route appears in the live probe observation set**.

**Neutralization:** reverted D2's fallback fake to `_git_scrubbed` and its `PROBE_GIT_FAKE_ROUTES` entry to `("_git_scrubbed", "worktree")`:

```python
    "test_worktree_baseline_fallback_on_enumeration_failure": {
        ("_git_scrubbed", "worktree"),
    },
...
    real_git = ED._git_scrubbed
...
    monkeypatch.setattr(ED, "_git_scrubbed", fail_worktree_list)
```

**Raw red** (node `test_probe_git_fake_routes_are_live`):

```
F                                                                        [100%]
=================================== FAILURES ===================================
_____________________ test_probe_git_fake_routes_are_live ______________________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-8193/test_probe_git_fake_routes_are0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10471f0d0>

    def test_probe_git_fake_routes_are_live(tmp_path, monkeypatch):
        ...
        for test_name, declared_routes in PROBE_GIT_FAKE_ROUTES.items():
            for route in declared_routes:
>               assert route in observed_set, (
                    "%s declares route %r but probe path never issued it; observed %r"
                    % (test_name, route, sorted(observed_set))
                )
E               AssertionError: test_worktree_baseline_fallback_on_enumeration_failure declares route ('_git_scrubbed', 'worktree') but probe path never issued it; observed [('_git_scrubbed', 'rev-parse'), ('_git_scrubbed_bytes', 'status'), ('_git_scrubbed_bytes', 'worktree')]
E               assert ('_git_scrubbed', 'worktree') in {('_git_scrubbed', 'rev-parse'), ('_git_scrubbed_bytes', 'status'), ('_git_scrubbed_bytes', 'worktree')}

plugins/superheroes/lib/tests/test_engine_dispatch.py:5862: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_routes_are_live
1 failed in 0.46s
```

**Restore:** inverse edit — restored `_git_scrubbed_bytes` fake and `("_git_scrubbed_bytes", "worktree")` declaration.

**Restore receipt:** restored lines:

```python
    "test_worktree_baseline_fallback_on_enumeration_failure": {
        ("_git_scrubbed_bytes", "worktree"),
    },
...
    real_bytes = ED._git_scrubbed_bytes
...
    monkeypatch.setattr(ED, "_git_scrubbed_bytes", fail_worktree_list)
```

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.40s
```

## BP21 — declaration completeness (`test_probe_git_fake_routes_are_live` step 4)

**Guarded element:** `test_engine_dispatch.py:test_probe_git_fake_routes_are_live` step-4 both-direction comparison — axis: **declared table keys match scanned fake sites exactly**.

**Neutralization:** removed `test_worktree_baseline_non_utf8_pathname_no_raise` from `PROBE_GIT_FAKE_ROUTES` while its fake site remained.

**Raw red** (node `test_probe_git_fake_routes_are_live`):

```
F                                                                        [100%]
=================================== FAILURES ===================================
_____________________ test_probe_git_fake_routes_are_live ______________________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-8195/test_probe_git_fake_routes_are0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x103b7d700>

    def test_probe_git_fake_routes_are_live(tmp_path, monkeypatch):
        ...
        undeclared = scanned_tests - declared_tests
>       assert not undeclared, (
            "fake sites without PROBE_GIT_FAKE_ROUTES entry: %r" % sorted(undeclared)
        )
E       AssertionError: fake sites without PROBE_GIT_FAKE_ROUTES entry: ['test_worktree_baseline_non_utf8_pathname_no_raise']
E       assert not {'test_worktree_baseline_non_utf8_pathname_no_raise'}

plugins/superheroes/lib/tests/test_engine_dispatch.py:5869: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_routes_are_live
1 failed in 0.46s
```

**Restore:** inverse edit — restored the removed `PROBE_GIT_FAKE_ROUTES` entry:

```python
    "test_worktree_baseline_non_utf8_pathname_no_raise": {
        ("_git_scrubbed_bytes", "status"),
    },
```

**Restore receipt:** restored entry quoted above.

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.40s
```

## BP22 — fixed test proves itself (`test_worktree_baseline_fallback_on_enumeration_failure`)

**Guarded element:** `test_engine_dispatch.py:test_worktree_baseline_fallback_on_enumeration_failure` — axis: **discriminating fake branch fired** (`assert worktree_list_call["n"] > 0`).

**Neutralization:** changed the fake discriminator so its branch can never match:

```python
        if args[:2] == ("worktree", "show"):
```

**Raw red** (node `test_worktree_baseline_fallback_on_enumeration_failure`):

```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_worktree_baseline_fallback_on_enumeration_failure ____________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-8198/test_worktree_baseline_fallbac0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10210d2e0>

    def test_worktree_baseline_fallback_on_enumeration_failure(tmp_path, monkeypatch):
        ...
>       assert worktree_list_call["n"] > 0
E       assert 0 > 0

plugins/superheroes/lib/tests/test_engine_dispatch.py:5459: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_baseline_fallback_on_enumeration_failure
1 failed in 0.49s
```

**Restore:** inverse edit — restored discriminator:

```python
        if args[:2] == ("worktree", "list"):
```

**Restore receipt:** restored line quoted above.

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.41s
```
