# WO-A1 (#1269) bite-proofs — seat-bundle entry surface

**Provenance:** cursor-agent / composer-2.5

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-A1-1 | `seat_bundle.scan_dropped_flags` | a `--engine` CLI invocation refuses before dispatch | `test_dropped_flag_with_valid_seat_still_refuses` |
| BP-A1-2 | `_parse_json` effort-key check | a JSON seat bundle without an `effort` key refuses | `test_effort_key_absent_refused` |

---

## BP-A1-1 — dropped-flag trap

- **axis:** a `--engine` CLI invocation refuses before dispatch

**neutralization** (early return `[]` in `scan_dropped_flags`):
```python
def scan_dropped_flags(argv: list[str]) -> list[str]:
    """Return dropped legacy flags present in argv (both spellings)."""
    return []
    found: list[str] = []
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_dropped_flag_with_valid_seat_still_refuses ________________

    def test_dropped_flag_with_valid_seat_still_refuses():
        argv = [
            "dispatch-review",
            "--engine", "codex",
            "--seat", _seat_json("codex", "gpt-5.6-sol", "high"),
            "--role", _REVIEW_ROLE,
            "--prompt-path", "p",
            "--repo-root", "/tmp",
            "--run-dir", "/tmp/r",
        ]
>       assert SB.scan_dropped_flags(argv)
E       assert []
E        +  where [] = <function scan_dropped_flags at 0x10479b670>(['dispatch-review', '--engine', 'codex', '--seat', '{"vendor": "codex", "model": "gpt-5.6-sol", "effort": "high"}', '--role', ...])
E        +    where <function scan_dropped_flags at 0x10479b670> = SB.scan_dropped_flags

plugins/superheroes/lib/tests/test_seat_bundle.py:208: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_bundle.py::test_dropped_flag_with_valid_seat_still_refuses
1 failed in 0.50s
```

**restore:** removed the `return []` early exit from `scan_dropped_flags`.

**restore receipt:**
```python
def scan_dropped_flags(argv: list[str]) -> list[str]:
    """Return dropped legacy flags present in argv (both spellings)."""
    found: list[str] = []
```

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 0.62s
```

---

## BP-A1-2 — required `effort` key

- **axis:** a JSON seat bundle without an `effort` key refuses

**neutralization** (treat absent key as `effort: null`):
```python
    if "effort" not in obj:
        obj = dict(obj)
        obj["effort"] = None
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________________ test_effort_key_absent_refused ________________________

    def test_effort_key_absent_refused():
        raw = json.dumps({"vendor": "cursor", "model": "composer-2.5"})
        bundle = SB.parse(raw)
>       assert bundle["ok"] is False
E       assert True is False

plugins/superheroes/lib/tests/test_seat_bundle.py:180: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_bundle.py::test_effort_key_absent_refused
1 failed in 0.72s
```

**restore:** reinstated the `effort-key-absent` refusal block in `_parse_json`.

**restore receipt:**
```python
    if "effort" not in obj:
        return {
            "ok": False,
            "reason": "effort-key-absent",
            "detail": (
                'JSON seat must include the "effort" key (value may be null); '
                f"accepted: {_ACCEPTED_SEAT}"
            ),
        }
```

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 0.56s
```

---

## Re-verification on the chokepoint head (2026-09-16, orchestrator)

Both elements re-run at their recorded sites after the seat parser was rewritten:

- **BP-A1-1** — `scan_dropped_flags` returning `[]` turned
  `test_dropped_flag_with_valid_seat_still_refuses` red (`1 failed in 0.14s`); the inverse edit turned
  it green (`1 passed in 0.12s`).
- **BP-A1-2** — treating an absent `effort` key as `null` turned `test_effort_key_absent_refused` red
  (`1 failed in 0.12s`); the inverse edit turned it green (`1 passed in 0.10s`). The neutralization
  now applies to the entry parser `_parse_entry_dict`, which is where the required-key check moved.

Tree confirmed clean after every restore.
