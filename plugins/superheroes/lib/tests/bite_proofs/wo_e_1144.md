# WO-E (#1144) bite-proof record — architect_config newer-record guard

## BP-E1 — write-side refusal over newer persisted record

- **guarded element:** `architect_config.py:write_policy` — axis: refuses to overwrite a persisted record whose `schemaVersion` exceeds `SCHEMA_VERSION`
- **neutralization:** removed the refusal `return None` inside the lock block (replaced with `pass` so the write proceeds)
- **command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_architect_config.py::test_write_policy_refuses_over_newer_persisted_no_schema_in_argument plugins/superheroes/lib/tests/test_architect_config.py::test_write_policy_refuses_over_newer_persisted_with_current_argument -q`

### raw red

```
FF                                                                       [100%]
=================================== FAILURES ===================================
_____ test_write_policy_refuses_over_newer_persisted_no_schema_in_argument _____

    def test_write_policy_refuses_over_newer_persisted_no_schema_in_argument(tmp_path):
        ...
        result = AC.write_policy(str(tmp_path), pol, root=store)
>       assert result is None
E       AssertionError: assert {'confirmed': False, 'disclosures': [], 'location': 'docs/other', 'schemaVersion': 2, ...} is None

_____ test_write_policy_refuses_over_newer_persisted_with_current_argument _____

    def test_write_policy_refuses_over_newer_persisted_with_current_argument(tmp_path):
        ...
        result = AC.write_policy(str(tmp_path), pol, root=store)
>       assert result is None
E       AssertionError: assert {'confirmed': False, 'disclosures': [], 'location': 'docs/other', 'schemaVersion': 2, ...} is None

=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_architect_config.py::test_write_policy_refuses_over_newer_persisted_no_schema_in_argument
FAILED plugins/superheroes/lib/tests/test_architect_config.py::test_write_policy_refuses_over_newer_persisted_with_current_argument
2 failed in 0.40s
```

### restore

Restored `return None` on the newer-version branch:

```python
                if declared is not None and declared > SCHEMA_VERSION:
                    return None
```

### restore receipt

```python
                if declared is not None and declared > SCHEMA_VERSION:
                    return None
```

(the refusal branch inside `write_policy`, quoted verbatim after restoration)

### raw green

```
..                                                                       [100%]
2 passed in 0.38s
```

---

## BP-E2 — read-side preservation of newer record version in memory

- **guarded element:** `architect_config.py:_migrate` — axis: preserves `schemaVersion` and unknown keys when record version exceeds `SCHEMA_VERSION`
- **neutralization:** added `result["schemaVersion"] = SCHEMA_VERSION` in the newer-record branch (stamp down)
- **command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_architect_config.py::test_read_policy_preserves_future_record_in_memory -q`

### raw red

```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_read_policy_preserves_future_record_in_memory ______________

    def test_read_policy_preserves_future_record_in_memory(tmp_path):
        ...
        migrated = AC._migrate(raw)
        assert migrated["futureKey"] == "keep me"
>       assert migrated["schemaVersion"] == 99
E       assert 2 == 99

=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_architect_config.py::test_read_policy_preserves_future_record_in_memory
1 failed in 0.18s
```

### restore

Removed the `result["schemaVersion"] = SCHEMA_VERSION` line; newer-record branch again preserves the record's own version.

### restore receipt

```python
        result = dict(rec)
        result["location"] = location
```

(no `schemaVersion` stamp in newer branch)

### raw green

```
.                                                                        [100%]
1 passed in 0.04s
```

---

## BP-E3 — traversal guard on newer records

- **guarded element:** `architect_config.py:_migrate` — axis: `_safe_location` applies to newer records (path-traversal guard, not schema opinion)
- **neutralization:** `result["location"] = rec.get("location")` instead of normalized `location` in newer-record branch
- **command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_architect_config.py::test_migrate_traversal_guard_applies_to_newer_record -q`

### raw red

```
F                                                                        [100%]
=================================== FAILURES ===================================
_____________ test_migrate_traversal_guard_applies_to_newer_record _____________

    def test_migrate_traversal_guard_applies_to_newer_record(tmp_path):
        ...
        migrated = AC._migrate(rec)
>       assert migrated["location"] == AC.DEFAULT_LOCATION
E       AssertionError: assert '../escape' == 'docs/superheroes'
E         
E         - docs/superheroes
E         + ../escape

=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_architect_config.py::test_migrate_traversal_guard_applies_to_newer_record
1 failed in 0.18s
```

### restore

```python
        result["location"] = location
```

(where `location` is `_safe_location(rec.get("location"))`)

### restore receipt

```python
        result["location"] = location
```

### raw green

```
.                                                                        [100%]
1 passed in 0.04s
```

---

All three neutralizations reverted by inverse edit. Post-restore `git status --porcelain`:

```
 M plugins/superheroes/lib/tests/bite_proofs/wo_e_1144.md
 M plugins/superheroes/lib/tests/test_architect_config.py
 M plugins/superheroes/skills/architect-init/SKILL.md
```
