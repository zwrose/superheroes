# WO-A (#1247) bite-proof — canary verdict vocabulary + producer

**Provenance:** cursor-agent / composer-2.5

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-A1 | `seat_canary._engaged_from_dispatch` | investigation evidence required for engagement | `test_engaged_from_dispatch_diverges_from_engagement_read`, `test_dod_row1_field_recurrence_specimen_not_engaged` |
| BP-A2 | `canary_outcome.classify` step 3 | an undetected plant is never `ok` | `test_dod_row2_engaged_dispatch_plant_undetected_when_marker_missing` |
| BP-A3 | `canary_outcome.normalize` cross-field guard | a caller cannot assert a pass its fields refuse | `test_normalize_outcome_ok_contradicts_fields` |
| BP-A4 | three member literals (`OUTCOME_OK`, `OUTCOME_NOT_ENGAGED`, `OUTCOME_PLANT_UNDETECTED`) | external-contract constant, pinned by literal | `test_canary_outcome_member_literals_pinned` |

---

## BP-A1 — `_engaged_from_dispatch`

- **axis:** investigation evidence required for engagement

**neutralization** (restore `engagement_read` equivalence):
```python
def _engaged_from_dispatch(res):
    return engine_adapter.engagement_read(res) == "engaged"
```

**raw red** (exit 1):
```
.......................FF.............                                   [100%]
=================================== FAILURES ===================================
___________ test_engaged_from_dispatch_diverges_from_engagement_read ___________

    def test_engaged_from_dispatch_diverges_from_engagement_read():
        findings_only = {
            "findings": [{"id": "f"}],
            "investigated": [],
            "engagement": {"toolCalls": None},
        }
        assert EA.engagement_read(findings_only) == "engaged"
>       assert SC._engaged_from_dispatch(findings_only) is False
E       AssertionError: assert True is False
E        +  where True = <function _engaged_from_dispatch at 0x10b01cb80>({'engagement': {'toolCalls': None}, 'findings': [{'id': 'f'}], 'investigated': []})
E        +    where <function _engaged_from_dispatch at 0x10b01cb80> = SC._engaged_from_dispatch

plugins/superheroes/lib/tests/test_seat_canary.py:543: AssertionError
_____________ test_dod_row1_field_recurrence_specimen_not_engaged ______________

    def test_dod_row1_field_recurrence_specimen_not_engaged():
        ...
>       assert out["engaged"] is False
E       assert True is False

plugins/superheroes/lib/tests/test_seat_canary.py:586: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_canary.py::test_engaged_from_dispatch_diverges_from_engagement_read
FAILED plugins/superheroes/lib/tests/test_seat_canary.py::test_dod_row1_field_recurrence_specimen_not_engaged
2 failed, 36 passed in 2.26s
```

**restore:** reinstate investigation-evidence predicate (non-empty `investigated` OR `toolCalls >= 1` after `engagement_read == "engaged"`).

**restore receipt:**
```
 M plugins/superheroes/lib/seat_canary.py
```

**raw green** (budget invocation 1 — same restored tree):
```
......................................                                   [100%]
38 passed in 2.08s
```

---

## BP-A2 — `classify` step 3

- **axis:** an undetected plant is never `ok`

**neutralization** (step 3 returns pass):
```python
    if detected_plant is not True:
        return OUTCOME_OK
```

**raw red** (exit 1):
```
.........................F....F.......                                   [100%]
=================================== FAILURES ===================================
_____ test_dod_row2_engaged_dispatch_plant_undetected_when_marker_missing ______

>       assert out["outcome"] == CO.OUTCOME_PLANT_UNDETECTED
E       AssertionError: assert 'ok' == 'plant-undetected'

plugins/superheroes/lib/tests/test_seat_canary.py:604: AssertionError
_________________ test_normalize_outcome_ok_contradicts_fields _________________

>       assert outcome == CO.OUTCOME_PLANT_UNDETECTED
E       AssertionError: assert 'ok' == 'plant-undetected'

plugins/superheroes/lib/tests/test_seat_canary.py:658: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_canary.py::test_dod_row2_engaged_dispatch_plant_undetected_when_marker_missing
FAILED plugins/superheroes/lib/tests/test_seat_canary.py::test_normalize_outcome_ok_contradicts_fields
2 failed, 36 passed in 2.41s
```

**restore:** `return OUTCOME_PLANT_UNDETECTED` on undetected plant branch.

**restore receipt:**
```
?? plugins/superheroes/lib/canary_outcome.py
```

**raw green:** same as BP-A1 green (38 passed in 2.08s).

---

## BP-A3 — `normalize` cross-field guard

- **axis:** a caller cannot assert a pass its fields refuse

**neutralization** (trust caller `outcome`; remove contradiction block):
```python
    return outcome, None
```
(deletes the `if outcome == OUTCOME_OK and ...` block)

**raw red** (exit 1):
```
..............................F.......                                   [100%]
=================================== FAILURES ===================================
_________________ test_normalize_outcome_ok_contradicts_fields _________________

>       assert outcome == CO.OUTCOME_PLANT_UNDETECTED
E       AssertionError: assert 'ok' == 'plant-undetected'

plugins/superheroes/lib/tests/test_seat_canary.py:658: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_canary.py::test_normalize_outcome_ok_contradicts_fields
1 failed, 37 passed in 1.68s
```

**restore:** reinstate contradiction guard returning `canary-outcome-contradicts-fields`.

**restore receipt:**
```
?? plugins/superheroes/lib/canary_outcome.py
```

**raw green:** same as BP-A1 green (38 passed in 2.08s).

---

## BP-A4 — member literals

- **axis:** external-contract constant, pinned by literal

**neutralization:**
```python
OUTCOME_OK = "ok-pass"  # the ONLY pass member
```

**raw red** (exit 1):
```
................................F.....                                   [100%]
=================================== FAILURES ===================================
__________________ test_canary_outcome_member_literals_pinned __________________

>       assert CO.OUTCOME_OK == "ok"
E       AssertionError: assert 'ok-pass' == 'ok'

plugins/superheroes/lib/tests/test_seat_canary.py:674: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_canary.py::test_canary_outcome_member_literals_pinned
1 failed, 37 passed in 2.20s
```

**restore:** `OUTCOME_OK = "ok"`

**restore receipt:**
```
?? plugins/superheroes/lib/canary_outcome.py
```

**raw green:** same as BP-A1 green (38 passed in 2.08s).
