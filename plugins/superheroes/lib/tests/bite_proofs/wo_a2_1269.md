# WO-A2 (#1269) bite-proof — write-path resolved-input snapshot echo and seat immutability

**Provenance:** cursor-agent / composer-2.5

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-A2-1 | `_resolved_inputs_echo_from_run_dir` | poll carries no seat; echo still comes from the journal snapshot, not the invocation | `test_write_resolved_inputs_poll_echo_matches_opened_snapshot_without_seat` |
| BP-A2-2 | `_continuation_seat_mismatch` | immutable seat at open; disagreeing continuation refuses by name | `test_write_continuation_different_seat_refuses` |

---

## BP-A2-1 — snapshot-sourced echo on poll

- **axis:** poll carries no seat; echo still comes from the journal snapshot, not the invocation

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `_resolved_inputs_echo_from_run_dir`):
```python
        echo = {
            "runOpened": True,
            "resolvedInputs": {},  # bite-proof neutralization WO-A2
        }
```
(replaces `"resolvedInputs": _resolved_inputs_from_opened(opened),`)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch_write.py::test_write_resolved_inputs_poll_echo_matches_opened_snapshot_without_seat -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
__ test_write_resolved_inputs_poll_echo_matches_opened_snapshot_without_seat ___

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-367/test_write_resolved_inputs_pol0')

    def test_write_resolved_inputs_poll_echo_matches_opened_snapshot_without_seat(tmp_path):
        # axis: I2 — poll has no seat; echo still comes from the journal snapshot
        wt, _main = _linked_worktree(tmp_path)
        run_dir = str(tmp_path / "poll-echo")
        fake = FakeRunner([])
        _dispatch_write(tmp_path, fake, cwd=wt, run_dir=run_dir, max_wait=0)
        snapshot = _write_opened_resolved_inputs(run_dir)
        polled = ED.dispatch_poll(run_dir)
        assert polled["runOpened"] is True
>       assert polled["resolvedInputs"] == snapshot
E       AssertionError: assert {} == {'baseSha': '...ed-none', ...}
E         
E         Right contains 36 more items:
E         {'baseSha': '57ca58ff00088f06b8bf82bfee33826b1cb7ccd2',
E          'baseShaSource': 'resolved',
E          'diffBase': None,
E          'diffBaseSource': 'declared-none',
E          'effort': 'high',...
E         
E         ...Full output truncated (32 lines hidden), use '-vv' to show

plugins/superheroes/lib/tests/test_engine_dispatch_write.py:2481: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch_write.py::test_write_resolved_inputs_poll_echo_matches_opened_snapshot_without_seat
1 failed in 2.87s
```

**restore:**
```python
        echo = {
            "runOpened": True,
            "resolvedInputs": _resolved_inputs_from_opened(opened),
        }
```

**raw green:**
```
.                                                                        [100%]
1 passed in 2.34s
```

---

## BP-A2-2 — continuation immutable seat check

- **axis:** immutable seat at open; disagreeing continuation refuses by name

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `_continuation_seat_mismatch`):
```python
def _continuation_seat_mismatch(opened, seat, role):
    return None  # bite-proof neutralization WO-A2
```
(replaces the snapshot comparison and `SEAT_REFUSAL_RUN_DIR_MISMATCH` return)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch_write.py::test_write_continuation_different_seat_refuses -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_write_continuation_different_seat_refuses ________________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-369/test_write_continuation_differ0')

    def test_write_continuation_different_seat_refuses(tmp_path):
        # axis: I3 — immutable seat at open; disagreeing continuation refuses by name
        wt, _main = _linked_worktree(tmp_path)
        run_dir = str(tmp_path / "seat-mismatch")
        fake = FakeRunner([])
        _dispatch_write(tmp_path, fake, cwd=wt, run_dir=run_dir, max_wait=0)
        res = ED.dispatch_write(
            seat=_cursor_seat(),
            role=_WRITE_ROLE,
            cwd=wt,
            run_dir=run_dir,
            order_id="order-1",
            prompt_path=_prompt(tmp_path),
            run_engine=FakeRunner([]),
            max_wait=0,
        )
>       assert res["detail"] == ED.SEAT_REFUSAL_RUN_DIR_MISMATCH
E       KeyError: 'detail'

plugins/superheroes/lib/tests/test_engine_dispatch_write.py:2500: KeyError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch_write.py::test_write_continuation_different_seat_refuses
1 failed in 3.65s
```

**restore:**
```python
def _continuation_seat_mismatch(opened, seat, role):
    snapshot = opened.get("resolvedInputs")
    if not isinstance(snapshot, dict):
        return None
    if _continuation_seat_tuple(snapshot) != _incoming_seat_tuple(seat, role):
        return SEAT_REFUSAL_RUN_DIR_MISMATCH
    return None
```

**raw green:**
```
.                                                                        [100%]
1 passed in 5.23s
```

---

## Re-verification on the chokepoint head (2026-09-16, orchestrator)

- **BP-A2-1** — replacing the snapshot-sourced echo with `{}` turned
  `test_write_resolved_inputs_poll_echo_matches_opened_snapshot_without_seat` red (`1 failed in
  0.44s`); the inverse edit turned it green (`1 passed in 0.45s`).
- **BP-A2-2** — an early `return None` in `_continuation_seat_mismatch` turned
  `test_write_continuation_different_seat_refuses` red (`1 failed in 0.48s`); the inverse edit turned
  it green (`1 passed in 0.45s`). The comparison now reads the role from the seat bundle rather than
  a separate argument, and the element still bites.

Tree confirmed clean after every restore.
