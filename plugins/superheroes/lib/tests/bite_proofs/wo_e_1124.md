# WO-E (#1124 item 7) bite-proofs — investigation-floor guidance drift-pinned to spot_check_investigated

## Baseline (pre-change)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py -q
```

**Output:**
```
99 passed in 0.86s
```

---

## N1 — dispatch-mechanics.md leg

**Guarded element:** `skills/workhorse/reference/dispatch-mechanics.md` Findings-only review prompts — axis: populated `investigated` requirement cannot be deleted without drift failure.

**Neutralization:** deleted the populated-`investigated` requirement sentence (vet probe 3's exact mutation):
```
A review prompt that constrains the seat's stdout to a **single JSON object** must also require a
**populated `investigated` array** and tell the seat that an **empty `findings` array alongside a
real investigation record is a valid, welcome answer**.
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated \
  -q
```

**Red run:**
```
E       AssertionError: dispatch-mechanics.md (Findings-only review prompts): investigation floor prose drift — missing: ['populated investigated required']
E       assert not ['populated investigated required']

plugins/superheroes/lib/tests/test_ssot_drift.py:468: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated
1 failed in 1.22s
```

**Restore:** reinserted the three deleted lines immediately under `### Findings-only review prompts`.

**Restore receipt:** restored lines quoted above — the sentence beginning `A review prompt that constrains the seat's stdout` through `real investigation record is a valid, welcome answer**.`

**Green run:**
```
1 passed in 0.17s
```

---

## N2 — auto-fix-loop.md leg

**Guarded element:** `skills/review-code/reference/auto-fix-loop.md` vacuous-forfeit block — axis: `only when investigated lists at least one path` clause cannot be deleted.

**Neutralization:** deleted `when `investigated` lists at least one path that` from the blockquote, leaving `only when that survives the runner's spot-check`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated \
  -q
```

**Red run:**
```
E       AssertionError: auto-fix-loop.md: investigation-floor requirement pin could not be located (moved or reformatted?)
E       assert None

plugins/superheroes/lib/tests/test_ssot_drift.py:453: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated
1 failed in 0.20s
```

**Restore:** restored `when `investigated` lists at least one path that` in the blockquote.

**Restore receipt:** restored substring `` `findings` array is accepted as *clean* **only** when `investigated` lists at least one path that survives the runner's spot-check ``

**Green run:**
```
1 passed in 0.15s
```

---

## N3 — code-floor binding

**Guarded element:** `engine_adapter.spot_check_investigated` docstring — axis: at-least-one threshold clause must remain parseable from home source.

**Neutralization:** changed docstring `at least one claimed path` to `ideally one claimed path` at `engine_adapter.py:1669`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated \
  -q
```

**Red run:**
```
E       AssertionError: spot_check_investigated floor could not be parsed — missing: ['at-least-one threshold in docstring']
E       assert not ['at-least-one threshold in docstring']

plugins/superheroes/lib/tests/test_ssot_drift.py:423: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated
1 failed in 0.20s
```

**Restore:** restored docstring clause to `at least one claimed path must resolve inside the repo and exist.`

**Restore receipt:** `A spot check, not an audit: at least one claimed path must resolve inside the repo and exist.`

**Green run:**
```
1 passed in 0.18s
```

---

## Neutralized-surface check

**Command:**
```
git status --porcelain
```

**Output:**
```
 M plugins/superheroes/lib/tests/test_ssot_drift.py
```

`dispatch-mechanics.md`, `auto-fix-loop.md`, and `engine_adapter.py` are absent from porcelain — all three neutralizations were restored by inverse edit.
