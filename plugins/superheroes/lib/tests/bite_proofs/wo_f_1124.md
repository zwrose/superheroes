# WO-F (#1124 items 19 + 20) bite-proofs — rewrite-failure diagnosable condition

## BP1 — `_cap_file_tail` rewrite-failure signal

**Guarded element:** `_cap_file_tail` rewrite-failure return third element (`engine_dispatch.py:2011–2013`) — axis: failed rewrite is distinguishable from clean cap via `rewrite_failed`.

**Neutralization:** changed rewrite-failure `except` to `return True, observed, False` (same third element as clean cap).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_cap_file_tail_rewrite_failure_preserves_over_cap_authority \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_rewrite_failure_capture_grades_truncated \
  -q -p no:xdist --tb=line
```

**Red run:**
```
FF                                                                       [100%]
=================================== FAILURES ===================================
...test_engine_dispatch.py:5678: assert False is True
...test_engine_dispatch.py:5729: KeyError: 'stdoutCapRewriteFailed'
=========================== short test summary info ============================
FAILED ...test_cap_file_tail_rewrite_failure_preserves_over_cap_authority
FAILED ...test_rewrite_failure_capture_grades_truncated
2 failed in 1.37s
```

**Restore:** restored `return True, observed, True` in the rewrite-failure `except` branch.

**Restore receipt:** `return True, observed, True`

**Green run:**
```
..                                                                       [100%]
2 passed in 1.37s
```

---

## BP2 — `stdoutCapRewriteFailed` stamp in `_run_engine_files`

**Guarded element:** `_run_engine_files` stamp (`engine_dispatch.py` stdoutCapRewriteFailed block) — axis: rewrite failure stamps `stdoutCapRewriteFailed` on attempt-ended.

**Neutralization:** deleted the `if stdout_rewrite_failed: ended_record["stdoutCapRewriteFailed"] = True` block.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_rewrite_failure_capture_grades_truncated \
  -q -p no:xdist --tb=line
```

**Red run:**
```
F                                                                        [100%]
FAILED ...test_rewrite_failure_capture_grades_truncated
1 failed in 1.2s
```

**Restore:** restored stamp block:
`if stdout_rewrite_failed: ended_record["stdoutCapRewriteFailed"] = True`

**Green run:** (same node as BP1 green — stamp present after restore)

---

## BP3 — ledger whitelist entry

**Guarded element:** `_ledger_attempt_records` whitelist tuple — axis: `stdoutCapRewriteFailed` reaches durable ledger row.

**Neutralization:** removed `"stdoutCapRewriteFailed"` from the whitelist tuple.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_ledger_attempt_records_carries_stdout_cap_rewrite_failed \
  -q -p no:xdist --tb=line
```

**Red run:**
```
F                                                                        [100%]
FAILED ...test_ledger_attempt_records_carries_stdout_cap_rewrite_failed
1 failed in 1.2s
```

**Restore:** restored `"stdoutCapRewriteFailed"` in the whitelist tuple.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.05s
```

---

## BP4 — disclosure branch

**Guarded element:** `_stdout_capped_forfeit` rewrite-failure disclosure branch — axis: disclosure names rewrite failure when stamped.

**Neutralization:** made rewrite-failure branch emit the ordinary truncation sentence instead of the rewrite-failure sentence.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_capped_forfeit_disclosure_rewrite_failure_names_rewrite_problem \
  -q -p no:xdist --tb=line
```

**Red run:**
```
F                                                                        [100%]
FAILED ...test_stdout_capped_forfeit_disclosure_rewrite_failure_names_rewrite_problem
1 failed in 1.2s
```

**Restore:** restored rewrite-failure truncation clause with "could not be rewritten" / "disk or filesystem problem".

**Green run:**
```
.                                                                        [100%]
1 passed in 0.05s
```

---

## BP5 — injection discriminator (load-bearing)

**Guarded element:** rewrite-failure test injection discriminator — axis: patched `open` matching on `"wb"` is observed; if it stops matching, rewrite succeeds and assertions fail.

**Neutralization:** changed `_cap_file_tail` rewrite to `os.fdopen(os.open(...))` so `builtins.open` patch with `mode == "wb"` no longer intercepts; rewrite succeeded (file capped, non-zero size).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_cap_file_tail_rewrite_failure_preserves_over_cap_authority \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_rewrite_failure_capture_grades_truncated \
  -q -p no:xdist --tb=line
```

**Red run:**
```
FF                                                                       [100%]
FAILED ...test_cap_file_tail_rewrite_failure_preserves_over_cap_authority
FAILED ...test_rewrite_failure_capture_grades_truncated
2 failed in 1.7s
```
(Confirmed rewrite succeeded under neutralization: injection no longer matched `builtins.open`.)

**Restore:** restored `with open(path, "wb") as fh:` for the rewrite path.

**Restore receipt:** `with open(path, "wb") as fh:`

**Green run:**
```
..                                                                       [100%]
2 passed in 1.3s
```
