# WO-L4A-A (#1273) bite-proof — claude argv census + adapter/dispatch seams

**Provenance:** cursor / composer-2.5 (implementer). **Rung line:** D1 retires when claude process launches no longer go through argv lists; tag `c14-l4a-D1`.

## Bite-proof table

| ID | Guarded element | Red exit | Green exit |
|---|---|---|---|
| BP-A1 | `test_claude_argv_census.py` mutator rule | 1 | 0 |
| BP-A2 | `test_claude_argv_census.py` scope-aware vendor-enum | 1 | 0 |
| BP-A3 | `test_claude_argv_census.py` path-spelling rule | 1 | 0 |
| BP-A4 | `engine_dispatch.py:_claude_cli` file walk | 1 | 0 |
| BP-A5 | `engine_adapter.py:claude_builder_argv` no `--restricted` | 1 | 0 |
| BP-A6 | `engine_adapter.py:claude_builder_argv` refusal (a) | 1 | 0 |
| BP-A7 | `engine_adapter.py:claude_builder_argv` refusal (c) | 1 | 0 |
| BP-A8 | `engine_dispatch.py:claude_session_stop_confirmed` done-row stop | 1 | 0 |
| BP-A9 | `engine_dispatch.py:claude_session_stop_confirmed` live pid | 1 | 0 |
| BP-A10 | `engine_dispatch.py:_review_terminal_forfeit` r1 `droppedCause` | 1 | 0 |

---

## BP-A1 — census mutator rule

- **axis:** `.append("claude")` is flagged as argv construction

**neutralization** (`test_claude_argv_census.py`):
```python
_MUTATOR_SUFFIXES = (".extend", ".insert")
```

**command:** `pytest plugins/superheroes/lib/tests/test_claude_argv_census.py::test_census_flags_every_argv_spelling[append-mutator] -q`

**raw red** (exit 1): `FAILED ...[append-mutator] ... assert []`

**restore:** `_MUTATOR_SUFFIXES = (".append", ".extend", ".insert")`

**raw green** (exit 0): `1 passed in 0.05s`

---

## BP-A2 — scope-aware vendor-enum rule

- **axis:** `cmd = ["claude"]` followed by `cmd +=` is flagged

**neutralization** (`test_claude_argv_census.py`):
```python
if isinstance(target, ast.Name):
    return True, "vendor-enum-assign"
```

**command:** `pytest ...::test_census_flags_every_argv_spelling[augassign-argv] -q`

**raw red** (exit 1): `FAILED ...[augassign-argv] ... assert []`

**restore:** reinstate `and target.id not in tainted_names` guard

**raw green** (exit 0): `1 passed`

---

## BP-A3 — path-spelling rule

- **axis:** `/usr/local/bin/claude` literals are flagged

**neutralization** (`test_claude_argv_census.py`):
```python
return isinstance(value, str) and value == "claude"
```

**command:** `pytest ...::test_census_flags_every_argv_spelling[path-spelling] -q`

**raw red** (exit 1): `FAILED ...[path-spelling] ... assert []`

**restore:** `value == "claude" or value.endswith("/claude")`

**raw green** (exit 0): `1 passed`

---

## BP-A4 — file walk reaches production code

- **axis:** hand-built argv in `engine_dispatch._claude_cli` is censused

**neutralization** (`engine_dispatch.py`):
```python
cmd = ["claude"] + list(args)
cmd = engine_adapter.claude_cli_argv(args)
```

**command:** `pytest ...::test_no_claude_argv_outside_engine_adapter -q`

**raw red** (exit 1): `AssertionError: claude-argv-outside-adapter:lib/engine_dispatch.py:861`

**restore:** remove the `["claude"] + list(args)` line

**raw green** (exit 0): `1 passed in 1.64s`

---

## BP-A5 — builder argv omits `--restricted`

**neutralization** (`engine_adapter.py` `claude_builder_argv`):
```python
argv.append("--restricted")
argv.append(prompt)
```

**command:** `pytest ...::test_claude_builder_argv_omits_restricted_and_print_flags -q`

**raw red** (exit 1): `assert '--restricted' not in argv`

**restore:** remove `argv.append("--restricted")`

**raw green** (exit 0): `1 passed`

---

## BP-A6 — refusal (a) unknown-claude-tier

**neutralization** (`engine_adapter.py`):
```python
if not isinstance(token, str):
```

**command:** `pytest ...::test_claude_builder_argv_unknown_claude_tier_refusal -q`

**raw red** (exit 1): `assert None == 'unknown-claude-tier'`

**restore:** reinstate `or token not in model_registry.claude_dispatch_tokens()`

**raw green** (exit 0): `1 passed`

---

## BP-A7 — refusal (c) builder-prompt-missing

**neutralization** (`engine_adapter.py`):
```python
if not isinstance(prompt, str):
```

**command:** `pytest ...::test_claude_builder_argv_builder_prompt_missing_refusal -q`

**raw red** (exit 1): `assert None == 'builder-prompt-missing'`

**restore:** reinstate `or not prompt.strip()`

**raw green** (exit 0): `1 passed`

---

## BP-A8 — done row still issues stop

**neutralization** (`engine_dispatch.py` `claude_session_stop_confirmed`):
```python
rows_pre, ok_pre = _claude_agents_rows(...)
if ok_pre:
    row_pre = _claude_agent_row_for_launch(rows_pre, launch_id)
    if isinstance(row_pre, dict) and row_pre.get("state") == "done":
        return "stopped"
```

**command:** `pytest ...::test_claude_session_stop_confirmed_done_row_still_issues_stop -q`

**raw red** (exit 1): `assert [] == [['stop', 'id-done']]`

**restore:** remove pre-stop early-return block

**raw green** (exit 0): `1 passed`

---

## BP-A9 — live pid → background-stop-unconfirmed

**neutralization** (`engine_dispatch.py`):
```python
return "stopped"
for poll in range(6):
```

**command:** `pytest ...::test_claude_session_stop_confirmed_live_pid_unconfirmed -q`

**raw red** (exit 1): `assert 'stopped' == 'background-stop-unconfirmed'`

**restore:** remove early `return "stopped"`

**raw green** (exit 0): `1 passed`

---

## BP-A10 — r1 droppedCause on terminal forfeit

**neutralization** (`engine_dispatch.py` `_review_terminal_forfeit`):
```python
# removed droppedCause assignment block
```

**command:** `pytest ...::test_review_terminal_forfeit_surfaces_dropped_cause -q`

**raw red** (exit 1): `KeyError: 'droppedCause'`

**restore:**
```python
if isinstance(dropped_cause, str) and dropped_cause:
    result["droppedCause"] = dropped_cause
```

**raw green** (exit 0): `1 passed`
