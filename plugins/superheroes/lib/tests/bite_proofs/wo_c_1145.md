# WO-C (#1145) bite-proofs — every emitted seat prompt carries schema-rendered example

## BP-C1 — C1 append (`engine_dispatch.py` review fed_prompt)

**Guarded element:** `test_review_dispatch_fed_prompt_includes_schema_example_block` — axis: composed fedPrompt must include `review_findings_schema.example_prompt_block()` (C1 runner chokepoint append).

**Neutralization:** replaced multi-line `fed_prompt = (ANTIHIJACK_PREAMBLE + notice + base_prompt + review_findings_schema.example_prompt_block())` with `fed_prompt = ANTIHIJACK_PREAMBLE + notice + base_prompt` only.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_includes_schema_example_block -q
```

**Red run (exit 1):**
```
>       assert block in opened["fedPrompt"]
E       AssertionError: assert 'Format only — your findings replace every value; an echoed example grades hollow.\n{...}' in 'You are a dispatched ONE-SHOT code reviewer...Review this code.\n'
plugins/superheroes/lib/tests/test_engine_dispatch.py:2801: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_includes_schema_example_block
```

**Restore:** reinstated `+ review_findings_schema.example_prompt_block()` append on the review-path `fed_prompt` composition.

**Restore receipt:**
```python
            fed_prompt = (
                ANTIHIJACK_PREAMBLE + notice + base_prompt
                + review_findings_schema.example_prompt_block()
            )
```

**Green run (exit 0):**
```
1 passed in 2.95s
```

---

## BP-C2 — echo still grades `prompt-echo-only` (early `prompt_echo_only` gate)

**Guarded element:** `test_grade_review_attempt_prompt_echo_payload_shape_prompt_echo_only` — axis: full prompt echo (including schema example JSON) forfeits as `prompt-echo-only`, not engaged findings.

**Neutralization:** removed the early `if prompt_echo_only:` return block in `_grade_review_attempt` (before `parse_result`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch.py::test_grade_review_attempt_prompt_echo_payload_shape_prompt_echo_only -q
```

**Red run (exit 1):**
```
>       assert grade.get("forfeit") is True
E       AssertionError: assert None is True
plugins/superheroes/lib/tests/test_engine_dispatch.py:2782: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_grade_review_attempt_prompt_echo_payload_shape_prompt_echo_only
```

**Restore:** reinserted early `prompt_echo_only` gate returning `SHAPE_PROMPT_ECHO_ONLY` before parse.

**Restore receipt:** full `if prompt_echo_only:` block restored in `_grade_review_attempt`.

**Green run (exit 0):**
```
1 passed in 0.52s
```

---

## BP-C3 — C2 emitting sites (panel stdout example + OUTPUT_CHANNEL_BLOCK)

**Guarded element:** `test_review_findings_example_emitting_sites_delegate_to_home` — axis: C2a/C2b sites reference home, not hand-written `{"findings": [...], "investigated": [...]}` literal.

**Neutralization:** in `_stdout_payload_example` panel branch, replaced `json.dumps(review_findings_schema.example_findings_object(), sort_keys=True)` with hand literal `'{"findings": [...], "investigated": [...]}'`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home -q
```

**Red run (exit 1):**
```
E       AssertionError: C2a (lib/round_orders.py): hand-written findings example literal reappeared: '{"findings": [...], "investigated": [...]}'
plugins/superheroes/lib/tests/test_ssot_drift.py:5843: AssertionError
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home
```

**Restore:** panel branch again returns `json.dumps(review_findings_schema.example_findings_object(), sort_keys=True)`.

**Restore receipt:** `return json.dumps(review_findings_schema.example_findings_object(), sort_keys=True), None`

**Green run (exit 0):**
```
1 passed in 0.12s
```

---

## BP-C4 — C3 seat canary contract block

**Guarded element:** `test_review_findings_example_emitting_sites_delegate_to_home` — axis: C3 `seat_canary.py` references `review_findings_schema.example_prompt_block()`, not hand-written inline JSON shape.

**Neutralization:** replaced `% review_findings_schema.example_prompt_block()` in `CANARY_FIXTURE_PROMPT` with hand-written `{"findings": [{"id": "...", ...}], "investigated": [...]}` inline literal.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home -q
```

**Red run (exit 1):**
```
E       AssertionError: C3 (lib/seat_canary.py): hand-written findings example literal reappeared: '{"findings": [{"id": "...", "severity": "...", "file": "...", "title": "...", "body": "..."}'
plugins/superheroes/lib/tests/test_ssot_drift.py:5843: AssertionError
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home
```

**Restore:** `CANARY_FIXTURE_PROMPT` again uses `% review_findings_schema.example_prompt_block()` for the contract block.

**Restore receipt:** `""" % review_findings_schema.example_prompt_block()` tail on `CANARY_FIXTURE_PROMPT`.

**Green run (exit 0):**
```
1 passed in 0.12s
```

---

## BP-C5 — renderer/grader census same-home leg

**Guarded element:** `test_review_findings_renderer_and_grader_census_share_home_objects` — axis: `example_findings_object` member keys and grader census frozensets are `review_findings_schema` module singletons (`is`, not re-typed equality).

**Neutralization:** **Unprovable as placed** — breaking module singleton identity or member-key alignment requires editing `review_findings_schema.py` (WO-A home; out of WO-C scope). No consumer on the WO-C surface can falsify `CANONICAL_MEMBER_KEYS is rfs_reimport.CANONICAL_MEMBER_KEYS` without mutating the declared home.

**Disclosure:** `Unprovable as placed` for production neutralization; detector exercises import-time singleton identity on the home module only.
