# WO-B (#1219) bite-proofs — per-clause rows for bite-proof doctrine one-home

Guarded-element set: FA-1, FA-2, FA-3, FA-4.

## FA-1 — implementer.md row (home and holder legs)

**Guarded element:** `_CLAUSE_ROWS` FA-1 row — axis: each declared clause occurs exactly its declared count in its declared section (home `rubric/bite-proof.md` § *The record*; holder `agents/implementer.md` § *Validating your work order*).

**Neutralization (home leg):** deleted `**The guarded-element set is declared up front.**` from the opening sentence of `## The record` in `rubric/bite-proof.md` (left `An order that adds or changes a detector` as the section opener).

**Command (home red):**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_bite_proof_doctrine.py::test_clause_present_in_home_and_copy_holder[FA-1]' -q
```

**Raw red (home leg):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_clause_present_in_home_and_copy_holder[FA-1] _______________

row = {'copy_holder': 'agents/implementer.md', 'copy_holder_section': '## Validating your work order', 'holder_clause': '**T...once in the bite-proof reference named above, § *The record*; read it there rather than here.', 'holder_count': 1, ...}

    @pytest.mark.parametrize("row", _CLAUSE_ROWS, ids=_ROW_IDS)
    def test_clause_present_in_home_and_copy_holder(row):
>       _check_clause_sync(row)

plugins/superheroes/lib/tests/test_bite_proof_doctrine.py:538: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
plugins/superheroes/lib/tests/test_bite_proof_doctrine.py:296: in _check_clause_sync
    check_clause(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

text = '# Bite-proof\n\nThis file is the **one home** for the bite-proof rule. Charters and skills point here rather\nthan re...he repository — and it\n  never asserts that a receipt is missing, because the build record is not among its inputs.\n'
rel = 'rubric/bite-proof.md', section_heading = '## The record'
clause = '**The guarded-element set is declared up front.**', expected_count = 1

    def check_clause(text, rel, section_heading, clause, expected_count):
        """Raise AssertionError when the section's clause count differs from expected."""
        actual = count_clause_in_section(text, rel, section_heading, clause)
        if actual != expected_count:
>           raise AssertionError(
                f"{rel} (section {section_heading}): clause {clause!r} expected count "
                f"{expected_count}, found {actual} — re-sync the text or update the roster"
            )
E           AssertionError: rubric/bite-proof.md (section ## The record): clause '**The guarded-element set is declared up front.**' expected count 1, found 0 — re-sync the text or update the roster

plugins/superheroes/lib/tests/clause_guard.py:59: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_bite_proof_doctrine.py::test_clause_present_in_home_and_copy_holder[FA-1]
1 failed in 0.81s
```

**Restore (home leg):** re-inserted `**The guarded-element set is declared up front.** ` before `An order that adds or changes a detector`.

**Restore receipt (home leg):** `git status --porcelain plugins/superheroes/rubric/bite-proof.md` → (empty after restore).

**Raw green (home leg):**
```
.                                                                        [100%]
1 passed in 0.99s
```

**Neutralization (holder leg):** deleted the validity-rule-6 sentence beginning `**The guarded-element set is declared up front** — what an order must declare…` from `agents/implementer.md` § *Validating your work order*.

**Raw red (holder leg):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_clause_present_in_home_and_copy_holder[FA-1] _______________

row = {'copy_holder': 'agents/implementer.md', 'copy_holder_section': '## Validating your work order', 'holder_clause': '**T...once in the bite-proof reference named above, § *The record*; read it there rather than here.', 'holder_count': 1, ...}

    @pytest.mark.parametrize("row", _CLAUSE_ROWS, ids=_ROW_IDS)
    def test_clause_present_in_home_and_copy_holder(row):
>       _check_clause_sync(row)

plugins/superheroes/lib/tests/test_bite_proof_doctrine.py:538: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
plugins/superheroes/lib/tests/test_bite_proof_doctrine.py:304: in _check_clause_sync
    check_clause(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

text = '---\nname: implementer\ndescription: Internal build subagent — implements one scoped work order dispatched by the Wor...ut of scope.\n- Return per the **Short structured return** rule above. Nothing beyond that — no verdict, no "ready."\n'
rel = 'agents/implementer.md', section_heading = '## Validating your work order'
clause = '**The guarded-element set is declared up front** — what an order must declare, what to do when it declares nothing, a...t enumeration are stated once in the bite-proof reference named above, § *The record*; read it there rather than here.'
expected_count = 1

    def check_clause(text, rel, section_heading, clause, expected_count):
        """Raise AssertionError when the section's clause count differs from expected."""
        actual = count_clause_in_section(text, rel, section_heading, clause)
        if actual != expected_count:
>           raise AssertionError(
                f"{rel} (section {section_heading}): clause {clause!r} expected count "
                f"{expected_count}, found {actual} — re-sync the text or update the roster"
            )
E           AssertionError: agents/implementer.md (section ## Validating your work order): clause "**The guarded-element set is declared up front** — what an order must declare, what to do when it declares nothing, and how the record's ceilings apply over that enumeration are stated once in the bite-proof reference named above, § *The record*; read it there rather than here." expected count 1, found 0 — re-sync the text or update the roster

plugins/superheroes/lib/tests/clause_guard.py:59: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_bite_proof_doctrine.py::test_clause_present_in_home_and_copy_holder[FA-1]
1 failed in 1.47s
```

**Restore (holder leg):** re-inserted the deleted validity-rule-6 sentence verbatim.

**Restore receipt (holder leg):** `git status --porcelain plugins/superheroes/agents/implementer.md` → (empty after restore).

**Raw green (holder leg):**
```
.                                                                        [100%]
1 passed in 0.99s
```

## FA-2 — test-reviewer.md row (home and holder legs)

**Guarded element:** `_CLAUSE_ROWS` FA-2 row — axis: literal-pin doctrine sentence count in home § *The obligation* and holder § *What to Flag*.

**Neutralization (home leg):** deleted the sentence `A proof that reaches the constant through the symbol that defines it stays green under *any* value, so it proves the plumbing and nothing about the contract (PR #1159, vet 181; same shape at PR #1156, vet 178).` from `rubric/bite-proof.md` § *The obligation*.

**Raw red (home leg):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_clause_present_in_home_and_copy_holder[FA-2] _______________

row = {'copy_holder': 'agents/test-reviewer.md', 'copy_holder_section': '## What to Flag', 'holder_clause': '**literal-pin**...xternal-contract constant through the symbol that defines it rather than pinning the literal.', 'holder_count': 1, ...}

    @pytest.mark.parametrize("row", _CLAUSE_ROWS, ids=_ROW_IDS)
    def test_clause_present_in_home_and_copy_holder(row):
>       _check_clause_sync(row)

plugins/superheroes/lib/tests/test_bite_proof_doctrine.py:538: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
plugins/superheroes/lib/tests/test_bite_proof_doctrine.py:296: in _check_clause_sync
    check_clause(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

rel = 'rubric/bite-proof.md', section_heading = '## The obligation'
clause = 'A proof that reaches the constant through the symbol that defines it stays green under *any* value, so it proves the plumbing and nothing about the contract (PR #1159, vet 181; same shape at PR #1156, vet 178).'
expected_count = 1

E           AssertionError: rubric/bite-proof.md (section ## The obligation): clause 'A proof that reaches the constant through the symbol that defines it stays green under *any* value, so it proves the plumbing and nothing about the contract (PR #1159, vet 181; same shape at PR #1156, vet 178).' expected count 1, found 0 — re-sync the text or update the roster

plugins/superheroes/lib/tests/clause_guard.py:59: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_bite_proof_doctrine.py::test_clause_present_in_home_and_copy_holder[FA-2]
1 failed in 1.38s
```

**Restore (home leg):** re-inserted the deleted sentence between the literal-pin paragraph and `A green run is equally consistent with`.

**Raw green (home leg):**
```
.                                                                        [100%]
1 passed in 1.10s
```

**Neutralization (holder leg):** in `agents/test-reviewer.md` § *What to Flag*, replaced the `literal-pin` bullet's naming sentence with `- **literal-pin** — The rule and its reason are stated once` (removed `a diff-added test that reaches an external-contract constant through the symbol that defines it rather than pinning the literal.`).

**Raw red (holder leg):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_clause_present_in_home_and_copy_holder[FA-2] _______________

rel = 'agents/test-reviewer.md', section_heading = '## What to Flag'
clause = '**literal-pin** — a diff-added test that reaches an external-contract constant through the symbol that defines it rather than pinning the literal.'
expected_count = 1

E           AssertionError: agents/test-reviewer.md (section ## What to Flag): clause '**literal-pin** — a diff-added test that reaches an external-contract constant through the symbol that defines it rather than pinning the literal.' expected count 1, found 0 — re-sync the text or update the roster

plugins/superheroes/lib/tests/clause_guard.py:59: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_bite_proof_doctrine.py::test_clause_present_in_home_and_copy_holder[FA-2]
1 failed in 1.74s
```

**Restore (holder leg):** restored the two-line `literal-pin` naming sentence verbatim.

**Raw green (holder leg):**
```
.                                                                        [100%]
1 passed in 1.30s
```

## FA-3 — partial-apply doctrine row (home-only)

**Guarded element:** `_CLAUSE_ROWS` FA-3 row (`home_only: True`) — axis: multi-replacement partial-apply paragraph count in `rubric/bite-proof.md` § *The record*.

**Neutralization:** deleted the three-sentence paragraph beginning `A neutralization that applies more than one replacement must assert that each replacement landed…` through `An aggregate-only assertion is the rejected shape.`

**Raw red:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_clause_present_in_home_and_copy_holder[FA-3] _______________

rel = 'rubric/bite-proof.md', section_heading = '## The record'
clause = "A neutralization that applies more than one replacement must assert that each replacement landed, never that the aggr...r goes red for the other replacement's sake — the proof is vacuous. An aggregate-only assertion is the rejected shape."
expected_count = 1

E           AssertionError: rubric/bite-proof.md (section ## The record): clause "A neutralization that applies more than one replacement must assert that each replacement landed, never that the aggregate diff is merely non-empty. When one anchor drifts, a single replacement can silently no-op while the aggregate diff still looks applied and the detector goes red for the other replacement's sake — the proof is vacuous. An aggregate-only assertion is the rejected shape." expected count 1, found 0 — re-sync the text or update the roster

plugins/superheroes/lib/tests/clause_guard.py:59: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_bite_proof_doctrine.py::test_clause_present_in_home_and_copy_holder[FA-3]
1 failed in 0.87s
```

**Restore:** re-inserted the three-sentence paragraph verbatim between the equivalence-class paragraph and `**Where each part lives, and why the split matters:**`.

**Restore receipt:** `git status --porcelain plugins/superheroes/rubric/bite-proof.md` → (empty after restore).

**Raw green:**
```
.                                                                        [100%]
1 passed in 0.87s
```

## FA-4 — count migration (`per guarded element` partial-drift representative)

**Guarded element:** `_CLAUSE_ROWS` row for `per guarded element` with `home_count: 2` — axis: under-count in `rubric/bite-proof.md` § *The record* must fail `check_clause` (partial drift invisible to the rejected presence-only shape).

**Neutralization:** changed `**One bite-proof entry, per guarded element, must contain:**` to `**One bite-proof entry must contain:**` (dropped exactly one of the two `per guarded element` occurrences in § *The record*).

**Raw red:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______ test_clause_present_in_home_and_copy_holder[per guarded element] _______

rel = 'rubric/bite-proof.md', section_heading = '## The record'
clause = 'per guarded element', expected_count = 2

E           AssertionError: rubric/bite-proof.md (section ## The record): clause 'per guarded element' expected count 2, found 1 — re-sync the text or update the roster

plugins/superheroes/lib/tests/clause_guard.py:59: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_bite_proof_doctrine.py::test_clause_present_in_home_and_copy_holder[per guarded element]
1 failed in 0.94s
```

**Contrast (not a shipped-detector run):** on pristine `rubric/bite-proof.md`, applied `drop_one_occurrence_in_section` in memory for `per guarded element` in § *The record*; the old presence-only check (`clause in section_text`) returned **True** while `count_clause_in_section` returned **1** (expected 2). Output:
```
After drop_one: count=1
Old presence-only (clause in section): True
New count check fails (count != 2): True
```

**Restore:** reverted to `**One bite-proof entry, per guarded element, must contain:**`.

**Restore receipt:** `git status --porcelain plugins/superheroes/rubric/bite-proof.md` → (empty after restore).

**Raw green:**
```
.                                                                        [100%]
1 passed in 0.76s
```
