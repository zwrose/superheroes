# WO-A layer 4c-3 bite-proof record (#1438)

Neutralizations are targeted edits in `round_certification.py`, reverted by inverse edit.

## B1 — closed-set disposition check (f)

**Status:** Unprovable as placed — neutralizing only `_new_issues_dispositioned` lets `test_e12_invalid_disposition_refuses` reach `check_disposition_without_receipt` first (`unknown disposition 'open'`), so the RED is not `refusal is None` and not `audited-chain-gap:new-issue-undispositioned`.

## B2 — `raisedRound >= fold_round` (c)+(d)

- **Neutralization:** `if False and raised_round < fold_round: return False`
- **RED:** `test_e8_stale_raised_round_refuses` → `assert refusal is not None` (`refusal = None`)
- **Restore:** remove `False and`
- **GREEN:** 1 passed (`/private/tmp/wo-a-1438/b2-green.txt`)

## B3 — representative key guard (e)

**Status:** Unprovable as placed — skipping `rep_key == fold_id` still leaves `test_e11_representative_key_equals_fold_id_refuses` refusing with `audited-chain-gap:new-issue-undispositioned` (representative row does not satisfy full reconciliation).

## B4 — post-raise `dispositionSeq` (f)

- **Neutralization:** `if False and disp_seq <= raised_seq: return False`
- **RED:** `test_e13_disposition_seq_not_after_raise_refuses` → `assert refusal is not None`
- **Restore:** remove `False and`
- **GREEN:** 1 passed (`/private/tmp/wo-a-1438/b4-green.txt`)

## B5 — every-quantifier / E14

- **Neutralization:** `return True` immediately after the first linked candidate completes the loop body
- **RED:** `test_e14_mixed_candidates_both_orders_refuse[pass-first]` → `assert refusal is not None`
- **Restore:** remove early `return True` in loop
- **GREEN:** 2 passed including case-8 (`/private/tmp/wo-a-1438/b5b6-green.txt` tail)

## B6 — `discharged-but-new-issue` acceptance at chokepoint

- **Neutralization:** `if ruling == "discharged-but-new-issue": return None` (skip predicate)
- **RED:** `test_case_8_new_issue_audit_refuses_then_certifies_once_dispositioned` → `assert receipt is None`
- **Restore:** reinstate `_new_issues_dispositioned` branch
- **GREEN:** 2 passed (`/private/tmp/wo-a-1438/b5b6-green.txt`)

Post-restore: `git diff --stat plugins/superheroes/lib/round_certification.py` shows only implementation lines (no neutralization).
