# WO-A (#1144) bite-proofs — decision-point census

## BP-1 — duplicate decision-point id (`test_fixture_duplicate_decision_block_id_fails`)

**Guarded element:** `_parse_decision_blocks` duplicate-id guard — axis: two blocks sharing an `id` must error.

**Neutralization:** changed the fixture's second block to `id=fixture-storage-b` (open and close), removing the duplicate.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_duplicate_decision_block_id_fails -q -p no:xdist
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_fixture_duplicate_decision_block_id_fails ________________
...
>       assert errors, "duplicate id fixture must produce well-formedness errors"
E       AssertionError: duplicate id fixture must produce well-formedness errors
E       assert []

plugins/superheroes/lib/tests/test_decision_point_census.py:574: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_duplicate_decision_block_id_fails
1 failed in 0.53s
```

**Restore:** reverted second block to `id=fixture-storage` for both open and close tags.

**Restore receipt:** second appended block again uses `id=fixture-storage` matching the duplicate under test.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.35s
```

---

## BP-2 — orphan close (`test_fixture_orphan_close_fails`)

**Guarded element:** orphan `<!-- /decision-point: id=… -->` without open — axis: unmatched close is an error.

**Neutralization:** replaced fixture body `"<!-- /decision-point: id=orphan -->\n"` with `"# empty\n"`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_orphan_close_fails -q -p no:xdist
```

**Red run:**
```
F                                                                        [100%]
...
>       assert any("orphan" in e for e in errors), "orphan close fixture must be reported"
E       AssertionError: orphan close fixture must be reported
E       assert False

plugins/superheroes/lib/tests/test_decision_point_census.py:589: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_orphan_close_fails
1 failed in 0.22s
```

**Restore:** restored `"<!-- /decision-point: id=orphan -->\n"` fixture line.

**Restore receipt:** orphan close literal back in `test_fixture_orphan_close_fails` write_text argument.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.21s
```

---

## BP-3 — waiting token inside block (`test_fixture_waiting_token_in_block_fails`)

**Guarded element:** `_WAITING_TOKENS` inside block prose — axis: no waiting token in any mode.

**Neutralization:** replaced `"and wait for the owner"` with `"after disclosure"` in the fixture gate prose string.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_waiting_token_in_block_fails -q -p no:xdist
```

**Red run:**
```
F                                                                        [100%]
...
>       assert hits, "waiting-token fixture must be detected inside block"
E       AssertionError: waiting-token fixture must be detected inside block
E       assert []

plugins/superheroes/lib/tests/test_decision_point_census.py:614: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_waiting_token_in_block_fails
1 failed in 0.08s
```

**Restore:** restored `"and wait for the owner"` in fixture prose.

**Restore receipt:** `GATE: write down, hand back, and wait for the owner. `/superheroes:configure`.`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.06s
```

---

## BP-4 — carrier transport structural checks (mutation proofs per carrier)

Replaces the prior fixture-only transport proof (empty block fails for unrelated reasons). Each proof
mutates the real shipped surface, runs `test_decision_block_carrier_transport` red, restores, green.

### BP-4a — `review-code-meta` (`skills/review-code/reference/setup.md`)

**Guarded element:** `_transport_violations` review-code-meta storage path — axis: fenced bash assigns
`SOURCE` from decide-location `.source`, usable-value guard covers `$SOURCE`, block prose names
`storageMode`/`storageSource`/`storageProvisional`.

**Neutralization:** deleted the capture line
`  SOURCE=$(printf '%s' "$DEC" | jq -r '.source')`

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_decision_block_carrier_transport -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____________________ test_decision_block_carrier_transport _____________________
...
E   AssertionError: #1144 decision block carrier transport violations. Every hit:
E     skills/review-code/reference/setup.md:150: review-code-meta requires SOURCE assignment from decide-location .source in block bash
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_decision_block_carrier_transport
1 failed in 0.05s
```

**Restore:** reinserted `  SOURCE=$(printf '%s' "$DEC" | jq -r '.source')`

**Restore receipt:** capture line quoted above restored verbatim in `setup.md` fenced bash.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

### BP-4b — `review-crew-layer` (`skills/review-init/SKILL.md` Step 4b pipe)

**Guarded element:** `_transport_violations` review-crew-layer — axis: `$REVIEW_LAYER_BODY` co-defines
`## Setup disclosures` and is piped to `write-layer --hero review-crew`.

**Neutralization:** changed Step 4b pipe target from `write-layer --hero review-crew` to
`write-layer --hero guardian`.

**Command:** same as BP-4a.

**Red run (exit 1):**
```
F                                                                        [100%]
...
E     skills/review-init/SKILL.md:78: review-crew-layer requires $REVIEW_LAYER_BODY piped to write-layer --hero review-crew
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_decision_block_carrier_transport
1 failed in 0.05s
```

**Restore:** reverted pipe hero to `review-crew`.

**Restore receipt:** `write-layer --hero review-crew --status "$STATUS" --rubric-version "$RUBRIC_VERSION"` restored on the `$REVIEW_LAYER_BODY` pipe line.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

### BP-4c — `test-pilot-layer` (`skills/configure/reference/set-up.md` pipe)

**Guarded element:** `_transport_violations` test-pilot-layer — axis: `$TEST_PILOT_LAYER_BODY` piped
to `write-layer --hero test-pilot`.

**Neutralization:** changed the set-up pipe hero from `test-pilot` to `guardian` on the
`$TEST_PILOT_LAYER_BODY` printf line.

**Command:** same as BP-4a.

**Red run (exit 1):**
```
F                                                                        [100%]
...
E     skills/configure/reference/set-up.md:98: test-pilot-layer requires $TEST_PILOT_LAYER_BODY piped to write-layer --hero test-pilot
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_decision_block_carrier_transport
1 failed in 0.06s
```

**Restore:** reverted pipe hero to `test-pilot`.

**Restore receipt:** `write-layer --hero test-pilot --status provisional` restored on the `$TEST_PILOT_LAYER_BODY` pipe line.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

### BP-4d — `review-spec-receipt` (`skills/review-spec/SKILL.md` disclosure line)

**Guarded element:** `_transport_violations` review-spec-receipt — axis: **Disclosure.** clause for
`receipt.md` names mode, source, and provisional.

**Neutralization:** removed `source,` from the disclosure field list (`mode, source, provisional` →
`mode, provisional`).

**Command:** same as BP-4a.

**Red run (exit 1):**
```
F                                                                        [100%]
...
E     skills/review-spec/SKILL.md:125: review-spec-receipt assembly must name mode, source, and provisional in receipt.md disclosure
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_decision_block_carrier_transport
1 failed in 0.06s
```

**Restore:** reinserted `source,` in the disclosure field list.

**Restore receipt:** `mode, source, provisional` restored in the `**Disclosure.**` receipt.md line.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

### BP-4e — `audit-report` (`skills/audit-debt/SKILL.md` disclosure clause)

**Guarded element:** `_transport_violations` audit-report — axis: post-**Disclosure.** clause for
`report.md` names mode, source, and provisional.

**Neutralization:** removed `its source,` from the report disclosure clause (`the storage mode taken,
its source, whether it is provisional` → `the storage mode taken, whether it is provisional`).

**Command:** same as BP-4a.

**Red run (exit 1):**
```
F                                                                        [100%]
...
E     skills/audit-debt/SKILL.md:96: audit-report assembly must name mode, source, and provisional in report.md disclosure
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_decision_block_carrier_transport
1 failed in 0.06s
```

**Restore:** reinserted `its source,` in the disclosure clause.

**Restore receipt:** `the storage mode taken, its source, whether it is provisional` restored in the **Disclosure.** sentence.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

### BP-4f — `doc-policy-disclosures` (`skills/architect-init/SKILL.md` write_policy call)

**Guarded element:** `_transport_violations` doc-policy-disclosures — axis: `write_policy` call
carries `disclosures` key and block prose names that transport.

**Neutralization:** deleted the line `    'disclosures': [disclosure],` from the `write_policy` dict.

**Command:** same as BP-4a.

**Red run (exit 1):**
```
F                                                                        [100%]
...
E     skills/architect-init/SKILL.md:88: doc-policy-disclosures requires write_policy call with disclosures field
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_decision_block_carrier_transport
1 failed in 0.06s
```

**Restore:** reinserted `    'disclosures': [disclosure],`

**Restore receipt:** `'disclosures': [disclosure],` restored inside the `architect_config.write_policy` dict.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

---

## BP-5 — E3 empty skills walk (`test_fixture_empty_skills_walk_fails`)

**Guarded element:** empty `skills/` walk guard — axis: zero files walked must fail loudly.

**Neutralization:** replaced `with pytest.raises(AssertionError, match="zero files"):` guard with bare `assert count > 0` on an empty directory.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_empty_skills_walk_fails -q -p no:xdist
```

**Red run:**
```
F                                                                        [100%]
...
>       assert count > 0, "#1144 decision-point census found zero files under skills/"
E       AssertionError: #1144 decision-point census found zero files under skills/
E       assert 0 > 0

plugins/superheroes/lib/tests/test_decision_point_census.py:646: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_empty_skills_walk_fails
1 failed in 0.08s
```

**Restore:** restored `with pytest.raises(AssertionError, match="zero files"):` wrapper.

**Restore receipt:** empty walk again expects pytest.raises around the failing assert.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.05s
```

---

## BP-6 — prohibition inverted (`test_fixture_wrapped_primitive_passes_prohibition`)

**Guarded element:** `_scan_forbidden_violations` — axis: primitive inside a decision block is not a violation.

**Neutralization:** replaced wrapped fixture content (decision block + `decide-location)`) with bare `decide-location)` line only.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_wrapped_primitive_passes_prohibition -q -p no:xdist
```

**Red run (block removed — detector reports violation on wrapped test):**
```
F                                                                        [100%]
...
>       assert not violations, "wrapped decide-location) must not violate prohibition"
E       AssertionError: wrapped decide-location) must not violate prohibition
E       assert not ['skills/demo/SKILL.md:1: decide-location)']

plugins/superheroes/lib/tests/test_decision_point_census.py:664: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_wrapped_primitive_passes_prohibition
1 failed in 0.10s
```

**Restore:** restored full decision-block fixture wrapping `decide-location)`.

**Restore receipt:** fixture content again includes decision-point open/close tags around the primitive.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.07s
```

---

## BP-7 — normalization false-positive (`test_fixture_disclosure_mention_no_violation`)

**Guarded element:** `_normalize_match_text` — axis: backtick-delimited mentions must not match forbidden `decide-location)` invocations.

**Neutralization:** reintroduced backtick stripping: `return text.replace("*", " ").replace("`", "")` in `_normalize_match_text`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_disclosure_mention_no_violation -q -p no:xdist
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_fixture_disclosure_mention_no_violation _________________
...
>       assert not violations, (
            "disclosure mention of decide-location must not violate prohibition"
        )
E       AssertionError: disclosure mention of decide-location must not violate prohibition
E       assert not ['.../skills/demo/SKILL.md:1: decide-location)']

plugins/superheroes/lib/tests/test_decision_point_census.py:710: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_disclosure_mention_no_violation
1 failed in 0.53s
```

**Restore:** removed `.replace("`", "")` — normalization strips `*` only (length-preserving: `* -> space`).

**Restore receipt:** `_normalize_match_text` returns `text.replace("*", " ")` only.

**Green run:**
```
..                                                                       [100%]
2 passed in 0.07s
```
(second test `test_fixture_real_invocation_violates` confirms bare invocation still hits)

---

## BP-8 — line attribution and dedup (`test_fixture_multiline_literal_start_line`)

**Guarded element:** `_scan_forbidden_violations` offset search — axis: each match attributed to its start line, deduplicated by `(path, line, literal)`.

**Neutralization:** restored per-line/joined-pair `_search_segments` loop without dedup (pre-WO-A2 behaviour).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_multiline_literal_start_line -q -p no:xdist
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________________ test_fixture_multiline_literal_start_line ___________________
...
>       assert len(violations) == 1, f"expected single hit, got {violations!r}"
E       AssertionError: expected single hit, got ['.../skills/demo/SKILL.md:1: only what they approve', '.../skills/demo/SKILL.md:2: only what they approve']
E       assert 2 == 1

plugins/superheroes/lib/tests/test_decision_point_census.py:765: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_multiline_literal_start_line
1 failed in 0.29s
```

**Restore:** reinstated whole-document offset search with `_match_search_text`, `_offset_to_line`, and `seen` keyed by `(rel, line_no, primitive)`.

**Restore receipt:** `_scan_forbidden_violations` uses `norm_text` + `search_text` + `line_starts` + dedup set; `_search_segments` removed.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.24s
```
