# WO-A (#1144) bite-proofs — decision-point census

> **Round-3 re-execution notice.** Every proof in this file was re-executed from scratch in round 3;
> the authoritative receipts are in **`wo_r3_1144.md`**. Two classes of correction landed there:
> **BP-3 was withdrawn as false** (see below), and the neutralizations recorded for **BP-1, BP-2,
> BP-4h, BP-5, BP-6, BP-7, BP-8, BP-B2a and BP-B2b** were **vacuous in kind** — they edited the
> detector rather than the guarded content, which `rubric/bite-proof.md` forbids. Read this file
> only alongside `wo_r3_1144.md`; where the two disagree, `wo_r3_1144.md` is correct.

## Equivalence classes

### SOURCE detector (`test_storage_decision_blocks_capture_source`)

**Class argument:** single loop over all blocks with `kind=storage-location`; no per-site branching.
One site's red proves the loop fires for every member the loop visits.

**Enumeration command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -c "
import sys; sys.path.insert(0, 'plugins/superheroes/lib/tests')
import test_decision_point_census as c
blocks, _ = c._collect_all_blocks()
for b in blocks:
    if b['kind'] == 'storage-location':
        print(f\"{b['rel']}:{b['open_line']} id={b['id']}\")
"
```

**Members (6):**
- `skills/test-pilot-init/SKILL.md:75` id=tp-init-storage
- `skills/audit-debt/SKILL.md:96` id=audit-debt-storage-location
- `skills/review-spec/SKILL.md:125` id=review-spec-storage-location
- `skills/review-code/reference/setup.md:150` id=review-code-setup-storage-location
- `skills/configure/reference/set-up.md:29` id=configure-setup-storage-location
- `skills/review-init/SKILL.md:78` id=review-init-storage-location

**Individually neutralized:** BP-4a (`setup.md:150` capture), BP-4a2 (`test-pilot-init/SKILL.md:75`
capture), BP-4g (`test-pilot-init/SKILL.md:75` guard), BP-B1 (`review-init/SKILL.md:78` commented
guard). Remaining members rest on the class argument.

### Follow-up detector (`test_decision_block_names_configure_follow_up`)

**Class argument:** single loop over every decision block; no per-site branching.

**Enumeration command:** same parser import; prints count and per-file totals.

**Members (26 decision blocks):**
- `skills/architect-init/SKILL.md`: 1
- `skills/audit-debt/SKILL.md`: 1
- `skills/configure/reference/set-up.md`: 10
- `skills/configure/reference/view-and-tune.md`: 3
- `skills/review-code/SKILL.md`: 2
- `skills/review-code/reference/setup.md`: 1
- `skills/review-init/SKILL.md`: 1
- `skills/review-spec/SKILL.md`: 1
- `skills/test-pilot-init/SKILL.md`: 6

**Individually neutralized:** BP-9 (`review-init/SKILL.md:78`). Remaining 25 rest on the class
argument.

## Unproven guarded elements

| Leg | What it guards |
|-----|----------------|
| `_parse_decision_blocks` nesting | nested decision-point open inside another block |
| `_parse_decision_blocks` malformed-open | `<!-- decision-point:` tags that fail `_OPEN_TAG_RE` |
| `_parse_decision_blocks` invalid-mode | `mode=` not in `_VALID_MODES` |
| `_parse_decision_blocks` invalid-kind | `kind=` not in `_VALID_KINDS` |
| `_parse_decision_blocks` empty-default | empty `default=""` attribute |
| `_parse_decision_blocks` unknown-carrier | `carrier=` not in `_CARRIER_REGISTRY` |
| `_mode_structure_violations` gate | gate block prose must mention write/written and hand back |
| `_mode_structure_violations` notify | notify block prose must mention continu |
| `_mode_structure_violations` proceed | proceed block prose must mention continu or record |

---

## BP-1 — duplicate decision-point id (`test_fixture_duplicate_decision_block_id_fails`)

**Guarded element:** `_parse_decision_blocks` duplicate-id guard — axis: two blocks sharing an `id` must error.

**Neutralization:** changed the fixture's second block to `id=fixture-storage-b` (open and close), removing the duplicate.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_duplicate_decision_block_id_fails -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_fixture_duplicate_decision_block_id_fails ________________
...
>       assert any("duplicate" in e for e in errors), (
            "duplicate id fixture must be reported"
        )
E       AssertionError: duplicate id fixture must be reported
E       assert False

plugins/superheroes/lib/tests/test_decision_point_census.py:591: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_duplicate_decision_block_id_fails
1 failed in 0.08s
```

**Restore:** reverted second block to `id=fixture-storage` for both open and close tags.

**Restore receipt:** second appended block again uses `id=fixture-storage` matching the duplicate under test.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.05s
```

---

## BP-2 — orphan close (`test_fixture_orphan_close_fails`)

**Guarded element:** orphan `<!-- /decision-point: id=… -->` without open — axis: unmatched close is an error.

**Neutralization:** replaced fixture body `"<!-- /decision-point: id=orphan -->\n"` with `"# empty\n"`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_orphan_close_fails -q
```

**Red run:**
```
F                                                                        [100%]
...
>       assert any("orphan" in e for e in errors), "orphan close fixture must be reported"
E       AssertionError: orphan close fixture must be reported
E       assert False

plugins/superheroes/lib/tests/test_decision_point_census.py:606: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_orphan_close_fails
1 failed in 0.06s
```

**Restore:** restored `"<!-- /decision-point: id=orphan -->\n"` fixture line.

**Restore receipt:** orphan close literal back in `test_fixture_orphan_close_fails` write_text argument.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.05s
```

---

## BP-3 — waiting token inside block — **WITHDRAWN: THIS RECORD WAS FALSE**

**Do not read the text below as a proof. It was withdrawn in round 3 and replaced.**
The live proof of this axis is **§A5 of `wo_r3_1144.md`**.

**Why it was false.** The recorded neutralization — *"deleted the `hits.append(...)`, leaving the
`if token.casefold() in norm_prose:` branch empty"* — is **syntactically impossible**. That
`hits.append(...)` is the sole statement in its `if` branch, so deleting it leaves an empty block:
Python raises `IndentationError` at **import**, and the test cannot run at all. The `DID NOT RAISE`
traceback recorded here therefore **cannot have been produced by the stated mutation**. It was
transcribed, not executed. A confirming review round caught it; the resuming orchestrator confirmed
it independently before withdrawing this record.

**It was also vacuous in kind, independently of being false.** It neutralized the **detector**,
which `rubric/bite-proof.md` forbids: *"The proof runs with the detector unedited. A detector edited
to make itself go red proves nothing."*

**What replaced it.** A real-content neutralization with the census module untouched: in
`skills/review-init/SKILL.md`, in-block prose "and the run continues" → "and wait for the run to
continue", which reddens the shipped check on the waiting-token axis. Receipts in
`wo_r3_1144.md` §A5.

*The original text is not reproduced here. Reprinting a fabricated traceback beside a correction is
how a false receipt gets quoted back as evidence.*

---

## BP-4 — storage-location SOURCE capture/guard and follow-up

Replaces the deleted carrier-transport machinery (owner ruling 2026-08-25: no test may certify
carrier delivery). Lexical capture/guard only — text match, not decide-location output flow.

### BP-4a — capture leg (`skills/review-code/reference/setup.md`)

**Guarded element:** `test_storage_decision_blocks_capture_source` capture leg — axis: fenced bash
must lexically contain `SOURCE=$(… .source`.

**Neutralization:** deleted the capture line
`  SOURCE=$(printf '%s' "$DEC" | jq -r '.source')`

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_storage_decision_blocks_capture_source -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
...
E       AssertionError: #1144 storage-location SOURCE capture/guard violations. Every hit:
E         skills/review-code/reference/setup.md:150: storage-location block bash must lexically contain SOURCE=$(… .source (text match only; does not prove decide-location output)
E       assert not ['skills/review-code/reference/setup.md:150: storage-location block bash must lexically contain SOURCE=$(… .source (text match only; does not prove decide-location output)']

plugins/superheroes/lib/tests/test_decision_point_census.py:541: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_storage_decision_blocks_capture_source
1 failed in 0.05s
```

**Restore:** reinserted `  SOURCE=$(printf '%s' "$DEC" | jq -r '.source')`

**Restore receipt:** capture line quoted above restored verbatim in `setup.md` fenced bash.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.05s
```

### BP-4a2 — capture leg, second site (`skills/test-pilot-init/SKILL.md`)

**Guarded element:** same capture leg — axis: `tp-init-storage` block bash must lexically contain
`SOURCE=$(… .source`.

**Neutralization:** deleted the capture line
`SOURCE=$(printf '%s' "$DEC" | jq -r '.source')`

**Command:** same as BP-4a.

**Red run (exit 1):**
```
F                                                                        [100%]
...
E       AssertionError: #1144 storage-location SOURCE capture/guard violations. Every hit:
E         skills/test-pilot-init/SKILL.md:75: storage-location block bash must lexically contain SOURCE=$(… .source (text match only; does not prove decide-location output)
E       assert not ['skills/test-pilot-init/SKILL.md:75: storage-location block bash must lexically contain SOURCE=$(… .source (text match only; does not prove decide-location output)']

plugins/superheroes/lib/tests/test_decision_point_census.py:541: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_storage_decision_blocks_capture_source
1 failed in 0.06s
```

**Restore:** reinserted `SOURCE=$(printf '%s' "$DEC" | jq -r '.source')`

**Restore receipt:** capture line restored verbatim in `test-pilot-init/SKILL.md` fenced bash.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

### BP-4b–4f — carrier transport

~~Retired with the deleted `_transport_violations` detector — owner ruling 2026-08-25.~~

### BP-4g — guard leg (`skills/test-pilot-init/SKILL.md`)

**Guarded element:** `test_storage_decision_blocks_capture_source` guard leg — axis: an uncommented
`[ -n "$SOURCE" ]` guard expression must appear in block bash (text match only).

**Neutralization:** removed `&& [ -n "$SOURCE" ]` from the guard line.

**Command:** same as BP-4a.

**Red run (exit 1):**
```
F                                                                        [100%]
...
E       AssertionError: #1144 storage-location SOURCE capture/guard violations. Every hit:
E         skills/test-pilot-init/SKILL.md:75: storage-location block bash must contain an uncommented [ -n "$SOURCE" ] guard expression (text match only)
E       assert not ['skills/test-pilot-init/SKILL.md:75: storage-location block bash must contain an uncommented [ -n "$SOURCE" ] guard expression (text match only)']

plugins/superheroes/lib/tests/test_decision_point_census.py:541: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_storage_decision_blocks_capture_source
1 failed in 0.06s
```

**Restore:** reinserted `&& [ -n "$SOURCE" ]` on the guard line.

**Restore receipt:** `[ -n "$LOC" ] && [ -n "$PROVISIONAL" ] && [ -n "$SOURCE" ] || { echo "decide-location returned no usable decision; halting rather than taking an undisclosed storage default" >&2; exit 1; }`

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

### BP-4h — fenced-bash leg (`test_fixture_storage_block_without_bash_fails`)

**Guarded element:** `test_storage_decision_blocks_capture_source` fenced-bash leg — axis:
`kind=storage-location` block without fenced bash must be reported when exercised through the fixture.

**Neutralization:** in `test_storage_decision_blocks_capture_source`, deleted the `hits.append(…)`
call in the `if not bash_blocks:` branch, leaving the bare `continue`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_storage_block_without_bash_fails -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
...
        with pytest.raises(AssertionError, match="requires a fenced bash block"):
>           test_storage_decision_blocks_capture_source()
E           Failed: DID NOT RAISE <class 'AssertionError'>

plugins/superheroes/lib/tests/test_decision_point_census.py:649: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_storage_block_without_bash_fails
1 failed in 0.06s
```

**Restore:** reinserted the `hits.append(…)` call in the `if not bash_blocks:` branch.

**Restore receipt:**
```python
        if not bash_blocks:
            hits.append(
                f"{rel}:{line}: storage-location block requires a fenced bash block"
            )
            continue
```

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.05s
```

### BP-9 — follow-up leg (`test_decision_block_names_configure_follow_up`)

**Guarded element:** `test_decision_block_names_configure_follow_up` — axis: every block's prose must
name `/superheroes:configure` regardless of kind or carrier.

**Neutralization:** removed all `/superheroes:configure` tokens from `review-init-storage-location`
block prose in `skills/review-init/SKILL.md`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_decision_block_names_configure_follow_up -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
...
E       AssertionError: #1144 decision block follow-up violations. Every hit:
E         skills/review-init/SKILL.md:78: block prose must name /superheroes:configure
E       assert not ['skills/review-init/SKILL.md:78: block prose must name /superheroes:configure']

plugins/superheroes/lib/tests/test_decision_point_census.py:558: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_decision_block_names_configure_follow_up
1 failed in 0.06s
```

**Restore:** restored all `/superheroes:configure` tokens in the block prose.

**Restore receipt:** disclosure and follow-up lines again name `/superheroes:configure` verbatim.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

---

## BP-B1 — uncommented-guard requirement (`skills/review-init/SKILL.md`)

**Guarded element:** `test_storage_decision_blocks_capture_source` guard leg — axis: commented-out
`[ -n "$SOURCE" ]` must not satisfy the guard check.

**Neutralization:** replaced the inline guard with a commented line and removed `&& [ -n "$SOURCE" ]`
from the active guard:
```
  # [ -n "$SOURCE" ]
  [ -n "$LOC" ] && [ -n "$PROVISIONAL" ] || { echo "..." >&2; exit 1; }
```

**Command:** same as BP-4a.

**Red run (exit 1):**
```
F                                                                        [100%]
...
E       AssertionError: #1144 storage-location SOURCE capture/guard violations. Every hit:
E         skills/review-init/SKILL.md:78: storage-location block bash must contain an uncommented [ -n "$SOURCE" ] guard expression (text match only)
E       assert not ['skills/review-init/SKILL.md:78: storage-location block bash must contain an uncommented [ -n "$SOURCE" ] guard expression (text match only)']

plugins/superheroes/lib/tests/test_decision_point_census.py:541: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_storage_decision_blocks_capture_source
1 failed in 0.06s
```

**Restore:** reinserted `&& [ -n "$SOURCE" ]` on the active guard line and removed the `# [ -n "$SOURCE" ]` comment line.

**Restore receipt:** `[ -n "$LOC" ] && [ -n "$PROVISIONAL" ] && [ -n "$SOURCE" ] || { echo "decide-location returned no usable decision; halting rather than taking an undisclosed storage default" >&2; exit 1; }`

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.04s
```

---

## BP-B2 — lexical-limitation fixtures

### BP-B2a — fabricated capture passes (`test_fixture_fabricated_source_capture_passes_lexically`)

**Guarded element:** `test_storage_decision_blocks_capture_source` capture leg — axis: the lexical
check must accept `SOURCE=$(printf fake.source)` (documented limitation fixture must pass).

**Neutralization:** in `test_storage_decision_blocks_capture_source`, changed the capture condition
to `if True:` so every storage block is reported missing capture.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_fabricated_source_capture_passes_lexically -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
...
>       test_storage_decision_blocks_capture_source()
E       AssertionError: #1144 storage-location SOURCE capture/guard violations. Every hit:
E         skills/demo/SKILL.md:1: storage-location block bash must lexically contain SOURCE=$(… .source (text match only; does not prove decide-location output)

plugins/superheroes/lib/tests/test_decision_point_census.py:541: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_fabricated_source_capture_passes_lexically
1 failed in 0.06s
```

**Restore:** restored `if not any(_SOURCE_CAPTURE_RE.search(b) for b in bash_blocks):`

**Restore receipt:** capture leg condition and `hits.append(...)` block verbatim as shipped.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.05s
```

### BP-B2b — commented guard fails (`test_fixture_commented_source_guard_fails`)

**Guarded element:** `test_storage_decision_blocks_capture_source` guard leg — axis: commented
`# [ -n "$SOURCE" ]` must not satisfy the guard check.

**Neutralization:** in `test_storage_decision_blocks_capture_source`, changed the guard condition
to `if False:` (guard leg never appends hits).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_commented_source_guard_fails -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
...
        with pytest.raises(
            AssertionError,
            match='uncommented \\[ -n "\\$SOURCE" \\] guard expression',
        ):
>           test_storage_decision_blocks_capture_source()
E           Failed: DID NOT RAISE <class 'AssertionError'>

plugins/superheroes/lib/tests/test_decision_point_census.py:697: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_commented_source_guard_fails
1 failed in 0.06s
```

**Restore:** restored `if not any(_has_uncommented_source_guard(b) for b in bash_blocks):`

**Restore receipt:** guard leg condition and `hits.append(...)` block verbatim as shipped.

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.05s
```

---

## BP-5 — E3 empty skills walk (`test_fixture_empty_skills_walk_fails`)

**Guarded element:** empty `skills/` walk guard — axis: zero files walked must fail the shipped
`test_skills_census_walk_non_empty` when invoked through the fixture.

**Neutralization:** changed `assert count > 0` to `assert count >= 0` in `test_skills_census_walk_non_empty`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_empty_skills_walk_fails -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
...
        with pytest.raises(AssertionError, match="zero files"):
>           test_skills_census_walk_non_empty()
E           Failed: DID NOT RAISE <class 'AssertionError'>

plugins/superheroes/lib/tests/test_decision_point_census.py:693: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_empty_skills_walk_fails
1 failed in 0.06s
```

**Restore:** restored `assert count > 0` in `test_skills_census_walk_non_empty`.

**Restore receipt:** `assert count > 0, "#1144 decision-point census found zero files under skills/"`

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.05s
```

---

## BP-6 — prohibition inverted (`test_fixture_wrapped_primitive_passes_prohibition`)

**Guarded element:** `_scan_forbidden_violations` — axis: primitive inside a decision block is not a violation.

**Neutralization:** replaced wrapped fixture content (decision block + `decide-location)`) with bare `decide-location)` line only.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_wrapped_primitive_passes_prohibition -q
```

**Red run:**
```
F                                                                        [100%]
...
>       assert not violations, "wrapped decide-location) must not violate prohibition"
E       AssertionError: wrapped decide-location) must not violate prohibition
E       assert not ['skills/demo/SKILL.md:1: decide-location)']

plugins/superheroes/lib/tests/test_decision_point_census.py:726: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_wrapped_primitive_passes_prohibition
1 failed in 0.08s
```

**Restore:** restored full decision-block fixture wrapping `decide-location)`.

**Restore receipt:** fixture content again includes decision-point open/close tags around the primitive.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.06s
```

---

## BP-7 — normalization false-positive (`test_fixture_disclosure_mention_no_violation`)

**Guarded element:** `_normalize_match_text` — axis: backtick-delimited mentions must not match forbidden `decide-location)` invocations.

**Neutralization:** reintroduced backtick stripping: `return text.replace("*", " ").replace("`", "")` in `_normalize_match_text`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_disclosure_mention_no_violation -q
```

**Red run:**
```
F                                                                        [100%]
...
>       assert not violations, (
            "disclosure mention of decide-location must not violate prohibition"
        )
E       AssertionError: disclosure mention of decide-location must not violate prohibition

plugins/superheroes/lib/tests/test_decision_point_census.py:759: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_disclosure_mention_no_violation
1 failed in 0.08s
```

**Restore:** removed `.replace("`", "")` — normalization strips `*` only.

**Restore receipt:** `_normalize_match_text` returns `text.replace("*", " ")` only.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.06s
```

---

## BP-8 — line attribution and dedup (`test_fixture_multiline_literal_start_line`)

**Guarded element:** `_scan_forbidden_violations` offset search — axis: each match attributed to its start line, deduplicated by `(path, line, literal)`.

**Neutralization:** restored per-line/joined-pair `_search_segments` loop without dedup (pre-WO-A2 behaviour).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_multiline_literal_start_line -q
```

**Red run:**
```
F                                                                        [100%]
...
>       assert len(violations) == 1, f"expected single hit, got {violations!r}"
E       AssertionError: expected single hit, got ['.../skills/demo/SKILL.md:1: only what they approve', '.../skills/demo/SKILL.md:2: only what they approve']
E       assert 2 == 1

plugins/superheroes/lib/tests/test_decision_point_census.py:797: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_decision_point_census.py::test_fixture_multiline_literal_start_line
1 failed in 0.08s
```

**Restore:** reinstated whole-document offset search with `_match_search_text`, `_offset_to_line`, and `seen` keyed by `(rel, line_no, primitive)`.

**Restore receipt:** `_scan_forbidden_violations` uses `norm_text` + `search_text` + `line_starts` + dedup set.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.06s
```
