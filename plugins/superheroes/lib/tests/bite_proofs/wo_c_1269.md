# WO-C (#1269) bite-proof — liveness cache TTL and cache provenance

**Provenance:** cursor-agent / composer-2.5

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-I1 | `effective_ttl` min(stored, configured) | a receipt written under 600s must not be served at age 900s when default is 3600 | `test_read_refuses_receipt_past_stored_shorter_ttl` |
| BP-I3 | `extend_notes_with_cache_provenance` on cache-hit dead notes | cached vendor-loss notes must disclose cache provenance in `reason` | `test_live_vendors_for_composition_cache_path_emits_cell_dead_notes` |

---

## BP-I1 — stored shorter TTL wins over raised default

- **axis:** a receipt written under 600s must not be served at age 900s when default is 3600

**neutralization** (`plugins/superheroes/lib/liveness_cache.py`, `effective_ttl`):
```python
    return configured  # bite-proof neutralization — ignores stored TTL
```
(replaces `return min(int(stored), configured)`)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_liveness_cache.py::test_read_refuses_receipt_past_stored_shorter_ttl -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_read_refuses_receipt_past_stored_shorter_ttl _______________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-119/test_read_refuses_receipt_past0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10256bf70>

    def test_read_refuses_receipt_past_stored_shorter_ttl(tmp_path, monkeypatch):
        # I1: a receipt written under 600s must not be served at age 900s when default is 3600.
        monkeypatch.delenv(lc._ENV_TTL, raising=False)
        path = str(tmp_path / "r.json")
        now = 10_000.0
        lc.write(_good_liveness(), _good_needed(), path=path, now=now - 900, ttl=600)
>       assert lc.read(path, now=now) is None
E       AssertionError: assert {'liveness': {'claude': {'cells': [], 'live': True, 'models': {}}, 'codex': {'cells': [{'detail': 'READY', 'effort': '...aude': [], 'codex': [['gpt-5.6-sol', 'medium'], ['gpt-5.6-terra', None]]}, 'probedAt': 9100.0, 'schemaVersion': 3, ...} is None
E        +  where {'liveness': {'claude': {'cells': [], 'live': True, 'models': {}}, 'codex': {'cells': [{'detail': 'READY', 'effort': '...aude': [], 'codex': [['gpt-5.6-sol', 'medium'], ['gpt-5.6-terra', None]]}, 'probedAt': 9100.0, 'schemaVersion': 3, ...} = <function read at 0x102a744c0>('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-119/test_read_refuses_receipt_past0/r.json', now=10000.0)
E        +    where <function read at 0x102a744c0> = lc.read

plugins/superheroes/lib/tests/test_liveness_cache.py:82: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_liveness_cache.py::test_read_refuses_receipt_past_stored_shorter_ttl
1 failed in 1.39s
```

**restore:**
```python
    return min(int(stored), configured)
```

**raw green:**
```
.                                                                        [100%]
1 passed in 1.01s
```

---

## BP-I3 — cached failure note carries provenance

- **axis:** cached vendor-loss notes must disclose cache provenance in `reason`

**neutralization** (`plugins/superheroes/lib/preflight_probe.py`, cache-hit branch):
```python
            # liveness_cache.extend_notes_with_cache_provenance(dead_notes, rec, now)  # bite-proof neutralization
            notes.extend(dead_notes)
```
(replaces the live `extend_notes_with_cache_provenance` call)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_preflight_probe.py::test_live_vendors_for_composition_cache_path_emits_cell_dead_notes -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_live_vendors_for_composition_cache_path_emits_cell_dead_notes ______

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-122/test_live_vendors_for_composit0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x101f8f430>

    def test_live_vendors_for_composition_cache_path_emits_cell_dead_notes(tmp_path, monkeypatch):
        ...
        assert any("timed out" in n["reason"] for n in cell_notes)
>       assert any("served from cache" in n["reason"] for n in cell_notes)
E       assert False
E        +  where False = any(<generator object test_live_vendors_for_composition_cache_path_emits_cell_dead_notes.<locals>.<genexpr> at 0x1020fb900>)

plugins/superheroes/lib/tests/test_preflight_probe.py:1613: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_preflight_probe.py::test_live_vendors_for_composition_cache_path_emits_cell_dead_notes
1 failed in 1.27s
```

**restore:**
```python
            liveness_cache.extend_notes_with_cache_provenance(dead_notes, rec, now)
            notes.extend(dead_notes)
```

**raw green:**
```
.                                                                        [100%]
1 passed in 1.19s
```

---

## Re-verification on the chokepoint head (2026-09-16, orchestrator)

- **BP-I1** — `effective_ttl` returning the configured value unconditionally turned
  `test_read_refuses_receipt_past_stored_shorter_ttl` red (`1 failed in 0.11s`); the inverse edit
  turned it green (`1 passed in 0.10s`).
- **BP-I3** — dropping the `extend_notes_with_cache_provenance` call on the cache-hit branch turned
  `test_live_vendors_for_composition_cache_path_emits_cell_dead_notes` red (`1 failed in 0.26s`); the
  inverse edit turned it green (`1 passed in 0.19s`). The call now goes through the module's public
  accessor; the element is unchanged.

Tree confirmed clean after every restore.
