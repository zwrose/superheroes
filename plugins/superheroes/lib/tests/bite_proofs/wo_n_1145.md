# WO-N (#1145) bite-proofs — body-only all-quantifier leg must exercise body handling

> **Superseded (2026-08-26).** BP1 neutralizes `_all_present_substance_fields_carry_sentinel` (body-key skip), which is deleted from live code. The owner-ruled #1145 scope amendment (2026-08-26) retired content-inspection echo legs for a per-dispatch nonce with near-copy placeholder equality — retired by design, not found broken. Replacement detectors are bite-proved in `wo_a_1145c3.md` and `wo_c_1145c3.md`. The guarded test was inverted: `test_member_carries_sentinel_true_for_body_only_quoting_sentinel` (body-only prose quoting the sentinel graded as echo — a confirmed Important defect) is now `test_member_carries_sentinel_false_for_body_only_quoting_sentinel` at `plugins/superheroes/lib/tests/test_review_findings_schema.py:383` (same payload parses clean); the inversion is the cure, not a regression. This record remains a valid receipt of the run it describes against the code as it stood at that commit; it is not withdrawn.

## BP1 — body-only quoting sentinel must echo via all-quantifier leg

**Guarded element:** `test_member_carries_sentinel_true_for_body_only_quoting_sentinel` — axis: all-quantifier leg when sole content-bearing substance field is body quoting example.

**Neutralization:** in `_all_present_substance_fields_carry_sentinel`, skipped the `body` key when building `present_values` (`plugins/superheroes/lib/review_findings_schema.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_body_only_quoting_sentinel -q
```

**Red run:**
```
=================================== FAILURES ===================================
_______ test_member_carries_sentinel_true_for_body_only_quoting_sentinel _______

>       assert RFS.member_carries_sentinel(
            {
                "severity": "Important",
                "body": "context:\n" + sentinel + " appears inside prose",
            }
        ) is True
E       AssertionError: assert False is True

plugins/superheroes/lib/tests/test_review_findings_schema.py:386: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_body_only_quoting_sentinel
1 failed in 0.15s
```

**Contrast (old prose-quoting fixture stays green under same neutralization):**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_false_for_title_plus_body_quoting_sentinel -q
```
```
1 passed in 0.13s
```

**Restore:** removed the `if key == "body": continue` skip from `_all_present_substance_fields_carry_sentinel`.

**Restore receipt:** `_all_present_substance_fields_carry_sentinel` again includes `body` when building `present_values` for the all-quantifier leg.

**Green run:**
```
1 passed in 0.13s
```
