# Bite-proof record — #1221 continuation order 2 (owner park ruling **b**)

**Head proved:** `4a6227c6` on `issue-1221-gate-guidance-to-fixer-order`, tree settled and committed
before the first probe. **Produced and run by the build orchestrator**, not by an implementer.

**Method, per element.** Neutralize the guarded element with a **targeted, revertible edit through
the host's edit action** — never a whole-file rewrite, never a shell edit, never a git discard —
run the **named** tests (exact node ids; never `-k`), record the runner's own exit code and the
failure list, restore with the **inverse edit**, re-run, and confirm `git status --porcelain` and
`git diff --stat` are both **empty** afterwards. Every red below ran with the **detector unedited**.

This record **supersedes the shape** proved in `wo_a_1221.md`: that record's G1–G9 were proved
against the row-carried guidance shape, which ruling **b** removed. The elements that survived the
re-keying are re-proved here on the new shape rather than carried over — that is the ruling's
item 4.

---

## N1 — the order's guidance comes from the fold-owned entries

**Guarded element.** `round_driver.py`, the `P_FIXER` arm of `_order_placeholders`:
`"GATE_GUIDANCE": _gate_guidance_block(guidance_entries)`.

**Neutralization.** `_gate_guidance_block(guidance_entries)` → `_gate_guidance_block([])`.

**Command.** `pytest test_order_input_contract.py::test_fixer_order_placeholders_include_guided_gate_block test_order_input_contract.py::test_row_carried_guidance_does_not_join_a_real_fold_entry test_round_driver.py::test_judgment_dispositions_guidance_survives_records_path_resume -q`

**RED — runner exit 1**

```
FAILED test_order_input_contract.py::test_fixer_order_placeholders_include_guided_gate_block
FAILED test_order_input_contract.py::test_row_carried_guidance_does_not_join_a_real_fold_entry
FAILED test_round_driver.py::test_judgment_dispositions_guidance_survives_records_path_resume
3 failed in 0.98s
```

**Restore** — inverse edit. **GREEN — runner exit 0:** `3 passed in 0.93s`. `git diff --stat` empty.

---

## N2 — the fold no longer stamps guidance onto a batch row

**Guarded element.** `_fold_judgment`'s `fix-with-guidance` arm writes the guidance to the
**disposition-log entry only**.

**Neutralization.** Re-add the removed row stamp: `g[GATE_GUIDANCE_RECORD_KEY] = guidance.strip()`
beside the entry write.

**Command.** `pytest test_order_input_contract.py::test_fold_judgment_writes_user_guidance_key -q`

**RED — runner exit 1**

```
plugins/superheroes/lib/tests/test_order_input_contract.py:117: AssertionError
FAILED test_order_input_contract.py::test_fold_judgment_writes_user_guidance_key
1 failed in 0.14s
```

**Restore** — inverse edit. **GREEN — runner exit 0:** `1 passed in 0.12s`. `git diff --stat` empty.

---

## N3 — an unattributed guidance record refuses the render

**Guarded element.** `_gate_guidance_entries`: a real guidance entry whose `id` is not a non-empty
string raises `order-render-refused:gate-guidance-unusable`.

**Neutralization.** `raise ValueError(...)` → `continue` (the silent-drop behaviour the refusal
replaces).

**Command.** `pytest test_order_input_contract.py::test_gate_guidance_entries_refuses_unusable_records -q`

**RED — runner exit 1**

```
FAILED ...::test_gate_guidance_entries_refuses_unusable_records[none-id]
FAILED ...::test_gate_guidance_entries_refuses_unusable_records[missing-id]
FAILED ...::test_gate_guidance_entries_refuses_unusable_records[non-string-id]
FAILED ...::test_gate_guidance_entries_refuses_unusable_records[blank-id]
4 failed, 1 passed in 0.15s
```

**Note the 1 passed:** the `duplicate-id` case stayed green, which is the evidence that N3 and N4
are **independently** guarded — neither dominates the other.

**Restore** — inverse edit. **GREEN** with N4 below.

---

## N4 — a duplicated guidance id refuses the render

**Guarded element.** `_gate_guidance_entries`: two real guidance entries sharing one stripped `id`
raise `order-render-refused:gate-guidance-unusable`.

**Neutralization.** `raise ValueError(...)` → `continue` (first-wins).

**Command.** same node id as N3.

**RED — runner exit 1**

```
FAILED ...::test_gate_guidance_entries_refuses_unusable_records[duplicate-id]
1 failed, 4 passed in 0.14s
```

Complementary to N3's red: each neutralization reddens exactly its own cases.

**Restore** — inverse edit. **GREEN — runner exit 0** for N3 + N4 together, with the
legitimate-shape guard alongside:
`pytest ...::test_gate_guidance_entries_refuses_unusable_records ...::test_fix_with_guidance_blank_entry_is_not_render_refusal -q` → `6 passed in 0.27s`. `git diff --stat` empty.

---

## N5 — a row-carried guidance key is disclosed, not swallowed

**Guarded element.** The `_record_round(state, _GATE_GUIDANCE_ROW_CARRIED_CHANNEL, unaccounted)`
write in the `P_FIXER` arm.

**Neutralization.** `if unaccounted:` → `if unaccounted and False:`.

**Command.** `pytest test_order_input_contract.py::test_gate_guidance_row_carried_disclosure_records_index_and_title_only -q`

**RED — runner exit 1**

```
FAILED ...::test_gate_guidance_row_carried_disclosure_records_index_and_title_only
1 failed in 0.26s
```

**Restore** — inverse edit. **GREEN** with N6 below.

---

## N6 — a render-only state records nothing and does not raise

**Guarded element.** The `if not render_only:` guard around the disclosure write.

**Neutralization.** `if not render_only:` → `if True:`.

**Command.** `pytest test_order_input_contract.py::test_gate_guidance_row_carried_render_only_state_records_nothing -q`

**RED — runner exit 1**, and note the failure is a real crash, not a soft assertion:

```
E       KeyError: 'rounds'
FAILED ...::test_gate_guidance_row_carried_render_only_state_records_nothing
1 failed in 0.30s
```

**Restore** — inverse edit. **GREEN — runner exit 0** for N5 + N6: `2 passed in 0.38s`.
`git diff --stat` empty.

---

## N7 — the emitted order never invites the fixer to obey a row field

**Guarded element.** The absence of the row-guidance instruction from every fixer-order copy
surface — the inverted drift pin. This is the **second half of the trust boundary**: the block can
be perfectly clean while the order itself tells the fixer to follow `userGuidance` off a row.

**Neutralization.** Re-introduce the removed line in `rubric/orders/dispatch-fixer.md`'s Input
bullet (`... suggestion, and optional / userGuidance)`).

**Command.** `pytest test_order_input_contract.py::test_fix_batch_guidance_key_absent_on_copy_surface test_round_orders.py::test_golden_render_matches_fixture -q`

**RED — runner exit 1**

```
FAILED ...::test_fix_batch_guidance_key_absent_on_copy_surface[rubric/orders/dispatch-fixer.md]
FAILED ...::test_golden_render_matches_fixture[dispatch-fixer]
2 failed, 8 passed in 0.20s
```

Two independent detectors bite: the copy-surface pin **and** the byte-level golden of the rendered
order.

**Restore** — inverse edit. **GREEN — runner exit 0:** `10 passed in 0.17s`. `git diff --stat` empty.

---

## N8 — `judgmentDispositions` has a receipt consumer that discloses something true

**Guarded element.** `build_receipt`'s fail-closed degradation line — the honest replacement for the
`if …: pass` fossil that used to satisfy the channel census vacuously.

**Neutralization.** `if fail_closed:` → `if fail_closed and False:`.

**Command.** `pytest test_round_driver.py::test_judgment_fail_closed_dispositions_degraded_on_receipt ::test_judgment_fail_closed_degraded_per_round ::test_judgment_fail_closed_degraded_omits_guidance_and_title ::test_judgment_valid_dispositions_not_degraded_on_receipt -q`

**RED — runner exit 1**

```
FAILED ...::test_judgment_fail_closed_dispositions_degraded_on_receipt
FAILED ...::test_judgment_fail_closed_degraded_per_round
FAILED ...::test_judgment_fail_closed_degraded_omits_guidance_and_title
3 failed, 1 passed in 0.71s
```

**Note the 1 passed:** `test_judgment_valid_dispositions_not_degraded_on_receipt` — the
no-fail-closed case — stays green under the neutralization, which is exactly right: it guards
against the consumer firing *unconditionally*, so a suppressed consumer must not redden it. That
asymmetry is the evidence the pair is not a single tautology.

**Restore** — inverse edit. **GREEN — runner exit 0:** `4 passed in 0.51s`. `git diff --stat` empty.

---

## N9 — the new channel is registered, not a local

**Guarded element.** `"gateGuidanceRowCarried": _dict_list` in `RESUMABLE_DISCLOSURE_CHANNELS`.

**Neutralization.** Remove that registry entry.

**Command.** `pytest test_round_driver.py test_round_driver_owner_artifact_vocabulary_sync.py -q -n auto`

**RED — runner exit 1**

```
FAILED ...::test_resume_restores_every_disclosure_channel_with_its_prose
FAILED ...::test_producer_emits_the_whole_declared_channel_set
FAILED ...::test_panel_round_channels_are_all_accounted_for
3 failed, 473 passed in 39.49s
```

**Restore** — inverse edit. **GREEN — runner exit 0** for those three plus the census:
`4 passed in 0.62s`. `git diff --stat` empty.

---

## N10 — the guidance survives a `recordsPath` resume (carried-forward axis, re-proved)

**Guarded element.** `"judgmentDispositions": _dict_list` in `RESUMABLE_DISCLOSURE_CHANNELS`. This
matters **more** on the new shape than it did on the old one: the record is now the guidance's
**only** copy, so losing the channel loses the owner's instruction outright rather than falling back
to a row.

**Neutralization.** Remove that registry entry.

**Command.** `pytest test_round_driver.py::test_judgment_dispositions_guidance_survives_records_path_resume test_round_driver_owner_artifact_vocabulary_sync.py -q`

**RED — runner exit 1**

```
FAILED ...::test_judgment_dispositions_guidance_survives_records_path_resume
1 failed, 13 passed in 0.63s
```

**Restore** — inverse edit. **GREEN — runner exit 0:** `14 passed in 0.65s`. `git diff --stat` empty.

---

## N11 — placeholder neutralization: measured dominance, proved against the minimal breaking set

**Guarded element.** The invariant that no interpolated field can inject or wedge the order
template's `{{PLACEHOLDER}}` grammar. It is enforced by **two mutually redundant mechanisms**:

- **(i)** the per-field escapes — `_escape_guidance_placeholder_syntax` on the title, the id label,
  and the truncated guidance body;
- **(ii)** the exit fixed-point loop — `while "{{" in result: result = _escape_guidance_placeholder_syntax(result)`.

**This element cannot be proved by a single-element probe, and this record says so rather than
reporting a misleading red.** The dominance was **measured on this shape**, not carried over from
`wo_a_1221.md`:

| Probe | Neutralized | Result | Reading |
| --- | --- | --- | --- |
| A | **(ii)** alone — exit loop removed | **8 passed**, exit 0 | (i) alone holds the invariant |
| B | **(i)** alone — all three per-field escapes removed | **8 passed**, exit 0 | (ii) alone holds the invariant |
| C | **(i) + (ii)** — the minimal breaking set | **7 failed, 1 passed**, exit 1 | the invariant is genuinely guarded |

Probes A and B are each **non-biting by construction**, and reporting either as a green "proof"
would be one of the four vacuity modes. Probe C is the honest proof.

**Commands** — the same eight exact node ids in all three probes:
`test_gate_guidance_round_placeholder_neutralized_in_rendered_order`,
`..._unknown_placeholder_does_not_wedge_render`,
`..._triple_brace_round_placeholder_neutralized_in_rendered_order`,
`..._long_odd_brace_run_neutralized_in_rendered_order`,
`..._title_placeholder_neutralized_in_rendered_order`,
`..._stamped_id_placeholder_neutralized_in_rendered_order`,
`..._all_fields_placeholder_neutralized_in_rendered_order`,
`..._markdown_in_guidance_stays_inside_delimiters`.

**RED (probe C) — runner exit 1**

```
FAILED ...::test_gate_guidance_round_placeholder_neutralized_in_rendered_order
FAILED ...::test_gate_guidance_unknown_placeholder_does_not_wedge_render
FAILED ...::test_gate_guidance_triple_brace_round_placeholder_neutralized_in_rendered_order
FAILED ...::test_gate_guidance_long_odd_brace_run_neutralized_in_rendered_order
FAILED ...::test_gate_guidance_title_placeholder_neutralized_in_rendered_order
FAILED ...::test_gate_guidance_stamped_id_placeholder_neutralized_in_rendered_order
FAILED ...::test_gate_guidance_all_fields_placeholder_neutralized_in_rendered_order
7 failed, 1 passed in 1.23s
```

The one survivor is `..._markdown_in_guidance_stays_inside_delimiters`, which pins delimiter
containment rather than placeholder syntax — correctly unaffected.

**Restore** — both inverse edits. **GREEN — runner exit 0:** `8 passed in 1.27s`.
`git diff --stat` and `git status --porcelain` both empty.

**Disclosure owed and stated:** probes A and B are recorded as **measured non-biting**, and the
invariant is proved against the minimal breaking set instead. This is the same limitation
`wo_a_1221.md` disclosed for its G7/G8 — re-measured on the new shape rather than assumed to carry.

---

## Coverage note — what this record does and does not claim

Every element the continuation **added or changed** is proved above. Elements this build did not
touch keep their proofs in `wo_a_1221.md`; where the re-keying could plausibly have resurrected a
prior rework axis, the axis is re-proved here (N10 for the resume channel, N11 for the escaping,
and N1/N2 for the id-minted-once shape, whose order-header id and record id are now the same field
of the same entry and therefore cannot disagree by construction).
