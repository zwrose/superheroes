# WO-I (#1145) bite-proofs — fallback enum order and drift pin

## BP1 — FIX-1: fallback-regime example severity is pinned

**Guarded element:** `test_example_findings_object_fallback_severity_is_pinned` — axis: fallback-regime example severity is deterministic and pinned to shipped order.

**Neutralization:** reordered the fallback severity enum list so `"Minor"` is first instead of `"Critical"` (`plugins/superheroes/lib/review_findings_schema.py`):

```python
_FALLBACK_FINDING_PROPERTY_SCHEMAS["severity"] = {
    "enum": ["Minor", "Critical", "Important", "Nit"]
}
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_example_findings_object_fallback_severity_is_pinned -q
```

**Red run:**
```
=================================== FAILURES ===================================
___________ test_example_findings_object_fallback_severity_is_pinned ___________

    def test_example_findings_object_fallback_severity_is_pinned(tmp_path):
        ...
        pinned_severity = _schema_severity_enum()[0]
        member = mod.example_findings_object()["findings"][0]
>       assert member["severity"] == pinned_severity
E       AssertionError: assert 'Minor' == 'Critical'
E         
E         - Critical
E         + Minor

FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_example_findings_object_fallback_severity_is_pinned
1 failed in 0.15s
```

**Restore:** `_FALLBACK_FINDING_PROPERTY_SCHEMAS["severity"] = {"enum": list(_FALLBACK_SEVERITY_ENUM)}`

**Green run:**
```
1 passed in 0.14s
```

---

## BP2 — FIX-2: ordered drift pin for fallback enum literals

**Guarded element:** `test_fallback_literals_match_shipped_schema` — axis: every fallback enum literal matches the shipped schema by order.

**Neutralization:** swapped the first two members of `_FALLBACK_DIMENSION_ENUM` (`"Architecture"` / `"Code"`) in `plugins/superheroes/lib/review_findings_schema.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_fallback_literals_match_shipped_schema -q
```

**Red run:**
```
=================================== FAILURES ===================================
_________________ test_fallback_literals_match_shipped_schema __________________

        for prop, shipped_enum in shipped_enum_props.items():
            ...
>           assert tuple(fallback_literal) == tuple(shipped_enum)
E           AssertionError: assert ('Code', 'Arc...Clarity', ...) == ('Architectur...Clarity', ...)
E             
E             At index 0 diff: 'Code' != 'Architecture'

FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_fallback_literals_match_shipped_schema
1 failed in 0.15s
```

**Restore:** reinstated `_FALLBACK_DIMENSION_ENUM` with `"Architecture"` before `"Code"`.

**Green run:**
```
1 passed in 0.13s
```

---

## BP3 — FIX-2: unmapped shipped enum fails closed

**Guarded element:** `test_fallback_literals_match_shipped_schema` census leg — axis: every shipped finding enum property has a fallback mapping entry.

**Neutralization:** removed `"confidence": "_FALLBACK_CONFIDENCE_ENUM"` from `_FALLBACK_ENUM_LITERALS_BY_PROPERTY` in `plugins/superheroes/lib/tests/test_review_findings_schema.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_fallback_literals_match_shipped_schema -q
```

**Red run:**
```
=================================== FAILURES ===================================
_________________ test_fallback_literals_match_shipped_schema __________________

>       assert set(_FALLBACK_ENUM_LITERALS_BY_PROPERTY.keys()) == set(shipped_enum_props.keys())
E       AssertionError: assert {'dimension', 'severity'} == {'confidence'...', 'severity'}
E         
E         Extra items in the right set:
E         'confidence'

FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_fallback_literals_match_shipped_schema
1 failed in 0.15s
```

**Restore:** reinstated `"confidence": "_FALLBACK_CONFIDENCE_ENUM"` in `_FALLBACK_ENUM_LITERALS_BY_PROPERTY`.

**Green run:**
```
1 passed in 0.21s
```
