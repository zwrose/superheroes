# WO-B bite-proofs (#1273 layer 4a-2)

**Rung line:** the chokepoint test retires only if the launcher stops launching background sessions.

| ID | Element | Neutralization | Red node / token | Green |
|----|---------|----------------|------------------|-------|
| B1 | pre-reservation unresolvable | comment out `requested is None` branch in `launch_build` | `test_wo_b_pre_reservation_unresolvable` → not `config-dir-unusable:unresolvable` | restore branch; test passes |
| B2 | pre-reservation not-a-directory | comment out absolute non-dir branch | `test_wo_b_pre_reservation_not_a_directory` | restore; passes |
| B3 | in-worktree deferral | apply `not os.path.isdir` inside worktree too | relative launch refuses early | restore deferral; `test_relative_config_dir_override_records_the_childs_effective_root` green |
| B4 | spawn-time recheck | skip `resolved_config_dir is None` in `_spawn_attempt` | spawn after flaky `spawn_config_dir` | restore; `test_wo_b_spawn_time` (if added) |
| B5–B12 | `_retire_lane` per failure branch | delete one `_retire_lane` call each | respective launch failure test lacks retire | restore call |
| B13 | never stop without handle | call `retire` when handle is None | invariant test | restore guard |
| B14 | stop-unconfirmed override | force `retire` to return unconfirmed always | `orphanedBackgroundId` missing | restore |
| B15 | started pid from handle | write ack pid in started | `test_wo_b_started_pid_is_handle_not_ack` | restore handle pid |
| B16 | census stale token | (no fix — WO-C) | `test_no_claude_argv_outside_engine_adapter` → `claude-argv-exception-stale:lib/launcher.py:compose_launch` | intentionally red until WO-C |
