# WO-C (#1154) bite-proofs — owner-authority-allowlist.md in SAFETY_MACHINERY

## BP-C1 — `test_safety_machinery_set_members_are_pinned`

**Guarded element:** `SAFETY_MACHINERY` tuple in `escalation.py` — axis: refusal set includes every owner-authority-gate-family member.

**Neutralization:** removed the new member line from `SAFETY_MACHINERY`:
```python
    "owner_authority_gate.py",# PreToolUse hook wrapper that invokes owner_authority
)
```
(deleted `"owner-authority-allowlist.md",  # named owner-authority-gate-family member — fixer must refuse or the carve-out is decorative on this member`)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_escalation.py::test_safety_machinery_set_members_are_pinned -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_safety_machinery_set_members_are_pinned _________________

    def test_safety_machinery_set_members_are_pinned():
        # The set is the single source of truth (§4 bound-2): the names whose edit could disable a
        # floor/gate/halt/escalation guarantee. Pin membership so the guard and the eval fixture
        # can't drift. escalation_resolve.py — the wrapper that OWNS the fail-closed verdict — is
        # included (review caught its omission: without it a fixer could neuter the guard).
>       assert set(ESC.SAFETY_MACHINERY) == {
            "escalation.py", "escalation_resolve.py", "loop_state.py", "circuit_breaker.py",
            "gate_write.py", "definition_doc.py",
            "model_tier.py", "model_registry.py",
            "engine_pref.py", "seat_map.py", "dispatch_guard.py", "engine_dispatch.py", "seat_canary.py",
            "engine_adapter.py",
            "hooks.json",
            "session_start.py",
            "escalation-base.md", "review-base.md",
            # shared review-and-fix loop (#104): deciders, durable record, and the orchestration shell
            "panel_tally.py", "loop_synthesis.py", "verification.py", "round_phases.py",
            "payload_contracts.py", "round_adapters.py", "verify_gate.py",
            "review_result.py", "round_driver.py", "audits.py", "delta_surface.py",
            # the two owner-named-risk gates + their hook wrappers (collector #695 item 55)
            "worktree_guard.py", "owner_authority.py",
            "worktree_guard_gate.py", "owner_authority_gate.py",
            "owner-authority-allowlist.md",  # named owner-authority-gate-family member — fixer must refuse or the carve-out is decorative on this member
        }
E       AssertionError: assert {'audits.py',...pter.py', ...} == {'audits.py',...pter.py', ...}
E         
E         Extra items in the right set:
E         'owner-authority-allowlist.md'
E         Use -v to get more diff

plugins/superheroes/lib/tests/test_escalation.py:104: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_escalation.py::test_safety_machinery_set_members_are_pinned
1 failed in 0.05s
```

**Restore:** re-added the member line to `SAFETY_MACHINERY`:
```python
    "owner_authority_gate.py",# PreToolUse hook wrapper that invokes owner_authority
    "owner-authority-allowlist.md",  # named owner-authority-gate-family member — fixer must refuse or the carve-out is decorative on this member
)
```

**Restore receipt (supplementary `git status --porcelain`):** ` M plugins/superheroes/lib/escalation.py`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.05s
```

---

## BP-C2 — `test_guard_fixture_covers_every_safety_member`

**Guarded element:** eval guard fixture refuse-case coverage — axis: every `SAFETY_MACHINERY` member has a refuse-case in `expected.json`.

**Neutralization:** removed the new refuse-case from the `guard` array:
```json
    {"path": "plugins/superheroes/hooks/owner_authority_gate.py", "allow": false},
    {"path": "plugins/superheroes/lib/decisions.py", "allow": true},
```
(deleted `{"path": "plugins/superheroes/reference/owner-authority-allowlist.md", "allow": false},`)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/eval/tests/test_escalation_eval.py::test_guard_fixture_covers_every_safety_member -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_guard_fixture_covers_every_safety_member _________________

    def test_guard_fixture_covers_every_safety_member():
        # Anti-drift: the eval fixture's refuse-cases must cover EVERY SAFETY_MACHINERY member, so a
        # future 10th member can't be pinned in the unit test yet silently uncovered by the eval gate.
        refused = {os.path.basename(c["path"]) for c in _fixture()["guard"] if c["allow"] is False}
        missing = set(ESC.SAFETY_MACHINERY) - refused
>       assert not missing, missing
E       AssertionError: {'owner-authority-allowlist.md'}
E       assert not {'owner-authority-allowlist.md'}

plugins/superheroes/eval/tests/test_escalation_eval.py:46: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/eval/tests/test_escalation_eval.py::test_guard_fixture_covers_every_safety_member
1 failed in 0.03s
```

**Restore:** re-added the refuse-case:
```json
    {"path": "plugins/superheroes/hooks/owner_authority_gate.py", "allow": false},
    {"path": "plugins/superheroes/reference/owner-authority-allowlist.md", "allow": false},
    {"path": "plugins/superheroes/lib/decisions.py", "allow": true},
```

**Restore receipt (supplementary `git status --porcelain`):** ` M plugins/superheroes/eval/escalation/expected.json`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.01s
```

---

## BP-C3 — `test_guard_fixture_exact_match`

**Guarded element:** eval guard fixture polarity for the real allowlist path — axis: `is_safety_machinery` genuinely refuses `reference/owner-authority-allowlist.md`, not merely tuple membership.

**Neutralization:** flipped the new case's `allow` to `true`:
```json
    {"path": "plugins/superheroes/reference/owner-authority-allowlist.md", "allow": true},
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/eval/tests/test_escalation_eval.py::test_guard_fixture_exact_match -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________________ test_guard_fixture_exact_match ________________________

    def test_guard_fixture_exact_match():
        band = [os.path.join(_REPO_ROOT, "plugins", "superheroes")]
        for case in _fixture()["guard"]:
            path = os.path.join(_REPO_ROOT, case["path"])
>           assert (not ESC.is_safety_machinery(path, band)) == case["allow"], case
E           AssertionError: {'allow': True, 'path': 'plugins/superheroes/reference/owner-authority-allowlist.md'}
E           assert not True == True
E            +  where True = <function is_safety_machinery at 0x101d055e0>('/Users/zwrose/.superheroes-worktrees/superheroes/1154-C/plugins/superheroes/reference/owner-authority-allowlist.md', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-C/plugins/superheroes'])
E            +    where <function is_safety_machinery at 0x101d055e0> = ESC.is_safety_machinery

plugins/superheroes/eval/tests/test_escalation_eval.py:39: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/eval/tests/test_escalation_eval.py::test_guard_fixture_exact_match
1 failed in 0.02s
```

**Restore:** restored `allow` to `false`:
```json
    {"path": "plugins/superheroes/reference/owner-authority-allowlist.md", "allow": false},
```

**Restore receipt (supplementary `git status --porcelain`):** ` M plugins/superheroes/eval/escalation/expected.json`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.01s
```
