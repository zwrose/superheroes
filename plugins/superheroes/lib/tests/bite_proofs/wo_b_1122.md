# WO-B (#1122) bite-proofs

## BP1 — `_worktree_dirt_verdict` HEAD comparison

**Guarded element:** `engine_dispatch.py:_worktree_dirt_verdict` (entriesVersion block) — axis: **HEAD identity**.

**Neutralization:** deleted the HEAD mismatch branch:

```python
        if (head.stdout or "").strip() != baseline["headSha"]:
            return True
```

**Raw red** (node `test_worktree_dirt_head_movement_reads_dirty`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_head_movement_reads_dirty
...
>       assert ED._worktree_dirt_verdict(baseline, cwd, timeout=5) is True
E       AssertionError: assert False is True
plugins/superheroes/lib/tests/test_engine_dispatch.py:5950: AssertionError
1 failed in 1.61s
```

**Restore:** inverse edit — restored the `if (head.stdout or "").strip() != baseline["headSha"]: return True` branch.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` empty after restore.

**Raw green:**

```
.                                                                        [100%]
1 passed in 2.24s
```

## BP2 — `_worktree_dirt_verdict` entry-set comparison

**Guarded element:** `engine_dispatch.py:_worktree_dirt_verdict` (entriesVersion block) — axis: **tree content delta**.

**Neutralization:** replaced the final entry-set comparison with unconditional clean:

```python
        return False
```

(in place of `return sorted(before) != sorted(after)`.)

**Raw red** (node `test_worktree_dirt_symlink_to_leased_root_reads_dirty`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_symlink_to_leased_root_reads_dirty
...
>           assert ED._worktree_dirt_verdict(baseline, cwd, timeout=5) is True
E           AssertionError: assert False is True
plugins/superheroes/lib/tests/test_engine_dispatch.py:5885: AssertionError
1 failed in 1.98s
```

**Restore:** inverse edit — restored `return sorted(before) != sorted(after)`.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` empty after restore.

**Raw green:**

```
.                                                                        [100%]
1 passed in 3.56s
```

## BP3 — `_worktree_dirt_verdict` overflow guard

**Guarded element:** `engine_dispatch.py:_worktree_dirt_verdict` (entriesVersion block) — axis: **unverifiable input ⇒ refuse**.

**Neutralization:** deleted the overflow fail-closed branch:

```python
        if baseline.get("entriesOverflow"):
            return None
```

**Raw red** (node `test_worktree_dirt_verdict_overflow_fail_closed`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_verdict_overflow_fail_closed
...
>       assert ED._worktree_dirt_verdict(baseline, cwd, timeout=5) is None
E       AssertionError: assert False is None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5904: AssertionError
1 failed in 1.44s
```

**Restore:** inverse edit — restored the `entriesOverflow` guard.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` empty after restore.

**Raw green:**

```
.                                                                        [100%]
1 passed in 1.27s
```

## BP4 — `_path_under_excluded_root` membership via `_filter_entry_list`

**Guarded element:** `engine_dispatch.py:_filter_entry_list` call site — axis: **path shape, not resolved target**.

**Neutralization:** changed `os.path.normpath` to `os.path.realpath` in `_filter_entry_list`:

```python
        entry_abs = os.path.realpath(os.path.join(cwd_real, rel))
```

**Raw red** (node `test_worktree_dirt_symlink_to_leased_root_reads_dirty`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_symlink_to_leased_root_reads_dirty
...
>           assert ED._worktree_dirt_verdict(baseline, cwd, timeout=5) is True
E           AssertionError: assert False is True
plugins/superheroes/lib/tests/test_engine_dispatch.py:5885: AssertionError
1 failed in 1.71s
```

**Restore:** inverse edit — restored `os.path.normpath` in `_filter_entry_list`.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` empty after restore.

**Raw green:**

```
.                                                                        [100%]
1 passed in 3.56s
```

## BP5 — `_worktree_dirt_verdict` exclusion union

**Guarded element:** `engine_dispatch.py:_worktree_dirt_verdict` (entriesVersion block) — axis:
**leased at EITHER end** (the union of baseline-side and verdict-side leased roots).

**Detector:** `test_worktree_dirt_ancestor_collapse_real_git_porcelain`.

**Detector correction (disclosed).** The WO-B work order named
`test_worktree_dirt_exclusion_grows_between_snapshots` as this element's detector. That was an
order-authoring error: in that fixture the sibling worktree is present at *both* ends, so
removing the union filters both sides identically and the unchanged-tree verdict stays `False`.
The WO-B implementer applied the neutralization, observed no red, and reported the
failure-to-redden rather than papering over it. The axis is only observable where the leased
root exists at **one** end — which is the ancestor-collapse fixture, where the sibling worktree
is created after the baseline is taken. Re-proved below by the orchestrator against the correct
detector.

**Neutralization:** deleted the live-root union so only baseline-side roots are used:

```python
        if live is not None:
            roots |= live
```

**Raw red** (detector unedited):

```
        assert baseline is not None
        assert "?? .claude/\n" in plain or plain.strip() == "?? .claude/"
        assert ".claude/worktrees/sib/" in uall
        assert "?? .claude/\n" not in uall
>       assert ED._worktree_dirt_verdict(baseline, cwd, timeout=5) is False
E       AssertionError: assert True is False
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_ancestor_collapse_real_git_porcelain
1 failed in 0.76s
```

The red is on the claimed axis: with the union removed, the leased sibling that exists only at
the verdict end is no longer excluded, so the verdict-side entry set carries
`?? .claude/worktrees/sib/` that the baseline set does not, and the tree reads dirty.

**Restore:** inverse edit — restored `if live is not None: roots |= live`.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` empty
after restore.

**Raw green:**

```
1 passed in 0.70s
```

## BP6 — `_foreign_leased_worktree_roots` inside-cwd rule

**Guarded element:** `engine_dispatch.py:_foreign_leased_worktree_roots` — axis: **a root at or above cwd must never be an exclusion root**.

**Neutralization:** deleted the strictly-inside-cwd guard:

```python
        # Axis: a root at or above cwd must never be an exclusion root — it filters the whole tree to empty.
        if not wt_path.startswith(cwd_real + os.sep):
            continue
```

**Raw red** (node `test_worktree_dirt_ancestor_exclusion_root_not_admitted`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_ancestor_exclusion_root_not_admitted
...
>           assert main_real not in roots
E           AssertionError: assert '.../main' not in {'.../main'}
plugins/superheroes/lib/tests/test_engine_dispatch.py:5989: AssertionError
1 failed in 0.92s
```

**Restore:** inverse edit — restored the strictly-inside-cwd guard.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` shows only WO-D implementation diff (no bite-proof residue).

**Raw green:**

```
1 passed in 4.42s
```

## BP7 — `_filter_entry_list` all-paths rule

**Guarded element:** `engine_dispatch.py:_filter_entry_list` — axis: **a record is dropped only when EVERY endpoint is excluded**.

**Detector:** `test_worktree_dirt_rename_out_of_excluded_root_reads_dirty`.

**Detector correction (disclosed).** WO-D's order named the neutralization "match on the first
path only". That cannot discriminate this axis: under `-z` a rename record lists its
**destination first**, so for `git mv leased/secret.txt escaped.txt` the first endpoint is
`escaped.txt` — already outside the excluded root — and first-path matching keeps the record,
leaving the detector green. The WO-D implementer applied it, observed no red, and reported the
failure-to-redden rather than papering over it. The neutralization that does discriminate is the
**inverse of the rule**: exclude when **ANY** endpoint is excluded rather than when **ALL** are.
Re-proved below by the orchestrator.

**Neutralization:** inverted the all-endpoints test to any-endpoint:

```python
        all_excluded = False
        for rel in paths:
            entry_abs = os.path.normpath(os.path.join(cwd_real, rel))
            if _path_under_excluded_root(entry_abs, excluded_roots):
                all_excluded = True
                break
```

**Raw red** (detector unedited):

```
E       AssertionError: assert False is True
E        +  where False = <function _worktree_dirt_verdict at 0x106c68670>({'entries': [], 'entriesOverflow': False, 'entriesVersion': 2, 'excludedRoots': ['<redacted-tmp>/wt/leased'], ...}, '<redacted-tmp>/wt', timeout=5)
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_rename_out_of_excluded_root_reads_dirty
1 failed in 0.59s
```

The red is on the claimed axis: with any-endpoint exclusion, the rename that moves a file OUT of
the leased root is discarded, both sides compare equal, and a genuinely dirtied tree reads clean.
(Absolute pytest tmp paths redacted to `<redacted-tmp>`; nothing else elided.)

**Restore:** inverse edit — restored the all-endpoints loop.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` empty
after restore.

**Raw green:**

```
1 passed in 0.70s
```

## BP8 — retained `mode` branch of `_worktree_dirt_verdict`

**Guarded element:** `engine_dispatch.py:_worktree_dirt_verdict` (mode branch) — axis: **a `mode` baseline is still graded, not ignored**.

**Neutralization:** replaced the mode branch body with unconditional clean:

```python
    if mode not in ("plain", "filtered"):
        return None
    return False
```

**Raw red** (node `test_mode_worktree_baseline_dirtied_tree_reads_dirty[plain]`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_mode_worktree_baseline_dirtied_tree_reads_dirty[plain]
...
>       assert ED._worktree_dirt_verdict(baseline, cwd, timeout=5) is True
E       AssertionError: assert False is True
plugins/superheroes/lib/tests/test_engine_dispatch.py:6232: AssertionError
1 failed in 1.00s
```

**Restore:** inverse edit — restored the full mode-branch head/porcelain comparison block.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` empty after restore.

**Raw green:**

```
.                                                                        [100%]
1 passed in 2.48s
```

## BP9 — legacy digest comparison

**Guarded element:** `engine_dispatch.py:_worktree_dirt_verdict` (legacy branch) — axis: **the persisted digest is actually compared**.

**Neutralization:** deleted the legacy digest comparison:

```python
        if porcelain_sha256 != baseline.get("porcelainSha256"):
            return True
```

**Raw red** (node `test_legacy_worktree_baseline_unchanged_tree_not_dirty`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_legacy_worktree_baseline_unchanged_tree_not_dirty
...
>       assert ED._worktree_dirt_verdict(legacy_wrong, cwd, timeout=5) is True
E       AssertionError: assert False is True
plugins/superheroes/lib/tests/test_engine_dispatch.py:5748: AssertionError
1 failed in 1.16s
```

**Restore:** inverse edit — restored the `porcelainSha256` comparison line.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` empty after restore.

**Raw green:**

```
.                                                                        [100%]
1 passed in 1.80s
```

## BP10 — injective record representation

**Guarded element:** `engine_dispatch.py:_filter_entry_list` — axis: **no in-band delimiter; two different records never collide**.

**Neutralization:** reassembled list records as delimiter strings and split them back in `_filter_entry_list`:

```python
        if isinstance(record, list) and len(record) >= 1:
            delim_record = record[0] + "\x1f" + "\x1f".join(record[1:])
            parts = delim_record.split("\x1f")
            if len(parts) < 2:
                kept.append(record)
                continue
            paths = parts[1:]
        else:
            kept.append(record)
            continue
```

(in place of `paths = record[1:]` with empty-path keep.)

**Raw red** (node `test_worktree_dirt_unit_separator_filename_outside_excluded_root_reads_dirty`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_unit_separator_filename_outside_excluded_root_reads_dirty
...
>       assert ED._worktree_dirt_verdict(baseline, cwd, timeout=5) is True
E       AssertionError: assert False is True
plugins/superheroes/lib/tests/test_engine_dispatch.py:6251: AssertionError
1 failed in 0.86s
```

**Restore:** inverse edit — restored `paths = record[1:]` with empty-path keep.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` shows only WO-G implementation diff (no bite-proof residue).

**Raw green:**

```
1 passed in 0.86s
```

## BP11 — consumer strict-inside-cwd root guard

**Guarded element:** `engine_dispatch.py:_worktree_dirt_verdict` (entriesVersion block) — axis: **a persisted root outside cwd cannot suppress the tree**.

**Neutralization:** deleted the consumer-side filter:

```python
        # Axis: the strict-inside-cwd invariant is enforced where it is load-bearing, because persisted roots arrive from disk and are not trustworthy.
        roots = {
            r for r in roots
            if isinstance(r, str) and r.startswith(cwd_real + os.sep)
        }
```

**Raw red** (node `test_worktree_dirt_consumer_root_guard_bad_persisted_root_reads_dirty`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_consumer_root_guard_bad_persisted_root_reads_dirty[cwd]
...
>       assert ED._worktree_dirt_verdict(baseline, cwd, timeout=5) is True
E       AssertionError: assert False is True
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_consumer_root_guard_bad_persisted_root_reads_dirty[empty]
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirt_consumer_root_guard_bad_persisted_root_reads_dirty[ancestor]
3 failed, 1 passed in 3.03s
```

**Restore:** inverse edit — restored the consumer-side strict-inside-cwd filter.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` shows only WO-G implementation diff (no bite-proof residue).

**Raw green:**

```
4 passed in 3.03s
```

## BP12 — decode fail-closed guard

**Guarded element:** `engine_dispatch.py:_worktree_entry_set` — axis: **undecodable output refuses rather than raising**.

**Neutralization:** removed `UnicodeDecodeError` from the except clause:

```python
    except subprocess.TimeoutExpired:
```

**Raw red** (node `test_worktree_entry_set_unicode_decode_error_fail_closed`):

```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_entry_set_unicode_decode_error_fail_closed
...
>       assert ED._worktree_entry_set(cwd, timeout=5) is None
E       UnicodeDecodeError: 'utf-8' codec can't decode bytes in position 0-0: invalid
plugins/superheroes/lib/tests/test_engine_dispatch.py:6335: UnicodeDecodeError
1 failed in 0.88s
```

**Restore:** inverse edit — restored `except (subprocess.TimeoutExpired, UnicodeDecodeError):`.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` shows only WO-G implementation diff (no bite-proof residue).

**Raw green:**

```
1 passed in 0.88s
```

