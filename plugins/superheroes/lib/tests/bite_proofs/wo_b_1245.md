# WO-B-1245 — `model_family` `None`-tolerance census bite-proofs

Detector: `plugins/superheroes/lib/tests/test_model_family_none_tolerance_census.py`.

**Provenance, disclosed.** The census file itself was implemented by the WO-B-1245 dispatch (cursor,
`composer-2.5`, maker family `xai`). That dispatch returned a **terminal forfeit**
(`worktree-dirtied-by-attempt`): its transport could not carry a gradeable report, and this record
file was among its declared items that never landed. The census file was reconstructed from the
engine's own worktree rather than retried, and **every proof below was run by the orchestrator on the
final integrated head** (`ab07a7e0`, WO-A + WO-B both landed) — not carried over from the forfeited
run. Each neutralization was a targeted, revertible edit applied through the host edit action and
reverted before the next proof; `git status --porcelain` came back empty after every restore, and
each restore was followed by its own green run.

**Command (all runs):**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_model_family_none_tolerance_census.py -q -p no:randomly
```

---

## BP1 — Part 1, in-place `None`-tolerant guard on a `model_family` call

**Guarded element:** `test_model_family_none_tolerance_census`'s violation assertion — axis: a
`None`-tolerant construct consuming a `model_family` call directly is detected.

**Neutralization:** in `plugins/superheroes/lib/model_registry.py`, `family_for`'s final line
`return model_family(vendor, model_id)` → `return model_family(vendor, model_id) or None` (pattern P1).

**Red run (exit 1):**
```
.F..                                                                     [100%]
=================================== FAILURES ===================================
___________________ test_model_family_none_tolerance_census ____________________
...
        violations = result["violations"]
>       assert not violations, (
            "None-tolerant guard on shipped model_family call:\n"
            + "\n".join("  %s" % v["detail"] for v in violations)
        )
E       AssertionError: None-tolerant guard on shipped model_family call:
E           model_registry.py:559 pattern P1
E       assert not [{'detail': 'model_registry.py:559 pattern P1', 'func': 'family_for', 'lineno': 559, 'pattern': 'P1'}]

plugins/superheroes/lib/tests/test_model_family_none_tolerance_census.py:418: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_model_family_none_tolerance_census.py::test_model_family_none_tolerance_census
1 failed, 3 passed in 7.55s
```

**Restore:** inverse edit — `or None` removed; `family_for` ends `return model_family(vendor, model_id)`.

**Restore receipt:** `git status --porcelain` empty.

**Green run (exit 0):**
```
....                                                                     [100%]
4 passed in 34.91s
```

---

## BP2 — Part 1, one level of local name binding (the motivating assign-then-guard shape)

**Guarded element:** `_analyze_scope`'s binding resolution — axis: a name bound to a `model_family`
call and guarded later in the same scope is detected exactly as an in-place guard is. This is the
shape the field defect actually wore (`fam = family_for(...)` … `if fam and fam in excl:`), so it is
proved separately from BP1.

**Neutralization:** in `model_registry.py`, `family_for`'s tail replaced with
```python
    fam = model_family(vendor, model_id)
    if fam and fam in VENDORS:
        return None
    return fam
```
(pattern P2 on a bound name).

**Red run (exit 1):**
```
.F..                                                                     [100%]
=================================== FAILURES ===================================
___________________ test_model_family_none_tolerance_census ____________________
...
E       AssertionError: None-tolerant guard on shipped model_family call:
E           model_registry.py:560 pattern P2 bound name fam (bound at line 559)
E           model_registry.py:560 pattern P2 bound name fam (bound at line 559)

plugins/superheroes/lib/tests/test_model_family_none_tolerance_census.py:418: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_model_family_none_tolerance_census.py::test_model_family_none_tolerance_census
1 failed, 3 passed in 10.24s
```

**Restore:** inverse edit — the four lines replaced by `return model_family(vendor, model_id)`.

**Restore receipt:** `git status --porcelain` empty.

**Green run (exit 0):**
```
....                                                                     [100%]
4 passed in 30.96s
```

---

## BP3 — the non-vacuity guard

**Guarded element:** the empty-enumeration `pytest.fail` — axis: a census that finds nothing fails
saying so, rather than passing as clean. Neutralized **through the guarded surface, with the detector
unedited**: the symbol the census enumerates was renamed, so the enumeration goes empty for a real
reason.

**Neutralization:** in `model_registry.py`, `def model_family(...)` → `def _resolve_family(...)` and
`family_for`'s call renamed to match — zero shipped `model_family` call sites remain.

**Red run (exit 1):**
```
.F..                                                                     [100%]
=================================== FAILURES ===================================
___________________ test_model_family_none_tolerance_census ____________________

    def test_model_family_none_tolerance_census():
        result = _collect_census("model_family")
        call_sites = result["call_sites"]
        if not call_sites:
>           pytest.fail(
                "model_family census enumeration went empty — no shipped model_family call sites found"
            )
E           Failed: model_family census enumeration went empty — no shipped model_family call sites found

plugins/superheroes/lib/tests/test_model_family_none_tolerance_census.py:406: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_model_family_none_tolerance_census.py::test_model_family_none_tolerance_census
1 failed, 3 passed in 8.36s
```

**Restore:** both names restored to `model_family`.

**Restore receipt:** `git status --porcelain` empty; `git diff --stat` empty.

**Green run (exit 0):**
```
....                                                                     [100%]
4 passed in 7.37s
```

---

## BP4 — Part 2a, the `family_for` consumer-module enumeration

**Guarded element:** `test_family_for_consumer_modules_enumerated` — axis: a new shipped module
resolving a maker family cannot appear unreviewed.

**Neutralization:** appended to `plugins/superheroes/lib/build_lane.py` — a module outside the
declared `FAMILY_FOR_MODULES` set:
```python
def _wo_b_1245_strike():
    return family_for("reviewer", "claude")
```

**Red run (exit 1):**
```
..F.                                                                     [100%]
=================================== FAILURES ===================================
_________________ test_family_for_consumer_modules_enumerated __________________
...
E       AssertionError: family_for consumer module set drifted — update FAMILY_FOR_MODULES consciously; known non-Python consumer: plugins/superheroes/skills/review-code/reference/setup.md (shell python3 -c family_for(...) or '' for author-family-unresolved). expected ['review_loop_runner.py', 'round_driver.py', 'seat_map.py'] got ['build_lane.py', 'review_loop_runner.py', 'round_driver.py', 'seat_map.py']
E         Extra items in the left set:
E         'build_lane.py'

plugins/superheroes/lib/tests/test_model_family_none_tolerance_census.py:427: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_family_for_consumer_modules_enumerated
1 failed, 3 passed in 8.13s
```

**Restore:** inverse edit — `_wo_b_1245_strike` removed; `build_lane.py` ends at its
`if __name__ == "__main__":` block.

**Restore receipt:** `git status --porcelain` empty.

**Green run (exit 0):**
```
....                                                                     [100%]
4 passed in 4.86s
```

---

## Stated coverage limits

- **`test_family_for_allowlist_justifications_are_non_empty`** (edge F6) carries **no** bite-proof.
  Neutralizing it means writing an empty justification into the allowlist, which is an edit to the
  detector's own data rather than to a guarded surface — the proof would be circular. Disclosed as
  unproven rather than claimed.
- **`test_family_for_none_tolerance_allowlist_matches_live_sites`** is exercised indirectly: BP4's
  neutralization adds a consumer module and BP1/BP2 exercise the shared pattern matcher, but no
  dedicated red→green proof was recorded for the per-function allowlist drift assertion itself.
  Disclosed as unproven.
- The census is **AST-over-Python only**. The shell `python3 -c` consumer in
  `plugins/superheroes/skills/review-code/reference/setup.md` is structurally invisible to it; the
  failure message names that file so a reader is not left believing it is covered. Stated limitation,
  not a covered case.
