# WO-L4A2P-A (#1273) bite-proof — builder argv chokepoint

## BP-A1 — census guards compose_launch adapter use

- **guarded element:** `launcher.compose_launch` must not hand-build claude argv
- **axis:** census `test_no_claude_argv_in_enumerated_shapes_outside_engine_adapter`

**neutralization** (`launcher.py` `compose_launch`):
```diff
-    built = engine_adapter.claude_builder_argv(token, session_id, prompt)
-    if built.get("ok") is False or built.get("reason") is not None:
-        return _fail(built["reason"])
-    argv = built["argv"]
+    argv = ["claude", "--model", token, "--session-id", session_id, "-p", prompt]
```

**command:** `pytest plugins/superheroes/lib/tests/test_claude_argv_census.py::test_no_claude_argv_in_enumerated_shapes_outside_engine_adapter -q`

**raw red** (exit 1): `AssertionError: claude-argv-outside-adapter:lib/launcher.py:1138`

**restore:** inverse diff (adapter call + refusal propagation restored)

**raw green** (exit 0): `1 passed in 2.24s`

---

## BP-A2 — session-id refusal leg

- **guarded element:** `engine_adapter.claude_builder_argv` session-id validation
- **axis:** `test_claude_builder_argv_session_id_invalid[none]`

**neutralization** (`engine_adapter.py`):
```diff
-    if not isinstance(session_id, str):
-        return _refuse(
-            REFUSAL_BUILDER_SESSION_ID_INVALID,
-            detail="a canonical lowercase UUID string",
-        )
-    try:
-        canonical = str(uuid.UUID(session_id))
-    except (ValueError, AttributeError, TypeError):
-        canonical = None
-    if canonical is None or canonical != session_id:
-        return _refuse(
-            REFUSAL_BUILDER_SESSION_ID_INVALID,
-            detail="a canonical lowercase UUID string",
-        )
+    # bite-proof neutralization: session-id validation disabled
+    canonical = session_id
```

**command:** `pytest plugins/superheroes/lib/tests/test_engine_adapter.py::test_claude_builder_argv_session_id_invalid[none] -q`

**raw red** (exit 1): `AssertionError: assert None == 'builder-session-id-invalid'`

**restore:** full session-id validation block quoted above restored verbatim

**raw green** (exit 0): `1 passed in 0.32s`

---

## BP-A3 — compose_launch propagates adapter refusal

- **guarded element:** `launcher.compose_launch` refusal propagation
- **axis:** `test_compose_launch_propagates_adapter_refusal`

**neutralization** (`launcher.py`):
```diff
-    built = engine_adapter.claude_builder_argv(token, session_id, prompt)
-    if built.get("ok") is False or built.get("reason") is not None:
-        return _fail(built["reason"])
-    argv = built["argv"]
+    built = engine_adapter.claude_builder_argv(token, session_id, prompt)
+    argv = built.get("argv") or []
```

**command:** `pytest plugins/superheroes/lib/tests/test_launcher.py::test_compose_launch_propagates_adapter_refusal -q`

**raw red** (exit 1): `AssertionError: assert True is False`

**restore:** refusal check before `argv = built["argv"]` restored

**raw green** (exit 0): `1 passed in 0.54s`
