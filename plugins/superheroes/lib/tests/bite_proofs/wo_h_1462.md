# #1462 bite-proof — legacy-claim tests plant legacy claims; the fresh wave pins its order id and prompt suffix

Per-element bite-proof for the assertions this change adds to `plugins/superheroes/lib/tests/test_conformance_probe.py`. For P1 and P2 the test's own claim plant was swapped by one targeted edit (the plant is the premise the assertion guards, not the assertion). For P3 the subject was mutated, never the test. Each named test ran alone and went red on the guarded axis, then the edit was reverted with the inverse edit and the test ran green.

**Guarded-element set (declared in the work order):** (P1) the legacy continuation keeps the legacy order id; (P2) the legacy orphan is read from the legacy claim file, not a current-format file; (P3) a fresh wave dispatches under the `registration-probe` order-id prefix.

**Head proven:** working tree on `2307434c` with this change's edits uncommitted, in the build worktree.

**Provenance:** an implementer (Claude Opus 5.5) ran these proofs.

**Command:** each run used the exact node id below, never `-k`:

```
scripts/pinned-python -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest <node id> -q
```

## P1 — legacy continuation keeps the legacy order id

- **Guarded element:** `test_astra_probe_continuation_legacy_claim_without_snapshot_uses_current_seat`, the assertion `order_id == "astra-probe-" + hashlib.sha256(wave.encode("utf-8")).hexdigest()[:12]`. Axis: only a claim at the legacy claim path makes the continuation dispatch under the legacy order id.
- **Neutralization** (test file, the claim plant):

```
-    legacy_claim_path = CP._legacy_claim_path(ledger_dir, wave)
+    legacy_claim_path = CP._registration_claim_path(ledger_dir, wave)
```

- **Node:** `plugins/superheroes/lib/tests/test_conformance_probe.py::test_astra_probe_continuation_legacy_claim_without_snapshot_uses_current_seat`
- **Raw red** (decisive lines; exit 1):

```
>       assert order_id == "astra-probe-" + hashlib.sha256(wave.encode("utf-8")).hexdigest()[:12]
E       AssertionError: assert 'registration...-256d32e548aa' == 'astra-probe-256d32e548aa'
E         
E         - astra-probe-256d32e548aa
E         ? ^
E         + registration-probe-256d32e548aa
E         ? ^^^^    ++++
FAILED plugins/superheroes/lib/tests/test_conformance_probe.py::test_astra_probe_continuation_legacy_claim_without_snapshot_uses_current_seat
1 failed in 7.97s
```

- **Restore:** the inverse edit. Restored line quoted back:

```
    legacy_claim_path = CP._legacy_claim_path(ledger_dir, wave)
```

- **Raw green** (exit 0):

```
.                                                                        [100%]
1 passed in 3.04s
```

## P2 — legacy orphan is read from the legacy claim file

- **Guarded element:** `test_astra_probe_orphan_legacy_claim_without_model_settles_unrecorded`, the assertions that the legacy file exists at `CP._legacy_claim_path(ledger_dir, old_wave)` and no file exists at `CP._registration_claim_path(ledger_dir, old_wave)`. Axis: the orphan the sweep settles was planted in the legacy format at the legacy path.
- **Neutralization** (test file, the claim plant):

```
-    legacy_claim_path = CP._legacy_claim_path(ledger_dir, old_wave)
+    legacy_claim_path = CP._registration_claim_path(ledger_dir, old_wave)
```

- **Node:** `plugins/superheroes/lib/tests/test_conformance_probe.py::test_astra_probe_orphan_legacy_claim_without_model_settles_unrecorded`
- **Raw red** (decisive lines; the temp-dir prefix is elided as `<tmp>`; exit 1):

```
>       assert os.path.isfile(CP._legacy_claim_path(ledger_dir, old_wave))
E       AssertionError: assert False
E        +  where False = <function isfile at 0x101cd4e00>('<tmp>/test_astra_probe_orphan_legacy0/ledger/astra-probe-claim-256d32e548aa1558.json')
FAILED plugins/superheroes/lib/tests/test_conformance_probe.py::test_astra_probe_orphan_legacy_claim_without_model_settles_unrecorded
1 failed in 2.91s
```

- **Restore:** the inverse edit. Restored line quoted back:

```
    legacy_claim_path = CP._legacy_claim_path(ledger_dir, old_wave)
```

- **Raw green** (exit 0):

```
.                                                                        [100%]
1 passed in 1.76s
```

## P3 — fresh wave dispatches under the registration-probe order id

- **Guarded element:** `plugins/superheroes/lib/conformance_probe.py:43` `REGISTRATION_ORDER_ID_PREFIX`, read by `test_registration_probe_new_writes_use_registration_names` through the spelled-out literal `"registration-probe-"`. Axis: the prefix drifting back to the pre-rename name goes red.
- **Neutralization** (subject, `conformance_probe.py:43`):

```
-REGISTRATION_ORDER_ID_PREFIX = "registration-probe"
+REGISTRATION_ORDER_ID_PREFIX = "astra-probe"
```

- **Node:** `plugins/superheroes/lib/tests/test_conformance_probe.py::test_registration_probe_new_writes_use_registration_names`
- **Raw red** (decisive lines; exit 1):

```
>       assert order_id == (
            "registration-probe-" + hashlib.sha256("wave-fresh".encode("utf-8")).hexdigest()[:12])
E       AssertionError: assert 'astra-probe-cac9927f5f36' == 'registration...-cac9927f5f36'
E         
E         - registration-probe-cac9927f5f36
E         ? ^^^^    ----
E         + astra-probe-cac9927f5f36
E         ? ^
FAILED plugins/superheroes/lib/tests/test_conformance_probe.py::test_registration_probe_new_writes_use_registration_names
1 failed in 1.48s
```

- **Restore:** the inverse edit. Restored line quoted back:

```
REGISTRATION_ORDER_ID_PREFIX = "registration-probe"
```

- **Restore receipt:** `git diff --stat -- plugins/superheroes/lib/conformance_probe.py` printed nothing after the restore.
- **Raw green** (exit 0):

```
.                                                                        [100%]
1 passed in 1.37s
```

Nothing was redacted beyond eliding the pytest temp-directory prefix in P2; the captures contain no secrets, tokens, private URLs or personal data.
