# WO-G (#1145 continuation-3) bite-proofs — investigated echo + gate assertion pin

## BP1 — investigated placeholder echo check

**Guarded element:** `test_parse_result_review_investigated_all_placeholder_echo_refused` — axis: all investigated entries that are whole-value placeholder near-copies must grade unreadable via `parse_result`.

**Neutralization:** `_investigated_list_all_placeholder_echo` returned `False` unconditionally (`plugins/superheroes/lib/engine_adapter.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_adapter.py::test_parse_result_review_investigated_all_placeholder_echo_refused' -q
```

**Raw red:**
```
FAILED plugins/superheroes/lib/tests/test_engine_adapter.py::test_parse_result_review_investigated_all_placeholder_echo_refused
>       assert EA.parse_result("codex", "review", stdout, echo_nonce=nonce) == _HOLLOW_MEMBER_MALFORMED
E       AssertionError: assert {'findings': ...': 'findings'} == {'ok': False,... 'unreadable'}
E         Differing items:
E         {'ok': True} != {'ok': False}
1 failed in 0.27s
```

**Restore:** reinstated `_investigated_list_all_placeholder_echo` body (`all(_investigated_path_is_placeholder_echo(...))`).

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_adapter.py` → ` M plugins/superheroes/lib/engine_adapter.py` (intended feature diff only; neutralization reverted).

**Raw green:**
```
2 passed in 0.67s
```
(representative node included in post-restore batch with BP2 node)

---

## BP2 — repaired gate assertion (nonce-invariant marker)

**Guarded element:** `test_review_dispatch_fed_prompt_result_contract_unpinned_lists_all_kinds` — axis: unpinned fedPrompt must not contain any findings example block (any nonce).

**Neutralization:** widened example gate to `expected_result_kind in (None, "findings")` (`plugins/superheroes/lib/engine_dispatch.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_result_contract_unpinned_lists_all_kinds' -q
```

**Raw red:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_result_contract_unpinned_lists_all_kinds
>       assert _FINDINGS_EXAMPLE_STABLE_MARKER not in prompt_text
E       assert 'an echoed e...ades hollow.' not in 'You are a d...it ruling)\n'
E         'an echoed example grades hollow.' is contained here:
E           ry value; an echoed example grades hollow.
1 failed in 1.22s
```

**Restore:** reinstated `if expected_result_kind == "findings":` gate only.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` → (empty; neutralization fully reverted).

**Raw green:**
```
2 passed in 0.67s
```
(BP1 + BP2 nodes in one post-restore invocation)
