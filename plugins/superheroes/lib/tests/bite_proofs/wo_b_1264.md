# Bite-proof record — WO-B (#1264 escalation safety list)

**Provenance: implementer-authored and implementer-run.** Neutralization applied as a targeted,
revertible edit to `plugins/superheroes/lib/escalation.py` and restored by inverse edit; no
`git checkout`/discard was used.

Command shape for every run below:

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest <exact node id> -q
```

## Guarded elements

| id | guarded element | axis |
|---|---|---|
| BP-WOB1 | `mode_registry.py` in `SAFETY_MACHINERY` | fixer must refuse edits to `mode_registry.py` (membership in the protected set) |

---

## BP-WOB1 — `mode_registry.py` stays in `SAFETY_MACHINERY`

**guarded element:** `plugins/superheroes/lib/escalation.py` — `"mode_registry.py"` tuple member
**axis:** removing `mode_registry.py` from `SAFETY_MACHINERY` opens it to unattended fixing; the
membership pin must catch that drift.

**neutralization** (production path — `SAFETY_MACHINERY` tuple; detectors untouched): removed the
`mode_registry.py` entry line from the tuple.

**raw red** (`test_safety_machinery_set_members_are_pinned`):

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_safety_machinery_set_members_are_pinned _________________

    def test_safety_machinery_set_members_are_pinned():
        # The set is the single source of truth (§4 bound-2): the names whose edit could disable a
        # floor/gate/halt/escalation guarantee. Pin membership so the guard and the eval fixture
        # can't drift. escalation_resolve.py — the wrapper that OWNS the fail-closed verdict — is
        # included (review caught its omission: without it a fixer could neuter the guard).
>       assert set(ESC.SAFETY_MACHINERY) == {
            "escalation.py", "escalation_resolve.py", "loop_state.py", "circuit_breaker.py",
            "gate_write.py", "definition_doc.py",
            "model_tier.py", "model_registry.py",
            "engine_pref.py", "seat_map.py", "dispatch_guard.py", "engine_dispatch.py", "seat_canary.py",
            "engine_adapter.py",
            "hooks.json",
            "session_start.py",
            "session_mode.py", "version_skew.py",
            "escalation-base.md", "review-base.md",
            # shared review-and-fix loop (#104): deciders, durable record, and the orchestration shell
            "panel_tally.py", "loop_synthesis.py", "verification.py", "round_phases.py",
            "payload_contracts.py", "round_adapters.py", "verify_gate.py",
            "review_result.py", "round_driver.py", "audits.py", "delta_surface.py",
            # worktree-protection gate + hook wrapper + band-wide storage resolver (collector #695 item 55)
            "worktree_guard.py", "mode_registry.py", "worktree_guard_gate.py",
        }
E       AssertionError: assert {'audits.py',...pter.py', ...} == {'audits.py',...pter.py', ...}
E         
E         Extra items in the right set:
E         'mode_registry.py'
E         Use -v to get more diff

plugins/superheroes/lib/tests/test_escalation.py:104: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_escalation.py::test_safety_machinery_set_members_are_pinned
1 failed in 0.21s
```

The red is on the declared axis: expected set still pins `mode_registry.py` but `SAFETY_MACHINERY`
no longer carries it.

**restore:** inverse edit reinstating the line between `worktree_guard.py` and
`worktree_guard_gate.py`:

```python
    "mode_registry.py",       # band-wide storage resolver — a fixer edit can silently redirect where every project reads calibration from or flip whether it reads as calibrated at all
```

**restore receipt:** restored line quoted above; supplementary `git status --porcelain` over the
worktree after restore showed only WO-B surface paths (no residue on `escalation.py` beyond the
intended final state).

**raw green:**

```
.                                                                        [100%]
1 passed in 0.33s
```
