# WO-A (#1131) bite-proof — `sweep_landing` stale-landing + both-shapes scan + sort tier

## BP-1 — `stale-landing` refusal token (`test_sweep_reports_full_envelope_stray_as_stale_landing`)

- **guarded element:** `plugins/superheroes/lib/round_records.py` — `_refuse("stale-landing", ...)` in `sweep_landing` stray loop
- **axis:** stray landing files map to no roster slot are refused with reason exactly `stale-landing`

**neutralization:** change emitted reason to `stale-landing-neutralized`:
```python
        results.append(_refuse("stale-landing-neutralized",
```

**raw red** (pytest exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_sweep_reports_full_envelope_stray_as_stale_landing ____________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-11387/test_sweep_reports_full_envelo0')

    def test_sweep_reports_full_envelope_stray_as_stale_landing(tmp_path):
        sd = _session(tmp_path)
        stray = RR.landing_path(sd, 1, PHASE, "stray-seat", 1)
        RR.atomic_write_json(stray, _env(seat="stray-seat"))
        out = RR.sweep_landing(sd, 1, PHASE, current_attempt=1, roster=ROSTER)
        stale = [r for r in out if r.get("reason") == "stale-landing"]
>       assert len(stale) == 1
E       assert 0 == 1
E        +  where 0 = len([])

plugins/superheroes/lib/tests/test_round_records.py:565: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_sweep_reports_full_envelope_stray_as_stale_landing
1 failed in 0.13s
```

**restore:** revert reason string to `stale-landing`:
```python
        results.append(_refuse("stale-landing",
```

**restore receipt:** `git status --porcelain` after all three restores (see BP-3).

**raw green** (batched with BP-2/BP-3 restores — full file, pytest exit 0):
```
........................................................................ [ 53%]
..............................................................           [100%]
134 passed in 0.51s
```

---

## BP-2 — both-shapes stray scan (`test_sweep_reports_bare_payload_stray_as_stale_landing`)

- **guarded element:** `plugins/superheroes/lib/round_records.py` — `sweep_landing` stray loop suffix matching
- **axis:** `<skey>.a<K>.payload.json` bare-payload landings are detected as strays, not silently skipped

**neutralization:** restore single-shape `.a<K>.json` suffix test only (remove `bare_suffix` branch and dedupe map):
```python
    suffix = ".a%d.json" % current_attempt if isinstance(current_attempt, int) else None
    ...
        if not name.endswith(suffix):
            continue
        results.append(_refuse("stale-landing", storageKey=name[:-len(suffix)], landingPaths=[name], ...))
```

**raw red** (pytest exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_sweep_reports_bare_payload_stray_as_stale_landing ____________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-11388/test_sweep_reports_bare_payloa0')

    def test_sweep_reports_bare_payload_stray_as_stale_landing(tmp_path):
        ...
        stale = [r for r in out if r.get("reason") == "stale-landing"]
>       assert len(stale) == 1
E       assert 0 == 1
E        +  where 0 = len([])

plugins/superheroes/lib/tests/test_round_records.py:579: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_sweep_reports_bare_payload_stray_as_stale_landing
1 failed in 0.13s
```

**restore:** reinstate `env_suffix`/`bare_suffix` dual-shape scan with per-storage-key dedupe (production code as shipped).

**restore receipt:** see BP-1.

**raw green:** see BP-1 batched output (`134 passed`).

---

## BP-3 — sort tier (`test_sweep_journals_roster_seats_before_stale_landing_refusals`)

- **guarded element:** `plugins/superheroes/lib/round_records.py` — `results.sort(...)` tier key in `sweep_landing`
- **axis:** roster-slot results sort before `stale-landing` strays regardless of storage-key lexicographic order

**neutralization:** remove tier prefix from sort key (lexicographic only):
```python
    results.sort(key=lambda r: (r.get("storageKey") or "", r.get("seatKey") or ""))
```

**raw red** (pytest exit 1; test uses `stray_skey = "a-stale"` which sorts before `RR.storage_key(SEAT)`):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_sweep_journals_roster_seats_before_stale_landing_refusals ________

    ...
>       assert out.index(roster_results[0]) < out.index(stale[0])
E       AssertionError: assert 1 < 0

plugins/superheroes/lib/tests/test_round_records.py:610: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_sweep_journals_roster_seats_before_stale_landing_refusals
1 failed in 0.13s
```

**restore:** reinstate tiered sort:
```python
    # Strays after roster results: round_driver._sweep_record refuses on the first non-ok result.
    results.sort(key=lambda r: (1 if r.get("reason") == "stale-landing" else 0,
                                r.get("storageKey") or "", r.get("seatKey") or ""))
```

**restore receipt** (`git status --porcelain`):
```
 M plugins/superheroes/lib/round_records.py
 M plugins/superheroes/lib/tests/test_round_records.py
```

**raw green:** see BP-1 (`134 passed in 0.51s`).
