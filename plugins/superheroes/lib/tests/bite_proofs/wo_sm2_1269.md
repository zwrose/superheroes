# WO-SM2 (#1269) bite-proof — journal discrimination + canary tier acceptance

**Provenance:** cursor composer-2.5 / dispatch-write

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-SM2-1 | `_entry_refusal_terminal` runDir clearing vs `_resolved_inputs_echo_from_run_dir` | unreadable journal refusal keeps `runDir` and `resolvedInputsStatus: unverifiable` | `test_sm2_1269_unreadable_journal_preserves_run_dir_on_refusal` |
| BP-SM2-2 | `_resolve_canary_identity` accepted-tier derivation | tier `seat_map` cannot emit must refuse; backfilled `reviewer` tier probes | `test_sm2_1269_grounding_refuses_reviewer_deep` + `test_sm2_1269_backfilled_tier_probes_not_refuses` (superseded for claude-fallback tiers by **BP-SM4-1**) |

---

## BP-SM2-1 — journal-state discrimination preserves runDir

- **axis:** entry refusal on unreadable journal keeps `runDir` and reports `resolvedInputsStatus: unverifiable`

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `_entry_refusal_terminal`):
```python
    if not out.get("runOpened"):
        out["runDir"] = ""
```
(replaces the `resolvedInputsStatus != "unverifiable"` guard)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch.py::test_sm2_1269_unreadable_journal_preserves_run_dir_on_refusal -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_sm2_1269_unreadable_journal_preserves_run_dir_on_refusal _________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-814/test_sm2_1269_unreadable_journ0')

    def test_sm2_1269_unreadable_journal_preserves_run_dir_on_refusal(tmp_path):
        # axis: entry refusal on unreadable journal keeps runDir and reports unverifiable
        run_dir = str(tmp_path / "corrupt-refusal")
        os.makedirs(run_dir, exist_ok=True)
        path = ED._journal_path(run_dir)
        os.makedirs(os.path.dirname(path), exist_ok=True)
        with open(path, "w", encoding="utf-8") as fh:
            fh.write("not-json\n")
        res = ED.dispatch_review(
            seat=_codex_seat(),
            prompt_path=_valid_prompt(tmp_path),
            repo_root=_repo(tmp_path),
            run_engine=_never_call,
            build_view=_never_build_view,
            run_dir=run_dir,
            prompt_pat="typo",
        )
        assert res.get("runOpened") is False
        assert res.get("resolvedInputsStatus") == "unverifiable"
>       assert res["runDir"] == os.path.realpath(run_dir)
E       AssertionError: assert '' == '/private/var...rrupt-refusal'
E         
E         - /private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-814/test_sm2_1269_unreadable_journ0/corrupt-refusal

plugins/superheroes/lib/tests/test_engine_dispatch.py:9582: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_sm2_1269_unreadable_journal_preserves_run_dir_on_refusal
1 failed in 0.51s
```

**restore:**
```python
    if not out.get("runOpened") and out.get("resolvedInputsStatus") != "unverifiable":
        out["runDir"] = ""
```

**raw green:**
```
.                                                                        [100%]
1 passed in 0.43s
```

---

## BP-SM2-2 — canary accepted-tier derivation (SUPERSEDED)

**Superseded:** the tier census in **BP-SM4-1** proved `reviewer-deep` is emittable for
`grounding-seat` through `_backfill`'s claude fallback, so the refusal this section recorded is no
longer correct behaviour. **Current record: BP-SM4-1.**

---

Historical record (do not treat as current):

**Re-taken:** the original proof ran against the over-permissive `accepted_tiers_for_seat` that
unconditionally unioned `reviewer-deep` into every seat's accepted set. That set no longer matches
what `seat_map.build` can emit; this proof was re-taken on the corrected head where accepted tiers
derive from `_backfill_rotation_tiers` only.

- **axis:** a tier `seat_map` cannot emit must refuse (grounding-seat / `reviewer-deep`); a
  legitimately backfilled `reviewer` tier on a lens seat still probes

**neutralization** (`plugins/superheroes/lib/seat_map.py`, `accepted_tiers_for_seat`):
```python
    primary = tiers_map.get(seat, "reviewer")
    return frozenset({primary, "reviewer", "reviewer-deep"})
```
(replaces `return frozenset(_backfill_rotation_tiers(primary))`)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_seat_canary.py::test_sm2_1269_grounding_refuses_reviewer_deep -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_sm2_1269_grounding_refuses_reviewer_deep _________________

    def test_sm2_1269_grounding_refuses_reviewer_deep():
        # axis: tier seat_map cannot emit for grounding-seat refuses naming accepted tiers
        out = SC.run_canary(
            "grounding-seat",
            _seat_config("codex", "gpt-5.6-sol", "xhigh", "reviewer-deep"),
            repo_root="/r",
        )
        assert out["outcome"] == "unrunnable"
>       assert (
            "requires tier 'reviewer'" in out["detail"]
            or "accepted tiers: reviewer" in out["detail"]
        )
E       assert ("requires tier 'reviewer'" in 'not-dispatched: repo-root-missing' or 'accepted tiers: reviewer' in 'not-dispatched: repo-root-missing')

plugins/superheroes/lib/tests/test_seat_canary.py:1117: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_canary.py::test_sm2_1269_grounding_refuses_reviewer_deep
1 failed in 0.13s
```

**restore:**
```python
    primary = tiers_map.get(seat, "reviewer")
    return frozenset(_backfill_rotation_tiers(primary))
```

**raw green:**
```
.                                                                        [100%]
1 passed in 0.12s
```

Current record for the AST census guard: **BP-SM4-1**.

---

## BP-SM4-1 — `_backfill` tier census matches `accepted_tiers_for_seat`

- **axis:** a tier `_backfill` can record is not in the accepted set

**Guarded element:** `test_sm4_1269_backfill_tier_census_matches_accepted_tiers`

**neutralization** (`plugins/superheroes/lib/seat_map.py`, `accepted_tiers_for_seat`):
```python
    return frozenset(_backfill_rotation_tiers(primary))
```
(replaces `return _backfill_emittable_tiers(primary)` — claude-fallback tiers remain in `_backfill` via `_BACKFILL_CLAUDE_ROTATION` but drop out of the accessor)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_seat_map.py::test_sm4_1269_backfill_tier_census_matches_accepted_tiers -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_sm4_1269_backfill_tier_census_matches_accepted_tiers ___________

    def test_sm4_1269_backfill_tier_census_matches_accepted_tiers():
        """AST census: accepted tiers equal tiers ``_backfill`` can assign — no hand-maintained list.

        If a third rotation is added to ``_backfill`` tomorrow, this test fails without updating it.
        """
        walk_tiers, undecidable = _collect_backfill_tier_literals_from_source()
        assert not undecidable, "undecidable tier constructs in _backfill: %s" % undecidable
        for seat in SM.PANEL_ROSTER:
            accepted = SM.accepted_tiers_for_seat(seat)
>           assert accepted == walk_tiers, (
                "accepted_tiers_for_seat(%r)=%s != AST walk %s"
                % (seat, sorted(accepted), sorted(walk_tiers))
            )
E           AssertionError: accepted_tiers_for_seat('grounding-seat')=['reviewer'] != AST walk ['reviewer', 'reviewer-deep']
E           assert frozenset({'reviewer'}) == {'reviewer', 'reviewer-deep'}
E             
E             Extra items in the right set:
E             'reviewer-deep'
E             Use -v to get more diff

plugins/superheroes/lib/tests/test_seat_map.py:3041: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_map.py::test_sm4_1269_backfill_tier_census_matches_accepted_tiers
1 failed in 0.15s
```

**restore:**
```python
    return _backfill_emittable_tiers(primary)
```

**raw green:**
```
.                                                                        [100%]
1 passed in 0.19s
```
