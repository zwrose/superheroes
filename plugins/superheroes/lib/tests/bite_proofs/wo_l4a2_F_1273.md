# C14 layer 4a-2 (#1273) WO-F bite-proofs

**Provenance.** Implementer dispatch (cursor composer-2.5). Each neutralization was one targeted edit, reverted with the inverse edit. Probe-only edits to `engine_adapter.py` and `claude_modes.py` were reverted byte-identically (`git diff --quiet` exit 0).
**Command prefix:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest <node> -q -p no:cacheprovider`.

**Rung line.** The single-home AST test retires when `launch_ledger` and `engine_adapter` stop deriving the mode from `claude_modes`.

| ID | Guarded element | Neutralization | Red (exit 1): the exact token or assertion | Green |
|---|---|---|---|---|
| F1 | `_background_fields_problem` is the fold's rule | `_validate_event_fields` started branch: replace helper call with `pass  # bite-proof F1: helper skipped` | `test_started_background_partial_set_refuses[partial_fields0]`: `E assert True is False`; agreement cases `partial_mode_id` etc.: fold passes when it should refuse | exit 0, 15 passed |
| F2 | `_background_fields_problem` is the repair's rule | `_validate_started_repair`: replace helper check with `return True  # bite-proof F2: helper skipped` | `test_background_fields_fold_and_repair_agree[partial_mode_id]`: `E assert True is False` on `repair_ok` | exit 0, 9 passed |
| F3 | single-home AST census in `launch_ledger.py` | `LAUNCH_MODE_BACKGROUND = claude_modes.MODE_BACKGROUND` → `LAUNCH_MODE_BACKGROUND = "background"` | `background-mode-literal-outside-home:launch_ledger.py:51` | exit 0, 1 passed |
| F4 | single-home AST census in `engine_adapter.py` (probe) | `MODE_BACKGROUND = claude_modes.MODE_BACKGROUND` → `MODE_BACKGROUND = "background"` at line 51 | `background-mode-literal-outside-home:engine_adapter.py:51` | probe reverted; `git diff --quiet -- plugins/superheroes/lib/engine_adapter.py` → 0 |
| F5 | literal pin on `claude_modes.MODE_BACKGROUND` (probe) | `MODE_BACKGROUND = "background"` → `MODE_BACKGROUND = "bg"` in `claude_modes.py` | `test_background_mode_literal_pinned`: `E AssertionError: assert 'bg' == 'background'` | probe reverted; `git diff --quiet -- plugins/superheroes/lib/claude_modes.py` → 0; literal pin test exit 0 |

**Restore receipts (F1).** Neutralization `pass  # bite-proof F1: helper skipped` restored to:
```
        problem = _background_fields_problem(rec)
        if problem:
            return problem
```

**Restore receipts (F2).** Neutralization `return True  # bite-proof F2: helper skipped` restored to:
```
    if _background_fields_problem(started_repair):
        return False
    return True
```

**Restore receipts (F3).** `LAUNCH_MODE_BACKGROUND = "background"` restored to `LAUNCH_MODE_BACKGROUND = claude_modes.MODE_BACKGROUND`.

**R28.** Vacuous detector `test_launch_mode_background_identity` (`is` identity) replaced by `test_background_mode_literal_pinned` and `test_background_mode_single_home`.
