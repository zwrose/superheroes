# WO-A #1145c3 bite-proof — review_findings_schema echo detection

Build: nonce-parameterized rendering + near-copy echo checker (substring leg retired).

## 1. Near-copy normalization

- **guarded element:** `review_findings_schema.py:_near_copy_normalize` — axis: casefold + whitespace-collapse enables uniform-retype echo detection without substring matching
- **neutralization:** In `_near_copy_normalize`, replaced `return re.sub(r"\s+", " ", value.casefold()).strip()` with `return value.strip()  # BITE-PROOF neutralization: strip only, no casefold`
- **raw red:**

```
$ /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_uniform_retype_of_example -q

F                                                                        [100%]
=================================== FAILURES ===================================
_______ test_member_carries_sentinel_true_for_uniform_retype_of_example ________

    def test_member_carries_sentinel_true_for_uniform_retype_of_example():
        # axis: uniform cosmetic retype keeps sentinel in every value but breaks exact placeholder match
        member = _uniform_retype_strings(RFS.example_findings_object()["findings"][0])
>       assert RFS.member_carries_sentinel(member) is True
E       AssertionError: assert False is True
E        +  where False = <function member_carries_sentinel at 0x102f9e790>({'body': 'body PLACEHOLDER [RFS-EXAMPLE-SENTINEL-1145]', ...})
E        +    where <function member_carries_sentinel at 0x102f9e790> = RFS.member_carries_sentinel

plugins/superheroes/lib/tests/test_review_findings_schema.py:441: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_uniform_retype_of_example
1 failed in 0.33s
```

- **restore:** Restored `return re.sub(r"\s+", " ", value.casefold()).strip()` in `_near_copy_normalize`
- **restore receipt:**

```
$ git status --porcelain plugins/superheroes/lib/review_findings_schema.py
 M plugins/superheroes/lib/review_findings_schema.py
```

(Neutralization line removed; file carries intended WO-A diff only.)

- **raw green:** (see combined green run in §4)

## 2. Fail-closed edge 3 — empty substance field set

- **guarded element:** `review_findings_schema.py:member_carries_sentinel` — axis: member with no present substance keys must not echo-verdict
- **neutralization:** After `if not isinstance(member, dict): return False`, inserted `if not any(key in member for key in _ECHO_SUBSTANCE_KEYS): return True  # BITE-PROOF neutralization: empty substance field set echoes`
- **raw red:**

```
$ /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_false_for_empty_substance_field_set -q

F                                                                        [100%]
=================================== FAILURES ===================================
_______ test_member_carries_sentinel_false_for_empty_substance_field_set _______

    def test_member_carries_sentinel_false_for_empty_substance_field_set():
        # axis: no present substance fields — all-quantifier leg must not echo-verdict
        member = {"severity": "Critical", "file": "lib/gate.py", "line": 12}
>       assert RFS.member_carries_sentinel(member) is False
E       AssertionError: assert True is False
E        +  where True = <function member_carries_sentinel at 0x102c7b820>({'file': 'lib/gate.py', 'line': 12, 'severity': 'Critical'})
E        +    where <function member_carries_sentinel at 0x102c7b820> = RFS.member_carries_sentinel

plugins/superheroes/lib/tests/test_review_findings_schema.py:447: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_false_for_empty_substance_field_set
1 failed in 0.28s
```

- **restore:** Removed the inserted early-return block
- **restore receipt:** same porcelain as §1 (neutralization line absent after restore)
- **raw green:** (see combined green run in §4)

## 3. Nonce keying

- **guarded element:** `review_findings_schema.py:_placeholder_string` — axis: per-dispatch nonce suffix distinguishes placeholder strings so cross-nonce check does not echo
- **neutralization:** Replaced `_placeholder_string` body with unconditional `return "%s placeholder %s" % (field_name, EXAMPLE_SENTINEL)  # ignore nonce`
- **raw red:**

```
$ /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_different_nonce_not_echo -q

F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_member_carries_sentinel_different_nonce_not_echo _____________

    def test_member_carries_sentinel_different_nonce_not_echo():
        # axis: same member is not echo when checked with a different nonce and no base-set match
        nonce_n = "dispatch-n"
        nonce_m = "dispatch-m"
        example = RFS.example_findings_object(nonce_n)["findings"][0]
>       assert RFS.member_carries_sentinel(example, nonce=nonce_m) is False
E       AssertionError: assert True is False
E        +  where True = <function member_carries_sentinel at 0x104f66790>({...}, nonce='dispatch-m')
E        +    where <function member_carries_sentinel at 0x104f66790> = RFS.member_carries_sentinel

plugins/superheroes/lib/tests/test_review_findings_schema.py:665: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_different_nonce_not_echo
1 failed in 0.41s
```

- **restore:** Restored nonce-aware `_placeholder_string` (`EXAMPLE_SENTINEL` bare when no nonce; `EXAMPLE_SENTINEL-<nonce>` when effective)
- **restore receipt:** same porcelain as §1
- **raw green:** (see combined green run in §4)

## 4. Combined green after all restores

```
$ /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_uniform_retype_of_example \
  plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_false_for_empty_substance_field_set \
  plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_different_nonce_not_echo \
  -q

...                                                                      [100%]
3 passed in 0.57s
```
