# WO-D (#1154) bite-proofs — safety-machinery route doctrine pin (legs 1–3)

## BP-D1 — `test_retired_preauthorization_rule_is_absent`

**Guarded element:** leg 1 — axis: *absence* of retired pre-authorization substrings in both prose homes.

**Neutralization:** re-introduced one retired sentence into `review-discipline.md`'s safety-machinery section:
```markdown
Owner authorization is required when the findings are blocking.
```
(inserted immediately before `**Blocking findings go out on advisor or builder authority.**`)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_retired_preauthorization_rule_is_absent -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_retired_preauthorization_rule_is_absent _________________

    def test_retired_preauthorization_rule_is_absent():
        home_text = _read(_HOME)
        copy_text = _read(_COPY_HOLDER)
        for substring in _RETIRED_BOTH_FILES:
            if substring in home_text:
>               raise AssertionError(
                    f"{_HOME}: retired rule substring must be absent, found: {substring!r}"
                )
E               AssertionError: rubric/review-discipline.md: retired rule substring must be absent, found: 'Owner authorization is required when the findings are blocking'

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:129: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_retired_preauthorization_rule_is_absent
1 failed in 0.04s
```

**Restore:** removed the inserted sentence (inverse edit — deleted the blank line and `Owner authorization is required when the findings are blocking.` line).

**Restore receipt (supplementary `git status --porcelain`):** (empty — only untracked test module remained)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.03s
```

---

## BP-D2 — `test_carveout_clauses_present_in_doctrine_home`

**Guarded element:** leg 2 — axis: *clause presence* of load-bearing carve-out clauses in the doctrine home section.

**Neutralization:** deleted one clause from the safety-machinery section:
```markdown
**Classification fails closed.**
```
(the heading sentence immediately before `Before the round, put each finding's surface…`)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_carveout_clauses_present_in_doctrine_home -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_carveout_clauses_present_in_doctrine_home ________________

    def test_carveout_clauses_present_in_doctrine_home():
        section = _collapse_whitespace(_route_section_text())
        for clause in _CARVEOUT_CLAUSES:
>           assert clause in section, (
                f"{_HOME} (section {_ROUTE_SECTION}): missing carve-out clause: {clause!r}"
            )
E           AssertionError: rubric/review-discipline.md (section ### The safety-machinery route — the guard refuses the fixer): missing carve-out clause: '**Classification fails closed.**'
E           assert '**Classification fails closed.**' in "### The safety-machinery route — the guard refuses the fixer Some of this plugin's own files are **safety machinery**...hing outside the owner-authority-gate family** by the 2026-08-25 ruling — historical evidence, not current procedure. "

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:146: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_carveout_clauses_present_in_doctrine_home
1 failed in 0.04s
```

**Restore:** re-added `**Classification fails closed.**` before `Before the round, put each finding's surface in exactly one of three`.

**Restore receipt (supplementary `git status --porcelain`):** (empty — only untracked test module remained)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.03s
```

---

## BP-D3 — `test_copyholder_pointer_states_the_new_shape`

**Guarded element:** leg 3 — axis: *copy-holder pointer* states the new route shape and cites the home.

**Neutralization:** removed the gate-family carve-out phrase from the pointer paragraph in `auto-fix-loop.md`:
```markdown
the owner's word required only for the owner-authority-gate family, and the
```
(replaced with `and the` so the surrounding sentence stayed syntactically connected)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_copyholder_pointer_states_the_new_shape -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_copyholder_pointer_states_the_new_shape _________________

    def test_copyholder_pointer_states_the_new_shape():
        paragraph = _pointer_paragraph_text()
        for clause in _POINTER_CLAUSES:
>           assert clause in paragraph, (
                f"{_COPY_HOLDER} (pointer paragraph): missing clause: {clause!r}"
            )
E           AssertionError: skills/review-code/reference/auto-fix-loop.md (pointer paragraph): missing clause: "the owner's word required only for the owner-authority-gate family"
E           assert "the owner's word required only for the owner-authority-gate family" in '**Where those findings go next.** A refusal here means this loop **cannot converge on that surface** —\nthat is the g...fixer*. Follow it\nrather than re-deriving it; do not retry the fixer, and never narrow the guard to converge a round.'

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:154: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_copyholder_pointer_states_the_new_shape
1 failed in 0.05s
```

**Restore:** re-inserted `the owner's word required only for the owner-authority-gate family, and the` in the pointer paragraph.

**Restore receipt (supplementary `git status --porcelain`):** (empty — only untracked test module remained)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.03s
```
