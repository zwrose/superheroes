# WO-H classification — #1271 fixture corpus catches up to tightened gates

Per failing test id at base commit `210bd9970f6751945c868b416565941cef5d1756`.

## Class A — refutation reason vs fix-content receipt

| Test | Class | Change |
|------|-------|--------|
| `test_check_disposition_without_receipt_clean_passes` | A | `_minimal_terminal_state` F1 + `_normalize_refuted_findings` supply `refutedReason`. |
| `test_certify_clean_session_returns_receipt` | A | same |
| `test_unknown_verdict_refuses` | A | same (disposition gate no longer masks verdict gate) |
| `test_resolve_terminal_certified_rejects_unlisted_decision_key` | A | same |
| `test_materialized_session_preserves_checked_base_guard` | A | same |
| `test_certified_receipt_projects_disposition_and_proof` | A | **Stopped** — see findings |

## Class B — dispatch-observed runner telemetry in generated corpus

| Test | Class | Change |
|------|-------|--------|
| `test_must_refuse_fixtures_keep_refusal_reason[base_guard_not_checked_session]` | B | Generator `_observation_fields(tool_calls=1)` for dispatch; regenerated tree. |
| `test_must_refuse_fixtures_keep_refusal_reason[case03_reverted_fix]` | B | same |
| `test_must_refuse_fixtures_keep_refusal_reason[case04_stale_cited_head]` | B | same |
| `test_must_refuse_fixtures_keep_refusal_reason[case05_critical_out_of_scope]` | B | same |
| `test_must_refuse_fixtures_keep_refusal_reason[case05_critical_skipped]` | B | same |
| `test_must_refuse_fixtures_keep_refusal_reason[followup_documented_trigger]` | B | same |
| `test_must_refuse_fixtures_keep_refusal_reason[followup_missing_class_closure]` | B | same |
| `test_must_refuse_fixtures_keep_refusal_reason[followup_no_revisit_trigger]` | B | same |

Hand-landed seats in generated fixtures keep `toolCalls: null` (`build_case06_mixed_panel`, audit specimens, etc.).

## Class C — execution-evidence token / digest alignment in ad-hoc sessions

| Test | Class | Change |
|------|-------|--------|
| `test_dispatch_observed_missing_runner_nonce_refuses` | C | `_normalize_dispatch_journal_row` injects `toolCalls: 1` so binding-incomplete is reached. |
| `test_hand_landed_forces_audited_chain_shape` | C | `_normalize_legacy_result_digest` on envelope + journal via `_normalize_journal_row`. |
| `test_hand_landed_journal_recorded_runner_nonce_certifies` | C | same |
| `test_slot_scoped_nonce_same_slot_certifies` | C | same |
| `test_slot_scoped_nonce_different_slot_refuses` | C | same |
| `test_bite_slot_scoped_nonce_refuses_cross_slot` | C | same |
| `test_repeated_seat_attempt_observation_is_slot_scoped` | C | `_binding_fields` default digest → `DEFAULT_FINDINGS_RESULT_SHA`; dispatch journal normalization. |

## Stopped — cannot green without assertion or gate change

**`test_certified_receipt_projects_disposition_and_proof`**

- Class A fix requires a recorded refutation reason (`refutedReason` via `_normalize_refuted_findings`).
- With a reason present, `_finding_disposition_proof` projects the **string** reason as `dispositionReceipt` (see `round_certification.py:_finding_disposition_proof`).
- The test still asserts `dispositionReceipt == {"headSha": …, "verifyResult": "pass"}` — a dict shape the tightened gate correctly rejects as a refutation reason and the projection path no longer emits once a real reason exists.
- Fixing projection or the assertion is **out of this order's scope** (no gate loosening, no assertion rewrite per R28).
