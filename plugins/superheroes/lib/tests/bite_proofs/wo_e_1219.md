# WO-E (#1219) bite-proofs — fence-aware `section_span` (CG-11)

Guarded-element set: **CG-11** alone — three legs on fenced-block heading misclassification.

## CG-11 leg 1 — `#` comment inside fence does not truncate the section

**Guarded element:** `test_cg11_fence_aware_section_span` (first assertion block) — axis: a clause after a closing fence is still inside the declared section at count 1.

**Neutralization:** reverted `section_span` in `clause_guard.py` to fence-blind heading detection (indices and boundary scan call `_heading_level` without consulting `_fence_states`).

```python
indices = [i for i, line in enumerate(lines) if line.strip() == heading]
# ...
level = _heading_level(lines[i])
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_clause_guard.py::test_cg11_fence_aware_section_span \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________________ test_cg11_fence_aware_section_span ______________________
plugins/superheroes/lib/tests/test_clause_guard.py:206: in test_cg11_fence_aware_section_span
    assert count_clause_in_section(text, REL, "## Declared section", clause_after_fence) == 1
E   AssertionError: assert 0 == 1
E    +  where 0 = count_clause_in_section('## Declared section\nintro\n```bash\n# shell comment must not truncate\n## fake heading inside fence\n```\nBody with clause after the fence is guarded.\n## Next section\nafter', 'synthetic.md', '## Declared section', 'clause after the fence is guarded')
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_clause_guard.py::test_cg11_fence_aware_section_span
1 failed in 0.65s
```

**Restore:** reinstated fence-aware `section_span` (inverse of neutralization — `_fence_states` consulted for heading indices and boundary scan).

**Restore receipt:** restored lines in `clause_guard.py`:
```python
    in_fence = _fence_states(lines)
    indices = [
        i
        for i, line in enumerate(lines)
        if not in_fence[i] and line.strip() == heading
    ]
    # ...
    start_level = _heading_level(lines[start], in_fence[start])
    # ...
        level = _heading_level(lines[i], in_fence[i])
```

**Green run:**
```
.                                                                        [100%]
1 passed in 0.44s
```

---

## CG-11 leg 2 — `##` inside fence does not satisfy or duplicate the declared heading

**Guarded element:** `test_cg11_fence_aware_section_span` (duplicate-heading block) — axis: a `##` line inside a fence does not create a duplicate-heading `RuntimeError` when a real heading of the same text exists outside the fence.

**Neutralization:** same fence-blind `section_span` as leg 1.

**Command:** in-process probe (test file unedited):
```python
section_span(fake.splitlines(), '## Real heading', REL)
```

**Red run:**
```
LEG2 RED: synthetic.md: expected exactly one '## Real heading' line, found 2
```

**Restore:** same inverse as leg 1.

**Green run:** `section_span` returns `(0, 5)` with `dup_lines[end] == '## Other'` and no `RuntimeError`.

---

## CG-11 leg 3 — real heading after closing fence still ends the section

**Guarded element:** `test_cg11_fence_aware_section_span` (boundary block) — axis: a real same-or-higher-level heading after fenced content still ends the span.

**Neutralization:** same fence-blind `section_span` as leg 1.

**Command:** in-process probe (test file unedited).

**Red run:**
```
LEG3 end line: '# comment'
LEG3 has more body: False
LEG3 tail excluded: True
LEG3 RED: expected end at ## Ends here
```

**Restore:** same inverse as leg 1.

**Green run:** `lines[end] == '## Ends here'`, `more body` inside span, `tail` excluded.

---

## Inherited real-channel red — FB-3 (`setup.md`) before fix

**Label:** inherited — not applied by this dispatch; captured on pre-fix head.

**Surface:** `test_test_receipt_evidence_doctrine.py` FB-3 (`skills/review-code/reference/setup.md`, `## Setup resolution — run these in order`).

**Failure summary (pre-fix):**
```
FAILED ...::test_consumer_section_points_at_test_receipt_evidence_home[FB-3]
  AssertionError: ... setup.md ... expected count 1, found 0
FAILED ...::test_pointer_roster_is_complete
  AssertionError: pointer roster drift: missing=[('skills/review-code/reference/setup.md', '## Setup resolution — run these in order')], unrostered=[]
FAILED ...::test_negative_consumer_pointer_missing_in_section[FB-3]
  AssertionError: mutation setup: clause 'rubric/test-receipt-evidence.md' not found ... (count=0)
FAILED ...::test_negative_consumer_pointer_over_count_in_section[FB-3]
  Failed: DID NOT RAISE <class 'AssertionError'>
```

**Green after fence-aware fix:**
```
....                                                                     [100%]
4 passed in 0.53s
```
