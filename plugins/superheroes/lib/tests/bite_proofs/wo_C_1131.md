# WO-C (#1131) bite-proof — drift pins for stale-landing, sweep-supersede-unsupported, unknown-seat

## BP-1 — `stale-landing` doc↔code pin (`test_stale_landing_refusal_token_in_round_driver_doc`)

- **guarded element:** `plugins/superheroes/lib/tests/test_ssot_drift.py::test_stale_landing_refusal_token_in_round_driver_doc`
- **axis:** `round-driver.md` still names the literal `stale-landing` token that `round_records.sweep_landing` emits

**neutralization:** in `round-driver.md` gap-sweep trap paragraph, change token to near-miss `stale-landng` (order's `stale-landings` is a false pass — substring `stale-landing` still satisfies `in doc`):
```markdown
*current* roster are hard-refused with **`stale-landng`**; the sweep **ingests and journals every
```

**raw red** (pytest exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_____________ test_stale_landing_refusal_token_in_round_driver_doc _____________
plugins/superheroes/lib/tests/test_ssot_drift.py:234: in test_stale_landing_refusal_token_in_round_driver_doc
    assert "stale-landing" in doc, (
E   AssertionError: round-driver.md: gap-sweep re-emission trap must name the `stale-landing` refusal that round_records.sweep_landing raises when a stray landing file maps to no roster slot in the current manifest
E   assert 'stale-landing' in '# Contents\n\n- [The one entrypoint](#the-one-entrypoint)\n- [next / submit protocol](#next--submit-protocol)\n- [che...ver.py` and the PARITY receipt in `test_retry_budget_parity.py`.\nTreat `round_driver.py` as the contract of record.\n'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_stale_landing_refusal_token_in_round_driver_doc
1 failed in 1.27s
```

**restore:** revert token to `stale-landing`:
```markdown
*current* roster are hard-refused with **`stale-landing`**; the sweep **ingests and journals every
```

**restore receipt:** quoted restored line contains `**`stale-landing`**`; `git status --porcelain` after all three restores (see BP-3).

**raw green** (batched whole-file run after all restores — pytest exit 0):
```
........................................................................ [ 60%]
...............................................                          [100%]
119 passed in 2.01s
```

---

## BP-2 — `sweep-supersede-unsupported` doc↔code pin (`test_sweep_supersede_unsupported_refusal_token_in_round_driver_doc`)

- **guarded element:** `plugins/superheroes/lib/tests/test_ssot_drift.py::test_sweep_supersede_unsupported_refusal_token_in_round_driver_doc`
- **axis:** `round-driver.md` still names the literal `sweep-supersede-unsupported` token that `round_driver._cmd_record_result_locked` emits

**neutralization:** in `round-driver.md` sweep-supersede block, change token to near-miss `sweep-supersede-unsuppored`:
```markdown
`--expect-sha256` returns the literal refusal **`sweep-supersede-unsuppored`**. Supersede is
```

**raw red** (pytest exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_sweep_supersede_unsupported_refusal_token_in_round_driver_doc ______
plugins/superheroes/lib/tests/test_ssot_drift.py:253: in test_sweep_supersede_unsupported_refusal_token_in_round_driver_doc
    assert "sweep-supersede-unsupported" in doc, (
E   AssertionError: round-driver.md: must name the `sweep-supersede-unsupported` refusal that record-result --sweep returns when combined with --supersede or --expect-sha256
E   assert 'sweep-supersede-unsupported' in '# Contents\n\n- [The one entrypoint](#the-one-entrypoint)\n- [next / submit protocol](#next--submit-protocol)\n- [che...ver.py` and the PARITY receipt in `test_retry_budget_parity.py`.\nTreat `round_driver.py` as the contract of record.\n'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_sweep_supersede_unsupported_refusal_token_in_round_driver_doc
1 failed in 0.46s
```

**restore:** revert token to `sweep-supersede-unsupported`:
```markdown
`--expect-sha256` returns the literal refusal **`sweep-supersede-unsupported`**. Supersede is
```

**restore receipt:** see BP-3.

**raw green:** see BP-1 batched output (`119 passed in 2.01s`).

---

## BP-3 — `unknown-seat` addressed-seat doc↔code pin (`test_unknown_seat_addressed_seat_refusal_token_in_round_driver_doc`)

- **guarded element:** `plugins/superheroes/lib/tests/test_ssot_drift.py::test_unknown_seat_addressed_seat_refusal_token_in_round_driver_doc`
- **axis:** `round-driver.md` still names the literal `unknown-seat` token (addressed-seat meaning) distinct from `stale-landing`

**neutralization:** in `round-driver.md` gap-sweep trap paragraph, change token to near-miss `unkown-seat`:
```markdown
files: **move only the stray file(s) aside — never delete — and re-run the sweep**. **`unkown-seat`**
```

**raw red** (pytest exit 1; first capture raced with parallel restore and falsely passed — re-run receipt):
```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_unknown_seat_addressed_seat_refusal_token_in_round_driver_doc ______
plugins/superheroes/lib/tests/test_ssot_drift.py:271: in test_unknown_seat_addressed_seat_refusal_token_in_round_driver_doc
    assert "unknown-seat" in doc, (
E   AssertionError: round-driver.md: must name the `unknown-seat` refusal for an addressed seat (`record-result --seat <key>`) that is not on the current roster — distinct from the sweep's `stale-landing` stray-file refusal
E   assert 'unknown-seat' in '# Contents\n\n- [The one entrypoint](#the-one-entrypoint)\n- [next / submit protocol](#next--submit-protocol)\n- [che...ver.py` and the PARITY receipt in `test_retry_budget_parity.py`.\nTreat `round_driver.py` as the contract of record.\n'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_unknown_seat_addressed_seat_refusal_token_in_round_driver_doc
1 failed in 0.49s
```

**restore:** revert token to `unknown-seat`:
```markdown
files: **move only the stray file(s) aside — never delete — and re-run the sweep**. **`unknown-seat`**
```

**restore receipt** (`git status --porcelain` after all three restores):
```
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_ssot_drift.py
 M plugins/superheroes/skills/review-code/reference/round-driver.md
```

**raw green:** see BP-1 batched output (`119 passed in 2.01s`).

---

## Orchestrator re-verification on the final head (`5dd8ead5`)

The three pins above were proved by the WO-C implementer at that order's base. The orchestrator
reflowed the trap paragraph at integration, so all three were re-proved on the **final** doc text —
each neutralization a targeted reversible edit to `round-driver.md`, reverted with its inverse, each
red run selected by exact test name. Each token appears exactly once in the doc (`grep -c`: 1, 1, 1),
so each neutralization removes the only occurrence.

| Proof | Doc neutralization | Red |
| --- | --- | --- |
| BP-1 | `**`stale-landing`**` → `**`stale-landng`**` | `test_stale_landing_refusal_token_in_round_driver_doc` — 1 failed in 0.33s |
| BP-2 | `**`sweep-supersede-unsupported`**` → `**`sweep-supersede-unsuppored`**` | `test_sweep_supersede_unsupported_refusal_token_in_round_driver_doc` — 1 failed in 0.24s |
| BP-3 | `**`unknown-seat`**` → `**`unknown-set`**` | `test_unknown_seat_addressed_seat_refusal_token_in_round_driver_doc` — 1 failed in 0.22s |

Green after all three restores, `git status --porcelain` showing no modification to
`round-driver.md`: `253 passed in 1.17s` (command as quoted in `wo_A_1131.md`).
