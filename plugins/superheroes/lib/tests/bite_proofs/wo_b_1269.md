# WO-B (#1269) bite-proof — allowlist hard shell (G1 entry, G2 spawn, R1 pre-upgrade)

**Provenance:** cursor-agent / composer-2.5

## Guarded elements

| ID | Guarded element | Axis | Neutralization covers paths |
|---|---|---|---|
| BP-B-G1 | `_dispatch_allowlist_validate` / G1 entry gate | off-allowlist fresh dispatch refuses before run-open with allowlist named | 1–5 (see table below) |
| BP-B-G2 | `_spawn_allowlist_verdict` / G2 spawn gate | continuation, retry, and run-child re-entry refuse off-allowlist journal seat before Popen | 6–8 |
| BP-B-R1 | `_spawn_allowlist_verdict` / R1 pre-upgrade | pre-upgrade run-opened without resolvedInputs refuses before spawn | 9 |

### Nine path refusal texts (load-bearing, guards restored)

| Path | Entry | Refusal text |
|---|---|---|
| 1 | `dispatch-review` CLI fresh | `model 'gpt-5.3-codex-high' is not on the reviewer/codex allowlist [(gpt-5.6-terra, high), (gpt-5.6-sol, high), (gpt-5.6-sol, xhigh)] — an unlisted model is a park, not a pick (#600). Pick a listed model or amend lib/model_registry.py.` |
| 2 | `dispatch-write` CLI fresh | `model 'gpt-5.3-codex-high' is not on the implementer/codex allowlist [(gpt-5.6-terra, high), (gpt-5.6-sol, high), (gpt-5.6-sol, xhigh)] — an unlisted model is a park, not a pick (#600). Pick a listed model or amend lib/model_registry.py.` |
| 3 | `dispatch-review --mode brief-check` | `model 'gpt-5.3-codex-high' is not on the brief-check/codex allowlist [(gpt-5.6-sol, xhigh)] — an unlisted model is a park, not a pick (#600). Pick a listed model or amend lib/model_registry.py.` |
| 4a | library `dispatch_review` | same as path 1 |
| 4b | library `dispatch_write` | same as path 2 |
| 5 | `dispatch_guard check` | `model 'gpt-5.3-codex-high' is not on the implementer/cursor allowlist [(composer-2.5, None), (cursor-grok-4.6, xhigh)] — an unlisted model is a park, not a pick (#600). Pick a listed model or amend lib/model_registry.py.` |
| 6 | continuation spawn | same G2 text as path 8 |
| 7 | retry spawn | same G2 text as path 8 |
| 8 | direct `run-child` / `_run_engine_files` | `model 'gpt-5.3-codex-high' is not on the reviewer/codex allowlist [(gpt-5.6-terra, high), (gpt-5.6-sol, high), (gpt-5.6-sol, xhigh)] — an unlisted model is a park, not a pick (#600). Pick a listed model or amend lib/model_registry.py.` |
| 9 | pre-upgrade journal (R1) | `run seat cannot be established — run-opened record lacks resolvedInputs; re-open the run with a fresh dispatch that persists resolvedInputs (engine/model/effort/role snapshot at open)` |

---

## BP-B-G1 — entry gate before argv composition

- **axis:** off-allowlist fresh dispatch refuses before run-open with allowlist named

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `_dispatch_allowlist_validate`):
```python
def _dispatch_allowlist_validate(role, vendor, model, effort):
    return {"ok": True, "reason": None, "allowlist": [], "allowlist_pairs": []}  # bite-proof neutralization WO-B G1
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch_write.py::test_entry_allowlist_refuses_off_allowlist_write_library -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_entry_allowlist_refuses_off_allowlist_write_library ___________

tmp_path = PosixPath('...')

    def test_entry_allowlist_refuses_off_allowlist_write_library(tmp_path):
        ...
>       _assert_allowlist_refusal(res)
E       AssertionError: assert (None or 'allowlist' in 'engine-config:unregistered-engine-model')

plugins/superheroes/lib/tests/test_engine_dispatch_write.py:2530: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch_write.py::test_entry_allowlist_refuses_off_allowlist_write_library
1 failed in 0.44s
```

**restore:** remove the early `return {"ok": True, ...}` line; function body begins with `try:` / `dispatch_guard.validate` again.

**restore receipt:** `_dispatch_allowlist_validate` opens with `try:` and calls `dispatch_guard.validate` — neutralization line removed.

**raw green:**
```
.                                                                        [100%]
1 passed in 0.40s
```

---

## BP-B-G2 — spawn gate on journal seat

- **axis:** run-child re-entry (representative for paths 6–8) refuses off-allowlist snapshot before engine Popen

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `_spawn_allowlist_verdict`):
```python
def _spawn_allowlist_verdict(opened, *, journal_corrupt=False):
    """G2/R1 spawn gate — validate seat from journal resolvedInputs snapshot."""
    return {"ok": True, "reason": None, "allowlist": [], "allowlist_pairs": []}  # bite-proof neutralization WO-B G2
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_child_spawn_gate_refuses_off_allowlist_snapshot -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_run_child_spawn_gate_refuses_off_allowlist_snapshot ___________
...
>       assert ended.get("exit") == 127
E       AssertionError: assert 0 == 127
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_child_spawn_gate_refuses_off_allowlist_snapshot
1 failed in 27.55s
```

**restore:** remove the early `return {"ok": True, ...}` line; function body begins with `if journal_corrupt:` again.

**restore receipt:** `_spawn_allowlist_verdict` opens with `if journal_corrupt:` — neutralization line removed.

**raw green:**
```
.                                                                        [100%]
1 passed in 0.42s
```

---

## BP-B-R1 — pre-upgrade journal without resolvedInputs

- **axis:** pre-upgrade run-opened without resolvedInputs refuses before spawn

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `_spawn_allowlist_verdict` resolvedInputs branch):
```python
    snapshot = opened.get("resolvedInputs")
    if not isinstance(snapshot, dict):
        return {"ok": True, "reason": None, "allowlist": [], "allowlist_pairs": []}  # bite-proof neutralization WO-B R1
```
(replaces the `run seat cannot be established — run-opened record lacks resolvedInputs` refusal block)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch.py::test_spawn_gate_refuses_pre_upgrade_journal_without_resolved_inputs -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_____ test_spawn_gate_refuses_pre_upgrade_journal_without_resolved_inputs ______
...
>       ok, detail = ED._spawn_attempt(run_dir, state, 1, run_engine=FakeRunner([]))
...
E           AssertionError: fake called too many times
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_spawn_gate_refuses_pre_upgrade_journal_without_resolved_inputs
1 failed in 0.56s
```

**restore:**
```python
    snapshot = opened.get("resolvedInputs")
    if not isinstance(snapshot, dict):
        return {
            "ok": False,
            "reason": (
                "run seat cannot be established — run-opened record lacks resolvedInputs; "
                "re-open the run with a fresh dispatch that persists resolvedInputs "
                "(engine/model/effort/role snapshot at open)"
            ),
            "allowlist": [],
            "allowlist_pairs": [],
        }
```

**raw green:**
```
.                                                                        [100%]
1 passed in 0.50s
```

---

## Re-verification on the chokepoint head (2026-09-16, orchestrator)

Every element in this record was re-run on the head that carries the role chokepoint, because the
code each proof guards was rewritten after the record was first written. Two results, one of which
is a correction:

- **BP-B-G1 — the recorded neutralization site is stale.** Removing the body of
  `engine_dispatch._dispatch_allowlist_validate` no longer turns the proving tests red: that function
  is now a thin caller, and the gate itself lives in `seat_bundle.resolve_entry`'s allowlist leg.
  Re-proved at the live site — neutralizing the allowlist leg in `resolve_entry` (returning a
  synthetic `ok: True` bundle instead of consulting `dispatch_guard`) turned **both**
  `test_entry_allowlist_refuses_off_allowlist_write_library` and
  `test_entry_allowlist_refuses_off_allowlist_write_cli` red (`2 failed in 0.54s`), and the inverse
  edit turned both green (`2 passed in 0.38s`). The element still bites; the record's site moved.
- **BP-B-G2 — same element, same proving test as the chokepoint record's BP-4.** Re-proved there:
  neutralizing the `resolve_entry` call inside `_spawn_allowlist_verdict` turned
  `test_run_child_spawn_gate_refuses_off_allowlist_snapshot` red (`1 failed in 24.80s`); the inverse
  edit turned it green (`1 passed in 0.44s`).
- **BP-B-R1 — unchanged and re-proved at its recorded site.** Neutralizing the
  resolvedInputs-absent branch turned `test_spawn_gate_refuses_pre_upgrade_journal_without_resolved_inputs`
  red (`1 failed in 0.58s`); the inverse edit turned it green (`1 passed in 0.42s`).

Each probe was a targeted edit applied through the editor and reverted by its inverse edit, with the
tree confirmed clean (`git status --porcelain` empty) after every restore.
