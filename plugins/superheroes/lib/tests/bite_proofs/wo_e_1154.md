# WO-E (#1154) bite-proofs — safety-machinery route pin (legs 4–7)

## BP-E1 — `test_doctrine_family_list_matches_pinned_roster`

**Guarded element:** leg 4 — axis: *the doctrine's list is the roster, and every member exists*.

**Neutralization:** deleted one bullet from the doctrine's family list in `review-discipline.md`:
```markdown
- `hooks/owner_authority_gate.py` (the PreToolUse gate hook)
```

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_doctrine_family_list_matches_pinned_roster -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_doctrine_family_list_matches_pinned_roster ________________

    def test_doctrine_family_list_matches_pinned_roster():
        extracted = _extract_doctrine_family_paths()
        assert extracted, (
            f"{_HOME}: doctrine family extractor yielded empty set — "
            "vacuous pass if compared to pinned roster"
        )
>       assert extracted == _PINNED_FAMILY_ROSTER, (
            f"{_HOME}: doctrine family list {sorted(extracted)!r} "
            f"!= pinned roster {sorted(_PINNED_FAMILY_ROSTER)!r}"
        )
E       AssertionError: rubric/review-discipline.md: doctrine family list ['lib/owner_authority.py', 'reference/owner-authority-allowlist.md'] != pinned roster ['hooks/owner_authority_gate.py', 'lib/owner_authority.py', 'reference/owner-authority-allowlist.md']
E       assert {'lib/owner_a...allowlist.md'} == frozenset({'h...llowlist.md'})
E         
E         Extra items in the right set:
E         'hooks/owner_authority_gate.py'
E         Use -v to get more diff

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:220: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_doctrine_family_list_matches_pinned_roster
1 failed in 0.43s
```

**Restore:** re-added the deleted bullet:
```markdown
- `hooks/owner_authority_gate.py` (the PreToolUse gate hook)
```

**Restore receipt (supplementary `git status --porcelain`):** (empty)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.19s
```

---

## BP-E2 — `test_gate_wiring_edges_hold` (edge 1 — registration)

**Guarded element:** leg 5 edge 1 — axis: *PreToolUse hook registration references the gate hook*.

**Neutralization:** changed `owner_authority_gate.py` to `owner_authority_gate_X.py` in the `hooks.json` PreToolUse command string.

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_wiring_edges_hold -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________________ test_gate_wiring_edges_hold __________________________

    def test_gate_wiring_edges_hold():
        hooks_text = _read(_HOOKS_JSON)
        try:
            hooks_data = json.loads(hooks_text)
        except json.JSONDecodeError as exc:
            raise AssertionError(
                f"{_HOOKS_JSON}: unparseable JSON — {exc}"
            ) from exc
        pre_tool_use = hooks_data.get("hooks", {}).get("PreToolUse", [])
        registration_found = False
        for entry in pre_tool_use:
            for hook in entry.get("hooks", []):
                command = hook.get("command", "")
                if "hooks/owner_authority_gate.py" in command:
                    registration_found = True
                    break
            if registration_found:
                break
>       assert registration_found, (
            f"{_HOOKS_JSON}: PreToolUse hook command must reference "
            "hooks/owner_authority_gate.py"
        )
E       AssertionError: hooks/hooks.json: PreToolUse hook command must reference hooks/owner_authority_gate.py
E       assert False

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:249: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_wiring_edges_hold
1 failed in 0.45s
```

**Restore:** reverted `owner_authority_gate_X.py` → `owner_authority_gate.py` in the PreToolUse command.

**Restore receipt (supplementary `git status --porcelain`):** (empty)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.37s
```

---

## BP-E3 — `test_gate_wiring_edges_hold` (edge 2 — hook→classifier)

**Guarded element:** leg 5 edge 2 — axis: *the gate hook imports the classifier*.

**Neutralization:** renamed the import line in `hooks/owner_authority_gate.py`:
```python
            import owner_authority_X
```
(was `import owner_authority`)

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_wiring_edges_hold -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________________ test_gate_wiring_edges_hold __________________________

    def test_gate_wiring_edges_hold():
        ...
        gate_text = _read(_OWNER_AUTHORITY_GATE)
>       assert re.search(r"\bimport owner_authority\b", gate_text), (
            f"{_OWNER_AUTHORITY_GATE}: must import owner_authority (hook→classifier edge)"
        )
E       AssertionError: hooks/owner_authority_gate.py: must import owner_authority (hook→classifier edge)
E       assert None

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:255: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_wiring_edges_hold
1 failed in 0.15s
```

**Restore:** reverted `import owner_authority_X` → `import owner_authority`.

**Restore receipt (supplementary `git status --porcelain`):** (empty)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.34s
```

---

## BP-E4 — `test_gate_wiring_edges_hold` (edge 3 — classifier→allowlist)

**Guarded element:** leg 5 edge 3 — axis: *the classifier cites the allowlist reference*.

**Neutralization:** changed the allowlist citation string in `lib/owner_authority.py`:
```python
                   "reference/owner-authority-allowlist_X.md")
```
(was `reference/owner-authority-allowlist.md`)

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_wiring_edges_hold -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________________ test_gate_wiring_edges_hold __________________________

    def test_gate_wiring_edges_hold():
        ...
        classifier_text = _read(_OWNER_AUTHORITY)
>       assert "reference/owner-authority-allowlist.md" in classifier_text, (
            f"{_OWNER_AUTHORITY}: must cite reference/owner-authority-allowlist.md "
            "(classifier→allowlist edge)"
        )
E       AssertionError: lib/owner_authority.py: must cite reference/owner-authority-allowlist.md (classifier→allowlist edge)
E       assert 'reference/owner-authority-allowlist.md' in '...'

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:260: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_wiring_edges_hold
1 failed in 0.34s
```

**Restore:** reverted `reference/owner-authority-allowlist_X.md` → `reference/owner-authority-allowlist.md`.

**Restore receipt (supplementary `git status --porcelain`):** (empty)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.25s
```

---

## BP-E5 — `test_classifier_direct_dependencies_are_classified`

**Guarded element:** leg 6 — axis: *no unclassified module can change the gate's verdict* (direct lib-local import roster).

**Neutralization:** added a new lib-local import inside `allow_file_path` in `lib/owner_authority.py`:
```python
    import core_md
    import mode_registry
```

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_classifier_direct_dependencies_are_classified -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_classifier_direct_dependencies_are_classified ______________

    def test_classifier_direct_dependencies_are_classified():
        direct = _direct_lib_local_imports(_OWNER_AUTHORITY)
        expected = {"mode_registry"}
>       assert direct == expected, (
            f"{_OWNER_AUTHORITY}: direct lib-local imports {sorted(direct)!r} "
            f"!= expected {sorted(expected)!r}"
        )
E       AssertionError: lib/owner_authority.py: direct lib-local imports ['core_md', 'mode_registry'] != expected ['mode_registry']
E       assert {'core_md', 'mode_registry'} == {'mode_registry'}
E         
E         Extra items in the left set:
E         'core_md'

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:272: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_classifier_direct_dependencies_are_classified
1 failed in 0.64s
```

**Restore:** removed the `import core_md` line from `allow_file_path`.

**Restore receipt (supplementary `git status --porcelain`):** (empty)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.34s
```

---

## BP-E6 — `test_every_family_member_is_refused_to_the_fixer` (member `owner_authority.py`)

**Guarded element:** leg 7 — axis: *refusal* for family member `lib/owner_authority.py`.

**Neutralization:** removed `"owner_authority.py"` from `SAFETY_MACHINERY` in `lib/escalation.py`:
```python
    "owner_authority.py",     # owner-authority gate core (merge/release/publish refusals)
```
(deleted line; also removed `owner-authority-allowlist.md` line in the same edit cluster — leg 7 failed on `owner_authority.py` first)

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_every_family_member_is_refused_to_the_fixer -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_every_family_member_is_refused_to_the_fixer _______________

    def test_every_family_member_is_refused_to_the_fixer():
        band_roots = [PLUGIN]
        for rel in sorted(_PINNED_FAMILY_ROSTER):
            abs_path = os.path.join(PLUGIN, rel)
>           assert ESC.is_safety_machinery(abs_path, band_roots) is True, (
                f"family member {rel!r} must be refused to the fixer "
                f"(is_safety_machinery({abs_path!r}, {band_roots!r}))"
            )
E           AssertionError: family member 'lib/owner_authority.py' must be refused to the fixer (is_safety_machinery('/Users/zwrose/.superheroes-worktrees/superheroes/1154-D/plugins/superheroes/lib/owner_authority.py', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-D/plugins/superheroes']))
E           assert False is True

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:282: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_every_family_member_is_refused_to_the_fixer
1 failed in 0.61s
```

**Restore:** re-added `"owner_authority.py"` and `"owner-authority-allowlist.md"` lines to `SAFETY_MACHINERY`.

**Restore receipt (supplementary `git status --porcelain`):** (empty)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.34s
```

---

## BP-E7 — `test_every_family_member_is_refused_to_the_fixer` (member `owner_authority_gate.py`)

**Guarded element:** leg 7 — axis: *refusal* for family member `hooks/owner_authority_gate.py`.

**Neutralization:** removed `"owner_authority_gate.py"` from `SAFETY_MACHINERY` in `lib/escalation.py`:
```python
    "owner_authority_gate.py",# PreToolUse hook wrapper that invokes owner_authority
```
(deleted line)

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_every_family_member_is_refused_to_the_fixer -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_every_family_member_is_refused_to_the_fixer _______________

    def test_every_family_member_is_refused_to_the_fixer():
        band_roots = [PLUGIN]
        for rel in sorted(_PINNED_FAMILY_ROSTER):
            abs_path = os.path.join(PLUGIN, rel)
>           assert ESC.is_safety_machinery(abs_path, band_roots) is True, (
                f"family member {rel!r} must be refused to the fixer "
                f"(is_safety_machinery({abs_path!r}, {band_roots!r}))"
            )
E           AssertionError: family member 'hooks/owner_authority_gate.py' must be refused to the fixer (is_safety_machinery('/Users/zwrose/.superheroes-worktrees/superheroes/1154-D/plugins/superheroes/hooks/owner_authority_gate.py', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-D/plugins/superheroes']))
E           assert False is True

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:282: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_every_family_member_is_refused_to_the_fixer
1 failed in 0.45s
```

**Restore:** re-added `"owner_authority_gate.py"` line to `SAFETY_MACHINERY`.

**Restore receipt (supplementary `git status --porcelain`):** (empty)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.25s
```

---

## BP-E8 — `test_every_family_member_is_refused_to_the_fixer` (member `owner-authority-allowlist.md`)

**Guarded element:** leg 7 — axis: *refusal* for family member `reference/owner-authority-allowlist.md`.

**Neutralization:** removed `"owner-authority-allowlist.md"` from `SAFETY_MACHINERY` in `lib/escalation.py`:
```python
    "owner-authority-allowlist.md",  # named owner-authority-gate-family member — fixer must refuse or the carve-out is decorative on this member
```
(deleted line)

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_every_family_member_is_refused_to_the_fixer -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_every_family_member_is_refused_to_the_fixer _______________

    def test_every_family_member_is_refused_to_the_fixer():
        band_roots = [PLUGIN]
        for rel in sorted(_PINNED_FAMILY_ROSTER):
            abs_path = os.path.join(PLUGIN, rel)
>           assert ESC.is_safety_machinery(abs_path, band_roots) is True, (
                f"family member {rel!r} must be refused to the fixer "
                f"(is_safety_machinery({abs_path!r}, {band_roots!r}))"
            )
E           AssertionError: family member 'reference/owner-authority-allowlist.md' must be refused to the fixer (is_safety_machinery('/Users/zwrose/.superheroes-worktrees/superheroes/1154-D/plugins/superheroes/reference/owner-authority-allowlist.md', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-D/plugins/superheroes']))
E           assert False is True

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:282: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_every_family_member_is_refused_to_the_fixer
1 failed in 0.27s
```

**Restore:** re-added `"owner-authority-allowlist.md"` line to `SAFETY_MACHINERY`.

**Restore receipt (supplementary `git status --porcelain`):** (empty)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.20s
```
