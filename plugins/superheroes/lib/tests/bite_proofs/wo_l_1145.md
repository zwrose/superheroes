# WO-L (#1145) bite-proofs — emit-imperative regression guard must bite

## BP1 — emit negative assertions fail on forbidden imperative regression

**Guarded element:** `test_panel_stdout_delivery_never_imperates_literal_example_emit` — axis: negative assertions built from `RFS.example_findings_object()` must fail when production text re-adds `Emit `<example>` as your final stdout`.

**Neutralization:** in `_panel_stdout_delivery_text`, inserted `Emit `%s` as your final stdout.` with `json.dumps(review_findings_schema.example_findings_object(), sort_keys=True)` after `example_prompt_block()` (`plugins/superheroes/lib/round_orders.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_orders.py::test_panel_stdout_delivery_never_imperates_literal_example_emit -q
```

**Red run (emit assertion — not format-only leg):**
```
=================================== FAILURES ===================================
_______ test_panel_stdout_delivery_never_imperates_literal_example_emit ________

>       assert f"emit `{example_json}`".lower() not in text.lower()
E       assert 'emit `{"fin...el-1145]"]}`' not in 'you are rev... sandbox).\n'
E         
E         'emit `{"findings":...-sentinel-1145]"]}`' is contained here:
E           ...Full output truncated (2 lines hidden), use '-vv' to show

plugins/superheroes/lib/tests/test_round_orders.py:802: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_orders.py::test_panel_stdout_delivery_never_imperates_literal_example_emit
1 failed in 0.12s
```

**Restore:** removed the inserted `Emit `%s` as your final stdout.` line from `_panel_stdout_delivery_text` lines.

**Restore receipt:** `_panel_stdout_delivery_text` again begins with `review_findings_schema.example_prompt_block(),` then blank line then `Emit a single JSON object of this shape...`.

**Green run:**
```
1 passed in 0.12s
```
