# WO-B (#1145) bite-proofs — tolerant grader at one chokepoint

## BP1 — sentinel refusal (B1)

**Guarded element:** `test_parse_result_review_near_copy_example_sentinel_is_hollow` — axis: near-copy example sentinel refuses engagement through parse_result.

**Neutralization:** removed `member_carries_sentinel` early return from `_finding_is_substantive` in `engine_adapter.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_adapter.py::test_parse_result_review_near_copy_example_sentinel_is_hollow' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_adapter.py::test_parse_result_review_near_copy_example_sentinel_is_hollow
>       assert EA.parse_result("codex", "review", stdout) == _HOLLOW_MEMBER_MALFORMED
E       AssertionError: assert {'findings': ...': 'findings'} == {'ok': False,... 'unreadable'}
1 failed in 0.59s
```

**Restore:** reinstated `if review_findings_schema.member_carries_sentinel(obj): return False` block.

**Restore receipt:** `if review_findings_schema.member_carries_sentinel(obj):\n        return False`

**Green run:**
```
1 passed in 0.51s
```

---

## BP2 — severity-enum leg (fail-closed censused gate)

**Guarded element:** `test_parse_result_review_off_scale_severity_with_censused_body_is_hollow[critical]` — axis: off-scale severity cannot unlock censused substance keys.

**Neutralization:** replaced `return review_findings_schema.member_is_engaged(obj)` with `return True` in `_finding_is_substantive`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_adapter.py::test_parse_result_review_off_scale_severity_with_censused_body_is_hollow[critical]' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_adapter.py::test_parse_result_review_off_scale_severity_with_censused_body_is_hollow[critical]
>       assert EA.parse_result("codex", "review", stdout) == _HOLLOW_MEMBER_MALFORMED
E       AssertionError: assert {'findings': ...': 'findings'} == {'ok': False,... 'unreadable'}
1 failed in 1.64s
```

**Restore:** reinstated `return review_findings_schema.member_is_engaged(obj)`.

**Restore receipt:** `return review_findings_schema.member_is_engaged(obj)`

**Green run:**
```
(covered by full-suite green; representative node passes after restore)
```

---

## BP3 — normalization-is-additive (B2)

**Guarded element:** `test_parse_result_review_normalize_member_is_additive` — axis: accepted members gain canonical keys from synonyms without dropping originals.

**Neutralization:** removed `f = review_findings_schema.normalize_member(f)` from `_scrub_findings`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_adapter.py::test_parse_result_review_normalize_member_is_additive' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_adapter.py::test_parse_result_review_normalize_member_is_additive
>       assert member["body"] == "issue found"
E       KeyError: 'body'
1 failed in 1.53s
```

**Restore:** reinstated `f = review_findings_schema.normalize_member(f)` after substantive check.

**Restore receipt:** `f = review_findings_schema.normalize_member(f)`

**Green run:**
```
1 passed in 1.50s
```

---

## BP4 — registry-based recognition (B3)

**Guarded element:** `test_review_payload_shape_valid_top_level_ruling_returns_none` — axis: valid ruling payloads diagnose clean via `_recognised_review_kinds` registry.

**Neutralization:** replaced registry diagnostic dispatch with unconditional `SHAPE_OBJECT_WITHOUT_FINDINGS` return for single-match objects in `review_payload_shape`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_adapter.py::test_review_payload_shape_valid_top_level_ruling_returns_none' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_adapter.py::test_review_payload_shape_valid_top_level_ruling_returns_none
E       assert {'parsed': 'object-without-findings', ...} is None
1 failed in 1.86s
```

**Restore:** reinstated `_REVIEW_PAYLOAD_SHAPE_DIAGNOSTICS[matched[0]](obj)` dispatch block.

**Restore receipt:**
```
diag = _REVIEW_PAYLOAD_SHAPE_DIAGNOSTICS[matched[0]](obj)
if diag is not None:
    return diag
return None
```

**Green run:**
```
1 passed in 1.16s
```

---

## BP5 — parser/diagnostic agreement (B4)

**Guarded element:** `test_findings_member_parse_and_shape_diagnostic_agree[member0-True]` — axis: hollow-member predicate shared between parse path and shape diagnostic.

**Neutralization:** replaced `_findings_list_has_hollow_member` body with `return False`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_adapter.py::test_findings_member_parse_and_shape_diagnostic_agree[member0-True]' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_adapter.py::test_findings_member_parse_and_shape_diagnostic_agree[member0-True]
>           assert _findings_member_hollow_via_shape(stdout) is expect_hollow
E           assert False is True
1 failed in 2.31s
```

**Restore:** reinstated `any(isinstance(x, dict) and not _finding_is_substantive(x) for x in findings)` loop.

**Restore receipt:**
```
return any(isinstance(x, dict) and not _finding_is_substantive(x) for x in findings)
```

**Green run:**
```
1 passed in 2.98s
```

**Post-restore `git status --porcelain`:**
```
 M plugins/superheroes/lib/engine_adapter.py
 M plugins/superheroes/lib/tests/test_engine_adapter.py
```
