# WO-A (#1264) bite-proof — worktree guard calibration probe relocation

## BP-A1 — `test_issue_682_checkout_revert_wipe_denies_on_dirty_tree`

**Guarded element:** `worktree_guard.classify` — axis: *refusal* on a calibrated dirty tree for `git checkout --` (checkout-revert wipe).

**Neutralization:** in `lib/worktree_guard.py` `classify`, forced the calibration probe result to permissive fall-open:
```python
            state = "uncalibrated"
```
(was `state = mode_registry.calibration_state(cwd)`)

Placed at the classify call site rather than inside `mode_registry.calibration_state` because the edge test monkeypatches `mode_registry.calibration_state` to `"calibrated"`, which would mask a probe-body neutralization.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_worktree_guard.py::test_issue_682_checkout_revert_wipe_denies_on_dirty_tree -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_issue_682_checkout_revert_wipe_denies_on_dirty_tree ___________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-427/test_issue_682_checkout_revert0')
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x1067ab490>

    def test_issue_682_checkout_revert_wipe_denies_on_dirty_tree(tmp_path, monkeypatch):
        """The recorded wipe: uncommitted delivery at a path must not be destroyed."""
        repo = _init_repo(tmp_path / "repo")
        _commit_file(repo, "f.txt", "committed\n")
        with open(os.path.join(repo, "f.txt"), "w") as f:
            f.write("PRIOR_UNCOMMITTED_DELIVERY")
        _calibrated(monkeypatch)
        decision, reason = wg.classify("git checkout -- f.txt", repo)
>       assert decision == "deny"
E       AssertionError: assert 'allow' == 'deny'
E         
E         - deny
E         + allow

plugins/superheroes/lib/tests/test_worktree_guard.py:56: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_worktree_guard.py::test_issue_682_checkout_revert_wipe_denies_on_dirty_tree
1 failed in 1.36s
```

**Restore:** reverted to:
```python
            state = mode_registry.calibration_state(cwd)
```

**Restore receipt (supplementary `git status --porcelain`):** (empty for `plugins/superheroes/lib/worktree_guard.py` after restore)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.88s
```
