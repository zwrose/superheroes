# WO-c6A (#1107) bite-proofs — one source of truth for prior comments

## BP-c6A1 — CLI materialization chokepoint

**Guarded element:** `round_driver.main` CLI `--prior-comments` acceptance
(`plugins/superheroes/lib/round_driver.py:8022`) — axis: validated prior comments must be
materialized to `$SESSION_DIR/prior-comments.json` before `config["priorComments"]` is set.

**Neutralization:** replaced the materialization gate with an unconditional assignment (skip
`_materialize_prior_comments`):

```python
                if isinstance(loaded, list):
                    overrides["priorComments"] = loaded  # BITE-PROOF-NEUTRALIZE-1107-c6A-1
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional -q
```

**Red run:**
```
F...FFF                                                                  [100%]
...
    def _assert_prior_comments_biconditional(session_dir, state):
        # axis: config["priorComments"] is non-None iff canonical prior-comments.json exists with equal contents
        canonical = _canonical_prior_path(session_dir)
        prior = state["config"].get("priorComments")
        file_exists = os.path.isfile(canonical)
        if prior is None:
            assert not file_exists
            return
>       assert file_exists
E       assert False

plugins/superheroes/lib/tests/test_prior_comments_mode.py:52: AssertionError
...
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[valid_list_elsewhere]
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[preexisting_canonical_replaced]
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[branch_mode_materialized]
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[materialization_write_failure]
4 failed, 3 passed in 9.96s
```

**Restore:** reinstated the materialization gate:

```python
                if isinstance(loaded, list):
                    if _materialize_prior_comments(args.session_dir, args.prior_comments, loaded):
                        overrides["priorComments"] = loaded
```

**Restore receipt:** post-restore `git status --porcelain plugins/superheroes/lib/round_driver.py`
showed only the order's real diff (no `BITE-PROOF-NEUTRALIZE` residue).

**Green run:**
```
7 passed in 9.97s
```

---

## BP-c6A2 — fail-closed direction on materialization failure

**Guarded element:** `round_driver.main` CLI `--prior-comments` acceptance
(`plugins/superheroes/lib/round_driver.py:8022-8023`) — axis: when materialization fails,
`config["priorComments"]` must stay unset so both consumers agree on unavailable.

**Neutralization:** called `_materialize_prior_comments` but set `priorComments` regardless of its
return value:

```python
                if isinstance(loaded, list):
                    _materialize_prior_comments(args.session_dir, args.prior_comments, loaded)  # BITE-PROOF-NEUTRALIZE-1107-c6A-2
                    overrides["priorComments"] = loaded
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[materialization_write_failure]' -q
```

**Red run:**
```
F                                                                        [100%]
...
    def _assert_prior_comments_biconditional(session_dir, state):
        ...
        if prior is None:
            assert not file_exists
            return
>       assert file_exists
E       assert False

plugins/superheroes/lib/tests/test_prior_comments_mode.py:52: AssertionError
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[materialization_write_failure]
1 failed in 2.31s
```

**Restore:** reinstated the fail-closed gate (inverse of neutralization).

**Restore receipt:** post-restore `git status --porcelain plugins/superheroes/lib/round_driver.py`
showed only the order's real diff (no `BITE-PROOF-NEUTRALIZE` residue).

**Green run:**
```
1 passed in 2.31s
```

---

## BP-c6A3 — PR-mode contradiction axis

**Guarded element:** `_resolve_prior_comments_path`
(`plugins/superheroes/lib/round_driver.py:5560-5561`) — axis: in PR mode, when the canonical
prior-comments file exists after CLI materialization, the resolver must return that path (never the
unavailable marker) so panel orders and the author-justification filter cannot disagree in one
receipt.

**Neutralization:** moved mode resolution ahead of the canonical-file early return and limited the
return to branch mode only (PR mode falls through to the unavailable marker even when the canonical
file exists):

```python
    path = _prior_comments_canonical_path(session_dir)
    cfg = state.get("config") if isinstance(state, dict) else {}
    meta = _session_meta(session_dir)
    mode_resolved = session_mode.resolve(meta, cfg)
    if os.path.isfile(path) and mode_resolved["mode"] == session_mode.MODE_BRANCH:  # BITE-PROOF-NEUTRALIZE-1107-c6A-3
        return path
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional -k "pr_" -q
```

**Red run:**
```
F.F.                                                                     [100%]
...
            resolved = RD._resolve_prior_comments_path(session_dir, state)
>           assert resolved == canonical
E           AssertionError: assert '(prior-comme...omments.json)' == '/private/var...comments.json'
...
plugins/superheroes/lib/tests/test_prior_comments_mode.py:209: AssertionError
FAILED plugins/superheroes/lib/tests/test_prior_comments_config_canonical_biconditional[pr_valid_list_elsewhere]
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[pr_preexisting_canonical_replaced]
2 failed, 2 passed, 7 deselected in 6.55s
```

**Restore:** reinstated the mode-independent canonical early return:

```python
    path = _prior_comments_canonical_path(session_dir)
    if os.path.isfile(path):
        return path
```

**Restore receipt:** post-restore `git status --porcelain plugins/superheroes/lib/round_driver.py`
showed no diff (no `BITE-PROOF-NEUTRALIZE` residue).

**Green run:**
```
4 passed, 7 deselected in 15.55s
```

---

## BP-c6A4 — fresh-state-only refusal for `--prior-comments`

### Element 1 — the refusal itself

**Guarded element:** `round_driver._dispatch` CLI `--prior-comments` fresh-state guard
(`plugins/superheroes/lib/round_driver.py:8013-8017`) — axis: non-fresh state with
`--prior-comments` must refuse with `prior-comments-not-fresh-state` and exit 1.

**Neutralization:** removed the fresh-state guard block entirely (materialization unchanged).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_on_non_fresh_state_refused_preserves_canonical -q
```

**Red run:**
```
F                                                                        [100%]
...
        rc, out = _run_next_prior_comments_cli(capsys, session_dir, prior_b)
>       assert rc == 1
E       assert 0 == 1

plugins/superheroes/lib/tests/test_prior_comments_mode.py:289: AssertionError
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_on_non_fresh_state_refused_preserves_canonical
1 failed in 1.90s
```

**Restore:** reinstated the fresh-state guard before materialization.

**Restore receipt:** post-restore `git status --porcelain plugins/superheroes/lib/round_driver.py`
showed only the order's real diff (no `BITE-PROOF-NEUTRALIZE` residue).

**Green run:**
```
1 passed in 1.90s
```

### Element 2 — guard ordering (write nothing on refuse)

**Guarded element:** same guard block — axis: the fresh-state check must run **before**
`_materialize_prior_comments` so a refusal leaves the canonical file byte-identical.

**Neutralization:** moved the guard to **after** the materialization `try`/`except` block
(refusal still fires, but the canonical file is already clobbered):

```python
            try:
                with open(args.prior_comments, encoding="utf-8") as fh:
                    loaded = json.load(fh)
                if isinstance(loaded, list):
                    if _materialize_prior_comments(args.session_dir, args.prior_comments, loaded):
                        overrides["priorComments"] = loaded
            except (OSError, ValueError):
                pass
            # BITE-PROOF-NEUTRALIZE-1107-c6A-4-ordering: guard after materialization
            st_ok, st = load_state(args.session_dir)
            if not (st_ok and st is None):
                ...
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_on_non_fresh_state_refused_preserves_canonical -q
```

**Red run:**
```
F                                                                        [100%]
...
        with open(canonical, "rb") as fh:
>           assert fh.read() == before_bytes
E           assert b'[{"id":"b",...:"from B"}]\n' == b'[{"id":"a",...:"from A"}]\n'
E             
E             At index 8 diff: b'b' != b'a'

plugins/superheroes/lib/tests/test_prior_comments_mode.py:293: AssertionError
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_on_non_fresh_state_refused_preserves_canonical
1 failed in 2.64s
```

**Restore:** moved the guard back before materialization (inverse of neutralization).

**Restore receipt:** post-restore `git status --porcelain plugins/superheroes/lib/round_driver.py`
showed only the order's real diff (no `BITE-PROOF-NEUTRALIZE` residue).

**Green run:**
```
1 passed in 1.90s
```
