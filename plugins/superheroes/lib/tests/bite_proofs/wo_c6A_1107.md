# WO-c6A (#1107) bite-proofs — one source of truth for prior comments

## BP-c6A1 — CLI materialization chokepoint

**Guarded element:** `round_driver.main` CLI `--prior-comments` acceptance
(`plugins/superheroes/lib/round_driver.py:8022`) — axis: validated prior comments must be
materialized to `$SESSION_DIR/prior-comments.json` before `config["priorComments"]` is set.

**Neutralization:** replaced the materialization gate with an unconditional assignment (skip
`_materialize_prior_comments`):

```python
                if isinstance(loaded, list):
                    overrides["priorComments"] = loaded  # BITE-PROOF-NEUTRALIZE-1107-c6A-1
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional -q
```

**Red run:**
```
F...FFF                                                                  [100%]
...
    def _assert_prior_comments_biconditional(session_dir, state):
        # axis: config["priorComments"] is non-None iff canonical prior-comments.json exists with equal contents
        canonical = _canonical_prior_path(session_dir)
        prior = state["config"].get("priorComments")
        file_exists = os.path.isfile(canonical)
        if prior is None:
            assert not file_exists
            return
>       assert file_exists
E       assert False

plugins/superheroes/lib/tests/test_prior_comments_mode.py:52: AssertionError
...
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[valid_list_elsewhere]
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[preexisting_canonical_replaced]
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[branch_mode_materialized]
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[materialization_write_failure]
4 failed, 3 passed in 9.96s
```

**Restore:** reinstated the materialization gate:

```python
                if isinstance(loaded, list):
                    if _materialize_prior_comments(args.session_dir, args.prior_comments, loaded):
                        overrides["priorComments"] = loaded
```

**Restore receipt:** post-restore `git status --porcelain plugins/superheroes/lib/round_driver.py`
showed only the order's real diff (no `BITE-PROOF-NEUTRALIZE` residue).

**Green run:**
```
7 passed in 9.97s
```

---

## BP-c6A2 — fail-closed direction on materialization failure

**Guarded element:** `round_driver.main` CLI `--prior-comments` acceptance
(`plugins/superheroes/lib/round_driver.py:8022-8023`) — axis: when materialization fails,
`config["priorComments"]` must stay unset so both consumers agree on unavailable.

**Neutralization:** called `_materialize_prior_comments` but set `priorComments` regardless of its
return value:

```python
                if isinstance(loaded, list):
                    _materialize_prior_comments(args.session_dir, args.prior_comments, loaded)  # BITE-PROOF-NEUTRALIZE-1107-c6A-2
                    overrides["priorComments"] = loaded
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[materialization_write_failure]' -q
```

**Red run:**
```
F                                                                        [100%]
...
    def _assert_prior_comments_biconditional(session_dir, state):
        ...
        if prior is None:
            assert not file_exists
            return
>       assert file_exists
E       assert False

plugins/superheroes/lib/tests/test_prior_comments_mode.py:52: AssertionError
FAILED plugins/superheroes/lib/tests/test_prior_comments_mode.py::test_prior_comments_config_canonical_biconditional[materialization_write_failure]
1 failed in 2.31s
```

**Restore:** reinstated the fail-closed gate (inverse of neutralization).

**Restore receipt:** post-restore `git status --porcelain plugins/superheroes/lib/round_driver.py`
showed only the order's real diff (no `BITE-PROOF-NEUTRALIZE` residue).

**Green run:**
```
1 passed in 2.31s
```
