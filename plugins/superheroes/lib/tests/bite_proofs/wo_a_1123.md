# WO-A (#1123) bite-proofs — payload contracts below transport core

## BP-A1 — import-direction detector (`test_engine_adapter_import_closure_is_pinned`)

**Guarded element:** `engine_adapter` module-scope import closure — axis: no round-phase orchestration modules loaded.

**Neutralization:** added `import round_adapters  # noqa: E402` beside `import payload_contracts` in `plugins/superheroes/lib/engine_adapter.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_payload_contracts_layering.py::test_engine_adapter_import_closure_is_pinned -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_engine_adapter_import_closure_is_pinned _________________
...
E           Failed: engine_adapter import closure mismatch: unexpected=['circuit_breaker', 'loop_state', 'panel_tally', 'review_result', 'round_adapters', 'round_records', 'verification'] missing=[]

plugins/superheroes/lib/tests/test_payload_contracts_layering.py:83: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_payload_contracts_layering.py::test_engine_adapter_import_closure_is_pinned
1 failed in 0.99s
```

**Restore:** removed the `import round_adapters` line (inverse of neutralization).

**Restore receipt:** `payload_fault = payload_contracts.payload_fault` binding unchanged; `engine_adapter.py` carries only `import payload_contracts`, not `import round_adapters`.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.31s
```

---

## BP-A2 — re-export census (`test_round_adapters_reexports_are_the_same_objects`)

**Guarded element:** `round_adapters.payload_fault` re-export — axis: same object as `payload_contracts.payload_fault`, not a wrapper.

**Neutralization:** replaced binding with wrapper in `plugins/superheroes/lib/round_adapters.py`:
```python
def payload_fault(*a, **k):
    return payload_contracts.payload_fault(*a, **k)
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_payload_contracts_layering.py::test_round_adapters_reexports_are_the_same_objects -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_round_adapters_reexports_are_the_same_objects ___________
...
E           AssertionError: 'payload_fault' is not the same object on both modules
E           assert <function payload_fault at 0x106558040> is <function payload_fault at 0x106547d30>

plugins/superheroes/lib/tests/test_payload_contracts_layering.py:94: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_payload_contracts_layering.py::test_round_adapters_reexports_are_the_same_objects
1 failed in 0.09s
```

**Restore:** restored `payload_fault = payload_contracts.payload_fault`.

**Restore receipt:** quoted binding line restored: `payload_fault = payload_contracts.payload_fault`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.31s
```

---

## BP-A3 — single contract home (`test_grouping_contract_copy_census`)

**Guarded element:** P_SYNTHESIS grouping predicate string — axis: lives only in `payload_contracts.py`, not copied into `round_adapters.py`.

**Neutralization:** added comment `# member_ids-non-empty-strings` in the re-export block of `plugins/superheroes/lib/round_adapters.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_grouping_contract_copy_census -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________________ test_grouping_contract_copy_census ______________________
...
E       AssertionError: P_SYNTHESIS grouping rule home missing or duplicated: ['duplicate-home', 'engine_adapter-delegation']
E       assert 0 == 1

plugins/superheroes/lib/tests/test_ssot_drift.py:5186: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_grouping_contract_copy_census
1 failed in 0.26s
```

**Restore:** removed the `# member_ids-non-empty-strings` comment line.

**Restore receipt:** re-export block comment restored to `# Re-export payload-contract names from the single contract home (CONVENTIONS §11 Pattern 1).` only.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.31s
```
