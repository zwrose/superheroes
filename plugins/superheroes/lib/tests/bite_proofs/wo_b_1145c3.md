# WO-B (#1145 continuation-3) bite-proofs — kind-aware review result contract

## BP1 — kind-scoped example gate

**Guarded element:** `test_review_dispatch_fed_prompt_result_contract_unpinned_lists_all_kinds` — axis: unpinned fedPrompt must not contain findings example block.

**Neutralization:** widened example gate to `expected_result_kind in (None, "findings")` (`plugins/superheroes/lib/engine_dispatch.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_result_contract_unpinned_lists_all_kinds' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_result_contract_unpinned_lists_all_kinds
>       assert RFS.example_prompt_block() not in prompt_text
E       AssertionError: assert 'Format only...' not in 'You are a d...'
1 failed in 0.91s
```

**Restore:** reinstated `if expected_result_kind == "findings":` gate only.

**Restore receipt:** `if expected_result_kind == "findings":` (no `None` branch).

**Raw green:**
```
3 passed in 0.54s
```
(representative node included in post-restore batch with BP2/BP3 nodes)

---

## BP2 — unconditional contract append

**Guarded element:** `test_review_dispatch_fed_prompt_result_contract_unpinned_lists_all_kinds` — axis: every review dispatch fedPrompt carries the kind-aware result contract.

**Neutralization:** removed `fed_prompt += engine_adapter.REVIEW_RESULT_CONTRACT(expected_result_kind)` (`plugins/superheroes/lib/engine_dispatch.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_result_contract_unpinned_lists_all_kinds' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_result_contract_unpinned_lists_all_kinds
>       assert contract in prompt_text
E       AssertionError: assert 'Review result contract ...' in 'You are a dispatched ONE-SHOT ...'
1 failed in 0.92s
```

**Restore:** reinstated `fed_prompt += engine_adapter.REVIEW_RESULT_CONTRACT(expected_result_kind)` after the example gate.

**Restore receipt:**
```
            fed_prompt += engine_adapter.REVIEW_RESULT_CONTRACT(expected_result_kind)
```

**Raw green:** see BP1 post-restore batch (`3 passed in 0.54s`).

---

## BP3 — canary findings pin

**Guarded element:** `test_run_canary_pins_findings_expected_result_kind` — axis: `run_canary` passes `expected_result_kind="findings"` to the dispatch call.

**Neutralization:** removed `expected_result_kind="findings"` from the `dispatch(...)` call in `run_canary` (`plugins/superheroes/lib/seat_canary.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_seat_canary.py::test_run_canary_pins_findings_expected_result_kind' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_seat_canary.py::test_run_canary_pins_findings_expected_result_kind
>       assert seen.get("expected_result_kind") == "findings"
E       AssertionError: assert None == 'findings'
1 failed in 0.28s
```

**Restore:** reinstated `expected_result_kind="findings"` on the dispatch call.

**Restore receipt:** `expected_result_kind="findings",` present in `run_canary` dispatch kwargs.

**Raw green:** see BP1 post-restore batch (`3 passed in 0.54s`).

**Post-restore `git status --porcelain` (production paths):**
```
 M plugins/superheroes/lib/engine_adapter.py
 M plugins/superheroes/lib/engine_dispatch.py
 M plugins/superheroes/lib/seat_canary.py
```
