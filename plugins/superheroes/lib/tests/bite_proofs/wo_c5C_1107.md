# Bite-proof record — wo/1107c5-C (review round-1 fix batch)

Per `plugins/superheroes/rubric/bite-proof.md`. Three guarded elements, one neutralization each, one red,
one restore, one green.

## Element 1 — stall fix batch union retains other open targets (`test_compose_stall_fix_batch_retains_other_open_targets_when_one_stalled`)

**Guarded element:** `plugins/superheroes/lib/tests/test_audit_target_identity.py::test_compose_stall_fix_batch_retains_other_open_targets_when_one_stalled`,
which pins that `_compose_stall_fix_batch` unions stalled targets with every not-discharged audit target
on the per-location key — not only when the stalled intersection is empty.
**Axis:** open-blocker union — a recurrent stall must not silently drop another open audit target (#1107).

**Neutralization:** restored the `if not batch:` guard so union with `notDischarged` targets runs only
when no stalled target matched:
```python
    batch = stalled
    if not batch:  # WO-c5C neutralization — removed after red capture
        batch = _union_open_blockers(new_blocking, nd_targets)
```

**Raw red** (`/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-c5C -m pytest plugins/superheroes/lib/tests/test_audit_target_identity.py::test_compose_stall_fix_batch_retains_other_open_targets_when_one_stalled -q`):
```
F                                                                        [100%]
=================================== FAILURES ===================================
___ test_compose_stall_fix_batch_retains_other_open_targets_when_one_stalled ___

    def test_compose_stall_fix_batch_retains_other_open_targets_when_one_stalled():
        # axis: stalled identity matching one audit target must not drop other not-discharged targets
        ident_a = FI.finding_identity(_finding(title="stall-me", line=1))
        ident_b = FI.finding_identity(_finding(title="also-open", line=2))
        target_a = {"id": "%s@L1" % ident_a, "identity": ident_a, "file": "f.py", "line": 1,
                    "title": "stall-me", "severity": "Important"}
        target_b = {"id": "%s@L2" % ident_b, "identity": ident_b, "file": "f.py", "line": 2,
                    "title": "also-open", "severity": "Important"}
        state = RD.new_state(_cfg())
        state["_auditTargets"] = [target_a, target_b]
        state["_auditOutcome"] = {
            "notDischarged": [target_a["id"], target_b["id"]],
            "discharged": [],
        }
        breaker = {"reason": "audit-stall", "detail": "x", "stalledIdentities": [ident_a]}
        batch, refusal = RD._compose_stall_fix_batch(state, breaker)
        ids = {b.get("id") for b in batch}
        assert target_a["id"] in ids
>       assert target_b["id"] in ids
E       AssertionError: assert 'f.py::alsoopen@L2' in {'f.py::stallme@L1'}

plugins/superheroes/lib/tests/test_audit_target_identity.py:447: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_audit_target_identity.py::test_compose_stall_fix_batch_retains_other_open_targets_when_one_stalled
1 failed in 0.14s
```

**Restore:** inverse edit — unconditional union restored:
```python
    batch = _union_open_blockers(stalled, new_blocking, nd_targets)
```

**Restore receipt:** `git diff plugins/superheroes/lib/round_driver.py` shows no `WO-c5C neutralization`
token and `_compose_stall_fix_batch` calls `_union_open_blockers(stalled, new_blocking, nd_targets)`.

**Raw green** (same command, post-restore):
```
.                                                                        [100%]
1 passed in 0.12s
```

## Element 2 — unrecognized skew status certifies as unknown, not absent (`test_certification_plugin_version_skew_unknown_status_agrees_with_degradation`)

**Guarded element:** `plugins/superheroes/lib/tests/test_round_driver.py::test_certification_plugin_version_skew_unknown_status_agrees_with_degradation`,
which pins that a seat-map skew receipt with an unrecognized `status` produces certification
`pluginVersionSkew` that is not `absent` and agrees with the degraded disclosure block.
**Axis:** fail-closed skew projection — unknown receipt status is degrading, not absent (#1107).

**Neutralization:** reverted unrecognized-status projection to `absent`:
```python
    return "absent"  # WO-c5C neutralization — removed after red capture
```
(replacing the `return "unknown"` tail of `_plugin_version_skew_status`.)

**Raw red** (`/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-c5C -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_certification_plugin_version_skew_unknown_status_agrees_with_degradation -q`):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_certification_plugin_version_skew_unknown_status_agrees_with_degradation _

    def test_certification_plugin_version_skew_unknown_status_agrees_with_degradation():
        # axis: unrecognized skew receipt status must not certify as absent — degradation agrees
        reason = "plugin-version-skew: future build persisted unrecognized status"
        cfg = _cfg(leg="panel", vendors=["codex", "cursor"])
        state = RD.new_state(cfg)
        state["seatMap"] = {
            "pluginVersionSkew": {
                "status": "future-status",
                "detail": version_skew.DETAIL_SEMANTICS_DIVERGENT,
                "inspectedRoot": "/tmp/repo",
            },
            "degradations": [{
                "constraint": version_skew.CONSTRAINT,
                "detail": version_skew.DETAIL_SEMANTICS_DIVERGENT,
                "reason": reason,
            }],
        }
        RD._terminal_converged(state, state["config"], full_panel=True)
        cert = state["certification"]
>       assert cert["pluginVersionSkew"] != "absent"
E       AssertionError: assert 'absent' != 'absent'

plugins/superheroes/lib/tests/test_round_driver.py:5406: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_certification_plugin_version_skew_unknown_status_agrees_with_degradation
1 failed in 0.44s
```

**Restore:** inverse edit — restored `return "unknown"` tail of `_plugin_version_skew_status`.

**Restore receipt:** `git diff plugins/superheroes/lib/round_driver.py` shows no `WO-c5C neutralization`
token; unrecognized statuses return `unknown`.

**Raw green** (same command, post-restore):
```
.                                                                        [100%]
1 passed in 0.38s
```

## Element 3 — fix-batch unavailable vs empty split (`test_fix_batch_unavailable_refuses_materialize`)

**Guarded element:** `plugins/superheroes/lib/tests/test_order_input_contract.py::test_fix_batch_unavailable_refuses_materialize`
(with companion `test_ensure_fix_batch_file_materializes_empty_batch` on the same surface),
which pins that an absent/non-list fix batch refuses with `order-render-refused:fix-batch-unavailable`
while a known-empty list materializes `[]`.
**Axis:** available-vs-empty — do not wedge empty batches or silently accept unavailable state (#1107).

**Neutralization:** restored the empty-list fallback in `_ensure_fix_batch_file`:
```python
    if not isinstance(batch, list):
        batch = []  # WO-c5C neutralization — removed after red capture
```
(replacing `raise ValueError("order-render-refused:fix-batch-unavailable")`.)

**Raw red** (`/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-c5C -m pytest plugins/superheroes/lib/tests/test_order_input_contract.py::test_fix_batch_unavailable_refuses_materialize -q`):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_fix_batch_unavailable_refuses_materialize ________________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-10979/test_fix_batch_unavailable_ref0')

    def test_fix_batch_unavailable_refuses_materialize(tmp_path):
        # axis: absent/non-list fix batch refuses before writing fix-batch.json
        session_dir = str(tmp_path / "batch-refuse")
        os.makedirs(session_dir)
        state = {"_fixBatch": None, "fixBatch": "not-a-list"}
        with pytest.raises(ValueError, match="order-render-refused:fix-batch-unavailable"):
>           RD._ensure_fix_batch_file(session_dir, 2, state)
E           Failed: DID NOT RAISE <class 'ValueError'>

plugins/superheroes/lib/tests/test_order_input_contract.py:189: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_order_input_contract.py::test_fix_batch_unavailable_refuses_materialize
1 failed in 0.12s
```

**Restore:** inverse edit — restored `raise ValueError("order-render-refused:fix-batch-unavailable")`.

**Restore receipt:** `git diff plugins/superheroes/lib/round_driver.py` shows no `WO-c5C neutralization`
token; unavailable batch raises before write.

**Raw green** (`/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-c5C -m pytest plugins/superheroes/lib/tests/test_order_input_contract.py::test_ensure_fix_batch_file_materializes_empty_batch plugins/superheroes/lib/tests/test_order_input_contract.py::test_fix_batch_unavailable_refuses_materialize -q`):
```
..                                                                       [100%]
2 passed in 0.11s
```
