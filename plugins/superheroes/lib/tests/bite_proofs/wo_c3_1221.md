# Bite-proof record — #1221 continuation order 3 (CP3 owner-gate ruling)

**Head proved:** `bcf1e69c` on `issue-1221-gate-guidance-to-fixer-order` — the final head, tree
settled and committed before the first probe. **Produced and run by the build orchestrator**, not by
an implementer.

**Method, per element.** Neutralize the guarded element with a **targeted, revertible edit through
the host's edit action** — never a whole-file rewrite, never a shell edit, never a git discard —
run the **named** tests (exact node ids; never `-k`), record the runner's **own** exit code and the
failure list, restore with the **inverse edit**, re-run, and confirm `git status --porcelain` is
empty afterwards. Every red below ran with the **detector unedited**. Every exit code below is the
runner's own (`> file 2>&1; echo $?`), never read through a pipe.

This record covers the elements continuation order 3 **added or changed**. The elements ruling **b**
established are recorded in `wo_c2_1221.md` and are not re-proved here except where this pass
changed them (G6, G7).

---

## G1 — the guidance header carries the guided row's identity

**Guarded element.** `round_driver.py`, `_gate_guidance_identity_line`:
`return "### %s:%s — %s" % (file_part, line_part, title)`.

**Neutralization.** Drop the row identity, keeping only the title: `return "### %s" % (title,)`.

**Command.** `pytest test_round_driver.py::test_guided_order_block_id_matches_judgment_dispositions_record test_round_driver.py::test_two_guided_findings_same_title_distinguished_by_id test_order_input_contract.py::test_gate_guidance_legacy_record_without_file_line_renders_guidance -q`

**RED — runner exit 1**

```
FAILED test_round_driver.py::test_guided_order_block_id_matches_judgment_dispositions_record
FAILED test_round_driver.py::test_two_guided_findings_same_title_distinguished_by_id
FAILED test_order_input_contract.py::test_gate_guidance_legacy_record_without_file_line_renders_guidance
3 failed in 1.21s
```

**Restore** — inverse edit. **GREEN — runner exit 0:** `3 passed in 0.97s`. `git status --porcelain` empty.

---

## G2 — no header field can emit a line at the order's top structural level

**Guarded element.** `_normalize_gate_guidance_header_field`'s control-character substitution — the
C0 range, DEL, and the Unicode line/paragraph separators.

**Neutralization.** Replace the two normalization lines with a substitution that collapses only
spaces and tabs, leaving every line break intact.

**Command.** `pytest test_order_input_contract.py::test_gate_guidance_header_field_cannot_emit_second_line -q` (24 parametrisations: 4 fields × 6 separator classes)

**RED — runner exit 1**

```
FAILED …::test_gate_guidance_header_field_cannot_emit_second_line[para-sep-line]
FAILED …::test_gate_guidance_header_field_cannot_emit_second_line[para-sep-title]
FAILED …::test_gate_guidance_header_field_cannot_emit_second_line[para-sep-id]
24 failed in 4.00s
```

**All 24 red** — every field and every separator class, which is the per-field guarantee this
element owes rather than one representative.

**Restore** — **disclosed:** the first inverse edit restored the line **semantically but not
byte-for-byte**, writing the literal U+2028/U+2029 characters where the source carries the
`\uXXXX` escape *text* inside a raw string. The tests passed either way, so the suite could not
have caught it. `git status` did, the file was restored to its committed bytes, and the GREEN run
below was then **re-run on the restored head** rather than being claimed from the earlier variant.

**GREEN — runner exit 0:** `24 passed in 3.77s`, on the byte-identical restored file.
`git status --porcelain` empty.

---

## G3 — the header field cap keeps an oversized field from costing its entry a place

**Guarded element.** `GATE_GUIDANCE_HEADER_FIELD_BYTE_CAP = 200` and its use in the normalizer.

**Neutralization.** Raise the constant to `1000000000`, disabling truncation in practice. The tests
pin the **rendered literals** (`bytes withheld`, an absolute byte bound on the identity line), not
the symbol, so a symbol-spelled assertion cannot pass this probe vacuously.

**Command.** `pytest test_order_input_contract.py::test_gate_guidance_oversized_title_truncated_and_guidance_still_renders test_order_input_contract.py::test_gate_guidance_truncation_collision_triggers_ambiguity_note test_order_input_contract.py::test_gate_guidance_header_field_one_byte_over_truncates_and_keeps_guidance -q`

**RED — runner exit 1**

```
FAILED …::test_gate_guidance_oversized_title_truncated_and_guidance_still_renders
FAILED …::test_gate_guidance_truncation_collision_triggers_ambiguity_note
FAILED …::test_gate_guidance_header_field_one_byte_over_truncates_and_keeps_guidance
3 failed in 40.62s
```

**Restore** — inverse edit. **GREEN — runner exit 0:** `3 passed in 0.59s`. `git status --porcelain` empty.

---

## G4 — colliding identities carry the ambiguity note

**Guarded element.** `_gate_guidance_block`: `if identity_counts[identity_line] >= 2:`.

**Neutralization.** `if False:` — the note is never emitted.

**Command.** `pytest test_order_input_contract.py::test_gate_guidance_identical_identities_carry_ambiguity_note_and_keep_guidance test_order_input_contract.py::test_gate_guidance_three_identical_identities_note_names_three test_order_input_contract.py::test_gate_guidance_truncation_collision_triggers_ambiguity_note test_round_driver.py::test_two_guided_findings_same_location_distinct_ids_match_record -q`

**RED — runner exit 1: `4 failed in 1.26s`** (all four named tests).

**Restore** — inverse edit. **GREEN — runner exit 0:** `4 passed in 1.16s`. `git status --porcelain` empty.

---

## G5 — the record-id line carries the fold-minted id, labelled audit-only

**Guarded element.** `_gate_guidance_record_id_line`'s interpolation of the id.

**Neutralization.** Return the same label with the id replaced by `(withheld)` — the line survives,
the traceability does not, which is the narrower and therefore stronger probe.

**Command.** `pytest test_order_input_contract.py::test_gate_guidance_record_id_line_is_audit_only_and_carries_fold_id test_round_driver.py::test_guided_order_block_id_matches_judgment_dispositions_record test_round_driver.py::test_two_guided_findings_same_location_distinct_ids_match_record -q`

**RED — runner exit 1: `3 failed in 1.21s`.**

**Restore** — inverse edit. **GREEN — runner exit 0:** `3 passed in 1.14s`. `git status --porcelain` empty.

---

## G6 — the fold records the guided row's file and line

**Guarded element.** `_fold_judgment`'s `fix-with-guidance` entry write:
`"file": f.get("file"), "line": f.get("line")`.

**Neutralization.** Remove both keys from the entry dict — the pre-continuation-3 shape.

**Command.** `pytest test_round_driver.py::test_guided_order_block_id_matches_judgment_dispositions_record test_round_driver.py::test_two_guided_findings_same_title_distinguished_by_id test_round_driver.py::test_two_guided_findings_same_location_distinct_ids_match_record -q`

**RED — runner exit 1: `3 failed in 1.27s`.**

**Restore** — inverse edit. **GREEN — runner exit 0:** `3 passed in 0.91s`. `git status --porcelain` empty.

---

## G7 — `_gate_guidance_entries` threads the identity through to the renderer

**Guarded element.** The `out.append({…, "file": item.get("file"), "line": item.get("line"), …})`
in `_gate_guidance_entries`.

**Neutralization.** Remove both keys, so the record holds the identity but the renderer never sees it.

**RED command (three tests).** `pytest test_round_driver.py::test_guided_order_block_id_matches_judgment_dispositions_record test_order_input_contract.py::test_gate_guidance_header_line_zero_renders_zero test_order_input_contract.py::test_gate_guidance_identical_identities_carry_ambiguity_note_and_keep_guidance -q`

**RED — runner exit 1**

```
FAILED test_round_driver.py::test_guided_order_block_id_matches_judgment_dispositions_record
FAILED test_order_input_contract.py::test_gate_guidance_header_line_zero_renders_zero
2 failed, 1 passed in 1.14s
```

**Recorded honestly: two of the three named tests went red, not three.** The ambiguity test is
**insensitive to this element by construction** — both of its entries lose `file`/`line` together,
so their identity lines still collide and the note still fires correctly. That is the right
behaviour, not a gap: the element is proved by the two tests that name the rendered identity. The
third is listed so the record shows what was run rather than only what failed.

**Restore** — inverse edit. **GREEN command (two tests, deliberately narrowed).** The green rerun
dropped the ambiguity test, because the RED run had just established that it is insensitive to this
element — rerunning it would have added a pass that proves nothing about G7. `pytest
test_round_driver.py::test_guided_order_block_id_matches_judgment_dispositions_record
test_order_input_contract.py::test_gate_guidance_header_line_zero_renders_zero -q`

**GREEN — runner exit 0:** `2 passed in 0.94s`. `git status --porcelain` empty. The two counts
differ on purpose — three tests named in RED, two rerun in GREEN — and the record says so here
rather than leaving a reader to reconcile them. The ambiguity test's own green is carried by G4,
where it is load-bearing.

---

## G8 — the unusable-record refusal reaches the real render entry point

**Guarded element.** The **uncaught** propagation of `order-render-refused:gate-guidance-unusable`
from `_gate_guidance_entries` through the `P_FIXER` arm of `_order_placeholders`.

**Neutralization.** Wrap the call site in `try: … except ValueError: guidance_entries = []` — the
exact mutant the pre-continuation-3 test could not catch, which makes the order silently render the
no-guidance form instead of refusing.

**Command.** `pytest test_order_input_contract.py::test_gate_guidance_refuses_unusable_records_at_render_entry_point test_order_input_contract.py::test_gate_guidance_entries_refuses_unusable_records -q`

**RED — runner exit 1**

```
FAILED …::test_gate_guidance_refuses_unusable_records_at_render_entry_point[none-id]
FAILED …::test_gate_guidance_refuses_unusable_records_at_render_entry_point[missing-id]
FAILED …::test_gate_guidance_refuses_unusable_records_at_render_entry_point[non-string-id]
FAILED …::test_gate_guidance_refuses_unusable_records_at_render_entry_point[blank-id]
FAILED …::test_gate_guidance_refuses_unusable_records_at_render_entry_point[duplicate-id]
5 failed, 5 passed in 1.30s
```

**This split is the review finding, demonstrated.** The five **entry-point** cases go red; the five
**direct-helper** cases — the shape the suite carried before this pass — stay **green** under the
same mutant. That is precisely the escape the finding named, and the reason the entry-point test had
to exist rather than the helper test being deemed sufficient.

**Restore** — inverse edit. **GREEN — runner exit 0:** `10 passed in 0.68s`. `git status --porcelain` empty.

---

## G9 — `_truncate_utf8_bytes` backs off to a codepoint boundary

**Guarded element.** The back-off loop: `except UnicodeDecodeError: cut = cut[:-1]`.

**Neutralization.** `except UnicodeDecodeError: break` — the helper gives up and returns
`("", len(raw))` instead of backing off.

**Command.** `pytest test_order_input_contract.py::test_truncate_utf8_bytes_mid_codepoint_backoff test_order_input_contract.py::test_truncate_utf8_bytes_cap_smaller_than_first_character test_order_input_contract.py::test_truncate_utf8_bytes_exact_codepoint_boundary test_order_input_contract.py::test_gate_guidance_header_multibyte_straddle_cap_is_valid_utf8 -q`

**This probe was run twice, and the first run is the reason WO-C exists.**

**First RED — runner exit 1: `1 failed, 3 passed in 0.44s`.** Only
`test_truncate_utf8_bytes_mid_codepoint_backoff` went red.
`test_gate_guidance_header_multibyte_straddle_cap_is_valid_utf8` stayed green under a mutant that
destroys the exact behaviour it names — because its only assertion was
`title_part.encode("utf-8").decode("utf-8")`, which **every Python `str` satisfies**. A detector that
cannot fail. The probe caught it; the suite never could have. WO-C rewrote it to pin the exact
truncated prefix and the withheld byte count, and audited the other seventeen added tests for the
same defect (no second instance found).

**Second RED, on the final head — runner exit 1**

```
FAILED …::test_truncate_utf8_bytes_mid_codepoint_backoff
FAILED …::test_gate_guidance_header_multibyte_straddle_cap_is_valid_utf8
2 failed, 2 passed in 0.68s
```

The two tests that name the back-off now both bite. The remaining two green ones name adjacent
behaviour a `break` mutant does not change (a cap below the first codepoint's width, and a cap
landing exactly on a boundary) — correctly insensitive, and listed rather than hidden.

**Restore** — inverse edit. **GREEN — runner exit 0:** `4 passed in 0.68s`. `git status --porcelain` empty.

---

## Residual disclosed

`test_gate_guidance_header_multibyte_straddle_cap_is_valid_utf8` retains two assertions that operate
on the test's own `expected_prefix` (`assert bare_title` and the byte-length bound) and therefore
cannot fail. They are inert rather than misleading — the test's load-bearing assertion is the exact
equality against the rendered identity, which the second RED above proves bites. Recorded here
rather than left for a reader to discover.
