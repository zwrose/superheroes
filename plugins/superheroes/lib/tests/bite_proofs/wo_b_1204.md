# WO-B (#1204) bite-proof — six unjudgeable-map guards repointed

## Guarded elements

| ID | Guarded element | Axis | Proving test(s) |
|---|---|---|---|
| BP-B1 | Rounds-ledger loop in `_seat_map_unjudgeable` | Round record arms only when receipts are judgeable | G1 `test_seat_map_unjudgeable_rounds_ledger_has_reader`, G2 `test_seat_map_unjudgeable_rounds_arm_with_judgeable_receipts` |
| BP-B2 | Control halves of G3/G4 | Detector must not fire on clean cursor control | G3/G4 control assertions |
| BP-B3 | `unjudgeable_receipts` universal quantifier | Any unjudgeable receipt arms — not existential | G5 `test_seat_map_unjudgeable_quantifier_later_unjudgeable_arms`, G6 `test_seat_map_unjudgeable_quantifier_earlier_unjudgeable_arms` |
| BP-B4 | `shapeDrivers` wiring for `_seat_map_unjudgeable` | Unjudgeable map adds `seat-map-unavailable` driver | G3 positive half |
| BP-B5 | `_cert_shape` wiring for `_seat_map_unjudgeable` | Unjudgeable map degrades certification shape | G4 positive half |

---

## BP-B1 — rounds-ledger arm has a reader

- **axis:** `seatMapUnjudgeable` round records arm `_seat_map_unjudgeable` when receipts alone would not

**neutralization** (delete rounds loop — receipts-only return):
```python
def _seat_map_unjudgeable(state):
    """Whether any submitted seat map's violation basis is incomplete — rounds ∪ receipts union.

    The per-round ``seatMapUnjudgeable`` record is round-scoped (#1204); this predicate keeps the
    whole-history union."""
    return bool(_sm_unjudgeable_receipts(state, _driver_author_family(state)))
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_rounds_ledger_has_reader plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_rounds_arm_with_judgeable_receipts -q -n auto
```

**raw red** (exit 1):
```
bringing up nodes...
bringing up nodes...

FF                                                                       [100%]
=================================== FAILURES ===================================
_________ test_seat_map_unjudgeable_rounds_arm_with_judgeable_receipts _________
[gw1] darwin -- Python 3.9.6 /Library/Developer/CommandLineTools/usr/bin/python3

    def test_seat_map_unjudgeable_rounds_arm_with_judgeable_receipts():
        ...
        state["rounds"] = {"2": {"seatMapUnjudgeable": ["no-author-family"]}}
>       assert RD._seat_map_unjudgeable(state) is True
E       AssertionError: assert False is True
...
______________ test_seat_map_unjudgeable_rounds_ledger_has_reader ______________
[gw0] darwin -- Python 3.9.6 /Library/Developer/CommandLineTools/usr/bin/python3

    def test_seat_map_unjudgeable_rounds_ledger_has_reader():
        ...
        state["rounds"] = {"1": {"seatMapUnjudgeable": ["no-author-family"]}}
>       assert RD._seat_map_unjudgeable(state) is True
E       AssertionError: assert False is True
...
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_rounds_arm_with_judgeable_receipts
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_rounds_ledger_has_reader
2 failed in 1.39s
```

**restore:** reinstate `for rec in (state.get("rounds") or {}).values(): ... return True` loop before receipts return (landed form).

**pre-fix comparison** (old G1/G2 body — `fixer_vendor=None`, no negative control):
```python
    cfg.pop("fixerVendor")
    seat_map = _assertable_seat_map(["claude", "codex"], fixer_vendor=None)
    _set_seat_map(state, seat_map)
    state["rounds"] = {"1": {"seatMapUnjudgeable": ["no-author-family"]}}
    assert RD._seat_map_unjudgeable(state) is True
```
**pre-fix with same neutralization:** **green** (order measured claim reproduced — receipts arm alone returns `True` because `fixer_vendor=None` map is self-asserted-unjudgeable; rounds loop deletion is not load-bearing).

---

## BP-B2 — controls are load-bearing

- **axis:** clean cursor control must keep `_seat_map_unjudgeable` false

**neutralization** (always-fire):
```python
def _seat_map_unjudgeable(state):
    return True
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_terminal_converged_shape_drivers_consumer plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_cert_shape_degraded_consumer -q -n auto
```

**raw red** (exit 1):
```
FF                                                                       [100%]
=================================== FAILURES ===================================
_____ test_seat_map_unjudgeable_terminal_converged_shape_drivers_consumer ______
...
>       assert RD._seat_map_unjudgeable(control_state) is False
E       AssertionError: assert True is False
...
____________ test_seat_map_unjudgeable_cert_shape_degraded_consumer ____________
...
>       assert RD._seat_map_unjudgeable(control_state) is False
E       AssertionError: assert True is False
...
2 failed in 1.02s
```

**restore:** full `_seat_map_unjudgeable` body (landed form).

**pre-fix comparison** (old control used `control_cfg["fixerVendor"] = "claude"` with only `not in` /
`not endswith` assertions) — **corrected by the orchestrator's own measurement; the original entry
here predicted rather than measured, and was half wrong.** Measured against the mutated library
(`orchestrator_1204.md` § BP-B2):

```
OLD G3 control  drivers=['seat-map-unavailable', 'seat-map-violation']  -> assertion passes? False
OLD G4 control  shape='full-panel-confirmed-constraint-violated'        -> assertion passes? True
```

So under the always-fire mutation the old **G3** control **would** have caught it; only the old
**G4** control was blind. Both old controls were still vacuous on the axis that matters — they
passed on the *shipped* code for the wrong reason (`shapeDrivers == ['seat-map-violation']`, not
`[]`), which is what the new `shapeDrivers == []` and exact-shape assertions forbid. The
disposition of both guards is unchanged; only this record's claim is.

---

## BP-B3 — receipts quantifier is universal

- **axis:** one unjudgeable receipt arms even when another is complete

**neutralization** (existential — return `[]` when any receipt is complete):
```python
    for entry in receipts(state):
        basis = seat_map.violation_basis(entry["map"], driver_author_family)
        if basis == seat_map.VIOLATION_BASIS_COMPLETE:
            return []
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_quantifier_later_unjudgeable_arms plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_quantifier_earlier_unjudgeable_arms -q -n auto
```

**raw red** (exit 1):
```
FF                                                                       [100%]
=================================== FAILURES ===================================
________ test_seat_map_unjudgeable_quantifier_earlier_unjudgeable_arms _________
...
>       assert RD._seat_map_unjudgeable(state) is True
E       AssertionError: assert False is True
...
_________ test_seat_map_unjudgeable_quantifier_later_unjudgeable_arms __________
...
>       assert RD._seat_map_unjudgeable(state) is True
E       AssertionError: assert False is True
...
2 failed in 1.07s
```

**restore:** universal loop only (landed form).

**pre-fix comparison** (old G5/G6 used `_fold_panel` with both maps `fixer_vendor=None` and rounds ledger populated): **green** under same existential neutralization when both maps are unjudgeable via receipts — quantifier defect masked because both receipts are unjudgeable and no complete receipt exists to trigger early `return []`.

---

## BP-B4 — `shapeDrivers` consumer

- **axis:** `_seat_map_unjudgeable` adds `seat-map-unavailable` to `shapeDrivers`

**neutralization** (drop unjudgeable from shapeDrivers wiring):
```python
    if _seat_map_unavailable(state):
        shape_drivers.append("seat-map-unavailable")
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_terminal_converged_shape_drivers_consumer -q -n auto
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_____ test_seat_map_unjudgeable_terminal_converged_shape_drivers_consumer ______
...
>       assert "seat-map-unavailable" in state["certification"]["shapeDrivers"]
E       AssertionError: assert 'seat-map-unavailable' in []
...
1 failed in 0.95s
```

**restore:** `if _seat_map_unavailable(state) or _seat_map_unjudgeable(state):` (landed form).

**pre-fix comparison:** old positive half same assertion; with claude control passing for wrong reason only. Neutralization still reds old positive half — **pre-fix positive half red** (same assertion).

---

## BP-B5 — `_cert_shape` consumer

- **axis:** `_seat_map_unjudgeable` forces `-degraded` certification shape

**neutralization** (drop unjudgeable from `_cert_shape`):
```python
        or _seat_map_unavailable(state)
    ):
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_cert_shape_degraded_consumer -q -n auto
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_seat_map_unjudgeable_cert_shape_degraded_consumer ____________
...
>       assert state["certification"]["shape"].endswith("-degraded")
E       AssertionError: assert False
E        +  where False = <built-in method endswith of str object at 0x1083753a0>('-degraded')
E        +    where <built-in method endswith of str object at 0x1083753a0> = 'full-panel-confirmed'.endswith
...
1 failed in 1.00s
```

**restore:** `or _seat_map_unjudgeable(state)` in `_cert_shape` (landed form).

**pre-fix comparison:** old positive half same `endswith("-degraded")` assertion — **pre-fix positive half red** under same neutralization.

---

## Batched green — all six guards (exit 0)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_rounds_ledger_has_reader plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_rounds_arm_with_judgeable_receipts plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_quantifier_later_unjudgeable_arms plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_quantifier_earlier_unjudgeable_arms plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_terminal_converged_shape_drivers_consumer plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_cert_shape_degraded_consumer -q -n auto
```

**raw green:**
```
......                                                                   [100%]
6 passed in 0.93s
```
