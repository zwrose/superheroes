# WO-R2 (#1124 review round 2) bite-proofs — doc-side investigation-floor threshold operands

## Baseline (post-change)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py -q
```

**Output:**
```
100 passed in 0.59s
```

(Pytest emitted `PytestWarning: (rm_rf) error removing ... Directory not empty` noise from xdist temp cleanup; exit code 0, all tests passed.)

---

## N1 — dispatch-mechanics.md doc-side operand

**Guarded element:** `_dispatch_mechanics_investigated_threshold_prose` doc-side operand — axis: quantity threshold in dispatch-mechanics threshold sentence must be read from captured prose, not assumed in locator regex; weakening doc to `ideally one` must fail as home↔doc disagreement.

**Neutralization:** rewrote line 177 threshold from `at least one claimed path` to `ideally one claimed path` in `skills/workhorse/reference/dispatch-mechanics.md` (home and auto-fix-loop untouched).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated \
  -p no:xdist -q
```

**Red run:**
```
E       AssertionError: investigation floor drift: dispatch-mechanics.md (investigated threshold) and spot_check_investigated disagree on at-least-one surviving-path threshold (home='at least one', doc='ideally one')
E       assert 'at least one' == 'ideally one'
E         
E         - ideally one
E         + at least one

plugins/superheroes/lib/tests/test_ssot_drift.py:477: AssertionError
=========================== short test summary info ============================
1 failed in 0.23s
```

**Restore:** restored line 177 to `at least one claimed path survives spot-checking`.

**Restore receipt:** `**`investigated`** is present only when at least one claimed path survives spot-checking`

**Green run:**
```
1 passed in 0.18s
```

---

## N2 — auto-fix-loop.md doc-side operand

**Guarded element:** `_auto_fix_loop_investigation_floor_block` doc-side operand — axis: quantity threshold in vacuous-forfeit block must be read from captured prose; weakening doc to `ideally one` must fail as disagreement (not locator miss).

**Neutralization:** rewrote line 63 from `lists at least one path` to `lists ideally one path` in `skills/review-code/reference/auto-fix-loop.md` (home and dispatch-mechanics untouched).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_ssot_drift.py::test_investigation_floor_prose_matches_spot_check_investigated \
  -p no:xdist -q
```

**Red run:**
```
E       AssertionError: investigation floor drift: auto-fix-loop.md (vacuous-forfeit block) and spot_check_investigated disagree on at-least-one surviving-path threshold (home='at least one', doc='ideally one')
E       assert 'at least one' == 'ideally one'

plugins/superheroes/lib/tests/test_ssot_drift.py:477: AssertionError
=========================== short test summary info ============================
1 failed in 0.22s
```

**Restore:** restored line 63 to `lists at least one path that`.

**Restore receipt:** `> `findings` array is accepted as *clean* **only** when `investigated` lists at least one path that`

**Green run:**
```
1 passed in 0.18s
```
