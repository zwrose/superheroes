# WO-J (#1145) bite-proofs — round-3 review fix batch

> **Superseded (2026-08-26).** BP3 and BP4 neutralizations target `_all_present_substance_fields_carry_sentinel` (and its call from `member_carries_sentinel` in BP3); both are deleted from live code. The owner-ruled #1145 scope amendment (2026-08-26) retired content-inspection echo legs for a per-dispatch nonce with near-copy placeholder equality — retired by design, not found broken. Replacement detectors are bite-proved in `wo_a_1145c3.md` and `wo_c_1145c3.md`. This record remains a valid receipt of the run it describes against the code as it stood at that commit; it is not withdrawn.

## BP1 — FIX-A: panel stdout never imperates literal example emit

**Guarded element:** `test_panel_stdout_delivery_never_imperates_literal_example_emit` — axis: rendered panel delivery text is format-only; sentinel-bearing example is never the object of an emit imperative.

**Neutralization:** reverted `_panel_derived_placeholders` stdout branch to pre-fix `Emit `%s` as your final stdout` with `json.dumps(review_findings_schema.example_findings_object(), sort_keys=True)` (`plugins/superheroes/lib/round_orders.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_orders.py::test_panel_stdout_delivery_never_imperates_literal_example_emit -q
```

**Red run:**
```
=================================== FAILURES ===================================
_______ test_panel_stdout_delivery_never_imperates_literal_example_emit ________

>       assert format_only in text
E       AssertionError: assert 'Format only — your findings replace every value' in 'You are reviewing branch for repo acme/widget, target feature/wo4.\nReview session mode branch (from session metadata...ur final stdout must be a single JSON object with nothing after it. Do not write a landing file (read-only sandbox).\n'

FAILED plugins/superheroes/lib/tests/test_round_orders.py::test_panel_stdout_delivery_never_imperates_literal_example_emit
1 failed in 0.12s
```

**Restore:** `_panel_derived_placeholders` stdout branch again uses `_panel_stdout_delivery_text(include_investigated_reminder=True)` plus sandbox note.

**Restore receipt:** `_panel_stdout_delivery_text(include_investigated_reminder=True)` + `do not write a findings file (read-only sandbox — nothing reads one).`

**Green run:**
```
1 passed in 0.10s
```

---

## BP2 — FIX-B: C2b per-site census bites `_panel_derived_placeholders` only

**Guarded element:** `test_review_findings_example_emitting_sites_delegate_to_home` — axis: C2b row fails when only `_panel_derived_placeholders` regresses to a hand literal (not whole-file C2a masking).

**Neutralization:** in `_panel_derived_placeholders` stdout branch only, replaced home delegation with hand literal `'{"findings": [...], "investigated": [...]}'` (`plugins/superheroes/lib/round_orders.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home -q
```

**Red run:**
```
=================================== FAILURES ===================================
_________ test_review_findings_example_emitting_sites_delegate_to_home _________

>               assert literal not in region, (
E               AssertionError: C2b (lib/round_orders.py:_panel_derived_placeholders): hand-written findings example literal reappeared: '{"findings": [...], "investigated": [...]}'

FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home
1 failed in 0.17s
```

**Restore:** stdout branch again uses `_panel_stdout_delivery_text(include_investigated_reminder=True)`.

**Restore receipt:** `_panel_stdout_delivery_text(include_investigated_reminder=True)` + sandbox note.

**Green run:**
```
1 passed in 0.17s
```

---

## BP3 — FIX-C: uniform retype uniform-retype detector

**Guarded element:** `test_member_carries_sentinel_true_for_uniform_retype_of_example` — axis: cosmetic `placeholder` recase keeps sentinel in every value but breaks exact equality; third leg must still echo-verdict.

**Neutralization:** removed `_all_present_substance_fields_carry_sentinel` call from `member_carries_sentinel` (`plugins/superheroes/lib/review_findings_schema.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_uniform_retype_of_example -q
```

**Red run:**
```
=================================== FAILURES ===================================
_______ test_member_carries_sentinel_true_for_uniform_retype_of_example ________

>       assert RFS.member_carries_sentinel(member) is True
E       AssertionError: assert False is True

FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_uniform_retype_of_example
1 failed in 0.16s
```

**Restore:** reinstated `if _all_present_substance_fields_carry_sentinel(member): return True` third leg.

**Restore receipt:** `if _all_present_substance_fields_carry_sentinel(member):\n        return True`

**Green run:**
```
1 passed in 0.16s
```

---

## BP4 — FIX-C: empty-substance-set fail-open edge

**Guarded element:** `test_member_carries_sentinel_false_for_empty_substance_field_set` — axis: member with no present substance fields must not echo-verdict via all-quantifier leg.

**Neutralization:** in `_all_present_substance_fields_carry_sentinel`, changed empty `present_values` branch from `return False` to `return True` (`plugins/superheroes/lib/review_findings_schema.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_false_for_empty_substance_field_set -q
```

**Red run:**
```
=================================== FAILURES ===================================
_______ test_member_carries_sentinel_false_for_empty_substance_field_set _______

>       assert RFS.member_carries_sentinel(member) is False
E       AssertionError: assert True is False

FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_false_for_empty_substance_field_set
1 failed in 0.15s
```

**Restore:** empty `present_values` branch again `return False`.

**Restore receipt:** `if not present_values:\n        return False`

**Green run:**
```
1 passed in 0.16s
```
