# WO-M (#1145) bite-proofs — null substance field must not disarm echo leg

## BP1 — uniform retype with one null substance field must stay echo

**Guarded element:** `test_member_carries_sentinel_true_for_uniform_retype_with_null_substance_field` — axis: schema types `suggestion`/`evidence` as `["string","null"]` and requires them; null must not disarm echo leg.

**Neutralization:** in `_all_present_substance_fields_carry_sentinel`, removed the `_substance_value_carries_content` gate and appended every key present in `member` (`plugins/superheroes/lib/review_findings_schema.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_uniform_retype_with_null_substance_field -q
```

**Red run:**
```
=================================== FAILURES ===================================
_ test_member_carries_sentinel_true_for_uniform_retype_with_null_substance_field _

>       assert RFS.member_carries_sentinel(member) is True
E       AssertionError: assert False is True

plugins/superheroes/lib/tests/test_review_findings_schema.py:410: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_uniform_retype_with_null_substance_field
1 failed in 0.32s
```

**Restore:** reinstated `_substance_value_carries_content(value)` before appending to `present_values`.

**Restore receipt:** `_all_present_substance_fields_carry_sentinel` again skips `None`, non-`str`/`dict`/`list` scalars, and whitespace-only strings when building `present_values`.

**Green run:**
```
1 passed in 0.32s
```

## BP2 — all-null substance member must not echo-verdict

**Guarded element:** `test_member_carries_sentinel_false_for_all_null_substance_fields` — axis: all-quantifier over empty content-bearing set must not echo-verdict.

**Neutralization:** in `_all_present_substance_fields_carry_sentinel`, changed the empty-set guard from `return False` to `return True` (`plugins/superheroes/lib/review_findings_schema.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_false_for_all_null_substance_fields -q
```

**Red run:**
```
=================================== FAILURES ===================================
________ test_member_carries_sentinel_false_for_all_null_substance_fields ______

>       assert RFS.member_carries_sentinel(member) is False
E       AssertionError: assert True is False

plugins/superheroes/lib/tests/test_review_findings_schema.py:422: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_false_for_all_null_substance_fields
1 failed in 0.32s
```

**Restore:** reverted empty-set guard to `return False`.

**Restore receipt:** `_all_present_substance_fields_carry_sentinel` again returns `False` when no content-bearing substance fields remain after the narrowed present rule.

**Green run:**
```
1 passed in 0.32s
```
