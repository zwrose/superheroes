# WO-A (#1124) bite-proofs — one cap-arithmetic home, stream-honest markers

## Baseline (pre-fix red)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_cap_file_tail_carries_stderr_marker_not_stdout \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_drain_in_memory_carries_stderr_marker_not_stdout \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_bytes_with_stdout_cap_clamps_marker_to_budget \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_cap_arithmetic_single_home_byte_identical \
  -q
```

**Output:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_cap_file_tail_carries_stderr_marker_not_stdout
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_drain_in_memory_carries_stderr_marker_not_stdout
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_bytes_with_stdout_cap_clamps_marker_to_budget
3 failed, 1 passed in 1.82s
```

---

## N1 — stderr branch of the file-capping path

**Guarded element:** `_run_engine_files` stderr `_cap_file_tail` call — axis: truncated stderr file capture carries stderr marker, never stdout marker.

**Neutralization:** pass `CAP_STREAM_STDOUT` instead of `CAP_STREAM_STDERR` at the stderr call site (`engine_dispatch.py` stderr `_cap_file_tail` line).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_cap_file_tail_carries_stderr_marker_not_stdout \
  -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_cap_file_tail_carries_stderr_marker_not_stdout
AssertionError: assert '<<<SUPERHEROES-STDERR-TRUNCATED:' in '<<<SUPERHEROES-STDOUT-TRUNCATED:2000>>>...'
1 failed in 1.40s
```

**Restore:** restored `CAP_STREAM_STDERR` at the stderr `_cap_file_tail` call site.

**Restore receipt:** `_, stderr_observed = _cap_file_tail(stderr_path, MAX_STDERR_CAPTURE, CAP_STREAM_STDERR)`

**Green run:**
```
1 passed in 1.34s
```

---

## N2 — stderr branch of the in-memory `_drain` path

**Guarded element:** stderr `_drain` thread stream argument — axis: in-memory stderr drain writes stderr marker, never stdout marker.

**Neutralization:** pass `CAP_STREAM_STDOUT` to the stderr `_drain` thread instead of `CAP_STREAM_STDERR`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_drain_in_memory_carries_stderr_marker_not_stdout \
  -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_drain_in_memory_carries_stderr_marker_not_stdout
AssertionError: assert '<<<SUPERHEROES-STDERR-TRUNCATED:' in '<<<SUPERHEROES-STDOUT-TRUNCATED:2000>>>...'
1 failed in 1.01s
```

**Restore:** restored `CAP_STREAM_STDERR` on the stderr `_drain` thread.

**Restore receipt:** `args=(proc.stderr, err, MAX_STDERR_CAPTURE, CAP_STREAM_STDERR)`

**Green run:**
```
1 passed in 1.34s
```

---

## N3 — clamp in the one arithmetic home

**Guarded element:** `_cap_bytes_with_truncation_marker` clamp — axis: cap arithmetic never returns more than `max_bytes` when the marker exceeds the budget.

**Neutralization:** deleted the `if len(capped) > max_bytes: capped = capped[:max_bytes]` clamp inside `_cap_bytes_with_truncation_marker`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_bytes_with_stdout_cap_clamps_marker_to_budget \
  -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_bytes_with_stdout_cap_clamps_marker_to_budget
AssertionError: assert 39 <= 10
1 failed in 0.75s
```

**Restore:** restored the clamp inside `_cap_bytes_with_truncation_marker`.

**Restore receipt:** `if len(capped) > max_bytes:\n        capped = capped[:max_bytes]`

**Green run:**
```
1 passed in 1.10s
```

---

## N4 — single-home delegation

**Guarded element:** `_bytes_with_stdout_cap` delegation — axis: file and memory entry points produce byte-identical capped output for the same data, budget, and stream.

**Neutralization:** replaced delegation with a private `content_budget` computation one byte smaller than the home (`content_budget = max(0, max_bytes - len(marker)) - 1`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_cap_arithmetic_single_home_byte_identical \
  -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_cap_arithmetic_single_home_byte_identical
AssertionError: assert memory_capped == file_capped
1 failed in 0.55s
```

**Restore:** restored `return _cap_bytes_with_truncation_marker(data, max_bytes, stream, observed_bytes)` in `_bytes_with_stdout_cap`.

**Restore receipt:** `return _cap_bytes_with_truncation_marker(data, max_bytes, stream, observed_bytes)`

**Green run:**
```
1 passed in 1.07s
```

---

## Final green (WO-A test block)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_cap_file_tail_carries_stderr_marker_not_stdout \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stderr_drain_in_memory_carries_stderr_marker_not_stdout \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_bytes_with_stdout_cap_clamps_marker_to_budget \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_cap_arithmetic_single_home_byte_identical \
  -q
```

**Output:**
```
4 passed
```
