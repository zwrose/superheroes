# WO-C (#1124 item 3) bite-proofs

## BP1 — LF-only split in `_parse_worktree_list` (shared home)

**Guarded element:** `sibling_worktree_probe.py:_parse_worktree_list` — axis: **split on `"\n"` only, not `str.splitlines()`**.

**Neutralization:** restored `str.splitlines()`:

```python
    for line in (text or "").splitlines():
```

**Raw red** (nodes `test_parse_git_worktree_list_preserves_cr_in_path` and `test_parse_worktree_list_preserves_cr_in_path`):

```
FF                                                                       [100%]
=================================== FAILURES ===================================
______________ test_parse_git_worktree_list_preserves_cr_in_path _______________
...
>       assert worktrees[0]["path"] == os.path.realpath(decoded_path)
E       AssertionError: assert '/private/tmp/example' == '/private/tmp/example\rpath'
...
________________ test_parse_worktree_list_preserves_cr_in_path _________________
...
>       assert blocks[0]["path"] == raw_path
E       AssertionError: assert '/tmp/example' == '/tmp/example\rpath'
2 failed in 0.74s
```

**Restore:** inverse edit — restored `split("\n")`.

**Restore receipt:** restored line:

```python
    for line in (text or "").split("\n"):
```

**Raw green:**

```
..                                                                       [100%]
2 passed in 0.64s
```

## BP2 — pathless-block skip in `_parse_git_worktree_list` wrapper

**Guarded element:** `engine_dispatch.py:_parse_git_worktree_list` — axis: **skip blocks with no `path` before `os.path.realpath`**.

**Neutralization:** removed the `if path is None: continue` guard:

```python
    for block in sibling_worktree_probe._parse_worktree_list(text):
        path = block.get("path")
        worktrees.append({"path": os.path.realpath(path)})
```

**Raw red** (node `test_parse_git_worktree_list_skips_pathless_block`):

```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_parse_git_worktree_list_skips_pathless_block _______________
...
>       worktrees = ED._parse_git_worktree_list(porcelain)
...
>       filename = os.fspath(filename)
E       TypeError: expected str, bytes or os.PathLike object, not NoneType
1 failed in 0.80s
```

**Restore:** inverse edit — restored skip:

```python
        if path is None:
            continue
```

**Restore receipt:** restored `if path is None: continue` guard in wrapper loop.

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.64s
```

## BP3 — surrogate-safe decode in `_parse_git_worktree_list` wrapper

**Guarded element:** `engine_dispatch.py:_parse_git_worktree_list` — axis: **decode with `errors="surrogateescape"` so surrogate-bearing `str` input does not raise**.

**Neutralization:** decode with `errors="strict"`:

```python
    text = porcelain_bytes.decode("utf-8", errors="strict")
```

**Raw red** (node `test_parse_and_filter_shims_surrogate_str_no_raise`):

```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_parse_and_filter_shims_surrogate_str_no_raise ______________
...
>       text = porcelain_bytes.decode("utf-8", errors="strict")
E       UnicodeDecodeError: 'utf-8' codec can't decode byte 0x80 in position 17: invalid start byte
1 failed in 0.80s
```

**Restore:** inverse edit — restored `errors="surrogateescape"`.

**Restore receipt:** restored line:

```python
    text = porcelain_bytes.decode("utf-8", errors="surrogateescape")
```

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.64s
```
