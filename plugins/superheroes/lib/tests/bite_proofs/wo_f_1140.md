# WO-F (#1140 round 2) bite-proofs — wiring_defects structural pin

The detector under proof is `wiring_defects()` in repo-root `source_guard.py`. Neutralizations
were applied as targeted reversible edits to `source_guard.py` for red runs and restored by
inverse edit before the green half; in-test proofs use `bite_support.patched_module`
(in-memory only).

## Baseline

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_source_guard.py -q -n auto
```

**Output (summary line; full log includes pytest temp-dir cleanup warnings):**
```
54 passed in 5.45s
```

---

## E13 — last assignment wins

**Guarded element:** among top-level `pytest_plugins` assignments, the **last** one in source
order — axis: a good assignment followed by `pytest_plugins = ()` must report defect 5.

**Neutralization:** `last_plugins_assign = node` →
`if last_plugins_assign is None:\n                                last_plugins_assign = node`
(Assign branch only).

**Red run** (`test_wiring_defects_good_then_empty_overwrite` with disk neutralization):
```
FAILED ...::test_wiring_defects_good_then_empty_overwrite
E   AssertionError: assert 'conftest.py pytest_plugins does not include source_guard' in []
1 failed in 0.08s
```

**Restore:** inverse edit restoring `last_plugins_assign = node`.

**Green run:** `test_bite_wiring_defects_last_assignment_wins` passes in E13–E15 green batch.

---

## E14 — module-level-only scan

**Guarded element:** iteration over `tree.body` only — axis: nested assignments such as
`if False: pytest_plugins = (...)` must not count as effective wiring.

**Neutralization:** `for node in tree.body:` → `for node in ast.walk(tree):`.

**Red run** (`test_wiring_defects_nested_pytest_plugins_assignment` with disk neutralization):
```
FAILED ...::test_wiring_defects_nested_pytest_plugins_assignment
E   AssertionError: assert 'conftest.py has no effective module-level pytest_plugins assignment' in []
1 failed in 0.08s
```

**Restore:** inverse edit restoring `for node in tree.body:`.

**Green run:** `test_bite_wiring_defects_module_level_only` passes in E13–E15 green batch.

---

## E15 — pytest.ini [pytest] section check

**Guarded element:** `parser.has_section("pytest")` — axis: a sectionless pytest.ini must
report defect 2.

**Neutralization:** `if not parser.has_section("pytest"):` →
`if False and not parser.has_section("pytest"):`.

**Red run** (`test_wiring_defects_pytest_ini_no_section` with disk neutralization):
```
FAILED ...::test_wiring_defects_pytest_ini_no_section
E   AssertionError: assert 'pytest.ini has no [pytest] section' in []
1 failed in 0.08s
```

**Restore:** inverse edit restoring `if not parser.has_section("pytest"):`.

**Green run:** `test_bite_wiring_defects_pytest_ini_section` passes in E13–E15 green batch.

---

## Final green (E13–E15 bite tests)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_source_guard.py::test_bite_wiring_defects_last_assignment_wins \
  plugins/superheroes/lib/tests/test_source_guard.py::test_bite_wiring_defects_module_level_only \
  plugins/superheroes/lib/tests/test_source_guard.py::test_bite_wiring_defects_pytest_ini_section \
  -q
```

**Output:**
```
3 passed in 0.08s
```

---

## Supersession (WO-H #1140 round 3)

E13–E15 pinned the deleted `wiring_defects` text-prediction scan (`ast` last-assignment rule,
module-level-only iteration, and `pytest.ini` `[pytest]` section parsing). Those text-prediction
checks were **deleted as non-terminating**, not re-homed. Effective-wiring assurance now lives in
the behavioural subprocess legs — `test_real_wiring_behavioural_with_assignment` /
`_without_assignment` and `test_real_wiring_behavioural_nested_cwd` /
`_without_pytest_ini`, recorded in `wo_i_1140.md`. `wo_h_1140.md` covers **file presence only**.
