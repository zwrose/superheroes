# WO-F (#1185) bite-proofs — comparison/binding census widen, accessor reads, embedded literals, legacy refusal branches

Baseline (budget invocations 1–2, no probes):

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_state_version_spelling_census.py -q
............                                                             [100%]
12 passed in 8.25s
```

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_state_version_rollback.py -q
......                                                                   [100%]
6 passed in 11.13s
```

---

## BP-F1 — comparison and binding legs run over every scanned module

**Guarded element:** `census_module` comparison/binding legs — axis: a module that hand-spells the version WITHOUT naming a pinned symbol is still reported.

**Neutralization:** restored the `refs_pinned` gate so `_scan_comparisons` and `_scan_bindings` run only when `_module_references_pinned_symbols` is true (inverse of WO-F's unconditional extend) in `plugins/superheroes/lib/tests/test_state_version_spelling_census.py` `census_module`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_comparison \
  plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_mint_binding \
  -q --tb=line
```

**Status: unproven**

No synthetic test in the suite exercises comparison or binding on a module that does not reference a pinned symbol. With the gate restored, `test_synthetic_injection_comparison` and `test_synthetic_injection_mint_binding` both inject into `round_driver.py`, which names pinned symbols — the gated legs still run, so both tests stayed green (exit 0, 2 passed). Probe was reverted before leaving the worktree.

---

## BP-F2 — comparison leg recognizes canonical version accessors

**Guarded element:** `_VERSION_ACCESSORS` / `_is_version_accessor_call` — axis: `_state_version(x) == <int>` is reported as a version read.

**Neutralization:** changed `_VERSION_ACCESSORS = ("_state_version", "_receipt_version")` to `_VERSION_ACCESSORS = ()` at `plugins/superheroes/lib/tests/test_state_version_spelling_census.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_comparison \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-7372e31a71b53878/plugins/superheroes/lib/tests/test_state_version_spelling_census.py:573: AssertionError: expected comparison leg on injected schemaVersion == 99
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_comparison
1 failed in 1.37s
```

**Restore:** changed `_VERSION_ACCESSORS = ()` back to `_VERSION_ACCESSORS = ("_state_version", "_receipt_version")`.

**Restore receipt:** `_VERSION_ACCESSORS = ("_state_version", "_receipt_version")`

**Green run (exit 0):**
```
...                                                                      [100%]
3 passed in 16.05s
```
(included in combined green run with BP-F4 and BP-F5 below)

---

## BP-F3 — string-literal leg searches instead of fullmatching

**Guarded element:** `_scan_string_literals` / `_RECEIPT_CERTIFIED_LITERAL_RE` — axis: a `receipt-certified/<digits>` embedded inside a longer string (a docstring or message) is reported.

**Neutralization:** changed `if _RECEIPT_CERTIFIED_LITERAL_RE.search(node.value):` to `if _RECEIPT_CERTIFIED_LITERAL_RE.fullmatch(node.value):` in `_scan_string_literals` at `plugins/superheroes/lib/tests/test_state_version_spelling_census.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_string_literal \
  -q --tb=line
```

**Status: unproven**

`test_synthetic_injection_string_literal` injects a standalone literal `_SYNTH = "receipt-certified/9"`, which still fullmatches the pattern — the probe did not fail to fire on that injection (exit 0, 1 passed). No other synthetic test injects an embedded `receipt-certified/<n>` inside a longer string. Probe was reverted before leaving the worktree.

---

## BP-F4 — `_legacy_session_refusal_detail`, durable-records branch

**Guarded element:** `_legacy_session_refusal_detail` durable-records branch — axis: a legacy session that holds durable seat records at the pending slot is told to start a fresh session dir, NOT that `next`/`submit` will finish it.

**Neutralization:** inserted `return False` immediately after the docstring in `_legacy_session_has_durable_pending_records` at `plugins/superheroes/lib/round_driver.py` (wrong constant for the durable-records branch).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_rollback.py::test_legacy_refusal_durable_records_names_fresh_session \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-7372e31a71b53878/plugins/superheroes/lib/tests/test_state_version_rollback.py:107: AssertionError: assert 'fresh session' in 'loop-state.json is schemaVersion 3; `next`/`submit` finish it'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_rollback.py::test_legacy_refusal_durable_records_names_fresh_session
1 failed in 39.92s
```

**Restore:** removed the inserted `return False` line from `_legacy_session_has_durable_pending_records`.

**Restore receipt:** function body begins with `if state.get("_submitUsed"):` — no early `return False` before the real predicate chain.

**Green run (exit 0):**
```
...                                                                      [100%]
3 passed in 16.05s
```
(included in combined green run with BP-F2 and BP-F5 below)

---

## BP-F5 — `_legacy_session_refusal_detail`, plain branch

**Guarded element:** `_legacy_session_refusal_detail` plain branch — axis: a legacy session with no durable pending records still gets the `next`/`submit` wording.

**Neutralization:** inserted `return True` immediately after the docstring in `_legacy_session_has_durable_pending_records` at `plugins/superheroes/lib/round_driver.py` (wrong constant for the plain branch).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_rollback.py::test_legacy_refusal_plain_session_names_next_submit \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-7372e31a71b53878/plugins/superheroes/lib/tests/test_state_version_rollback.py:95: AssertionError: assert '`next`/`submit`' in 'loop-state.json is schemaVersion 3 with durable seat record(s) at the pending slot — hand `submit` refuses `record-submit-interleaved`; start a fresh session dir to continue'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_rollback.py::test_legacy_refusal_plain_session_names_next_submit
1 failed in 52.67s
```

**Restore:** removed the inserted `return True` line from `_legacy_session_has_durable_pending_records`.

**Restore receipt:** function body begins with `if state.get("_submitUsed"):` — no early `return True` before the real predicate chain.

**Green run (exit 0):**
```
...                                                                      [100%]
3 passed in 16.05s
```
(included in combined green run with BP-F2 and BP-F4 below)

---

Combined green run (budget invocation 4, all probes reverted):

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_spelling_census.py \
  plugins/superheroes/lib/tests/test_state_version_rollback.py -q
..................                                                       [100%]
18 passed in 24.27s
```
