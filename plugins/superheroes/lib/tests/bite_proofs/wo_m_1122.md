# WO-M (#1122) bite-proofs

## BP23 — P1 (declaration↔code binding)

**Guarded element:** `test_engine_dispatch.py:_finish_probe_git_fake_route_check` declared-route equality — axis: **installed routes must exactly match `PROBE_GIT_FAKE_ROUTES` for that test**.

**Neutralization:** disabled the binding branch:

```python
        if False and installed != declared:
```

**Raw red** (node `test_probe_git_fake_declaration_binding_detects_helper_mismatch`):

```
=================================== FAILURES ===================================
_______ test_probe_git_fake_declaration_binding_detects_helper_mismatch ________

    def test_probe_git_fake_declaration_binding_detects_helper_mismatch():
        with pytest.raises(AssertionError, match="!="):
>           _finish_probe_git_fake_route_check(
                "test_worktree_baseline_fallback_on_enumeration_failure",
                {("_git_scrubbed", "worktree")},
            )
E           Failed: DID NOT RAISE <class 'AssertionError'>

plugins/superheroes/lib/tests/test_engine_dispatch.py:5961: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_declaration_binding_detects_helper_mismatch
1 failed in 0.37s
```

**Restore:** inverse edit — restored `if installed != declared:`.

**Restore receipt:** restored line:

```python
        if installed != declared:
```

**Raw green:**

```
1 passed in 0.34s
```

## BP24 — P2 (no lexical blind spot)

**Guarded element:** `test_engine_dispatch.py:_register_probe_git_fake` route recording — axis: **every chokepoint install is recorded at install time regardless of source layout**.

**Neutralization:** skipped adding to `installed_routes`:

```python
            if False:
                _PROBE_GIT_STATE.installed_routes.add((helper, subcommand))
```

**Raw red** (node `test_probe_git_fake_detects_undeclared_wrapped_install`):

```
=================================== FAILURES ===================================
____________ test_probe_git_fake_detects_undeclared_wrapped_install ____________

    def test_probe_git_fake_detects_undeclared_wrapped_install(monkeypatch):
        ...
        with pytest.raises(AssertionError, match="undeclared"):
>           _finish_probe_git_fake_route_check(
                _PROBE_GIT_STATE.test_name,
                set(_PROBE_GIT_STATE.installed_routes),
            )
E           Failed: DID NOT RAISE <class 'AssertionError'>

plugins/superheroes/lib/tests/test_engine_dispatch.py:5975: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_detects_undeclared_wrapped_install
1 failed in 0.40s
```

**Restore:** inverse edit — restored `_PROBE_GIT_STATE.installed_routes.add((helper, subcommand))`.

**Restore receipt:** restored the unconditional `add` call quoted above.

**Raw green:**

```
1 passed in 0.34s
```

## BP25 — P3 (bypass is loud)

**Guarded element:** `test_engine_dispatch.py:_wrap_monkeypatch_for_probe_git_fakes` bypass guard — axis: **literal `monkeypatch.setattr(ED, "_git_scrubbed…")` fails by test name**.

**Neutralization:** disabled the bypass failure:

```python
            if False and not _PROBE_GIT_STATE._chokepoint_active:
```

**Raw red** (node `test_probe_git_fake_bypass_setattr_fails_by_name`):

```
=================================== FAILURES ===================================
_______________ test_probe_git_fake_bypass_setattr_fails_by_name _______________

    def test_probe_git_fake_bypass_setattr_fails_by_name(monkeypatch):
        with pytest.raises(pytest.fail.Exception, match="setattr bypass"):
>           monkeypatch.setattr(ED, "_git_scrubbed_bytes", lambda *a, **k: None)
E           Failed: DID NOT RAISE <class 'Failed'>

plugins/superheroes/lib/tests/test_engine_dispatch.py:5987: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_bypass_setattr_fails_by_name
1 failed in 0.39s
```

**Restore:** inverse edit — restored `if not _PROBE_GIT_STATE._chokepoint_active:`.

**Restore receipt:** restored line quoted above.

**Raw green:**

```
1 passed in 0.32s
```

## BP26 — the observation pin

**Guarded element:** `test_engine_dispatch.py:test_probe_git_fake_routes_are_live` observation pin — axis: **observed production route set equals `PROBE_PRODUCTION_GIT_ROUTES` exactly (both directions)**.

**Neutralization:** weakened equality to non-empty only, with spurious route injected in `_install_git_route_recorders`:

```python
        observed.append(("_git_scrubbed_bytes", "fetch"))
...
    assert observed_set, "observation recorded no git routes"
```

**Raw red** (node `test_probe_git_fake_routes_are_live`):

```
1 passed in 0.45s
```

**Restore:** inverse edits — restored equality assertion and removed spurious `fetch` append.

**Restore receipt:** restored lines:

```python
    assert observed_set == PROBE_PRODUCTION_GIT_ROUTES, (
        "observed production git routes %r != expected %r"
        % (sorted(observed_set), sorted(PROBE_PRODUCTION_GIT_ROUTES))
    )
```

(without the `observed.append(("_git_scrubbed_bytes", "fetch"))` line).

**Raw green** (with spurious route still present, equality restored — detector catches mutation):

```
=================================== FAILURES ===================================
_____________________ test_probe_git_fake_routes_are_live ______________________

>       assert observed_set == PROBE_PRODUCTION_GIT_ROUTES, (
            "observed production git routes %r != expected %r"
            % (sorted(observed_set), sorted(PROBE_PRODUCTION_GIT_ROUTES))
        )
E       AssertionError: observed production git routes [('_git_scrubbed', 'rev-parse'), ('_git_scrubbed_bytes', 'fetch'), ('_git_scrubbed_bytes', 'status'), ('_git_scrubbed_bytes', 'worktree')] != expected [('_git_scrubbed', 'rev-parse'), ('_git_scrubbed_bytes', 'status'), ('_git_scrubbed_bytes', 'worktree')]

plugins/superheroes/lib/tests/test_engine_dispatch.py:5947: AssertionError
1 failed in 0.47s
```

(with spurious route removed, re-run: `1 passed in 0.41s`.)

## BP27 — the identity backstop

**Guarded element:** `test_engine_dispatch.py:_finish_probe_git_fake_identity_check` — axis: **every probe git helper on `ED` must be either its test-start snapshot or an object registered via `_register_probe_git_fake`**.

**Neutralization:** disabled the identity check body:

```python
def _finish_probe_git_fake_identity_check(test_name, snapshots, registered_fakes):
    return
    errors = []
```

**Raw red** (node `test_probe_git_fake_raw_assignment_bypass_detected`):

```
=================================== FAILURES ===================================
______________ test_probe_git_fake_raw_assignment_bypass_detected ______________
plugins/superheroes/lib/tests/test_engine_dispatch.py:6045: in test_probe_git_fake_raw_assignment_bypass_detected
    _finish_probe_git_fake_identity_check(
E   Failed: DID NOT RAISE <class 'AssertionError'>
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_raw_assignment_bypass_detected
============================== 1 failed in 0.37s ===============================
```

**Restore:** inverse edit — removed the leading `return` so the check body runs again.

**Restore receipt:** restored opening:

```python
def _finish_probe_git_fake_identity_check(test_name, snapshots, registered_fakes):
    errors = []
```

**Raw green:**

```
1 passed in 0.30s
```

## BP28 — the back-door closure (3b)

**Guarded element:** removal of `_PROBE_GIT_STATE._chokepoint_active` plus identity backstop — axis: **flag-flip setattr bypass cannot hide an unregistered install**.

**Neutralization:** reintroduced the settable `_chokepoint_active` flag on `_ProbeGitFakeInstallState`, let `guarded_setattr` honor it when depth is zero, and disabled the identity backstop (`return` at top of `_finish_probe_git_fake_identity_check`).

**Raw red** (node `test_probe_git_fake_flag_flip_bypass_dead`):

```
=================================== FAILURES ===================================
__________________ test_probe_git_fake_flag_flip_bypass_dead ___________________
plugins/superheroes/lib/tests/test_engine_dispatch.py:6071: in test_probe_git_fake_flag_flip_bypass_dead
    assert not hasattr(_PROBE_GIT_STATE, "_chokepoint_active")
E   AssertionError: assert not True
E    +  where True = hasattr(<test_engine_dispatch._ProbeGitFakeInstallState object at 0x106a27490>, '_chokepoint_active')
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_flag_flip_bypass_dead
============================== 1 failed in 0.37s ===============================
```

**Restore:** inverse edits — removed `_chokepoint_active` from state, restored depth-only setattr guard, restored identity check body.

**Restore receipt:** `_ProbeGitFakeInstallState` has no `_chokepoint_active`; `guarded_setattr` uses `if _probe_git_chokepoint_depth == 0:`; `_finish_probe_git_fake_identity_check` begins with `errors = []`.

**Raw green:**

```
1 passed in 0.34s
```
