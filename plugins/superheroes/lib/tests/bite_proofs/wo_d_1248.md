# WO-D (#1248 leg d) bite-proof — the `dispatch-record-conflict` refusal

**Producer note.** WO-D's dispatch forfeited on transport (`worktree-dirtied-by-attempt`) with its
code and tests delivered but its bite-proof record lost with the report. This record was produced by
the **orchestrator**, running the neutralize→red→restore→green cycle itself under
`rubric/bite-proof.md`, as the verification duty the workhorse charter §8 assigns it. The probe was
applied and reverted through the host's edit action, on a committed tree, per the mutation-probe
mechanics.

## Guarded element

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-D1 | `cmd_record_dispatch`'s conflicting-row refusal | A hand-recorded dispatch row never overwrites a differing row already in the manifest — it refuses by name and changes no bytes | `test_record_dispatch_refuses_conflicting_existing_row` |

The literal pinned by the proving assertion is `"dispatch-record-conflict"`, not a symbol.

---

## BP-D1 — a hand row cannot overwrite a runner-written row

**neutralization** (the refusal removed; the conflicting write falls through to the merge below):

```python
        return _refuse_cmd(session_dir, "record-dispatch", "dispatch-record-conflict",
                           phase=phase, rnd=rnd, attempt=cur_attempt, seat=seat)
```

became

```python
        pass
```

in `plugins/superheroes/lib/round_driver.py`, `cmd_record_dispatch`'s existing-row branch.

Probe residue check while applied — `git diff --stat plugins/superheroes/lib/round_driver.py`:

```
 plugins/superheroes/lib/round_driver.py | 3 +--
 1 file changed, 1 insertion(+), 2 deletions(-)
```

**command:**

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-bp -m pytest \
  "plugins/superheroes/lib/tests/test_round_driver_advance.py::test_record_dispatch_refuses_conflicting_existing_row" \
  -q --tb=line
```

**raw red** (exit 1):

```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1248-ebdf001e91df3116/plugins/superheroes/lib/tests/test_round_driver_advance.py:1450: assert (True is False)
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_advance.py::test_record_dispatch_refuses_conflicting_existing_row
1 failed in 10.06s
```

**restore:** the inverse edit put the two `_refuse_cmd` lines back verbatim. Post-restore
`git diff --stat plugins/superheroes/lib/round_driver.py` was **empty** — no probe residue.

**raw green** (exit 0, run together with the WO-B proof's proving test on the restored tree):

```
..                                                                       [100%]
2 passed in 20.12s
```
