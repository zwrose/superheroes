# WO-B (#1247) bite-proof — fail-closed canary-outcome handling at the consumer

**Provenance: orchestrator-authored, from the orchestrator's own re-runs.** WO-B's implementer
dispatch forfeited at its engine's 18,483,154-byte stdout cap before writing this record (the code
it landed was reconstructed from the diff and completed by WO-B2). Every neutralization, red and
green below was applied and run by the build orchestrator on the **final head**, not inherited from
an implementer report. This authorship deviation is a disclosed degradation in the PR body.

All three proofs were run on the final head with the tree otherwise clean, using targeted revertible
edits through the host's edit action. Command for every run:

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_round_driver.py \
  plugins/superheroes/lib/tests/test_canary_outcome_census.py -q -n auto
```

## Guarded elements

| ID | Guarded element | Axis |
|---|---|---|
| BP-B1 | `round_driver.canary_liveness` normalization at the chokepoint | a probe's self-asserted outcome cannot certify a vendor |
| BP-B2 | `round_driver.canary_liveness` `plant-undetected` status | an engaged probe that missed the plant is not `proven` |
| BP-B3 | `_fold_panel`'s `canary_panel_gap` for `plant-undetected` | a non-pass canary withholds panel certification |

---

## BP-B1 — normalization at the chokepoint

- **axis:** a probe's self-asserted outcome cannot certify a vendor

**neutralization** (trust the caller's `outcome` instead of normalizing):
```python
            outcome, fault = probe.get("outcome"), None
            normalized.append((probe, outcome, fault))
```

**raw red** (exit 1):
```
plugins/superheroes/lib/tests/test_round_driver.py:6298: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_plant_undetected_engaged_miss_withholds_certification
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_dead_beats_plant_undetected_beats_proven_both_orders
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_ok_contradicts_fields_not_proven
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_unknown_outcome_not_proven
4 failed, 456 passed in 99.78s (0:01:39)
```

**restore:** reinstate `outcome, fault = canary_outcome.normalize(probe)`.

**restore receipt:** `git status --porcelain` over the neutralized path returned **empty** (clean).

**raw green:** `498 passed in 75.69s (0:01:15)` (with `test_seat_canary.py` also in the selection).

**Finer-reading signal for the advisor — not a rework demand.** A *second*, finer neutralization was
also attempted on this element: bucketing `dead` on the raw `probe.get("engaged")` field rather than
on the normalized outcome. That neutralization came back **green** — `454 passed`. The discriminating
input is a probe carrying `engaged: True` together with a dispatch-failure `outcome` (e.g.
`unrunnable`); under the real code it is `dead` (seats downgraded to `missing`), under the
neutralization it falls through to `unproven` (seats not downgraded). No fixture in
`test_round_driver.py` or the census constructs that combination — `_probe_for_outcome` pairs every
dispatch-failure member with `engaged=False` — and census leg 4 asserts only `!= "proven"`, which
does not discriminate the two. Per `rubric/bite-proof.md`, a grader reading the declared set one
level finer records the finer reading as **a signal for the advisor, never a rework demand**, so it
is recorded here and carried as a follow-up rather than patched in this build. The **declared** axis
above is proven by the red run above.

---

## BP-B2 — the `plant-undetected` vendor status

- **axis:** an engaged probe that missed the plant is not `proven`

**neutralization** (collapse the new status back into `proven`):
```python
        elif plant_undetected:
            deciding, _, fault = sorted(
                plant_undetected, key=lambda t: _canary_probe_sort_key(t[0]))[0]
            st = "proven"
```

**raw red** (exit 1):
```
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_dead_beats_plant_undetected_beats_proven_both_orders
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_plant_undetected_engaged_miss_withholds_certification
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_ok_contradicts_fields_not_proven
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_unknown_outcome_not_proven
FAILED plugins/superheroes/lib/tests/test_canary_outcome_census.py::test_leg4_non_pass_fail_closed_cross_product
5 failed, 455 passed in 90.55s (0:01:30)
```

This red also discharges **BP-C4** (census leg 4, *a consumer that passes a non-pass member is
caught*) on the final head: leg 4 fails here precisely because a non-pass member became `proven` at
a declared consumer.

**restore:** reinstate `st = canary_outcome.OUTCOME_PLANT_UNDETECTED`.

**restore receipt:** `git status --porcelain` over the neutralized path returned **empty** (clean).

**raw green:** `498 passed in 75.69s (0:01:15)`.

---

## BP-B3 — panel certification withheld

- **axis:** a non-pass canary withholds panel certification

**neutralization** (stop setting the panel gap, leaving the channel record and the decision line
intact — so the red is on the *certification* axis and not merely on "something was recorded"):
```python
        # axis: a non-pass canary withholds panel certification
        canary_panel_gap = canary_panel_gap
```

**raw red** (exit 1):
```
plugins/superheroes/lib/tests/test_round_driver.py:6300: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_plant_undetected_engaged_miss_withholds_certification
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_ok_contradicts_fields_not_proven
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_canary_unknown_outcome_not_proven
3 failed, 457 passed in 76.25s (0:01:16)
```

Note the axis discrimination: `canaryPlantUndetected` was still recorded and the decision line still
emitted under this neutralization — the three tests fail on `state["fullPanelRan"]`, i.e. on
certification, which is the axis this element claims.

**restore:** reinstate `canary_panel_gap = True`.

**restore receipt:** `git status --porcelain` over the neutralized path returned **empty** (clean).

**raw green:** `498 passed in 75.69s (0:01:15)`.

---

## Residue

None. After the final restore, `git status --porcelain` over the whole worktree returned empty, and
the four repository validators returned OK.
