# WO-L4A-F (#1273) bite-proof — claude argv census recorded exception

**Provenance:** cursor / composer-2.5 (implementer). **Rung line:** the exception entry retires when layer 4a-2 retires the launcher's hand-built argv; `claude-argv-exception-stale` forces the entry's deletion. D1 itself keeps its existing rung, tag `c14-l4a-D1`.

## Bite-proof table

| ID | Guarded element | Red exit | Green exit |
|---|---|---|---|
| BP-F1 | `test_no_claude_argv_outside_engine_adapter` file walk | 1 | 0 |
| BP-F2 | `test_no_claude_argv_outside_engine_adapter` exact-one count | 1 | 0 |
| BP-F3 | `test_no_claude_argv_outside_engine_adapter` stale exception | 1 | 0 |

---

## BP-F1 — file walk reaches production code (replaces BP-A4)

See **BP-A4** in `wo_l4a_A_1273.md` for neutralization, command, raw red, restore, and raw green (updated to token shape).

---

## BP-F2 — second hit inside excepted function

- **axis:** two hits in `compose_launch` emit outside tokens for every hit

**neutralization** (`launcher.py` `compose_launch`):
```python
    _probe = ["claude", "--bg"]
    argv = ["claude", "--model", token, "--session-id", session_id, "-p", prompt]
```

**command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-wo-F -m pytest plugins/superheroes/lib/tests/test_claude_argv_census.py::test_no_claude_argv_outside_engine_adapter -q`

**raw red** (exit 1):
```
AssertionError: claude-argv-outside-adapter:lib/launcher.py:1102
  claude-argv-outside-adapter:lib/launcher.py:1103
```

**restore:** remove the `_probe = ["claude", "--bg"]` line

**restore receipt:** `argv = ["claude", "--model", token, "--session-id", session_id, "-p", prompt]` restored at line 1102

**raw green** (exit 0): `1 passed in 1.80s`

---

## BP-F3 — stale exception entry

- **axis:** zero hits in `compose_launch` emits `claude-argv-exception-stale`

**neutralization** (`launcher.py` `compose_launch`):
```python
    argv = [str("claude"), "--model", token, "--session-id", session_id, "-p", prompt]
```

**command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-wo-F -m pytest plugins/superheroes/lib/tests/test_claude_argv_census.py::test_no_claude_argv_outside_engine_adapter -q`

**raw red** (exit 1):
```
AssertionError: claude-argv-exception-stale:lib/launcher.py:compose_launch
```

**restore:** `argv = ["claude", "--model", token, "--session-id", session_id, "-p", prompt]`

**restore receipt:** hand-built argv literal restored to `"claude"` at line 1102

**raw green** (exit 0): `1 passed in 1.80s`
