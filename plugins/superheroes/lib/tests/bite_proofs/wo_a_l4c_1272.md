# WO-A layer 4c bite-proof record (#1272)

Proofs target `lib/tests/test_round_certification_six_cases.py` and `lib/tests/test_layer4c_certification_finish_1272.py`.
Neutralizations are targeted edits reverted by inverse edit before the next proof.

## G1 — `_cited_head_qualifies` chain acceptance

- **Guarded:** `_cited_head_qualifies` in `round_certification.py`
- **Neutralization:** at start of function after certified_head resolution, `if cited_head != certified_head: return False, "audited-chain-gap:panel"`
- **RED:** `test_case_7_audited_chain_certifies` → `AssertionError: ... 'bindingFailure': 'execution-evidence-stale-head' ... (audited-chain-gap:panel)` (see `/private/tmp/l4c-wo-a/g1-red.txt`)
- **GREEN:** `1 passed` (`/private/tmp/l4c-wo-a/g1-green.txt`)
- **Restore:** remove the early return block
- **GREEN:** `test_case_7_audited_chain_certifies` passes

## G2–G6 — `_audited_chain` legs (parametrized `test_case_7_missing_leg_refuses`)

Each neutralization forces the named leg to succeed while others stay real; the matching parametrized case must certify (refusal None) on RED.

| Leg | Neutralization site | RED test id |
|-----|---------------------|-------------|
| panel | skip roster/head validation (`panelHead` forced, `complete` stays false until end bypass) | `[panel]` |
| descent | treat `_is_ancestor` as always True | `[descent]` |
| fix-receipt | skip fixed-finding receipt scan | `[fix-receipt]` |
| scoped-finder | skip scoped-finder legs | `[scoped-finder]` |
| verify | skip verify leg | `[verify]` |

## G7 — D-B auditor role

- **Neutralization:** restore `family_for("verifier", vendor)` at both audit sites in `check_seat_independence` / `_independence_block`
- **RED:** `test_auditor_family_discriminator_follows_auditor_role`
- **GREEN:** restore `auditor` role lookup

## G8–G10 — run-kind gate

- **Neutralization:** no-op `_dispatch_run_kind_qualifies` (always return True, None)
- **RED:** each of `test_run_kind_panel_write_refuses`, `test_run_kind_fixer_review_refuses`, `test_run_kind_absent_refuses` sees refusal None
- **GREEN:** restore check

## G11 — `control_probe_shape`

- **Neutralization:** `return True` at top of `receipt_disclosures.control_probe_shape`
- **RED:** `test_control_probe_shape_rejects_malformed[None]`
- **GREEN:** restore validation
