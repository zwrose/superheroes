# WO-B (#1185) bite-proofs — state schema v4 bump and min-version round-entry gate

## BP-B1 — min-version comparison in `_round_entry_key_allowed`

**Guarded element:** `_round_entry_key_allowed` certified-form branch — axis: a key is carried on certified receipts when `_receipt_version(state) >=` the key's `min_certified_version`.

**Neutralization:** changed `"min_certified_version": 3` to `"min_certified_version": 4` in `ROUND_ENTRY_KEY_FORMS["verifyPasses"]` at `plugins/superheroes/lib/round_driver.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_driver_verify_passes_per_round.py::test_v3_certified_receipt_still_carries_verify_passes -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_v3_certified_receipt_still_carries_verify_passes _____________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-218/test_v3_certified_receipt_stil0')

    def test_v3_certified_receipt_still_carries_verify_passes(tmp_path):
        """v3 certified receipts still carry verifyPasses (no regression from the v4 bump)."""
        state = _state_with_verify_passes(3)
        state["terminal"] = "converged"
        state["certification"] = {"shape": "audited-chain"}
        receipt = round_driver.build_receipt(state, str(tmp_path))
        assert receipt["schemaVersion"] == 3
        rounds = receipt.get("rounds") or []
        assert rounds, "expected at least one round entry"
        for rd in rounds:
            assert isinstance(rd, dict)
>           assert "verifyPasses" in rd
E           AssertionError: assert 'verifyPasses' in {'auditProvenance': None, 'audits': None, 'authorJustifiedDrops': None, 'blockingCount': None, ...}

plugins/superheroes/lib/tests/test_round_driver_verify_passes_per_round.py:217: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_verify_passes_per_round.py::test_v3_certified_receipt_still_carries_verify_passes
1 failed in 0.23s
```

**Restore:** changed `"min_certified_version": 4` back to `"min_certified_version": 3`.

**Restore receipt:** `"min_certified_version": 3,`

**Green run (exit 0):**
```
..                                                                       [100%]
2 passed in 0.19s
```
(included in combined green run with BP-B2 below)

---

## BP-B2 — `SUPPORTED_STATE_VERSIONS` membership check in `load_state`

**Guarded element:** `_state_version` / `load_state` version gate — axis: only versions in `SUPPORTED_STATE_VERSIONS` load successfully.

**Neutralization:** changed `SUPPORTED_STATE_VERSIONS = (2, 3, 4)` to `SUPPORTED_STATE_VERSIONS = (2, 3)` in the pinned declaration block at `plugins/superheroes/lib/round_driver.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_state_version_rollback.py::test_load_state_accepts_supported_versions_unchanged[4]' -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_load_state_accepts_supported_versions_unchanged[4] ____________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-219/test_load_state_accepts_suppor0')
version = 4

    @pytest.mark.parametrize("version", [2, 3, 4])
    def test_load_state_accepts_supported_versions_unchanged(tmp_path, version):
        """The current reader accepts 2, 3, and 4 and returns schemaVersion exactly as persisted."""
        d = str(tmp_path / ("v%d" % version))
        os.makedirs(d)
        payload = {"schemaVersion": version, "rounds": {}, "round": 1}
        with open(os.path.join(d, RD.STATE_FILE), "w", encoding="utf-8") as fh:
            json.dump(payload, fh)
        ok, loaded = RD.load_state(d)
>       assert ok is True
E       assert False is True

plugins/superheroes/lib/tests/test_state_version_rollback.py:41: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_rollback.py::test_load_state_accepts_supported_versions_unchanged[4]
1 failed in 0.19s
```

**Restore:** changed `SUPPORTED_STATE_VERSIONS = (2, 3)` back to `SUPPORTED_STATE_VERSIONS = (2, 3, 4)`.

**Restore receipt:** `SUPPORTED_STATE_VERSIONS = (2, 3, 4)`

**Green run (exit 0):**
```
..                                                                       [100%]
2 passed in 0.19s
```
