# WO-A (#1136 carve round) bite-proofs — no read-only path ever writes

## BP-1 — `test_preview_leaves_absent_registry_absent`

**Guarded element:** `enumerate_flip()` → `mode_registry.resolve(..., persist_backfill=False)` — axis: preview must not create the project store or write `registry.json`.

**Neutralization:** restored persisting resolve in `enumerate_flip` (dropped `persist_backfill=False`):
```python
current = mode_registry.resolve(cwd, root)["mode"]
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_mode_migrate.py::test_preview_leaves_absent_registry_absent -q -p no:warnings
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________________ test_preview_leaves_absent_registry_absent __________________
...
>       assert not os.path.isdir(store_dir)
E       AssertionError: assert not True
E        +  where True = <function isdir at 0x104923b80>('.../store/projects/6d5c8a7b6c8ca477')
E        +    where <function isdir at 0x104923b80> = os.path.isdir

plugins/superheroes/lib/tests/test_mode_migrate.py:203: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_mode_migrate.py::test_preview_leaves_absent_registry_absent
1 failed in 0.45s
```

**Restore:** re-applied `persist_backfill=False` on the `resolve` call in `enumerate_flip`.

**Restore receipt:** `current = mode_registry.resolve(cwd, root, persist_backfill=False)["mode"]`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.26s
```

---

## BP-2 — `test_preview_migration_cannot_be_executed`

**Guarded element:** `execute()` `owner_authorized is not True` gate — axis: preview Migration refused at the sink.

**Neutralization:** disabled the gate with `if False and migration.owner_authorized is not True:` in `execute()`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_mode_migrate.py::test_preview_migration_cannot_be_executed -q -p no:warnings
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_preview_migration_cannot_be_executed ____________________
...
E       AssertionError: assert 'done' == 'blocked'
E         
E         - blocked
E         + done

plugins/superheroes/lib/tests/test_mode_migrate.py:220: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_mode_migrate.py::test_preview_migration_cannot_be_executed
1 failed in 0.45s
```

**Restore:** restored `if migration.owner_authorized is not True:` (removed `False and`).

**Restore receipt:** `if migration.owner_authorized is not True:` gate active in `execute()`.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.45s
```

---

## BP-3 — `test_plan_authorized_migration_still_executes`

**Guarded element:** `plan()` authorized path mints `owner_authorized=True` — axis: authorized plan Migration still executes.

**Neutralization:** changed `m.owner_authorized = True` to `m.owner_authorized = False` in `plan()`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_mode_migrate.py::test_plan_authorized_migration_still_executes -q -p no:warnings
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_plan_authorized_migration_still_executes ________________
...
E       assert False is True
E        +  where False = <mode_migrate.Migration object at 0x102129670>.owner_authorized

plugins/superheroes/lib/tests/test_mode_migrate.py:232: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_mode_migrate.py::test_plan_authorized_migration_still_executes
1 failed in 0.45s
```

**Restore:** restored `m.owner_authorized = True` in `plan()`.

**Restore receipt:** `m.owner_authorized = True`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.45s
```

---

## BP-4 — `test_persist_backfill_false_does_not_write_and_default_still_does`

**Guarded element:** `resolve()` `persist_backfill=False` branch — axis: evidence reported without writing; default still backfills.

**Neutralization:** made the `persist_backfill=False` branch call `write_registry` before returning:
```python
if not persist_backfill:
    remote_hash = store_core.derive_identifiers(cwd)["remote_hash"]
    write_registry(cwd, verdict, remote_hash, root)
    return {"mode": verdict, "authoritative": False,
            "source": "evidence", "evidence": verdict}
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_mode_registry.py::test_persist_backfill_false_does_not_write_and_default_still_does -q -p no:warnings
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_persist_backfill_false_does_not_write_and_default_still_does _______
...
>       assert not os.path.isdir(store_dir)
E       AssertionError: assert not True
E        +  where True = <function isdir at 0x10483baf0>('.../store/projects/6d5c8a7b6c8ca477')

plugins/superheroes/lib/tests/test_mode_registry.py:606: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_mode_registry.py::test_persist_backfill_false_does_not_write_and_default_still_does
1 failed in 0.45s
```

**Restore:** removed the `write_registry` call from the `persist_backfill=False` branch (early return only).

**Restore receipt:**
```python
if not persist_backfill:
    return {"mode": verdict, "authoritative": False,
            "source": "evidence", "evidence": verdict}
```

**Green run:**
```
.                                                                        [100%]
1 passed in 0.45s
```
