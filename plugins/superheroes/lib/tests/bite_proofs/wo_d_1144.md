# WO-D (#1144) bite-proofs — architect_config disclosures carrier

## BP-D1 — `disclosures` survives `_migrate` (`test_disclosures_survive_roundtrip`)

**Guarded element:** `_migrate` return dict — axis: `disclosures` must survive normalization, not be dropped.

**Neutralization:** removed `"disclosures": disclosures` from the `_migrate` return dict (temporary edit; restored after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_architect_config.py::test_disclosures_survive_roundtrip -vv --tb=short -p no:xdist
```

**Red run:**
```
plugins/superheroes/lib/tests/test_architect_config.py::test_disclosures_survive_roundtrip FAILED [100%]
=================================== FAILURES ===================================
______________________ test_disclosures_survive_roundtrip ______________________
plugins/superheroes/lib/tests/test_architect_config.py:55: in test_disclosures_survive_roundtrip
    assert written["disclosures"] == [disclosure]
E   KeyError: 'disclosures'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_architect_config.py::test_disclosures_survive_roundtrip - KeyError: 'disclosures'
============================== 1 failed in 0.18s ===============================
```

**Restore:** re-added `"disclosures": disclosures` to the `_migrate` return dict.

**Restore receipt:** `_migrate` again returns `{"schemaVersion", "location", "visibility", "confirmed", "disclosures"}`.

**Green run:**
```
.                                                                        [100%]
1 passed in 2.36s
```

---

## BP-D2 — old-record upgrade path D-E1 (`test_old_schema_record_migrates_disclosures_to_empty`)

**Guarded element:** `_normalize_disclosures(None)` — axis: a schemaVersion-1 record with no `disclosures` key migrates to `[]`, never raising or discarding.

**Neutralization:** changed `_normalize_disclosures` so `raw is None` returns `["wrong-default"]` instead of `[]` (temporary edit; restored after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_architect_config.py::test_old_schema_record_migrates_disclosures_to_empty -vv --tb=short -p no:xdist
```

**Red run:**
```
plugins/superheroes/lib/tests/test_architect_config.py::test_old_schema_record_migrates_disclosures_to_empty FAILED [100%]
=================================== FAILURES ===================================
_____________ test_old_schema_record_migrates_disclosures_to_empty _____________
plugins/superheroes/lib/tests/test_architect_config.py:76: in test_old_schema_record_migrates_disclosures_to_empty
    assert got["disclosures"] == []
E   AssertionError: assert ['wrong-default'] == []
E     
E     Left contains one more item: 'wrong-default'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_architect_config.py::test_old_schema_record_migrates_disclosures_to_empty
```

**Restore:** restored `if raw is None: return []` in `_normalize_disclosures`.

**Restore receipt:** `_normalize_disclosures(None)` returns `[]` again.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.08s
```

---

## BP-D3 — round trip D-E2 (`test_disclosures_survive_roundtrip`)

**Guarded element:** `read_policy` return dict — axis: disclosures written by `write_policy` must round-trip through read.

**Neutralization:** removed `"disclosures": migrated["disclosures"]` from `read_policy`'s return dict (temporary edit; `_migrate` still preserved disclosures on disk; restored after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_architect_config.py::test_disclosures_survive_roundtrip -vv --tb=short -p no:xdist
```

**Red run:**
```
plugins/superheroes/lib/tests/test_architect_config.py::test_disclosures_survive_roundtrip FAILED [100%]
=================================== FAILURES ===================================
______________________ test_disclosures_survive_roundtrip ______________________
plugins/superheroes/lib/tests/test_architect_config.py:57: in test_disclosures_survive_roundtrip
    assert got["disclosures"] == [disclosure]
E   KeyError: 'disclosures'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_architect_config.py::test_disclosures_survive_roundtrip - KeyError: 'disclosures'
============================== 1 failed in 0.29s ===============================
```

**Restore:** re-added `"disclosures": migrated["disclosures"]` to `read_policy`'s return dict.

**Restore receipt:** `read_policy` again returns `disclosures` from the migrated record.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.08s
```
