# WO-B (#1151) bite-proofs — plugin-version-skew vocabulary home

## BP-B1 — append rule (`APPENDS_DEGRADATION`)

**Guarded element:** `APPENDS_DEGRADATION` / `appends_degradation()` — axis: only declared statuses append to `degradations`.

**Neutralization:** added `STATUS_CHECKED_CLEAN` to `APPENDS_DEGRADATION` in `plugins/superheroes/lib/version_skew.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-b -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_append_rule_phrase_pin plugins/superheroes/lib/tests/test_seat_map.py::test_compose_skew_record_appends_degradation_only_when_degraded -q
```

**Red run (exit 1):**
```
=================================== FAILURES ===================================
_______________ test_plugin_version_skew_append_rule_phrase_pin ________________
...
E       AssertionError: APPENDS_DEGRADATION changed (['checked-clean', 'checked-degraded']) — update the doc phrasing contract in this test
...
 test_compose_skew_record_appends_degradation_only_when_degraded[checked-clean-no-divergence-False] _
...
E           AssertionError: assert [{'constraint...r test', ...}] == []
...
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_append_rule_phrase_pin
FAILED plugins/superheroes/lib/tests/test_seat_map.py::test_compose_skew_record_appends_degradation_only_when_degraded[checked-clean-no-divergence-False]
2 failed, 2 passed in 0.40s
```

**Restore:** removed `STATUS_CHECKED_CLEAN` from `APPENDS_DEGRADATION` (inverse of neutralization).

**Restore receipt:** `APPENDS_DEGRADATION = frozenset({STATUS_CHECKED_DEGRADED})`

**Green run (exit 0):**
```
4 passed in 0.40s
```

---

## BP-B2 — watch-set doc census

**Guarded element:** `test_plugin_version_skew_watch_set_doc_census` — axis: any line naming two+ `SEMANTICS_FILES` entries must name all of them.

**Neutralization:** dropped `lib/version_skew.py` from the watch-set line in `CONVENTIONS.md` line 682 (left only `lib/model_registry.py` and `lib/seat_map.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-b -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_watch_set_doc_census -q
```

**Red run (exit 1):**
```
E       AssertionError: watch-set restatement missing SEMANTICS_FILES entries — file, line, missing: [('CONVENTIONS.md', 682, ['lib/version_skew.py'])]
...
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_watch_set_doc_census
1 failed in 0.33s
```

**Restore:** re-added `lib/version_skew.py` to the `CONVENTIONS.md` line 682 watch-set list.

**Restore receipt:** ``plugin's `lib/model_registry.py`, `lib/seat_map.py`, or `lib/version_skew.py` diverges from``

**Green run (exit 0):**
```
1 passed in 0.33s
```

---

## BP-B3 — status-token copy register census

**Guarded element:** `test_plugin_version_skew_status_token_copy_register_census` — axis: every `.md` carrying a `STATUSES` token must be in the register.

**Neutralization:** appended `<!-- bite-proof neutralization: not-checked -->` to `README.md` (not in register).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-b -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_status_token_copy_register_census -q
```

**Red run (exit 1):**
```
E       AssertionError: unregistered plugin-version-skew status-token copy — add to _PLUGIN_VERSION_SKEW_STATUS_TOKEN_COPY_REGISTER: ['README.md']
...
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_status_token_copy_register_census
1 failed in 0.28s
```

**Restore:** removed the `README.md` comment line (inverse of neutralization).

**Restore receipt:** `[MIT](LICENSE) © Zach Rose` (no trailing comment)

**Green run (exit 0):**
```
1 passed in 0.28s
```

---

## BP-B4 — chokepoint census

**Guarded element:** `test_plugin_version_skew_chokepoint_census` — axis: no non-test `.py` outside `version_skew.py` may reference `STATUS_CHECKED_DEGRADED` or `checked-degraded` literals; `seat_map.py` must call `appends_degradation(`.

**Neutralization:** replaced `version_skew.appends_degradation(skew_record.get("status"))` with `skew_record.get("status") == version_skew.STATUS_CHECKED_DEGRADED` at `seat_map.py` line 1089.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-b -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_chokepoint_census -q
```

**Red run (exit 1):**
```
E       AssertionError: plugin-version-skew chokepoint violation outside version_skew.py: [('lib/seat_map.py', 'STATUS_CHECKED_DEGRADED')]
...
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_plugin_version_skew_chokepoint_census
1 failed in 0.31s
```

**Restore:** restored `if version_skew.appends_degradation(skew_record.get("status")):` at `seat_map.py` line 1089.

**Restore receipt:** `if version_skew.appends_degradation(skew_record.get("status")):`

**Green run (exit 0):**
```
1 passed in 0.31s
```

---

## BP-B5 — `SEMANTICS_FILES` membership of `lib/version_skew.py`

**Guarded element:** `SEMANTICS_FILES` includes `lib/version_skew.py` — axis: divergence in the home module is detected as `checked-degraded` / `semantics-divergent`.

**Neutralization:** removed `"lib/version_skew.py"` from `SEMANTICS_FILES` in `version_skew.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-b -m pytest plugins/superheroes/lib/tests/test_version_skew.py::test_skew_version_skew_divergent -q
```

**Red run (exit 1):**
```
E       AssertionError: assert 'checked-clean' == 'checked-degraded'
...
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_version_skew.py::test_skew_version_skew_divergent
1 failed in 0.33s
```

**Restore:** re-added `"lib/version_skew.py"` to `SEMANTICS_FILES`.

**Restore receipt:**
```
SEMANTICS_FILES = (
    "lib/model_registry.py",
    "lib/seat_map.py",
    "lib/version_skew.py",
)
```

**Green run (exit 0):**
```
1 passed in 0.33s
```
