# WO-F (#1145 continuation-3) bite-proofs — resultKind absence + fedPrompt delimiter

## BP1 — resultKind absence in seat-facing contract

**Guarded element:** `test_review_result_contract_unpinned_never_instructs_result_kind` — axis: unpinned seat-facing contract must not mention emitting a `resultKind` field.

**Neutralization:** re-added the `resultKind` instruction lines to `REVIEW_RESULT_CONTRACT` in `plugins/superheroes/lib/engine_adapter.py` (pinned branch: `"The graded result must carry \`resultKind\`: \"%s\" naming exactly this payload.\n"`; unpinned branch: `"The graded result must carry \`resultKind\` naming exactly one payload key of that name."`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_adapter.py::test_review_result_contract_unpinned_never_instructs_result_kind' -q
```

**Raw red:**
```
FAILED plugins/superheroes/lib/tests/test_engine_adapter.py::test_review_result_contract_unpinned_never_instructs_result_kind
>       assert "resultKind" not in contract
E       AssertionError: assert 'resultKind' not in 'Review resu...it ruling)\n'
E         
E         'resultKind' is contained here:
E           st carry `resultKind` naming exactly one payload key of that name.
1 failed in 0.30s
```

**Restore:** removed both `resultKind` instruction lines from `REVIEW_RESULT_CONTRACT` (pinned and unpinned branches).

**Restore receipt:** `git status --porcelain` over production paths shows only intended WO-F edits:
```
 M plugins/superheroes/lib/engine_adapter.py
 M plugins/superheroes/lib/engine_dispatch.py
```

**Raw green:** see post-restore batch below (`2 passed in 0.71s`).

---

## BP2 — fedPrompt section delimiter

**Guarded element:** `test_review_dispatch_fed_prompt_blocks_never_abut` — axis: example JSON closing brace must never abut the contract heading; contract must be preceded by `\n\n`.

**Neutralization:** removed `_prompt_section_sep = "\n\n"` and reverted to bare `+=` concatenation for the example block and contract append (`plugins/superheroes/lib/engine_dispatch.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_blocks_never_abut' -q
```

**Raw red:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_blocks_never_abut
>               assert pos >= 2 and prompt_text[pos - 2:pos] == "\n\n"
E               AssertionError: assert (1200 >= 2 and '.\n' == '\n\n'
E                 
E                 Strings contain only whitespace, escaping them using repr()
E                 - '\n\n'
E                 + '.\n')
1 failed in 0.66s
```

**Restore:** reinstated `_prompt_section_sep = "\n\n"` before both the example-block and contract appends.

**Restore receipt:**
```
            _prompt_section_sep = "\n\n"
            if expected_result_kind == "findings":
                fed_prompt += _prompt_section_sep + review_findings_schema.example_prompt_block(echo_nonce)
            fed_prompt += _prompt_section_sep + engine_adapter.REVIEW_RESULT_CONTRACT(expected_result_kind)
```

**Raw green:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_adapter.py::test_review_result_contract_unpinned_never_instructs_result_kind' 'plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_dispatch_fed_prompt_blocks_never_abut' -q
```
```
..                                                                       [100%]
2 passed in 0.71s
```
