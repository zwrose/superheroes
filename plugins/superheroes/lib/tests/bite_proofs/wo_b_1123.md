# WO-B (#1123) bite-proofs — payload semantics registry

## BP-B1 — census per-kind: `findings`

**Guarded element:** `_REVIEW_PAYLOAD_SEMANTICS['findings']` — axis: per-kind registry coverage.

**Neutralization:** deleted only the `"findings"` entry from `_REVIEW_PAYLOAD_SEMANTICS` in `plugins/superheroes/lib/engine_adapter.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_every_registered_kind_has_payload_semantics -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_every_registered_kind_has_payload_semantics _______________
...
E           AssertionError: review result kind registries differ; symmetric difference=['findings']

plugins/superheroes/lib/tests/test_review_payload_semantics.py:39: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_every_registered_kind_has_payload_semantics
1 failed in 0.16s
```

**Restore:** re-inserted the `"findings": _ReviewPayloadSemantics(...)` entry (inverse of neutralization).

**Restore receipt:** `"findings": _ReviewPayloadSemantics("findings", _carries_findings, _nonempty_findings, _engaged_findings),`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.20s
```

---

## BP-B2 — census per-kind: `verdicts`

**Guarded element:** `_REVIEW_PAYLOAD_SEMANTICS['verdicts']` — axis: per-kind registry coverage.

**Neutralization:** deleted only the `"verdicts"` entry from `_REVIEW_PAYLOAD_SEMANTICS`.

**Command:** (same node id as BP-B1)

**Red run:**
```
E           AssertionError: review result kind registries differ; symmetric difference=['verdicts']
```

**Restore:** re-inserted the `"verdicts": _ReviewPayloadSemantics(...)` entry.

**Restore receipt:** `"verdicts": _ReviewPayloadSemantics("verdicts", _carries_verdicts, _nonempty_verdicts, _engaged_verdicts),`

**Green run:** `1 passed in 0.20s` (census test after restore)

---

## BP-B3 — census per-kind: `grouping`

**Guarded element:** `_REVIEW_PAYLOAD_SEMANTICS['grouping']` — axis: per-kind registry coverage.

**Neutralization:** deleted only the `"grouping"` entry from `_REVIEW_PAYLOAD_SEMANTICS`.

**Command:** (same node id as BP-B1)

**Red run:**
```
E           AssertionError: review result kind registries differ; symmetric difference=['grouping']
```

**Restore:** re-inserted the `"grouping": _ReviewPayloadSemantics(...)` entry.

**Restore receipt:** `"grouping": _ReviewPayloadSemantics("grouping", _carries_grouping, _nonempty_grouping, _engaged_grouping),`

**Green run:** `1 passed` (census test after restore)

---

## BP-B4 — census per-kind: `ruling`

**Guarded element:** `_REVIEW_PAYLOAD_SEMANTICS['ruling']` — axis: per-kind registry coverage.

**Neutralization:** deleted only the `"ruling"` entry from `_REVIEW_PAYLOAD_SEMANTICS`.

**Command:** (same node id as BP-B1)

**Red run:**
```
E           AssertionError: review result kind registries differ; symmetric difference=['ruling']
```

**Restore:** re-inserted the `"ruling": _ReviewPayloadSemantics(...)` entry.

**Restore receipt:** `"ruling": _ReviewPayloadSemantics("ruling", _carries_ruling, _nonempty_ruling, _engaged_ruling),`

**Green run:** `1 passed` (census test after restore)

---

## BP-B5 — parity matrix: `_parse_review_has_payload` grouping branch

**Guarded element:** `test_derived_sites_agree_with_the_registry` — axis: dispatch derives nonempty from registry, not a hand `return True` for grouping.

**Neutralization:** in `plugins/superheroes/lib/engine_dispatch.py` `_parse_review_has_payload`, inserted pre-registry branch before the registry call:
```python
    if kind == "grouping":
        return True
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_derived_sites_agree_with_the_registry -q
```

**Red run:**
```
.........FF.........                                                     [100%]
...
E           AssertionError: assert True == False
E            +  where True = <function _parse_review_has_payload ...>({'grouping': [], 'ok': True, 'resultKind': 'grouping'})
E            +  and   False = <function review_payload_nonempty ...>('grouping', [])
...
FAILED plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_derived_sites_agree_with_the_registry[grouping-dispatch_result9-engagement_input9]
FAILED plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_derived_sites_agree_with_the_registry[grouping-dispatch_result10-engagement_input10]
2 failed, 18 passed in 1.39s
```

**Restore:** removed the `if kind == "grouping": return True` branch; function again ends with `return engine_adapter.review_payload_nonempty(kind, payload)`.

**Restore receipt:** trailing body is only `return engine_adapter.review_payload_nonempty(kind, payload)` after the `if not has_payload: return False` guard.

**Green run:**
```
....................                                                     [100%]
20 passed in 1.39s
```
