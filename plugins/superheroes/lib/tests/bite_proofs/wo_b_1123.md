# WO-B (#1123) bite-proofs — payload semantics registry

## BP-B1 — census per-kind: `findings`

**Guarded element:** `_REVIEW_PAYLOAD_SEMANTICS['findings']` — axis: per-kind registry coverage.

**Neutralization:** deleted only the `"findings"` entry from `_REVIEW_PAYLOAD_SEMANTICS` in `plugins/superheroes/lib/engine_adapter.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_every_registered_kind_has_payload_semantics -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_every_registered_kind_has_payload_semantics _______________
...
E           AssertionError: review result kind registries differ; symmetric difference=['findings']

plugins/superheroes/lib/tests/test_review_payload_semantics.py:39: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_every_registered_kind_has_payload_semantics
1 failed in 0.16s
```

**Restore:** re-inserted the `"findings": _ReviewPayloadSemantics(...)` entry (inverse of neutralization).

**Restore receipt:** `"findings": _ReviewPayloadSemantics("findings", _carries_findings, _nonempty_findings, _engaged_findings),`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.20s
```

---

## BP-B2 — census per-kind: `verdicts`

**Guarded element:** `_REVIEW_PAYLOAD_SEMANTICS['verdicts']` — axis: per-kind registry coverage.

**Neutralization:** deleted only the `"verdicts"` entry from `_REVIEW_PAYLOAD_SEMANTICS`.

**Command:** (same node id as BP-B1)

**Red run:**
```
E           AssertionError: review result kind registries differ; symmetric difference=['verdicts']
```

**Restore:** re-inserted the `"verdicts": _ReviewPayloadSemantics(...)` entry.

**Restore receipt:** `"verdicts": _ReviewPayloadSemantics("verdicts", _carries_verdicts, _nonempty_verdicts, _engaged_verdicts),`

**Green run:** `1 passed in 0.20s` (census test after restore)

---

## BP-B3 — census per-kind: `grouping`

**Guarded element:** `_REVIEW_PAYLOAD_SEMANTICS['grouping']` — axis: per-kind registry coverage.

**Neutralization:** deleted only the `"grouping"` entry from `_REVIEW_PAYLOAD_SEMANTICS`.

**Command:** (same node id as BP-B1)

**Red run:**
```
E           AssertionError: review result kind registries differ; symmetric difference=['grouping']
```

**Restore:** re-inserted the `"grouping": _ReviewPayloadSemantics(...)` entry.

**Restore receipt:** `"grouping": _ReviewPayloadSemantics("grouping", _carries_grouping, _nonempty_grouping, _engaged_grouping),`

**Green run:** `1 passed` (census test after restore)

---

## BP-B4 — census per-kind: `ruling`

**Guarded element:** `_REVIEW_PAYLOAD_SEMANTICS['ruling']` — axis: per-kind registry coverage.

**Neutralization:** deleted only the `"ruling"` entry from `_REVIEW_PAYLOAD_SEMANTICS`.

**Command:** (same node id as BP-B1)

**Red run:**
```
E           AssertionError: review result kind registries differ; symmetric difference=['ruling']
```

**Restore:** re-inserted the `"ruling": _ReviewPayloadSemantics(...)` entry.

**Restore receipt:** `"ruling": _ReviewPayloadSemantics("ruling", _carries_ruling, _nonempty_ruling, _engaged_ruling),`

**Green run:** `1 passed` (census test after restore)

---

## BP-B5 — parity matrix: `_parse_review_has_payload` grouping branch

**Guarded element:** `test_derived_sites_agree_with_the_registry` — axis: dispatch derives nonempty from registry, not a hand `return True` for grouping.

**Neutralization:** in `plugins/superheroes/lib/engine_dispatch.py` `_parse_review_has_payload`, inserted pre-registry branch before the registry call:
```python
    if kind == "grouping":
        return True
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_derived_sites_agree_with_the_registry -q
```

**Red run:**
```
.........FF.........                                                     [100%]
...
E           AssertionError: assert True == False
E            +  where True = <function _parse_review_has_payload ...>({'grouping': [], 'ok': True, 'resultKind': 'grouping'})
E            +  and   False = <function review_payload_nonempty ...>('grouping', [])
...
FAILED plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_derived_sites_agree_with_the_registry[grouping-dispatch_result9-engagement_input9]
FAILED plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_derived_sites_agree_with_the_registry[grouping-dispatch_result10-engagement_input10]
2 failed, 18 passed in 1.39s
```

**Restore:** removed the `if kind == "grouping": return True` branch; function again ends with `return engine_adapter.review_payload_nonempty(kind, payload)`.

**Restore receipt:** trailing body is only `return engine_adapter.review_payload_nonempty(kind, payload)` after the `if not has_payload: return False` guard.

**Green run:**
```
....................                                                     [100%]
20 passed in 1.39s
```

---

## WO-C

### BP-C1 — per-kind `carries` no-value discipline: `findings`

**Guarded element:** `_carries_findings` — axis: not-carried branch returns no value (`(False, None)`).

**Neutralization:** changed the non-list branch from `return False, None` to `return False, value`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_carries_returns_no_value_when_not_carried[findings] -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_carries_returns_no_value_when_not_carried[findings] ___________
...
E       AssertionError: assert (False, 'oops') == (False, None)
...
FAILED ...::test_carries_returns_no_value_when_not_carried[findings]
1 failed in 0.16s
```

**Restore:** reverted non-list branch to `return False, None`.

**Restore receipt:** `return False, None`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.11s
```

---

### BP-C2 — per-kind `carries` no-value discipline: `verdicts`

**Guarded element:** `_carries_verdicts` — axis: not-carried branch returns no value.

**Neutralization:** changed the non-list branch from `return False, None` to `return False, value`.

**Command:** (same node id pattern with `[verdicts]`)

**Red run:**
```
E       AssertionError: assert (False, 'oops') == (False, None)
...
FAILED ...::test_carries_returns_no_value_when_not_carried[verdicts]
1 failed in 0.10s
```

**Restore:** reverted non-list branch to `return False, None`.

**Restore receipt:** `return False, None`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.08s
```

---

### BP-C3 — per-kind `carries` no-value discipline: `grouping`

**Guarded element:** `_carries_grouping` — axis: not-carried branch returns no value.

**Neutralization:** changed `return False, None` (key absent) to `return False, ""`.

**Command:** (node id `[grouping]`)

**Red run:**
```
E       AssertionError: assert (False, '') == (False, None)
...
FAILED ...::test_carries_returns_no_value_when_not_carried[grouping]
1 failed in 0.14s
```

**Restore:** reverted to `return False, None`.

**Restore receipt:** `return False, None`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.11s
```

---

### BP-C4 — per-kind `carries` no-value discipline: `ruling`

**Guarded element:** `_carries_ruling` — axis: not-carried branch returns no value.

**Neutralization:** changed the failing branch from `return False, None` to `return False, value`.

**Command:** (node id `[ruling]`)

**Red run:**
```
E       AssertionError: assert (False, 'oops') == (False, None)
...
FAILED ...::test_carries_returns_no_value_when_not_carried[ruling]
1 failed in 0.11s
```

**Restore:** reverted to `return False, None`.

**Restore receipt:** `return False, None`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.07s
```

---

### BP-C5 — private registry completeness (census fourth set)

**Guarded element:** `_REVIEW_PAYLOAD_SEMANTICS` — axis: private registry stays equal to the other three census sets.

**Neutralization:** deleted the `"ruling": _ReviewPayloadSemantics(...)` entry from `_REVIEW_PAYLOAD_SEMANTICS` in `plugins/superheroes/lib/engine_adapter.py`.

**Note:** Original BP-C5 mutated `REVIEW_PAYLOAD_SEMANTIC_KINDS`; WO-E deleted that constant.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_every_registered_kind_has_payload_semantics -q
```

**Red run:**
```
E           AssertionError: review result kind registries differ; union=['findings', 'grouping', 'ruling', 'verdicts']; _REVIEW_PAYLOAD_SEMANTICS missing=['ruling']
...
FAILED plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_every_registered_kind_has_payload_semantics
1 failed
```

**Restore:** re-inserted `"ruling": _ReviewPayloadSemantics("ruling", _carries_ruling, _nonempty_ruling, _engaged_ruling),`

**Restore receipt:** `"ruling": _ReviewPayloadSemantics("ruling", _carries_ruling, _nonempty_ruling, _engaged_ruling),`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.10s
```

---

## WO-D

### BP-D1 — census message names broken registry and missing kind (`_REVIEW_PAYLOAD_SEMANTICS`)

**Guarded element:** `test_every_registered_kind_has_payload_semantics` failure message — axis: per-registry diagnosis names the registry that is wrong and the kind it is missing (not XOR false positives).

**Neutralization:** deleted the `"ruling": _ReviewPayloadSemantics(...)` entry from `_REVIEW_PAYLOAD_SEMANTICS` in `plugins/superheroes/lib/engine_adapter.py`.

**Note:** Original BP-D1 mutated `REVIEW_PAYLOAD_SEMANTIC_KINDS`; WO-E deleted that constant.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_every_registered_kind_has_payload_semantics -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_every_registered_kind_has_payload_semantics _______________
...
E           AssertionError: review result kind registries differ; union=['findings', 'grouping', 'ruling', 'verdicts']; _REVIEW_PAYLOAD_SEMANTICS missing=['ruling']

plugins/superheroes/lib/tests/test_review_payload_semantics.py:45: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_every_registered_kind_has_payload_semantics
1 failed in 0.32s
```

**Restore:** re-inserted `"ruling": _ReviewPayloadSemantics("ruling", _carries_ruling, _nonempty_ruling, _engaged_ruling),`

**Restore receipt:** `"ruling": _ReviewPayloadSemantics("ruling", _carries_ruling, _nonempty_ruling, _engaged_ruling),`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.10s
```

---

### BP-D2 — census message names `_REVIEW_PAYLOAD_SEMANTICS` (and derived public tuple)

**Guarded element:** same census failure message — axis: per-registry report, not hard-coded to one registry name.

**Neutralization:** deleted the `"ruling": _ReviewPayloadSemantics(...)` entry from `_REVIEW_PAYLOAD_SEMANTICS` in `plugins/superheroes/lib/engine_adapter.py`. Auxiliary neutralization (WO-C module import guard): removed `"ruling": "oops"` from `_CARRIES_NOT_CARRIED_WRONG_TYPED` so pytest can collect the module; reverted with the primary restore.

**Command:** (same node id as BP-D1)

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_every_registered_kind_has_payload_semantics _______________
...
E           AssertionError: review result kind registries differ; union=['findings', 'grouping', 'ruling', 'verdicts']; REVIEW_PAYLOAD_SEMANTIC_KINDS missing=['ruling']; _REVIEW_PAYLOAD_SEMANTICS missing=['ruling']

plugins/superheroes/lib/tests/test_review_payload_semantics.py:46: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_payload_semantics.py::test_every_registered_kind_has_payload_semantics
1 failed in 0.17s
```

**Restore:** re-inserted the `"ruling": _ReviewPayloadSemantics(...)` entry; re-inserted `"ruling": "oops"` in `_CARRIES_NOT_CARRIED_WRONG_TYPED`.

**Restore receipt:** `"ruling": _ReviewPayloadSemantics("ruling", _carries_ruling, _nonempty_ruling, _engaged_ruling),` and `"ruling": "oops",`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.15s
```

---

## WO-E

### BP-E1a — `round_phases.py` safety-machinery guard

**Guarded element:** `escalation.SAFETY_MACHINERY` membership for `round_phases.py` — axis: fixer file-scope guard refuses the file under a band root.

**Neutralization:** removed `"round_phases.py",` line from `SAFETY_MACHINERY` in `plugins/superheroes/lib/escalation.py` only.

**Guard (red):** `{"allow": true, "degraded": false}`

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_escalation.py::test_safety_machinery_set_members_are_pinned plugins/superheroes/eval/tests/test_escalation_eval.py -q
```

**Red run:**
```
F..F.
2 failed, 3 passed in 0.17s
```

**Restore:** re-inserted `"round_phases.py",          # verifier-verdict enum + verify submit-shape guard (#1123)`

**Restore receipt:** `"round_phases.py",          # verifier-verdict enum + verify submit-shape guard (#1123)`

**Guard (green):** `{"allow": false, "degraded": false}`

**Green run:**
```
5 passed in 0.14s
```

---

### BP-E1b — `payload_contracts.py` safety-machinery guard

**Guarded element:** `escalation.SAFETY_MACHINERY` membership for `payload_contracts.py` — axis: fixer file-scope guard refuses the file under a band root.

**Neutralization:** removed `"payload_contracts.py",` line from `SAFETY_MACHINERY` in `plugins/superheroes/lib/escalation.py` only.

**Guard (red):** `{"allow": true, "degraded": false}`

**Command:** (same as BP-E1a)

**Red run:**
```
F..F.
2 failed, 3 passed in 0.50s
```

**Restore:** re-inserted `"payload_contracts.py",     # per-seat payload contracts engine_adapter enforces (#1123)`

**Restore receipt:** `"payload_contracts.py",     # per-seat payload contracts engine_adapter enforces (#1123)`

**Guard (green):** `{"allow": false, "degraded": false}`

**Green run:**
```
5 passed in 0.10s
```

---

### BP-E7 — census diagnoses registry mismatch (no collection error)

**Guarded element:** module import of `test_review_payload_semantics.py` — axis: registry mismatch yields test failure with census diagnosis, not `Interrupted: 1 error during collection`.

**Neutralization:** deleted `"ruling": _ReviewPayloadSemantics(...)` from `_REVIEW_PAYLOAD_SEMANTICS` in `engine_adapter.py`.

**Old output (WO-C module-level assert, order-measured):**
```
ERROR plugins/superheroes/lib/tests/test_review_payload_semantics.py - Assert...
!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
1 error in 0.19s
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_payload_semantics.py -q
```

**Red run:**
```
2 failed, 25 passed in 0.13s
E           AssertionError: review result kind registries differ; union=['findings', 'grouping', 'ruling', 'verdicts']; _REVIEW_PAYLOAD_SEMANTICS missing=['ruling']
```

**Restore:** re-inserted `"ruling": _ReviewPayloadSemantics("ruling", _carries_ruling, _nonempty_ruling, _engaged_ruling),`

**Restore receipt:** `"ruling": _ReviewPayloadSemantics("ruling", _carries_ruling, _nonempty_ruling, _engaged_ruling),`

**Green run:**
```
29 passed in 0.56s
```

---

## WO-F

### BP-F1 — corrected `payload_contracts` closure pin still bites

**Guarded element:** `test_payload_contracts_import_closure_is_pinned` — axis: equality pin rejects a round-layer import added to `payload_contracts.py`.

**Neutralization:** added `import verification  # noqa: E402` at module scope in `plugins/superheroes/lib/payload_contracts.py` after the existing direct imports.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_payload_contracts_layering.py::test_payload_contracts_import_closure_is_pinned -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_payload_contracts_import_closure_is_pinned ________________
...
E           Failed: payload_contracts import closure mismatch: unexpected=['circuit_breaker', 'loop_state', 'panel_tally', 'review_result', 'verification'] missing=[]

plugins/superheroes/lib/tests/test_payload_contracts_layering.py:107: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_payload_contracts_layering.py::test_payload_contracts_import_closure_is_pinned
1 failed in 0.39s
```

**Restore:** removed the `import verification  # noqa: E402` line (inverse of neutralization).

**Restore receipt:** direct imports are only `import audits`, `import dispatch_outcome`, `import round_phases` — no `import verification`.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.57s
```

**Cross-pin observation (same mutation, not restored between):** `test_engine_adapter_import_closure_is_pinned` also goes red — the pins are not independent; a round-layer import in `payload_contracts` enlarges `engine_adapter`'s closure transitively.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_payload_contracts_layering.py::test_engine_adapter_import_closure_is_pinned -q
```

**Red run (under mutation):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_engine_adapter_import_closure_is_pinned _________________
...
E           Failed: engine_adapter import closure mismatch: unexpected=['circuit_breaker', 'loop_state', 'panel_tally', 'review_result', 'verification'] missing=[]

plugins/superheroes/lib/tests/test_payload_contracts_layering.py:94: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_payload_contracts_layering.py::test_engine_adapter_import_closure_is_pinned
1 failed in 0.72s
```

**Green run (after restore):**
```
.                                                                        [100%]
1 passed in 0.50s
```
