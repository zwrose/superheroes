# Bite-proof record — wo/1107-c4B (unencodable manifest version must not crash skew detection)

Per `plugins/superheroes/rubric/bite-proof.md`. One guarded element, one neutralization, one red,
one restore, one green.

## Element 1 — unencodable manifest version gate (`test_repo_manifest_lone_surrogate_version_evidence_unreadable`)

**Guarded element:** `plugins/superheroes/lib/tests/test_version_skew.py::test_repo_manifest_lone_surrogate_version_evidence_unreadable`,
which pins that a repo `plugin.json` whose `version` is a lone surrogate (accepted by `json.loads`
but unencodable as UTF-8) yields `checked-degraded` / `evidence-unreadable` rather than raising or
returning `checked-clean`.
**Axis:** fail-closed — unencodable version evidence is unreadable, not proof of no skew (#1107).

**Neutralization:** appended `and False` to the unencodable-version gate in `detect()` so the
degrading path is skipped while `_read_repo_version` remains safe:
```python
    if (
        _manifest_version_unencodable(manifest) or _manifest_version_unencodable(
            installed_manifest,
        )
    ) and False:  # WO-c4B neutralization — removed after red capture
```

**Raw red** (`/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-B -m pytest plugins/superheroes/lib/tests/test_version_skew.py::test_repo_manifest_lone_surrogate_version_evidence_unreadable -q`):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_repo_manifest_lone_surrogate_version_evidence_unreadable _________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-10880/test_repo_manifest_lone_surrog0')

    def test_repo_manifest_lone_surrogate_version_evidence_unreadable(tmp_path):
        repo = tmp_path / "repo"
        manifest_path = repo / "plugins" / "superheroes" / ".claude-plugin" / "plugin.json"
        manifest_path.parent.mkdir(parents=True, exist_ok=True)
        manifest_path.write_text(
            json.dumps({"name": "superheroes", "version": "\ud800"}),
            encoding="utf-8",
        )
        _copy_semantics_to(repo)
        plugin = _plugin_tree(tmp_path)
        record = VS.detect(str(repo), plugin)
        _assert_record_shape(record)
>       assert record["status"] == "checked-degraded"
E       AssertionError: assert 'checked-clean' == 'checked-degraded'
E         
E         - checked-degraded
E         + checked-clean

plugins/superheroes/lib/tests/test_version_skew.py:462: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_version_skew.py::test_repo_manifest_lone_surrogate_version_evidence_unreadable
1 failed in 0.08s
```

**Restore:** inverse edit — removed `and False` and the WO-c4B comment, restoring:
```python
    if _manifest_version_unencodable(manifest) or _manifest_version_unencodable(
        installed_manifest,
    ):
```

**Restore receipt:** restored lines quoted above; `git diff plugins/superheroes/lib/version_skew.py`
shows no `and False` or `WO-c4B neutralization` token after restore.

**Raw green** (same command, post-restore):
```
.                                                                        [100%]
1 passed in 0.08s
```
