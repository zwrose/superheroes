# WO-A (#1165) bite-proofs — `_compose_stall_fix_batch` stall union detectors

## BP1 — union widening (`test_handle_stall_unions_stalled_alias_with_not_discharged_targets`)

**Guarded element:** `_union_open_blockers(stalled, nd_targets)` on the stalled-non-empty path — axis: stalled alias ∪ not-discharged audit targets.

**Neutralization:** `batch = stalled` (drop the union).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_unions_stalled_alias_with_not_discharged_targets" -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______ test_handle_stall_unions_stalled_alias_with_not_discharged_targets ______

    def test_handle_stall_unions_stalled_alias_with_not_discharged_targets():
        ...
>       assert len(batch) == 2
E       AssertionError: assert 1 == 2
E        +  where 1 = len([{'classKey': 'Security::CWE-401::orig', 'dimension': 'Security', 'file': 'f.py', 'id': 'f.py::old title@L1', ...}])
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_unions_stalled_alias_with_not_discharged_targets
1 failed in 1.37s
```

**Restore:** reverted `batch = stalled` to `batch = _union_open_blockers(stalled, nd_targets)`.

**Restore receipt:**
```
 M plugins/superheroes/lib/round_driver.py
```

**Green run:**
```
.                                                                        [100%]
1 passed in 1.29s
```

---

## BP2 — no second open target dropped (`test_stall_batch_never_drops_a_second_open_target_regression_1165`)

**Guarded element:** same union on stalled-non-empty path — axis: no second open not-discharged target dropped.

**Neutralization:** `batch = stalled` (same as BP1; separate red run).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_stall_batch_never_drops_a_second_open_target_regression_1165" -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_stall_batch_never_drops_a_second_open_target_regression_1165 ______

    def test_stall_batch_never_drops_a_second_open_target_regression_1165():
        ...
>       assert {b["id"] for b in batch} == {target_a["id"], target_b["id"]}
E       AssertionError: assert {'a.py::first@L1'} == {'a.py::first...y::second@L2'}
E         
E         Extra items in the right set:
E         'b.py::second@L2'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_audit_target_identity.py::test_stall_batch_never_drops_a_second_open_target_regression_1165
1 failed in 1.24s
```

**Restore:** reverted `batch = stalled` to `batch = _union_open_blockers(stalled, nd_targets)`.

**Restore receipt:**
```
 M plugins/superheroes/lib/round_driver.py
```

**Green run:**
```
.                                                                        [100%]
1 passed in 1.29s
```

---

## BP3 — never new blocking findings (`test_handle_stall_unions_exclude_new_blocking_findings`)

**Guarded element:** stalled-path union must not admit `_blocking(findings)` — axis: never new blocking findings.

**Neutralization:** `batch = _union_open_blockers(stalled, nd_targets + _blocking(state.get("findings") or []))`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_unions_exclude_new_blocking_findings" -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_handle_stall_unions_exclude_new_blocking_findings ____________

    def test_handle_stall_unions_exclude_new_blocking_findings():
        ...
>       assert len(batch) == 2
E       AssertionError: assert 3 == 2
E        +  where 3 = len([{...}, {...}, {'file': 'h.py', 'line': 9, 'severity': 'Critical', 'title': 'fresh blocker'}])
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_unions_exclude_new_blocking_findings
1 failed in 1.38s
```

**Restore:** removed `_blocking(...)` from the stalled-path union argument.

**Restore receipt:**
```
 M plugins/superheroes/lib/round_driver.py
```

**Green run:**
```
.                                                                        [100%]
1 passed in 1.43s
```

---

## BP4 — malformed open set parks (`test_handle_stall_malformed_not_discharged_parks_with_stalled_target`)

**Guarded element:** early `nd_raw is not None` + `_open_audit_ids_from_not_discharged` None → `REFUSAL_UNRESOLVABLE_OPEN_SET` — axis: present-but-unreadable open set fails closed.

**Neutralization:** deleted the malformed-outcome park branch (three lines after `nd_raw = outcome.get("notDischarged")`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_malformed_not_discharged_parks_with_stalled_target" -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_handle_stall_malformed_not_discharged_parks_with_stalled_target ________

    def test_handle_stall_malformed_not_discharged_parks_with_stalled_target():
        ...
>       assert state["step"] == RD.P_TERMINAL
E       AssertionError: assert 'dispatch-fixer' == 'terminal'
E         
E         - terminal
E         + dispatch-fixer
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_malformed_not_discharged_parks_with_stalled_target
1 failed in 5.12s
```

**Restore:** restored the malformed-outcome park branch.

**Restore receipt:**
```
 M plugins/superheroes/lib/round_driver.py
```

**Green run:**
```
.                                                                        [100%]
1 passed in 4.58s
```

---

## BP5 — unrepresentable open id refuses (`test_handle_stall_uncomposable_open_id_refuses`)

**Guarded element:** `nd_ids - matched_ids` refusal on stalled-non-empty path — axis: an unrepresentable open target refuses, never partial dispatch.

**Neutralization:** deleted the `matched_ids` / `nd_ids - matched_ids` refusal block.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_uncomposable_open_id_refuses" -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_handle_stall_uncomposable_open_id_refuses ________________

    def test_handle_stall_uncomposable_open_id_refuses():
        ...
>       assert state["step"] == RD.P_TERMINAL
E       AssertionError: assert 'dispatch-fixer' == 'terminal'
E         
E         - terminal
E         + dispatch-fixer
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_uncomposable_open_id_refuses
1 failed in 4.04s
```

**Restore:** restored the uncomposable-open-id refusal block.

**Restore receipt:**
```
 M plugins/superheroes/lib/round_driver.py
```

**Green run:**
```
.                                                                        [100%]
1 passed in 4.11s
```

---

## BP6 — first-wins never superseded (`test_union_open_blockers_never_downgrades_an_earlier_entry`)

**Guarded element:** `_union_open_blockers` — axis: first-wins; an admitted entry is never superseded.

**Neutralization:** when an id-bearing item arrives at a location with a prior id-less entry, remove the id-less item and admit the id-bearing one (replace `if loc_key in idless_locs: continue` with a filter-and-discard block).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_union_open_blockers_never_downgrades_an_earlier_entry" -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_union_open_blockers_never_downgrades_an_earlier_entry __________

    def test_union_open_blockers_never_downgrades_an_earlier_entry():
        ...
>       assert batch[0]["severity"] == "Critical"
E       AssertionError: assert 'Important' == 'Critical'
E         
E         - Critical
E         + Important

plugins/superheroes/lib/tests/test_audit_target_identity.py:610: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_audit_target_identity.py::test_union_open_blockers_never_downgrades_an_earlier_entry
1 failed in 0.24s
```

**Restore:** reverted the id-bearing branch's **second** guard to `if loc_key in idless_locs: continue`
(skip, do not supersede) — that is the line the neutralization above replaced.

**Restore receipt:**
```
 M plugins/superheroes/lib/round_driver.py
```
(the implementer's own receipt, taken while its whole order was still uncommitted — see the
orchestrator re-run below for a clean one)

**Orchestrator re-run (authoritative).** Re-run against the committed tree at `65611cb2`. A first
attempt used a filter keyed on `(b.get("identity") or "")`, which did **not** match the id-less
finding (it carries no `identity` key), so the red landed on `assert 2 == 1` — the **count**, not the
severity axis this detector claims. Recorded here because it is the rubric's "bites on the wrong
axis" trap. The corrected neutralization keyed the filter on `finding_identity(b)` and produced the
axis-correct red `AssertionError: assert 'Important' == 'Critical'`; the same input through the
helper returned `severity=Important` under neutralization and `severity=Critical` after restore.
`git status --porcelain plugins/superheroes/lib/round_driver.py` came back **empty** after restore,
then `1 passed in 0.14s`.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.14s
```

---

## BP7 — distinct occurrence ids both survive (`test_union_open_blockers_keeps_distinct_occurrence_ids`)

**Guarded element:** `_union_open_blockers` — axis: distinct occurrence ids at one location both survive.

**Neutralization:** dedupe id-bearing items on per-location key (`if loc_key in seen_locs: continue`) instead of allowing distinct ids at the same location.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_union_open_blockers_keeps_distinct_occurrence_ids" -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_union_open_blockers_keeps_distinct_occurrence_ids ____________

    def test_union_open_blockers_keeps_distinct_occurrence_ids():
        ...
>       assert len(batch) == 2
E       AssertionError: assert 1 == 2
E        +  where 1 = len([{'file': 'f.py', 'id': 'f.py::dup@L1', 'identity': 'f.py::dup', 'line': 1, ...}])

plugins/superheroes/lib/tests/test_audit_target_identity.py:621: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_audit_target_identity.py::test_union_open_blockers_keeps_distinct_occurrence_ids
1 failed in 0.15s
```

**Restore:** reverted the id-bearing branch's **first** guard to `if tid in seen_ids: continue` — that
is the line the neutralization above replaced. The branch's second guard
(`if loc_key in idless_locs: continue`, BP6's element) was never touched by this neutralization and
is unchanged throughout.

**Restore receipt:**
```
 M plugins/superheroes/lib/round_driver.py
```
(the implementer's own receipt, taken while its whole order was still uncommitted — see the
orchestrator re-run below for a clean one)

**Orchestrator re-run (authoritative).** Re-run against the committed tree at `65611cb2`: same
neutralization (`if tid in seen_ids:` → `if loc_key in seen_locs:`), red on
`assert {'f.py::dup@L1'} == {'f.py::dup@L1', 'f.py::dup@L1#1'}` (`1 failed in 0.15s`), restored to
`if tid in seen_ids: continue`, and `git status --porcelain plugins/superheroes/lib/round_driver.py`
came back **empty**, then `1 passed in 0.14s`.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.13s
```

---

## BP8 — no-stalled-match still refuses a partial batch (`test_handle_stall_uncomposable_open_id_refuses_empty_stalled_match`)

**Produced by:** the orchestrator. The review round-1 fixer added this detector but forfeited
(`worktree-dirtied-by-attempt`) before writing its record; the orchestrator re-ran the proof.

**Guarded element:** `_compose_stall_fix_batch` open-id completeness refusal, hoisted above the
stalled/empty branch (round_driver.py:3329) — axis: the "no partial batch" contract holds on the
stalled-empty arm too, not only when a stalled alias matched.

**Neutralization:** re-confine the refusal to the stalled arm — `if nd_ids - matched_ids:` →
`if stalled and (nd_ids - matched_ids):`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_uncomposable_open_id_refuses_empty_stalled_match" -q
```

**Red run:**
```
>       assert state["step"] == RD.P_TERMINAL
E       AssertionError: assert 'dispatch-fixer' == 'terminal'
E         - terminal
E         + dispatch-fixer
plugins/superheroes/lib/tests/test_audit_target_identity.py:371: AssertionError
FAILED ...::test_handle_stall_uncomposable_open_id_refuses_empty_stalled_match
1 failed in 0.15s
```

**Restore:** reverted the guard to `if nd_ids - matched_ids:`.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/round_driver.py` → empty.

**Green run:**
```
1 passed in 0.14s
```

---

## BP9 — non-dict `_auditTargets` member fails closed (`test_handle_stall_malformed_audit_target_member_parks`)

**Produced by:** the orchestrator (same reason as BP8).

**Guarded element:** the `isinstance(t, dict)` guard on the `nd_targets` comprehension
(round_driver.py:3328) — axis: a malformed `_auditTargets` member parks rather than raising out of
stall recovery.

**Neutralization:** dropped the guard — `if isinstance(t, dict) and t.get("id") in nd_ids` →
`if t.get("id") in nd_ids`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_malformed_audit_target_member_parks" -q
```

**Red run:**
```
plugins/superheroes/lib/round_driver.py:3398: in _handle_stall
    batch, refusal = _compose_stall_fix_batch(state, breaker)
plugins/superheroes/lib/round_driver.py:3327: in _compose_stall_fix_batch
    nd_targets = [dict(t) for t in (state.get("_auditTargets") or [])
>                 if t.get("id") in nd_ids]
E   AttributeError: 'str' object has no attribute 'get'
FAILED ...::test_handle_stall_malformed_audit_target_member_parks
1 failed in 0.26s
```

**Restore:** re-added `isinstance(t, dict) and`.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/round_driver.py` → empty.

**Green run:**
```
1 passed in 0.14s
```

---

## BP10 — occurrence-suffixed siblings survive the stall union (`test_handle_stall_retains_occurrence_suffixed_open_targets`)

**Produced by:** the orchestrator (same reason as BP8).

**Guarded element:** `_union_open_blockers` id-keyed admission for id-bearing items
(round_driver.py:3291) — axis: two not-discharged occurrence ids at one location both stay in the
recovery batch, end-to-end through `_handle_stall`.

**Neutralization:** keyed id-bearing items on the per-location key instead of `id` —
`if tid in seen_ids:` → `if loc_key in seen_locs:`.

**Note on a rejected first attempt** (recorded because it is the rubric's "bites on the wrong axis"
trap): neutralizing the *widening* instead (`batch = stalled`) left this test **green**, because the
breaker's alias matches both occurrence targets, so `stalled` already carried both. Only the union's
dedupe key bites this axis.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_audit_target_identity.py::test_handle_stall_retains_occurrence_suffixed_open_targets" -q
```

**Red run:**
```
        assert state["step"] == RD.P_FIXER
        batch = state.get("_fixBatch") or []
>       assert {b["id"] for b in batch} == {target_a["id"], target_b["id"]}
E       AssertionError: assert {'f.py::dup@L1'} == {'f.py::dup@L...py::dup@L1#1'}
E         Extra items in the right set:
E         'f.py::dup@L1#1'
plugins/superheroes/lib/tests/test_audit_target_identity.py:410: AssertionError
1 failed in 0.16s
```

**Restore:** reverted to `if tid in seen_ids:`.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/round_driver.py` → empty.

**Green run:**
```
1 passed in 0.14s
```

---

## BP11 — a union-admitted tradeoff target still gates on judgment (`test_stall_union_admitted_tradeoff_target_routes_to_judgment`)

**Produced by:** the orchestrator (same reason as BP8).

**Guarded element:** the widening itself — `batch = _union_open_blockers(stalled, nd_targets)`
(round_driver.py:3333) — axis: a tradeoff sibling admitted by the #1165 union (not by the stalled
alias) still diverts self-recovery to the judgment gate instead of being auto-fixed.

**Neutralization:** removed the widening — `batch = _union_open_blockers(stalled, nd_targets)` →
`batch = stalled`, so the tradeoff sibling is never admitted.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest "plugins/superheroes/lib/tests/test_stall_judgment_gate.py::test_stall_union_admitted_tradeoff_target_routes_to_judgment" -q
```

**Red run:**
```
        breaker = {"reason": "audit-stall", "detail": "x", "stalledIdentities": [mech_ident]}
        RD._handle_stall(state, state["config"], breaker)
>       assert state["step"] == RD.P_JUDGMENT
E       AssertionError: assert 'dispatch-fixer' == 'present-judgment'
E         - present-judgment
E         + dispatch-fixer
plugins/superheroes/lib/tests/test_stall_judgment_gate.py:96: AssertionError
1 failed in 0.12s
```

**Restore:** reverted to `batch = _union_open_blockers(stalled, nd_targets)`.

**Restore receipt:** `git status --porcelain plugins/superheroes/lib/round_driver.py` → empty.

**Green run:**
```
1 passed in 0.12s
```
