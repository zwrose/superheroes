# WO-R1 (#1204) bite-proof — run-level seat-map-unjudgeable disclosure fallback

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-R1 | `build_receipt` run-level fallback after per-round loop | Predicate armed via receipts but per-round record cleared (same-round bad→good re-fold) still emits exactly one `seat-map-unjudgeable` line naming the basis | `test_seat_map_unjudgeable_double_fold_bad_then_good_receipt_disclosure_fallback` |

**Note (BP-R1b withdrawn — the claim it made was false).** The original row claimed
`test_seat_map_unavailable_disclosure_not_unjudgeable_prose` proves
`unjudgeable_run_level_disclosure`'s `no_seat_map_submitted` guard. It does not: the orchestrator
measured that removing the guard leaves that test **green** (exit 0), because in that state no
receipt exists, so `unjudgeable_receipts` is already empty (the function returns `None` on
`if not unj`) and `build_receipt` additionally gates the append on `_seat_map_unjudgeable(state)`,
which is `False`. A round-2 review seat and its verifier reached the same conclusion independently.
The guard is therefore **dominated and non-biting**, and is recorded here as an **UNPROVEN,
disclosed residual** rather than claimed as proven.

**Correction to the round-2 wording, made by the orchestrator:** an earlier version of this note
said the parameter "was removed". It was **not**. `per_round_emitted` and `no_seat_map_submitted`
survive as accepted-and-ignored legacy keyword arguments (`del per_round_emitted,
no_seat_map_submitted`), and `round_driver.build_receipt` still computes and passes both — including
the now-unused `_emitted_seat_map_unjudgeable` tracker. That is dead parameter plumbing, not a
behaviour defect: judgeability of the disclosure is derived inside the function from state. Reaping
it would have been a **third** corrective pass on this surface in one build, which
`rubric/review-discipline.md` § *The third-rework tripwire* forbids, so it ships as a **disclosed
follow-up** for the advisor rather than a fourth patch.

---

## BP-R1 — run-level fallback arm

- **axis:** disclosure never goes silent while `_seat_map_unjudgeable(state)` is True and a map was submitted

**neutralization** (delete the post-loop fallback block in `build_receipt` and the `_emitted_seat_map_unjudgeable` tracker):

```python
    # remove:
    # _emitted_seat_map_unjudgeable = False
    # ... _emitted_seat_map_unjudgeable = True in smuj branch ...
    # _run_unj = _sm_unjudgeable_run_level_disclosure(...)
    # if _run_unj and _seat_map_unjudgeable(state):
    #     degraded.append(_run_unj)
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_double_fold_bad_then_good_receipt_disclosure_fallback -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_seat_map_unjudgeable_double_fold_bad_then_good_receipt_disclosure_fallback _
plugins/superheroes/lib/tests/test_round_driver.py:5403: in test_seat_map_unjudgeable_double_fold_bad_then_good_receipt_disclosure_fallback
    assert len(unj_lines) == 1
E   AssertionError: assert 0 == 1
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_double_fold_bad_then_good_receipt_disclosure_fallback
1 failed
```

**restore:** reinstate `_emitted_seat_map_unjudgeable` tracking and the post-loop `_sm_unjudgeable_run_level_disclosure` append (landed form).
