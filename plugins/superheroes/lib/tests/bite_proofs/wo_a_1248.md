# WO-A (#1248 leg a) bite-proof — driver-state null-terminal arm

## Guarded element

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-A1 | `_driver_state_refusal` null/absent/empty `terminal` arm | Refuse handback when loop reached no terminal | `test_specimen_shape_refuses` |

---

## BP-A1 — null-terminal arm

- **axis:** a validated review-session marker with `terminal` null/absent/empty refuses before receipt path

**neutralization** (disable the null-terminal refusal arm):
```python
    terminal = loop_state.get("terminal")
    if False and (not isinstance(terminal, str) or not terminal):
        return _refuse("handback-driver-abandoned",
                        "driver loop terminal is null",
                        subject=subject, sidecar_path=sidecar_path, head_sha=head_sha)
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-a -m pytest plugins/superheroes/lib/tests/test_handback_gate.py::test_specimen_shape_refuses -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________________ test_specimen_shape_refuses __________________________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-1217/test_specimen_shape_refuses0')

    def test_specimen_shape_refuses(tmp_path):
        repo, _, _ = _forensic_specimen(tmp_path)
        result = hg.validate_handback("gh pr ready", repo)
>       assert result["decision"] == "refuse"
E       AssertionError: assert 'allow' == 'refuse'
E         
E         - refuse
E         + allow

plugins/superheroes/lib/tests/test_handback_gate.py:837: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_handback_gate.py::test_specimen_shape_refuses
1 failed in 2.22s
```

**restore:**
```python
    terminal = loop_state.get("terminal")
    if not isinstance(terminal, str) or not terminal:
        return _refuse("handback-driver-abandoned",
                        "driver loop terminal is null",
                        subject=subject, sidecar_path=sidecar_path, head_sha=head_sha)
```

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 4.24s
```

Post-restore `git status --porcelain` showed only the three intended deliverable paths modified (no probe residue).
