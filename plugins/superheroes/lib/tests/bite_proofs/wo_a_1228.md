# Bite-proof record — WO-A #1228 (`validate_python_pin.py`)

**Provenance:** implementer dispatch `wo-a-1228` (cursor `composer-2.5`). Detector:
`.github/scripts/validate_python_pin.py`. Every neutralization was a targeted edit and restored by
inverse edit; no `git checkout` / `git restore`.

Command shape for G1–G6 and green halves:

```
scripts/pinned-python .github/scripts/validate_python_pin.py
```

## Guarded elements (G1–G7)

| id | rule token | neutralization surface |
|---|---|---|
| G1 | `workflow-python-version-literal` | `.github/workflows/ci.yml` setup-python `with` |
| G2 | `workflow-python-before-setup` | `.github/workflows/release-please.yml` pinned setup step |
| G3 | `workflow-setup-python-unpinned` | `.github/workflows/pr-title.yml` `python-version-file` |
| G4 | `absolute-interpreter-path` | `CLAUDE.md` first local-gate line |
| G5 | `version-literal-mismatch` (uv `--python`) | `scripts/pinned-python` |
| G6 | `version-literal-mismatch` (prose) | `CONTRIBUTING.md` Development setup fence |
| G7 | `pin-malformed` | `.python-version` two version lines |

---

## G1 — `workflow-python-version-literal`

**neutralization:** `python-version-file: .python-version` → `python-version: "3.12"` in
`.github/workflows/ci.yml` setup-python `with`.

**raw red** (exit 1; decisive line):

```
validate_python_pin: workflow-python-version-literal: .github/workflows/ci.yml:31: workflow must use python-version-file, not python-version
```

**restore:** inverse edit back to `python-version-file: .python-version`.
**restore receipt:** `git status --porcelain -- .github/workflows/ci.yml` empty after restore;
`git diff -- .github/workflows/ci.yml` empty for the neutralized lines (post-restore tree retains
the new UFR-8 CI step only).
**raw green** (exit 0):

```
validate_python_pin: ok (pin 3.12, 25 homes checked)
```

---

## G2 — `workflow-python-before-setup`

**neutralization:** removed the four-line `Set up the pinned Python` step from
`release-please.yml`.

**raw red** (exit 1):

```
validate_python_pin: workflow-python-before-setup: .github/workflows/release-please.yml: Python runs before pinned setup-python in job
```

**restore:** reinserted the four-line setup-python step.
**restore receipt:** `git status` / `git diff` on `release-please.yml` → empty.
**raw green** (exit 0):

```
validate_python_pin: ok (pin 3.12, 25 homes checked)
```

---

## G3 — `workflow-setup-python-unpinned`

**neutralization:** `python-version-file: .python-version` → `python-version-file: .tool-versions`
in `pr-title.yml`.

**raw red** (exit 1; decisive line):

```
validate_python_pin: workflow-setup-python-unpinned: .github/workflows/pr-title.yml: setup-python python-version-file must be .python-version
```

**restore:** inverse edit on `pr-title.yml`.
**restore receipt:** empty `git status` / `git diff` on path.
**raw green** (exit 0):

```
validate_python_pin: ok (pin 3.12, 25 homes checked)
```

---

## G4 — `absolute-interpreter-path`

**neutralization:** `scripts/pinned-python .github/scripts/validate_python_pin.py` →
`/usr/bin/python3 .github/scripts/validate_python_pin.py` in `CLAUDE.md` local-gate block.

**raw red** (exit 1):

```
validate_python_pin: absolute-interpreter-path: CLAUDE.md:132: absolute interpreter path
```

**restore:** inverse edit on `CLAUDE.md`.
**restore receipt:** empty `git status` / `git diff` on `CLAUDE.md` for the gate line.
**raw green** (exit 0):

```
validate_python_pin: ok (pin 3.12, 25 homes checked)
```

---

## G5 — `version-literal-mismatch` (uv grammar)

**neutralization:** `--python "$pin"` → `--python 3.11` in `scripts/pinned-python`.

**raw red** (exit 1):

```
validate_python_pin: version-literal-mismatch: scripts/pinned-python:10: version 3.11 disagrees with pin 3.12
```

**restore:** inverse edit on `scripts/pinned-python`.
**restore receipt:** empty `git status` / `git diff` on path.
**raw green** (exit 0):

```
validate_python_pin: ok (pin 3.12, 25 homes checked)
```

---

## G6 — `version-literal-mismatch` (prose)

**neutralization:** appended comment line `# Tested on Python 3.9.` inside the Development setup
bash fence in `CONTRIBUTING.md`.

**raw red** (exit 1):

```
validate_python_pin: version-literal-mismatch: CONTRIBUTING.md:27: version 3.9 disagrees with pin 3.12
```

**restore:** removed the added comment line.
**restore receipt:** empty `git status` / `git diff` on `CONTRIBUTING.md`.
**raw green** (exit 0):

```
validate_python_pin: ok (pin 3.12, 25 homes checked)
```

---

## G7 — `pin-malformed`

**neutralization:** `.python-version` contents `3.12` then `3.11` on two lines.

**raw red:** `scripts/pinned-python` cannot start the detector when the pin file is malformed
(`tr -d '[:space:]'` joins lines to `3.123.11`; exit 2 from uv). **Unreachable through this entry
point** for the red half. Red captured via the CI-equivalent interpreter selection (explicit pin
`3.12`, same requirements as `pinned-python`):

```
validate_python_pin: pin-malformed: .python-version: expected exactly one version line matching 3.N or 3.N.P
validate_python_pin: FAIL (1 violation(s))
```

(exit 1)

**restore:** `.python-version` back to single line `3.12`.
**restore receipt:** empty `git status` / `git diff` on `.python-version`.
**raw green** through `scripts/pinned-python` (exit 0):

```
validate_python_pin: ok (pin 3.12, 25 homes checked)
```

---

## Proven by unit specimens only

| rule | test function |
|---|---|
| `pin-missing` | `test_pin_missing` |
| `home-missing` | `test_home_missing_explicit`, `test_home_missing_workflows` |
| `home-unreadable` | `test_home_unreadable` |
| `workflow-unparseable` | `test_workflow_unparseable` |
| `yaml-unavailable` | `test_yaml_unavailable` |
| `running-interpreter-mismatch` | `test_running_interpreter_mismatch_flag` (specimen receipt below) |

**`running-interpreter-mismatch` — construction bound on the live interpreter.** A real red half
needs `--require-running-pin` under an interpreter whose major.minor disagrees with
`.python-version` (for example `uv python install 3.11` then
`UV_PYTHON=3.11 uv run --no-project python .github/scripts/validate_python_pin.py --require-running-pin`
from the repo root). CI runs under the pin, so the flag is not exercised there.

**guarded element:** `.github/scripts/tests/test_validate_python_pin.py::test_running_interpreter_mismatch_flag`
**axis:** with a clean `check()` tree, `--require-running-pin` fails when `sys.version_info` disagrees.

**neutralization:** monkeypatch `sys.version_info` to `(3, 11, 0, "final", 0)` inside the specimen
(the detector itself untouched).

**raw red** (exit 1; stdout quoted from the specimen's `main()` call):

```
validate_python_pin: running-interpreter-mismatch: .python-version: running interpreter 3.11 disagrees with pin 3.12
validate_python_pin: FAIL (1 violation(s))
```

**restore:** monkeypatch scope ends with the test; no repo files touched.
**restore receipt:** specimen uses `tmp_path` only — `git status --porcelain` over the worktree
empty before and after the test node.
**raw green** (specimen pass, 2026-09-25):

```
.                                                                        [100%]
1 passed in 0.67s
```

(Command:
`python -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest
.github/scripts/tests/test_validate_python_pin.py::test_running_interpreter_mismatch_flag -q`.)

---

## Round-1 fix leg (WO-F1)

**Provenance:** auto-fix round 3 re-ran every WO-F1 neutralization on the repo root (inverse edit
restore, no `git checkout` / `git restore`) and quoted stdout verbatim from
`scripts/pinned-python .github/scripts/validate_python_pin.py`. Specimen cross-check:
`python -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest` on the named nodes in
`.github/scripts/tests/test_validate_python_pin.py` (exact node ids, never `-k`).

### G8 — `pin-home-duplicate`

**guarded element:** `.python-versions` (repo root)
**axis:** a second pin file anywhere competes with the root pin.

**neutralization:** created root `.python-versions` containing `3.11` (creation/deletion is the only
expressible neutralization for a new competing pin file).

**raw red** (exit 1; stdout verbatim, 2026-09-25):

```
validate_python_pin: pin-home-duplicate: .python-versions: competing Python pin home
validate_python_pin: FAIL (1 violation(s))
```

**restore:** deleted `.python-versions`.
**restore receipt:** `test ! -e .python-versions`; `git status --porcelain -- .python-versions` empty
after restore.
**raw green** (exit 0; stdout verbatim, 2026-09-25):

```
validate_python_pin: ok (pin 3.12, 29 homes checked)
```

---

### G9 — `bare-interpreter-command`

**guarded element:** `CONTRIBUTING.md:43` (local gate line)
**axis:** ambient `python` / `pip` in non-workflow homes.

**neutralization:** `CONTRIBUTING.md` gate line
`scripts/pinned-python .github/scripts/validate_marketplace.py` →
`python3 .github/scripts/validate_marketplace.py`.

**raw red** (exit 1; stdout verbatim from the neutralized tree):

```
validate_python_pin: bare-interpreter-command: CONTRIBUTING.md:43: bare ambient python or pip command
validate_python_pin: FAIL (1 violation(s))
```

**restore:** inverse edit on `CONTRIBUTING.md`.
**restore receipt:** empty `git status --porcelain -- CONTRIBUTING.md` and empty
`git diff -- CONTRIBUTING.md` after restore.
**raw green** (exit 0; stdout verbatim after restore):

```
validate_python_pin: ok (pin 3.12, 29 homes checked)
```

---

### G10 — `workflow-python-before-setup` (`shell: python`)

**guarded element:** `.github/workflows/pr-title.yml` (job `validate`)
**axis:** `shell: python` runs before pinned `setup-python`.

**neutralization:** added first step in `pr-title.yml` before checkout:
`shell: python` + `run: print(1)`.

**raw red** (exit 1; stdout verbatim from the neutralized tree):

```
validate_python_pin: workflow-python-before-setup: .github/workflows/pr-title.yml: Python runs before pinned setup-python in job
validate_python_pin: FAIL (1 violation(s))
```

**restore:** removed the injected step.
**restore receipt:** empty `git status --porcelain -- .github/workflows/pr-title.yml` and empty
`git diff -- .github/workflows/pr-title.yml` after restore.
**raw green** (exit 0; stdout verbatim after restore):

```
validate_python_pin: ok (pin 3.12, 29 homes checked)
```

---

### G11 — `absolute-interpreter-path` (added home)

**guarded element:** `docs/superheroes/KEEP-OR-RETIRE.md:1580`
**axis:** declared maintainer-command homes stay path-free.

**neutralization:** `docs/superheroes/KEEP-OR-RETIRE.md` line 1580 migrated command reverted to
`/usr/bin/python3 -B plugins/superheroes/lib/dispatch_entry_doc.py --check` (line 1589 left on
`scripts/pinned-python`).

**raw red** (exit 1; stdout verbatim from the neutralized tree):

```
validate_python_pin: absolute-interpreter-path: docs/superheroes/KEEP-OR-RETIRE.md:1580: absolute interpreter path
validate_python_pin: FAIL (1 violation(s))
```

**restore:** inverse edit on line 1580 back to `scripts/pinned-python …`.
**restore receipt:** empty `git status --porcelain -- docs/superheroes/KEEP-OR-RETIRE.md` and empty
`git diff -- docs/superheroes/KEEP-OR-RETIRE.md` for the neutralized line after restore.
**raw green** (exit 0; stdout verbatim after restore):

```
validate_python_pin: ok (pin 3.12, 29 homes checked)
```

**WO-F1 specimen cross-check (green, 2026-09-25):**

```
....                                                                     [100%]
4 passed in 0.62s
```

(Nodes:
`test_pin_home_duplicate_root_python_versions`,
`test_workflow_python_before_setup_shell_python`,
`test_absolute_in_keep_or_retire_home`,
`test_running_interpreter_mismatch_flag`.)

## Round-5 fix leg (WO-F5)

### G13 — `bare-interpreter-command` (env-assignment prefix)

**guarded element:** `CONTRIBUTING.md:43`
**axis:** `FOO=1 python3 …` in a declared home.

**neutralization:** first `scripts/pinned-python .github/scripts/validate_marketplace.py` line →
`FOO=1 python3 .github/scripts/validate_marketplace.py`.

**raw red** (exit 1; stdout verbatim from the neutralized tree):

```
validate_python_pin: bare-interpreter-command: CONTRIBUTING.md:43: bare ambient python or pip command
validate_python_pin: FAIL (1 violation(s))
```

**restore:** inverse edit on `CONTRIBUTING.md` line 43 back to `scripts/pinned-python …`.
**restore receipt:** empty `git status --porcelain -- CONTRIBUTING.md` after restore.
**raw green** (exit 0; stdout verbatim after restore):

```
validate_python_pin: ok (pin 3.12, 29 homes checked)
```

---

### G14 — `workflow-python-before-setup` (conditional setup-python)

**guarded element:** `.github/workflows/pr-title.yml` setup-python step
**axis:** `if: false` on the only setup-python step; later `run: python3` has no pinned setup.

**neutralization:** added `if: false` to the setup-python step.

**raw red** (exit 1; stdout verbatim from the neutralized tree):

```
validate_python_pin: workflow-python-before-setup: .github/workflows/pr-title.yml: Python runs before pinned setup-python in job
validate_python_pin: FAIL (1 violation(s))
```

**restore:** removed `if: false` from the setup-python step.
**restore receipt:** empty `git status --porcelain -- .github/workflows/pr-title.yml` after restore.
**raw green** (exit 0; stdout verbatim after restore):

```
validate_python_pin: ok (pin 3.12, 29 homes checked)
```
