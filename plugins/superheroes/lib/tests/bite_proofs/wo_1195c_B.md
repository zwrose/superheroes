# WO-1195c-B bite-proofs — strengthened round-int census

## B1 — bare `int(rec.get("round"))` in previously unscanned `round_driver.py`

**Guarded element:** `test_round_value_int_calls_census_only_inside_arbiter` — axis: a bare round `int()` newly introduced in `round_driver.py` fails the census.

**Neutralization:** deliberate defect in production code — inserted bare `int(rec.get("round"))` in `_seed_resume`:

```python
    for rec in records:
        rnd = rec.get("round")
        int(rec.get("round"))
        if rnd is not None and review_loop_plan._round_number(rnd) is None:
```

**Command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_review_loop_plan.py::test_round_value_int_calls_census_only_inside_arbiter' -q`

**Raw red:**

```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_round_value_int_calls_census_only_inside_arbiter _____________

    def test_round_value_int_calls_census_only_inside_arbiter():
        """Census — no round int() outside _round_number except the closed allowlist."""
        violations = _round_int_conversions_outside_arbiter()
>       assert violations == [], "round int() violations: %r" % violations
E       AssertionError: round int() violations: ['round_driver.py:1351 (_seed_resume)']
E       assert ['round_drive...seed_resume)'] == []
E         
E         Left contains one more item: 'round_driver.py:1351 (_seed_resume)'
E         Use -v to get more diff

plugins/superheroes/lib/tests/test_review_loop_plan.py:1223: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_loop_plan.py::test_round_value_int_calls_census_only_inside_arbiter
1 failed in 0.53s
```

**Restore:** removed the inserted `int(rec.get("round"))` line from `_seed_resume`.

**Restore receipt:** `for rec in records:\n        rnd = rec.get("round")\n        if rnd is not None and review_loop_plan._round_number(rnd) is None:`

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.48s
```

## B2 — aliased conversion `raw = rec.get("round"); int(raw)`

**Guarded element:** `test_round_value_int_calls_census_only_inside_arbiter` — axis: one-hop alias of a `.get("round")` read followed by `int()` fails the census.

**Neutralization:** deliberate defect in production code — replaced arbiter routing in `_resume_round`:

```python
        raw = rec.get("round")
        n = int(raw)
        if n is None:
```

**Command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_review_loop_plan.py::test_round_value_int_calls_census_only_inside_arbiter' -q`

**Raw red:**

```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_round_value_int_calls_census_only_inside_arbiter _____________

    def test_round_value_int_calls_census_only_inside_arbiter():
        """Census — no round int() outside _round_number except the closed allowlist."""
        violations = _round_int_conversions_outside_arbiter()
>       assert violations == [], "round int() violations: %r" % violations
E       AssertionError: round int() violations: ['review_loop_plan.py:134 (_resume_round)']
E       assert ['review_loop...esume_round)'] == []
E         
E         Left contains one more item: 'review_loop_plan.py:134 (_resume_round)'
E         Use -v to get more diff

plugins/superheroes/lib/tests/test_review_loop_plan.py:1244: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_loop_plan.py::test_round_value_int_calls_census_only_inside_arbiter
1 failed in 0.81s
```

**Restore:** restored arbiter routing at site 134.

**Restore receipt:** `n = _round_number(rec.get("round")); if n is None:`

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.64s
```

## B3 — stale allowlist entry

**Guarded element:** `_ROUND_INT_ALLOWLIST` stale-entry leg — axis: an allowlisted site that no longer exists fails the census.

**Neutralization:** added a nonexistent allowlist tuple to the test (census itself unedited):

```python
    ("_removed_fn", "int(x)"),
```

**Command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_review_loop_plan.py::test_round_value_int_calls_census_only_inside_arbiter' -q`

**Raw red:**

```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_round_value_int_calls_census_only_inside_arbiter _____________

    def test_round_value_int_calls_census_only_inside_arbiter():
        """Census — no round int() outside _round_number except the closed allowlist."""
        violations = _round_int_conversions_outside_arbiter()
>       assert violations == [], "round int() violations: %r" % violations
E       AssertionError: round int() violations: ["stale allowlist entry (_removed_fn, 'int(x)')"]
E       assert ["stale allow...n, 'int(x)')"] == []
E         
E         Left contains one more item: "stale allowlist entry (_removed_fn, 'int(x)')"
E         Use -v to get more diff

plugins/superheroes/lib/tests/test_review_loop_plan.py:1245: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_loop_plan.py::test_round_value_int_calls_census_only_inside_arbiter
1 failed in 0.79s
```

**Restore:** removed the `("_removed_fn", "int(x)")` tuple from `_ROUND_INT_ALLOWLIST`.

**Restore receipt:** allowlist ends with `("_round_num", "int(r)"),` only — no `_removed_fn` entry.

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.66s
```
