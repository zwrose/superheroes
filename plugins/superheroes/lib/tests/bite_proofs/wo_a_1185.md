# WO-A (#1185) bite-proofs — state/receipt version spelling census (six legs)

## BP-A1 — mod-format

**Guarded element:** `census_module` / `_scan_mod_format` — axis: no hand-spelled integer in `RECEIPT_CERTIFIED_SCHEMA % …` outside the pinned version path.

**Neutralization:** changed `return RECEIPT_CERTIFIED_SCHEMA % _receipt_version(state)` to `return RECEIPT_CERTIFIED_SCHEMA % 99` at `plugins/superheroes/lib/round_driver.py` line 230.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_state_version_spelling_census.py -q
```

**Red run (exit 1):**
```
..F.........                                                             [100%]
=================================== FAILURES ===================================
______________________ test_state_version_spelling_census ______________________

    def test_state_version_spelling_census():
        findings = run_tree_census()
        unexpected = [f for f in findings if not _is_allowlisted(f)]
>       assert not unexpected, (
            "hand-spelled state/receipt version sites:\n" + _format_findings(unexpected)
        )
E       AssertionError: hand-spelled state/receipt version sites:
E           round_driver.py:230 [mod-format] 'RECEIPT_CERTIFIED_SCHEMA % 99' — mod-format
E       assert not [Finding(relpath='round_driver.py', line=230, segment='RECEIPT_CERTIFIED_SCHEMA % 99', leg='mod-format')]

plugins/superheroes/lib/tests/test_state_version_spelling_census.py:443: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_state_version_spelling_census
1 failed, 11 passed in 2.56s
```

**Restore:** changed `return RECEIPT_CERTIFIED_SCHEMA % 99` back to `return RECEIPT_CERTIFIED_SCHEMA % _receipt_version(state)`.

**Restore receipt:** `return RECEIPT_CERTIFIED_SCHEMA % _receipt_version(state)`

**Green run (exit 0):**
```
............                                                             [100%]
12 passed in 2.37s
```

---

## BP-A2 — comparison

**Guarded element:** `census_module` / `_scan_comparisons` — axis: no hand-spelled integer compared to a `schemaVersion` read that appears **syntactically inside** the comparison operand.

> **Axis widened after this record was written** (review round 1 of the second continuation, finding
> `v5`, CONFIRMED Important): the detector now also resolves **per-scope local aliases**, so
> `version = state.get("schemaVersion")` followed by `version == 99` **is** reported. The
> parenthetical this record originally carried — that aliased locals are out of scope and alias
> resolution is deliberately not built — no longer describes the shipped detector. The red/green
> runs below were recorded against the **pre-widening** detector; both axes are re-proved on the
> final head in `wo_r1_1185cont2.md` (BP-C2a direct operand, BP-C2b aliased local).

**Neutralization:** changed `if _state_version(loaded) != STATE_SCHEMA_VERSION:` to `if loaded.get("schemaVersion") == 99:` at `plugins/superheroes/lib/round_driver.py` line 4999.

**Command:** (same as BP-A1)

**Red run (exit 1):**
```
..F.........                                                             [100%]
=================================== FAILURES ===================================
______________________ test_state_version_spelling_census ______________________

    def test_state_version_spelling_census():
        findings = run_tree_census()
        unexpected = [f for f in findings if not _is_allowlisted(f)]
>       assert not unexpected, (
            "hand-spelled state/receipt version sites:\n" + _format_findings(unexpected)
        )
E       AssertionError: hand-spelled state/receipt version sites:
E           round_driver.py:4999 [comparison] 'loaded.get("schemaVersion") == 99' — comparison
E       assert not [Finding(relpath='round_driver.py', line=4999, segment='loaded.get("schemaVersion") == 99', leg='comparison')]

plugins/superheroes/lib/tests/test_state_version_spelling_census.py:443: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_state_version_spelling_census
1 failed, 11 passed in 2.57s
```

**Restore:** changed `if loaded.get("schemaVersion") == 99:` back to `if _state_version(loaded) != STATE_SCHEMA_VERSION:`.

**Restore receipt:** `if _state_version(loaded) != STATE_SCHEMA_VERSION:`

**Green run (exit 0):**
```
............                                                             [100%]
12 passed in 2.36s
```

---

## BP-A3 — binding

**Guarded element:** `census_module` / `_scan_bindings` — axis: no hand-spelled integer bound to `schemaVersion`.

**Neutralization:** changed `"schemaVersion": STATE_SCHEMA_VERSION,` to `"schemaVersion": 99,` at `plugins/superheroes/lib/round_driver.py` line 1183.

**Command:** (same as BP-A1)

**Red run (exit 1):**
```
..F.........                                                             [100%]
=================================== FAILURES ===================================
______________________ test_state_version_spelling_census ______________________

    def test_state_version_spelling_census():
        findings = run_tree_census()
        unexpected = [f for f in findings if not _is_allowlisted(f)]
>       assert not unexpected, (
            "hand-spelled state/receipt version sites:\n" + _format_findings(unexpected)
        )
E       AssertionError: hand-spelled state/receipt version sites:
E           round_driver.py:1183 [binding] '"schemaVersion": 99' — binding
E       assert not [Finding(relpath='round_driver.py', line=1183, segment='"schemaVersion": 99', leg='binding')]

plugins/superheroes/lib/tests/test_state_version_spelling_census.py:443: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_state_version_spelling_census
1 failed, 11 passed in 2.43s
```

**Restore:** changed `"schemaVersion": 99,` back to `"schemaVersion": STATE_SCHEMA_VERSION,`.

**Restore receipt:** `"schemaVersion": STATE_SCHEMA_VERSION,`

**Green run (exit 0):**
```
............                                                             [100%]
12 passed in 2.37s
```

---

## BP-A4 — constant-assignment

**Guarded element:** `census_module` / `_scan_constant_assignments` — axis: pinned version symbols may be assigned only inside the marker-delimited block (empty exempt range when a module carries no markers).

> **Axis widened after this record was written** (same round, same finding): the scanner now
> recognises **qualified attribute targets** (`<module>.STATE_SCHEMA_VERSION = 99`) in addition to
> bare names. Both target forms are re-proved on the final head in `wo_r1_1185cont2.md`
> (BP-C4a bare name, BP-C4b attribute target).

**Neutralization:** inserted `STATE_SCHEMA_VERSION = 99` immediately after the pinned declaration block END marker at `plugins/superheroes/lib/round_driver.py`.

**Command:** (same as BP-A1)

**Red run (exit 1):**
```
..FF........                                                             [100%]
=================================== FAILURES ===================================
______________________ test_state_version_spelling_census ______________________

    def test_state_version_spelling_census():
        findings = run_tree_census()
        unexpected = [f for f in findings if not _is_allowlisted(f)]
>       assert not unexpected, (
            "hand-spelled state/receipt version sites:\n" + _format_findings(unexpected)
        )
E       AssertionError: hand-spelled state/receipt version sites:
E           round_driver.py:147 [constant-assignment] 'STATE_SCHEMA_VERSION = 99' — constant-assignment
E       assert not [Finding(relpath='round_driver.py', line=147, segment='STATE_SCHEMA_VERSION = 99', leg='constant-assignment')]

plugins/superheroes/lib/tests/test_state_version_spelling_census.py:443: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_state_version_spelling_census
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_prose_census
2 failed, 10 passed in 2.59s
```
(Collateral: mutating `round_driver.py` rebinds `RD.STATE_SCHEMA_VERSION` at import time, so the prose census also fails until the insert is removed.)

**Restore:** removed the inserted line `STATE_SCHEMA_VERSION = 99` after the END marker.

**Restore receipt:** `# --- version spelling: pinned declaration block (END) ---` with no following assignment.

**Green run (exit 0):**
```
............                                                             [100%]
12 passed in 2.38s
```

---

## BP-A5 — string-literal

**Guarded element:** `census_module` / `_scan_string_literals` — axis: no `receipt-certified/N` string literal outside the pinned spelling path.

**Neutralization:** appended `_BP_SYNTH = "receipt-certified/9"` to `plugins/superheroes/lib/handback_gate.py`.

**Command:** (same as BP-A1)

**Red run (exit 1):**
```
..F.........                                                             [100%]
=================================== FAILURES ===================================
______________________ test_state_version_spelling_census ______________________

    def test_state_version_spelling_census():
        findings = run_tree_census()
        unexpected = [f for f in findings if not _is_allowlisted(f)]
>       assert not unexpected, (
            "hand-spelled state/receipt version sites:\n" + _format_findings(unexpected)
        )
E       AssertionError: hand-spelled state/receipt version sites:
E           handback_gate.py:1076 [string-literal] '"receipt-certified/9"' — string-literal
E       assert not [Finding(relpath='handback_gate.py', line=1076, segment='"receipt-certified/9"', leg='string-literal')]

plugins/superheroes/lib/tests/test_state_version_spelling_census.py:443: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_state_version_spelling_census
1 failed, 11 passed in 2.42s
```

**Restore:** removed the appended `_BP_SYNTH` line.

**Restore receipt:** `handback_gate.py` ends at `_allow(...)` with no synthetic literal appended.

**Green run (exit 0):**
```
............                                                             [100%]
12 passed in 2.39s
```

---

## BP-A6 — prose

**Guarded element:** `census_prose` — axis: `round-driver.md` states the same `STATE_SCHEMA_VERSION` and supported receipt `schemaVersion` set as `round_driver`.

**Neutralization:** changed (`STATE_SCHEMA_VERSION` = 4) to (`STATE_SCHEMA_VERSION` = 3) in `plugins/superheroes/skills/review-code/reference/round-driver.md` line 641.

**Command:** (same as BP-A1)

**Red run (exit 1):**
```
...F........                                                             [100%]
=================================== FAILURES ===================================
___________________ test_state_version_spelling_prose_census ___________________

    def test_state_version_spelling_prose_census():
        errors = census_prose()
>       assert not errors, "\n".join(errors)
E       AssertionError: prose leg: round-driver.md states STATE_SCHEMA_VERSION=3 but round_driver.STATE_SCHEMA_VERSION=4
E       assert not ['prose leg: round-driver.md states STATE_SCHEMA_VERSION=3 but round_driver.STATE_SCHEMA_VERSION=4']

plugins/superheroes/lib/tests/test_state_version_spelling_census.py:450: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_state_version_spelling_prose_census
1 failed, 11 passed in 2.41s
```

**Restore:** changed (`STATE_SCHEMA_VERSION` = 3) back to (`STATE_SCHEMA_VERSION` = 4).

**Restore receipt:** (`STATE_SCHEMA_VERSION` = 4)

**Green run (exit 0):**
```
............                                                             [100%]
12 passed in 2.37s
```
