# WO-A (#1219) bite-proofs — shared clause-guard chokepoint

Guarded-element set: CG-1 … CG-10 (ten elements; three grouped into failure-mode equivalence classes below).

## CG-1 … CG-5 — exact clause count in bounded section (equivalence class)

**Failure mode:** `check_clause` no longer fails when the section count is wrong — under-count, partial drift, over-count, restatement elsewhere, and following-section-only presence all stay green.

**Representative:** CG-1 (`test_cg1_absent_clause_raises`).

**Neutralization:** replaced the `check_clause` mismatch branch with `return  # neutralized: check_clause no-op` in `clause_guard.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest test_clause_guard.py -q -k 'cg1 or cg2 or cg3 or cg4 or cg5'
```

**Red run:**
```
FFFFF.                                                                   [100%]
=================================== FAILURES ===================================
________________________ test_cg1_absent_clause_raises _________________________

    def test_cg1_absent_clause_raises():
        """CG-1: a clause absent from its declared section makes check_clause raise."""
        text = _doc(
            SECTION_A,
            "No target clause in this section.",
            SECTION_B,
            "other",
        )
        with pytest.raises(AssertionError, match=SECTION_A):
>           check_clause(text, REL, SECTION_A, CLAUSE, 1)
E           Failed: DID NOT RAISE <class 'AssertionError'>

test_clause_guard.py:70: Failed
________________________ test_cg2_partial_drift_raises _________________________

    def test_cg2_partial_drift_raises():
        """CG-2: dropping one of two occurrences makes check_clause raise."""
        text = _doc(
            SECTION_A,
            f"First {CLAUSE_TWICE}.",
            f"Second {CLAUSE_TWICE}.",
            SECTION_B,
            "other",
        )
        mutated = drop_one_occurrence_in_section(text, REL, SECTION_A, CLAUSE_TWICE)
        with pytest.raises(AssertionError, match="expected count 2"):
>           check_clause(mutated, REL, SECTION_A, CLAUSE_TWICE, 2)
E           Failed: DID NOT RAISE <class 'AssertionError'>

test_clause_guard.py:84: Failed
__________________________ test_cg3_over_count_raises __________________________

    def test_cg3_over_count_raises():
        """CG-3: adding an extra occurrence makes check_clause raise."""
        text = _doc(
            SECTION_A,
            f"One {CLAUSE_TWICE}.",
            f"Two {CLAUSE_TWICE}.",
            SECTION_B,
            "other",
        )
        mutated = add_one_occurrence_in_section(text, REL, SECTION_A, CLAUSE_TWICE)
        with pytest.raises(AssertionError, match="expected count 2"):
>           check_clause(mutated, REL, SECTION_A, CLAUSE_TWICE, 2)
E           Failed: DID NOT RAISE <class 'AssertionError'>

test_clause_guard.py:98: Failed
____________________ test_cg4_restatement_elsewhere_raises _____________________

    def test_cg4_restatement_elsewhere_raises():
        """CG-4: a clause restated in another section still makes check_clause raise."""
        text = _doc(
            SECTION_A,
            "Section A without the clause.",
            SECTION_B,
            f"Restated {CLAUSE} in section B.",
        )
        with pytest.raises(AssertionError, match=SECTION_A):
>           check_clause(text, REL, SECTION_A, CLAUSE, 1)
E           Failed: DID NOT RAISE <class 'AssertionError'>

test_clause_guard.py:110: Failed
____________________ test_cg5_following_section_only_raises ____________________

    def test_cg5_following_section_only_raises():
        """CG-5: a clause present only in the following section makes check_clause raise."""
        text = _doc(
            SECTION_A,
            "Section A without the clause.",
            SECTION_B,
            f"Only here: {CLAUSE}.",
        )
        with pytest.raises(AssertionError, match=SECTION_A):
>           check_clause(text, REL, SECTION_A, CLAUSE, 1)
E           Failed: DID NOT RAISE <class 'AssertionError'>

test_clause_guard.py:122: Failed
=========================== short test summary info ============================
FAILED test_clause_guard.py::test_cg1_absent_clause_raises - Failed: DID NOT ...
FAILED test_clause_guard.py::test_cg2_partial_drift_raises - Failed: DID NOT ...
FAILED test_clause_guard.py::test_cg3_over_count_raises - Failed: DID NOT RAI...
FAILED test_clause_guard.py::test_cg4_restatement_elsewhere_raises - Failed: ...
FAILED test_clause_guard.py::test_cg5_following_section_only_raises - Failed:...
5 failed, 1 passed, 6 deselected in 0.15s
```

**Restore:** reinstated the `if actual != expected_count: raise AssertionError(...)` branch in `check_clause`.

**Restore receipt:** `check_clause` again raises on both under- and over-count mismatches.

**Green run:**
```
......                                                                   [100%]
6 passed, 6 deselected in 0.07s
```

**Also covered by this class:** CG-2, CG-3, CG-4, CG-5.

---

## CG-6 … CG-7 — section heading uniqueness (equivalence class)

**Failure mode:** `section_span` accepts zero or duplicate matching headings instead of raising `RuntimeError`.

**Representative:** CG-6 (`test_cg6_section_span_zero_headings_raises`).

**Neutralization:** replaced the zero/duplicate heading `RuntimeError` guards with `if not indices: return 0, 0` and unconditional `start = indices[0]` in `section_span`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest test_clause_guard.py -q -k 'cg6 or cg7'
```

**Red run:**
```
FF                                                                       [100%]
=================================== FAILURES ===================================
__________________ test_cg6_section_span_zero_headings_raises __________________

    def test_cg6_section_span_zero_headings_raises():
        """CG-6: section_span with zero matching headings raises RuntimeError."""
        lines = [SECTION_B, "text"]
        with pytest.raises(RuntimeError, match="expected exactly one '## Missing'"):
>           section_span(lines, "## Missing", REL)
E           Failed: DID NOT RAISE <class 'RuntimeError'>

test_clause_guard.py:129: Failed
_______________ test_cg7_section_span_duplicate_headings_raises ________________

    def test_cg7_section_span_duplicate_headings_raises():
        """CG-7: section_span with two matching headings raises RuntimeError."""
        lines = _doc("## Dup", "a", "## Dup", "b").splitlines()
        with pytest.raises(RuntimeError, match="expected exactly one '## Dup'"):
>           section_span(lines, "## Dup", REL)
E           Failed: DID NOT RAISE <class 'RuntimeError'>

test_clause_guard.py:136: Failed
=========================== short test summary info ============================
FAILED test_clause_guard.py::test_cg6_section_span_zero_headings_raises - Fai...
FAILED test_clause_guard.py::test_cg7_section_span_duplicate_headings_raises
2 failed, 10 deselected in 0.12s
```

**Restore:** reinstated the `len(indices) == 0` and `len(indices) > 1` `RuntimeError` branches in `section_span`.

**Restore receipt:** `section_span` again raises on zero and duplicate heading matches.

**Green run:**
```
..                                                                       [100%]
2 passed, 10 deselected in 0.09s
```

**Also covered by this class:** CG-7.

---

## CG-8 — whitespace-normalized literal clause match

**Guarded element:** `test_cg8_line_wrapped_clause_matches` — axis: line-wrapped clauses match via whitespace normalization; one word changed does not.

**Neutralization:** replaced `len(pattern.findall(section_text))` in `count_clause_in_section` with `section_text.count(clause)` (literal substring, no wrap tolerance).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest test_clause_guard.py -q -k cg8
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_____________________ test_cg8_line_wrapped_clause_matches _____________________

    def test_cg8_line_wrapped_clause_matches():
        """CG-8: a line-wrapped clause matches; one word changed does not."""
        wrapped = "line wrapped phrase matches"
        text = _doc(
            SECTION_A,
            "line wrapped",
            "phrase matches here.",
            SECTION_B,
            "other",
        )
>       assert count_clause_in_section(text, REL, SECTION_A, wrapped) == 1
E       AssertionError: assert 0 == 1
E        +  where 0 = count_clause_in_section('## Section A\nline wrapped\nphrase matches here.\n## Section B\nother', 'synthetic.md', '## Section A', 'line wrapped phrase matches')

test_clause_guard.py:149: AssertionError
=========================== short test summary info ============================
FAILED test_clause_guard.py::test_cg8_line_wrapped_clause_matches - Assertion...
1 failed, 11 deselected in 0.10s
```

**Restore:** reinstated `normalize_clause` regex counting in `count_clause_in_section`.

**Restore receipt:** `count_clause_in_section` uses `normalize_clause(clause)` and `pattern.findall` again.

**Green run:**
```
.                                                                        [100%]
1 passed, 11 deselected in 0.10s
```

---

## CG-9 — census exclusion directory boundary

**Guarded element:** `test_cg9_census_excluded` — axis: excluded-directory paths require `normdir + os.sep` prefix, not bare directory-name prefix.

**Neutralization:** replaced `norm.startswith(normdir + os.sep)` with `norm.startswith(normdir)` in `census_excluded`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest test_clause_guard.py -q -k cg9
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________________________ test_cg9_census_excluded ___________________________

    def test_cg9_census_excluded():
        """CG-9: census_excluded boundary for excluded dir, file, prefix-sibling, and elsewhere."""
        excluded_dirs = ("lib/tests/bite_proofs",)
        excluded_files = ("CHANGELOG.md",)
        assert census_excluded("lib/tests/bite_proofs/wo_a_1219.md", excluded_dirs, excluded_files)
        assert census_excluded("CHANGELOG.md", excluded_dirs, excluded_files)
>       assert not census_excluded("lib/tests/bite_proofs_notes.md", excluded_dirs, excluded_files)
E       AssertionError: assert not True
E        +  where True = census_excluded('lib/tests/bite_proofs_notes.md', ('lib/tests/bite_proofs',), ('CHANGELOG.md',))

test_clause_guard.py:159: AssertionError
=========================== short test summary info ============================
FAILED test_clause_guard.py::test_cg9_census_excluded - AssertionError: asser...
1 failed, 11 deselected in 0.13s
```

**Restore:** reinstated `norm.startswith(normdir + os.sep)` in `census_excluded`.

**Restore receipt:** `census_excluded` again uses the `+ os.sep` directory boundary.

**Green run:**
```
.                                                                        [100%]
1 passed, 11 deselected in 0.10s
```

---

## CG-10 — mutation helper per-replacement assertions

**Guarded element:** `test_cg10_mutation_helpers_raise_on_drifted_anchor` — axis: mutation helpers raise when the anchor clause count is wrong instead of silently no-op'ing.

**Neutralization:** replaced the bodies of `without_clause_in_section`, `drop_one_occurrence_in_section`, and `plant_clause_elsewhere` with `return text` (aggregate no-op; no per-replacement assertion).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest test_clause_guard.py -q -k cg10
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_cg10_mutation_helpers_raise_on_drifted_anchor ______________

    def test_cg10_mutation_helpers_raise_on_drifted_anchor():
        """CG-10: mutation helpers raise when their anchor has drifted."""
        absent_clause_doc = _doc(
            SECTION_A,
            "Section A without the target clause.",
            SECTION_B,
            "other",
        )
        with pytest.raises(AssertionError, match="not found"):
>           without_clause_in_section(absent_clause_doc, REL, SECTION_A, CLAUSE)
E           Failed: DID NOT RAISE <class 'AssertionError'>

test_clause_guard.py:172: Failed
=========================== short test summary info ============================
FAILED test_clause_guard.py::test_cg10_mutation_helpers_raise_on_drifted_anchor
1 failed, 11 deselected in 0.17s
```

**Restore:** reinstated full per-replacement assertion bodies for all three mutation helpers.

**Restore receipt:** mutation helpers again assert anchor counts before and after each replacement.

**Green run:**
```
.                                                                        [100%]
1 passed, 11 deselected in 0.06s
```
