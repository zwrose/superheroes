# WO-F (#1154) bite-proofs — gate-dependency fall-open closure

## BP-F1 — `mode_registry.py` membership in `SAFETY_MACHINERY`

**Guarded element:** `mode_registry.py` in `SAFETY_MACHINERY` — axis: *refusal*.

**Neutralization:** removed the `mode_registry.py` entry from `SAFETY_MACHINERY` in `lib/escalation.py`:
```python
    "mode_registry.py",       # decides owner_authority.calibration_state(); uncalibrated → gate allow (permissive fall-open) — fixer must refuse
```
(deleted line)

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_dependencies_are_fixer_refused -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________________ test_gate_dependencies_are_fixer_refused ___________________

    def test_gate_dependencies_are_fixer_refused():
        band_roots = [PLUGIN]
        for module in sorted(_direct_lib_local_imports(_OWNER_AUTHORITY)):
            abs_path = os.path.join(PLUGIN, "lib", f"{module}.py")
>           assert ESC.is_safety_machinery(abs_path, band_roots) is True, (
                f"gate dependency {module!r} must be refused to the fixer "
                f"(is_safety_machinery({abs_path!r}, {band_roots!r}))"
            )
E           AssertionError: gate dependency 'mode_registry' must be refused to the fixer (is_safety_machinery('/Users/zwrose/.superheroes-worktrees/superheroes/1154-F/plugins/superheroes/lib/mode_registry.py', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-F/plugins/superheroes']))
E           assert False is True
E            +  where False = <function is_safety_machinery at 0x106706f70>('/Users/zwrose/.superheroes-worktrees/superheroes/1154-F/plugins/superheroes/lib/mode_registry.py', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-F/plugins/superheroes'])
E            +    where <function is_safety_machinery at 0x106706f70> = ESC.is_safety_machinery

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:296: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_dependencies_are_fixer_refused
1 failed in 0.08s
```

**Restore:** re-added the deleted line:
```python
    "mode_registry.py",       # decides owner_authority.calibration_state(); uncalibrated → gate allow (permissive fall-open) — fixer must refuse
```

**Restore receipt (supplementary `git status --porcelain`):**
```
 M plugins/superheroes/lib/escalation.py
```
(restored line present; `M` reflects other WO-F deliverables in the same file)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.06s
```

---

## BP-F2 — leg 8 per-dependency reach

**Guarded element:** `test_gate_dependencies_are_fixer_refused` — axis: *every dependency, not one representative*.

**Neutralization:** added a second lib-local import inside `allow_file_path` in `lib/owner_authority.py`:
```python
    import core_md
    import mode_registry
```

**Command:**
```
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_dependencies_are_fixer_refused -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________________ test_gate_dependencies_are_fixer_refused ___________________

    def test_gate_dependencies_are_fixer_refused():
        band_roots = [PLUGIN]
        for module in sorted(_direct_lib_local_imports(_OWNER_AUTHORITY)):
            abs_path = os.path.join(PLUGIN, "lib", f"{module}.py")
>           assert ESC.is_safety_machinery(abs_path, band_roots) is True, (
                f"gate dependency {module!r} must be refused to the fixer "
                f"(is_safety_machinery({abs_path!r}, {band_roots!r}))"
            )
E           AssertionError: gate dependency 'core_md' must be refused to the fixer (is_safety_machinery('/Users/zwrose/.superheroes-worktrees/superheroes/1154-F/plugins/superheroes/lib/core_md.py', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-F/plugins/superheroes']))
E           assert False is True
E            +  where False = <function is_safety_machinery at 0x106352f70>('/Users/zwrose/.superheroes-worktrees/superheroes/1154-F/plugins/superheroes/lib/core_md.py', ['/Users/zwrose/.superheroes-worktrees/superheroes/1154-F/plugins/superheroes'])
E            +    where <function is_safety_machinery at 0x106352f70> = ESC.is_safety_machinery

plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py:296: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_safety_machinery_route_pin.py::test_gate_dependencies_are_fixer_refused
1 failed in 0.09s
```

**Restore:** removed the `import core_md` line from `allow_file_path`.

**Restore receipt (supplementary `git status --porcelain`):** (empty for `plugins/superheroes/lib/owner_authority.py`)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.06s
```
