# WO-D (#1151) bite-proofs — derived placeholder fixture

## BP-D1 — template-derived fixture (drift pin)

**Correction (#1162, 2026-08-26, re-executed from scratch — not transcribed).** The green run
recorded here originally read `1 passed`, which cannot be the result of the two-test command above
it (PR #1160 round-1 Minor). Both runs below were re-executed on this branch's head with that exact
command; the green is `2 passed`.

**Guarded element:** `_phase_placeholders` derivation from order templates — axis: a placeholder added to a template must surface in the drift pin without editing the fixture builder.

**Neutralization:** added `{{PROBE_ONLY}}` on its own line after `{{MODE_EVIDENCE}}` in `plugins/superheroes/rubric/orders/dispatch-panel.md` only.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-d -m pytest 'plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder' 'plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_engine_channel_renders_no_write_instruction_for_every_phase[dispatch-panel]' -q
```

**Red run (exit 1):**
```
F.                                                                       [100%]
=================================== FAILURES ===================================
____ test_placeholder_fixture_known_table_covers_every_template_placeholder ____
E       AssertionError: template placeholder(s) missing realistic fixture value: ['dispatch-panel:PROBE_ONLY']
plugins/superheroes/lib/tests/test_round_driver_order_channel.py:709: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder
1 failed, 1 passed in 0.85s
```

**Restore:** removed the `{{PROBE_ONLY}}` line from `dispatch-panel.md` (inverse of neutralization).

**Restore receipt:** `git status --porcelain` over the whole tree → empty output; the
`{{MODE_EVIDENCE}}` line in `dispatch-panel.md` carries no `{{PROBE_ONLY}}` line after it.

**Green run (exit 0) — the two-test command, both tests:**
```
2 passed in 0.64s
```

---

## BP-D2 — anti-vacuity guard

**Guarded element:** `test_panel_fixture_supplies_realistic_values_and_exact_key_set` — axis: the panel fixture must supply a realistic (non-stub) value for `DIFF_PATH`.

**Neutralization:** in `plugins/superheroes/lib/tests/test_round_driver_order_channel.py`, changed `_KNOWN_REALISTIC_VALUES[RD.P_PANEL]["DIFF_PATH"]` from `_DIFF` to `"STUB:DIFF_PATH"`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-d -m pytest 'plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_panel_fixture_supplies_realistic_values_and_exact_key_set' -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_panel_fixture_supplies_realistic_values_and_exact_key_set ___________

    def test_panel_fixture_supplies_realistic_values_and_exact_key_set():
        ...
        for name in ("MODE", "DIFF_PATH", "FINDINGS_OUTPUT_PATH", "CHANNEL"):
>           assert not ph[name].startswith("STUB:"), (name, ph[name])
E           AssertionError: ('DIFF_PATH', 'STUB:DIFF_PATH')
E           assert not True
E            +  where True = <built-in method startswith of str object at 0x106bf85b0>('STUB:')
E            +    where <built-in method startswith of str object at 0x106bf85b0> = 'STUB:DIFF_PATH'.startswith

plugins/superheroes/lib/tests/test_round_driver_order_channel.py:669: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_panel_fixture_supplies_realistic_values_and_exact_key_set
1 failed in 0.18s
```

**Restore:** changed `_KNOWN_REALISTIC_VALUES[RD.P_PANEL]["DIFF_PATH"]` back to `_DIFF`.

**Restore receipt:** `"DIFF_PATH": _DIFF,` in the `RD.P_PANEL` block of `_KNOWN_REALISTIC_VALUES`.

**Green run (exit 0):**
```
1 passed in 0.14s
```
