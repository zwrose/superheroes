# WO-G (#1140 round 2) bite-proofs — session reset, last-mention pin, malformed files

The detector under proof is `source_guard.py` (repo-root pytest plugin). Neutralizations
were applied as targeted reversible edits to `source_guard.py` for red runs and restored
by inverse edit before the green half; in-test proofs use `bite_support.patched_module`
(in-memory only).

## Baseline

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_source_guard.py -q -n auto
```

**Output:**
```
65 passed in 6.98s
```

---

## E16 — session-end reset of `configured`

**Guarded element:** `self.configured = False` at the start of `session_finish` — axis: a
second pytest session in the same interpreter must reconfigure from scratch.

**Neutralization:** `def session_finish` body's `self.configured = False` →
`pass  # configured reset neutralized` (session_finish entry only).

**Red run** (`test_bite_session_finish_resets_configured` with in-memory neutralization):
```
AssertionError: session_finish_reset: neutralized copy must not fire (red half)
```

**Restore:** inverse edit restoring `self.configured = False`.

**Green run:** `test_bite_session_finish_resets_configured` passes in E16–E18 green batch.

---

## E17 — last mention must be the good assignment

**Guarded element:** collection of `ast.Delete` and `ast.Expr` mentions in `wiring_defects`
— axis: `del pytest_plugins` after a good assignment must report defect.

**Neutralization:** `elif isinstance(node, ast.Delete):` →
`elif False and isinstance(node, ast.Delete):` (Delete branch only).

**Red run** (`test_bite_wiring_defects_last_mention_must_be_assignment` with in-memory
neutralization):
```
AssertionError: last_mention_assignment: neutralized copy must not fire (red half)
```

**Restore:** inverse edit restoring the `ast.Delete` branch.

**Green run:** `test_bite_wiring_defects_last_mention_must_be_assignment` passes in E16–E18
green batch.

---

## E18 — broadened malformed-file handling

**Guarded element:** `except (configparser.Error, OSError, UnicodeDecodeError, ValueError)`
around `pytest.ini` read/parse — axis: invalid UTF-8 must return a defect, not raise.

**Neutralization:** broad except → `except configparser.Error:` only.

**Red run** (`test_bite_wiring_defects_malformed_file_handling` with in-memory neutralization):
```
AssertionError: malformed_file_handling: neutralized copy must not fire (red half)
```

**Restore:** inverse edit restoring the broad except tuple.

**Green run:** `test_bite_wiring_defects_malformed_file_handling` passes in E16–E18 green batch.

---

## Final green (E16–E18 bite tests)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_source_guard.py::test_bite_session_finish_resets_configured \
  plugins/superheroes/lib/tests/test_source_guard.py::test_bite_wiring_defects_last_mention_must_be_assignment \
  plugins/superheroes/lib/tests/test_source_guard.py::test_bite_wiring_defects_malformed_file_handling \
  -q
```

**Output:**
```
3 passed in 0.12s
```

---

## Supersession (WO-H #1140 round 3)

E17 (`ast.Delete` / last-mention collection) and E18 (broad malformed-file `except` around
`pytest.ini` read/parse) pinned the deleted `wiring_defects` text-prediction scan. Those proofs are
superseded by `wo_h_1140.md`, which records bite-proofs for the presence-only
`missing_wiring_files()` checks that replaced the scan. E16 (`session_finish` configured reset) is
unchanged and not superseded.
