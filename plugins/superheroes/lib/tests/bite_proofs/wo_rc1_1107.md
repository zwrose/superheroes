# WO-rc1 (#1107) bite-proofs — receipt-mode truth + meta-wins precedence ratification

Four guarded elements. Each was neutralized with a targeted, reversible single-site edit,
shown red, restored, and shown green. `git diff` after each restore contains only the
order's real (non-neutralization) change — quoted per element below.

Pycache prefix used on every run: `-B -X pycache_prefix=/private/tmp/superheroes-pyc-rc1`.

## BP-rc1-1 — I1's precedence pin (`session_mode.resolve`)

**Guarded element:** `session_mode.resolve` (`plugins/superheroes/lib/session_mode.py:57`) —
axis: **precedence** — a VALID session-metadata mode must win even when driver config carries a
different VALID mode (meta-wins is ratified, not incidental).

**Neutralization:** made the meta-branch's early return conditional on agreement with config,
so a disagreeing config value falls through to the (incorrect) invalid-meta branch:

```python
        if valid is not None and meta.get("mode") == config.get("mode"):  # BITE-PROOF-NEUTRALIZE-1107-rc1
            return _fresh_result(
                valid, EVIDENCE_SESSION_META, True, None,
            )
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-rc1 -m pytest plugins/superheroes/lib/tests/test_session_mode.py::test_resolve_valid_meta_wins_over_disagreeing_valid_config -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_resolve_valid_meta_wins_over_disagreeing_valid_config __________

    def test_resolve_valid_meta_wins_over_disagreeing_valid_config():
        """I1 (#1107 WO-rc1): meta-wins precedence is ratified, not incidental. A VALID
        session-metadata mode wins even when driver config carries a different VALID mode —
        `resolve` never arbitrates disagreement, it always prefers the higher-authority source."""
>       _assert_resolved(
            sm.resolve({"mode": "branch"}, {"mode": "pr"}), "branch", sm.EVIDENCE_SESSION_META,
        )

plugins/superheroes/lib/tests/test_session_mode.py:101:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

result = {'disclosure': "Review session mode in session metadata was invalid ('branch'); defaulting to PR review.", 'evidence': 'unresolved', 'mode': 'pr', 'resolved': False}
mode = 'branch', evidence = 'session-meta'

    def _assert_resolved(result, mode, evidence):
>       assert result == {
            "mode": mode,
            "evidence": evidence,
            "resolved": True,
            "disclosure": None,
        }
E       assert {'disclosure'...olved': False} == {'disclosure'...solved': True}
E
E         Differing items:
E         {'resolved': False} != {'resolved': True}
E         {'disclosure': "Review session mode in session metadata was invalid ('branch'); defaulting to PR review."} != {'disclosure': None}
E         {'evidence': 'unresolved'} != {'evidence': 'session-meta'}
E         {'mode': 'pr'} != {'mode': 'branch'}
E       Use -v to get more diff

plugins/superheroes/lib/tests/test_session_mode.py:52: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_session_mode.py::test_resolve_valid_meta_wins_over_disagreeing_valid_config
1 failed in 0.32s
```
(Framework-noise `PytestWarning`/`rm_rf`/`OSError` lines from an unrelated shared-tmp-cleanup
race on this machine are elided from this and every capture below — none are part of the test's
own output.)

**Restore:** reverted the neutralized `if` condition back to `if valid is not None:` (inverse of
neutralization).

**Restore receipt:** `git diff plugins/superheroes/lib/session_mode.py` after restore was
**empty** — I1 required NO production change (per the work order, the current `resolve` already
implements meta-wins correctly; only the pin was missing).

**Green run:**
```
.                                                                        [100%]
1 passed in 0.04s
```

---

## BP-rc1-2 — I2's receipt-mode routing (`round_driver.build_receipt`)

**Guarded element:** `round_driver.build_receipt`'s `base["mode"]` assignment
(`plugins/superheroes/lib/round_driver.py:~3715-3727`) — axis: **truth** — the receipt's
`base.mode` must be the driver-RESOLVED mode (via `session_mode.resolve`), never a raw
`cfg.get("mode")` echo, since under meta-wins precedence the two can legitimately disagree.

**Neutralization:** replaced the resolve-routing block with the raw cfg echo it replaces:

```python
    base["mode"] = cfg.get("mode")  # BITE-PROOF-NEUTRALIZE-1107-rc1: raw cfg echo
    if base.get("mode") is None:
        base.pop("mode", None)
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-rc1 -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_receipt_base_mode_routes_through_session_mode_meta_wins -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_receipt_base_mode_routes_through_session_mode_meta_wins _________

    def test_receipt_base_mode_routes_through_session_mode_meta_wins(tmp_path):
        """I2 (#1107 WO-rc1): the receipt's base.mode is the DRIVER-RESOLVED mode, never a raw
        cfg echo. Under meta-wins precedence, session metadata overrides a disagreeing driver
        config value in the receipt too — the same precedence `session_mode.resolve` already
        applies everywhere else."""
        session_dir = str(tmp_path / "sess")
        _write_meta_json(session_dir, {"mode": "branch"})
        state = RD.new_state(_cfg(mode="pr"))
        receipt = RD.build_receipt(state, session_dir)
>       assert receipt["base"]["mode"] == "branch"
E       AssertionError: assert 'pr' == 'branch'
E
E         - branch
E         + pr

plugins/superheroes/lib/tests/test_round_driver.py:3820: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_receipt_base_mode_routes_through_session_mode_meta_wins
1 failed in 0.70s
```

**Restore:** reverted to the resolve-routing block (inverse of neutralization).

**Restore receipt:** `git diff plugins/superheroes/lib/round_driver.py` after restore, quoted:
```python
-    base = {k: cfg.get(k) for k in ("baseRef", "baseBranch", "baseFetch", "mode", "baseRepo",
+    base = {k: cfg.get(k) for k in ("baseRef", "baseBranch", "baseFetch", "baseRepo",
                                     "baseRepoCheck", "repoRoot", "diffBinding")
             if cfg.get(k) is not None}
+    # "mode" is deliberately NOT part of the generic cfg-echo tuple above (#1107 WO-rc1): under
+    # meta-wins precedence, the driver config's raw mode value is not necessarily the mode the
+    # driver actually resolved and acted on — a receipt echoing it raw would be a false receipt.
+    # Route it through the same session_mode.resolve the driver used elsewhere, and record it only
+    # when the resolution is GROUNDED (never the UNRESOLVED_MODE default — that fail-closed "pr"
+    # was never chosen by anything, so the receipt must not claim it was).
+    _meta_for_mode = _session_meta(session_dir) if session_dir else {}
+    _mode_resolved = session_mode.resolve(_meta_for_mode, cfg)
+    if _mode_resolved["resolved"]:
+        base["mode"] = _mode_resolved["mode"]
```
This IS the order's real (non-neutralization) diff — confirming the neutralization left no
residue behind.

**Green run** (all 5 of I2's edge tests, `-k receipt_base_mode`):
```
.....                                                                    [100%]
5 passed, 377 deselected in 0.53s
```

---

## BP-rc1-3 — I3's guard single-sourcing (`review_base_guard.check_base`)

**Guarded element:** `review_base_guard.check_base`'s returned `"mode"` key
(`plugins/superheroes/lib/review_base_guard.py:~589-592`) — axis: **single-sourcing** — the
returned value must be `mode_resolved["mode"]` (the ONE resolution), never a second raw re-read
of `meta.get("mode")`.

Through `check_base`'s own public entry point the two are equal by construction today (the
refusal above the return guarantees `session_mode.resolve` only reaches the return when the raw
meta value was already valid — there is no live input that makes them diverge at that entry
point). Per the bite-proof contract's "Unreachable through this entry point" shape, the proof
below is a **unit-level test on the guard itself**: it monkeypatches `session_mode.resolve` — the
exact seam `check_base` calls — to return a mode token that differs from the raw meta value
(`"resolved-not-raw"` vs `"branch"`), which lets the two owners diverge observably without also
perturbing PR-mode's unrelated repo-matching logic (the fake token is neither `"pr"` nor
`"branch"`, so the `mode_resolved["mode"] == session_mode.MODE_PR` branch stays false).

**Neutralization:**
```python
        "mode": mode,  # BITE-PROOF-NEUTRALIZE-1107-rc1: raw meta.get("mode") re-read
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-rc1 -m pytest plugins/superheroes/lib/tests/test_review_base_guard.py::test_check_base_returns_resolved_mode_not_raw_meta -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_check_base_returns_resolved_mode_not_raw_meta ______________

    def test_check_base_returns_resolved_mode_not_raw_meta(git_repo, tmp_path, monkeypatch):
        ...
        monkeypatch.setattr(rbg.session_mode, "resolve", _fake_resolve)
        r = rbg.check_base(session, root)
        assert r["ok"] is True
>       assert r["mode"] == "resolved-not-raw"
E       AssertionError: assert 'branch' == 'resolved-not-raw'
E
E         - resolved-not-raw
E         + branch

plugins/superheroes/lib/tests/test_review_base_guard.py:412: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_base_guard.py::test_check_base_returns_resolved_mode_not_raw_meta
1 failed in 0.34s
```

**Restore:** reverted to `"mode": mode_resolved["mode"],` with its single-source comment
(inverse of neutralization).

**Restore receipt:** `git diff plugins/superheroes/lib/review_base_guard.py` after restore,
quoted (the full and only diff on this file):
```python
-        "mode": mode,
+        # Single-source (#1107 WO-rc1 I3): `mode_resolved` above is the ONE resolution of this
+        # value — the raw `mode` local (read a few lines up) stays only for the refusal `detail`
+        # message above, never as a second owner of the returned value.
+        "mode": mode_resolved["mode"],
```

**Green run:**
```
.                                                                        [100%]
1 passed in 0.31s
```

---

## BP-rc1-4 — census detector (`test_ssot_drift.py`)

The census rides `test_ssot_drift.py`'s existing pinned-set mechanism (same shape as
`test_plugin_version_skew_chokepoint_census` and friends) rather than a new detector class —
`test_round_driver_and_review_base_guard_raw_mode_read_census` pins the exact set of
`get("mode")`/`["mode"]`/`"mode":`-shaped lines in `round_driver.py` and `review_base_guard.py`.

**Guarded element:** the pinned set `_EXPECTED_RAW_MODE_READS`
(`plugins/superheroes/lib/tests/test_ssot_drift.py`) — axis: **completeness** — a NEW raw-mode-read
site added to either module must show up as a set-diff failure, not silently pass.

**Neutralization:** hand-added a new raw-mode-read site (a stray extra `meta.get("mode")` call,
never referenced) right after the existing `mode = meta.get("mode")` line in
`review_base_guard.check_base`:
```python
    mode = meta.get("mode")
    _census_bite_proof_probe = meta.get("mode")  # BITE-PROOF-NEUTRALIZE-1107-rc1-census
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-rc1 -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_round_driver_and_review_base_guard_raw_mode_read_census -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_round_driver_and_review_base_guard_raw_mode_read_census _________

    def test_round_driver_and_review_base_guard_raw_mode_read_census():
        ...
        for rel, expected in _EXPECTED_RAW_MODE_READS.items():
            text = _read(rel)
            found = {
                line.strip() for line in text.splitlines() if _RAW_MODE_READ_PATTERN.search(line)
            }
>           assert found == expected, (
                "%s: raw-mode-read census drift — expected %r, found %r (a new or removed site "
                "changes this order's I1/I2/I3 analysis and must be reclassified)"
                % (rel, sorted(expected), sorted(found))
            )
E           AssertionError: lib/review_base_guard.py: raw-mode-read census drift — expected ['"mode": mode_resolved["mode"],', 'if mode_resolved["mode"] == session_mode.MODE_PR:', 'mode = meta.get("mode")'], found ['"mode": mode_resolved["mode"],', '_census_bite_proof_probe = meta.get("mode")  # BITE-PROOF-NEUTRALIZE-1107-rc1-census', 'if mode_resolved["mode"] == session_mode.MODE_PR:', 'mode = meta.get("mode")'] (a new or removed site changes this order's I1/I2/I3 analysis and must be reclassified)
E           assert {'"mode": mod....get("mode")'} == {'"mode": mod....get("mode")'}
E
E             Extra items in the left set:
E             '_census_bite_proof_probe = meta.get("mode")  # BITE-PROOF-NEUTRALIZE-1107-rc1-census'
E             Use -v to get more diff

plugins/superheroes/lib/tests/test_ssot_drift.py:2240: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_round_driver_and_review_base_guard_raw_mode_read_census
1 failed in 0.34s
```

**Restore:** removed the hand-added probe line (inverse of neutralization).

**Restore receipt:** `git diff plugins/superheroes/lib/review_base_guard.py` after restore was
identical to BP-rc1-3's final restored diff (only the real I3 change remains) — quoted there.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.25s
```
