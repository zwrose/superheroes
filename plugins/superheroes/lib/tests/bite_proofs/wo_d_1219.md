# WO-D (#1219) bite-proofs — `verifyPasses` referent pin (family C)

Guarded-element set: FC-1 … FC-5 (five elements; not grouped).

## FC-1 — `## checkpoint` prose cites `rounds[].verifyPasses`

**Guarded element:** `test_verify_passes_checkpoint_prose_pinned` — axis: the complete checkpoint sentence naming per-pass verdict totals as `rounds[].verifyPasses` is present at count 1 in `## checkpoint`.

**Neutralization:** removed the rostered sentence from `## checkpoint` via `clause_guard.without_clause_in_section` on `plugins/superheroes/skills/review-code/reference/round-driver.md`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_verify_passes_checkpoint_prose_pinned \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________________ test_verify_passes_checkpoint_prose_pinned __________________
plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:386: in test_verify_passes_checkpoint_prose_pinned
    _assert_verify_passes_checkpoint_clause(text)
plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:328: in _assert_verify_passes_checkpoint_clause
    check_clause(text, _REF_REL, _CHECKPOINT_SECTION, _CHECKPOINT_VERIFY_PASSES_CLAUSE, 1)
plugins/superheroes/lib/tests/clause_guard.py:59: in check_clause
    raise AssertionError(
E   AssertionError: skills/review-code/reference/round-driver.md (section ## checkpoint): clause '`build_receipt` (journal-derived `rounds`, `findings`, `decisions`, `seatMap`, `scriptRan`, `degraded`, `skippedBlockers` — including **per-pass verdict totals** on each round as `rounds[].verifyPasses`), then strips terminal-only keys (`certification`, `certificationShape`, `schemaVersion`, `verdict`), sets `schema` to `receipt-interim/1`, and adds a `stop` block (`reason`, `writtenAt`).' expected count 1, found 0 — re-sync the text or update the roster
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_verify_passes_checkpoint_prose_pinned
1 failed in 2.69s
```

**Restore:** reinstated the rostered sentence in `## checkpoint` (inverse of `without_clause_in_section`).

**Restore receipt:** `git status --porcelain plugins/superheroes/skills/review-code/reference/round-driver.md` — empty (file byte-for-byte restored).

**Green run:**
```
.                                                                        [100%]
1 passed in 1.75s
```

---

## FC-2 — per-round `rounds` enumeration bullet names `verifyPasses`

**Guarded element:** `test_verify_passes_rounds_bullet_pinned` — axis: `verifyPasses` occurs exactly once on the single `- \`rounds\` — per-round` bullet line.

**Neutralization:** removed `` `verifyPasses`, `` from the per-round bullet on `plugins/superheroes/skills/review-code/reference/round-driver.md`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_verify_passes_rounds_bullet_pinned \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________________ test_verify_passes_rounds_bullet_pinned ____________________
plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:392: in test_verify_passes_rounds_bullet_pinned
    _assert_verify_passes_on_rounds_bullet(text)
plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:336: in _assert_verify_passes_on_rounds_bullet
    raise AssertionError(
E   AssertionError: round-driver.md per-round `rounds` bullet: the doc's verifyPasses citation names a channel the code no longer registers, so the mirror is stale and the reference dangles
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_verify_passes_rounds_bullet_pinned
1 failed in 2.63s
```

**Restore:** reinstated `` `verifyPasses`, `` on the per-round bullet line.

**Restore receipt:** `git status --porcelain plugins/superheroes/skills/review-code/reference/round-driver.md` — empty (file byte-for-byte restored).

**Green run:**
```
.                                                                        [100%]
1 passed in 1.52s
```

---

## FC-3 — `verifyPasses` key of `ROUND_ENTRY_KEY_FORMS`

**Guarded element:** `test_verify_passes_round_entry_key_forms_membership_pinned` — axis: membership of `"verifyPasses"` in `round_driver.ROUND_ENTRY_KEY_FORMS` (derived at runtime; values inside the entry not pinned).

**Neutralization (in-memory only):** removed the `ROUND_ENTRY_KEY_FORMS["verifyPasses"]` entry block via `bite_support.patched_module` — no file written.

```python
patched_module(RD, [(_ROUND_ENTRY_KEY_FORMS_ENTRY, '')])
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_verify_passes_round_entry_key_forms_membership_pinned \
  -q --tb=short
```

**Red run** (positive detector against in-memory neutralized module):
```
Traceback (most recent call last):
  File "<stdin>", line 11, in <module>
  File "plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py", line 345, in _assert_verify_passes_in_round_entry_key_forms
    raise AssertionError(
AssertionError: round_driver.ROUND_ENTRY_KEY_FORMS: the doc's verifyPasses citation names a channel the code no longer registers, so the mirror is stale and the reference dangles
```

**Restore:** in-memory only — discard patched module; nothing written to disk.

**Restore receipt:** trivially clean because nothing was written.

**Green run:**
```
.                                                                        [100%]
1 passed in 1.70s
```

---

## FC-4 — `verifyPasses` member of `VERIFIER_FOLD_DISCLOSURE_CHANNELS`

**Guarded element:** `test_verify_passes_verifier_fold_channel_membership_pinned` — axis: membership of `"verifyPasses"` in `round_driver.VERIFIER_FOLD_DISCLOSURE_CHANNELS`.

**Neutralization (in-memory only):** `VERIFIER_FOLD_DISCLOSURE_CHANNELS = ("verifyPasses",)` → `VERIFIER_FOLD_DISCLOSURE_CHANNELS = ()` via `patched_module` — no file written.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_verify_passes_verifier_fold_channel_membership_pinned \
  -q --tb=short
```

**Red run** (positive detector against in-memory neutralized module):
```
Traceback (most recent call last):
  File "<stdin>", line 9, in <module>
  File "plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py", line 354, in _assert_verify_passes_in_verifier_fold_channels
    raise AssertionError(
AssertionError: round_driver.VERIFIER_FOLD_DISCLOSURE_CHANNELS: the doc's verifyPasses citation names a channel the code no longer registers, so the mirror is stale and the reference dangles
```

**Restore:** in-memory only — discard patched module; nothing written to disk.

**Restore receipt:** trivially clean because nothing was written.

**Green run:**
```
.                                                                        [100%]
1 passed in 1.58s
```

---

## FC-5 — `verifyPasses` member of `RESUMABLE_DISCLOSURE_CHANNELS`

**Guarded element:** `test_verify_passes_resumable_channel_membership_pinned` — axis: membership of `"verifyPasses"` in `round_driver.RESUMABLE_DISCLOSURE_CHANNELS`.

**Neutralization (in-memory only):** removed `    "verifyPasses": _dict_list,` from the registry dict via `patched_module` — no file written.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_verify_passes_resumable_channel_membership_pinned \
  -q --tb=short
```

**Red run** (positive detector against in-memory neutralized module):
```
Traceback (most recent call last):
  File "<stdin>", line 9, in <module>
  File "plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py", line 364, in _assert_verify_passes_in_resumable_channels
    raise AssertionError(
AssertionError: round_driver.RESUMABLE_DISCLOSURE_CHANNELS: the doc's verifyPasses citation names a channel the code no longer registers, so the mirror is stale and the reference dangles
```

**Restore:** in-memory only — discard patched module; nothing written to disk.

**Restore receipt:** trivially clean because nothing was written.

**Green run:**
```
.                                                                        [100%]
1 passed in 1.99s
```
