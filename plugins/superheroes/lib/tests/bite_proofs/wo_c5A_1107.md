# Bite-proof record — wo/1107-c5A (derived-placeholder registry drift pin)

Per `plugins/superheroes/rubric/bite-proof.md`. One guarded element, one neutralization, one red,
one restore, one green.

## Element 1 — derived-placeholder registry drift pin (`test_derived_placeholder_names_match_production_derivation`)

**Guarded element:** `plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_derived_placeholder_names_match_production_derivation`,
which pins that `_DERIVED_PLACEHOLDER_NAMES` equals the union of names production
`_derived_placeholders` actually adds beyond the placeholders it was handed, computed per phase.
**Axis:** registry drift — a name registered as derived that no derivation produces, or a name
production derives that nobody registered (#1107).

**Neutralization:** removed the `PRIOR_COMMENTS_INSTRUCTION_BLOCK` derivation block from
`_panel_derived_placeholders` in `round_orders.py` (the targeted, revertible edit that leaves the
detector unedited):
```python
    # WO-c5A neutralization — removed after red capture
```
(replacing the `prior_instr = ph.get("PRIOR_COMMENTS_PATH", "")` through
`ph["PRIOR_COMMENTS_INSTRUCTION_BLOCK"] = ""` branches.)

**Raw red** (`/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-c5A -m pytest plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_derived_placeholder_names_match_production_derivation -q`):
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_derived_placeholder_names_match_production_derivation __________

    def test_derived_placeholder_names_match_production_derivation():
        """`_DERIVED_PLACEHOLDER_NAMES` must equal the union of names production `_derived_placeholders`
        adds beyond the placeholders it was handed, computed per phase from the fixture's direct names.

        Bite axis: registry drift — a name registered as derived that no derivation produces, or a name
        production derives that nobody registered — not render behaviour, which the phase-census tests
        above already cover.
        """
        produced_union = set()
        for phase in RO.ORDER_PHASES:
            ph = _phase_placeholders(phase, RD.CHANNEL_FILE)
            context = {
                "session_dir": SESSION,
                "round": 1,
                "attempt": 1,
                "diff_path": _DIFF,
                "rubric_path": "/plugin/rubric/review-base.md",
                "core_path": "/proj/core.md",
                "layer_path": "/proj/layer.md",
                "repo_root": "/proj",
                "landing_path": BARE,
                "envelope_stub_path": SESSION + "/round-1/landing/p/skey.a1.stub.json",
                "ratified_residuals": "",
                "residuals_provenance": "prov",
                "residuals_read_failure": None,
                "payload": {},
                "host_seat": True,
                "placeholders": ph,
            }
            derived = RO._derived_placeholders(phase, context)
            produced_union |= set(derived.keys()) - set(ph.keys())
        registered = set(_DERIVED_PLACEHOLDER_NAMES)
        missing_from_production = registered - produced_union
>       assert not missing_from_production, (
            "registered derived name(s) production does not produce: %s"
            % sorted(missing_from_production)
        )
E       AssertionError: registered derived name(s) production does not produce: ['PRIOR_COMMENTS_INSTRUCTION_BLOCK']
E       assert not {'PRIOR_COMMENTS_INSTRUCTION_BLOCK'}

plugins/superheroes/lib/tests/test_round_driver_order_channel.py:751: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_derived_placeholder_names_match_production_derivation
1 failed in 0.16s
```

**Restore:** inverse edit — restored the full `prior_instr` derivation block in
`_panel_derived_placeholders`:
```python
    prior_instr = ph.get("PRIOR_COMMENTS_PATH", "")
    if not isinstance(prior_instr, str):
        prior_instr = ""
    if prior_instr and not prior_instr.startswith("("):
        ph["PRIOR_COMMENTS_INSTRUCTION_BLOCK"] = (
            "%s contains prior review comments and their\nthreads." % prior_instr
        )
    elif prior_instr.startswith("("):
        ph["PRIOR_COMMENTS_INSTRUCTION_BLOCK"] = (
            "No prior-comments.json was supplied for this PR — there are no prior review "
            "comments or author justifications to consult."
        )
    else:
        ph["PRIOR_COMMENTS_INSTRUCTION_BLOCK"] = ""
```

**Restore receipt:** `git diff plugins/superheroes/lib/round_orders.py` shows no
`WO-c5A neutralization` token after restore; the derivation block is present.

**Raw green** (same command, post-restore):
```
.                                                                        [100%]
1 passed in 0.14s
```
