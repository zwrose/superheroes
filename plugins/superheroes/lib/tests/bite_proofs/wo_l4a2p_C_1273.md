# WO-L4A2P-C (#1273) bite-proof — launcher `canary` refusal ladder

**Covers:** WO-C / WO-C2 (`launcher.canary` + CLI verb).

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-C1 | `if size > MAX_STDOUT_CAPTURE and tool_calls == 0: return _fail("canary-transcript-truncated")` | truncated transcript with zero retained tool calls refuses | `test_canary_transcript_truncated_zero_tool_calls` |
| BP-C2 | `if read["state"] != "ok":` ledger gate | non-ok ledger states refuse before fold | `test_canary_ledger_unreadable_states[interiorCorrupt]` |
| BP-C3 | `if len(paths) > 1: return _fail("canary-transcript-ambiguous")` | two transcript paths refuse | `test_canary_transcript_ambiguous` |
| BP-C4 | lane `configDir` required (no `os.environ.get("CLAUDE_CONFIG_DIR")` fallback) | caller env cannot substitute missing lane root | `test_canary_config_dir_absent` |
| BP-C5 | AST: no `engine_dispatch._*` attribute access in `launcher.py` | private dispatch names unreachable from launcher | `test_canary_launcher_no_private_engine_dispatch_access` |

---

## BP-C1 — truncation with zero tool calls

**neutralization:** delete the `size > MAX_STDOUT_CAPTURE and tool_calls == 0` refusal block in `canary`.

**command:** `pytest plugins/superheroes/lib/tests/test_launcher.py::test_canary_transcript_truncated_zero_tool_calls -q`

**raw red (exit 1):** `assert True is False` at `test_launcher.py:7824`

**restore:** reinstate `if size > engine_dispatch.MAX_STDOUT_CAPTURE and tool_calls == 0: return _fail("canary-transcript-truncated")`.

**raw green (exit 0):** `1 passed in 0.56s`

---

## BP-C2 — ledger state gate

**neutralization:** replace `if read["state"] != "ok":` with `if False:`.

**command:** `pytest …::test_canary_ledger_unreadable_states[interiorCorrupt] -q`

**raw red (exit 1):** `assert 'canary-lane-unknown' == 'canary-ledger-unreadable:interiorCorrupt'` at `test_launcher.py:7661`

**restore:** reinstate `if read["state"] != "ok":`.

**raw green (exit 0):** `1 passed in 0.38s`

---

## BP-C3 — transcript ambiguity

**neutralization:** remove `if len(paths) > 1: return _fail("canary-transcript-ambiguous")`.

**command:** `pytest …::test_canary_transcript_ambiguous -q`

**raw red (exit 1):** `assert 'canary-transcript-unreadable' == 'canary-transcript-ambiguous'` at `test_launcher.py:7783`

**restore:** reinstate ambiguous check.

**raw green (exit 0):** `1 passed in 0.51s`

---

## BP-C4 — config dir fail-closed

**neutralization:** when lane `configDir` absent, set `config_dir = os.environ.get("CLAUDE_CONFIG_DIR")` before refusal.

**command:** `pytest …::test_canary_config_dir_absent -q`

**raw red (exit 1):** `assert True is False` at `test_launcher.py:7752`

**restore:** remove env fallback; keep lane-only `configDir`.

**raw green (exit 0):** `1 passed in 0.52s`

---

## BP-C5 — no private engine_dispatch from launcher

**neutralization:** add `_probe = engine_dispatch._glob_transcript_paths` inside `canary`.

**command:** `pytest …::test_canary_launcher_no_private_engine_dispatch_access -q`

**raw red (exit 1):** `assert ['launcher-private-engine-dispatch:2306'] == []` at `test_launcher.py:7868`

**restore:** remove the `_probe` line.

**raw green (exit 0):** `1 passed in 0.38s`
