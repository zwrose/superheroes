# WO-A (#1145) bite-proofs — review findings-member schema home

## BP1 — canonical keys match schema

**Guarded element:** `test_canonical_member_keys_match_schema_independently` — axis: canonical member keys are read from the shipped schema, not hand-copied.

**Neutralization:** appended `("bogus",)` to the schema-read assignment in `_init_schema_constants()` (`plugins/superheroes/lib/review_findings_schema.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_canonical_member_keys_match_schema_independently -q
```

**Red run:**
```
=================================== FAILURES ===================================
____________ test_canonical_member_keys_match_schema_independently _____________

    def test_canonical_member_keys_match_schema_independently():
        schema_keys = tuple(_schema_finding_properties().keys())
>       assert RFS.CANONICAL_MEMBER_KEYS == schema_keys
E       AssertionError: assert ('id', 'sever..., 'file', ...) == ('id', 'sever..., 'file', ...)
E         Left contains one more item: 'bogus'

plugins/superheroes/lib/tests/test_review_findings_schema.py:254: AssertionError
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_canonical_member_keys_match_schema_independently
1 failed in 0.44s
```

**Restore:** removed `+ ("bogus",)` from the assignment (inverse of neutralization).

**Restore receipt:** `CANONICAL_MEMBER_KEYS = tuple(finding_props.keys())`

**Green run:**
```
1 passed in 0.15s
```

---

## BP2 — synonym targets are canonical

**Guarded element:** `test_substance_key_synonym_targets_are_canonical` — axis: every synonym maps to a declared canonical member key.

**Neutralization:** changed `"message": "body"` to `"message": "not_canonical"` in `SUBSTANCE_KEY_SYNONYMS`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_substance_key_synonym_targets_are_canonical -q
```

**Red run:**
```
=================================== FAILURES ===================================
_______________ test_substance_key_synonym_targets_are_canonical _______________

>           assert target in canonical, "%s -> %s" % (synonym, target)
E           AssertionError: message -> not_canonical

plugins/superheroes/lib/tests/test_review_findings_schema.py:334: AssertionError
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_substance_key_synonym_targets_are_canonical
1 failed in 0.29s
```

**Restore:** reverted `"message": "not_canonical"` to `"message": "body"`.

**Restore receipt:** `"message": "body",`

**Green run:**
```
1 passed in 0.15s
```

---

## BP3 — censused substance without severity (fail-closed engagement)

**Guarded element:** `test_member_is_engaged_table[censused-without-severity]` — axis: censused substance keys require valid severity (leg b).

**Neutralization:** inserted an early return treating any censused key with non-trivial text as engaged before the severity gate (leg (a) bleed).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_is_engaged_table[censused-without-severity]' -q
```

**Red run:**
```
>       assert RFS.member_is_engaged(member) is expected
E       AssertionError: assert True is False

plugins/superheroes/lib/tests/test_review_findings_schema.py:375: AssertionError
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_is_engaged_table[censused-without-severity]
1 failed in 0.18s
```

**Restore:** removed the inserted early-return block (inverse edit).

**Restore receipt:** leg (b) censused loop follows legacy leg (a) with no censused early return.

**Green run:**
```
1 passed in 0.12s
```

---

## BP4 — trivial whitespace body (fail-closed engagement)

**Guarded element:** `test_member_is_engaged_table[trivial-body]` — axis: non-trivial string requires `.strip()` non-empty.

**Neutralization:** changed `_non_trivial_string` to `return isinstance(value, str)` (dropped strip check).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_is_engaged_table[trivial-body]' -q
```

**Red run:**
```
>       assert RFS.member_is_engaged(member) is expected
E       AssertionError: assert True is False

plugins/superheroes/lib/tests/test_review_findings_schema.py:375: AssertionError
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_is_engaged_table[trivial-body]
1 failed in 0.13s
```

**Restore:** restored `return isinstance(value, str) and value.strip() != ""`.

**Restore receipt:** quoted restored line in `_non_trivial_string`.

**Green run:**
```
1 passed in 0.10s
```

---

## BP5 — normalize does not overwrite existing canonical target

**Guarded element:** `test_normalize_member_does_not_overwrite_existing_target` — axis: additive normalization never overwrites an existing canonical key.

**Neutralization:** changed `if synonym in out and canonical not in out:` to `if synonym in out:` in `normalize_member`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_normalize_member_does_not_overwrite_existing_target -q
```

**Red run:**
```
>       assert out["body"] == "canonical body"
E       AssertionError: assert 'from message' == 'canonical body'

plugins/superheroes/lib/tests/test_review_findings_schema.py:389: AssertionError
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_normalize_member_does_not_overwrite_existing_target
1 failed in 0.13s
```

**Restore:** restored `if synonym in out and canonical not in out:`.

**Restore receipt:** `if synonym in out and canonical not in out:`

**Green run:**
```
1 passed in 0.10s
```

---

## BP6 — severity enum membership (leg b)

**Guarded element:** `test_member_is_engaged_off_scale_severity_fails_leg_b` — axis: leg (b) requires enum membership, not mere severity presence.

**Neutralization:** replaced `if isinstance(severity, str) and severity in SEVERITY_TIERS:` with `if member.get("severity"):`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_is_engaged_off_scale_severity_fails_leg_b -q
```

**Red run:**
```
_______ test_member_is_engaged_off_scale_severity_fails_leg_b[lowercase] _______
>       assert RFS.member_is_engaged(member) is False
E       AssertionError: assert True is False

plugins/superheroes/lib/tests/test_review_findings_schema.py:306: AssertionError
(... four additional parametrized cases failed similarly ...)
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_is_engaged_off_scale_severity_fails_leg_b[lowercase]
FAILED ...[true]
FAILED ...[off-scale-string]
6 failed in 0.15s
```

**Restore:** restored `if isinstance(severity, str) and severity in SEVERITY_TIERS:`.

**Restore receipt:** `if isinstance(severity, str) and severity in SEVERITY_TIERS:`

**Green run:**
```
6 passed in 0.15s
```

---

## BP7 — near-copy sentinel walk

**Guarded element:** `test_member_carries_sentinel_true_for_near_copy` — axis: sentinel anywhere in member refuses near-copy bypass (not only id/severity).

**Neutralization:** replaced recursive `_walk` in `member_carries_sentinel` with top-level `id`/`severity` checks only.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_near_copy -q
```

**Red run:**
```
>       assert RFS.member_carries_sentinel(near_copy) is True
E       AssertionError: assert False is True

plugins/superheroes/lib/tests/test_review_findings_schema.py:321: AssertionError
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_near_copy
1 failed in 0.25s
```

**Restore:** re-inserted recursive `_walk` body (inverse edit; no `git checkout`).

**Restore receipt:** full recursive `member_carries_sentinel` with `_walk` restored.

**Green run:**
```
1 passed in 0.14s
```

---

## BP8 — import-cycle pin

**Guarded element:** `test_review_findings_schema_import_cycle_pin` — axis: declared home imports neither `engine_adapter`, `round_orders`, nor `payload_contracts`.

**Neutralization:** inserted `import engine_adapter  # bite-proof neutralization` after the module docstring.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_review_findings_schema_import_cycle_pin -q
```

**Red run:**
```
>                   assert root not in forbidden
E                   AssertionError: assert 'engine_adapter' not in {'engine_adapter', 'payload_contracts', 'round_orders'}

plugins/superheroes/lib/tests/test_review_findings_schema.py:291: AssertionError
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_review_findings_schema_import_cycle_pin
1 failed in 0.26s
```

**Restore:** deleted the inserted import line (inverse edit).

**Restore receipt:** module opens with docstring then `import json` / `import os` only.

**Green run:**
```
1 passed in 0.26s
```
