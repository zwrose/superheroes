# WO-D (#1124 items 4 + 12 + 15) bite-proofs

## Baseline (post-WO-D test edits)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_result_kind_census_survives_consumers \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_teardown_raises_on_undeclared_route_end_to_end \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_teardown_raises_on_declared_route_mismatch_end_to_end \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_git_fake_teardown_raises_on_unregistered_helper_value_end_to_end \
  -q
```

**Output:**
```
7 passed in 5.02s
```

---

## N1 — single-payload census

**Guarded element:** `test_review_result_kind_census_survives_consumers` other-kind absence loop — axis: a review result carries the payload of its own `resultKind` and of no other kind (key absence carries discrimination; `_review_result_payload(result, other_kind)` is vacuously true because it short-circuits on `resultKind` mismatch).

**Neutralization:** after `graded = ED._build_running_graded(...)`, injected a second kind's payload into both `res` and `graded[0]`:
```python
other_kind = next(k for k in ED.REVIEW_RESULT_KINDS if k != kind)
res[other_kind] = res[kind]
graded[0][other_kind] = graded[0][kind]
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_result_kind_census_survives_consumers[findings]' \
  -q
```

**Red run:**
```
E           AssertionError: assert 'verdicts' not in {'attempt': 1, 'findings': [{'id': 'f1', 'message': 'issue found'}], 'resultKind': 'findings', 'verdicts': [{'id': 'f1', 'message': 'issue found'}]}

plugins/superheroes/lib/tests/test_engine_dispatch.py:2941: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_result_kind_census_survives_consumers[findings]
1 failed in 0.76s
```

**Restore:** removed the three injection lines from the census test.

**Restore receipt:** restored lines removed from `test_engine_dispatch.py` (no `engine_dispatch.py` touch).

**Green run:**
```
1 passed in 0.68s
```

---

## N2 — mtime candidate in post-reap fold

**Guarded element:** `_fold_stream_activity` mtime candidate — axis: when post-reap growth sees `mtime` later than the in-band tick, folded `lastActivityAt` is that `mtime`.

**Neutralization:** deleted `mtime` from `candidates` at `engine_dispatch.py:2118` (`candidates = [tick_moment]`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_fold_stream_activity_mtime_wins_when_later_than_tick \
  -q
```

**Red run:**
```
>       assert last_activity_at == stored_mtime
E       assert 1000000.0 == 1000500.0

plugins/superheroes/lib/tests/test_engine_dispatch.py:3238: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_fold_stream_activity_mtime_wins_when_later_than_tick
1 failed in 0.74s
```

**Restore:** restored `candidates = [mtime, tick_moment]` at `engine_dispatch.py:2118`.

**Restore receipt:** `git status --porcelain` showed no `engine_dispatch.py` entry after restore.

**Green run:**
```
1 passed in 0.64s
```

---

## N3 — tick candidate in post-reap fold

**Guarded element:** `_fold_stream_activity` tick candidate — axis: when the in-band tick beats file `mtime`, folded `lastActivityAt` is the tick.

**Neutralization:** deleted `tick_moment` from `candidates` at `engine_dispatch.py:2118` (`candidates = [mtime]`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_fold_stream_activity_tick_wins_when_later_than_mtime \
  -q
```

**Red run:**
```
>       assert last_activity_at == tick_moment
E       assert 1000000.0 == 1000500.0

plugins/superheroes/lib/tests/test_engine_dispatch.py:3256: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_fold_stream_activity_tick_wins_when_later_than_mtime
1 failed in 0.78s
```

**Restore:** restored `candidates = [mtime, tick_moment]` at `engine_dispatch.py:2118`.

**Restore receipt:** `git status --porcelain` showed no `engine_dispatch.py` entry after restore.

**Green run:**
```
1 passed in 0.61s
```

---

## N4 — child conftest premise before import

**Guarded element:** `test_probe_guard_selftest_child_conftest_premise_before_import` — axis: generated child `conftest_source` places the premise `assert` before `import test_engine_dispatch`.

**Neutralization:** moved the premise `assert` line back below `import test_engine_dispatch as TED` in `_run_probe_guard_selftest_child`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_guard_selftest_child_conftest_premise_before_import \
  -q
```

**Red run:**
```
>       assert assert_pos < import_pos
E       assert 542 < 402

plugins/superheroes/lib/tests/test_engine_dispatch.py:6565: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_guard_selftest_child_conftest_premise_before_import
1 failed in 0.69s
```

**Restore:** moved premise `assert` back above `import test_engine_dispatch as TED` in `_run_probe_guard_selftest_child`.

**Restore receipt:** restored `conftest_source` string assembly in `test_engine_dispatch.py`.

**Green run:**
```
1 passed in 0.64s
```

---

## Delivered production diff

`engine_dispatch.py` delivered diff is empty — confirmed via `git status --porcelain` (only `plugins/superheroes/lib/tests/test_engine_dispatch.py` modified).
