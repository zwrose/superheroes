# WO-E (#1145) bite-proofs — `_worktree_dirtied_forfeit` disclosure truth

## BP-E1 — attempt-number substitution in disclosure

**Guarded element:** `_worktree_dirtied_forfeit` disclosure attempt clause — axis: disclosure names the real `attempts` parameter, not a hard-coded 1.

**Neutralization:** substituted `int(attempts)` with literal `1` in the disclosure format tuple (`engine_dispatch.py` disclosure `%` line).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirtied_forfeit_disclosure_attempt2_names_attempt_not_retry_refused \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_worktree_dirtied_forfeit_disclosure_attempt2_names_attempt_not_retry_refused _
plugins/superheroes/lib/tests/test_engine_dispatch.py:5959: in test_worktree_dirtied_forfeit_disclosure_attempt2_names_attempt_not_retry_refused
    assert "attempt %d" % ED.MAX_ATTEMPTS in disclosure
E   AssertionError: assert ('attempt %d' % 2) in 'cursor attempt 1 report was not gradeable; the attempt budget was exhausted. The worktree is left exactly as the engine left it — inspect and clean it yourself.'
E    +  where 2 = ED.MAX_ATTEMPTS
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirtied_forfeit_disclosure_attempt2_names_attempt_not_retry_refused
1 failed in 4.11s
```

**Restore:** reverted disclosure tuple to `% (engine, int(attempts), reason_clause)`.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
.                                                                        [100%]
1 passed in 3.42s
```

---

## BP-E2 — reason branch in disclosure

**Guarded element:** `_worktree_dirtied_forfeit` reason branch — axis: `attempts >= MAX_ATTEMPTS` cites exhausted budget, not a refused retry.

**Neutralization:** changed the `else` branch `reason_clause` to the retry-refused sentence (same as the `< MAX_ATTEMPTS` branch).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirtied_forfeit_disclosure_attempt2_names_attempt_not_retry_refused \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_worktree_dirtied_forfeit_disclosure_attempt2_names_attempt_not_retry_refused _
plugins/superheroes/lib/tests/test_engine_dispatch.py:5960: in test_worktree_dirtied_forfeit_disclosure_attempt2_names_attempt_not_retry_refused
    assert "retry was refused" not in disclosure
E   AssertionError: assert 'retry was refused' not in 'cursor atte...it yourself.'
E     
E     'retry was refused' is contained here:
E       able; the retry was refused because a second attempt on a dirtied tree can contaminate or commit partial work. The worktree is left exactly as the engine left it — inspect and clean it yourself.
E     ?           +++++++++++++++++
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirtied_forfeit_disclosure_attempt2_names_attempt_not_retry_refused
1 failed in 3.17s
```

**Restore:** restored `else` branch to `reason_clause = "the attempt budget was exhausted. "`.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
..                                                                       [100%]
2 passed in 3.53s
```

---

## Final green (all WO-E tests)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirtied_forfeit_disclosure_attempt1_names_attempt_and_refused_retry \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_worktree_dirtied_forfeit_disclosure_attempt2_names_attempt_not_retry_refused \
  -q
```

**Output:**
```
..                                                                       [100%]
2 passed in 3.53s
```
