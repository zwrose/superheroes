# WO-D (#1145) bite-proofs — four non-biting detectors cured

## Baseline (post-fix full file)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py -q -n auto
```

**Output:**
```
472 passed in 38.21s
```

---

## D1 — `_cap_file_tail` rewrite_failed signal

**Guarded element:** `test_cap_file_tail_rewrite_failure_signal_distinct_from_clean_cap` — axis: failed rewrite returns `(truncated=True, observed=<pre-cap count>, rewrite_failed=True)` distinct from clean cap.

**Neutralization:** in `_cap_file_tail` OSError handler, returned `rewrite_failed=False` instead of `True` (`engine_dispatch.py:2014-2015`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_cap_file_tail_rewrite_failure_signal_distinct_from_clean_cap \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_cap_file_tail_rewrite_failure_signal_distinct_from_clean_cap _______
plugins/superheroes/lib/tests/test_engine_dispatch.py:5804: in test_cap_file_tail_rewrite_failure_signal_distinct_from_clean_cap
    assert rewrite_failed is True
E   assert False is True
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_cap_file_tail_rewrite_failure_signal_distinct_from_clean_cap
1 failed in 1.78s
```

**Restore:** reverted handler to `return True, observed, True` with axis comment.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py` (during proof only; clean after restore)

**Green run:**
```
.                                                                        [100%]
1 passed in 1.45s
```

---

## D2a — cross-kind payload key absence

**Guarded element:** `test_review_result_kind_census_survives_consumers` other-kind absence loop — axis: no other kind's payload key is present on the result.

**Neutralization:** after building `res` / `graded[0]`, injected a second kind's payload:
```python
res[other_kinds[0]] = res[kind]
graded[0][other_kinds[0]] = graded[0][kind]
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_result_kind_census_survives_consumers[findings]' \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_review_result_kind_census_survives_consumers[findings] __________
plugins/superheroes/lib/tests/test_engine_dispatch.py:2903: in test_review_result_kind_census_survives_consumers
    assert other_kind not in res
E   AssertionError: assert 'verdicts' not in {'argv': [...], 'findings': [{'id': 'f1', 'message': 'issue found'}], ...}
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_result_kind_census_survives_consumers[findings]
1 failed in 1.50s
```

**Restore:** removed the two injection lines from the census test.

**Restore receipt:** test file only (no production touch).

**Green run:** covered by baseline (472 passed).

---

## D2b — `_review_result_payload` kind-mismatch short-circuit

**Guarded element:** `test_review_result_payload_short_circuits_on_kind_mismatch` — axis: returns `(False, None)` on `resultKind` mismatch even when the other kind's payload key is present.

**Neutralization:** removed the `result.get("resultKind") != kind` guard from `_review_result_payload` (`engine_dispatch.py:2487-2488`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_result_payload_short_circuits_on_kind_mismatch \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_review_result_payload_short_circuits_on_kind_mismatch __________
plugins/superheroes/lib/tests/test_engine_dispatch.py:2962: in test_review_result_payload_short_circuits_on_kind_mismatch
    assert has_payload is False
E   assert True is False
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_review_result_payload_short_circuits_on_kind_mismatch
1 failed in 2.66s
```

**Restore:** restored the `resultKind` mismatch guard.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py` (during proof only; clean after restore)

**Green run:**
```
.                                                                        [100%]
1 passed in 1.28s
```

---

## D3 — generated conftest premise-before-import ordering

**Guarded element:** `test_probe_guard_selftest_child_conftest_premise_before_import` — axis: premise `assert` precedes `import test_engine_dispatch` in generated conftest text.

**Neutralization:** in `_probe_guard_selftest_conftest_source`, moved `import test_engine_dispatch as TED` above the premise `assert`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_guard_selftest_child_conftest_premise_before_import \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_probe_guard_selftest_child_conftest_premise_before_import ________
plugins/superheroes/lib/tests/test_engine_dispatch.py:6909: in test_probe_guard_selftest_child_conftest_premise_before_import
    assert assert_pos < import_pos
E   assert 125 < 89
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_probe_guard_selftest_child_conftest_premise_before_import
1 failed in 1.28s
```

**Restore:** reverted `_probe_guard_selftest_conftest_source` to assert-before-import order.

**Restore receipt:** test file only.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.68s
```

---

## D4 — `stdout-marker-overruled` journal guard

**Guarded element:** `test_stdout_marker_overruled_absent_when_stamped_capture_lacks_marker` — axis: stamped authoritative record with no truncation marker writes no `stdout-marker-overruled` journal.

**Neutralization:** hoisted `_journal_append` out of the `_stdout_capture_truncated(text)` guard in `_attempt_stdout_truncated` stamped branch (`engine_dispatch.py:2807-2816`).

**Command (red negative + green positives under hoist):**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_absent_when_stamped_capture_lacks_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_journal_on_authoritative_suppression \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_oversized_digit_claim_null_marker_bytes \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_just_over_digit_bound_claim_null_marker_bytes \
  -q --tb=line
```

**Red run:**
```
F...                                                                     [100%]
=================================== FAILURES ===================================
/private/tmp/sh1145/wo-D/plugins/superheroes/lib/tests/test_engine_dispatch.py:6001: AssertionError: assert 1 == 0
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_absent_when_stamped_capture_lacks_marker
1 failed, 3 passed in 2.41s
```

**Restore:** restored `_journal_append` inside the `_stdout_capture_truncated(text)` guard.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py` (during proof only; clean after restore)

**Green run (both negative cases):**
```
..                                                                       [100%]
2 passed in 2.47s
```

**Post-restore `git status --porcelain`:**
```
 M plugins/superheroes/lib/tests/test_engine_dispatch.py
```
