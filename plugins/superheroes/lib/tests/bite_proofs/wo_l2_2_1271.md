# Bite-proof record — #1271 layer 2 (seam detectors WO-L2-3)

Contract: `rubric/bite-proof.md`. Every proof below ran with the **detector unedited**; the
neutralization was applied as a targeted, reversible edit to the **production call site**, and
reverted by its exact inverse. Probes ran in the implementer worktree at `60933608`.

**Who ran these.** A dispatched implementer (WO-L2-3). Mutation probes are verification receipts
for the orchestrator to re-run independently.

**Redaction.** Nothing here required redaction: no secrets, tokens, private URLs, or PII appear in
any capture. Raw captures are quoted to their decisive lines; full captures were session-scoped
scratch at `/private/tmp/wo_l2_3_proofs.json`, removed after this record was written.

**The guarded-element set — sixteen elements, declared in WO-L2-3 and enumerated here.** Each is an
independently neutralizable element with its own refusal or assertion; none is grouped into an
equivalence class.

| # | Guarded element | Axis |
|---|---|---|
| 1 | digest comparison in `_assemble_dispatch_evidence` | `evidence-result-mismatch` on digest disagreement |
| 2 | result-kind membership in landing payload | `evidence-result-mismatch` when kind absent from landing |
| 3 | result digest/kind presence in runner record | `result-binding-incomplete` |
| 4 | `_result_digest_and_kind_from_parse` parse independence | zero-finding review still yields digest and kind |
| 5 | refusal branch in `_attempt_ended_successfully` | `attempt-not-completed` on engine refusal |
| 6 | timeout branch in `_attempt_ended_successfully` | `attempt-not-completed` on timeout |
| 7 | exit-code branch in `_attempt_ended_successfully` | `attempt-not-completed` on non-zero exit |
| 8 | tokens validation in `_validate_execution_evidence` | malformed observation `tokens` |
| 9 | toolCalls validation | malformed observation `toolCalls` |
| 10 | stdoutBytes validation | malformed observation `stdoutBytes` |
| 11 | wallSeconds validation | malformed observation `wallSeconds` |
| 12 | observation source validation | malformed observation `source` |
| 13 | read literal validation | observation `read` outside closed set |
| 14 | `EXECUTION_EVIDENCE_READ_VALUES` drift binding | read literals bound to `ENGAGEMENT_READ_VALUES` |
| 15 | bool exclusion in tokens and stdoutBytes checks | bool masquerading as integer |
| 16 | missing `resultKind` in execution evidence | refusal when `resultKind` is absent |

---

## 1 — evidence-result-mismatch — digest disagreement

**Guarded element.** `round_driver.py:7280-7283 — digest comparison in `_assemble_dispatch_evidence``.
**Axis.** Refusal when runner result digest disagrees with landing payload digest.

**Neutralization** (production call site, targeted):

```python
-     if result_digest != payload_digest:
+     if False and result_digest != payload_digest:
```

**Test.** `plugins/superheroes/lib/tests/test_round_commit_seams.py::test_seam_a_evidence_result_digest_mismatch_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_____________ test_seam_a_evidence_result_digest_mismatch_refuses ______________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-969/test_seam_a_evidence_result_di0')
adapters = <test_round_commit_seams.FakeAdapters object at 0x10ab94be0>

    def test_seam_a_evidence_result_digest_mismatch_refuses(tmp_path, adapters):
      d = _session(tmp_path, name="ev-digest-mismatch")
      pend = _pending(d)
      path, _env, before = _dispatch_observed_land(
          d, "code-reviewer", payload={"findings": ["stale-content"]})
      order_path = RR.order_prompt_path(d, pend["round"], pend["phase"],
                                         RR.storage_key("code-reviewer"), pend["attempt"])
      run_dir = _execution_run_dir(tmp_path, order_path, name="ev-digest-mismatch-run")
      out = RD.cmd_record_result(d, "code-reviewer", evidence_run_dir=run_dir)
>     assert out["ok"] is False
E     assert True is False

plugins/superheroes/lib/tests/test_round_commit_seams.py:784: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_commit_seams.py::test_seam_a_evidence_result_digest_mismatch_refuses
1 failed in 1.85s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 1.88s
```
---

## 2 — evidence-result-mismatch — kind absent from landing

**Guarded element.** `round_driver.py:7276-7278 — result kind membership in landing payload`.
**Axis.** Refusal when parsed result kind is absent from the landing payload.

**Neutralization** (production call site, targeted):

```python
-     if not isinstance(envelope_payload, dict) or result_kind not in envelope_payload:
+     if False and (not isinstance(envelope_payload, dict) or result_kind not in envelope_payload):
```

**Test.** `plugins/superheroes/lib/tests/test_round_commit_seams.py::test_seam_a_evidence_result_kind_absent_from_landing_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_seam_a_evidence_result_kind_absent_from_landing_refuses _________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-971/test_seam_a_evidence_result_ki0')
adapters = <test_round_commit_seams.FakeAdapters object at 0x1066a8670>

    def test_seam_a_evidence_result_kind_absent_from_landing_refuses(tmp_path, adapters):
      d = _session(tmp_path, name="ev-kind-absent")
      pend = _pending(d)
      path, _env, before = _dispatch_observed_land(d, "code-reviewer", payload={"other": []})
      order_path = RR.order_prompt_path(d, pend["round"], pend["phase"],
                                         RR.storage_key("code-reviewer"), pend["attempt"])
      run_dir = _execution_run_dir(tmp_path, order_path, name="ev-kind-absent-run")
>     out = RD.cmd_record_result(d, "code-reviewer", evidence_run_dir=run_dir)

plugins/superheroes/lib/tests/test_round_commit_seams.py:796: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
plugins/superheroes/lib/round_driver.py:7246: in cmd_record_result
    return _cmd_record_result_locked(session_dir, seat=seat, attempt=attempt,
plugins/superheroes/lib/round_driver.py:7411: in _cmd_record_result_locked
    assembled, ev_reason, ev_extra = _assemble_dispatch_evidence(
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

session_dir = '/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-971/test_seam_a_evidence_result_ki0/ev-kind-absent'
envelope = {'attempt': 0, 'dispatchRef': 'feb91032a2cb2106f089a25063b8178527ae4359f5412ccf549e9d2f98f28ce9', 'envelopeSha256': '3...340faf8e95cd88da54d269dfe5', 'manifestSha256': 'feb91032a2cb2106f089a25063b8178527ae4359f5412ccf549e9d2f98f28ce9', ...}
evidence_run_dir = '/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-971/test_seam_a_evidence_result_ki0/ev-kind-absent-run'

    def _assemble_dispatch_evidence(session_dir, envelope, evidence_run_dir):
        """Bind runner telemetry to the driver's order hash. Returns (envelope, refusal_reason, extra)."""
        if not evidence_run_dir:
            return None, None, {}
        import engine_dispatch
        record, err = engine_dispatch.run_execution_record(evidence_run_dir)
        if err is not None:
            return None, "evidence-run-dir-unreadable", {"detail": err}
        prompt_sha = record.get("orderPromptSha256")
        order_sha = envelope.get("orderSha256")
        # Absent order-prompt hash cannot prove the binding — refuse rather than compare promptSha256.
        if not isinstance(prompt_sha, str) or not prompt_sha or prompt_sha != order_sha:
            return None, "evidence-order-mismatch", {"orderPromptSha256": prompt_sha,
                                                     "orderSha256": order_sha}
        result_digest = record.get("resultDigest")
        result_kind = record.get("resultKind")
        if (not isinstance(result_digest, str) or not result_digest
                or not isinstance(result_kind, str) or not result_kind):
            return None, "evidence-run-dir-unreadable", {"detail": "result-binding-incomplete"}
        envelope_payload = envelope.get("payload")
        if False and (not isinstance(envelope_payload, dict) or result_kind not in envelope_payload):
            return None, "evidence-result-mismatch", {"resultDigest": result_digest,
                                                       "resultKind": result_kind}
>       payload_digest = round_records.payload_sha256(envelope_payload[result_kind])
E       KeyError: 'findings'

plugins/superheroes/lib/round_driver.py:7279: KeyError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_commit_seams.py::test_seam_a_evidence_result_kind_absent_from_landing_refuses
1 failed in 2.30s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 2.08s
```
---

## 3 — result-binding-incomplete

**Guarded element.** `round_driver.py:7272-7274 — result digest/kind presence in runner record`.
**Axis.** Refusal when runner record lacks complete result binding.

**Neutralization** (production call site, targeted):

```python
-     if (not isinstance(result_digest, str) or not result_digest
-             or not isinstance(result_kind, str) or not result_kind):
+     if False and (not isinstance(result_digest, str) or not result_digest
+             or not isinstance(result_kind, str) or not result_kind):
```

**Test.** `plugins/superheroes/lib/tests/test_round_commit_seams.py::test_seam_a_evidence_result_binding_incomplete_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_seam_a_evidence_result_binding_incomplete_refuses ____________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-973/test_seam_a_evidence_result_bi0')
adapters = <test_round_commit_seams.FakeAdapters object at 0x10a7b67f0>

    def test_seam_a_evidence_result_binding_incomplete_refuses(tmp_path, adapters):
      d = _session(tmp_path, name="ev-binding-incomplete")
      pend = _pending(d)
      path, _env, before = _dispatch_observed_land(d, "code-reviewer")
      order_path = RR.order_prompt_path(d, pend["round"], pend["phase"],
                                         RR.storage_key("code-reviewer"), pend["attempt"])
      run_dir = _execution_run_dir(tmp_path, order_path, name="ev-binding-incomplete-run")
      open(os.path.join(run_dir, "attempt-1.stdout"), "wb").write(b"not parseable review output\n")
      out = RD.cmd_record_result(d, "code-reviewer", evidence_run_dir=run_dir)
      assert out["ok"] is False
>     assert out["reason"] == "evidence-run-dir-unreadable"
E     AssertionError: assert 'evidence-result-mismatch' == 'evidence-run-dir-unreadable'
E       
E       - evidence-run-dir-unreadable
E       + evidence-result-mismatch

plugins/superheroes/lib/tests/test_round_commit_seams.py:813: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_commit_seams.py::test_seam_a_evidence_result_binding_incomplete_refuses
1 failed in 1.88s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 1.82s
```
---

## 4 — parse-not-grade — zero-finding review still stamps

**Guarded element.** `engine_dispatch.py:4279-4285 — `_result_digest_and_kind_from_parse``.
**Axis.** Zero-finding review still yields digest and kind from parse, not grading.

**Neutralization** (production call site, targeted):

```python
-     if kind is None:
-         return None, None
-     import round_records
-     return round_records.payload_sha256(content), kind
+     if kind is None:
+         return None, None
+     if kind == "findings" and content == []:
+         return None, None
+     import round_records
+     return round_records.payload_sha256(content), kind
```

**Test.** `plugins/superheroes/lib/tests/test_round_commit_seams.py::test_seam_a_zero_finding_review_still_stamps`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_seam_a_zero_finding_review_still_stamps _________________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-975/test_seam_a_zero_finding_revie0')
adapters = <test_round_commit_seams.FakeAdapters object at 0x10acbda60>

    def test_seam_a_zero_finding_review_still_stamps(tmp_path, adapters):
      d = _session(tmp_path, name="ev-zero-findings")
      pend = _pending(d)
      path, _env, before = _dispatch_observed_land(d, "code-reviewer", payload={"findings": []})
      order_path = RR.order_prompt_path(d, pend["round"], pend["phase"],
                                         RR.storage_key("code-reviewer"), pend["attempt"])
      run_dir = _execution_run_dir(tmp_path, order_path, name="ev-zero-findings-run")
      record, err = ED.run_execution_record(run_dir)
      assert err is None
>     assert record.get("resultKind") == "findings"
E     AssertionError: assert None == 'findings'
E      +  where None = <built-in method get of dict object at 0x10acaf840>('resultKind')
E      +    where <built-in method get of dict object at 0x10acaf840> = {'observation': {'read': 'unknown', 'source': 'none', 'stdoutBytes': 16, 'telemetry': 'none', ...}, 'orderPromptSha256...957e2b5b4beb5581fcbfb4038f5f', 'recordDigest': '1130c48f64a2b20318d4bbec50f9a6d6d9b8ca818ccedf681a9fe7dad61a93e6', ...}.get

plugins/superheroes/lib/tests/test_round_commit_seams.py:827: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_commit_seams.py::test_seam_a_zero_finding_review_still_stamps
1 failed in 1.99s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 1.91s
```
---

## 5 — attempt-not-completed — refusal

**Guarded element.** `engine_dispatch.py:4209-4210 — refusal branch in `_attempt_ended_successfully``.
**Axis.** Refusal when attempt ended with engine refusal.

**Neutralization** (production call site, targeted):

```python
-     if ended.get("refusal"):
+     if False and ended.get("refusal"):
```

**Test.** `plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_execution_record_refuses_attempt_ended_by_refusal`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_run_execution_record_refuses_attempt_ended_by_refusal __________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-977/test_run_execution_record_refu0')

    def test_run_execution_record_refuses_attempt_ended_by_refusal(tmp_path):
        run_dir = str(tmp_path / "attempt-refusal")
        _manual_open_review_run(tmp_path, run_dir)
        _append_flat_attempt_ended(run_dir, refusal="engine-refused")
        record, error = ED.run_execution_record(run_dir)
        assert record is None
>       assert error == "attempt-not-completed"
E       AssertionError: assert 'runner-nonce-missing' == 'attempt-not-completed'
E         
E         - attempt-not-completed
E         + runner-nonce-missing

plugins/superheroes/lib/tests/test_engine_dispatch.py:8836: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_execution_record_refuses_attempt_ended_by_refusal
1 failed in 0.49s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.42s
```
---

## 6 — attempt-not-completed — timeout

**Guarded element.** `engine_dispatch.py:4211-4212 — timeout branch in `_attempt_ended_successfully``.
**Axis.** Refusal when attempt ended by timeout.

**Neutralization** (production call site, targeted):

```python
-     if ended.get("timedOut"):
+     if False and ended.get("timedOut"):
```

**Test.** `plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_execution_record_refuses_attempt_ended_by_timeout`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_run_execution_record_refuses_attempt_ended_by_timeout __________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-979/test_run_execution_record_refu0')

    def test_run_execution_record_refuses_attempt_ended_by_timeout(tmp_path):
        run_dir = str(tmp_path / "attempt-timeout")
        _manual_open_review_run(tmp_path, run_dir)
        _append_flat_attempt_ended(run_dir, timedOut=True)
        record, error = ED.run_execution_record(run_dir)
        assert record is None
>       assert error == "attempt-not-completed"
E       AssertionError: assert 'runner-nonce-missing' == 'attempt-not-completed'
E         
E         - attempt-not-completed
E         + runner-nonce-missing

plugins/superheroes/lib/tests/test_engine_dispatch.py:8845: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_execution_record_refuses_attempt_ended_by_timeout
1 failed in 0.49s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.41s
```
---

## 7 — attempt-not-completed — non-zero exit

**Guarded element.** `engine_dispatch.py:4213-4214 — exit-code branch in `_attempt_ended_successfully``.
**Axis.** Refusal when attempt ended with non-zero exit.

**Neutralization** (production call site, targeted):

```python
-     if ended.get("exit") not in (0, None):
+     if False and ended.get("exit") not in (0, None):
```

**Test.** `plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_execution_record_refuses_attempt_with_nonzero_exit`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_run_execution_record_refuses_attempt_with_nonzero_exit __________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-981/test_run_execution_record_refu0')

    def test_run_execution_record_refuses_attempt_with_nonzero_exit(tmp_path):
        run_dir = str(tmp_path / "attempt-nonzero-exit")
        _manual_open_review_run(tmp_path, run_dir)
        _append_flat_attempt_ended(run_dir, exit=1)
        record, error = ED.run_execution_record(run_dir)
        assert record is None
>       assert error == "attempt-not-completed"
E       AssertionError: assert 'runner-nonce-missing' == 'attempt-not-completed'
E         
E         - attempt-not-completed
E         + runner-nonce-missing

plugins/superheroes/lib/tests/test_engine_dispatch.py:8854: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_execution_record_refuses_attempt_with_nonzero_exit
1 failed in 0.49s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.41s
```
---

## 8 — observation tokens value check

**Guarded element.** `round_records.py:593-595 — tokens validation in `_validate_execution_evidence``.
**Axis.** Refusal on malformed observation tokens.

**Neutralization** (production call site, targeted):

```python
-     if tokens is not None and (not isinstance(tokens, int) or isinstance(tokens, bool) or tokens < 0):
+     if False and tokens is not None and (not isinstance(tokens, int) or isinstance(tokens, bool) or tokens < 0):
```

**Test.** `plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_tokens_value_refuses_malformed[not-int-dispatch-observed]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_v2_execution_evidence_observation_bad_tokens_value_refuses_malformed[not-int-dispatch-observed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-983/test_v2_execution_evidence_obs0')
provenance = 'dispatch-observed', bad_tokens = 'not-int'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    @pytest.mark.parametrize("bad_tokens", ["not-int", -1])
    def test_v2_execution_evidence_observation_bad_tokens_value_refuses_malformed(
            tmp_path, provenance, bad_tokens):
        sd = _session(tmp_path)
        obs = _well_formed_observation(tokens=bad_tokens)
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': '2ed76cda8afd428f4d4a7d28889d05474c7c6b8c...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:1959: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_tokens_value_refuses_malformed[not-int-dispatch-observed]
1 failed in 0.19s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.17s
```
---

## 9 — observation toolCalls value check

**Guarded element.** `round_records.py:596-599 — toolCalls validation`.
**Axis.** Refusal on malformed observation toolcalls.

**Neutralization** (production call site, targeted):

```python
-     if (tool_calls is not None
-             and (not isinstance(tool_calls, int) or isinstance(tool_calls, bool) or tool_calls < 0)):
+     if False and (tool_calls is not None
+             and (not isinstance(tool_calls, int) or isinstance(tool_calls, bool) or tool_calls < 0)):
```

**Test.** `plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_tool_calls_value_refuses_malformed[not-int-dispatch-observed]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_v2_execution_evidence_observation_bad_tool_calls_value_refuses_malformed[not-int-dispatch-observed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-985/test_v2_execution_evidence_obs0')
provenance = 'dispatch-observed', bad_tool_calls = 'not-int'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    @pytest.mark.parametrize("bad_tool_calls", ["not-int", -1])
    def test_v2_execution_evidence_observation_bad_tool_calls_value_refuses_malformed(
            tmp_path, provenance, bad_tool_calls):
        sd = _session(tmp_path)
        obs = _well_formed_observation(toolCalls=bad_tool_calls)
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': '300f18e9109928df2f26df5e71d413cba3c711e6...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:1971: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_tool_calls_value_refuses_malformed[not-int-dispatch-observed]
1 failed in 0.18s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.18s
```
---

## 10 — observation stdoutBytes value check

**Guarded element.** `round_records.py:600-602 — stdoutBytes validation`.
**Axis.** Refusal on malformed observation stdoutbytes.

**Neutralization** (production call site, targeted):

```python
-     if (not isinstance(stdout_bytes, int) or isinstance(stdout_bytes, bool) or stdout_bytes < 0):
+     if False and (not isinstance(stdout_bytes, int) or isinstance(stdout_bytes, bool) or stdout_bytes < 0):
```

**Test.** `plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_stdout_bytes_value_refuses_malformed[not-int-dispatch-observed]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_v2_execution_evidence_observation_bad_stdout_bytes_value_refuses_malformed[not-int-dispatch-observed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-987/test_v2_execution_evidence_obs0')
provenance = 'dispatch-observed', bad_stdout_bytes = 'not-int'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    @pytest.mark.parametrize("bad_stdout_bytes", ["not-int", -1])
    def test_v2_execution_evidence_observation_bad_stdout_bytes_value_refuses_malformed(
            tmp_path, provenance, bad_stdout_bytes):
        sd = _session(tmp_path)
        obs = _well_formed_observation(stdoutBytes=bad_stdout_bytes)
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': '077499b294b68bd059bb91309197cefbc83c0dd2...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:1983: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_stdout_bytes_value_refuses_malformed[not-int-dispatch-observed]
1 failed in 0.19s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.17s
```
---

## 11 — observation wallSeconds value check

**Guarded element.** `round_records.py:603-606 — wallSeconds validation`.
**Axis.** Refusal on malformed observation wallseconds.

**Neutralization** (production call site, targeted):

```python
-     if (not isinstance(wall_seconds, (int, float)) or isinstance(wall_seconds, bool)
-             or wall_seconds < 0):
+     if False and (not isinstance(wall_seconds, (int, float)) or isinstance(wall_seconds, bool)
+             or wall_seconds < 0):
```

**Test.** `plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_wall_seconds_value_refuses_malformed[not-int-dispatch-observed]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_v2_execution_evidence_observation_bad_wall_seconds_value_refuses_malformed[not-int-dispatch-observed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-989/test_v2_execution_evidence_obs0')
provenance = 'dispatch-observed', bad_wall_seconds = 'not-int'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    @pytest.mark.parametrize("bad_wall_seconds", ["not-int", -1])
    def test_v2_execution_evidence_observation_bad_wall_seconds_value_refuses_malformed(
            tmp_path, provenance, bad_wall_seconds):
        sd = _session(tmp_path)
        obs = _well_formed_observation(wallSeconds=bad_wall_seconds)
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': 'fe6d4d94f417a1632cd8619c5a5553c62cd444d7...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:1995: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_wall_seconds_value_refuses_malformed[not-int-dispatch-observed]
1 failed in 0.19s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.17s
```
---

## 12 — observation source value check

**Guarded element.** `round_records.py:607-609 — observation source validation`.
**Axis.** Refusal on malformed observation source.

**Neutralization** (production call site, targeted):

```python
-     if not isinstance(obs_source, str) or not obs_source:
+     if False and (not isinstance(obs_source, str) or not obs_source):
```

**Test.** `plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_source_value_refuses_malformed[dispatch-observed]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_v2_execution_evidence_observation_bad_source_value_refuses_malformed[dispatch-observed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-991/test_v2_execution_evidence_obs0')
provenance = 'dispatch-observed'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    def test_v2_execution_evidence_observation_bad_source_value_refuses_malformed(tmp_path, provenance):
        sd = _session(tmp_path)
        obs = _well_formed_observation(source=42)
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': 'ad09261c8863f6f5140390caa69b96083f9695e0...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:2005: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_source_value_refuses_malformed[dispatch-observed]
1 failed in 0.19s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.17s
```
---

## 13 — observation read value check

**Guarded element.** `round_records.py:610-612 — read literal validation`.
**Axis.** Refusal on observation read outside closed set.

**Neutralization** (production call site, targeted):

```python
-     if read not in EXECUTION_EVIDENCE_READ_VALUES:
+     if False and read not in EXECUTION_EVIDENCE_READ_VALUES:
```

**Test.** `plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_read_value_refuses_malformed[dispatch-observed]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_v2_execution_evidence_observation_bad_read_value_refuses_malformed[dispatch-observed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-993/test_v2_execution_evidence_obs0')
provenance = 'dispatch-observed'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    def test_v2_execution_evidence_observation_bad_read_value_refuses_malformed(tmp_path, provenance):
        sd = _session(tmp_path)
        obs = _well_formed_observation(read="maybe")
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': '1437d4dddf9af800f3a7a90274415fe03189beb7...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:2015: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_read_value_refuses_malformed[dispatch-observed]
1 failed in 0.20s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.16s
```
---

## 14 — EXECUTION_EVIDENCE_READ_VALUES drift binding

**Guarded element.** `round_records.py:79 — `EXECUTION_EVIDENCE_READ_VALUES``.
**Axis.** Execution-evidence read literals stay bound to engine_adapter engagement read.

**Neutralization** (production call site, targeted):

```python
- EXECUTION_EVIDENCE_READ_VALUES = frozenset(("engaged", "unknown"))
+ EXECUTION_EVIDENCE_READ_VALUES = frozenset(("engaged", "unknown", "drift-probe"))
```

**Test.** `plugins/superheroes/lib/tests/test_ssot_drift.py::test_execution_evidence_read_values_match_engagement_read`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_execution_evidence_read_values_match_engagement_read ___________

    def test_execution_evidence_read_values_match_engagement_read():
        """axis: execution-evidence read literals stay bound to engine_adapter.engagement_read."""
        import engine_adapter
        import round_records
    
>       assert round_records.EXECUTION_EVIDENCE_READ_VALUES == engine_adapter.ENGAGEMENT_READ_VALUES
E       AssertionError: assert frozenset({'d...', 'unknown'}) == frozenset({'e...', 'unknown'})
E         
E         Extra items in the left set:
E         'drift-probe'
E         Use -v to get more diff

plugins/superheroes/lib/tests/test_ssot_drift.py:1064: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_execution_evidence_read_values_match_engagement_read
1 failed in 0.23s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.18s
```
---

## 15 — observation bool exclusion

**Guarded element.** `round_records.py:594,601 — bool exclusion in tokens and stdoutBytes checks`.
**Axis.** Refusal when bool masquerades as integer observation field.

**Neutralization** (production call site, targeted):

```python
-     if tokens is not None and (not isinstance(tokens, int) or isinstance(tokens, bool) or tokens < 0):
-         return ("execution-evidence-malformed", {})
-     tool_calls = observation.get("toolCalls")
-     if (tool_calls is not None
-             and (not isinstance(tool_calls, int) or isinstance(tool_calls, bool) or tool_calls < 0)):
-         return ("execution-evidence-malformed", {})
-     stdout_bytes = observation.get("stdoutBytes")
-     if (not isinstance(stdout_bytes, int) or isinstance(stdout_bytes, bool) or stdout_bytes < 0):
+     if tokens is not None and (not isinstance(tokens, int) or tokens < 0):
+         return ("execution-evidence-malformed", {})
+     tool_calls = observation.get("toolCalls")
+     if (tool_calls is not None
+             and (not isinstance(tool_calls, int) or isinstance(tool_calls, bool) or tool_calls < 0)):
+         return ("execution-evidence-malformed", {})
+     stdout_bytes = observation.get("stdoutBytes")
+     if (not isinstance(stdout_bytes, int) or stdout_bytes < 0):
```

**Test.** `plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bool_is_not_an_integer_refuses_malformed[bad_obs0-dispatch-observed]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_v2_execution_evidence_observation_bool_is_not_an_integer_refuses_malformed[bad_obs0-dispatch-observed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-997/test_v2_execution_evidence_obs0')
provenance = 'dispatch-observed', bad_obs = {'tokens': True}

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    @pytest.mark.parametrize("bad_obs", [{"tokens": True}, {"stdoutBytes": False}])
    def test_v2_execution_evidence_observation_bool_is_not_an_integer_refuses_malformed(
            tmp_path, provenance, bad_obs):
        sd = _session(tmp_path)
        obs = _well_formed_observation(**bad_obs)
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': 'ba826e73871259c63d5944c0081aa8661fa778d5...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:2027: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bool_is_not_an_integer_refuses_malformed[bad_obs0-dispatch-observed]
1 failed in 0.20s
```

**Restore.** The exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.17s
```
---

## 16 — missing resultKind

**Guarded element.** `round_records.py:576-577 — resultKind presence in `_validate_execution_evidence``.
**Axis.** Refusal when `resultKind` is missing from execution evidence.

**Neutralization attempted A** (line-576 type/presence check):

```python
-     if not isinstance(result_kind, str) or not result_kind:
+     if False and (not isinstance(result_kind, str) or not result_kind):
```

**Neutralization attempted B** (required-field loop):

```python
-     for field in EXECUTION_EVIDENCE_FIELDS:
-         if field not in evidence:
+     for field in EXECUTION_EVIDENCE_FIELDS:
+         if field not in evidence and field != "resultKind":
```

**Test.** `plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_result_kind_missing_refuses_malformed[dispatch-observed]`.

**FINDING — probe stayed GREEN under single neutralization.** Missing `resultKind`
is refused by two independent production checks: the required-field loop (`round_records.py:559-561`)
and the explicit `result_kind` type/presence check (`round_records.py:576-577`). Neutralizing only
one leaves the other live, so the named test stays green.

**Raw red — loop-only neutralization (B)** (`PYTEST_EXIT=0`, finding):

```
.                                                                        [100%]
1 passed in 0.16s
```

**Raw red — line-576-only neutralization (A)** (`PYTEST_EXIT=0`, finding):

```
.                                                                        [100%]
1 passed in 0.16s
```

**Restore.** Each probe reverted by its exact inverse edit.
**Restore receipt.** `git status --porcelain`: **empty** (after each probe).

**Raw green** — post-restore, named test (`PYTEST_EXIT=0`):

```
.                                                                        [100%]
1 passed in 0.17s
```
