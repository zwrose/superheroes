# C14 layer 3b R7 bite-proofs

| ID | Guarded element | Axis | Detector |
|----|-----------------|------|----------|
| BP-R7-1 | `seat_map.py` `build()` pin effort line | a model-only one-run pin seats at its own model's effort, never the role pin's | `test_one_run_model_pin_takes_own_effort_in_build` |
| BP-R7-2 | `seat_map.py` `reachable_configs()` pin effort line | a model-only one-run pin's needed-set cell uses its own model's effort, never the role pin's | `test_one_run_model_pin_takes_own_effort_in_reachable_configs` |

## BP-R7-1

**Axis:** a model-only one-run pin seats at its own model's effort, never the role pin's.

**Neutralization:** Restored `build()` pin effort line to `effort = pin.get("effort", default_effort)`.

**Red command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/wh1273r7-pyc -m pytest "plugins/superheroes/lib/tests/test_seat_map.py::test_one_run_model_pin_takes_own_effort_in_build" -q -p no:cacheprovider`

**Red:** `AssertionError: assert 'claude' == 'codex'` in `test_one_run_model_pin_takes_own_effort_in_build` (pin fell back to rotation because role-pin effort `high` was used instead of model's `xhigh`).

**Restore:** `effort = pin["effort"] if "effort" in pin else (model_registry.resolve_dispatch(tier, pin_vendor, model, None).get("effort") if "model" in pin else default_effort)`

**Green:** `1 passed in 0.14s`.

## BP-R7-2

**Axis:** a model-only one-run pin's needed-set cell uses its own model's effort, never the role pin's.

**Neutralization:** Restored `reachable_configs()` pin effort line to `effort = pin.get("effort", default_effort)`.

**Red command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/wh1273r7-pyc -m pytest "plugins/superheroes/lib/tests/test_seat_map.py::test_one_run_model_pin_takes_own_effort_in_reachable_configs" -q -p no:cacheprovider`

**Red:** `AssertionError: assert ['gpt-5.6-sol', 'xhigh'] in [['gpt-5.6-terra', 'high'], ['gpt-6-astra', 'high']]` in `test_one_run_model_pin_takes_own_effort_in_reachable_configs`.

**Restore:** `effort = pin["effort"] if "effort" in pin else (model_registry.resolve_dispatch(tier, v, model, None).get("effort") if "model" in pin else default_effort)`

**Green:** `1 passed in 0.14s`.
