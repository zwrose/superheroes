# Bite-proof record — #1271 layer 2 (WO-L2-B receipt writer)

Contract: `rubric/bite-proof.md`. Every proof below ran with the **detector unedited**; the
neutralization was applied as a targeted, reversible edit to the **production check function**,
and reverted by its exact inverse. Probes ran in the implementer worktree at `a6f4094a`.

**Who ran these.** A dispatched implementer (WO-L2-B).

**The guarded-element set — four escape-class checks plus the driverless import boundary.**

| # | Guarded element | Axis |
|---|---|---|
| 1 | `check_unrun_review` | dispatch-observed / hand-landed engagement refuses |
| 2 | `check_same_family_seat` | same-family seat map degradation refuses |
| 3 | `check_unfetched_findings` | journal/envelope reconciliation refuses |
| 4 | `check_disposition_without_receipt` | disposition/base-guard/follow-up refuses |
| 5 | forbidden-import guard in `test_round_certification_driverless.py` | module must not import driver packages |

---

## 1 — unrun-review

**Guarded element.** `round_certification.py — check_unrun_review`.
**Axis.** Missing dispatch-observed execution telemetry refuses certification.

**Neutralization** (production call site, targeted):

```python
 def check_unrun_review(ctx):
+    return None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_bite_unrun_review_dispatch_telemetry_removed_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_bite_unrun_review_dispatch_telemetry_removed_refuses ___________
plugins/superheroes/lib/tests/test_round_certification.py:516: in test_bite_unrun_review_dispatch_telemetry_removed_refuses
    assert receipt is None
E   AssertionError: assert {'baseGuard': 'checked-stat-bound', ...} is None
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_bite_unrun_review_dispatch_telemetry_removed_refuses
1 failed in 0.07s
```

**Restore.** Removed the two neutralization lines (exact inverse).
**Restore receipt.** Restored lines:

```python
def check_unrun_review(ctx):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.06s
```

---

## 2 — same-family-seat

**Guarded element.** `round_certification.py — check_same_family_seat`.
**Axis.** Seat-map same-family degradation refuses certification.

**Neutralization** (production call site, targeted):

```python
 def check_same_family_seat(ctx):
+    return None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_bite_same_family_seat_degradation_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_bite_same_family_seat_degradation_refuses ________________
plugins/superheroes/lib/tests/test_round_certification.py:537: in test_bite_same_family_seat_degradation_refuses
    assert receipt is None
E   AssertionError: assert {'baseGuard': 'checked-stat-bound', ...} is None
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_bite_same_family_seat_degradation_refuses
1 failed in 0.07s
```

**Restore.** Exact inverse edit.
**Restore receipt.** Restored lines:

```python
def check_same_family_seat(ctx):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.06s
```

---

## 3 — unfetched-findings

**Guarded element.** `round_certification.py — check_unfetched_findings`.
**Axis.** Open journal seat / missing envelope refuses certification.

**Neutralization** (production call site, targeted):

```python
 def check_unfetched_findings(ctx):
+    return None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_bite_unfetched_findings_open_seat_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_bite_unfetched_findings_open_seat_refuses ________________
plugins/superheroes/lib/tests/test_round_certification.py:558: in test_bite_unfetched_findings_open_seat_refuses
    assert refusal["class"] == "unfetched-findings"
E   AssertionError: assert 'unrun-review' == 'unfetched-findings'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_bite_unfetched_findings_open_seat_refuses
1 failed in 0.07s
```

**Restore.** Exact inverse edit.
**Restore receipt.** Restored lines:

```python
def check_unfetched_findings(ctx):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.06s
```

---

## 4 — disposition-without-receipt

**Guarded element.** `round_certification.py — check_disposition_without_receipt`.
**Axis.** Base guard not-checked refuses certification.

**Neutralization** (production call site, targeted):

```python
 def check_disposition_without_receipt(ctx):
+    return None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_bite_disposition_without_receipt_base_guard_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_bite_disposition_without_receipt_base_guard_refuses ___________
plugins/superheroes/lib/tests/test_round_certification.py:568: in test_bite_disposition_without_receipt_base_guard_refuses
    assert receipt is None
E   AssertionError: assert {'baseGuard': 'not-checked', ...} is None
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_bite_disposition_without_receipt_base_guard_refuses
1 failed in 0.07s
```

**Restore.** Exact inverse edit.
**Restore receipt.** Restored lines:

```python
def check_disposition_without_receipt(ctx):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.06s
```

---

## 5 — driverless import boundary

**Guarded element.** `test_round_certification_driverless.py — _assert_no_forbidden_imports`.
**Axis.** Shipped writer source must not import `round_driver`, `round_phases`, or `round_records`.

**Neutralization** (in-memory poison of module source for the guard test):

```python
 import re
+import round_driver  # bite-proof probe
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_driverless.py::test_bite_added_round_driver_import_fails_forbidden_import_guard`.

**Raw red** — `PYTEST_EXIT=0` (guard test raises AssertionError internally):

```
3 passed in 1.30s
```

(The named test passes by expecting `AssertionError` from `_assert_no_forbidden_imports` on poisoned source — the guard fires without mutating the on-disk module under parallel pytest.)

**Restore.** N/A — poison was in-memory only.
**Restore receipt.** On-disk `round_certification.py` unchanged.

**Raw green** — `test_module_source_has_no_forbidden_driver_imports` (`PYTEST_EXIT=0`):

```
3 passed in 1.30s
```
