# Bite-proof record — #1271 layer 2 (WO-L2-B receipt writer)

Contract: `rubric/bite-proof.md`. Every proof below ran with the **detector unedited**; the
neutralization was applied as a targeted, reversible edit to the **production check function**,
and reverted by its exact inverse. Probes ran in the implementer worktree at `a6f4094a`.

**Who ran these.** A dispatched implementer (WO-L2-B).

**The guarded-element set — four escape-class checks plus the driverless import boundary.**

| # | Guarded element | Axis |
|---|---|---|
| 1 | `check_unrun_review` | dispatch-observed / hand-landed engagement refuses |
| 2 | `check_same_family_seat` | same-family seat map degradation refuses |
| 3 | `check_unfetched_findings` | journal/envelope reconciliation refuses |
| 4 | `check_disposition_without_receipt` | disposition/base-guard/follow-up refuses |
| 5 | forbidden-import guard in `test_round_certification_driverless.py` | module must not import driver packages |

---

## 1 — unrun-review

**Guarded element.** `round_certification.py — check_unrun_review`.
**Axis.** Missing dispatch-observed execution telemetry refuses certification.

**Neutralization** (production call site, targeted):

```python
 def check_unrun_review(ctx):
+    return None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_bite_unrun_review_dispatch_telemetry_removed_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_bite_unrun_review_dispatch_telemetry_removed_refuses ___________
plugins/superheroes/lib/tests/test_round_certification.py:516: in test_bite_unrun_review_dispatch_telemetry_removed_refuses
    assert receipt is None
E   AssertionError: assert {'baseGuard': 'checked-stat-bound', ...} is None
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_bite_unrun_review_dispatch_telemetry_removed_refuses
1 failed in 0.07s
```

**Restore.** Removed the two neutralization lines (exact inverse).
**Restore receipt.** Restored lines:

```python
def check_unrun_review(ctx):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.06s
```

---

## 2 — same-family-seat

**Guarded element.** `round_certification.py — check_same_family_seat`.
**Axis.** Seat-map same-family degradation refuses certification.

**Neutralization** (production call site, targeted):

```python
 def check_same_family_seat(ctx):
+    return None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_bite_same_family_seat_degradation_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_bite_same_family_seat_degradation_refuses ________________
plugins/superheroes/lib/tests/test_round_certification.py:537: in test_bite_same_family_seat_degradation_refuses
    assert receipt is None
E   AssertionError: assert {'baseGuard': 'checked-stat-bound', ...} is None
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_bite_same_family_seat_degradation_refuses
1 failed in 0.07s
```

**Restore.** Exact inverse edit.
**Restore receipt.** Restored lines:

```python
def check_same_family_seat(ctx):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.06s
```

---

## 3 — unfetched-findings

**Guarded element.** `round_certification.py — check_unfetched_findings`.
**Axis.** Open journal seat / missing envelope refuses certification.

**Neutralization** (production call site, targeted):

```python
 def check_unfetched_findings(ctx):
+    return None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_bite_unfetched_findings_open_seat_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_bite_unfetched_findings_open_seat_refuses ________________
plugins/superheroes/lib/tests/test_round_certification.py:558: in test_bite_unfetched_findings_open_seat_refuses
    assert refusal["class"] == "unfetched-findings"
E   AssertionError: assert 'unrun-review' == 'unfetched-findings'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_bite_unfetched_findings_open_seat_refuses
1 failed in 0.07s
```

**Restore.** Exact inverse edit.
**Restore receipt.** Restored lines:

```python
def check_unfetched_findings(ctx):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.06s
```

---

## 4 — disposition-without-receipt

**Guarded element.** `round_certification.py — check_disposition_without_receipt`.
**Axis.** Base guard not-checked refuses certification.

**Neutralization** (production call site, targeted):

```python
 def check_disposition_without_receipt(ctx):
+    return None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_bite_disposition_without_receipt_base_guard_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_bite_disposition_without_receipt_base_guard_refuses ___________
plugins/superheroes/lib/tests/test_round_certification.py:568: in test_bite_disposition_without_receipt_base_guard_refuses
    assert receipt is None
E   AssertionError: assert {'baseGuard': 'not-checked', ...} is None
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_bite_disposition_without_receipt_base_guard_refuses
1 failed in 0.07s
```

**Restore.** Exact inverse edit.
**Restore receipt.** Restored lines:

```python
def check_disposition_without_receipt(ctx):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.06s
```

---

## 5 — driverless import boundary

**Guarded element.** `test_round_certification_driverless.py — _assert_no_forbidden_imports`.
**Axis.** Shipped writer source must not import `round_driver`, `round_phases`, or `round_records`.

**Neutralization** (in-memory poison of module source for the guard test):

```python
 import re
+import round_driver  # bite-proof probe
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_driverless.py::test_bite_added_round_driver_import_fails_forbidden_import_guard`.

**Raw red** — `PYTEST_EXIT=0` (guard test raises AssertionError internally):

```
3 passed in 1.30s
```

(The named test passes by expecting `AssertionError` from `_assert_no_forbidden_imports` on poisoned source — the guard fires without mutating the on-disk module under parallel pytest.)

**Restore.** N/A — poison was in-memory only.
**Restore receipt.** On-disk `round_certification.py` unchanged.

**Raw green** — `test_module_source_has_no_forbidden_driver_imports` (`PYTEST_EXIT=0`):

```
3 passed in 1.30s
```

---

## 6 — subprocess driverless boundary (WO-L2-C rework)

**Guarded element.** `test_round_certification_driverless.py — test_certify_subprocess_with_forbidden_modules_unimportable`.
**Axis.** Any import of a forbidden module by the writer — in any syntactic form — makes certify fail to run in a subprocess whose `sys.meta_path` blocks `round_driver`, `round_phases`, and `round_records`.

### 6a — literal `import round_driver`

**Neutralization** (production module, targeted):

```python
 import re
+import round_driver
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_driverless.py::test_certify_subprocess_with_forbidden_modules_unimportable`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_certify_subprocess_with_forbidden_modules_unimportable __________
plugins/superheroes/lib/tests/test_round_certification_driverless.py:76: in test_certify_subprocess_with_forbidden_modules_unimportable
    assert result.returncode == 0, result.stdout + result.stderr
E   AssertionError: Traceback (most recent call last):
E           File "<string>", line 22, in <module>
E           File "/private/tmp/wh1271L2-B/plugins/superheroes/lib/round_certification.py", line 9, in <module>
E             import round_driver
E           File "<string>", line 16, in load_module
E         ImportError: forbidden import blocked: round_driver
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_driverless.py::test_certify_subprocess_with_forbidden_modules_unimportable
1 failed in 1.99s
```

**Restore.** Removed `import round_driver` line (exact inverse).
**Restore receipt.** Restored lines:

```python
import re

import model_registry
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 1.82s
```

**`git status --porcelain` after restore** (expected dirty — WO-L2-C work in progress):

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification_drift.py
 M plugins/superheroes/lib/tests/test_round_certification_driverless.py
```

### 6b — runtime `importlib.import_module("round_driver")`

**Neutralization** (production module, targeted):

```python
+import importlib
 ...
 import model_registry
+
+importlib.import_module("round_driver")
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_driverless.py::test_certify_subprocess_with_forbidden_modules_unimportable`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_certify_subprocess_with_forbidden_modules_unimportable __________
plugins/superheroes/lib/tests/test_round_certification_driverless.py:76: in test_certify_subprocess_with_forbidden_modules_unimportable
    assert result.returncode == 0, result.stdout + result.stderr
E   AssertionError: Traceback (most recent call last):
E           File "<string>", line 22, in <module>
E           File "/private/tmp/wh1271L2-B/plugins/superheroes/lib/round_certification.py", line 13, in <module>
E             importlib.import_module("round_driver")
E         ImportError: forbidden import blocked: round_driver
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_driverless.py::test_certify_subprocess_with_forbidden_modules_unimportable
1 failed in 2.00s
```

**Restore.** Removed `import importlib` and `importlib.import_module("round_driver")` lines (exact inverse).
**Restore receipt.** Restored lines:

```python
import hashlib
import json
import os
import re

import model_registry
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.89s
```

**`git status --porcelain` after restore** (expected dirty — WO-L2-C work in progress):

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification_drift.py
 M plugins/superheroes/lib/tests/test_round_certification_driverless.py
```

---

## 7 — WO-L2-E: declared same-family collection (fail-closed)

**Guarded element.** `round_certification.py — _declared_same_family_seats`.
**Axis.** Upstream `constraint: same-family` degradations are collected unconditionally; registry comparison may not gate them.

**Neutralization** (production collection path, targeted — re-add registry filter):

```python
 def _declared_same_family_seats(state):
+    author = _author_family(state)
     seats = []
+    seat_configs = {}
     for entry in _seat_map_receipts(state):
         smap = entry["map"]
+        raw_seats = smap.get("seats")
+        if isinstance(raw_seats, dict):
+            seat_configs.update(raw_seats)
         degradations = smap.get("degradations")
         ...
+            if _seat_family(seat, seat_configs.get(seat)) != author:
+                continue
             seats.append(seat)
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_same_family_declared_degradation_refuses[False]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_____________ test_same_family_declared_degradation_refuses[False] _____________
plugins/superheroes/lib/tests/test_round_certification.py:340: in test_same_family_declared_degradation_refuses
    assert refusal["class"] == "same-family-seat"
E   TypeError: 'NoneType' object is not subscriptable
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_same_family_declared_degradation_refuses[False]
1 failed in 0.14s
```

**Restore.** Removed the `author`/`seat_configs` setup and the `_seat_family(...) != author` filter (exact inverse).
**Restore receipt.** Restored lines:

```python
def _declared_same_family_seats(state):
    """Seats named by upstream same-family degradations — collected unconditionally."""
    seats = []
    for entry in _seat_map_receipts(state):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.13s
```

**`git status --porcelain` after restore** (expected dirty — WO-L2-E work in progress):

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification.py
```

---

## 8 — WO-L2-E: unresolvable maker-family refusal (fail-closed)

**Guarded element.** `round_certification.py — check_same_family_seat` (unresolvable `_author_family` branch).
**Axis.** An unresolvable maker family with declared same-family degradations refuses with `unfetched-findings`, not a silent pass.

**Neutralization** (production check, targeted — early `return None`):

```python
     if not author:
+        return None
         if declared:
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_same_family_unresolvable_maker_family_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
______________ test_same_family_unresolvable_maker_family_refuses ______________
plugins/superheroes/lib/tests/test_round_certification.py:399: in test_same_family_unresolvable_maker_family_refuses
    assert refusal["class"] == "unfetched-findings"
E   TypeError: 'NoneType' object is not subscriptable
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_same_family_unresolvable_maker_family_refuses
1 failed in 0.16s
```

**Restore.** Removed the early `return None` line (exact inverse).
**Restore receipt.** Restored lines:

```python
    if not author:
        if declared:
            cfg = state.get("config") or {}
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.14s
```

**`git status --porcelain` after restore** (expected dirty — WO-L2-E work in progress):

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification.py
```

---

## 9 — WO-L2-G: driver receipt parity (`verifyPasses` always-emit)

**Guarded element.** `round_certification.py — _build_receipt_rounds` (`verifyPasses` emission).
**Axis.** Every round entry on a certified receipt must carry `verifyPasses` when the form gate permits it; absence breaks field-for-field parity with `round_driver.build_receipt`.

**Neutralization** (production round builder, targeted):

```python
        if rec.get("lensCoverage") is not None:
            rd["lensCoverage"] = rec.get("lensCoverage")
-        if _round_entry_key_allowed("verifyPasses", form, state):
+        if False and _round_entry_key_allowed("verifyPasses", form, state):
            verify_passes = rec.get("verifyPasses")
            rd["verifyPasses"] = verify_passes if isinstance(verify_passes, list) else []
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_parity.py::test_certification_receipt_matches_driver_today_fields[converged-single-round]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_certification_receipt_matches_driver_today_fields[converged-single-round] _
plugins/superheroes/lib/tests/test_round_certification_parity.py:39: in test_certification_receipt_matches_driver_today_fields
    _assert_receipt_parity(session_dir)
plugins/superheroes/lib/tests/test_round_certification_parity.py:29: in _assert_receipt_parity
    assert cert_receipt[key] == driver_receipt[key], "mismatch on key %r" % key
E   AssertionError: mismatch on key 'rounds'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_parity.py::test_certification_receipt_matches_driver_today_fields[converged-single-round]
1 failed in 0.76s
```

**Restore.** Replaced `if False and _round_entry_key_allowed` with `if _round_entry_key_allowed` (exact inverse).
**Restore receipt.** Restored lines:

```python
        if _round_entry_key_allowed("verifyPasses", form, state):
            verify_passes = rec.get("verifyPasses")
            rd["verifyPasses"] = verify_passes if isinstance(verify_passes, list) else []
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.88s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/tests/round_certification_fixtures.py
?? plugins/superheroes/lib/tests/test_round_certification_parity.py
 M plugins/superheroes/lib/tests/bite_proofs/wo_l2_b_1271.md
```

---

## 9 — WO-L2-G: driver receipt parity (`verifyPasses` always-emit)

**Guarded element.** `round_certification.py — _build_receipt_rounds` (`verifyPasses` emission).
**Axis.** Every round entry on a certified receipt must carry `verifyPasses` when the form gate permits it; absence breaks field-for-field parity with `round_driver.build_receipt`.

**Neutralization** (production round builder, targeted):

```python
        if rec.get("lensCoverage") is not None:
            rd["lensCoverage"] = rec.get("lensCoverage")
-        if _round_entry_key_allowed("verifyPasses", form, state):
+        if False and _round_entry_key_allowed("verifyPasses", form, state):
            verify_passes = rec.get("verifyPasses")
            rd["verifyPasses"] = verify_passes if isinstance(verify_passes, list) else []
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_parity.py::test_certification_receipt_matches_driver_today_fields[converged-single-round]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_certification_receipt_matches_driver_today_fields[converged-single-round] _
plugins/superheroes/lib/tests/test_round_certification_parity.py:39: in test_certification_receipt_matches_driver_today_fields
    _assert_receipt_parity(session_dir)
plugins/superheroes/lib/tests/test_round_certification_parity.py:29: in _assert_receipt_parity
    assert cert_receipt[key] == driver_receipt[key], "mismatch on key %r" % key
E   AssertionError: mismatch on key 'rounds'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_parity.py::test_certification_receipt_matches_driver_today_fields[converged-single-round]
1 failed in 0.76s
```

**Restore.** Replaced `if False and _round_entry_key_allowed` with `if _round_entry_key_allowed` (exact inverse).
**Restore receipt.** Restored lines:

```python
        if _round_entry_key_allowed("verifyPasses", form, state):
            verify_passes = rec.get("verifyPasses")
            rd["verifyPasses"] = verify_passes if isinstance(verify_passes, list) else []
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.88s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/tests/round_certification_fixtures.py
?? plugins/superheroes/lib/tests/test_round_certification_parity.py
 M plugins/superheroes/lib/tests/bite_proofs/wo_l2_b_1271.md
```

---

## 10 — WO-L2-H: parity exception guard (exactly-one-member)

**Guarded element.** `test_round_certification_parity.py — assert len(PARITY_FIELD_EXCEPTIONS) == 1`.
**Axis.** A second field quietly added to the exception set erodes parity silently; the guard must fail closed.

**Neutralization** (test constant, targeted):

```python
-PARITY_FIELD_EXCEPTIONS = frozenset({"certificationShape"})
+PARITY_FIELD_EXCEPTIONS = frozenset({"certificationShape", "verdict"})
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_parity.py::test_certification_receipt_matches_driver_today_fields[converged-single-round]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_certification_receipt_matches_driver_today_fields[converged-single-round] _
plugins/superheroes/lib/tests/test_round_certification_parity.py:126: in test_certification_receipt_matches_driver_today_fields
    _assert_receipt_parity(session_dir)
plugins/superheroes/lib/tests/test_round_certification_parity.py:32: in _assert_receipt_parity
    assert len(PARITY_FIELD_EXCEPTIONS) == 1
E   AssertionError: assert 2 == 1
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_parity.py::test_certification_receipt_matches_driver_today_fields[converged-single-round]
9 failed in 0.57s
```

**Restore.** Removed `"verdict"` from the frozenset (exact inverse).
**Restore receipt.** Restored lines:

```python
PARITY_FIELD_EXCEPTIONS = frozenset({"certificationShape"})
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.51s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_round_certification_parity.py
```

---

## 11 — WO-L2-H: materialized state round-trip (`rounds` field)

**Guarded element.** `round_driver.py — _materialize_run_loop_session` (`state_copy["rounds"]` persistence).
**Axis.** Dropping `rounds` from the materialized loop-state breaks the round-trip chokepoint on the `rounds` key.

**Neutralization** (production materialization path, targeted):

```python
+    state_copy.pop("rounds", None)  # bite neutralized
     save_state(session_dir, state_copy)
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_parity.py::test_materialized_state_round_trip_matches_driver_receipt[converged-single-round]`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_materialized_state_round_trip_matches_driver_receipt[converged-single-round] _
plugins/superheroes/lib/tests/test_round_certification_parity.py:146: in test_materialized_state_round_trip_matches_driver_receipt
    assert cert_receipt[key] == driver_receipt[key], "mismatch on key %r" % key
E   AssertionError: mismatch on key 'rounds'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_parity.py::test_materialized_state_round_trip_matches_driver_receipt[converged-single-round]
1 failed in 0.56s
```

**Restore.** Removed the `state_copy.pop("rounds", None)` line (exact inverse).
**Restore receipt.** Restored lines:

```python
    save_state(session_dir, state_copy)
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.46s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_round_certification_parity.py
```

---

## 12 — WO-L2-I: producer meta cannot bypass escape-class checks

**Guarded element.** `round_certification.py — certify` (the four-check loop).
**Axis.** `meta.json` producer labels must not skip any escape-class check.

**Neutralization** (production call site, targeted — re-add producer gate):

```python
     meta = ctx.get("meta") or {}
     if meta.get("producer") != "run-loop":
         for check in (
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_meta_producer_cannot_bypass_disposition_without_receipt`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_meta_producer_cannot_bypass_disposition_without_receipt _________
plugins/superheroes/lib/tests/test_round_certification.py:844: in test_meta_producer_cannot_bypass_disposition_without_receipt
    assert receipt is None
E   AssertionError: assert {'baseGuard': 'not-checked', 'certification': {'base': 'fetched', 'fullPanel': True, 'independence': 'independent', 'p...tificationShape': 'full-panel-confirmed', 'decisions': [{'detail': 'certified', 'kind': 'converged', 'round': 1}], ...} is None
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_meta_producer_cannot_bypass_disposition_without_receipt
1 failed in 0.09s
```

**Restore.** Removed the `meta` gate and un-indented the check loop (exact inverse).
**Restore receipt.** Restored lines:

```python
    for check in (
        check_unfetched_findings,
        check_unrun_review,
        check_same_family_seat,
        check_disposition_without_receipt,
    ):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.16s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_round_certification.py
```

---

## 13 — WO-L2-I: base guard recorded in state, not implied by a label

**Guarded element.** `round_driver.py — _materialize_run_loop_session` (`baseGuard` re-application).
**Axis.** Checked base guard is written into materialized `loop-state.json` only when the in-memory config already recorded `BASE_GUARD_CHECKED`; a producer label cannot substitute.

**Neutralization** (production materialization path, targeted — drop re-application):

```python
     source_guard = (state.get("config") or {}).get("baseGuard")
     cfg.pop("baseGuard", None)
-    if source_guard == BASE_GUARD_CHECKED:
-        cfg["baseGuard"] = BASE_GUARD_CHECKED
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification.py::test_materialized_session_preserves_checked_base_guard`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_materialized_session_preserves_checked_base_guard ____________
plugins/superheroes/lib/tests/test_round_certification.py:892: in test_materialized_session_preserves_checked_base_guard
    assert materialized_state["config"]["baseGuard"] == RC.BASE_GUARD_CHECKED
E   KeyError: 'baseGuard'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification.py::test_materialized_session_preserves_checked_base_guard
1 failed in 0.16s
```

**Restore.** Re-added the `if source_guard == BASE_GUARD_CHECKED` assignment block (exact inverse).
**Restore receipt.** Restored lines:

```python
    if source_guard == BASE_GUARD_CHECKED:
        cfg["baseGuard"] = BASE_GUARD_CHECKED
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.14s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_round_certification.py
```

---

## 14 — WO-L2-K: Critical out-of-scope refusal (six-case suite)

**Guarded element.** `round_certification.py — check_disposition_without_receipt` (Critical `out-of-scope` branch).
**Axis.** A Critical finding carried out of scope must refuse with `disposition-without-receipt`.

**Neutralization** (production check, targeted):

```python
        elif disposition == "out-of-scope":
-            if _severity_rank(severity) == _severity_rank("Critical"):
+            if False:  # bite neutralized — Critical out-of-scope refusal
                return _refusal(
                    "disposition-without-receipt",
                    fid,
                    "Critical finding may not take out-of-scope disposition",
                )
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_six_cases.py::test_case_5_critical_out_of_scope_refuses`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
__________________ test_case_5_critical_out_of_scope_refuses ___________________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-1157/test_case_5_critical_out_of_sc0')

    def test_case_5_critical_out_of_scope_refuses(tmp_path):
        session_dir = case05_critical_out_of_scope(tmp_path)
        receipt, refusal = _certify(session_dir)
>       assert receipt is None
E       AssertionError: assert {'baseGuard': 'checked-stat-bound', 'certification': {'base': 'fetched', 'fullPanel': True, 'independence': 'independe...tificationShape': 'full-panel-confirmed', 'decisions': [{'detail': 'certified', 'kind': 'converged', 'round': 1}], ...} is None

plugins/superheroes/lib/tests/test_round_certification_six_cases.py:88: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_six_cases.py::test_case_5_critical_out_of_scope_refuses
1 failed in 0.08s
```

**Restore.** Replaced `if False:` with `if _severity_rank(severity) == _severity_rank("Critical"):` (exact inverse).
**Restore receipt.** Restored lines:

```python
            if _severity_rank(severity) == _severity_rank("Critical"):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.07s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/tests/round_certification_fixtures.py
?? plugins/superheroes/lib/tests/test_round_certification_six_cases.py
```

---

## 15 — WO-L2-K: hand-landed forces audited-chain (six-case suite)

**Guarded element.** `round_certification.py — _certification_shape` (hand-landed override block).
**Axis.** Any hand-landed seat forces `audited-chain`, never `full-panel-confirmed`.

**Neutralization** (production shape helper, targeted):

```python
    hand_landed = any(s.get("provenance") == PROVENANCE_HAND_LANDED for s in seats)
-    if hand_landed:
+    if False and hand_landed:  # bite neutralized — hand-landed audited-chain override
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_six_cases.py::test_case_6_mixed_panel_certifies_audited_chain` (same axis also guards `test_specimen_must_certify_sixteen_seat_out_of_manifest_audit`).

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_case_6_mixed_panel_certifies_audited_chain ________________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-1159/test_case_6_mixed_panel_certif0')

    def test_case_6_mixed_panel_certifies_audited_chain(tmp_path):
        session_dir = case06_mixed_panel(tmp_path)
        receipt, refusal = _certify(session_dir)
        assert refusal is None, refusal
        assert receipt is not None
>       assert receipt["certificationShape"] == "audited-chain"
E       AssertionError: assert 'full-panel-confirmed' == 'audited-chain'
E         
E         - audited-chain
E         + full-panel-confirmed

plugins/superheroes/lib/tests/test_round_certification_six_cases.py:108: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_six_cases.py::test_case_6_mixed_panel_certifies_audited_chain
1 failed in 0.08s
```

**Restore.** Replaced `if False and hand_landed:` with `if hand_landed:` (exact inverse).
**Restore receipt.** Restored lines:

```python
    if hand_landed:
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.07s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/tests/round_certification_fixtures.py
?? plugins/superheroes/lib/tests/test_round_certification_six_cases.py
```

---

## WO-L2-L — content-at-head (hole 1)

**Guarded element.** `round_certification.py — _fix_still_present_at_head`.
**Axis.** A reverted fix on the certified head refuses `disposition-without-receipt`.

**Neutralization** (production call site, targeted):

```python
 def _fix_still_present_at_head(ctx, finding, receipt):
+    return None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_six_cases.py::test_case_3_reverted_fix_refuses_disposition_without_receipt`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_case_3_reverted_fix_refuses_disposition_without_receipt _________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-1171/test_case_3_reverted_fix_refus0')

    def test_case_3_reverted_fix_refuses_disposition_without_receipt(tmp_path):
        session_dir = case03_reverted_fix(tmp_path)
        receipt, refusal = _certify(session_dir)
>       assert receipt is None
E       AssertionError: assert {'baseGuard': 'checked-stat-bound', 'certification': {'base': 'fetched', 'fullPanel': True, 'independence': 'independe...tificationShape': 'full-panel-confirmed', 'decisions': [{'detail': 'certified', 'kind': 'converged', 'round': 1}], ...} is None

plugins/superheroes/lib/tests/test_round_certification_six_cases.py:69: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_six_cases.py::test_case_3_reverted_fix_refuses_disposition_without_receipt
1 failed in 0.08s
```

**Restore.** Removed the two neutralization lines (exact inverse).
**Restore receipt.** Restored lines:

```python
def _fix_still_present_at_head(ctx, finding, receipt):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.07s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification_drift.py
```

---

## WO-L2-L — runner-origin binding (hole 2)

**Guarded element.** `round_certification.py — _execution_binding_matches_journal`.
**Axis.** Caller-supplied execution evidence refuses `unrun-review`.

**Neutralization** (production call site, targeted):

```python
 def _execution_binding_matches_journal(evidence, journal_binding, recorded_nonces):
+    return True, None  # bite neutralized
+    _orig = 1
```

**Test.** `plugins/superheroes/lib/tests/test_round_certification_six_cases.py::test_specimen_refuse_caller_supplied_execution_evidence`.

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_specimen_refuse_caller_supplied_execution_evidence ____________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-1173/test_specimen_refuse_caller_su0')

    def test_specimen_refuse_caller_supplied_execution_evidence(tmp_path):
        session_dir = specimen_refuse_caller_supplied_execution_evidence(tmp_path)
        receipt, refusal = _certify(session_dir)
>       assert receipt is None
E       AssertionError: assert {'baseGuard': 'checked-stat-bound', 'certification': {'base': 'fetched', 'fullPanel': True, 'independence': 'independe...}, 'certificationShape': 'audited-chain', 'decisions': [{'detail': 'certified', 'kind': 'converged', 'round': 1}], ...} is None

plugins/superheroes/lib/tests/test_round_certification_six_cases.py:155: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_certification_six_cases.py::test_specimen_refuse_caller_supplied_execution_evidence
1 failed in 0.08s
```

**Restore.** Removed the two neutralization lines (exact inverse).
**Restore receipt.** Restored lines:

```python
def _execution_binding_matches_journal(evidence, journal_binding, recorded_nonces):
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.07s
```

**`git status --porcelain` after restore**:

```
 M plugins/superheroes/lib/round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification.py
 M plugins/superheroes/lib/tests/test_round_certification_drift.py
```
