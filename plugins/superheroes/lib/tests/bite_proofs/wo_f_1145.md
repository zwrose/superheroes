# WO-F (#1145) bite-proofs — narrowed echo rule for prompt-quoting findings

## BP1 — prose quoting sentinel survives (contains-anywhere regression)

**Guarded element:** `test_dispatch_genuine_finding_quoting_the_prompt_survives` — axis: prompt-quoting finding body survives parse when sentinel is embedded in prose, not a placeholder field value.

**Neutralization:** reverted `member_carries_sentinel` to substring `EXAMPLE_SENTINEL in value` walk (old contains-anywhere rule).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_engine_dispatch.py::test_dispatch_genuine_finding_quoting_the_prompt_survives' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_dispatch_genuine_finding_quoting_the_prompt_survives
>       assert marker in body
E       AssertionError: assert 'PROMPT_TAIL_MARKER_668' in 'context:\n'
1 failed in 1.05s
```

**Restore:** reinstated placeholder field-value equality rule with `_placeholder_field_value_count` and `_member_has_non_placeholder_substance`.

**Restore receipt:**
```
    placeholders = example_member_values()
    placeholder_count = _placeholder_field_value_count(member, placeholders)
    if placeholder_count >= 2:
        return True
    if placeholder_count == 1 and not _member_has_non_placeholder_substance(
        member, placeholders
    ):
        return True
    return False
```

**Green run:**
```
1 passed in 0.81s
```

---

## BP2 — verbatim echo and near-copy still refused (always-False regression)

**Guarded element:** `test_member_carries_sentinel_true_for_example_member` and `test_member_carries_sentinel_true_for_near_copy` — axis: two-or-more exact placeholder field values (and lone placeholder with no other substance) refuse hollow echo.

**Neutralization:** replaced `member_carries_sentinel` body with unconditional `return False`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_example_member' 'plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_near_copy' -q
```

**Red run:**
```
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_example_member
>       assert RFS.member_carries_sentinel(example) is True
E       AssertionError: assert False is True
FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_member_carries_sentinel_true_for_near_copy
>       assert RFS.member_carries_sentinel(near_copy) is True
E       AssertionError: assert False is True
2 failed in 0.14s
```

**Restore:** reinstated placeholder field-value equality rule (same block as BP1 restore receipt).

**Restore receipt:** same as BP1 restore receipt.

**Green run:**
```
2 passed in 0.12s
```

**Post-restore `git status --porcelain`:**
```
 M plugins/superheroes/lib/review_findings_schema.py
 M plugins/superheroes/lib/tests/test_engine_dispatch.py
 M plugins/superheroes/lib/tests/test_payload_contracts_layering.py
 M plugins/superheroes/lib/tests/test_review_findings_schema.py
```
