# WO-G (#1185) bite-proofs — no-pinned-symbol comparison/binding census, embedded string literals

Baseline (budget invocation 1):

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_state_version_spelling_census.py -q --tb=line
..............                                                           [100%]
14 passed in 3.20s
```

New tests green (budget invocation 2):

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_no_pinned_symbol_module \
  plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_embedded_string_literal \
  -q --tb=line
..                                                                       [100%]
2 passed in 0.13s
```

---

## BP-G1 — comparison and binding legs run on modules with no pinned-symbol reference

**Guarded element:** `census_module` comparison/binding legs — axis: a module that hand-spells the version WITHOUT naming a pinned symbol is still reported.

**Neutralization:** restored the `refs_pinned` gate so `_scan_comparisons` and `_scan_bindings` run only when `_module_references_pinned_symbols` is true in `plugins/superheroes/lib/tests/test_state_version_spelling_census.py` `census_module` (wrapped both `findings.extend(_scan_comparisons(...))` and `findings.extend(_scan_bindings(...))` in `if refs_pinned:`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_no_pinned_symbol_module \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-1a9031ea062d9ea1/plugins/superheroes/lib/tests/test_state_version_spelling_census.py:654: AssertionError: comparison leg must run on a module with no pinned-symbol reference
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_no_pinned_symbol_module
1 failed in 0.11s
```

**Restore:** removed the `if refs_pinned:` wrapper around `_scan_comparisons` and `_scan_bindings`; both legs again run unconditionally after the mod-format gate.

**Restore receipt:**
```
    if refs_pinned:
        findings.extend(_scan_mod_format(tree, source, relpath))
    findings.extend(_scan_comparisons(tree, source, relpath))
    findings.extend(_scan_bindings(tree, source, relpath))
    if refs_pinned:
```

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.11s
```

---

## BP-G2 — string-literal leg searches instead of fullmatching

**Guarded element:** `_scan_string_literals` / `_RECEIPT_CERTIFIED_LITERAL_RE` — axis: a `receipt-certified/<digits>` embedded inside a longer string is reported.

**Neutralization:** changed `if _RECEIPT_CERTIFIED_LITERAL_RE.search(node.value):` to `if _RECEIPT_CERTIFIED_LITERAL_RE.fullmatch(node.value):` in `_scan_string_literals` at `plugins/superheroes/lib/tests/test_state_version_spelling_census.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_embedded_string_literal \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-1a9031ea062d9ea1/plugins/superheroes/lib/tests/test_state_version_spelling_census.py:675: AssertionError: string-literal leg must find an embedded `receipt-certified/<n>`, not only a whole-string match
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_state_version_spelling_census.py::test_synthetic_injection_embedded_string_literal
1 failed in 0.12s
```

**Restore:** changed `_RECEIPT_CERTIFIED_LITERAL_RE.fullmatch(node.value)` back to `_RECEIPT_CERTIFIED_LITERAL_RE.search(node.value)`.

**Restore receipt:** `if _RECEIPT_CERTIFIED_LITERAL_RE.search(node.value):`

**Green run (exit 0):**
```
.                                                                        [100%]
1 passed in 0.13s
```

---

Final whole-file run (budget invocation 7, all probes reverted):

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_state_version_spelling_census.py -q --tb=line
..............                                                           [100%]
14 passed in 3.10s
```

Post-restore `git status --porcelain` (only the three deliverables differ from HEAD):

```
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_state_version_spelling_census.py
?? plugins/superheroes/lib/tests/bite_proofs/wo_g_1185.md
```
