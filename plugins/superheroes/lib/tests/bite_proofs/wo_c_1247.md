# WO-C (#1247) bite-proof — canary outcome census

**Provenance:** cursor-agent / composer-2.5

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-C1 | `test_leg1_member_set_derived` | a member absent from `ALL_OUTCOMES` is caught | `test_leg1_member_set_derived` |
| BP-C2 | `test_leg2_consumer_set_derived` | an unregistered consumer is caught | `test_leg2_consumer_set_derived` |
| BP-C3 | `test_leg3_literal_ban_clean` | a token spelled outside its home is caught | `test_leg3_literal_ban_clean` |
| BP-C4 | `test_leg4_non_pass_fail_closed_cross_product` | a consumer that passes a non-pass member is caught | `test_leg4_non_pass_fail_closed_cross_product` |
| BP-C5 | `test_leg5_vocabulary_sweep_docs` | a member the docs never name is caught | `test_leg5_vocabulary_sweep_docs` |

---

## BP-C1 — derived member set

- **axis:** a member absent from `ALL_OUTCOMES` is caught

**neutralization:**
```python
OUTCOME_THROWAWAY = "throwaway"  # bite-proof neutralization — not in ALL_OUTCOMES
```
(inserted after `OUTCOME_PLANT_UNDETECTED` in `canary_outcome.py`)

**raw red** (exit 1):
```
F...........................................                             [100%]
=================================== FAILURES ===================================
_________________________ test_leg1_member_set_derived _________________________
...
E       AssertionError: ALL_OUTCOMES must equal every OUTCOME_* constant plus re-exported dispatch reasons; derived=['forfeit-with-engaged-artifact', 'forfeited', 'not-engaged', 'ok', 'plant-undetected', 'throwaway', 'unrunnable', 'vacuous'] declared=['forfeit-with-engaged-artifact', 'forfeited', 'not-engaged', 'ok', 'plant-undetected', 'unrunnable', 'vacuous']
...
FAILED plugins/superheroes/lib/tests/test_canary_outcome_census.py::test_leg1_member_set_derived
1 failed, 43 passed in 3.64s
```

**restore:** remove `OUTCOME_THROWAWAY` line.

**restore receipt:**
```
 M plugins/superheroes/lib/canary_outcome.py
```

**raw green:**
```
............................................                             [100%]
44 passed in 2.44s
```

---

## BP-C2 — derived consumer set

- **axis:** an unregistered consumer is caught

**neutralization:**
```python
import canary_outcome  # bite-proof neutralization — unregistered consumer
```
(inserted after `sys.path` setup in `engine_adapter.py`)

**raw red** (exit 1):
```
...
E       AssertionError: CONSUMERS must equal every module that imports canary_outcome; derived=['plugins/superheroes/eval/review_loop_runner.py', 'plugins/superheroes/lib/engine_adapter.py', 'plugins/superheroes/lib/round_driver.py', 'plugins/superheroes/lib/seat_canary.py'] declared=['plugins/superheroes/eval/review_loop_runner.py', 'plugins/superheroes/lib/round_driver.py', 'plugins/superheroes/lib/seat_canary.py']
...
FAILED plugins/superheroes/lib/tests/test_canary_outcome_census.py::test_leg2_consumer_set_derived
1 failed, 43 passed in 3.45s
```

**restore:** remove the `import canary_outcome` line from `engine_adapter.py`.

**restore receipt:** (no change — neutralization was reverted before status)

**raw green:**
```
............................................                             [100%]
44 passed in 2.44s
```

---

## BP-C3 — literal ban

- **axis:** a token spelled outside its home is caught

**neutralization:**
```python
_BP_LITERAL = "plant-undetected"  # bite-proof neutralization
```
(inserted after imports in `seat_canary.py` — a consumer module now in the widened census population)

**raw red** (exit 1):
```
..F.........................................                             [100%]
=================================== FAILURES ===================================
_________________________ test_leg3_literal_ban_clean __________________________
...
E       AssertionError: INVARIANT: canary outcome tokens live only in canary_outcome.py; violations:
E           seat_canary.py:20: literal 'plant-undetected' (clause: canary outcome tokens live only in canary_outcome.py)
...
FAILED plugins/superheroes/lib/tests/test_canary_outcome_census.py::test_leg3_literal_ban_clean
1 failed, 43 passed in 1.77s
```

**restore:** remove `_BP_LITERAL` line from `seat_canary.py`.

**restore receipt:**
```python
import review_findings_schema  # noqa: E402

PLANT_MARKER = "check_receipt"
```
(`_BP_LITERAL` line removed — inverse edit)

**raw green:**
```
............................................                             [100%]
44 passed in 2.65s
```

---

## BP-C4 — cross-product fail-closed

- **axis:** a consumer that passes a non-pass member is caught

**neutralization:**
```python
            st = "proven"  # bite-proof neutralization — plant-undetected treated as pass
```
(replaces `st = canary_outcome.OUTCOME_PLANT_UNDETECTED` in `canary_liveness` plant_undetected branch)

**raw red** (exit 1):
```
...
E           AssertionError: plant-undetected
E           assert 'proven' != 'proven'
...
FAILED plugins/superheroes/lib/tests/test_canary_outcome_census.py::test_leg4_non_pass_fail_closed_cross_product
1 failed, 43 passed in 3.46s
```

**restore:** reinstate `st = canary_outcome.OUTCOME_PLANT_UNDETECTED`.

**restore receipt:**
```
 M plugins/superheroes/lib/round_driver.py
```

**raw green:**
```
............................................                             [100%]
44 passed in 2.44s
```

---

## BP-C5 — vocabulary sweep

- **axis:** a member the docs never name is caught

**neutralization:** removed `` `plant-undetected` `` from the Outcome axis bullet in `auto-fix-loop.md` (left `not-engaged` intact).

**raw red** (exit 1):
```
...
E       AssertionError: auto-fix-loop.md missing canary-native outcome member(s): plant-undetected
...
FAILED plugins/superheroes/lib/tests/test_canary_outcome_census.py::test_leg5_vocabulary_sweep_docs
1 failed, 43 passed in 3.13s
```

**restore:** reinstate `` `plant-undetected` `` in the Outcome axis bullet.

**restore receipt:**
```
 M plugins/superheroes/skills/review-code/reference/auto-fix-loop.md
```

**raw green:**
```
............................................                             [100%]
44 passed in 2.44s
```
