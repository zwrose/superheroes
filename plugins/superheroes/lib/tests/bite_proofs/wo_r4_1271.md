# Bite-proof record — #1271 WO-R4 (stamping-chokepoint `investigated` census birth duties)

Contract: `rubric/bite-proof.md`. Proofs ran with the **detector unedited**; neutralizations were
targeted, reversible edits to `engine_dispatch.py` or the census assertion in
`test_engine_dispatch.py`, reverted by exact inverse before the next proof or green half.

**The guarded-element set — three elements:**

| # | Guarded element | Axis |
|---|---|---|
| C1 | `test_engagement_with_read_signature_has_no_investigated_parameter` | `_engagement_with_read` declares no `investigated` parameter |
| C2 | `test_engagement_with_read_call_sites_pass_no_investigated_keyword` | no call site passes `investigated=` |
| C3 | `test_engagement_with_read_call_sites_pass_no_investigated_keyword` (floor) | call-site count is measured, not vacuously asserted |

---

## Detector C1 — the signature test

**Guarded element.** `engine_dispatch.py:2464` — `_engagement_with_read` signature.
**Axis.** The stamping function must not declare an `investigated` parameter (register R7).

**Neutralization:**

```python
-def _engagement_with_read(engagement, *, result_kind=None, items=None):
+def _engagement_with_read(engagement, *, result_kind=None, items=None, investigated=None):
```

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_engagement_with_read_signature_has_no_investigated_parameter _______

    def test_engagement_with_read_signature_has_no_investigated_parameter():
        path = os.path.join(_HERE, "..", "engine_dispatch.py")
        with open(path, encoding="utf-8") as fh:
            tree = ast.parse(fh.read())
        for node in ast.walk(tree):
            if isinstance(node, ast.FunctionDef) and node.name == "_engagement_with_read":
                names = [a.arg for a in node.args.args] + [a.arg for a in node.args.kwonlyargs]
>               assert "investigated" not in names
E               AssertionError: assert 'investigated' not in ['engagement', 'result_kind', 'items', 'investigated']

plugins/superheroes/lib/tests/test_engine_dispatch.py:8566: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_engagement_with_read_signature_has_no_investigated_parameter
1 failed in 0.74s
```

**Restore.** Inverse edit restoring `def _engagement_with_read(engagement, *, result_kind=None, items=None):`.

**Restore receipt.**

```
 M docs/superheroes/KEEP-OR-RETIRE.md
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.68s
```

---

## Detector C2 — the call-site census

**Guarded element.** `engine_dispatch.py:2815` — `_grade_review_attempt` fallback call in the
`if accepted:` branch.
**Axis.** No `_engagement_with_read` call site may pass `investigated=`.

**Planted call site.** Line 2815 in `_grade_review_attempt`: the `else` arm of
`engagement_stamped = stamped if stamped is not None else _engagement_with_read(...)`. This branch
is not executed when running `test_engagement_with_read_call_sites_pass_no_investigated_keyword`,
which only AST-parses `engine_dispatch.py` and never invokes review grading — so the proof
measures the census, not a runtime `TypeError` from an invalid keyword on the landed signature.

**Neutralization:**

```python
         engagement_stamped = stamped if stamped is not None else _engagement_with_read(
-            engagement, result_kind=kind, items=[])
+            engagement, result_kind=kind, items=[], investigated=None)
```

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_engagement_with_read_call_sites_pass_no_investigated_keyword _______

    def test_engagement_with_read_call_sites_pass_no_investigated_keyword():
        path = os.path.join(_HERE, "..", "engine_dispatch.py")
        with open(path, encoding="utf-8") as fh:
            tree = ast.parse(fh.read())
        call_sites = []
        for node in ast.walk(tree):
            if not isinstance(node, ast.Call):
                continue
            func = node.func
            if isinstance(func, ast.Name) and func.id == "_engagement_with_read":
                call_sites.append(node)
            elif isinstance(func, ast.Attribute) and func.attr == "_engagement_with_read":
                call_sites.append(node)
        assert len(call_sites) >= 9
        for call in call_sites:
            for kw in call.keywords:
>               assert kw.arg != "investigated", "investigated= at line %d" % call.lineno
E               AssertionError: investigated= at line 2815
E               assert 'investigated' != 'investigated'
E                +  where 'investigated' = <ast.keyword object at 0x10936f580>.arg

plugins/superheroes/lib/tests/test_engine_dispatch.py:8587: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_engagement_with_read_call_sites_pass_no_investigated_keyword
1 failed in 0.77s
```

**Restore.** Inverse edit restoring `engagement, result_kind=kind, items=[])` without
`investigated=None`.

**Restore receipt.**

```
 M docs/superheroes/KEEP-OR-RETIRE.md
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 0.79s
```

---

## Detector C3 — the non-vacuity floor

**Guarded element.** `test_engine_dispatch.py:8584` — `assert len(call_sites) >= 9`.
**Axis.** The floor is measured from the AST census (nine sites in the landed file), not asserted
into existence.

**Neutralization** (temporary assertion edit, reversed before green gate):

```python
-    assert len(call_sites) >= 9
+    assert len(call_sites) >= 99
```

**Raw red** — `PYTEST_EXIT=1`:

```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_engagement_with_read_call_sites_pass_no_investigated_keyword _______

    def test_engagement_with_read_call_sites_pass_no_investigated_keyword():
        path = os.path.join(_HERE, "..", "engine_dispatch.py")
        with open(path, encoding="utf-8") as fh:
            tree = ast.parse(fh.read())
        call_sites = []
        for node in ast.walk(tree):
            if not isinstance(node, ast.Call):
                continue
            func = node.func
            if isinstance(func, ast.Name) and func.id == "_engagement_with_read":
                call_sites.append(node)
            elif isinstance(func, ast.Attribute) and func.attr == "_engagement_with_read":
                call_sites.append(node)
>       assert len(call_sites) >= 99
E       assert 9 >= 99
E        +  where 9 = len([<ast.Call object at 0x10c7bc8b0>, <ast.Call object at 0x10996f160>, <ast.Call object at 0x10c644cd0>, <ast.Call object at 0x10960b460>, <ast.Call object at 0x109fb25b0>, <ast.Call object at 0x109eb1850>, ...])

plugins/superheroes/lib/tests/test_engine_dispatch.py:8584: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_engagement_with_read_call_sites_pass_no_investigated_keyword
1 failed in 0.72s
```

**Restore.** Inverse edit restoring `assert len(call_sites) >= 9`.

**Restore receipt.**

```
 M docs/superheroes/KEEP-OR-RETIRE.md
?? plugins/superheroes/lib/tests/bite_proofs/wo_r4_1271.md
```

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 1.16s
```

---

## Green gate — both AST tests

**Raw green** — `PYTEST_EXIT=0`:

```
..                                                                       [100%]
2 passed in 1.28s
```
