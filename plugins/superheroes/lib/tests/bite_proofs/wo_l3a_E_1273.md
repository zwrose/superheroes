# WO-L3A-E (#1273) bite-proof — timeoutAt recorded once

**Provenance:** cursor / composer-2.5 (implementer).

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-E1 | `_grade_write_attempt` chokepoint | timed-out write without usable `timeoutAt` forfeits `timeout-deadline-unrecorded` | `test_timed_out_write_without_recorded_deadline_forfeits` |
| BP-E2 | `_admit_native_write_result` mtime gate | native result with mtime after recorded cap is refused | `test_native_write_timeout_poll_gap_result_after_cap_refused` |

---

## BP-E1 — timeout-deadline-unrecorded chokepoint

- **axis:** timed-out write without usable `timeoutAt` forfeits before admission

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `_grade_write_attempt`):
```python
        timeout_deadline = ended.get("timeoutAt") if ended.get("timedOut") else None  # bite-proof neutralization
```
(replaces the `_recorded_timeout_deadline` fail-closed chokepoint)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch.py::test_timed_out_write_without_recorded_deadline_forfeits -q
```

**raw red** (exit 1):
```
FFF                                                                      [100%]
=================================== FAILURES ===================================
_______ test_timed_out_write_without_recorded_deadline_forfeits[absent] ________
>       assert grade.get("forfeit") is True
E       AssertionError: assert None is True
FAILED ...::test_timed_out_write_without_recorded_deadline_forfeits[absent]
_______ test_timed_out_write_without_recorded_deadline_forfeits[none] _________
>       assert grade.get("forfeit") is True
E       AssertionError: assert None is True
FAILED ...::test_timed_out_write_without_recorded_deadline_forfeits[none]
_____ test_timed_out_write_without_recorded_deadline_forfeits[non-numeric] _____
>       grade = ED._grade_write_attempt(run_dir, state, 1)
E       TypeError: '>' not supported between instances of 'float' and 'str'
FAILED ...::test_timed_out_write_without_recorded_deadline_forfeits[non-numeric]
3 failed in 1.53s
```

**restore** (`plugins/superheroes/lib/engine_dispatch.py`, `_grade_write_attempt`):
```python
        if ended.get("timedOut"):
            timeout_deadline = _recorded_timeout_deadline(ended)
            if timeout_deadline is None:
                return {
                    "forfeit": True,
                    "reason": dispatch_outcome.REASON_FORFEITED,
                    "detail": "timeout-deadline-unrecorded",
                }
        else:
            timeout_deadline = None
```

**raw green** (exit 0):
```
...                                                                      [100%]
3 passed in 0.96s
```

---

## BP-E2 — poll-gap native-result-after-timeout

- **axis:** result mtime after recorded cap is refused even when poll instant is later

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `_admit_native_write_result`):
```python
            if False and (mtime is None or mtime > timeout_deadline):  # bite-proof neutralization
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch.py::test_native_write_timeout_poll_gap_result_after_cap_refused -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________ test_native_write_timeout_poll_gap_result_after_cap_refused __________
>       assert grade.get("forfeit") is True
E       AssertionError: assert None is True
FAILED ...::test_native_write_timeout_poll_gap_result_after_cap_refused
1 failed in 1.19s
```

**restore** (`plugins/superheroes/lib/engine_dispatch.py`, `_admit_native_write_result`):
```python
            if mtime is None or mtime > timeout_deadline:
```

**raw green:** not captured within the 8-invocation budget — the first post-restore run hit an accidental indentation regression (mtime gate nested under `if detail:`) introduced during neutralization; indentation was corrected before handback. Orchestrator must re-run the green half.
