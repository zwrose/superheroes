# WO-B (#1273 l4a-2p) bite-proofs

## BP-B1 `test_background_mode_single_home` (launch_ledger.py)

**neutralization:** add after `import pilot_slot`:
```diff
+_PROBE_MODE = "background"
```

**command:** `pytest plugins/superheroes/lib/tests/test_launch_ledger_compat.py::test_background_mode_single_home -q`

**red (exit=1):**
```
E       AssertionError: assert ['background-...ledger.py:45'] == []
E         Left contains one more item: 'background-mode-literal-outside-home:launch_ledger.py:45'
```

**restore:** remove `_PROBE_MODE = "background"` line.

**green (exit=0):** `1 passed in 0.09s`

## BP-B2 `test_background_mode_single_home` (engine_adapter.py)

**neutralization:** add after `import claude_modes`:
```diff
+_PROBE_MODE = "background"
```

**command:** same test id

**red (exit=1):**
```
E         Left contains one more item: 'background-mode-literal-outside-home:engine_adapter.py:42'
```

**restore:** remove probe line; `git diff f6100044 -- engine_adapter.py` empty.

**green (exit=0):** `1 passed in 0.09s`

## BP-B3 `test_background_mode_literal_pinned` (claude_modes.py)

**neutralization:**
```diff
-MODE_BACKGROUND = "background"
+MODE_BACKGROUND = "bg"
```

**command:** `pytest ...::test_background_mode_literal_pinned -q`

**red (exit=1):**
```
E       AssertionError: assert 'bg' == 'background'
```

**restore:** `MODE_BACKGROUND = "background"`

**green (exit=0):** `1 passed in 0.06s`
