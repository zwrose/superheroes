# WO-I (#1140 round 3) bite-proofs — nested-invocation behavioural wiring

The detector under proof is the behavioural subprocess group leg
`test_real_wiring_behavioural_nested_cwd` in `test_source_guard.py`. Neutralization removes the
copied root `pytest.ini` from the fixture repo (the same seam exercised by
`test_real_wiring_behavioural_nested_cwd_without_pytest_ini`).

## Baseline

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_source_guard.py -q
```

**Output (summary line; full log includes pytest temp-dir cleanup warnings):**
```
56 passed in 12.62s
```

---

## I1 — root pytest.ini anchors guard for nested invocation

**Guarded element:** root `pytest.ini` existence — axis: for a pytest run started from a nested test
directory against real root wiring copies, the guard must engage (violator fails with
`ShippedSourceWrite`); without root `pytest.ini` the guard never loads and the violator passes.

**Neutralization:** after `_copy_real_root_wiring(root)` in `_build_real_nested_wiring_repo`,
delete the copied `pytest.ini`:
```
(root / "pytest.ini").unlink()
```
(applied as a temporary edit to the builder for the red run only; restored by removing that line.)

**Red run** (`test_real_wiring_behavioural_nested_cwd` with builder neutralization):
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________________ test_real_wiring_behavioural_nested_cwd ____________________

tmp_path = PosixPath('.../pytest-of-zwrose/pytest-10434/test_real_wiring_behavioural_n0')

    def test_real_wiring_behavioural_nested_cwd(tmp_path):
        root = tmp_path / "real_nested_wiring"
        root.mkdir()
        test_dir = _build_real_nested_wiring_repo(root)
        proc = _run_real_nested_wiring_pytest(root, test_dir)
>       _assert_wiring_failure(proc, root)

plugins/superheroes/lib/tests/test_source_guard.py:1209:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

proc = CompletedProcess(args=['...', '-B', '-m', 'pytest', 'test_violator.py', ...],
    stdout='.                                                                        [100%]\n1 passed in 0.01s\n', stderr='')
root = PosixPath('.../real_nested_wiring')

    def _assert_wiring_failure(proc, root):
        combined = proc.stdout + proc.stderr
>       assert proc.returncode != 0, "micro-suite must fail on shipped rewrite:\n%s" % combined
E       AssertionError: micro-suite must fail on shipped rewrite:
E         .                                                                        [100%]
E         1 passed in 0.01s
E
E       assert 0 != 0
E        +  where 0 = CompletedProcess(...).returncode

plugins/superheroes/lib/tests/test_source_guard.py:799: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_source_guard.py::test_real_wiring_behavioural_nested_cwd
1 failed in 0.77s
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  "plugins/superheroes/lib/tests/test_source_guard.py::test_real_wiring_behavioural_nested_cwd" \
  -q
```

**Restore:** remove `(root / "pytest.ini").unlink()` from `_build_real_nested_wiring_repo` (inverse
edit — builder no longer omits `pytest.ini`).

**Green run** (both new nested legs after restore):
```
..                                                                       [100%]
2 passed in 1.45s
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  "plugins/superheroes/lib/tests/test_source_guard.py::test_real_wiring_behavioural_nested_cwd" \
  "plugins/superheroes/lib/tests/test_source_guard.py::test_real_wiring_behavioural_nested_cwd_without_pytest_ini" \
  -q
```
