# Bite-proof record — WO-A/WO-B, #1205 (pilot process-group teardown family)

**What changed:** five detectors of process-group teardown regressions were rewritten onto a shared
readiness/retry probe (`plugins/superheroes/lib/tests/grandchild_probe.py`). Changing a detector owes
a recorded red→green proof, so this record covers all five.

**Guarded-element set, declared up front (five independent elements):**

1. `test_pilot_bounded_run.py::test_retain_output_false_timeout_kills_grandchild` — hanging leader
2. `test_pilot_cleanup.py::test_run_bounded_kills_grandchild_on_timeout` — hanging leader
3. `test_pilot_cleanup.py::test_run_bounded_kills_orphan_grandchild_when_leader_exits_first` — leader `exit 0`
4. `test_pilot_cleanup.py::test_run_bounded_kills_sigterm_ignoring_orphan_grandchild_when_leader_exits_first` — leader `exit 0` + `trap '' TERM`
5. `test_pilot_mint.py::test_gate_off_grandchild_timeout_reaps_process_group` — Python argv, `SIGTERM` `SIG_IGN`

**Neutralization site — the chokepoint, in shipped source:** `pilot_bounded_run._terminate`, which
every one of the three surfaces reaches (`pilot_cleanup.run_bounded` and `pilot_mint.run_gate_off_test`
both delegate to `pilot_bounded_run.run_bounded`). Mutations were applied as targeted, revertible
edits through the host's edit action and reverted by the inverse edit; the landed implementer work
was committed first (`b6971ae9`, `69c43cec`), so no probe could discard uncommitted work.

`_terminate` signals the group **twice** — `SIGTERM`, then `SIGKILL` after a 1 s pause — so one
mutation cannot bite every element on the same axis. Two mutations, each named to its axis (this is
the correction the pre-code brief check forced; see finding `reviewer-003`).

---

## M1 — axis: *the whole process group is signalled*

Both group-directed signals neutralized (`os.killpg(pgid, signal.SIGTERM)` and
`os.killpg(pgid, signal.SIGKILL)` each replaced by `pass  # BITEPROOF-M1`; each literal occurs
exactly once in the file, verified by `grep -c`). Leader-only reaping left intact.

**Expected: all five red, on the survivor axis. Observed: all five red.**

```
FAILED plugins/superheroes/lib/tests/test_pilot_bounded_run.py::test_retain_output_false_timeout_kills_grandchild
FAILED plugins/superheroes/lib/tests/test_pilot_cleanup.py::test_run_bounded_kills_grandchild_on_timeout
FAILED plugins/superheroes/lib/tests/test_pilot_cleanup.py::test_run_bounded_kills_orphan_grandchild_when_leader_exits_first
FAILED plugins/superheroes/lib/tests/test_pilot_cleanup.py::test_run_bounded_kills_sigterm_ignoring_orphan_grandchild_when_leader_exits_first
FAILED plugins/superheroes/lib/tests/test_pilot_mint.py::test_gate_off_grandchild_timeout_reaps_process_group
5 failed in 74.58s (0:01:14)
```

Axis check — the red is on **survivor**, not on the setup race. Representative message:

```
E  Failed: grandchild pid 87513 still alive after gate-off timeout reap;
   observed state: alive (kill(pid, 0) succeeded; /proc unavailable); pgid=87444
```

## M2 — axis: *escalation reaches a SIGTERM-ignoring member*

`SIGTERM` restored; **only** the `SIGKILL` escalation neutralized.

**Expected: elements (4) and (5) red; (1)–(3) green. Observed: exactly that.**

```
FAILED plugins/superheroes/lib/tests/test_pilot_cleanup.py::test_run_bounded_kills_sigterm_ignoring_orphan_grandchild_when_leader_exits_first
FAILED plugins/superheroes/lib/tests/test_pilot_mint.py::test_gate_off_grandchild_timeout_reaps_process_group
2 failed, 3 passed in 35.14s
```

**What M2 does and does not prove.** Elements (1)–(3) launch descendants with the default `SIGTERM`
disposition, so they are green under M2 by construction: they prove *the group is signalled*, not
*escalation happens*. Only (4) and (5) — whose descendants ignore `SIGTERM` — can bite the
escalation axis, and both did. Stated here rather than left as an unexplained green.

## Restore and green

Both mutations reverted by inverse edit; `git status --porcelain` and `git diff --stat` on
`plugins/superheroes/lib/pilot_bounded_run.py` came back **empty** (the shipped file is byte-identical
to `origin/main`; this diff touches no shipped source at all).

```
5 passed in 17.07s
```

**Vacuity check against the four ways** (`rubric/bite-proof.md`): the mutation is at the **production
call site** the guarantee lives at, not a precondition helper (1); each red is named to the axis its
mutation neutralizes, and M2's three greens are explained rather than counted as proof (2); **five
separate elements, five reds under M1** — no representative stood in for the set (3); the mutation
reaches every element through the path each test actually uses, and each red arrived where the
reasoning said it would (4).
