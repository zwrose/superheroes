# Round 4 (#1144) — the proofs round 3 recorded as unprovable, and two record corrections

**What this file is.** The owner's 2026-08-26 ruling (walk-4 item 9 = `a` with the cap) ordered one
final pass. Two of its four items are records: **produce the proofs round 3's confirming review
showed were readily provable** (its findings 2 and 3), and **correct what round 3's ledger claims**
(its finding 1). This file is both.

**Who ran it.** The orchestrator, at verification, in this session. Not delegated — round 2's defect
was receipts transcribed rather than executed, and round 3 repeated the rule with feeling.

**Head.** Every red and green below was produced at **`9612a396`** — the final head of this round's
ordered scope, *after* WO-A rewrote the census module's failure messages. That matters: a proof
recorded against an earlier message text would quote a string this branch no longer prints. Each
red capture below is the message the shipped code prints **now**.

**Restore discipline.** Every neutralization was applied as a targeted, reversible edit through the
host's edit action and reverted by its exact inverse — never a `git` discard, never a whole-file
rewrite. The tree was committed clean before the first probe. **The census module was unedited
throughout**: every neutralization mutates real skill content or a real block tag.

```
$ git rev-parse HEAD        # before and after the whole probe sequence
9612a3963e4ae86b18db4cbcf64ececbb28cd119
$ git status --porcelain    # after the last restore
                            # empty
$ pytest plugins/superheroes/lib/tests/test_decision_point_census.py -q
22 passed
```

Nothing below is redacted: no secrets, tokens, private URLs or PII appear in any capture. Captures
are quoted decisively rather than in full, per the bite-proof rubric's bounded-volume rule.

---

## A — The mode-structure legs round 3 recorded as unproven

Round 3's §C2 said: *"The `gate` and `proceed` legs have **no** red run, in this round or any earlier
one."* Both are proven here, on real block prose.

### R4-1 — `gate` leg, `write`/`written` axis

**Guarded element:** `_mode_structure_violations`, `mode == "gate"`, first predicate.
**Neutralization:** in `skills/review-code/SKILL.md`, block `id=review-present-judgment` (line 302),
`the choice is **written down** in the durable session record` → `the choice is **noted** in the
durable session record`. One occurrence; the block's only `write`/`written` substring.

```
E   AssertionError: #1144 decision block mode-structure violations. Every hit:
E     skills/review-code/SKILL.md:302: gate block prose missing write or written substring
1 failed
```

**Restore + green:** inverse edit; `git status --porcelain` empty; check green.

### R4-2 — `gate` leg, `hand back`/`hands back` axis

**Neutralization:** same block, `the run **hands back**` → `the run **stops**`.

```
E     skills/review-code/SKILL.md:302: gate block prose missing hand back or hands back substring
1 failed
```

### R4-3 — `proceed` leg

**Neutralization:** in `skills/architect-init/SKILL.md`, block
`id=architect-doc-policy-default` (line 93), `The run continues after a successful write.` → `The
run proceeds after a successful save.` The block contains **no** `record` substring, so this single
edit neutralizes both disjuncts of the predicate — which is why this site was chosen.

```
E     skills/architect-init/SKILL.md:93: proceed block prose missing continu or record substring
1 failed
```

**Why these three needed real sites.** They are three separate substring predicates, not members of
one loop over like elements, so no class argument covers them — round 3 said exactly this when it
declined to claim them. The `notify` leg was already proven in round 3 §A8.

---

## B — The six `_parse_decision_blocks` legs round 3 carried forward as unproven

All six are proven here by mutating **one real block's open tag** in
`skills/architect-init/SKILL.md` (line 93) — the smallest edit that reaches each guarded element.

| # | Guarded element | Neutralization | Red capture |
|---|---|---|---|
| R4-4 | invalid `mode` | `mode=proceed` → `mode=proceeds` | `skills/architect-init/SKILL.md:93: invalid mode='proceeds'` |
| R4-5 | invalid `kind` | `kind=owner-gate` → `kind=owner-gates` | `skills/architect-init/SKILL.md:93: invalid kind='owner-gates'` |
| R4-6 | empty `default` | `default="analysis-informed provisional policy"` → `default=""` | `skills/architect-init/SKILL.md:93: empty default= for id='architect-doc-policy-default'` |
| R4-7 | unknown `carrier` | `carrier=doc-policy-disclosures` → `carrier=doc-policy-disclosure` | `skills/architect-init/SKILL.md:93: unknown carrier='doc-policy-disclosure'` |
| R4-8 | malformed open tag | the ` carrier=…` attribute deleted from the open tag | `skills/architect-init/SKILL.md:93: malformed decision-point open tag` |
| R4-9 | nesting forbidden | a second complete open/close pair inserted inside the block | `skills/architect-init/SKILL.md:94: nested decision-point blocks are forbidden (id='nested-probe' inside id='architect-doc-policy-default')` |

Each ran `test_decision_blocks_well_formed` alone, red, and was reverted by its exact inverse before
the next. Two notes on what these captures do and do not show:

- **R4-8 also produced** `skills/architect-init/SKILL.md:99: orphan decision-point close
  id='architect-doc-policy-default'`. That is correct and expected — an open tag the regex cannot
  parse leaves its close unmatched — not a second, independent proof.
- **R4-7's neutralization is a one-character truncation** (`disclosures` → `disclosure`). It is
  deliberately the smallest edit that leaves a *plausible* carrier name, so the leg is shown catching
  a typo rather than only an obviously-absent key.

---

## C — `test_skills_census_walk_non_empty`: round 3's disclosure was wrong

**Round 3 recorded it as "unprovable as placed"**, reasoning that a real-content proof "would mean
emptying the repository's `skills/` tree". The confirming review refuted that (its finding 2), and
the refutation is correct: the tree does not have to be emptied, only **moved aside and moved back**.

**R4-10 — neutralization:** `os.rename("plugins/superheroes/skills",
"plugins/superheroes/.skills-probe-1144r4")`, run the check, then `os.rename` back in a `finally`
block so the restore runs even if the check raises.

```
E   AssertionError: #1144 decision-point census found zero files under skills/
E   assert 0 > 0
EXIT= 1
restored: True   probe gone: True
```

`git status --porcelain` was empty afterwards.

**Mechanism disclosure.** This is the one probe in this round **not** applied through the host's
edit action — a directory rename cannot be. It was applied by a two-line Python rename with the
inverse in a `finally`, which is why the restore is asserted in the capture itself rather than only
by the tree check after it.

**The correction this makes to the record:** round 3's §C2 entry for this check is **withdrawn as
inaccurate**. Its protection is *not* "verified only through the fixture's monkeypatch"; it is
verified against the real tree, here. The disclosure was more pessimistic than the truth, which is
its own species of false record — the one this round's finding 2 caught.

---

## D — Correction to round 3's BP-4a / BP-4a2 classification

Round 3's summary table lists **BP-4a** and **BP-4a2** under *"Sound on real content — re-executed,
**reproduced**"*. The confirming review (its finding 1) showed the word "reproduced" overstates what
happened, and it is right:

- **What `wo_a_1144.md` recorded** as BP-4a's neutralization: *"deleted the capture line
  `SOURCE=$(printf '%s' "$DEC" | jq -r '.source')`"*.
- **What round 3 actually ran:** `jq -r '.source'` → `jq -r '.src'` at the same site.

Both mutate real skill content, both exercise the same guarded element on the same axis, and both go
red with the same message — so round 3's *verdict* (sound, not vacuous) stands. What does not stand
is "reproduced": round 3 produced a **fresh proof of the same element**, which **supersedes** the
recorded one rather than re-running it.

**Recorded correction, not a rewrite.** `wo_a_1144.md`'s BP-4a and BP-4a2 entries are left
byte-for-byte as they were: they are a record of what an earlier session recorded, and editing them
to match a later run is how a hybrid receipt — a command that was never issued next to a capture from
a different run — gets manufactured. Round 3's table gets a correction note instead, and this section
is the durable statement of what actually happened.

---

## E — What is still unproven, and why (rubric-complete)

**`review-init` Step 4b's payload guard and `test-pilot-init` Step 6's payload guard.**

- *What makes them unprovable:* both are instructions in **skill documents**. Nothing in the test
  suite executes skill prose, so there is no entry point at which a neutralization could be observed
  going red. This is a construction bound, not a missing effort.
- *What would make them provable:* converting each write path to executable shell that a harness can
  drive with an assembled payload, so the refusal is observable at runtime.
- *Plain statement:* their protection is **unverified**.
- *Where it is disclosed:* as of this round, `test-pilot-init`'s guard carries that statement **at
  the guard itself** (WO-B), not only here. `review-init`'s sibling guard still carries it only in a
  build record — deliberately out of this round's ordered scope, and a standing follow-up.

**Nothing else on this branch is recorded as unproven.** The two legs and six parser elements that
carried that label out of round 3 are proven above; the `test_skills_census_walk_non_empty`
disclosure is withdrawn as inaccurate.

---

## F — This round's own detectors

**None.** WO-A changed no predicate, condition, regex, set literal or control flow; WO-B and WO-C
changed a docstring and added a disclosure sentence. **No detector was added or changed in round 4**,
so no new bite-proof is owed by the work itself. Everything above is the *re-execution* debt the
ruling ordered cleared, not a proof of new protection.
