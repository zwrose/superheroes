# Bite-proof record — #1271 layer 1 (the `seat-result/2` envelope seam and the CAS-token record identity)

Contract: `rubric/bite-proof.md`. Every proof below ran with the **detector unedited**; the
neutralization was applied as a targeted, reversible edit to the **production call site**, and
reverted by its exact inverse. Probes ran in a **detached worktree pinned to the build head**
(`/private/tmp/wh1271M-probe` at `249c178d`, `/private/tmp/wh1271M-probe2` at `651f21b9`), never in a
tree a live reader was reading, and the landed implementer work was committed before any probe.

**Who ran these.** The orchestrator, not an implementer. Mutation probes are verification, and
verification authority does not delegate; these records are therefore orchestrator-authored from
orchestrator-run probes rather than an implementer's proof re-run.

**Redaction.** Nothing here required redaction: no secrets, tokens, private URLs, or PII appear in
any capture. Raw captures are quoted to their decisive lines; the full captures were session-scoped
scratch files outside the repository, the largest 32 KiB, removed once this verification closed.
Every quoted line below is verbatim from the runner's own stdout, and every exit code is the
runner's own, never a pipe's.

**The guarded-element set this record covers — eleven elements, declared in the orders that
dispatched the work and enumerated here.** They are the nine elements named by keep-or-retire
entries D11, D12 and D13, plus the two detectors this wave births. Each is an independently
neutralizable element with its own refusal or assertion; none is grouped into an equivalence class,
because each of the nine refusal tokens is a distinct failure mode.

| # | Guarded element | Entry | Axis |
|---|---|---|---|
| 1 | `provenance-unknown` | D11 | the refusal on an unenumerated provenance |
| 2 | `execution-evidence-malformed` | D11 | the refusal on a present-but-ill-formed evidence block |
| 3 | `execution-evidence-not-inline` | D11 | the refusal on a pointer key anywhere in the evidence tree |
| 4 | `execution-evidence-unexpected` | D11 | the refusal on evidence under `orchestrator-fulfilled` |
| 5 | `envelope-torn` | D11 | the refusal on a declared envelope hash that does not match the computed one |
| 6 | `schema-version-mismatch` | D12 | the refusal on an envelope minted for the wrong state version |
| 7 | `state-version-unsupported` | D12 | the refusal on a session whose state version has no seat-result decode |
| 8 | `evidence-run-dir-unreadable` | D13 | the refusal on an unreadable runner record |
| 9 | `evidence-order-mismatch` | D13 | the refusal when a stamped record's prompt hash is not this order's hash |
| 10 | the journal-row CAS-token census | D14 (new) | every `recorded` row carrying a revision carries the stored envelope's CAS token |
| 11 | the reconcile CAS-token identity | D14 (new) | reconcile compares CAS token against CAS token, never payload hash against envelope hash |

---

## 1 — `provenance-unknown` (D11)

**Guarded element.** `round_records.py:759` — `if provenance not in SEAT_PROVENANCE`.
**Axis.** The refusal fires when a v2 envelope declares a provenance outside the closed enumeration.

**Neutralization** (production call site, targeted):

```python
-            if provenance not in SEAT_PROVENANCE:
+            if False:
                 return None, _refuse("provenance-unknown", provenance=provenance)
```

**Raw red** — `PYTEST_EXIT=1`:

```
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_provenance_unknown_refuses[None]
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_provenance_unknown_refuses[invented]
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_opt_in_v2_refuses_provenance_unknown
3 failed in 0.49s
```

**Restore.** The exact inverse edit, restoring `if provenance not in SEAT_PROVENANCE:`.
**Restore receipt.** `git status --porcelain` over the probe worktree: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
...                                                                      [100%]
3 passed in 0.28s
```

---

## 2 — `execution-evidence-malformed` (D11)

**Guarded element.** `round_records.py:765-767` — the refusal that carries
`_validate_execution_evidence`'s verdict out of `validate_landing`.
**Axis.** The refusal fires when a present evidence block is not a well-formed inline object.

**Neutralization** (suppresses exactly this token, leaving the sibling token live, so the red is on
this element's axis and not on its neighbour's):

```python
-                if evidence_reason is not None:
+                if evidence_reason is not None and evidence_reason != "execution-evidence-malformed":
                     return None, _refuse(evidence_reason)
```

**Raw red** — `PYTEST_EXIT=1`, decisive lines (9 failed, 1 passed, 218 deselected):

```
FAILED …::test_v2_execution_evidence_key_present_not_dict_refuses_malformed[bad_value0-dispatch-observed]
FAILED …::test_v2_execution_evidence_key_present_not_dict_refuses_malformed[string-hand-landed]
FAILED …::test_v2_execution_evidence_key_present_not_dict_refuses_malformed[42-dispatch-observed]
FAILED …::test_v2_opt_in_v2_refuses_execution_evidence_malformed
9 failed, 1 passed, 218 deselected in 0.45s
```

The one passing node is the `orchestrator-fulfilled` case, which refuses `execution-evidence-unexpected`
**before** reaching the neutralized line — the correct behaviour, and evidence the neutralization was
confined to this element.

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
..........                                                               [100%]
10 passed, 218 deselected in 0.33s
```

---

## 3 — `execution-evidence-not-inline` (D11)

**Guarded element.** The same refusal edge, carrying `_execution_evidence_has_pointer`'s verdict.
**Axis.** The refusal fires when a pointer key appears anywhere in the evidence tree — nested or
inside a list.

**Neutralization** (this token suppressed, its sibling left live):

```python
-                if evidence_reason is not None:
+                if evidence_reason is not None and evidence_reason != "execution-evidence-not-inline":
                     return None, _refuse(evidence_reason)
```

**Raw red** — `PYTEST_EXIT=1`, decisive lines (21 failed, 207 deselected):

```
FAILED …::test_v2_execution_evidence_pointer_key_nested_two_levels_refuses_not_inline[dispatch-observed]
FAILED …::test_v2_execution_evidence_pointer_key_inside_list_refuses_not_inline[hand-landed]
FAILED …::test_v2_opt_in_v2_refuses_execution_evidence_not_inline
21 failed, 207 deselected in 0.72s
```

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.....................                                                    [100%]
21 passed, 207 deselected in 0.86s
```

---

## 4 — `execution-evidence-unexpected` (D11)

**Guarded element.** `round_records.py:763-764` — evidence present under
`PROVENANCE_ORCHESTRATOR_FULFILLED`.
**Axis.** The refusal fires when a record for a phase on which **no seat was dispatched** nonetheless
carries execution evidence — a claim of a dispatch that did not happen.

**Neutralization:**

```python
-                if provenance == PROVENANCE_ORCHESTRATOR_FULFILLED:
+                if False:
                     return None, _refuse("execution-evidence-unexpected")
```

**Raw red** — `PYTEST_EXIT=1`:

```
FAILED …::test_v2_execution_evidence_key_present_null_refuses[orchestrator-fulfilled-execution-evidence-unexpected]
FAILED …::test_v2_execution_evidence_key_present_well_formed[orchestrator-fulfilled-execution-evidence-unexpected]
2 failed, 21 passed, 205 deselected in 1.21s
```

The 21 that stayed green are the `dispatch-observed` and `hand-landed` parametrizations, which this
element does not govern — the red is on this element's axis alone.

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.......................                                                  [100%]
23 passed, 205 deselected in 1.12s
```

---

## 5 — `envelope-torn` (D11)

**Guarded element.** `round_records.py:773-775` — `declared_envelope_sha != computed_envelope_sha`.
**Axis.** The refusal fires when the declared envelope hash does not identify the
`(payload, evidence)` pair actually present — the re-pairing this hash exists to close.

**Neutralization:**

```python
-            if declared_envelope_sha != computed_envelope_sha:
+            if False:
                 return None, _refuse("envelope-torn", computed=computed_envelope_sha,
                                      declared=declared_envelope_sha, landingPath=lpath)
```

**Raw red** — `PYTEST_EXIT=1`:

```
FAILED …test_round_records.py::test_v2_execution_evidence_key_absent_stale_hash_refuses_envelope_torn[dispatch-observed]
FAILED …test_round_records.py::test_v2_execution_evidence_key_absent_stale_hash_refuses_envelope_torn[hand-landed]
FAILED …test_round_records.py::test_v2_execution_evidence_key_absent_stale_hash_refuses_envelope_torn[orchestrator-fulfilled]
FAILED …test_round_driver_advance.py::test_head_diff_rewrite_keeps_v2_envelope_self_consistent
4 failed in 4.02s
```

The fourth node is the head-diff leg added this wave: it asserts the refusal token as a spelled-out
literal, so it reddens here too, which is what makes the head-diff recompute genuinely guarded rather
than merely written.

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
....                                                                     [100%]
4 passed in 4.44s
```

---

## 6 — `schema-version-mismatch` (D12)

**Guarded element.** `round_records.py:750-752` — `schema != expected_schema`.
**Axis.** The refusal fires when a producer mints an envelope for a state version other than the
session's.

**Neutralization:**

```python
-        if schema != expected_schema:
+        if False:
             return None, _refuse("schema-version-mismatch", schema=schema,
                                  expectedSchema=expected_schema)
```

**Raw red** — `PYTEST_EXIT=1`:

```
FAILED …::test_v2_opt_in_none_refuses_v2_schema_version_mismatch
FAILED …::test_v2_opt_in_v1_refuses_v2_schema_version_mismatch
2 failed in 0.39s
```

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
..                                                                       [100%]
2 passed in 0.27s
```

---

## 7 — `state-version-unsupported` (D12)

**Guarded element.** `round_driver.py:7359`, `:7501`, `:7646` — the three refusal sites reached when
`_seat_result_schema(state)` returns `None`.
**Axis.** The refusal fires, **under its own name**, when a session's state version has no
seat-result decode.

**This element had no detector at all before this build.** A repository-wide grep for the token
across `plugins/superheroes/lib/tests/` returned **zero matches**, so there was nothing to redden; a
guard with no detector is an unverified claim however emphatic its name. WO-M5 added one test per
refusal site — three, not one representative, because a single test would say nothing about the
other two sites. That the three reach three **distinct** sites was verified against the code, not
assumed: `_cmd_record_result_locked` returns `_sweep_record(...)` **before** its own
`state-version-unsupported` check, so the sweep test genuinely reaches `:7501`.

**Neutralization** (the token itself, at all three sites — the axis is the refusal's identity, which
is what other code and other readers match on):

```python
-        "state-version-unsupported"
+        "state-version-NEUTRALIZED"
```

**Raw red** — `PYTEST_EXIT=1`:

```
E       AssertionError: assert 'state-version-NEUTRALIZED' == 'state-version-unsupported'
E         - state-version-unsupported
E         + state-version-NEUTRALIZED
FAILED …::test_cmd_record_result_refuses_state_version_unsupported
FAILED …::test_cmd_record_result_sweep_refuses_state_version_unsupported
FAILED …::test_cmd_record_missing_refuses_state_version_unsupported
3 failed, 32 deselected in 13.11s
```

**Restore.** The exact inverse edit at all three sites. **Restore receipt.** `git status --porcelain`:
**empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
...                                                                      [100%]
3 passed, 32 deselected in 11.14s
```

---

## 8 — `evidence-run-dir-unreadable` (D13)

**Guarded element.** `round_driver._assemble_dispatch_evidence` — the refusal on an unreadable
runner record.
**Axis.** The refusal fires, **under its own name**, when the cited run directory cannot be read.

**A first neutralization was discarded for biting on the wrong axis, and that is recorded rather
than hidden.** Suppressing the branch with `if False:` produced a red — but an
`AttributeError: 'NoneType' object has no attribute 'get'` at `round_driver.py:7247`, a crash rather
than a wrong refusal. Per `rubric/bite-proof.md` trap 2, a red on an adjacent axis is not the red
this element claims. The neutralization was re-aimed at the refusal **token**, which is the axis the
detector actually asserts.

**Neutralization:**

```python
     if err is not None:
-        return None, "evidence-run-dir-unreadable", {"detail": err}
+        return None, "evidence-run-dir-NEUTRALIZED", {"detail": err}
```

**Raw red** — `PYTEST_EXIT=1`:

```
E       - evidence-run-dir-unreadable
E       + evidence-run-dir-NEUTRALIZED
FAILED …::test_seam_a_record_result_refusal_evidence_run_dir_unreadable_leaves_landing_bytes
1 failed in 2.25s
```

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 2.37s
```

---

## 9 — `evidence-order-mismatch` (D13)

**Guarded element.** `round_driver._assemble_dispatch_evidence` — the binding that a stamped runner
record belongs to *this* order, comparing the record's `promptSha256` against the envelope's
`orderSha256`.
**Axis.** The refusal fires, under its own name, when a stamped record was produced by some other
order.

**Neutralization:**

```python
-        return None, "evidence-order-mismatch", {"promptSha256": prompt_sha,
+        return None, "evidence-order-NEUTRALIZED", {"promptSha256": prompt_sha,
                                                  "orderSha256": order_sha}
```

**Raw red** — `PYTEST_EXIT=1`:

```
E       - evidence-order-mismatch
E       + evidence-order-NEUTRALIZED
FAILED …::test_seam_a_record_result_refusal_evidence_order_mismatch_leaves_landing_bytes
1 failed in 2.20s
```

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
.                                                                        [100%]
1 passed in 2.15s
```

---

## 10 — The journal-row CAS-token census (D14, new this wave)

**Guarded element.** The invariant that **every** `recorded` journal row carrying a stored envelope's
revision carries that envelope's CAS token. Six sites journal a revision; two of them do it by a
**late subscript assignment inside `_store_head_diff`**, not by a kwarg at the call site.
**Axis.** Coverage of every governed site, not of the sites a source grep happens to reach.

**The first proof came back green, and that green is the reason this element has a second detector.**
With `round_driver._store_head_diff`'s revision update neutralized:

```python
-            journal_entry.update(_journal_revision_fields(final))
+            journal_entry["payloadSha256"] = payload_sha
```

the census test then in place stayed **green**:

```
$ pytest …test_round_driver_advance.py::test_every_recorded_row_carries_the_stored_envelopes_cas_token
1 passed in 2.88s
```

That is `rubric/bite-proof.md` trap 4 exactly — the fixture never creates the condition the assertion
discriminates on, so for those two sites the assertion was measuring nothing. **The cure was the
fixture, never the assertion:** WO-M6 added two tests that reach the two `_store_head_diff` call
sites (the `_repair_fixer_head_diff` route and the sweep head-diff bind), each asserting the row's
`casToken` equals the stored envelope's `envelope_cas_token`, **and** that at `seat-result/2` it
equals the stored `envelopeSha256` and is **not** equal to the stored `payloadSha256` — the
assertion that discriminates a real CAS token from a payload hash wearing the name.

**The proof was then re-run on the final head** (`651f21b9`, probe worktree
`/private/tmp/wh1271M-probe2`), with the same neutralization.

**Raw red** — `PYTEST_EXIT=1`:

```
FAILED …::test_repair_fixer_head_diff_journal_carries_envelope_cas_token
FAILED …::test_sweep_fresh_fixer_head_diff_bind_journal_carries_envelope_cas_token
2 failed in 5.51s
```

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`, the two new tests plus the original census:

```
...                                                                      [100%]
3 passed in 16.61s
```

---

## 11 — The reconcile CAS-token identity (D14, new this wave)

**Guarded element.** `round_records.reconcile` — the journal-side token resolution that compares CAS
token against CAS token rather than a journal payload hash against a store envelope hash.
**Axis.** Reconcile's re-append decision, on the identity the envelope actually uses.

**Neutralization** (drops the journal-side CAS token, which is precisely the pre-fix state):

```python
                     ident = journal_idents.get(key)
-                    journaled_cas = ident.get("casToken") if ident else None
+                    journaled_cas = None
```

**Raw red** — `PYTEST_EXIT=1`. The first line is the exact signature the second park recorded:

```
E       AssertionError: assert 10 == 5
E       AssertionError: assert [{'attempt': ...4d475e', ...}] == []
E       AssertionError: assert [{'attempt': ...5f6aac', ...}] == []
E       AssertionError: assert [{'attempt': ...5f6aac', ...}] == []
FAILED …test_round_driver_advance.py::test_death_between_journal_append_and_advance
FAILED …test_round_driver_advance.py::test_orchestrator_fulfilled_record_and_journal_reconcile_clean
FAILED …test_round_records.py::test_reconcile_reappends_when_identical_payload_differs_in_execution_evidence
FAILED …test_round_records.py::test_reconcile_bounded_two_pass_transition_for_v2_without_journal_cas_token
4 failed in 4.97s
```

`assert 10 == 5` is five roster seats producing ten `recorded` rows after one `advance` — the
journal doubling that stopped the lane, reproduced on demand.

The third and fourth nodes are what make this proof discriminating rather than merely reproducing:
the third is the supersede that carries an **identical payload with different execution evidence**,
which option (b) — comparing `payloadSha256` on both sides — would have reconciled as unchanged; the
fourth asserts the pre-transition v2 row reappends **once** and is clean on the second pass, so the
transition is bounded rather than perpetual.

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `PYTEST_EXIT=0`:

```
....                                                                     [100%]
4 passed in 5.30s
```

---

## Elements listed as unproven

**None.** Every element of the declared set above carries its own neutralization, red, restore,
restore receipt, and green.

---

# Bite-proof record — the review fix wave (#1271 layer 1, round 1 fixes)

R27 makes a recorded bite-proof a **birth duty** for anything that detects, so the detectors the fix
wave births carry their own proofs here. Same contract, same discipline: probes ran in a detached
worktree pinned to the build head (`/private/tmp/wh1271M-probe3` and `-probe4` at `22bd5103`), the
detector was unedited, every neutralization was reverted by its exact inverse, and every restore
carries a `git status --porcelain` receipt. Run by the orchestrator, not an implementer. **No
redaction was required** — no secrets, tokens, private URLs or PII appear in any capture.

**Guarded-element set this record covers — four elements**, declared in the orders that dispatched
the work:

| # | Guarded element | Axis |
|---|---|---|
| 12 | the order-hash binding's **positive** leg | a run directory opened the real way **satisfies** the binding |
| 13 | `sweep-evidence-unsupported` | the refusal on `--sweep` together with `--evidence-run-dir` |
| 14 | the grading-agreement invariant | both grading paths grade one attempt identically |
| 15 | the `seat-result/2` doc-rider drift test | the documented v2 field list matches `SEAT_RESULT_V2_FIELDS` |

---

## 12 — The order-hash binding's positive leg

**Guarded element.** `test_seam_a_record_result_evidence_binding_accepts_genuine_run` and
`test_seam_a_record_ingest_replaces_landing_when_evidence_stamped`.
**Axis.** The binding **accepts** a run directory produced the way production produces one.

**This element's first proof came back GREEN, and that is why the fixture changed.** With the
production binding reverted to the pre-fix comparison —

```python
-    prompt_sha = record.get("orderPromptSha256")
+    prompt_sha = record.get("promptSha256")
```

— `test_seam_a_record_result_evidence_binding_accepts_genuine_run` **passed**:

```
1 passed in 18.45s
```

The fixture called the real open path with `fed_prompt=None`, which writes the **raw order** into
`prompt.txt`, so `promptSha256` and `orderPromptSha256` were equal and both comparisons succeeded.
`rubric/bite-proof.md` trap 4 again — **the fourth instance on this branch** — and the cure was the
fixture, never the assertion. WO-F7 made the fixture build the decorated prompt the way the dispatch
path does, and added an explicit guard so it cannot silently stop:
`assert record["orderPromptSha256"] != record["promptSha256"]`.

**Re-probed after the fixture change, with the same neutralization. Raw red** — `RED_EXIT=1`:

```
E     AssertionError: {'ok': False, 'orderPromptSha256': '7f06efe4ff1a41e0…',
E                      'orderSha256': 'e9d2da18664a43f5…', 'reason': 'evidence-order-mismatch', …}
FAILED …::test_seam_a_record_result_evidence_binding_accepts_genuine_run
FAILED …::test_seam_a_record_ingest_replaces_landing_when_evidence_stamped
2 failed in 5.42s
```

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain` shows only the
test file this order changed; `round_driver.py` is **absent from the list** — unmodified.

**Raw green** — `GREEN_EXIT=0`:

```
..                                                                       [100%]
2 passed in 4.98s
```

---

## 13 — `sweep-evidence-unsupported`

**Guarded element.** `round_driver.py` — the refusal on `--sweep` together with
`--evidence-run-dir`.
**Axis.** The refusal fires, **under its own name**, on the combination — and not on a plain sweep.

Before WO-F6 this refusal had **no test anywhere**, so it could not carry a proof at all.

**Neutralization** (the token itself — the axis is the refusal's identity):

```python
-        return _refuse_cmd(session_dir, "record-result", "sweep-evidence-unsupported",
+        return _refuse_cmd(session_dir, "record-result", "sweep-evidence-NEUTRALIZED",
```

**Raw red** — `RED_EXIT=1`:

```
E       - sweep-evidence-unsupported
E       + sweep-evidence-NEUTRALIZED
FAILED …::test_seam_a_record_result_refusal_sweep_evidence_unsupported_fires
FAILED …::test_seam_a_record_result_refusal_sweep_evidence_unsupported_leaves_disk_and_sweep_alone_ok
2 failed, 68 deselected in 8.63s
```

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `GREEN_EXIT=0`:

```
..                                                                       [100%]
2 passed, 68 deselected in 11.05s
```

The second test is what keeps this pair from being a tautology: it asserts the landing bytes are
untouched, no store file appears, **and** that a plain sweep from that same state still succeeds — so
a change that broke every sweep would fail it rather than pass it.

---

## 14 — The grading-agreement invariant

**Guarded element.** The shared grading decision in `engine_dispatch.py`, and the test asserting the
dispatch-grading path and the execution-record observer grade one attempt identically.
**Axis.** Both paths receive the same action evidence and reach the same `read`.

**Neutralization** (drops the accepted investigated paths from the shared decision — precisely the
one-sided edit the duplication made possible):

```python
         if has_payload:
             stamped = _engagement_with_read(
-                engagement, result_kind=kind, items=payload, investigated=accepted)
+                engagement, result_kind=kind, items=payload)
```

**Raw red** — `RED_EXIT=1`:

```
FAILED …::test_run_execution_record_codex_engaged_by_investigated_paths
FAILED …::test_grade_review_attempt_engaged_by_investigated_paths
2 failed, 4 passed, 502 deselected in 2.41s
```

Both paths redden from **one** edit, which is the property the extraction bought: before it, the same
correction had to be made twice and either could be missed.

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `GREEN_EXIT=0`:

```
......                                                                   [100%]
6 passed, 502 deselected in 1.11s
```

---

## 15 — The `seat-result/2` doc-rider drift test

**Guarded element.** `test_documented_seat_result_v2_fields_match_authority` and
`test_envelope_built_from_documented_v2_fields_validates_at_ingest`.
**Axis.** The documented v2 field table matches `round_records.SEAT_RESULT_V2_FIELDS`.

This is the detector that gave `SEAT_RESULT_V2_FIELDS` its **first consumer** — before it, the tuple
was declared and read by nothing, which is why the producer documentation drifted to `seat-result/1`
undetected.

**Neutralization** (removes one member from the authority tuple):

```python
-SEAT_RESULT_V2_FIELDS = SEAT_RESULT_FIELDS + ("executionEvidence", "provenance",
-                                              "envelopeSha256")
+SEAT_RESULT_V2_FIELDS = SEAT_RESULT_FIELDS + ("executionEvidence", "provenance")
```

**Raw red** — `RED_EXIT=1`:

```
FAILED …::test_documented_seat_result_v2_fields_match_authority
FAILED …::test_envelope_built_from_documented_v2_fields_validates_at_ingest
2 failed, 5 passed in 0.36s
```

**Restore.** The exact inverse edit. **Restore receipt.** `git status --porcelain`: **empty**.

**Raw green** — `GREEN_EXIT=0`:

```
.......                                                                  [100%]
7 passed in 0.16s
```

---

## Elements listed as unproven

**None.** Every element of both declared sets in this file carries its own neutralization, red,
restore, restore receipt, and green.

## The lesson this branch paid for four times

Elements 10 and 12 both came back **green with the guarded thing neutralized**, and in both cases the
cause was a fixture manufacturing a state the production path cannot produce — a census that never
reached the late-assignment sites, and a run directory whose prompt was never decorated. The rule
carried out of it, and now enforced in the fixtures themselves: **a refusal's proof owes a positive
leg on a realistic artifact, and the fixture owes an assertion that it actually created the
discriminating condition.** A probe that comes back green where your own reasoning says red is a
defect in the fixture, never a reason to relax the assertion.
