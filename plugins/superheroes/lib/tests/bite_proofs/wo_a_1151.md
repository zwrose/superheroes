# WO-A (#1151) bite-proofs — session-mode SSOT

## BP-A1 — fail direction (`UNRESOLVED_MODE`)

**Guarded element:** `session_mode.UNRESOLVED_MODE` — axis: unresolved inputs default to PR, not branch.

**Neutralization:** changed `UNRESOLVED_MODE = MODE_PR` to `UNRESOLVED_MODE = MODE_BRANCH` in `plugins/superheroes/lib/session_mode.py` only.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-a -m pytest plugins/superheroes/lib/tests/test_session_mode.py::test_resolve_unresolved_when_both_empty plugins/superheroes/lib/tests/test_round_orders.py::test_panel_order_modeless_session_defaults_to_pr_with_disclosure -q
```

**Red run:**
```
FF                                                                       [100%]
=================================== FAILURES ===================================
___________________ test_resolve_unresolved_when_both_empty ____________________

    def test_resolve_unresolved_when_both_empty():
>       _assert_unresolved(sm.resolve({}, {}), "not set")

plugins/superheroes/lib/tests/test_session_mode.py:43: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

result = {'disclosure': 'Review session mode was not set in session metadata or driver configuration; defaulting to PR review.', 'evidence': 'unresolved', 'mode': 'branch', 'resolved': False}
disclosure_substr = 'not set'

    def _assert_unresolved(result, disclosure_substr=None):
>       assert result["mode"] == sm.MODE_PR
E       AssertionError: assert 'branch' == 'pr'
E         
E         - pr
E         + branch

plugins/superheroes/lib/tests/test_session_mode.py:17: AssertionError
_______ test_panel_order_modeless_session_defaults_to_pr_with_disclosure _______

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-9349/test_panel_order_modeless_sess0')

    def test_panel_order_modeless_session_defaults_to_pr_with_disclosure(tmp_path):
        ...
>       assert ph["MODE"] == "pr"
E       AssertionError: assert 'branch' == 'pr'
E         
E         - pr
E         + branch

plugins/superheroes/lib/tests/test_round_orders.py:920: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_session_mode.py::test_resolve_unresolved_when_both_empty
FAILED plugins/superheroes/lib/tests/test_round_orders.py::test_panel_order_modeless_session_defaults_to_pr_with_disclosure
2 failed in 0.23s
```

**Restore:** reverted line to `UNRESOLVED_MODE = MODE_PR` (inverse of neutralization).

**Restore receipt:** quoted restored line: `UNRESOLVED_MODE = MODE_PR`

**Green run:**
```
..                                                                       [100%]
2 passed in 0.23s
```

---

## BP-A2 — chokepoint pin (`round_driver` fail-open)

**Guarded element:** `round_driver._order_placeholders` MODE expression — axis: no `or "branch"` fail-open default for session mode.

**Neutralization:** replaced `"MODE": mode_resolved["mode"]` with `"MODE": meta.get("mode") or cfg.get("mode") or "branch"` in `plugins/superheroes/lib/round_driver.py` (left `MODE_EVIDENCE` on resolved path).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-a -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_round_driver_no_fail_open_branch_default_for_session_mode plugins/superheroes/lib/tests/test_round_orders.py::test_panel_order_modeless_session_defaults_to_pr_with_disclosure -q
```

**Red run:**
```
FF                                                                       [100%]
=================================== FAILURES ===================================
________ test_round_driver_no_fail_open_branch_default_for_session_mode ________

    def test_round_driver_no_fail_open_branch_default_for_session_mode():
        """Chokepoint census: round_driver must not default session mode to branch."""
        text = _read("lib/round_driver.py")
>       assert 'or "branch"' not in text
E       assert 'or "branch"' not in '#!/usr/bin/...it(main())\n'
E         
E         'or "branch"' is contained here:
E           t("mode") or "branch",
E                       "MODE_EVIDENCE": session_mode.evidence_line(mode_resolved),

plugins/superheroes/lib/tests/test_ssot_drift.py:5230: AssertionError
_______ test_panel_order_modeless_session_defaults_to_pr_with_disclosure _______

    ...
>       assert ph["MODE"] == "pr"
E       AssertionError: assert 'branch' == 'pr'
E         
E         - pr
E         + branch

plugins/superheroes/lib/tests/test_round_orders.py:920: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_round_driver_no_fail_open_branch_default_for_session_mode
FAILED plugins/superheroes/lib/tests/test_round_orders.py::test_panel_order_modeless_session_defaults_to_pr_with_disclosure
2 failed in 0.42s
```

**Restore:** reverted MODE binding to `"MODE": mode_resolved["mode"]` (inverse of neutralization).

**Restore receipt:** quoted restored lines:
```
        mode_resolved = session_mode.resolve(meta, cfg)
        ph = {
            "MODE": mode_resolved["mode"],
            "MODE_EVIDENCE": session_mode.evidence_line(mode_resolved),
```

**Green run:**
```
..                                                                       [100%]
2 passed in 0.42s
```
