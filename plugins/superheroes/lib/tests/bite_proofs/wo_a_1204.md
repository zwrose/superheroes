# WO-A (#1204) bite-proof — per-round `seatMapUnjudgeable` round-scoped projection

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-A1 | Call site uses `_sm_round_governing_unjudgeable` | Round 2 record must clear when its own map is judgeable despite earlier bad receipt | `test_seat_map_unjudgeable_per_round_unjudgeable_then_judgeable` |
| BP-A2 | `_clear_round` on empty bases | Double fold bad→good must remove stale `seatMapUnjudgeable` | `test_seat_map_unjudgeable_double_fold_bad_then_good_clears_record` |
| BP-A3 | Own-submission-wins selector | Round's last receipt beats `effective_seat_map` | `test_round_governing_unjudgeable_own_submission_wins_over_effective_map` |
| BP-A4 | Effective-map fallback | No own submission → judge `effective_seat_map` | `test_seat_map_unjudgeable_no_submission_judged_on_effective_map` |
| BP-A5 | `unjudgeable_receipts` whole-history | All unjudgeable receipts returned, not last-only | `test_unjudgeable_receipts_whole_history_not_round_scoped` |

---

## BP-A1 — round scoping at call site

- **axis:** per-round record reflects governing map, not whole-history union

**neutralization** (point call site back at whole-history reader):
```python
    _unjudgeable = _sm_unjudgeable_receipts(state, _driver_author_family(state))
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_seat_map_unjudgeable_per_round_unjudgeable_then_judgeable ________
plugins/superheroes/lib/tests/test_round_driver.py:5332: in test_seat_map_unjudgeable_per_round_unjudgeable_then_judgeable
    assert "seatMapUnjudgeable" not in state["rounds"]["2"]
E   AssertionError: assert 'seatMapUnjudgeable' not in {'canaryVerified': {}, 'compileDrops': [], 'fellOpenProvenanceMissing': ['premortem-reviewer'], 'lensCoverage': {'expected': 5, 'floor': False, 'ran': 5}, ...}
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_per_round_unjudgeable_then_judgeable
1 failed in 5.14s
```

**restore:** `_unjudgeable = _sm_round_governing_unjudgeable(state, state["round"], _driver_author_family(state))`

---

## BP-A2 — `_clear_round` on the call site

- **axis:** channel cleared when bases empty after recomputation

**neutralization** (restore `if _bases:`-only form — no `_clear_round`):
```python
    _unjudgeable = _sm_round_governing_unjudgeable(
        state, state["round"], _driver_author_family(state))
    if _unjudgeable:
        _bases = sorted({entry["basis"] for entry in _unjudgeable if isinstance(entry, dict)})
        if _bases:
            _record_round(state, "seatMapUnjudgeable", _bases)
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_seat_map_unjudgeable_double_fold_bad_then_good_clears_record _______
plugins/superheroes/lib/tests/test_round_driver.py:5370: in test_seat_map_unjudgeable_double_fold_bad_then_good_clears_record
    assert "seatMapUnjudgeable" not in state["rounds"]["1"]
E   AssertionError: assert 'seatMapUnjudgeable' not in {'canaryVerified': {}, 'compileDrops': [], 'fellOpenProvenanceMissing': ['premortem-reviewer'], 'lensCoverage': {'expected': 5, 'floor': False, 'ran': 5}, ...}
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_double_fold_bad_then_good_clears_record
1 failed in 6.60s
```

**restore:** settle channel with `_bases` / `_clear_round` else branch (landed form).

---

## BP-A3 — own-submission-wins

- **axis:** round's own last receipt wins over `effective_seat_map`

**neutralization** (always use `effective_seat_map`, ignore own submission):
```python
    round_label = str(round_id)
    selected_map = effective_seat_map(state)
    selected_round_label = round_label
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
___ test_round_governing_unjudgeable_own_submission_wins_over_effective_map ____
plugins/superheroes/lib/tests/test_seat_map_receipts.py:366: in test_round_governing_unjudgeable_own_submission_wins_over_effective_map
    assert SMR.round_governing_unjudgeable(state, "2", driver_fam) == [
E   AssertionError: assert [] == [{'basis': 'n...'round': '2'}]
E     
E     Right contains one more item: {'basis': 'no-seats', 'round': '2'}
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_map_receipts.py::test_round_governing_unjudgeable_own_submission_wins_over_effective_map
1 failed in 2.19s
```

**restore:** own-submission loop + `effective_seat_map` fallback (landed form).

---

## BP-A4 — effective-map fallback

- **axis:** round with no own submission judged on governing effective map

**neutralization** (return `[]` when round submitted nothing):
```python
    if selected_map is None:
        return []
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______ test_seat_map_unjudgeable_no_submission_judged_on_effective_map ________
plugins/superheroes/lib/tests/test_round_driver.py:5404: in test_seat_map_unjudgeable_no_submission_judged_on_effective_map
    assert state["rounds"]["2"]["seatMapUnjudgeable"] == [SM.VIOLATION_BASIS_NO_AUTHOR_FAMILY]
E   KeyError: 'seatMapUnjudgeable'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_seat_map_unjudgeable_no_submission_judged_on_effective_map
1 failed in 2.39s
```

**restore:** `selected_map = effective_seat_map(state)` when no own submission (landed form).

---

## BP-A5 — whole-history reader untouched

- **axis:** `unjudgeable_receipts` returns every unjudgeable receipt

**neutralization** (narrow to last receipt only):
```python
    receipt_list = receipts(state)
    if not receipt_list:
        return []
    entry = receipt_list[-1]
    basis = seat_map.violation_basis(entry["map"], driver_author_family)
    if basis == seat_map.VIOLATION_BASIS_COMPLETE:
        return []
    return [{"round": str(entry.get("round", "")), "basis": basis}]
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_unjudgeable_receipts_whole_history_not_round_scoped ___________
plugins/superheroes/lib/tests/test_seat_map_receipts.py:385: in test_unjudgeable_receipts_whole_history_not_round_scoped
    assert len(result) == 2
E   AssertionError: assert 1 == 2
E    +  where 1 = len([{'basis': 'no-seats', 'round': '3'}])
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_map_receipts.py::test_unjudgeable_receipts_whole_history_not_round_scoped
1 failed in 1.86s
```

**restore:** loop all receipts (landed form).

---

## Green batch (all five proving tests, exit 0)

```
.....                                                                    [100%]
5 passed in 6.28s
```

Post-restore `git status --porcelain` showed only the four intended deliverable paths modified (no probe residue).
