# WO-G (#1151) bite-proofs — fixture value quality + compose degrading-detail coverage

## BP-G1 — non-panel stub value (strong-form drift pin)

**Guarded element:** `test_placeholder_fixture_known_table_covers_every_template_placeholder` — axis: every direct template placeholder must have a non-stub, nonempty fixture value across all read-only channel phases (not only the four panel names).

**Neutralization:** in `plugins/superheroes/lib/tests/test_round_driver_order_channel.py`, changed `_KNOWN_REALISTIC_VALUES[RD.P_VERIFIERS]["CLUSTER_FINDINGS_PATH"]` from `SESSION + "/round-1/clusters/0.json"` to `"STUB:CLUSTER_FINDINGS_PATH"`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-g -m pytest 'plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder' -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____ test_placeholder_fixture_known_table_covers_every_template_placeholder ____

    def test_placeholder_fixture_known_table_covers_every_template_placeholder():
        ...
>       assert not stub_values, "template placeholder(s) with stub or empty fixture value: %s" % stub_values
E       AssertionError: template placeholder(s) with stub or empty fixture value: ["dispatch-verifiers:CLUSTER_FINDINGS_PATH='STUB:CLUSTER_FINDINGS_PATH'"]
E       assert not ["dispatch-verifiers:CLUSTER_FINDINGS_PATH='STUB:CLUSTER_FINDINGS_PATH'"]

plugins/superheroes/lib/tests/test_round_driver_order_channel.py:700: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder
1 failed in 0.13s
```

**Restore:** changed `_KNOWN_REALISTIC_VALUES[RD.P_VERIFIERS]["CLUSTER_FINDINGS_PATH"]` back to `SESSION + "/round-1/clusters/0.json"`.

**Restore receipt:** `"CLUSTER_FINDINGS_PATH": SESSION + "/round-1/clusters/0.json",` in the `RD.P_VERIFIERS` block of `_KNOWN_REALISTIC_VALUES`.

**Green run (exit 0):**
```
1 passed in 0.12s
```

---

## BP-G2 — empty fixture value (nonempty half)

**Guarded element:** `test_placeholder_fixture_known_table_covers_every_template_placeholder` — axis: direct placeholder values must be nonempty strings, not only non-stub.

**Neutralization:** in `plugins/superheroes/lib/tests/test_round_driver_order_channel.py`, changed `_KNOWN_REALISTIC_VALUES[RD.P_PANEL]["MODE"]` from `"branch"` to `""`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-g -m pytest 'plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder' -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____ test_placeholder_fixture_known_table_covers_every_template_placeholder ____

    def test_placeholder_fixture_known_table_covers_every_template_placeholder():
        ...
>       assert not stub_values, "template placeholder(s) with stub or empty fixture value: %s" % stub_values
E       AssertionError: template placeholder(s) with stub or empty fixture value: ["dispatch-panel:MODE=''"]
E       assert not ["dispatch-panel:MODE=''"]

plugins/superheroes/lib/tests/test_round_driver_order_channel.py:700: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder
1 failed in 0.13s
```

**Restore:** changed `_KNOWN_REALISTIC_VALUES[RD.P_PANEL]["MODE"]` back to `"branch"`.

**Restore receipt:** `"MODE": "branch",` in the `RD.P_PANEL` block of `_KNOWN_REALISTIC_VALUES`.

**Green run (exit 0):**
```
1 passed in 0.13s
```

---

## BP-G3 — derived degrading-detail row set (evidence-unreadable gap)

**Guarded element:** `_COMPOSE_SKEW_APPEND_CASES` derivation from `version_skew.DEGRADING_DETAILS` — axis: every home-declared degrading detail must have a compose parametrization row.

**Neutralization:** in `plugins/superheroes/lib/tests/test_seat_map.py`, filtered `DETAIL_EVIDENCE_UNREADABLE` out of the list comprehension building positive rows (simulating a hand-typed partial set).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-g -m pytest 'plugins/superheroes/lib/tests/test_seat_map.py::test_compose_skew_record_appends_degradation_only_when_degraded' -q
```

**Red run (exit 4 — collection error):**
```
ERROR: found no collectors for /private/tmp/sh1151-g/plugins/superheroes/lib/tests/test_seat_map.py::test_compose_skew_record_appends_degradation_only_when_degraded


==================================== ERRORS ====================================
_______ ERROR collecting plugins/superheroes/lib/tests/test_seat_map.py ________
plugins/superheroes/lib/tests/test_seat_map.py:2150: in <module>
    assert frozenset(
E   AssertionError: assert frozenset({'semantics-divergent'}) == frozenset({'evidence-unreadable', 'semantics-divergent'})
E    +  where frozenset({'semantics-divergent'}) = frozenset(<generator object <genexpr> at 0x1045fc9e0>)
E    +  and   frozenset({'evidence-unreadable', 'semantics-divergent'}) = <module 'version_skew' from '/private/tmp/sh1151-g/plugins/superheroes/lib/tests/../version_skew.py'>.DEGRADING_DETAILS
=========================== short test summary info ============================
ERROR plugins/superheroes/lib/tests/test_seat_map.py - AssertionError: assert...
1 error in 0.11s
```

**Restore:** restored list comprehension to `for degrading_detail in sorted(version_skew.DEGRADING_DETAILS)` (inverse of neutralization).

**Restore receipt:** positive rows derive from the full `version_skew.DEGRADING_DETAILS` set; module-level assert matches that set.

**Green run (exit 0):**
```
4 passed in 0.10s
```

---

## BP-G4 — detail preservation assertion (not append-only)

**Guarded element:** `assert skew[0]["detail"] == detail` in `test_compose_skew_record_appends_degradation_only_when_degraded` — axis: appended degradation records preserve the degrading detail, not merely presence.

**Neutralization:** in the test's `_fake_detect`, changed `"detail": detail` to `"detail": version_skew.DETAIL_SEMANTICS_DIVERGENT` (simulates compose appending a record whose detail does not match the detected degrading detail). `seat_map.py` was not edited (out of scope).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-g -m pytest 'plugins/superheroes/lib/tests/test_seat_map.py::test_compose_skew_record_appends_degradation_only_when_degraded[checked-degraded-evidence-unreadable-True]' -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_compose_skew_record_appends_degradation_only_when_degraded[checked-degraded-evidence-unreadable-True] _

    ...
        if should_append:
            assert len(skew) == 1
            assert skew[0]["status"] == status
>           assert skew[0]["detail"] == detail
E           AssertionError: assert 'semantics-divergent' == 'evidence-unreadable'
E            +  where 'evidence-unreadable' = detail

plugins/superheroes/lib/tests/test_seat_map.py:2194: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_seat_map.py::test_compose_skew_record_appends_degradation_only_when_degraded[checked-degraded-evidence-unreadable-True]
1 failed in 0.13s
```

**Restore:** changed `_fake_detect` return dict back to `"detail": detail`.

**Restore receipt:** `"detail": detail,` in the `_fake_detect` return dict inside `test_compose_skew_record_appends_degradation_only_when_degraded`.

**Green run (exit 0):**
```
1 passed in 0.10s
```
