# C14 layer 3b review round 1 — bite proofs (#1273 WO-R1)

| ID | Guarded element | Axis | Detector |
|----|-----------------|------|----------|
| BP-R1-1 | `receipt_disclosures._native_in_session_seats` | per-event round governing map | `test_native_in_session_disclosure_uses_round_governing_map_not_latest` |
| BP-R1-2 | `seat_map.reachable_configs._add_cell` | matrix cell in needed set beside pin | `test_reachable_configs_includes_role_pinned_cell` |
| BP-R1-3 | `seat_map.build` role-pin disclosure site | honest once-at-final-seating | `test_codex_role_pin_no_degradation_when_seat_not_on_codex` |
| BP-R1-4 | `seat_map.main` claude-engine author family | unknown host falls back to claude family | `test_cli_compose_host_model_unknown_degrades` |

## BP-R1-1

- **neutralization:** `_native_in_session_seats` uses `effective_seat_map(state)` once for all events.
- **red:** `test_native_in_session_disclosure_uses_round_governing_map_not_latest` — `AssertionError: assert False` (no unprobed-native line).
- **restore:** per-event `round_governing_map(state, round_label)` lookup restored.
- **green:** `1 passed in 0.13s`.

## BP-R1-2

- **neutralization:** `_add_cell` resolves through `_cell(tier, vendor, codex_role_pins)`.
- **order-named detector:** `test_codex_role_pin_reviewer_not_live_seats_matrix_with_one_degradation` stays green — **Unprovable as placed** through that entry point (explicit `live_cells` bypasses `reachable_configs`).
- **red (axis):** `test_reachable_configs_includes_role_pinned_cell` — `AssertionError: assert ['gpt-5.6-terra', 'high'] in [['gpt-5.6-sol', 'high'], ...]`.
- **restore:** `_add_cell` uses `_cell(tier, vendor, None)`.
- **green:** `1 passed in 0.14s`.

## BP-R1-3

- **neutralization:** role-pin degradations recorded inside `_resolve_at_tier`; post-seating block removed.
- **red:** `test_codex_role_pin_no_degradation_when_seat_not_on_codex` — spurious `role-pin-not-live` on `grounding-seat` while final vendor is not codex.
- **restore:** degradations removed from `_resolve_at_tier`; post-seating `role_pin_degraded` loop restored.
- **green:** `1 passed in 0.14s`.

## BP-R1-4

- **neutralization:** `author_family = host_fam` for claude engine (no claude-family fallback).
- **red:** `test_cli_compose_host_model_unknown_degrades` — `AssertionError: assert None == 'anthropic'`.
- **restore:** `author_family = host_fam or model_registry.family_for("code-fixer", "claude")`.
- **green:** `1 passed in 0.16s`.
