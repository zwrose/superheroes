# WO-L4A-C (#1273) bite-proof — started background fields fold in launch ledger

**Covers:** WO-C (launch ledger reads background-shaped `started` records; layer 4a).

**Provenance:** cursor / composer-2.5 (implementer).

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-C1 | leg (b), all-or-none | partial background field sets refuse before fold | `test_started_background_partial_set_refuses[partial_fields2]` |
| BP-C2 | leg (c) | launchMode must be in LAUNCH_MODES | `test_started_background_invalid_launch_mode_refuses` |
| BP-C3 | leg (d) | backgroundId must match `^[0-9a-f]{8}$` | `test_started_background_invalid_background_id_refuses` |
| BP-C4 | leg (e) | sessionId must parse as UUID | `test_started_background_invalid_session_id_refuses` |
| BP-C5 | leg (f), relational prefix | sessionId must begin with backgroundId | `test_started_background_session_id_prefix_mismatch_refuses` |
| BP-C6 | fold exposes `started.sessionId` | current reader copies sessionId from started | `test_current_reader_folds_background_session_id` |
| BP-C7 | started repair carries fields | terminalize repair copies background fields | `test_terminalize_repair_carries_background_fields` |
| BP-C8 | D5 compat leg | — | owned by WO-B |

---

## BP-C1 — all-or-none partial-set refusal

- **axis:** partial background field sets refuse with `fold-bad-field:started:background`

**neutralization** (`plugins/superheroes/lib/launch_ledger.py`, `_validate_event_fields` started branch):

before:
```python
        if not present:
            pass
        elif len(present) != 3:
            return "fold-bad-field:started:background"
        else:
```

after:
```python
        if len(present) == 3:
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-wo-C -m pytest 'plugins/superheroes/lib/tests/test_launch_ledger.py::test_started_background_partial_set_refuses[partial_fields2]' -q
```

**raw red** (exit 1): `AssertionError: assert True is False` — fold returned `ok: True` for `{"backgroundId", "sessionId"}` partial set; validation no longer refuses before fold.

**restore:** reinstate the `if not present` / `elif len(present) != 3` / `else` structure.

**restore receipt:** production validation branch restored to pre-neutralization text.

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 0.42s
```

---

## BP-C2 — launchMode enum

- **axis:** launchMode must be in LAUNCH_MODES

**neutralization:**
```python
            if False:
                return "fold-bad-field:started:launchMode"
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-wo-C -m pytest plugins/superheroes/lib/tests/test_launch_ledger.py::test_started_background_invalid_launch_mode_refuses -q
```

**raw red** (exit 1): `AssertionError: assert True is False`

**restore:** reinstate `if launch_mode not in LAUNCH_MODES:`

**restore receipt:** production branch restored.

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 0.35s
```

---

## BP-C3 — backgroundId shape

- **axis:** backgroundId must match `^[0-9a-f]{8}$`

**neutralization:**
```python
            if False:
                return "fold-bad-field:started:backgroundId"
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-wo-C -m pytest plugins/superheroes/lib/tests/test_launch_ledger.py::test_started_background_invalid_background_id_refuses -q
```

**raw red** (exit 1): `AssertionError: assert True is False`

**restore:** reinstate backgroundId shape check.

**restore receipt:** production branch restored.

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 0.40s
```

---

## BP-C4 — sessionId UUID parse

- **axis:** sessionId must parse as UUID

**neutralization:** replace UUID parse block with `pass`.

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-wo-C -m pytest plugins/superheroes/lib/tests/test_launch_ledger.py::test_started_background_invalid_session_id_refuses -q
```

**raw red** (exit 1): `AssertionError: assert True is False`

**restore:** reinstate `uuid.UUID(session_id)` try/except block.

**restore receipt:** production branch restored.

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 0.43s
```

---

## BP-C5 — sessionId prefix relational leg

- **axis:** sessionId must begin with backgroundId

**neutralization:**
```python
            if False:
                return "fold-bad-field:started:background"
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-wo-C -m pytest plugins/superheroes/lib/tests/test_launch_ledger.py::test_started_background_session_id_prefix_mismatch_refuses -q
```

**raw red** (exit 1): `AssertionError: assert True is False`

**restore:** reinstate `if not session_id.startswith(background_id):`

**restore receipt:** production branch restored.

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 0.34s
```

---

## BP-C6 — fold copies started.sessionId

- **axis:** current reader exposes started.sessionId on the folded lane

**neutralization** (`fold`, started branch):
```python
                pass
```
replacing `info["sessionId"] = rec["sessionId"]`.

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-wo-C -m pytest plugins/superheroes/lib/tests/test_launch_ledger_compat.py::test_current_reader_folds_background_session_id -q
```

**raw red** (exit 1): `AssertionError: assert None == 'feac172f-474c-424d-86e7-0e50688972c9'`

**restore:** reinstate `info["sessionId"] = rec["sessionId"]`.

**restore receipt:** production fold branch restored.

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 0.07s
```

---

## BP-C7 — repair carries background fields

- **axis:** terminalize repair copies launchMode, backgroundId, sessionId onto repaired started record

**neutralization** (`terminalize`, repair record construction):
```python
                pass
```
replacing the `if "launchMode" in started_repair:` copy block.

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-wo-C -m pytest plugins/superheroes/lib/tests/test_launch_ledger.py::test_terminalize_repair_carries_background_fields -q
```

**raw red** (exit 1): `KeyError: 'launchMode'` on repaired record assertion

**restore:** reinstate background field copy block in `terminalize`.

**restore receipt:** production repair branch restored.

**raw green** (exit 0):
```
.                                                                        [100%]
1 passed in 0.94s
```

---

## Bite-proof summary

| ID | red exit | green exit |
|---|---|---|
| BP-C1 | 1 | 0 |
| BP-C2 | 1 | 0 |
| BP-C3 | 1 | 0 |
| BP-C4 | 1 | 0 |
| BP-C5 | 1 | 0 |
| BP-C6 | 1 | 0 |
| BP-C7 | 1 | 0 |
