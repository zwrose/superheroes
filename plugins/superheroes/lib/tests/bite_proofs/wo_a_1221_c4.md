# Bite-proof record — WO-A (#1221 continuation): identity title segment never empty

**Branch:** `wo/1221c4-a`. **Produced by implementer dispatch WO-A.**

**Method.** Neutralize the guarded element with a **targeted, revertible edit** — never a whole-file
rewrite, never a git discard — run the **named** test node (exact id; never `-k`), record the runner's
**own** exit code and which parametrized cases went red, restore with the **inverse edit**, re-run,
and confirm the restore. Every red below ran with the **detector unedited**.

---

## G1 — normalized-empty title renders `(no title)` sentinel

**Guarded element.** `round_driver.py`, `_gate_guidance_identity_line`: after normalization, an empty
title segment must render `(no title)` rather than a blank segment.

**Axis.** Title segment content on the rendered identity line `### <file>:<line> — <title>`.

**Neutralization.** Restore pre-fix ordering: evaluate `entry.get("title") or "(no title)"` before
calling `_normalize_gate_guidance_header_field` (inverse of the WO-A fix).

**Command.**

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/sh-pyc-woa -m pytest \
  "plugins/superheroes/lib/tests/test_order_input_contract.py::test_gate_guidance_identity_line_empty_title_renders_no_title_sentinel" -q
```

**RED — runner exit 1**

```
FFFF..                                                                   [100%]
=================================== FAILURES ===================================
_ test_gate_guidance_identity_line_empty_title_renders_no_title_sentinel[spaces] _
...
E       AssertionError: assert '### f.py:1 — ' == '### f.py:1 — (no title)'
...
_ test_gate_guidance_identity_line_empty_title_renders_no_title_sentinel[mixed-ws] _
...
E       AssertionError: assert '### f.py:1 — ' == '### f.py:1 — (no title)'
...
_ test_gate_guidance_identity_line_empty_title_renders_no_title_sentinel[nul] __
...
E       AssertionError: assert '### f.py:1 — ' == '### f.py:1 — (no title)'
...
_ test_gate_guidance_identity_line_empty_title_renders_no_title_sentinel[line-sep] _
...
E       AssertionError: assert '### f.py:1 — ' == '### f.py:1 — (no title)'
...
=========================== short test summary info ============================
FAILED …::test_gate_guidance_identity_line_empty_title_renders_no_title_sentinel[spaces]
FAILED …::test_gate_guidance_identity_line_empty_title_renders_no_title_sentinel[mixed-ws]
FAILED …::test_gate_guidance_identity_line_empty_title_renders_no_title_sentinel[nul]
FAILED …::test_gate_guidance_identity_line_empty_title_renders_no_title_sentinel[line-sep]
4 failed, 2 passed in 1.08s
```

**Cases red:** `spaces`, `mixed-ws`, `nul`, `line-sep` — includes the control-character and
line-separator routes the brief named. **Cases green under mutant:** `empty`, `none` (pre-existing
falsy fallback still applied before normalization).

**Restore** — inverse edit (normalize first, then `(no title)` when normalized result empty).

**Restored lines in `round_driver.py` (`_gate_guidance_identity_line`):**

```python
    raw = entry.get("title")
    title = _normalize_gate_guidance_header_field(raw if raw is not None else "")
    if not title:
        title = "(no title)"
```

**GREEN — runner exit 0:** `6 passed in 0.95s`.

**`git status --porcelain` after restore** (pre-commit dispatch; deliverable edits still unstaged):

```
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_order_input_contract.py
```

No neutralization residue — the green run confirms `round_driver.py` matches the fix head; porcelain
lists only the intentional WO deliverables still awaiting commit.

---

## G2 — ordinary title survives normalization unchanged into identity line

**Guarded element.** `test_order_input_contract.py`,
`test_gate_guidance_identity_line_ordinary_title_renders_title`: a non-empty ordinary title must
appear unchanged on the rendered identity line after normalization.

**Axis.** Title segment content on the rendered identity line `### <file>:<line> — <title>` for a
non-sentinel title.

**Construction bound.** Neutralization is confined to the test assertion (not `round_driver.py` per
scope fence). Mutating the expected string proves this detector can fail when its expectation is
wrong; it does not independently prove production normalization — that axis is covered by G1's
production neutralization.

**Neutralization.** Change the assertion's expected identity line to a wrong title string.

**Command.**

```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/sh-pyc-wod -m pytest \
  "plugins/superheroes/lib/tests/test_order_input_contract.py::test_gate_guidance_identity_line_ordinary_title_renders_title" -q
```

**RED — runner exit 1**

```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_gate_guidance_identity_line_ordinary_title_renders_title _________

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/com.apple.shortcuts.mac-helper/pytest-of-zwrose/pytest-36/test_gate_guidance_identity_li0')

    def test_gate_guidance_identity_line_ordinary_title_renders_title(tmp_path):
        state = _fixer_guidance_state(
            tmp_path,
            [_guidance_disposition("f.py::widen the api@L1", "widen the API", "guidance body",
                                  file="f.py", line=1)],
        )
        ph, _session_dir, _paths = _fixer_order_ph(tmp_path, state, session_name="ordinary-title")
        identity = [ln for ln in ph["GATE_GUIDANCE"].splitlines() if ln.startswith("### ")][0]
>       assert identity == "### f.py:1 — wrong title"
E       AssertionError: assert '### f.py:1 — widen the API' == '### f.py:1 — wrong title'
E         
E         - ### f.py:1 — wrong title
E         + ### f.py:1 — widen the API

plugins/superheroes/lib/tests/test_order_input_contract.py:293: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_order_input_contract.py::test_gate_guidance_identity_line_ordinary_title_renders_title
1 failed in 0.69s
```

**Restore** — inverse edit (restore expected identity line).

**Restored line in `test_order_input_contract.py`:**

```python
    assert identity == "### f.py:1 — widen the API"
```

**GREEN — runner exit 0:** `1 passed in 0.68s`.

**`git status --porcelain` after restore:**

```
 M plugins/superheroes/lib/tests/test_order_input_contract.py
 M plugins/superheroes/skills/review-code/SKILL.md
 M plugins/superheroes/skills/review-code/reference/auto-fix-loop.md
```

No neutralization residue — assertion matches production render.
