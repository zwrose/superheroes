# WO-A l4b bite-proof — control probe must not gate certification (#1272)

Four guarded elements; detector tests unedited throughout.

## G1 — probe-derived gap in `incomplete`

**Guarded element:** `round_driver._fold_panel` — `incomplete = bool(missing_dims)` (no probe-derived gap).

**Axis:** Plant-undetected / unproven / dead probe outcomes must not mark the panel incomplete.

**Neutralization:** After `controlProbe` record, replace `incomplete = bool(missing_dims)` with `_probe_gap` over `live["byVendor"]` statuses in `dead`, `outcome-failed`, `unproven`, `OUTCOME_PLANT_UNDETECTED`, then `incomplete = bool(missing_dims) or _probe_gap`.

**Red** (`/tmp/l4b-C-3.txt`, `test_probe_states_do_not_change_verdict_surface`):

```
>       assert baseline["fullPanelRan"] is True
E       assert False is True
```

(All six parametrized cases failed, including `absent`, on the absolute anchor.)

**Restore:** `incomplete = bool(missing_dims)` only (remove `_probe_gap` block).

**Green** (`/tmp/l4b-C-4.txt`): `6 passed in 0.85s`

---

## G2 — dead-probe seat downgrade

**Guarded element:** `round_driver._fold_panel` — no downgrade of `seat_status` to `missing` for dead canary vendors.

**Axis:** A dead / not-engaged probe must not change `seatStatus`.

**Neutralization:** After the `by_vendor` classification loop, re-insert:

```python
    for dim in _canary_dims_for_status(live, by_vendor, "dead"):
        seat_status[dim] = "missing"
        if dim not in missing_dims:
            missing_dims.append(dim)
```

**Red** (`/tmp/l4b-C-5.txt`, `test_probe_states_do_not_change_verdict_surface[not-engaged]`):

```
>           assert probe[key] == baseline[key], key
E           AssertionError: seatStatus
E           {'code-reviewer': 'missing'} != {'code-reviewer': 'run'}
```

**Restore:** Remove the `for dim in _canary_dims_for_status(..., "dead")` block.

**Green** (`/tmp/l4b-C-6.txt`): `1 passed in 0.88s`

---

## G3 — canary degraded-prose arm

**Guarded element:** `receipt_disclosures.build_degraded_prose` — no `canary-plant-undetected` degraded line.

**Axis:** `canaryPlantUndetected` on the round must not surface in receipt `degraded` prose.

**Neutralization:** After `_probe_record_only_declared(declared)`, append when `declared.get("canaryPlantUndetected")` is truthy:

```python
            degraded_out.append(
                "canary-plant-undetected (round %s): probe engaged but plant not detected"
                % rkey)
```

**Red** (`/tmp/l4b-C-7.txt`, `test_dry_run_b_plant_undetected_certifies_via_writer_no_canary_degraded_line`):

```
>       assert not any("canary-" in line for line in receipt["degraded"])
E       assert not True
```

**Restore:** Remove the `canaryPlantUndetected` `degraded_out.append` arm.

**Green** (`/tmp/l4b-C-8.txt`): `1 passed in 0.87s`

---

## G4 — adapter refusal on malformed probe

**Guarded element:** `round_adapters._normalize_canary` — malformed list entries pass through instead of refusing assembly.

**Axis:** A dict probe without string `engine` must not refuse at assemble time.

**Neutralization:** In the list branch, before appending each probe:

```python
        if isinstance(probe, dict) and not isinstance(probe.get("engine"), str):
            return None, "canary-entry-has-no-engine:index-%d" % index
```

**Red** (`/tmp/l4b-C-9.txt`):

```
>       assert reason is None and artifact is not None
E       AssertionError: assert ('canary-entry-has-no-engine:index-1' is None)
```

(`test_malformed_canary_assemble_not_refused` and `test_edge_canary_entry_with_no_engine`.)

**Restore:** List branch appends probes without the engine check.

**Green** (`/tmp/l4b-C-10.txt`): `2 passed in 2.51s`

---

## G5 — fossil check still bites on record-only membership

**Guarded element:** `RECORD_ONLY_DISCLOSURE_CHANNELS` — `controlProbe` must stay listed so the fossil census accepts it only via the record-only path.

**Axis:** Removing a probe channel from the record-only set must make `test_disclosure_channels_have_one_home_read_by_receipt_and_resume` fail as a fossil.

**Neutralization:** Remove `"controlProbe"` from `RECORD_ONLY_DISCLOSURE_CHANNELS` in `receipt_disclosures.py`.

**Red** (`/tmp/l4b-D-3.txt`, `test_disclosure_channels_have_one_home_read_by_receipt_and_resume`):

```
>           assert source_obj_accesses_key(src, "rec|rrec|declared", chan), \
E           AssertionError: 'controlProbe' is named restorable but no round record read consumes it
```

**Restore:** Re-add `"controlProbe"` to `RECORD_ONLY_DISCLOSURE_CHANNELS`.

**Green** (`/tmp/l4b-D-4.txt`): `1 passed`

---

## G6 — degraded-prose ast census bites

**Guarded element:** `test_record_only_channels_never_read_in_build_degraded_prose` — no literal `.get()` on record-only channel names inside `build_degraded_prose`.

**Axis:** Reading `canaryPlantUndetected` into degraded prose must fail the ast census.

**Neutralization:** In `build_degraded_prose`'s per-round loop after `declared = receipt_round_disclosures(...)`, insert:

```python
        if declared.get("canaryPlantUndetected"):
            degraded_out.append("canary-plant-undetected (round %s)" % rkey)
```

**Red** (`/tmp/l4b-D-5.txt`, `test_record_only_channels_never_read_in_build_degraded_prose`):

```
E       AssertionError: record-only channels must not be read in build_degraded_prose: ['canaryPlantUndetected']
```

**Restore:** Remove the inserted `canaryPlantUndetected` block.

**Green** (`/tmp/l4b-D-6.txt`): `1 passed`
