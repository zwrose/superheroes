# WO-A l4b bite-proof — control probe must not gate certification

**Guarded element:** `round_driver._fold_panel` incomplete computation (probe-derived gap removed in #1272 l4b).

**Axis:** A plant-undetected control probe must not change `terminal` / certification shape (dry run B).

## Neutralization

In `round_driver._fold_panel`, after `controlProbe` record:

```python
    _probe_gap = False
    for _info in (live.get("byVendor") or {}).values():
        if isinstance(_info, dict) and _info.get("status") in (
                "dead", "outcome-failed", "unproven", canary_outcome.OUTCOME_PLANT_UNDETECTED):
            _probe_gap = True
            break
    incomplete = bool(missing_dims) or _probe_gap
```

(replaces `incomplete = bool(missing_dims)`.)

## Red (`/tmp/l4b-A-5-red.txt`)

```
>       assert state["terminal"] == "converged"
E       AssertionError: assert 'cannot-certify' == 'converged'
```

Summary: `1 failed, 1 passed in 0.65s` (dry run B failed; plant-undetected parametrized case passed).

## Restore

Reverted to:

```python
    incomplete = bool(missing_dims)
```

## Green (`/tmp/l4b-A-6-green.txt`)

```
1 passed in 0.67s
```
