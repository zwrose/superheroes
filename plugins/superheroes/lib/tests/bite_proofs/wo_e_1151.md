# WO-E (#1151) bite-proofs — session-mode resolution home + shell-fence pin

Full pytest stderr (PytestWarning rm_rf noise) for every run: `/private/tmp/wo_e_bite_proofs_1151/`. Failure bodies below omit that tail when it exceeds the bite-proof ceiling; first/last substantive lines are quoted.

## BP-E1 — shell-fence exact-set pin (added value)

**Guarded element:** `test_review_code_skill_shell_mode_assignments_match_session_mode_modes` (T1) — axis: `MODE=` assignments in `skills/review-code/SKILL.md` bash fences must equal `session_mode.MODES` exactly.

**Neutralization:** changed `MODE=pr` to `MODE=pr-review` on `skills/review-code/SKILL.md:63`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-e -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_code_skill_shell_mode_assignments_match_session_mode_modes -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____ test_review_code_skill_shell_mode_assignments_match_session_mode_modes ____
...
E       AssertionError: review-code SKILL.md MODE= assignments ['branch', 'pr-review'] must equal session_mode.MODES ['branch', 'pr']
...
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_code_skill_shell_mode_assignments_match_session_mode_modes
1 failed in 0.17s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e1_red.txt`)

**Restore:** reverted line 63 to `BRANCH=$(git rev-parse --abbrev-ref HEAD); MODE=pr`.

**Restore receipt:** `BRANCH=$(git rev-parse --abbrev-ref HEAD); MODE=pr`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.13s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e1_green.txt`)

---

## BP-E2 — shell-fence exact-set pin (missing value)

**Guarded element:** same T1 test — axis: deleting a fence assignment must go red (not subset-only).

**Neutralization:** removed `; MODE=branch` from `skills/review-code/SKILL.md:105`.

**Command:** same as BP-E1.

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____ test_review_code_skill_shell_mode_assignments_match_session_mode_modes ____
...
E       AssertionError: review-code SKILL.md: expected at least two MODE= assignments in bash fences, found 1 (anti-vacuity)
...
1 failed in 0.18s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e2_red.txt`)

**Restore:** restored line 105 to `BRANCH=$(git rev-parse --abbrev-ref HEAD); MODE=branch`.

**Restore receipt:** `BRANCH=$(git rev-parse --abbrev-ref HEAD); MODE=branch`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.13s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e2_green.txt`)

---

## BP-E3 — anti-vacuity pin (T2)

**Guarded element:** `test_review_code_skill_shell_mode_assignments_anti_vacuity` (T2) — axis: parse must find ≥2 assignments.

**Neutralization:** pointed `_REVIEW_CODE_SKILL_MODE_DOC` at `skills/review-code/reference/setup.md` (no `MODE=` in bash fences).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-e -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_code_skill_shell_mode_assignments_anti_vacuity -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_review_code_skill_shell_mode_assignments_anti_vacuity __________
...
E       AssertionError: review-code SKILL.md: MODE= parse found 0 assignment(s); need >= 2
...
1 failed in 0.16s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e3_red.txt`)

**Restore:** reverted `_REVIEW_CODE_SKILL_MODE_DOC` to `skills/review-code/SKILL.md`.

**Restore receipt:** `_REVIEW_CODE_SKILL_MODE_DOC = "skills/review-code/SKILL.md"`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.13s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e3_green.txt`)

---

## BP-E4 — derivation pin (T3)

**Guarded element:** `test_session_mode_guards_derive_through_resolve` (T3) — axis: `check_base` must call `session_mode.resolve`, not inline `not in SESSION_MODES`.

**Neutralization:** restored inline `if mode not in SESSION_MODES:` in `review_base_guard.check_base` (removed `session_mode.resolve` call).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-e -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_session_mode_guards_derive_through_resolve -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_session_mode_guards_derive_through_resolve ________________
...
>       assert "session_mode.resolve" in check_base_source
E       assert 'session_mode.resolve' in 'def check_base(session_dir, repo_root, prior_pin=None, run=None):\n...
...
1 failed in 0.18s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e4_red.txt`)

**Restore:** reinstated `mode_resolved = session_mode.resolve(meta, None)` / `if not mode_resolved["resolved"]:` block.

**Restore receipt:**
```
    mode_resolved = session_mode.resolve(meta, None)
    if not mode_resolved["resolved"]:
```

**Green run:**
```
.                                                                        [100%]
1 passed in 0.16s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e4_green.txt`)

---

## BP-E5 — branch-disposition constant pin

**Guarded element:** `test_grounding_stage_branch_disposition_uses_mode_branch_constant` — axis: `grounding_stage.stage` must compare via `session_mode.MODE_BRANCH`.

**Neutralization:** changed `meta["mode"] == session_mode.MODE_BRANCH` to `meta["mode"] == "branch"` at `grounding_stage.py:916` (`stage()` only).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-e -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_grounding_stage_branch_disposition_uses_mode_branch_constant -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_grounding_stage_branch_disposition_uses_mode_branch_constant _______
...
E           AssertionError: grounding_stage.stage must compare against session_mode.MODE_BRANCH
...
1 failed in 0.18s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e5_red.txt`)

**Restore:** reverted to `if meta["mode"] == session_mode.MODE_BRANCH:`.

**Restore receipt:** `if meta["mode"] == session_mode.MODE_BRANCH:`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.15s
```
(Full: `/private/tmp/wo_e_bite_proofs_1151/bp_e5_green.txt`)
