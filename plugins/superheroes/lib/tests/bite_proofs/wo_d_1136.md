# WO-D (#1136 carve round) bite-proofs — inverted mode-wiring + content-keyed allowlist

## BP-D1 — `test_bootstrap_skill_does_not_reconcile_mode` (reconcile --mode absence)

**Guarded element:** `skills/review-init/SKILL.md` bootstrap block — axis: bootstrap skills must not invoke `reconcile --mode` (FR-3/#1136).

**Neutralization:** inserted `python3 -B "$ROOT_DIR/lib/review_store.py" reconcile --mode "$LOC"` into the `if [ "$LOCATION" = "none" ]` bootstrap block in `plugins/superheroes/skills/review-init/SKILL.md` (temporary edit; line removed after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_mode_wiring.py::test_bootstrap_skill_does_not_reconcile_mode[review-init]' -q -p no:warnings
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________ test_bootstrap_skill_does_not_reconcile_mode[review-init] ___________

name = 'review-init'

    @pytest.mark.parametrize("name", BOOTSTRAP_NO_RECONCILE_SKILLS)
    def test_bootstrap_skill_does_not_reconcile_mode(name):
        body = _skill(name)
>       assert "reconcile --mode" not in body, (
            f"{name} bootstrap must not invoke reconcile --mode — authoritative mode writes belong "
            "on configure's owner-authorized migration path only (FR-3/#1136)"
        )
E       AssertionError: review-init bootstrap must not invoke reconcile --mode — authoritative mode writes belong on configure's owner-authorized migration path only (FR-3/#1136)
E       assert 'reconcile --mode' not in '---\nname: ... add it. |\n'
E         
E         'reconcile --mode' is contained here:
E           store.py" reconcile --mode "$LOC"
E             PROFILE=$(python3 -B "$ROOT_DIR/lib/review_store.py" create --kind profile --location "$LOC")
E           fi
E           ```
E           ...
E         
E         ...Full output truncated (147 lines hidden), use '-vv' to show

plugins/superheroes/lib/tests/test_mode_wiring.py:30: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_mode_wiring.py::test_bootstrap_skill_does_not_reconcile_mode[review-init]
1 failed in 0.04s
```

**Restore:** removed the inserted `reconcile --mode` line from the review-init bootstrap bash block.

**Restore receipt:** bootstrap bash block in `review-init/SKILL.md` contains no `reconcile --mode` substring.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.02s
```

---

## BP-D2a — `test_retired_presence_premise_literals_census[no human]` (line-shift survives — inverted proof)

**Guarded element:** content-keyed allowlist entry for `skills/review-code/SKILL.md` "no human" line — axis: an edit above the allowlisted line must not stale the key (regression that caused the original failure).

**Proof shape:** inverted — green-under-mutation is the pass. Inserted a blank line above the allowlisted sentence; census must stay green.

**Neutralization:** inserted one extra blank line between the count-summary bullet and the allowlisted terminal-report sentence in `plugins/superheroes/skills/review-code/SKILL.md` (temporary edit; blank line removed after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_retired_presence_premise_literals_census[no human]' -q -p no:warnings -p no:xdist
```

**Green-under-mutation run (pass):**
```
.                                                                        [100%]
1 passed in 0.10s
```

**Restore:** removed the inserted blank line; allowlisted sentence restored to its original position.

**Restore receipt:** the allowlisted sentence is again immediately below the count-summary bullet with a single blank line between them.

---

## BP-D2b — `test_retired_presence_premise_literals_census[no human]` (changed sentence text goes red)

**Guarded element:** same content-keyed allowlist entry — axis: changed line text is no longer allowlisted and must be re-adjudicated.

**Neutralization:** changed `because no human answered the review gate` to `because no human was available at the review gate` on the allowlisted line in `plugins/superheroes/skills/review-code/SKILL.md` (temporary edit; original wording restored after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_retired_presence_premise_literals_census[no human]' -q -p no:warnings -p no:xdist
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_retired_presence_premise_literals_census[no human] ____________

literal = 'no human'

>       assert not hits, (
E       AssertionError: retired presence-premise literal %r found outside the explicit allowlist (#1136). Every hit:
E         skills/review-code/SKILL.md:365: Print the artifact's path and a terminal report for the **approved** set only — grouped by severity, verdict label in bold. For each approved finding: severity tag, `file:line`, title, body, and the orchestrator POV line. State in one line that the `ask-set` went undecided because no human was available at the review gate. `compiled.json` already holds the full record; the artifact is the durable presentation.

plugins/superheroes/lib/tests/test_presence_flag_retired.py:422: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_retired_presence_premise_literals_census[no human]
1 failed in 0.12s
```

**Restore:** reverted the sentence to `because no human answered the review gate`.

**Restore receipt:** `State in one line that the \`ask-set\` went undecided because no human answered the review gate.` is present again on the allowlisted line.

**Green run:**
```
(implicit — census passes on scoped suite after restore; line-shift proof above also confirms green post-restore)
```

---

## BP-D3 — `test_premise_literal_allowlist_entries_exist` (dead entry)

**Guarded element:** `_PREMISE_LITERAL_ALLOWLIST` — axis: every allowlist entry must match a line that currently exists; a dead entry fails the suite.

**Neutralization:** appended a temporary allowlist entry `("skills/review-code/SKILL.md", "THIS LINE DOES NOT EXIST ANYWHERE IN THE FILE", "no human")` to `_PREMISE_LITERAL_ALLOWLIST` in `test_presence_flag_retired.py` (temporary edit; entry removed after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_premise_literal_allowlist_entries_exist -q -p no:warnings -p no:xdist
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________ test_premise_literal_allowlist_entries_exist _________________

>       assert not stale, (
E       AssertionError: premise literal allowlist contains dead entries — prune or re-adjudicate (#1136). Every stale entry:
E         skills/review-code/SKILL.md: allowlisted line not found (literal 'no human'): 'THIS LINE DOES NOT EXIST ANYWHERE IN THE FILE'

plugins/superheroes/lib/tests/test_presence_flag_retired.py:414: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_premise_literal_allowlist_entries_exist
1 failed in 0.04s
```

**Restore:** removed the temporary dead allowlist entry from `_PREMISE_LITERAL_ALLOWLIST`.

**Restore receipt:** `_PREMISE_LITERAL_ALLOWLIST` contains only entries whose line text exists in the named file.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.03s
```
