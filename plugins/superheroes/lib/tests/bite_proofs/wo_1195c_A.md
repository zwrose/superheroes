# WO-1195c-A bite-proofs

**Declared guarded element set:** E1, E2, E3, E4, E5, E6 (one proof each).

## E1 — `_round_number` non-finite-float rejection (`OverflowError` leg)

**Guarded element:** `review_loop_plan._round_number` — axis: non-finite floats return None, never raise.

**Neutralization:** removed `OverflowError` from the `except` tuple:

```python
    except (TypeError, ValueError):
        return None
```

**Command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_review_loop_plan.py::test_resume_round_skips_junk_never_raises[inf]' -q`

**Raw red:**

```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_resume_round_skips_junk_never_raises[inf] ________________

junk = inf

    @pytest.mark.parametrize("junk", _JUNK_CASES)
    def test_resume_round_skips_junk_never_raises(junk):
        """Site 116 — junk round skipped; inf must not raise OverflowError."""
>       assert rlp._resume_round([{"round": 1}, {"round": junk}]) == 2

plugins/superheroes/lib/tests/test_review_loop_plan.py:1032: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 
plugins/superheroes/lib/review_loop_plan.py:133: in _resume_round
    n = _round_number(rec.get("round"))
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

value = inf

    def _round_number(value):
        ...
        try:
>           i = int(value)
E           OverflowError: cannot convert float infinity to integer

plugins/superheroes/lib/review_loop_plan.py:119: OverflowError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_loop_plan.py::test_resume_round_skips_junk_never_raises[inf]
1 failed in 0.47s
```

**Restore:** re-added `OverflowError` to the `except` tuple (inverse of neutralization).

**Restore receipt:** `except (TypeError, ValueError, OverflowError):`

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.38s
```

## E2 — `_round_number` non-integral-float rejection (`1.5` must not alias round 1)

**Guarded element:** `review_loop_plan._round_number` — axis: `1.5` is junk, not int `1`.

**Neutralization:** removed the non-integral float guard:

```python
    if isinstance(value, float) and i != value:
        return None
```

**Command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_round_driver.py::test_round_disclosure_key_rejects_junk_and_never_aliases' -q`

**Raw red:**

```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_round_disclosure_key_rejects_junk_and_never_aliases ___________

    def test_round_disclosure_key_rejects_junk_and_never_aliases():
        ...
>       assert RD._round_disclosure_key(1.5) is None
E       AssertionError: assert '1' is None
E        +  where '1' = <function _round_disclosure_key at 0x10a7468b0>(1.5)
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_round_disclosure_key_rejects_junk_and_never_aliases
1 failed in 1.37s
```

**Restore:** re-added the `isinstance(value, float) and i != value` guard.

**Restore receipt:** `if isinstance(value, float) and i != value: return None`

**Raw green:**

```
.                                                                        [100%]
1 passed in 1.36s
```

## E3 — `_round_number` bool rejection

**Guarded element:** `review_loop_plan._round_number` — axis: bools are junk, not int `1`.

**Neutralization:** removed the bool early-return:

```python
    if isinstance(value, bool):
        return None
```

**Command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_round_driver.py::test_round_disclosure_key_rejects_junk_and_never_aliases' -q`

**Raw red:**

```
F                                                                        [100%]
=================================== FAILURES ===================================
___________ test_round_disclosure_key_rejects_junk_and_never_aliases ___________

>       assert RD._round_disclosure_key(True) is None
E       AssertionError: assert '1' is None
E        +  where '1' = <function _round_disclosure_key at 0x10633a8b0>(True)
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_round_disclosure_key_rejects_junk_and_never_aliases
1 failed in 1.98s
```

**Restore:** re-added the bool early-return block.

**Restore receipt:** `if isinstance(value, bool): return None`

**Raw green:**

```
.                                                                        [100%]
1 passed in 1.67s
```

## E4 — `_seed_resume` junk-round park

**Guarded element:** `round_driver._seed_resume` — axis: junk `round` parks `_resumeCorrupt`, never resumes silently.

**Neutralization:** removed the junk-round park loop before `_resume_round`.

**Command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_round_driver_resume_and_park_seeding.py::test_junk_infinity_round_parks_resume_corrupt' -q`

**Raw red:**

```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_junk_infinity_round_parks_resume_corrupt _________________

>       assert isinstance(state.get("_resumeCorrupt"), str)
E       AssertionError: assert False
E        +  where False = isinstance(None, str)
E        +    where None = ...get('_resumeCorrupt')
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_resume_and_park_seeding.py::test_junk_infinity_round_parks_resume_corrupt
1 failed in 1.18s
```

**Restore:** re-added the park loop with `rnd is not None and review_loop_plan._round_number(rnd) is None`.

**Restore receipt:** full park loop restored at `round_driver.py` before `resume_round = review_loop_plan._resume_round(records)`.

**Raw green:**

```
.                                                                        [100%]
1 passed in 2.18s
```

## E5 — `_seed_resume` `None`-is-not-junk carve-out

**Guarded element:** `round_driver._seed_resume` park condition — axis: `round: None` legacy records must not park.

**Neutralization:** removed the `rnd is not None and` guard so `None` parks:

```python
        if review_loop_plan._round_number(rnd) is None:
```

**Command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_round_driver_resume_and_park_seeding.py::test_legacy_none_round_does_not_park_and_resumes' -q`

**Raw red:**

```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________ test_legacy_none_round_does_not_park_and_resumes _______________

>       assert state.get("_resumeCorrupt") is None
E       AssertionError: assert 'resume state corrupt (record round None: round value is not a valid round number) — cannot certify; a fresh full reviewer-deep round is owed' is None
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_resume_and_park_seeding.py::test_legacy_none_round_does_not_park_and_resumes
1 failed in 1.35s
```

**Restore:** re-added `rnd is not None and` to the park condition.

**Restore receipt:** `if rnd is not None and review_loop_plan._round_number(rnd) is None:`

**Raw green:**

```
.                                                                        [100%]
1 passed in 1.19s
```

## E6 — census test (no bare `int(rec.get("round"))` outside arbiter)

**Guarded element:** `test_round_value_int_calls_census_only_inside_arbiter` — axis: any new bare round `int()` outside `_round_number` fails the census.

**Neutralization:** reintroduced bare `int(rec.get("round"))` at `_resume_round` site 116:

```python
        try:
            n = int(rec.get("round"))
        except (TypeError, ValueError):
            continue
```

**Command:** `/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_review_loop_plan.py::test_round_value_int_calls_census_only_inside_arbiter' -q`

**Raw red:**

```
F                                                                        [100%]
=================================== FAILURES ===================================
____________ test_round_value_int_calls_census_only_inside_arbiter _____________

>       assert violations == [], "bare int(round) at lines %r" % violations
E       AssertionError: bare int(round) at lines [134]
E       assert [134] == []
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_loop_plan.py::test_round_value_int_calls_census_only_inside_arbiter
1 failed, 1 error in 1.10s
```

**Restore:** restored `_round_number` routing at site 116.

**Restore receipt:** `n = _round_number(rec.get("round")); if n is None: continue`

**Raw green:**

```
.                                                                        [100%]
1 passed in 0.88s
```
