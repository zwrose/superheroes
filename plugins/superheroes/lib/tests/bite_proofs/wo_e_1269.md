# WO-E (#1269) bite-proof — dispatch entry doc drift guard

**Provenance:** cursor-agent / composer-2.5

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-E1 | `test_generated_doc_matches_committed_file` | the generated doc matches the declaration | `test_generated_doc_matches_committed_file` |
| BP-E2 | `generate(check_contracts=True)` undeclared census | undeclared caller-contract arguments fail generation | `test_undeclared_contract_is_a_generator_defect` |

---

## BP-E1 — generated doc matches declaration

- **axis:** the generated doc matches the declaration

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `build_parser` dispatch-poll subcommand):
```python
    cc.add_argument(p, "--bite-proof-drift", contract="free-text", default=None)  # bite-proof neutralization
```
(added after `--run-dir`; `dispatch-entry.md` not regenerated)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_dispatch_entry_doc.py::test_generated_doc_matches_committed_file -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
__________________ test_generated_doc_matches_committed_file ___________________

    def test_generated_doc_matches_committed_file():
        # bite-axis: the generated doc matches the declaration.
        with tempfile.NamedTemporaryFile("w", delete=False, suffix=".md") as fh:
            tmp_path = fh.name
        try:
            text = DED.generate()
            with open(tmp_path, "w", encoding="utf-8") as fh:
                fh.write(text)
            with open(_COMMITTED, encoding="utf-8") as fh:
                committed = fh.read()
>           assert text == committed
E           AssertionError: assert '<!-- generat...0 seconds |\n' == '<!-- generat...0 seconds |\n'
E             
E             Skipping 1344 identical leading characters in diff, use -v to show
E               none |  |
E             + | `--bite-proof-drift` | no | `free-text` | none |  |
E               
E               #### `engine_dispatch.py dispatch-write`
E               ...
E             
E             ...Full output truncated (99 lines hidden), use '-vv' to show

plugins/superheroes/lib/tests/test_dispatch_entry_doc.py:79: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_dispatch_entry_doc.py::test_generated_doc_matches_committed_file
1 failed in 0.12s
```

**restore:** removed the `--bite-proof-drift` line from `build_parser`.

**raw green:**
```
.                                                                        [100%]
1 passed in 0.11s
```

---

## BP-E2 — undeclared contract is a generator defect

- **axis:** undeclared caller-contract arguments fail generation, not silent omission

**neutralization** (`plugins/superheroes/lib/dispatch_entry_doc.py`, `generate`):
```python
    if False and check_contracts:  # bite-proof neutralization
```
(replaces `if check_contracts:`)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_dispatch_entry_doc.py::test_undeclared_contract_is_a_generator_defect -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________ test_undeclared_contract_is_a_generator_defect ________________

    def test_undeclared_contract_is_a_generator_defect():
        parser = DED.engine_dispatch.build_parser()
        sub = None
        for action in parser._actions:
            if isinstance(action, argparse._SubParsersAction):
                sub = action
                break
        assert sub is not None
        leaked = sub.choices["dispatch-review"]
        leaked.add_argument("--bite-proof-leak", dest="bite_proof_leak")
        original = DED.engine_dispatch.build_parser
        DED.engine_dispatch.build_parser = lambda: parser
        try:
            with pytest.raises(DED.DispatchEntryDocError, match="undeclared"):
>               DED.generate()
E               Failed: DID NOT RAISE <class 'dispatch_entry_doc.DispatchEntryDocError'>

plugins/superheroes/lib/tests/test_dispatch_entry_doc.py:134: Failed
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_dispatch_entry_doc.py::test_undeclared_contract_is_a_generator_defect
1 failed in 0.12s
```

**restore:**
```python
    if check_contracts:
```

**raw green:**
```
.                                                                        [100%]
1 passed in 0.10s
```

---

## Re-verification on the chokepoint head (2026-09-16, orchestrator)

- **BP-E1** — adding an undeclared `--bite-proof-drift` argument to the `dispatch-poll` parser turned
  `test_generated_doc_matches_committed_file` red (`1 failed in 0.14s`); removing it turned the test
  green (`1 passed in 0.13s`), with the committed doc regenerated from the post-chokepoint
  declarations.
- **BP-E2** — disabling the `check_contracts` branch of `generate` turned
  `test_undeclared_contract_is_a_generator_defect` red (`1 failed in 0.12s`); the inverse edit turned
  it green (`1 passed in 0.20s`).

Tree confirmed clean after every restore.
