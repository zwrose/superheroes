# WO-C (#1219) bite-proofs — test-receipt evidence doctrine drift guard

Guarded-element set: FB-1 … FB-11 (eleven roster ids; FB-5 directory and file exclusions proved separately).

## FB-1 … FB-2 — rostered consumer pointer count in bounded section (equivalence class)

**Failure mode:** `check_clause` no longer fails when a rostered consumer section's pointer count is wrong (deleted or extra pointer).

**Representative:** FB-1 (`agents/grounding-seat.md`, `## What you check (self-claims → repo)`).

**Also covered by this class:** FB-2 (`skills/review-code/SKILL.md`, `## The verify command`).

**Neutralization (FB-1):** replaced `in \`rubric/test-receipt-evidence.md\`.` with `in the test-receipt evidence policy.` in `plugins/superheroes/agents/grounding-seat.md`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_consumer_section_points_at_test_receipt_evidence_home[FB-1]' -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______ test_consumer_section_points_at_test_receipt_evidence_home[FB-1] _______

rel = 'agents/grounding-seat.md'
section = '## What you check (self-claims → repo)', expected_count = 1

    @pytest.mark.parametrize(
        "rel,section,expected_count",
        _CONSUMER_ROSTER,
        ids=[f"FB-{i}" for i in range(1, len(_CONSUMER_ROSTER) + 1)],
    )
    def test_consumer_section_points_at_test_receipt_evidence_home(rel, section, expected_count):
>       check_clause(_read(rel), rel, section, _POINTER, expected_count)

plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:280: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

text = '---\nname: grounding-seat\ndescription: Internal grounding seat (NOT one of the five risk-domain review lenses). Chec...ted among the five default-crew dimensions and adds none — it is\nthe narrow sixth seat recorded in the base rubric.\n'
rel = 'agents/grounding-seat.md'
section_heading = '## What you check (self-claims → repo)'
clause = 'rubric/test-receipt-evidence.md', expected_count = 1

    def check_clause(text, rel, section_heading, clause, expected_count):
        """Raise AssertionError when the section's clause count differs from expected."""
        actual = count_clause_in_section(text, rel, section_heading, clause)
        if actual != expected_count:
>           raise AssertionError(
                f"{rel} (section {section_heading}): clause {clause!r} expected count "
                f"{expected_count}, found {actual} — re-sync the text or update the roster"
            )
E           AssertionError: agents/grounding-seat.md (section ## What you check (self-claims → repo)): clause 'rubric/test-receipt-evidence.md' expected count 1, found 0 — re-sync the text or update the roster

plugins/superheroes/lib/tests/clause_guard.py:59: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_consumer_section_points_at_test_receipt_evidence_home[FB-1]
1 failed in 1.58s
```

**Restore:** reinstated `in \`rubric/test-receipt-evidence.md\`.` in `plugins/superheroes/agents/grounding-seat.md`.

**Restore receipt:**
```
```
(no porcelain output — file matches restored content)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.72s
```

---

## FB-3 — rostered consumer pointer count (`setup.md`)

**Failure mode:** `check_clause` no longer fails when FB-3's pointer count is wrong inside its fence-aware bounded section (line 186 is now inside `## Setup resolution — run these in order`).

**Surface:** FB-3 (`skills/review-code/reference/setup.md`, `## Setup resolution — run these in order`).

### FB-3 leg 1 — pointer deleted (under-count)

**Neutralization:** replaced `read \`rubric/test-receipt-evidence.md\`.` with `read the test-receipt evidence policy.` at line 186 in `plugins/superheroes/skills/review-code/reference/setup.md`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_consumer_section_points_at_test_receipt_evidence_home[FB-3]' -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______ test_consumer_section_points_at_test_receipt_evidence_home[FB-3] _______
plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:275: in test_consumer_section_points_at_test_receipt_evidence_home
    check_clause(_read(rel), rel, section, _POINTER, expected_count)
plugins/superheroes/lib/tests/clause_guard.py:121: in check_clause
    raise AssertionError(
E   AssertionError: skills/review-code/reference/setup.md (section ## Setup resolution — run these in order): clause 'rubric/test-receipt-evidence.md' expected count 1, found 0 — re-sync the text or update the roster
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_consumer_section_points_at_test_receipt_evidence_home[FB-3]
1 failed in 2.59s
```

**Restore:** reinstated `read \`rubric/test-receipt-evidence.md\`.` at line 186.

### FB-3 leg 2 — pointer duplicated (over-count)

**Neutralization:** duplicated the pointer literal on line 186: `read \`rubric/test-receipt-evidence.md\` and again \`rubric/test-receipt-evidence.md\`.`

**Command:** same as leg 1.

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______ test_consumer_section_points_at_test_receipt_evidence_home[FB-3] _______
plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:275: in test_consumer_section_points_at_test_receipt_evidence_home
    check_clause(_read(rel), rel, section, _POINTER, expected_count)
plugins/superheroes/lib/tests/clause_guard.py:121: in check_clause
    raise AssertionError(
E   AssertionError: skills/review-code/reference/setup.md (section ## Setup resolution — run these in order): clause 'rubric/test-receipt-evidence.md' expected count 1, found 2 — re-sync the text or update the roster
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_consumer_section_points_at_test_receipt_evidence_home[FB-3]
1 failed in 0.28s
```

**Restore:** reinstated single pointer occurrence at line 186.

**Green run (both legs restored):**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_consumer_section_points_at_test_receipt_evidence_home[FB-3]' \
  'plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_negative_consumer_pointer_missing_in_section[FB-3]' \
  'plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_negative_consumer_pointer_over_count_in_section[FB-3]' \
  -q
```
```
...                                                                      [100%]
3 passed in 0.58s
```

---

## FB-4 — pointer roster completeness walker

**Failure mode:** an unrostered level-2 section carrying the pointer does not make `test_pointer_roster_is_complete` raise with `unrostered` in the message.

**Representative:** `test_pointer_roster_is_complete` (completeness walker vs hand roster).

**Neutralization:** inserted after `## What you check (self-claims → repo)` in `plugins/superheroes/agents/grounding-seat.md`:

```
## Unrostered bite-proof section

See `rubric/test-receipt-evidence.md`.
```

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_pointer_roster_is_complete -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_______________________ test_pointer_roster_is_complete ________________________

    def test_pointer_roster_is_complete():
>       _check_pointer_roster_complete(_walk_pointer_carrying_sections())

plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:294: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

found_sections = {('agents/grounding-seat.md', '## Unrostered bite-proof section'), ('skills/review-code/SKILL.md', '## The verify command')}

    def _check_pointer_roster_complete(found_sections):
        roster_sections = frozenset((rel, section) for rel, section, _ in _CONSUMER_ROSTER)
        if found_sections != roster_sections:
            missing = sorted(roster_sections - found_sections)
            extra = sorted(found_sections - roster_sections)
>           raise AssertionError(
                f"pointer roster drift: missing={missing!r}, unrostered={extra!r} — extend roster"
            )
E           AssertionError: pointer roster drift: missing=[('agents/grounding-seat.md', '## What you check (self-claims → repo)'), ('skills/review-code/reference/setup.md', '## Setup resolution — run these in order')], unrostered=[('agents/grounding-seat.md', '## Unrostered bite-proof section')] — extend roster

plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:196: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_pointer_roster_is_complete
1 failed in 1.07s
```

**Restore:** removed the `## Unrostered bite-proof section` block; reinstated the original `## What you check (self-claims → repo)` header line alone.

**Restore receipt:**
```
```
(no porcelain output)

**Green run:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_pointer_roster_is_complete -q
```
```
.                                                                        [100%]
1 passed in 0.45s
```

---

## FB-5a — census directory exclusion (`lib/tests/bite_proofs/`)

**Failure mode:** disabling the directory exclusion admits bite-proof records into the pointer census.

**Representative:** `test_walk_skips_bite_proof_records`.

**Neutralization:** `_CENSUS_EXCLUDED_DIRS = ()` in `test_test_receipt_evidence_doctrine.py` (file exclusion unchanged).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_walk_skips_bite_proof_records -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______________________ test_walk_skips_bite_proof_records ______________________
plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:226: in test_walk_skips_bite_proof_records
    assert not [rel for rel, _ in found if rel.startswith(_BITE_PROOF_DIR)], (
E   AssertionError: bite-proof record surfaced in the census walk: [('agents/grounding-seat.md', '## What you check (self-claims → repo)'), ('lib/tests/bite_proofs/wo_synthetic.md', '## Neutralization')]
E   assert not ['lib/tests/bite_proofs/wo_synthetic.md']
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_walk_skips_bite_proof_records
1 failed in 0.31s
```

**Restore:** reinstated `_CENSUS_EXCLUDED_DIRS = ("lib/tests/bite_proofs",)`.

**Restore receipt:**
```
```
(no porcelain output)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.85s
```

---

## FB-5b — census file exclusion (`CHANGELOG.md`)

**Failure mode:** disabling the file exclusion makes `test_census_excludes_changelog` fail.

**Representative:** `test_census_excludes_changelog`.

**Neutralization:** `_CENSUS_EXCLUDED_FILES = ()` in `test_test_receipt_evidence_doctrine.py` (directory exclusion unchanged).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_census_excludes_changelog -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________________ test_census_excludes_changelog ________________________
plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:249: in test_census_excludes_changelog
    assert _census_excluded("CHANGELOG.md")
E   AssertionError: assert False
E    +  where False = _census_excluded('CHANGELOG.md')
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_census_excludes_changelog
1 failed in 0.37s
```

**Restore:** reinstated `_CENSUS_EXCLUDED_FILES = ("CHANGELOG.md",)`.

**Restore receipt:**
```
```
(no porcelain output)

**Green run:**
```
.                                                                        [100%]
1 passed in 0.37s
```

---

## FB-6 … FB-7 — home level-2 headings (equivalence class)

**Failure mode:** deleting a rostered heading line from the home does not make `test_home_has_heading` raise naming the heading.

**Representative:** FB-6 (`## Verify receipt is not a test receipt`).

**Also covered:** FB-7 (`## What does ground a test-pass claim`).

**Neutralization:** deleted the line `## Verify receipt is not a test receipt` from `plugins/superheroes/rubric/test-receipt-evidence.md`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_home_has_heading[FB-6]' -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_________________________ test_home_has_heading[FB-6] __________________________

heading = '## Verify receipt is not a test receipt'

    @pytest.mark.parametrize("heading", _HEADINGS, ids=["FB-6", "FB-7"])
    def test_home_has_heading(heading):
>       _check_home_heading(_read(_HOME), heading)

plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:285: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

    def _check_home_heading(text, heading):
        if heading not in text.splitlines():
>           raise AssertionError(
                f"{_HOME}: heading {heading!r} missing — re-add or update "
                f"test_test_receipt_evidence_doctrine.py"
            )
E           AssertionError: rubric/test-receipt-evidence.md: heading '## Verify receipt is not a test receipt' missing — re-add or update test_test_receipt_evidence_doctrine.py

plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:121: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_home_has_heading[FB-6]
1 failed in 1.41s
```

**Restore:** reinserted `## Verify receipt is not a test receipt` and its following blank line after the home preamble.

**Restore receipt:** heading line `## Verify receipt is not a test receipt` is present again in `rubric/test-receipt-evidence.md`.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.48s
```

---

## FB-8 … FB-11 — home clause rows at exact count in bounded section (equivalence class)

**Failure mode:** `check_clause` no longer fails when a rostered home sentence is removed, restated in the other home section, or duplicated (over-count).

**Representative:** FB-8 (green-verify sentence in `## Verify receipt is not a test receipt`).

**Also covered:** FB-9, FB-10, FB-11 (parametrized negatives `test_negative_home_clause_*`).

**Neutralization:** deleted the FB-8 sentence from `plugins/superheroes/rubric/test-receipt-evidence.md`:

`A green verify exit is **not** evidence the test suite passed — calibration may run no tests at all.`

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest 'plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_home_clause_present[FB-8]' -q
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
________________________ test_home_clause_present[FB-8] ________________________

row = {'clause': 'A green verify exit is **not** evidence the test suite passed — calibration may run no tests at all.', 'home_section': '## Verify receipt is not a test receipt', 'id': 'FB-8'}

    @pytest.mark.parametrize("row", _CLAUSE_ROWS, ids=[r["id"] for r in _CLAUSE_ROWS])
    def test_home_clause_present(row):
>       _check_home_clause(row)

plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py:290: 
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ 

    def _check_home_clause(row, read_text=None):
        ...
>       check_clause(
E           AssertionError: rubric/test-receipt-evidence.md (section ## Verify receipt is not a test receipt): clause 'A green verify exit is **not** evidence the test suite passed — calibration may run no tests at all.' expected count 1, found 0 — re-sync the text or update the roster

plugins/superheroes/lib/tests/clause_guard.py:59: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_test_receipt_evidence_doctrine.py::test_home_clause_present[FB-8]
1 failed in 1.18s
```

**Restore:** reinserted the FB-8 sentence immediately under `## Verify receipt is not a test receipt`.

**Restore receipt:** FB-8 sentence is present again under `## Verify receipt is not a test receipt` in `rubric/test-receipt-evidence.md`.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.42s
```
