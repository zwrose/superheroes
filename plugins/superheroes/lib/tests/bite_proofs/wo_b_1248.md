# WO-B (#1248 leg b, round 3) bite-proof — hand-graded withhold arm

## Guarded element

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-B1 | `_terminal_converged` → `_hand_graded_seat_landing` withhold arm | Hand submit on a dispatch seat withholds certification (`"cannot-certify"`) | `test_hand_graded_seat_submit_folds_dispatch_panel_terminal_cannot_certify` |

---

## BP-B1 — hand-graded withhold arm

- **axis:** a hand submit folding `dispatch-panel` must terminal at `"cannot-certify"`, not `"converged"`

**neutralization** (disable the withhold arm — fall through to converge):
```python
    withhold, why = _hand_graded_seat_landing(state, session_dir)
    if False and withhold:
        _park_cannot_certify(state, why)
        return
```

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-b3 -m pytest plugins/superheroes/lib/tests/test_round_driver.py::test_hand_graded_seat_submit_folds_dispatch_panel_terminal_cannot_certify -q --tb=line -n auto
```

**raw red** (exit 1):
```
bringing up nodes...
bringing up nodes...

F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1248-ebdf001e91df3116/plugins/superheroes/lib/tests/test_round_driver.py:7832: AssertionError: assert 'converged' == 'cannot-certify'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver.py::test_hand_graded_seat_submit_folds_dispatch_panel_terminal_cannot_certify
1 failed in 3.16s
```

**restore:**
```python
    withhold, why = _hand_graded_seat_landing(state, session_dir)
    if withhold:
        _park_cannot_certify(state, why)
        return
```

**raw green** (exit 0):
```
bringing up nodes...
bringing up nodes...

.                                                                        [100%]
1 passed in 5.96s
```

Post-restore `git diff --stat plugins/superheroes/lib/round_driver.py` was empty (no probe residue).
