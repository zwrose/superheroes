# Bite-proof record — #1271 WO-R2 (`investigated` disclosure + execution-evidence fence)

Contract: `rubric/bite-proof.md`. Proofs ran with the **detector unedited**; neutralizations were
targeted, reversible edits to `_validate_execution_evidence` in `round_records.py`, reverted by exact
inverse before the green half.

## Detector A — observation unknown-key enumeration

**Guarded element.** `round_records.py` — `extra_obs` branch in `_validate_execution_evidence`.
**Axis.** An observation key outside the closed runner-observed set refuses
`execution-evidence-unknown-field`.

**Neutralization:**

```python
-    if extra_obs:
+    if False and extra_obs:
```

**Raw red** — `PYTEST_EXIT=1`:

```
FAILED …::test_v2_execution_evidence_observation_extra_key_refuses_unknown_field[dispatch-observed]
FAILED …::test_v2_execution_evidence_observation_extra_key_refuses_unknown_field[hand-landed]
2 failed in 0.21s
```

**Restore.** Inverse edit restoring `if extra_obs:`.

**Restore receipt.** Neutralization line restored; `if False and extra_obs:` absent from
`_validate_execution_evidence`.

**Raw green** — `PYTEST_EXIT=0`:

```
..                                                                       [100%]
2 passed in 0.17s
```

## Detector B — telemetry presence requirement

**Guarded element.** `round_records.py` — telemetry literal and closed observation key-set checks in
`_validate_execution_evidence`.
**Axis.** An observation omitting `telemetry` refuses `execution-evidence-malformed`.

**Neutralization:**

```python
-    if telemetry not in EXECUTION_EVIDENCE_TELEMETRY_VALUES:
+    if False and telemetry not in EXECUTION_EVIDENCE_TELEMETRY_VALUES:
         return ("execution-evidence-malformed", {})
-    if set(observation.keys()) != EXECUTION_EVIDENCE_OBSERVATION_FIELDS:
+    if False and set(observation.keys()) != EXECUTION_EVIDENCE_OBSERVATION_FIELDS:
         return ("execution-evidence-malformed", {})
```

**Raw red** — `PYTEST_EXIT=1`:

```
FAILED …::test_v2_execution_evidence_observation_missing_telemetry_refuses_malformed[dispatch-observed]
FAILED …::test_v2_execution_evidence_observation_missing_telemetry_refuses_malformed[hand-landed]
2 failed in 0.21s
```

**Restore.** Both `if False and …` prefixes removed; restored lines:

```python
    if telemetry not in EXECUTION_EVIDENCE_TELEMETRY_VALUES:
        return ("execution-evidence-malformed", {})
    if set(observation.keys()) != EXECUTION_EVIDENCE_OBSERVATION_FIELDS:
        return ("execution-evidence-malformed", {})
```

**Restore receipt.** Both neutralized conditions restored verbatim in `_validate_execution_evidence`.

**Raw green** — `PYTEST_EXIT=0`:

```
..                                                                       [100%]
2 passed in 0.17s
```
