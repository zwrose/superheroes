# Bite-proof record — #1271 WO-R2 (`investigated` disclosure + execution-evidence fence)

Contract: `rubric/bite-proof.md`. Proofs ran with the **detector unedited**; neutralizations were
targeted, reversible edits to `_validate_execution_evidence` in `round_records.py`, reverted by exact
inverse before the green half.

## Detector A — observation unknown-key enumeration

**Guarded element.** `round_records.py` — `extra_obs` branch in `_validate_execution_evidence`.
**Axis.** An observation key outside the closed runner-observed set refuses
`execution-evidence-unknown-field`.

**Neutralization:**

```python
-    if extra_obs:
+    if False and extra_obs:
```

**Raw red** — `PYTEST_EXIT=1`:

```
FAILED …::test_v2_execution_evidence_observation_extra_key_refuses_unknown_field[dispatch-observed]
FAILED …::test_v2_execution_evidence_observation_extra_key_refuses_unknown_field[hand-landed]
2 failed in 0.21s
```

**Restore.** Inverse edit restoring `if extra_obs:`.

**Restore receipt.**

```
 M docs/superheroes/KEEP-OR-RETIRE.md
 M plugins/superheroes/lib/tests/bite_proofs/wo_r2_1271.md
 M plugins/superheroes/lib/tests/test_round_records.py
```

**Raw green** — `PYTEST_EXIT=0`:

```
..                                                                       [100%]
2 passed in 0.17s
```

## Detector B1 — telemetry literal check

**Guarded element.** `round_records.py` — telemetry literal check in `_validate_execution_evidence`.
**Axis.** An observation whose `telemetry` is not a closed literal refuses
`execution-evidence-malformed`.

**Neutralization:**

```python
-    if telemetry not in EXECUTION_EVIDENCE_TELEMETRY_VALUES:
+    if False and telemetry not in EXECUTION_EVIDENCE_TELEMETRY_VALUES:
```

**Test.** `test_v2_execution_evidence_observation_bad_telemetry_value_refuses_malformed` (T_lit).

**Raw red** — T_lit `PYTEST_EXIT=1`:

```
FF                                                                       [100%]
=================================== FAILURES ===================================
_ test_v2_execution_evidence_observation_bad_telemetry_value_refuses_malformed[dispatch-observed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-461/test_v2_execution_evidence_obs0')
provenance = 'dispatch-observed'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    def test_v2_execution_evidence_observation_bad_telemetry_value_refuses_malformed(tmp_path, provenance):
        # axis: execution-evidence-malformed — wo_r3_1271 telemetry literal check
        sd = _session(tmp_path)
        obs = _well_formed_observation(telemetry="maybe")
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': '84e375dc5df513352b015ecdbfae13bef1909d84...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:1945: AssertionError
_ test_v2_execution_evidence_observation_bad_telemetry_value_refuses_malformed[hand-landed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-461/test_v2_execution_evidence_obs1')
provenance = 'hand-landed'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    def test_v2_execution_evidence_observation_bad_telemetry_value_refuses_malformed(tmp_path, provenance):
        # axis: execution-evidence-malformed — wo_r3_1271 telemetry literal check
        sd = _session(tmp_path)
        obs = _well_formed_observation(telemetry="maybe")
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': '84e375dc5df513352b015ecdbfae13bef1909d84...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:1945: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_telemetry_value_refuses_malformed[dispatch-observed]
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_bad_telemetry_value_refuses_malformed[hand-landed]
2 failed in 0.32s
```

**Raw red** — T_set `PYTEST_EXIT=0` (must stay green under B1 neutralization):

```
..                                                                       [100%]
2 passed in 0.29s
```

**Restore.** Inverse edit restoring `if telemetry not in EXECUTION_EVIDENCE_TELEMETRY_VALUES:`.

**Restore receipt.**

```
 M docs/superheroes/KEEP-OR-RETIRE.md
 M plugins/superheroes/lib/tests/bite_proofs/wo_r2_1271.md
 M plugins/superheroes/lib/tests/test_round_records.py
```

**Raw green** — T_lit and T_set `PYTEST_EXIT=0`:

```
....                                                                     [100%]
4 passed in 0.28s
```

## Detector B2 — observation key-set check

**Guarded element.** `round_records.py` — closed observation key-set check in
`_validate_execution_evidence`.
**Axis.** An observation missing a required key refuses `execution-evidence-malformed`.

**Neutralization:**

```python
-    if set(observation.keys()) != EXECUTION_EVIDENCE_OBSERVATION_FIELDS:
+    if False and set(observation.keys()) != EXECUTION_EVIDENCE_OBSERVATION_FIELDS:
```

**Test.** `test_v2_execution_evidence_observation_missing_required_key_refuses_malformed` (T_set).

**Raw red** — T_set `PYTEST_EXIT=1`:

```
FF                                                                       [100%]
=================================== FAILURES ===================================
_ test_v2_execution_evidence_observation_missing_required_key_refuses_malformed[dispatch-observed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-464/test_v2_execution_evidence_obs0')
provenance = 'dispatch-observed'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    def test_v2_execution_evidence_observation_missing_required_key_refuses_malformed(tmp_path, provenance):
        # axis: execution-evidence-malformed — wo_r3_1271 observation key-set check
        sd = _session(tmp_path)
        obs = _well_formed_observation()
        del obs["read"]
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': 'a5eed9fffd2b9af0dda3c6940284e74c099775e5...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:1957: AssertionError
_ test_v2_execution_evidence_observation_missing_required_key_refuses_malformed[hand-landed] _

tmp_path = PosixPath('/private/var/folders/dy/s097fm_n7tldcbdtthd1zgqh0000gn/T/pytest-of-zwrose/pytest-464/test_v2_execution_evidence_obs1')
provenance = 'hand-landed'

    @pytest.mark.parametrize("provenance", RR.EVIDENCE_BEARING_PROVENANCE)
    def test_v2_execution_evidence_observation_missing_required_key_refuses_malformed(tmp_path, provenance):
        # axis: execution-evidence-malformed — wo_r3_1271 observation key-set check
        sd = _session(tmp_path)
        obs = _well_formed_observation()
        del obs["read"]
        env = _v2_env(provenance=provenance, execution_evidence=_execution_evidence(observation=obs))
        _land(sd, env)
        plan, refusal = _validate(sd, seat_result_schema=RR.SEAT_RESULT_SCHEMA_V2)
>       assert plan is None and refusal["reason"] == "execution-evidence-malformed"
E       AssertionError: assert ({'envelope': {'attempt': 1, 'dispatchRef': 'dispatch-abc', 'envelopeSha256': 'a5eed9fffd2b9af0dda3c6940284e74c099775e5...0, 'payloadSha256': 'adcb5786ce256af09c3e08308bfaca00ecdbb9d640b8404d1fa54de6ca5f6aac', 'seatKey': 'src/a/b.py:3', ...} is None)

plugins/superheroes/lib/tests/test_round_records.py:1957: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_missing_required_key_refuses_malformed[dispatch-observed]
FAILED plugins/superheroes/lib/tests/test_round_records.py::test_v2_execution_evidence_observation_missing_required_key_refuses_malformed[hand-landed]
2 failed in 0.33s
```

**Raw red** — T_lit `PYTEST_EXIT=0` (must stay green under B2 neutralization):

```
..                                                                       [100%]
2 passed in 0.25s
```

**Restore.** Inverse edit restoring
`if set(observation.keys()) != EXECUTION_EVIDENCE_OBSERVATION_FIELDS:`.

**Restore receipt.**

```
 M docs/superheroes/KEEP-OR-RETIRE.md
 M plugins/superheroes/lib/tests/bite_proofs/wo_r2_1271.md
 M plugins/superheroes/lib/tests/test_round_records.py
```

**Raw green** — T_lit and T_set `PYTEST_EXIT=0`:

```
....                                                                     [100%]
4 passed in 0.25s
```
