# WO-A (#1125) bite-proofs — authoritative under-cap stdoutBytes outranks marker text

## Baseline (pre-production edit)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_under_cap_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_injected_seam_count_does_not_override_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_over_cap_count_grades_truncated_with_under_cap_file \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_file_over_cap_grades_truncated_despite_under_cap_recorded_count \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_complete_marker_mid_body_not_truncated \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_genuine_truncation_detected_without_stdout_bytes \
  -q -p no:randomly
```

**Output:**
```
E       AssertionError: assert 58 is None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5388: AssertionError
E       AssertionError: assert 8388608 is None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5399: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_under_cap_count_overrides_forged_head_marker
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker
2 failed, 5 passed in 0.49s
```

---

## E1 — arm 4 exists (`_attempt_stdout_truncated` chokepoint)

**Guarded element:** `_attempt_stdout_truncated` arm 4 — axis: authoritative under-cap recorded count suppresses marker text.

**Neutralization:** deleted arm 4 (axis comment block and `if observed is not None and ended.get("activitySource") != "injected-seam": return None`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_under_cap_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert 58 is None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5388: AssertionError
E       AssertionError: assert 8388608 is None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5399: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_under_cap_count_overrides_forged_head_marker
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker
2 failed in 0.46s
```

**Restore:** re-inserted arm 4 with axis comment and provenance conjunct.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
2 passed in 0.34s
```

---

## E2 — cap boundary is `>`, not `>=`, in arm 1

**Guarded element:** `_attempt_stdout_truncated` arm 1 comparison — axis: exactly-at-cap recorded count is not truncated.

**Neutralization:** changed `observed > MAX_STDOUT_CAPTURE` to `observed >= MAX_STDOUT_CAPTURE`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert 8388608 is None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5399: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker
1 failed in 0.49s
```

**Restore:** restored `observed > MAX_STDOUT_CAPTURE`.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
1 passed in 2.01s
```

---

## E3 — provenance is part of authority

**Guarded element:** arm 4 `activitySource != "injected-seam"` conjunct — axis: injected-seam post-cap count cannot suppress marker grading.

**Neutralization:** replaced `if observed is not None and ended.get("activitySource") != "injected-seam":` with `if observed is not None:`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_injected_seam_count_does_not_override_head_marker \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert None == 58
plugins/superheroes/lib/tests/test_engine_dispatch.py:5419: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_injected_seam_count_does_not_override_head_marker
1 failed in 2.19s
```

**Restore:** restored `and ended.get("activitySource") != "injected-seam"` conjunct.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
1 passed in 1.72s
```

---

## E4 — recorded-over-cap arm

**Guarded element:** `_attempt_stdout_truncated` arm 1 — axis: recorded stdoutBytes above cap grades truncated before file inspection.

**Neutralization:** deleted arm 1 (`if observed is not None and observed > MAX_STDOUT_CAPTURE: return observed`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_over_cap_count_grades_truncated_with_under_cap_file \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert None == 8389120
plugins/superheroes/lib/tests/test_engine_dispatch.py:5431: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_over_cap_count_grades_truncated_with_under_cap_file
1 failed in 0.83s
```

**Restore:** re-inserted arm 1 over-cap early return.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
1 passed in 1.14s
```

---

## E5 — arm order (file-size arm precedes suppression)

**Guarded element:** `_attempt_stdout_truncated` arm ordering — axis: on-disk over-cap size is graded before authoritative under-cap suppression.

**Neutralization:** moved arm 4 block above `if size > MAX_STDOUT_CAPTURE: return size`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_file_over_cap_grades_truncated_despite_under_cap_recorded_count \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert None == 8389120
plugins/superheroes/lib/tests/test_engine_dispatch.py:5443: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_file_over_cap_grades_truncated_despite_under_cap_recorded_count
1 failed in 0.80s
```

**Restore:** moved arm 4 back below the file-size arm.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
1 passed in 1.01s
```

---

## E6 — head anchoring of `_stdout_capture_truncated`

**Guarded element:** `_STDOUT_TRUNCATION_MARKER_RE` + `_stdout_capture_truncated` — axis: marker must be at capture head, not mid-body.

**Neutralization:** removed `^` anchor from `_STDOUT_TRUNCATION_MARKER_RE` and changed `.match(text)` to `.search(text)`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_complete_marker_mid_body_not_truncated \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert True is False
plugins/superheroes/lib/tests/test_engine_dispatch.py:5450: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_complete_marker_mid_body_not_truncated
1 failed in 0.64s
```

**Restore:** restored `^` anchor and `.match(text)`.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
1 passed in 0.56s
```

---

## E7 — preserved no-recorded-count marker fallback

**Guarded element:** `_attempt_stdout_truncated` marker arm return — axis: genuine head marker without recorded count still grades truncated.

**Neutralization:** changed `return observed if observed is not None else size` to `return None`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_genuine_truncation_detected_without_stdout_bytes \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert None is not None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5372: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_genuine_truncation_detected_without_stdout_bytes
1 failed in 0.66s
```

**Restore:** restored `return observed if observed is not None else size`.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
1 passed in 0.58s
```

---

## Final green (all seven node ids)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_under_cap_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_injected_seam_count_does_not_override_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_over_cap_count_grades_truncated_with_under_cap_file \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_file_over_cap_grades_truncated_despite_under_cap_recorded_count \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_complete_marker_mid_body_not_truncated \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_genuine_truncation_detected_without_stdout_bytes \
  -q -p no:randomly
```

**Output:**
```
7 passed in 0.56s
```
