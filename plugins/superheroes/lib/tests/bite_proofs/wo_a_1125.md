# WO-A (#1125) bite-proofs — stdoutBytesPreCap stamp asserts truncation authority

## Baseline (post-WO-B production edit)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_under_cap_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_injected_seam_count_does_not_override_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_unstamped_unknown_producer_count_does_not_override_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_over_cap_count_grades_truncated_with_under_cap_file \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_file_over_cap_grades_truncated_despite_under_cap_recorded_count \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_complete_marker_mid_body_not_truncated \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_genuine_truncation_detected_without_stdout_bytes \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_engine_files_stamps_stdout_bytes_pre_cap \
  -q -p no:randomly
```

**Output:**
```
9 passed in 0.98s
```

---

## E1 — arm 4 exists (`_attempt_stdout_truncated` chokepoint)

**Guarded element:** `_attempt_stdout_truncated` arm 4 — axis: authoritative under-cap recorded count suppresses marker text.

**Neutralization:** deleted arm 4 (axis comment block, `if observed is not None and ended.get("stdoutBytesPreCap") is True:` body including the marker-overruled journal record, and `return None`). Arm 4 on this branch also includes the `_journal_append` marker-overruled record added by later work; the neutralization deletes the entire block.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_under_cap_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert 58 is None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5464: AssertionError
E       AssertionError: assert 8388608 is None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5484: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_under_cap_count_overrides_forged_head_marker
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker
2 failed in 0.51s
```

(Pytest temp-dir cleanup warnings elided from all captures in this record — ~185 KiB per run; decisive failure lines quoted. Nothing sensitive to redact.)

**Restore:** re-inserted arm 4 with axis comment, `stdoutBytesPreCap` conjunct, marker-overruled journal record, and `return None`.

**Restore receipt:** *(empty — `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` returned no output)*

**Green run:**
```
2 passed in 0.38s
```

---

## E2 — cap boundary is `>`, not `>=`, in arm 1

**Guarded element:** `_attempt_stdout_truncated` arm 1 comparison — axis: exactly-at-cap recorded count is not truncated.

**Neutralization:** changed `observed > MAX_STDOUT_CAPTURE` to `observed >= MAX_STDOUT_CAPTURE`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert 8388608 is None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5484: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker
1 failed in 0.45s
```

**Restore:** restored `observed > MAX_STDOUT_CAPTURE`.

**Restore receipt:** *(empty — `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` returned no output)*

**Green run:**
```
1 passed in 0.38s
```

---

## E3 — the stamp is required

**Guarded element:** arm 4 `ended.get("stdoutBytesPreCap") is True` conjunct — axis: unstamped stdoutBytes is not authoritative; marker read decides.

**Neutralization:** replaced `if observed is not None and ended.get("stdoutBytesPreCap") is True:` with `if observed is not None:`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_injected_seam_count_does_not_override_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_unstamped_unknown_producer_count_does_not_override_head_marker \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert None == 58
plugins/superheroes/lib/tests/test_engine_dispatch.py:5504: AssertionError
E       AssertionError: assert None == 58
plugins/superheroes/lib/tests/test_engine_dispatch.py:5524: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_injected_seam_count_does_not_override_head_marker
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_unstamped_unknown_producer_count_does_not_override_head_marker
2 failed in 0.54s
```

**Restore:** restored `and ended.get("stdoutBytesPreCap") is True` conjunct.

**Restore receipt:** *(empty — `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` returned no output)*

**Green run:**
```
2 passed in 0.39s
```

---

## E4 — recorded-over-cap arm

**Guarded element:** `_attempt_stdout_truncated` arm 1 — axis: recorded stdoutBytes above cap grades truncated before file inspection.

**Neutralization:** deleted arm 1 (`if observed is not None and observed > MAX_STDOUT_CAPTURE: return observed`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_over_cap_count_grades_truncated_with_under_cap_file \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert None == 8389120
plugins/superheroes/lib/tests/test_engine_dispatch.py:5552: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_over_cap_count_grades_truncated_with_under_cap_file
1 failed in 0.48s
```

**Restore:** re-inserted arm 1 over-cap early return.

**Restore receipt:** *(empty — `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` returned no output)*

**Green run:**
```
1 passed in 0.39s
```

---

## E5 — arm order (file-size arm precedes suppression)

**Guarded element:** `_attempt_stdout_truncated` arm ordering — axis: on-disk over-cap size is graded before authoritative under-cap suppression.

**Neutralization:** moved arm 4 block above `if size > MAX_STDOUT_CAPTURE: return size`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_file_over_cap_grades_truncated_despite_under_cap_recorded_count \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert None == 8389120
plugins/superheroes/lib/tests/test_engine_dispatch.py:5564: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_file_over_cap_grades_truncated_despite_under_cap_recorded_count
1 failed in 0.56s
```

**Restore:** moved arm 4 back below the file-size arm.

**Restore receipt:** *(empty — `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` returned no output)*

**Green run:**
```
1 passed in 0.45s
```

---

## E6 — head anchoring of `_stdout_capture_truncated`

**Guarded element:** `_STDOUT_TRUNCATION_MARKER_RE` + `_stdout_capture_truncated` — axis: marker must be at capture head, not mid-body.

**Neutralization:** removed `^` anchor from `_STDOUT_TRUNCATION_MARKER_RE` and changed `.match(text)` to `.search(text)`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_complete_marker_mid_body_not_truncated \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert True is False
plugins/superheroes/lib/tests/test_engine_dispatch.py:5794: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_complete_marker_mid_body_not_truncated
1 failed in 0.46s
```

**Restore:** restored `^` anchor and `.match(text)`.

**Restore receipt:** *(empty — `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` returned no output)*

**Green run:**
```
1 passed in 0.41s
```

---

## E7 — marker arm returns byte count, not None

**Guarded element:** `_attempt_stdout_truncated` marker arm return — axis: genuine head marker grades truncated via byte count.

**Neutralization:** changed `return observed if observed is not None else size` to `return None`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_genuine_truncation_detected_without_stdout_bytes \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert None is not None
plugins/superheroes/lib/tests/test_engine_dispatch.py:5448: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_genuine_truncation_detected_without_stdout_bytes
1 failed in 0.47s
```

**Restore:** restored `return observed if observed is not None else size`.

**Restore receipt:** *(empty — `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` returned no output)*

**Green run:**
```
1 passed in 0.40s
```

---

## E8 — the `observed is not None` conjunct in arm 4

**Guarded element:** arm 4 `observed is not None` conjunct — axis: provenance stamp alone, with no recorded count, must not suppress the marker read.

**Neutralization:** deleted `observed is not None and ` from arm 4, leaving `if ended.get("stdoutBytesPreCap") is True: return None`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stamp_without_recorded_count_does_not_suppress \
  -q -p no:randomly
```

**Red run:**
```
E       AssertionError: assert None == 54
plugins/superheroes/lib/tests/test_engine_dispatch.py:5540: AssertionError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stamp_without_recorded_count_does_not_suppress
1 failed in 0.46s
```

**Restore:** restored `observed is not None and ` conjunct.

**Restore receipt:** *(empty — `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` returned no output)*

**Green run:**
```
1 passed in 0.39s
```

**Note:** The first-attempt neutralization/detector pairing (`test_genuine_truncation_detected_without_stdout_bytes`) was unprovable as placed — that fixture's `ended` dict is empty, so arm 4 does not fire with or without the conjunct. Restructured via R9 (`test_stamp_without_recorded_count_does_not_suppress`) rather than disclosed.

---

## E9 — the producer actually stamps

**Guarded element:** `_run_engine_files` `stdoutBytesPreCap` stamp — axis: the gate is not inert; real spawn asserts pre-cap measurement.

**Neutralization:** removed `ended_record["stdoutBytesPreCap"] = True` from `_run_engine_files`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_engine_files_stamps_stdout_bytes_pre_cap \
  -q -p no:randomly
```

**Red run:**
```
E       KeyError: 'stdoutBytesPreCap'
plugins/superheroes/lib/tests/test_engine_dispatch.py:5572: KeyError
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_engine_files_stamps_stdout_bytes_pre_cap
1 failed in 1.10s
```

**Restore:** re-inserted `ended_record["stdoutBytesPreCap"] = True` in `_run_engine_files`.

**Restore receipt:** *(empty — `git status --porcelain plugins/superheroes/lib/engine_dispatch.py` returned no output)*

**Green run:**
```
1 passed in 1.00s
```

---

## Final green (all ten node ids)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_under_cap_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_cap_boundary_count_overrides_forged_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_injected_seam_count_does_not_override_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_unstamped_unknown_producer_count_does_not_override_head_marker \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_recorded_over_cap_count_grades_truncated_with_under_cap_file \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_file_over_cap_grades_truncated_despite_under_cap_recorded_count \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_complete_marker_mid_body_not_truncated \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_genuine_truncation_detected_without_stdout_bytes \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stamp_without_recorded_count_does_not_suppress \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_run_engine_files_stamps_stdout_bytes_pre_cap \
  -q -p no:randomly
```

**Output:**
```
10 passed in 1.06s
```
