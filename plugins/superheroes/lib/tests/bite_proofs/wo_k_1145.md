# WO-K (#1145) bite-proofs — emitting-site census comment/docstring class closure

## BP1 — comment-only neutralization cannot satisfy C2b home-reference leg

**Guarded element:** `test_review_findings_example_emitting_sites_delegate_to_home` — axis: C2b home-reference assertion is satisfied only by code in `_panel_stdout_delivery_text`, never by a comment or docstring.

**Neutralization:** in `_panel_stdout_delivery_text`, removed `review_findings_schema.example_prompt_block()` delegation and left only `# Delivery block is format-only (wraps review_findings_schema.example_findings_object).` (`plugins/superheroes/lib/round_orders.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home -q
```

**Red run (home-reference leg — not hand-literal leg):**
```
=================================== FAILURES ===================================
_________ test_review_findings_example_emitting_sites_delegate_to_home _________

>           assert home_ref in region, (
E           AssertionError: C2b (lib/round_orders.py:_panel_stdout_delivery_text): missing home reference 'review_findings_schema.example_prompt_block'

FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home
1 failed in 0.20s
```

**Restore:** reinstated `review_findings_schema.example_prompt_block(),` as the first `lines` entry in `_panel_stdout_delivery_text`.

**Restore receipt:** `review_findings_schema.example_prompt_block(),` first element of `lines`.

**Green run:**
```
1 passed in 0.16s
```

---

## BP2 — re-pointed C2b row bites `_panel_stdout_delivery_text`

**Guarded element:** `test_review_findings_example_emitting_sites_delegate_to_home` — axis: C2b row anchored to `_panel_stdout_delivery_text` fails when that definition regresses away from home rendering.

**Neutralization:** in `_panel_stdout_delivery_text`, replaced `review_findings_schema.example_prompt_block()` with hand literal `'{"findings": [...], "investigated": [...]}'` (`plugins/superheroes/lib/round_orders.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home -q
```

**Red run (hand-literal leg):**
```
=================================== FAILURES ===================================
_________ test_review_findings_example_emitting_sites_delegate_to_home _________

>               assert literal not in region, (
E               AssertionError: C2b (lib/round_orders.py:_panel_stdout_delivery_text): hand-written findings example literal reappeared: '{"findings": [...], "investigated": [...]}'

FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_example_emitting_sites_delegate_to_home
1 failed in 0.87s
```

**Restore:** first `lines` entry again `review_findings_schema.example_prompt_block(),`.

**Restore receipt:** `review_findings_schema.example_prompt_block(),` first element of `lines`.

**Green run:**
```
1 passed in 0.17s
```
