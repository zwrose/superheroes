# WO-D (#1185) bite-proofs — owner-gate `_provenance` producer declaration + drift pin

## BP-D1 — `ruledBy` required-field check in `_owner_artifact_provenance_well_formed`

**Guarded element:** `_owner_artifact_provenance_well_formed` `ruledBy` branch — axis: a malformed `_provenance` block with blank `ruledBy` is rejected, so an owner ruling that is not well-formed is journalled `owner-unattributed`.

**Neutralization:** inserted `if field == "ruledBy": continue` inside the predicate loop in `_owner_artifact_provenance_well_formed` at `plugins/superheroes/lib/round_driver.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_artifact_provenance_well_formed_rejects_blank_ruled_by' \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-7372e31a71b53878/plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:212: AssertionError: assert not True
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_artifact_provenance_well_formed_rejects_blank_ruled_by
1 failed in 0.11s
```

**Restore:** removed the `if field == "ruledBy": continue` line.

**Restore receipt:** predicate loop restored to derive all three fields from `OWNER_PROVENANCE_FIELD_SHAPES` without a `ruledBy` skip.

**Green run (exit 0):** covered by combined green run below.

---

## BP-D2 — `ruledAt` required-field check in `_owner_artifact_provenance_well_formed`

**Guarded element:** `_owner_artifact_provenance_well_formed` `ruledAt` branch — axis: a malformed `_provenance` block with blank `ruledAt` is rejected, so an owner ruling that is not well-formed is journalled `owner-unattributed`.

**Neutralization:** inserted `if field == "ruledAt": continue` inside the predicate loop.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_artifact_provenance_well_formed_rejects_blank_ruled_at' \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-7372e31a71b53878/plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:217: AssertionError: assert not True
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_artifact_provenance_well_formed_rejects_blank_ruled_at
1 failed in 0.11s
```

**Restore:** removed the `if field == "ruledAt": continue` line.

**Restore receipt:** predicate loop restored without a `ruledAt` skip.

**Green run (exit 0):** covered by combined green run below.

---

## BP-D3 — `records` required-field check in `_owner_artifact_provenance_well_formed`

**Guarded element:** `_owner_artifact_provenance_well_formed` `records` branch — axis: a malformed `_provenance` block with empty `records` is rejected, so an owner ruling that is not well-formed is journalled `owner-unattributed`.

**Neutralization:** inserted `if field == "records": continue` inside the predicate loop.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_artifact_provenance_well_formed_rejects_empty_records' \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-7372e31a71b53878/plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:222: AssertionError: assert not True
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_artifact_provenance_well_formed_rejects_empty_records
1 failed in 0.11s
```

**Restore:** removed the `if field == "records": continue` line.

**Restore receipt:** predicate loop restored without a `records` skip.

**Green run (exit 0):** covered by combined green run below.

---

## BP-D4 — drift pin `test_owner_provenance_field_shapes_match_docs`

**Guarded element:** `test_owner_provenance_field_shapes_match_docs` — axis: documented `field — shape` pairs match `OWNER_PROVENANCE_FIELD_SHAPES` verbatim.

**Neutralization:** changed `ruledBy — non-empty string` to `ruledBy — nonempty string` in the fenced block at `plugins/superheroes/skills/review-code/reference/round-driver.md`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_provenance_field_shapes_match_docs' \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/private/tmp/sh1185-wod/plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:143: AssertionError: owner-gate _provenance field shape mismatch between docs and code: {'ruledBy': ('nonempty string', 'non-empty string')}
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_provenance_field_shapes_match_docs
1 failed in 0.11s
```

**Restore:** changed `ruledBy — nonempty string` back to `ruledBy — non-empty string`.

**Restore receipt:** `ruledBy — non-empty string`

**Green run (exit 0):**
```
.....                                                                    [100%]
5 passed in 2.19s
```
(combined with boundary test and three negative unit rows)

**Post-restore `git status --porcelain`:**
```
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py
 M plugins/superheroes/skills/review-code/reference/round-driver.md
```

---

## BP-D5 — drift pin `test_gate_artifact_example_dispositions_match_judgment_vocabulary`

**Guarded element:** `test_gate_artifact_example_dispositions_match_judgment_vocabulary` — axis: every disposition value in the worked `present-judgment` gate-artifact JSON example is a member of `JUDGMENT_DISPOSITIONS`.

**Neutralization:** changed `"disposition": "fix-as-suggested"` to `"disposition": "accept"` in the fenced JSON example at `plugins/superheroes/skills/review-code/reference/round-driver.md`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_gate_artifact_example_dispositions_match_judgment_vocabulary' \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/Users/zwrose/.superheroes-worktrees/superheroes/issue-1185-7372e31a71b53878/plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:162: AssertionError: round-driver.md gate-artifact example uses judgment dispositions the driver does not recognize: ['accept']
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_gate_artifact_example_dispositions_match_judgment_vocabulary
1 failed in 0.11s
```

**Restore:** changed `"disposition": "accept"` back to `"disposition": "fix-as-suggested"`.

**Restore receipt:** `"disposition": "fix-as-suggested"`

**Green run (exit 0):**
```
....                                                                     [100%]
4 passed in 0.11s
```
(combined green run for BP-D1..D3 negative rows and BP-D5)

**Post-restore `git status --porcelain`:**
```
 M plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py
 M plugins/superheroes/lib/tests/bite_proofs/wo_d_1185.md
 M plugins/superheroes/skills/review-code/reference/round-driver.md
```
