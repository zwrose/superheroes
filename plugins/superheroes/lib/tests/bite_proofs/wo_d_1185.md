# WO-D (#1185) bite-proofs — owner-gate `_provenance` producer declaration + drift pin

## BP-D1 — `ruledBy` required-field check in `_owner_artifact_provenance_well_formed`

**Guarded element:** `_owner_artifact_provenance_well_formed` `ruledBy` branch — axis: a well-formed block requires `ruledBy` as a non-empty string.

**Neutralization:** inserted `if field == "ruledBy": return False` before the predicate loop in `_owner_artifact_provenance_well_formed` at `plugins/superheroes/lib/round_driver.py`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_round_driver_advance.py::test_advance_owner_artifact_well_formed_provenance_yields_owner_supplied' \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/private/tmp/sh1185-wod/plugins/superheroes/lib/tests/test_round_driver_advance.py:2368: AssertionError: assert 'owner-unattributed' == 'owner-supplied'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_advance.py::test_advance_owner_artifact_well_formed_provenance_yields_owner_supplied
1 failed in 2.18s
```

**Restore:** removed the `if field == "ruledBy": return False` line.

**Restore receipt:** predicate loop restored to derive all three fields from `OWNER_PROVENANCE_FIELD_SHAPES` without a `ruledBy` short-circuit.

**Green run (exit 0):** covered by combined green run below.

---

## BP-D2 — `ruledAt` required-field check in `_owner_artifact_provenance_well_formed`

**Guarded element:** `_owner_artifact_provenance_well_formed` `ruledAt` branch — axis: a well-formed block requires `ruledAt` as a non-empty string.

**Neutralization:** inserted `if field == "ruledAt": return False` before the predicate loop.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_round_driver_advance.py::test_advance_owner_artifact_well_formed_provenance_yields_owner_supplied' \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/private/tmp/sh1185-wod/plugins/superheroes/lib/tests/test_round_driver_advance.py:2368: AssertionError: assert 'owner-unattributed' == 'owner-supplied'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_advance.py::test_advance_owner_artifact_well_formed_provenance_yields_owner_supplied
1 failed in 2.17s
```

**Restore:** removed the `if field == "ruledAt": return False` line.

**Restore receipt:** predicate loop restored without a `ruledAt` short-circuit.

**Green run (exit 0):** covered by combined green run below.

---

## BP-D3 — `records` required-field check in `_owner_artifact_provenance_well_formed`

**Guarded element:** `_owner_artifact_provenance_well_formed` `records` branch — axis: a well-formed block requires `records` as a non-empty list of non-empty strings.

**Neutralization:** inserted `if field == "records": return False` before the predicate loop.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_round_driver_advance.py::test_advance_owner_artifact_well_formed_provenance_yields_owner_supplied' \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/private/tmp/sh1185-wod/plugins/superheroes/lib/tests/test_round_driver_advance.py:2368: AssertionError: assert 'owner-unattributed' == 'owner-supplied'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_advance.py::test_advance_owner_artifact_well_formed_provenance_yields_owner_supplied
1 failed in 2.19s
```

**Restore:** removed the `if field == "records": return False` line.

**Restore receipt:** predicate loop restored without a `records` short-circuit.

**Green run (exit 0):** covered by combined green run below.

---

## BP-D4 — drift pin `test_owner_provenance_field_shapes_match_docs`

**Guarded element:** `test_owner_provenance_field_shapes_match_docs` — axis: documented `field — shape` pairs match `OWNER_PROVENANCE_FIELD_SHAPES` verbatim.

**Neutralization:** changed `ruledBy — non-empty string` to `ruledBy — nonempty string` in the fenced block at `plugins/superheroes/skills/review-code/reference/round-driver.md`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  'plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_provenance_field_shapes_match_docs' \
  -q --tb=line
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
/private/tmp/sh1185-wod/plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py:143: AssertionError: owner-gate _provenance field shape mismatch between docs and code: {'ruledBy': ('nonempty string', 'non-empty string')}
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py::test_owner_provenance_field_shapes_match_docs
1 failed in 0.11s
```

**Restore:** changed `ruledBy — nonempty string` back to `ruledBy — non-empty string`.

**Restore receipt:** `ruledBy — non-empty string`

**Green run (exit 0):**
```
.....                                                                    [100%]
5 passed in 2.19s
```
(combined with boundary test and three negative unit rows)

**Post-restore `git status --porcelain`:**
```
 M plugins/superheroes/lib/round_driver.py
 M plugins/superheroes/lib/tests/test_round_driver_owner_artifact_vocabulary_sync.py
 M plugins/superheroes/skills/review-code/reference/round-driver.md
```
