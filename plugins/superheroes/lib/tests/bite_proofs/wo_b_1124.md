# WO-B (#1124 items 6 + 17) bite-proofs — forfeit disclosure truth + marker-overruled journal

## Baseline (pre-fix — implementation reverted, tests present)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_capped_forfeit_disclosure_attempt1_names_attempt_and_refused_retry \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_journal_on_authoritative_suppression \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_oversized_digit_claim_null_marker_bytes \
  -q
```

**Output:**
```
.FFF                                                                     [100%]
=================================== FAILURES ===================================
_ test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused _

    assert "attempt 2" in disclosure
E   AssertionError: assert 'attempt 2' in 'cursor attempt 1 report was truncated at the 9000000-byte stdout capture cap; the retry was refused because the grade...'

______ test_stdout_marker_overruled_journal_on_authoritative_suppression _______

    assert len(overruled) == 1
E   assert 0 == 1

_____ test_stdout_marker_overruled_oversized_digit_claim_null_marker_bytes _____

    assert len(overruled) == 1
E   assert 0 == 1

3 failed, 1 passed in 0.97s
```

---

## BP1 — attempt-number substitution in disclosure

**Guarded element:** `_stdout_capped_forfeit` disclosure attempt clause — axis: disclosure names the real `attempts` parameter, not a hard-coded 1.

**Neutralization:** substituted `int(attempts)` with literal `1` in the disclosure format tuple (`engine_dispatch.py` disclosure `%` line).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused _
plugins/superheroes/lib/tests/test_engine_dispatch.py:5726: in test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused
    assert "attempt 2" in disclosure
E   AssertionError: assert 'attempt 2' in 'cursor attempt 1 report was truncated at the 9000000-byte stdout capture cap; the attempt budget was exhausted. The w...'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused
1 failed in 0.79s
```

**Restore:** reverted disclosure tuple to `% (engine, int(attempts), observed_int, reason_clause)`.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.73s
```

---

## BP2 — reason branch in disclosure

**Guarded element:** `_stdout_capped_forfeit` reason branch — axis: `attempts >= MAX_ATTEMPTS` cites exhausted budget, not a refused retry.

**Neutralization:** changed the `else` branch `reason_clause` to the retry-refused sentence (same as the `< MAX_ATTEMPTS` branch).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused _
plugins/superheroes/lib/tests/test_engine_dispatch.py:5727: in test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused
    assert "retry was refused" not in disclosure
E   AssertionError: assert 'retry was refused' not in 'cursor atte...g the order.'
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused
1 failed in 0.85s
```

**Restore:** restored `else` branch to `reason_clause = "the attempt budget was exhausted. "`.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.70s
```

---

## BP3 — marker-overruled journal write

**Guarded element:** `_attempt_stdout_truncated` suppression branch `_journal_append` — axis: stamped under-cap count overrules head marker and journals both counts.

**Neutralization:** deleted the `_journal_append(...)` call on the suppression branch (left `pass` under the marker-present guard).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_journal_on_authoritative_suppression \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
______ test_stdout_marker_overruled_journal_on_authoritative_suppression _______
plugins/superheroes/lib/tests/test_engine_dispatch.py:5743: in test_stdout_marker_overruled_journal_on_authoritative_suppression
    assert len(overruled) == 1
E   assert 0 == 1
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_journal_on_authoritative_suppression
1 failed in 0.82s
```

**Restore:** restored the `_journal_append` block with `kind: stdout-marker-overruled` and both byte counts.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.73s
```

---

## BP4 — digit-run bound in marker extraction

**Guarded element:** `_bounded_stdout_marker_claimed_bytes` length bound — axis: oversized forged digit run yields `markerBytes: null` without raising on the suppression path.

**Neutralization:** removed the `len(digits) > _STDOUT_MARKER_CLAIMED_BYTES_DIGITS_MAX` guard so a 5000-digit marker reaches `int()`.

**Branch reach confirmation:** red failure is on `markerBytes is None` after `len(overruled) == 1` — the oversized marker was parsed and journaled, proving the suppression branch and extraction path ran (not a vacuous green).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_oversized_digit_claim_null_marker_bytes \
  -q --tb=short
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_____ test_stdout_marker_overruled_oversized_digit_claim_null_marker_bytes _____
plugins/superheroes/lib/tests/test_engine_dispatch.py:5773: in test_stdout_marker_overruled_oversized_digit_claim_null_marker_bytes
    assert overruled[0]["markerBytes"] is None
E   assert 999999999999999999...9999999999999999999 is None
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_oversized_digit_claim_null_marker_bytes
1 failed in 0.85s
```

**Restore:** restored `if len(digits) > _STDOUT_MARKER_CLAIMED_BYTES_DIGITS_MAX: return None`.

**Restore receipt:** ` M plugins/superheroes/lib/engine_dispatch.py`

**Green run:**
```
.                                                                        [100%]
1 passed in 0.70s
```

---

## Final green (all WO-B tests)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_capped_forfeit_disclosure_attempt1_names_attempt_and_refused_retry \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_capped_forfeit_disclosure_attempt2_names_attempt_not_retry_refused \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_journal_on_authoritative_suppression \
  plugins/superheroes/lib/tests/test_engine_dispatch.py::test_stdout_marker_overruled_oversized_digit_claim_null_marker_bytes \
  -q
```

**Output:**
```
....                                                                     [100%]
4 passed in 0.67s
```
