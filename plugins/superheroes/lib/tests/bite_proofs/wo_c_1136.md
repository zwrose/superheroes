# WO-C (#1136 carve round) bite-proofs — detectors derive their census

## BP-C1a — `test_bootstrap_surface_reads_provisional_and_discloses` (missing disclosure pin)

**Guarded element:** `skills/audit-debt/SKILL.md` disclosure prose in the bootstrap window — axis: a surface that calls `decide-location` without the byte-pinned disclosure sentence must go red.

**Neutralization:** deleted the `**Storage location (\`decide-location\`).** … **Follow-up:** \`/superheroes:configure\`.` paragraph from `plugins/superheroes/skills/audit-debt/SKILL.md` (temporary edit; file restored after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_bootstrap_surface_reads_provisional_and_discloses[skills/audit-debt/SKILL.md] -q -p no:warnings
```

**Red run:**
```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_bootstrap_surface_reads_provisional_and_discloses[skills/audit-debt/SKILL.md] _
...
E       AssertionError: skills/audit-debt/SKILL.md is missing the byte-pinned disclosure sentence for carrier 'audit report' in the bootstrap window — a default would be taken silently (#1136)
plugins/superheroes/lib/tests/test_presence_flag_retired.py:481: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_bootstrap_surface_reads_provisional_and_discloses[skills/audit-debt/SKILL.md]
1 failed in 0.05s
```

**Restore:** re-inserted the deleted disclosure paragraph at its original location in `plugins/superheroes/skills/audit-debt/SKILL.md`.

**Restore receipt:** the `**Storage location (\`decide-location\`).** … **Follow-up:** \`/superheroes:configure\`.` paragraph is present again in `plugins/superheroes/skills/audit-debt/SKILL.md`.

**Green run:**
```
.                                                                        [100%]
1 passed in 0.03s
```

---

## BP-C1b — `test_bootstrap_disclosure_pin_cardinality_floor` (extra call site)

**Guarded element:** derived decide-location census vs `_DISCLOSURE_PIN_BY_SURFACE` — axis: a new call site without a matching pin must go red.

**Neutralization:** appended `DEC=$(python3 -B "$ROOT_DIR/lib/review_store.py" decide-location)` to `plugins/superheroes/skills/showrunner/SKILL.md` (temporary edit; line removed after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_bootstrap_disclosure_pin_cardinality_floor -q -p no:warnings
```

**Red run:**
```
=================================== FAILURES ===================================
>       assert not missing_pins, (
E       AssertionError: decide-location call site(s) lack a disclosure pin — add to _DISCLOSURE_PIN_BY_SURFACE (#1136). Missing:
E         skills/showrunner/SKILL.md
plugins/superheroes/lib/tests/test_presence_flag_retired.py:447: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_bootstrap_disclosure_pin_cardinality_floor
```

**Restore:** removed the appended `decide-location` line from `plugins/superheroes/skills/showrunner/SKILL.md`.

**Restore receipt:** `showrunner/SKILL.md` no longer contains a `DEC=$(python… decide-location)` invocation.

**Green run:**
```
1 passed in 0.04s
```

---

## BP-C2a — `test_bootstrap_surface_safety_guards` (non-zero-exit guard)

**Guarded element:** `skills/audit-debt/SKILL.md` bootstrap bash block — axis: non-zero-exit guard on `decide-location` must be present.

**Neutralization:** removed `|| { echo "decide-location exited non-zero …" >&2; exit 1; }` from the `DEC=$(… decide-location)` line in `plugins/superheroes/skills/audit-debt/SKILL.md` (temporary edit; guard restored after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_bootstrap_surface_safety_guards[skills/audit-debt/SKILL.md] -q -p no:warnings
```

**Red run:**
```
=================================== FAILURES ===================================
>       assert _NONZERO_EXIT_GUARD in bash_block, (
E       AssertionError: skills/audit-debt/SKILL.md bootstrap is missing the non-zero-exit guard on decide-location (#1136)
plugins/superheroes/lib/tests/test_presence_flag_retired.py:505: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_bootstrap_surface_safety_guards[skills/audit-debt/SKILL.md]
```

**Restore:** re-applied `|| { echo "decide-location exited non-zero (exit $?); halting rather than taking an undisclosed storage default" >&2; exit 1; }` on the `DEC=$(… decide-location)` line.

**Restore receipt:** `decide-location) || {` is present again in the audit-debt bootstrap bash block.

**Green run:**
```
1 passed in 0.03s
```

---

## BP-C2b — `test_bootstrap_surface_safety_guards` (reconcile --mode absence)

**Guarded element:** `skills/audit-debt/SKILL.md` bootstrap bash block — axis: no `reconcile --mode` invocation during bootstrap (R-1).

**Neutralization:** inserted `python3 -B "$ROOT_DIR/lib/review_store.py" reconcile --mode "$LOC"` into the bootstrap bash block in `plugins/superheroes/skills/audit-debt/SKILL.md` (temporary edit; line removed after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_bootstrap_surface_safety_guards[skills/audit-debt/SKILL.md] -q -p no:warnings
```

**Red run:**
```
=================================== FAILURES ===================================
>       assert not _RECONCILE_MODE_PATTERN.search(bash_block), (
E       AssertionError: skills/audit-debt/SKILL.md bootstrap still contains reconcile --mode — bootstrap reconciliation is retired (#1136)
plugins/superheroes/lib/tests/test_presence_flag_retired.py:511: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_presence_flag_retired.py::test_bootstrap_surface_safety_guards[skills/audit-debt/SKILL.md]
```

**Restore:** removed the inserted `reconcile --mode` line from the bootstrap bash block.

**Restore receipt:** bootstrap bash block in `audit-debt/SKILL.md` contains no `reconcile --mode` substring.

**Green run:**
```
1 passed in 0.03s
```

---

## BP-C3 — `test_review_code_surfaces_ask_user_question_census`

**Guarded element:** `skills/review-code/reference/verification-pass.md` — axis: every `AskUserQuestion` occurrence under `skills/review-code/` must match an allowlisted prohibition line.

**Neutralization:** inserted the line `use AskUserQuestion to confirm` after `## Where it runs` in `plugins/superheroes/skills/review-code/reference/verification-pass.md` (temporary edit; line removed after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_only_headless.py::test_review_code_surfaces_ask_user_question_census -q -p no:warnings
```

**Red run:**
```
=================================== FAILURES ===================================
>       assert not hits, (
E       AssertionError: an `AskUserQuestion` occurrence is not one of the pinned prohibition lines — a positive instruction or evasion would pass a verb heuristic (#1136). Hits:
E         skills/review-code/reference/verification-pass.md line 19: use AskUserQuestion to confirm
plugins/superheroes/lib/tests/test_review_only_headless.py:182: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_only_headless.py::test_review_code_surfaces_ask_user_question_census
```

**Restore:** removed `use AskUserQuestion to confirm` from `verification-pass.md`.

**Restore receipt:** `verification-pass.md` has no `AskUserQuestion` occurrences.

**Green run:**
```
1 passed in 0.03s
```

---

## BP-C4 — `test_inline_bootstrap_writes_headless_answers`

**Guarded element:** inline bootstrap paragraph in `skills/review-code/reference/setup.md` — axis: byte-pin of the normative uncommitted sentence, not a substring on `uncommitted`.

**Neutralization:** replaced the normative sentence with `do not leave files uncommitted; commit them automatically` (still contains the word `uncommitted`; temporary edit; original sentence restored after capture).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_only_headless.py::test_inline_bootstrap_writes_headless_answers -q -p no:warnings
```

**Red run:**
```
=================================== FAILURES ===================================
>       assert _NORMATIVE_UNCOMMITTED_SENTENCE in route, (
E       AssertionError: the inline bootstrap is missing the byte-pinned normative sentence requiring created files stay uncommitted and untracked with no commit question — prose that merely contains 'uncommitted' while authorizing the opposite would pass a substring search (#1136)
plugins/superheroes/lib/tests/test_review_only_headless.py:261: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_review_only_headless.py::test_inline_bootstrap_writes_headless_answers
```

**Restore:** re-inserted the full normative sentence:
`When the created layer lands **in-repo**, **do not ask whether to commit** the new files: write the core + layer, leave them **uncommitted and untracked**, say so in the **dispatch summary**, and continue. Committing them unasked would be a review writing to the user's index — the honest answer is to leave the files for a human to stage.`

**Restore receipt:** the normative sentence above is present again in `setup.md`.

**Green run:**
```
1 passed in 0.03s
```
