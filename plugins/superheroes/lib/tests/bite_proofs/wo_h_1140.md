# WO-H (#1140 round 3) bite-proofs — missing_wiring_files presence checks (H1–H4)

The detector under proof is `missing_wiring_files()` in repo-root `source_guard.py`. Neutralizations
were applied as targeted reversible edits to `source_guard.py` for red runs and restored by
inverse edit before the green half; in-test proofs use `bite_support.patched_module`
(in-memory only).

## Baseline

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_source_guard.py -q
```

**Output:**
```
54 passed in 33.49s
```

---

## H1 — pytest.ini presence check

**Guarded element:** `os.path.isfile(pytest_ini_path)` — axis: a tree with no `pytest.ini` must
report `"pytest.ini is missing at the repository root"`.

**Neutralization:** `if not os.path.isfile(pytest_ini_path):` →
`if False and not os.path.isfile(pytest_ini_path):`.

**Red run** (`test_missing_wiring_files_reports_missing_pytest_ini` with disk neutralization):
```
FAILED ...::test_missing_wiring_files_reports_missing_pytest_ini
E   AssertionError: assert 'pytest.ini is missing at the repository root' in []
1 failed in 0.08s
```

**Restore:** inverse edit restoring `if not os.path.isfile(pytest_ini_path):`.

**Green run** (`test_bite_missing_wiring_files_reports_pytest_ini`):
```
3 passed in 0.64s
```
(includes H1–H3 bite tests in the green batch command below)

---

## H2 — conftest.py presence check

**Guarded element:** `os.path.isfile(conftest_path)` — axis: a tree with no `conftest.py` must
report `"conftest.py is missing at the repository root"`.

**Neutralization:** `if not os.path.isfile(conftest_path):` →
`if False and not os.path.isfile(conftest_path):`.

**Red run** (`test_missing_wiring_files_reports_missing_conftest` with disk neutralization):
```
FAILED ...::test_missing_wiring_files_reports_missing_conftest
E   AssertionError: assert 'conftest.py is missing at the repository root' in []
1 failed in 0.08s
```

**Restore:** inverse edit restoring `if not os.path.isfile(conftest_path):`.

**Green run:** `test_bite_missing_wiring_files_reports_conftest` passes in H1–H3 green batch.

---

## H3 — source_guard.py presence check

**Guarded element:** `os.path.isfile(source_guard_path)` — axis: a tree with no `source_guard.py`
must report `"source_guard.py is missing at the repository root"`.

**Neutralization:** `if not os.path.isfile(source_guard_path):` →
`if False and not os.path.isfile(source_guard_path):`.

**Red run** (`test_missing_wiring_files_reports_missing_source_guard` with disk neutralization):
```
FAILED ...::test_missing_wiring_files_reports_missing_source_guard
E   AssertionError: assert 'source_guard.py is missing at the repository root' in []
1 failed in 0.08s
```

**Restore:** inverse edit restoring `if not os.path.isfile(source_guard_path):`.

**Green run:** `test_bite_missing_wiring_files_reports_source_guard` passes in H1–H3 green batch.

---

## H4 — empty-root isdir guard (fail-closed)

**Guarded element:** `os.path.isdir(root)` — axis: `root=""` must report all three required files
missing even when the process cwd contains them (fail-closed, not fail-open via cwd fall-through).

**Neutralization:** `if not os.path.isdir(root):` →
`if False and not os.path.isdir(root):`.

**Red run** (`test_missing_wiring_files_fails_closed_for_empty_root` with disk neutralization):
```
FAILED ...::test_missing_wiring_files_fails_closed_for_empty_root
E   AssertionError: assert 'pytest.ini is missing at the repository root' in []
1 failed in 0.09s
```

**Restore:** inverse edit restoring `if not os.path.isdir(root):`.

**Green run** (`test_bite_missing_wiring_files_fails_closed_for_empty_root`):
```
1 passed in 0.07s
```

---

## Final green (H1–H3 bite tests)

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest \
  plugins/superheroes/lib/tests/test_source_guard.py::test_bite_missing_wiring_files_reports_pytest_ini \
  plugins/superheroes/lib/tests/test_source_guard.py::test_bite_missing_wiring_files_reports_conftest \
  plugins/superheroes/lib/tests/test_source_guard.py::test_bite_missing_wiring_files_reports_source_guard \
  -q
```

**Output:**
```
3 passed in 0.64s
```
