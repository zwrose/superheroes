# WO-SM6 (#1269) bite-proof — max-wait entry refusal + whitelist census

**Provenance:** cursor composer-2.5 / dispatch-write

## Guarded elements

| ID | Guarded element | Axis | Proving test |
|---|---|---|---|
| BP-SM6-1 | `_max_wait_refusal` run_dir threading | I1 — out-of-range `--max-wait` on continuation echoes `runOpened: true` and journal `runDir` | `test_max_wait_refusal_preserves_existing_review_run_provenance` |
| BP-SM6-2 | Entry-refusal return whitelist census | return not traceable to an approved producer | `test_entry_refusal_chokepoint_invariant_returns_trace_to_approved_producers` |

**Item 3 note:** the inverted `_backfill` tier census is proven by
`test_sm6_1269_backfill_tier_census_rejects_planted_shapes` over synthetic source text — no
production mutation proof is needed because the detector exercises escape shapes directly without
touching `seat_map.py`.

---

## BP-SM6-1 — `_max_wait_refusal` routes through chokepoint

- **axis:** I1 — out-of-range `--max-wait` on continuation echoes `runOpened: true` and journal `runDir`

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `_max_wait_refusal`):
```python
    return _with_run_fields(
        {"ok": False, "reason": dispatch_outcome.REASON_UNRUNNABLE, "detail": detail,
         "attempts": 0, "forfeited": False, "terminal": True},
        run_dir="", argv=[],
    )
```
(replaces `_entry_refusal_terminal(refusal, run_dir=run_dir, ...)`)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch.py::test_max_wait_refusal_preserves_existing_review_run_provenance -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
________ test_max_wait_refusal_preserves_existing_review_run_provenance ________

    def test_max_wait_refusal_preserves_existing_review_run_provenance(tmp_path):
        ...
>       assert res.get("runOpened") is True
E       AssertionError: assert False is True

plugins/superheroes/lib/tests/test_engine_dispatch.py:9355: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_max_wait_refusal_preserves_existing_review_run_provenance
1 failed in 0.52s
```

**restore:** revert `_max_wait_refusal` to build the refusal dict and `return _entry_refusal_terminal(refusal, run_dir=run_dir, mode=mode, argv=[], repo_root=repo_root, engine=engine, run_kind=run_kind)`.

**raw green:**
```
.                                                                        [100%]
1 passed in 0.44s
```

---

## BP-SM6-2 — entry-refusal return whitelist census

- **axis:** a return not traceable to an approved producer

**neutralization** (`plugins/superheroes/lib/engine_dispatch.py`, `dispatch_review` legacy-call branch):
```python
            out = {}
            out["runDir"] = ""
            return out
```
(replaces `return _entry_refusal_terminal(_legacy_dispatch_refusal(...), run_dir=run_dir, mode=...)`)

**command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_dispatch.py::test_entry_refusal_chokepoint_invariant_returns_trace_to_approved_producers -q
```

**raw red** (exit 1):
```
F                                                                        [100%]
=================================== FAILURES ===================================
_ test_entry_refusal_chokepoint_invariant_returns_trace_to_approved_producers __

    def test_entry_refusal_chokepoint_invariant_returns_trace_to_approved_producers():
        ...
>       assert violations == []
E       AssertionError: assert [('dispatch_review', 3854)] == []

plugins/superheroes/lib/tests/test_engine_dispatch.py:9239: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_engine_dispatch.py::test_entry_refusal_chokepoint_invariant_returns_trace_to_approved_producers
1 failed in 0.55s
```

**restore:** revert the legacy-call branch to `return _entry_refusal_terminal(_legacy_dispatch_refusal(mode=mode or sanitized_view.MODE_REVIEW), run_dir=run_dir, mode=mode or sanitized_view.MODE_REVIEW)`.

**raw green:**
```
.                                                                        [100%]
1 passed in 0.47s
```
