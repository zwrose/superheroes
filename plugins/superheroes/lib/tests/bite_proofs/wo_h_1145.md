# WO-H (#1145) bite-proofs — review-round-2 blocker closure

## BP1 — FIX-1: part synonym secret scrub before normalize

**Guarded element:** `test_scrub_findings_part_synonym_secret_redacted_before_normalize` — axis: structural synonym keys scrub before normalization copies onto canonical structural keys.

**Neutralization:** removed the pre-scrub loop in `_scrub_findings` so `normalize_member` runs on the raw input (`plugins/superheroes/lib/engine_adapter.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_adapter.py::test_scrub_findings_part_synonym_secret_redacted_before_normalize -q
```

**Red run:**
```
=================================== FAILURES ===================================
______ test_scrub_findings_part_synonym_secret_redacted_before_normalize _______

    def test_scrub_findings_part_synonym_secret_redacted_before_normalize():
        secret = "ghp_EXAMPLEfakenotarealtoken000000000"
        ...
        for val in _walk_all_nested_values(accepted[0]):
            if isinstance(val, str):
>               assert secret not in val
E               AssertionError: assert 'ghp_EXAMPLE...ken000000000' not in 'ghp_EXAMPLE...ken000000000'

FAILED plugins/superheroes/lib/tests/test_engine_adapter.py::test_scrub_findings_part_synonym_secret_redacted_before_normalize
1 failed in 0.30s
```

**Restore:** reinstated pre-scrub loop before `normalize_member(pre_scrubbed)`.

**Restore receipt:** `pre_scrubbed = dict(f)` … `f = review_findings_schema.normalize_member(pre_scrubbed)`

**Green run:**
```
1 passed in 0.27s
```

---

## BP2 — FIX-2: example renderer survives fallback regime

**Guarded element:** `test_example_findings_object_works_in_fallback_regime` — axis: example renderer uses cached fallback metadata when shipped schema cannot be read.

**Neutralization:** reverted `_shipped_finding_property_schemas()` to reopen the schema file at render time instead of returning `FINDING_PROPERTY_SCHEMAS` (`plugins/superheroes/lib/review_findings_schema.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_review_findings_schema.py::test_example_findings_object_works_in_fallback_regime -q
```

**Red run:**
```
=================================== FAILURES ===================================
____________ test_example_findings_object_works_in_fallback_regime _____________

>       obj = mod.example_findings_object()
...
>       with open(REVIEW_FINDINGS_SCHEMA_PATH, encoding="utf-8") as fh:
E       FileNotFoundError: [Errno 2] No such file or directory: '.../missing-schema.json'

FAILED plugins/superheroes/lib/tests/test_review_findings_schema.py::test_example_findings_object_works_in_fallback_regime
1 failed in 0.96s
```

**Restore:** `_shipped_finding_property_schemas()` returns cached `FINDING_PROPERTY_SCHEMAS`.

**Restore receipt:** `return FINDING_PROPERTY_SCHEMAS`

**Green run:**
```
1 passed in 0.16s
```

---

## BP3 — FIX-3: investigated-only invalid payload keeps diagnosis

**Guarded element:** `test_review_payload_shape_investigated_only_invalid_entries_not_clean` — axis: investigated-only payloads agree with `_scrub_investigated` acceptance.

**Neutralization:** replaced `_scrub_investigated` gate with unconditional `return None` for any non-empty investigated list in `_review_payload_shape_findings_obj` (`plugins/superheroes/lib/engine_adapter.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_engine_adapter.py::test_review_payload_shape_investigated_only_invalid_entries_not_clean -q
```

**Red run:**
```
=================================== FAILURES ===================================
____ test_review_payload_shape_investigated_only_invalid_entries_not_clean _____

>       assert res is not None
E       assert None is not None

FAILED plugins/superheroes/lib/tests/test_engine_adapter.py::test_review_payload_shape_investigated_only_invalid_entries_not_clean
1 failed in 0.30s
```

**Restore:** reinstated `accepted, _ = _scrub_investigated(investigated)` with `if accepted: return None`.

**Restore receipt:** `accepted, _ = _scrub_investigated(investigated)` / `if accepted: return None`

**Green run:**
```
1 passed in 0.31s
```

---

## BP4 — FIX-4: SSOT test observes engine_adapter re-exports

**Guarded element:** `test_review_findings_renderer_and_grader_census_share_home_objects` — axis: grader census frozensets are `review_findings_schema` module singletons (identity, not re-typed literals).

**Neutralization:** rebound `engine_adapter.REVIEW_SEVERITY_TIERS` to `frozenset(set(review_findings_schema.SEVERITY_TIERS))` — equal by `==`, distinct by `is` (`plugins/superheroes/lib/engine_adapter.py`).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_renderer_and_grader_census_share_home_objects -q
```

**Red run:**
```
=================================== FAILURES ===================================
______ test_review_findings_renderer_and_grader_census_share_home_objects ______

>       assert ea.REVIEW_SEVERITY_TIERS is rfs.SEVERITY_TIERS
E       AssertionError: assert frozenset({'Critical', 'Important', 'Minor', 'Nit'}) is frozenset({'Critical', 'Important', 'Minor', 'Nit'})

FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_review_findings_renderer_and_grader_census_share_home_objects
1 failed in 0.18s
```

**Restore:** `REVIEW_SEVERITY_TIERS = review_findings_schema.SEVERITY_TIERS`

**Restore receipt:** `REVIEW_SEVERITY_TIERS = review_findings_schema.SEVERITY_TIERS`

**Green run:**
```
1 passed in 0.18s
```
