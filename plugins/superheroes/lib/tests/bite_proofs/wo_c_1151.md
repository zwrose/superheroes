# WO-C (#1151) bite-proofs — census exclusion, SAFETY_MACHINERY parity

## BP-C1 — `session_mode.py` SAFETY_MACHINERY membership

**Guarded element:** `escalation.SAFETY_MACHINERY` membership for `session_mode.py` — axis: fixer file-scope guard refuses the file under a band root.

**Neutralization:** removed `"session_mode.py",` line (and its comment) from `SAFETY_MACHINERY` in `plugins/superheroes/lib/escalation.py` only.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-c -m pytest plugins/superheroes/lib/tests/test_escalation.py::test_safety_machinery_set_members_are_pinned plugins/superheroes/eval/tests/test_escalation_eval.py -q
```

**Red run (exit 1):**
```
F..F.
=================================== FAILURES ===================================
_________________ test_safety_machinery_set_members_are_pinned _________________

    def test_safety_machinery_set_members_are_pinned():
        ...
>       assert set(ESC.SAFETY_MACHINERY) == {
            ...
        }
E       AssertionError: assert {'audits.py',...pter.py', ...} == {'audits.py',...pter.py', ...}
E         Extra items in the right set:
E         'session_mode.py'

plugins/superheroes/lib/tests/test_escalation.py:104: AssertionError
________________________ test_guard_fixture_exact_match ________________________

    def test_guard_fixture_exact_match():
        ...
>           assert (not ESC.is_safety_machinery(path, band)) == case["allow"], case
E           AssertionError: {'allow': False, 'path': 'plugins/superheroes/lib/session_mode.py'}

plugins/superheroes/eval/tests/test_escalation_eval.py:39: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_escalation.py::test_safety_machinery_set_members_are_pinned
FAILED plugins/superheroes/eval/tests/test_escalation_eval.py::test_guard_fixture_exact_match
2 failed, 3 passed in 0.12s
```

**Restore:** re-inserted `"session_mode.py",        # #1151: session-mode fail direction (UNRESOLVED_MODE -> PR); a fixer editing it could flip conservative PR default to branch and drop PR-only context`

**Restore receipt:** `"session_mode.py",        # #1151: session-mode fail direction (UNRESOLVED_MODE -> PR); a fixer editing it could flip conservative PR default to branch and drop PR-only context`

**Green run (exit 0):**
```
5 passed in 0.06s
```

---

## BP-C2 — `version_skew.py` SAFETY_MACHINERY membership

**Guarded element:** `escalation.SAFETY_MACHINERY` membership for `version_skew.py` — axis: fixer file-scope guard refuses the file under a band root.

**Neutralization:** removed `"version_skew.py",` line (and its comment) from `SAFETY_MACHINERY` in `plugins/superheroes/lib/escalation.py` only.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-c -m pytest plugins/superheroes/lib/tests/test_escalation.py::test_safety_machinery_set_members_are_pinned plugins/superheroes/eval/tests/test_escalation_eval.py -q
```

**Red run (exit 1):**
```
F..F.
=================================== FAILURES ===================================
_________________ test_safety_machinery_set_members_are_pinned _________________

    def test_safety_machinery_set_members_are_pinned():
        ...
>       assert set(ESC.SAFETY_MACHINERY) == {
            ...
        }
E       AssertionError: assert {'audits.py',...pter.py', ...} == {'audits.py',...pter.py', ...}
E         Extra items in the right set:
E         'version_skew.py'

plugins/superheroes/lib/tests/test_escalation.py:104: AssertionError
________________________ test_guard_fixture_exact_match ________________________

    def test_guard_fixture_exact_match():
        ...
>           assert (not ESC.is_safety_machinery(path, band)) == case["allow"], case
E           AssertionError: {'allow': False, 'path': 'plugins/superheroes/lib/version_skew.py'}

plugins/superheroes/eval/tests/test_escalation_eval.py:39: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_escalation.py::test_safety_machinery_set_members_are_pinned
FAILED plugins/superheroes/eval/tests/test_escalation_eval.py::test_guard_fixture_exact_match
2 failed, 3 passed in 0.08s
```

**Restore:** re-inserted `"version_skew.py",        # #1151: append-to-degradations rule (APPENDS_DEGRADATION); a fixer editing it could widen which skew statuses ride degradations`

**Restore receipt:** `"version_skew.py",        # #1151: append-to-degradations rule (APPENDS_DEGRADATION); a fixer editing it could widen which skew statuses ride degradations`

**Green run (exit 0):**
```
5 passed in 0.04s
```
---

## BP-C3 — anti-vacuity census test

**Guarded element:** `test_repo_markdown_files_excludes_tests_but_keeps_registered_docs` — axis: `_repo_markdown_files()` must still yield all four registered status-token copies and must not yield anything under `lib/tests/`.

**Neutralization:** broadened `_repo_markdown_files()` to also skip paths under `plugins/superheroes/skills/` (added `skills_root` and a second `commonpath` continue).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-c -m pytest plugins/superheroes/lib/tests/test_ssot_drift.py::test_repo_markdown_files_excludes_tests_but_keeps_registered_docs -q
```

**Red run (exit 1):**
```
F
=================================== FAILURES ===================================
________ test_repo_markdown_files_excludes_tests_but_keeps_registered_docs _______

    def test_repo_markdown_files_excludes_tests_but_keeps_registered_docs():
        ...
>       assert _PLUGIN_VERSION_SKEW_STATUS_TOKEN_COPY_REGISTER <= paths, (
            ...
        )
E       AssertionError: registered plugin-version-skew status-token copies missing from census: ['/private/tmp/sh1151-c/plugins/superheroes/skills/review-code/reference/round-driver.md', '/private/tmp/sh1151-c/plugins/superheroes/skills/review-code/reference/setup.md']

plugins/superheroes/lib/tests/test_ssot_drift.py:265: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_ssot_drift.py::test_repo_markdown_files_excludes_tests_but_keeps_registered_docs
1 failed in 0.18s
```

**Restore:** removed the `skills_root` variable and its `commonpath` continue (inverse of neutralization).

**Restore receipt:** `_repo_markdown_files()` excludes only `lib/tests/` via `tests_root`, not `skills/`.

**Green run (exit 0):**
```
1 passed in 0.14s
```
