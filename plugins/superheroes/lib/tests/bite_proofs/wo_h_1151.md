# WO-H (#1151) bite-proofs — reverse fixture-table drift pin (deletion direction)

## BP-H1 — stale known-table key after template placeholder deletion

**Guarded element:** `test_placeholder_fixture_known_table_covers_every_template_placeholder` — axis: every key in `_KNOWN_REALISTIC_VALUES[phase]` must be a direct placeholder that phase's template still declares or a registered auxiliary input (`_fixture_direct_placeholder_names`).

**Pre-fix contrast (same neutralization, without the reverse assertion):** removed the `stale_keys` loop and final `assert not stale_keys` (leaving the addition-direction halves intact), deleted `{{TARGET_ID}}` from `plugins/superheroes/rubric/orders/dispatch-audits.md`. Coverage test **PASS** — deletion direction was open.

**Neutralization:** deleted the line `- Target identifier (echo verbatim in your result): {{TARGET_ID}}` from `plugins/superheroes/rubric/orders/dispatch-audits.md` (full fix in place).

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-h -m pytest 'plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder' -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____ test_placeholder_fixture_known_table_covers_every_template_placeholder ____

    def test_placeholder_fixture_known_table_covers_every_template_placeholder():
        ...
>       assert not stale_keys, "stale fixture key(s) not declared in template or aux inputs: %s" % stale_keys
E       AssertionError: stale fixture key(s) not declared in template or aux inputs: ['dispatch-audits:TARGET_ID']
E       assert not ['dispatch-audits:TARGET_ID']

plugins/superheroes/lib/tests/test_round_driver_order_channel.py:711: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder
1 failed in 0.14s
```

**Restore:** restored `- Target identifier (echo verbatim in your result): {{TARGET_ID}}` in `plugins/superheroes/rubric/orders/dispatch-audits.md`.

**Restore receipt:** `- Target identifier (echo verbatim in your result): {{TARGET_ID}}`

**Green run (exit 0):**
```
1 passed in 0.12s
```

---

## BP-H2 — aux-only keys do not false-positive (green-only by design)

**Guarded element:** same test — axis: `PR_CHECKOUT_PATH` and `FOCUS_NOTES` are in `_KNOWN_REALISTIC_VALUES[RD.P_PANEL]` and in `_FIXTURE_AUX_INPUTS[RD.P_PANEL]` but are not `{{...}}` placeholders in `dispatch-panel.md`; checking against `_fixture_direct_placeholder_names` (which unions `aux`) must stay **green**.

**Neutralization:** none — this is the anti-false-positive half; aux-only entries are already present in the tree.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-h -m pytest 'plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder' -q
```

**Green run (exit 0):**
```
1 passed in 0.12s
```

---

## BP-H3 — anti-vacuity on phase enumeration

**Guarded element:** `assert phases, "read-only channel phases must be nonempty for fixture drift pin"` at the top of `test_placeholder_fixture_known_table_covers_every_template_placeholder` — axis: the reverse (and forward) checks must iterate a nonempty phase set, not zero phases.

**Neutralization:** in `plugins/superheroes/lib/tests/test_round_driver_order_channel.py`, changed `phases = sorted(RD._READ_ONLY_CHANNEL_PHASES)` to `phases = []`.

**Command:**
```
/usr/bin/python3 -B -X pycache_prefix=/private/tmp/superheroes-pyc-h -m pytest 'plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder' -q
```

**Red run (exit 1):**
```
F                                                                        [100%]
=================================== FAILURES ===================================
____ test_placeholder_fixture_known_table_covers_every_template_placeholder ____

    def test_placeholder_fixture_known_table_covers_every_template_placeholder():
        ...
        phases = []
>       assert phases, "read-only channel phases must be nonempty for fixture drift pin"
E       AssertionError: read-only channel phases must be nonempty for fixture drift pin
E       assert []

plugins/superheroes/lib/tests/test_round_driver_order_channel.py:686: AssertionError
=========================== short test summary info ============================
FAILED plugins/superheroes/lib/tests/test_round_driver_order_channel.py::test_placeholder_fixture_known_table_covers_every_template_placeholder
1 failed in 0.13s
```

**Restore:** changed `phases = []` back to `phases = sorted(RD._READ_ONLY_CHANNEL_PHASES)`.

**Restore receipt:** `phases = sorted(RD._READ_ONLY_CHANNEL_PHASES)`

**Green run (exit 0):**
```
1 passed in 0.12s
```
