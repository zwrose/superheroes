{
  "dispatch-panel": [
    "only flag code in `+` or\n`-` lines",
    "`file:line` citation required. No citation",
    "You are reviewing CHANGES MADE BY THIS PR/BRANCH",
    "That's the #1 source of false\npositives",
    "raise the finding AND note the prior justification",
    "Base rubric (binding) > CLAUDE.md (conventions)",
    "Never one whole-file read",
    "Set `dimension` to",
    "Nits at most 5 reported per agent",
    "This is the ONLY source of truth for verifying code"
  ],
  "dispatch-verifiers": [
    "You are the per-finding verifier for one cluster",
    "You NEVER read the PR's own description, title, or any\nauthor narrative — judge only from the diff and the repo",
    "verdict: \"CONFIRMED\" | \"PLAUSIBLE\" | \"REFUTED\"",
    "id: the issue's staged id, echoed verbatim",
    "Never change the repository, and never claim a run you did not make",
    "Judge only the issues in this cluster",
    "Never write a probe into the tree",
    "exactly one entry per cluster issue"
  ],
  "dispatch-synthesis": [
    "Your job is **not** keep/drop",
    "you group issues that share the same root cause",
    "coverage guarantee",
    "synthesis drops nothing",
    "CONFIRMED only when a member at the merged (highest) severity is CONFIRMED-with-evidence",
    "Echo staged ids verbatim",
    "every survivor id appears exactly once"
  ],
  "dispatch-fixer": [
    "You are the fixer for one round of an auto-fix code-review loop",
    "gate it with the fixer file-scope guard",
    "No branch-controlled path may be interpolated into\nshell text",
    "printf '%s' \"$path\"",
    "--stdin-path",
    "if `allow` is false (or `degraded` is true), DO NOT edit that file",
    "Fix ONLY what the findings call for. No unrelated refactors (YAGNI)",
    "retry ONCE. If it still fails, STOP and report CHECK_FAILED",
    "autofix-probe-",
    "Commit ALL changes in ONE commit",
    "do NOT guess. Report it for owner escalation (see Payload contract)"
  ],
  "dispatch-gap-sweep": [
    "You are the gap-sweep finder for a big-diff review round",
    "only flag code in `+` or `-` lines",
    "You are reviewing CHANGES MADE BY THIS PR/BRANCH",
    "`file:line` citation required",
    "Never change the repository, and never claim a run you did not make",
    "Do NOT decide the run's outcome or re-run panel seats",
    "Never one whole-file read"
  ],
  "dispatch-audits": [
    "You are the fix auditor for one cleared-or-stalled finding target",
    "You are **never** the fixer's vendor",
    "Return exactly one audit result for this target",
    "Judge only this one target",
    "Never change the repository, and never claim a run you did not make",
    "Transport keys use the per-location target identifier"
  ],
  "dispatch-scoped-finder": [
    "You are the scoped finder for a delta review round",
    "only flag code in `+` or `-` lines within those\nhunks",
    "You are reviewing CHANGES MADE BY THIS FIX",
    "`file:line` citation required",
    "Do NOT scan outside the scoped hunks file",
    "Never change the repository, and never claim a run you did not make",
    "Review only the hunks named in"
  ]
}
