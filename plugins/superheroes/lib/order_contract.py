"""Shared order-rendering contract literals (#1272).

Pure, stdlib-only leaf: one home for the payload-contract heading used by
``round_orders`` (renderer) and ``order_lint`` (shape guard).
"""

PAYLOAD_CONTRACT_HEADING = "## Payload contract"
