# C4 — feat(superheroes): discovery — consent-gated investigation, adaptive elicitation, three exits, the weight call

**Anchor:** spec `front-half-sdlc-core-6181ee` §Discovery (FR-11..FR-17) + UFR-10 (first acceptance bullet — abandoned discovery) — owner-approved 2026-08-07, as-of amendment #4.

**What:** Discovery becomes the one front door for requirements: no separate spike surface
(FR-11, including the recorded-ruling bypass — rulings already made need no discovery);
owner consent named-in-cost before any investigation spend, re-asked at the bound (FR-12);
exactly three exits each with a durable artifact and an advisor report (FR-13); adaptive
prose elicitation with no up-front ceremony choice and no pick-one widgets (FR-14/FR-15);
the review-weight call on the completed draft with the 10-line guideline and one-sentence
override (FR-16, R5); a light spec is the same artifact class (FR-17). Abandoned
discoveries get park notes per R7 (UFR-10a).

**DoD:**
- Discovery/architect charter text: the one front door (no separate spike surface; the
  recorded-ruling bypass's ROUTING outcome is C3's, not this child's); the
  consent gate names its spend, starts nothing on silence — while the owner is unavailable
  the item WAITS or PARKS (per R7) — stops at the named bound and re-asks, and is stated
  as the SOLE mid-flight consent point.
- The three exits each with their distinct artifact: the approved spec; the findings
  record WITH owner ratification recorded and an explicit no-spec-fabricated outcome; the
  park note per R7 — and every exit reports to the advisor.
- Adaptive elicitation per FR-14's own terms: per-dimension care probing (each
  opinion-bearing dimension probed, the owner asked whether they care) and the
  Dispositions-driven stopping rule (a small surface closes having asked only what its
  table needed) — both as charter text.
- Prose elicitation: FR-15's behavior demonstrated by a recorded rehearsal or completed
  live-discovery transcript INCLUDED IN THIS CHILD'S HANDBACK, showing a clarifying or
  mixed free-form answer accepted and the dialogue proceeding without re-forcing a single
  selection.
- Weight call: R5's vocabulary verbatim; BOTH classification inputs graded (gradable-line
  count AND interlocking sections); BOTH paths shipped end-to-end — light (one seat, light
  vet, in-channel approval) and full (panel, full spec-vet, scheduled owner review) — with
  the one-sentence override rule; the 10-line number positioned as a guideline (the
  five-amendment number is C5's, not this child's).
- Light spec same-class: a light-spec fixture or template assertion covering all four
  equivalences — same template, same home, same anchor power, same owner approval
  authority — with empty sections omitted rather than filled.
- UFR-10a: the abandoned-discovery park per R7, stated as advisor duty.

**Register text consumed (verbatim):**

> **R5 — The weight vocabulary.** A weight call names `light` or `full`, states its measurables (gradable-line count for a spec draft; child count and register-entry count for a package read), names a round ceiling when it governs a read loop, and may be overridden in either direction by one stated sentence; the numeric bars are guidelines, never gates.

> **R7 — The park surface.** A park lands the full park note — what was elicited or found so far, explicitly marked unapproved — on the owner's reading surface at park time: in the advisor's delivery message when the owner is present, else as the opening item of the advisor's next delivery message; a durable copy lands as a comment on the parked item's issue or PR, and the durable copy is for the record — it is never required owner reading.
