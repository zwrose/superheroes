# Contract register — reset-landing-2026-09-14

Cross-child technical decisions for the landing epic of Spec A (the forward doctrine) and Spec B
(the re-derivation), both owner-stamped 2026-09-14. Numbered entries; each a plain binding
sentence with its consuming children named; owner rulings embedded as dated copies pointing at
their home; amendments dated. The advisor owns edits; a builder never amends the register it is
graded against. Children quote entry text **verbatim**.

Children: C1 founding texts · C2 front-door references · C3 review doctrine and one home ·
C4 committed-docs correction · C5 merge-gate retirement · C6 keep-or-retire rollout ·
C7 charter restructure · C8 calmer reviews · C9 background-session trial · C10 dispatch entry
shrink · C11 native channels and the probe · C12 receipt writer · C13 certification loop ·
C14 CLI Claude engine and Astra · C15 orchestration decommission · C16 sanitized-view shrink ·
C17 configuration items.

---

**R1 — The vocabulary and its slugs.** The canonical glossary (one file, named by C1) carries one stable slug per term of Spec A FR-F9's seed, the whole seed and nothing less: ruling; decision walk; gardening pass, record, and window; condition window; keep-or-retire list; keep-or-retire entry; project configuration and configuration item; plugin component; retirement condition; catch-based, citation-based, and usage-based; real catch and noise; citation; consumer evidence; unmeasured; fired; proposal; retirement ruling; foundational component; authority surface; the priority tiers; declined registry, trigger, misses log, collector, and pending-words line; dial, machinery lane, product-forward lane, and N; craft call and owner call; material consequence; kind label; fold-in rules and folded-in items; workaround marker and delete-when condition; always-loaded set; keep list; rail; doctrine surface; foreign catch; the threat tags on the machinery map; and the four tags structural, capability-gap, harness-limit, and mixed; plus ground-truth sources. Every other surface cites a slug; no surface restates a definition. *(Amended 2026-09-15 with Spec A amendment #3: the background-session-trial and child-names entries are project vocabulary and stay out of the shipped glossary; every entry carries a definition of its own, never only a pointer; no project provenance, per R29.)*
*Owner ruling embedded:* Spec A FR-F9 (the glossary seed), the renames of 2026-09-12 and 2026-09-13, and stable slugs ruled 2026-09-06 sitting 5.
*Consumers:* C1 (owns the glossary), C2, C3, C4, C6, C7, C8, C10, C12, C13, C17 (cite slugs).

**R2 — One home per rule.** Every rule, schema, and contract has exactly one home, the one Spec A Part 6 names, and every other surface points at it; a drift-tested copy is not a second home, and a drift pin on a mirror is a delete signal for the mirror. The homes this epic lands: the front door in `skills/showrunner/reference/owner-decisions.md`; the tier vocabulary, size slot, kind labels, and the four routing-time calls in `skills/showrunner/reference/issue-contract.md`; Changes 1, 2, 4-doctrine, and 7 with the pin rule in `rubric/review-discipline.md`; the keep-or-retire list and the workaround-marker inventory in one new keep-or-retire reference doc; the prose standard in `rubric/prose-standard.md`; the glossary in one file; the forward share and dial principle in PHILOSOPHY with the numbers in the configure profile; promise 1's new shape in PHILOSOPHY, the covenant, showrunner duty 6, and `merge-train.md` amended together in one change set (C1); the trust boundary in PHILOSOPHY's threat-model paragraph, the configure profile's item 10, and one sentence in the security lens prompt; the host position in CONVENTIONS section 7, the README host line, and the dated `hosts/codex-tools.md`.
*Owner ruling embedded:* Change 7 (Spec A FR-B7), the one-home map (Spec A Part 6), ruled 2026-08-30 and 2026-09-06.
*Consumers:* C1, C2, C3, C4, C6, C7 (each lands the homes its scope names and points from everywhere else).

**R3 — Pointer-only from the stamp.** From 2026-09-14 no new drift-tested copy of any rule is added anywhere; CONVENTIONS section 11 and `rubric/review-base.md` verification rule 6 become pointer-only, and the retirement of existing drift-tested copies completes when the last layer of C3 lands. In the interim an un-amended surface reads as a pointer-pending mirror, and the stamped spec is the rule's home.
*Owner ruling embedded:* Spec A Part 6, the two-homes interim rule; Change 7.
*Consumers:* C3 (owns the two amendments), C4, C7 (add no copies; delete mirrors they touch).

**R4 — The pin rule.** A test on a doctrine surface pins structure, never sentences: it may assert that a check id, a heading, or a column exists where code and doc both need it, and it never asserts verbatim prose. Existing verbatim-clause pins on surfaces this epic edits retire rather than move; a red pin on replaced text is expected and is closed by retiring the pin, and `lib/tests/test_disposition_flow.py`'s worth-it-gate pins across five files are reconciled by C2.
*Owner ruling embedded:* Spec A FR-B7's pin rule, ruled 2026-09-06 sitting 3.
*Consumers:* C2 (owns the disposition-flow reconciliation), C3, C5, C7 (retire pins they hit).

**R5 — The receipt writer's boundary.** The certification receipt is derived by a receipt writer that is its own module: its only input is the review journal on disk, it imports nothing from the round driver, and its tests run with no driver. It emits the receipt or a refusal that names the class and the artifact. The receipt carries, beside today's fields: the terminal state, one of certified, cap, or cannot-certify; for a non-certified terminal its cause, either a loop reason from the closed set (latch, resume, state hash, detector, fixer cycle) or a non-loop reason (budget, open findings); each seat's provenance in its own field, dispatch-observed or hand-landed, kept separate from the certification shape; the disclosures block; and each finding's severity, disposition, and the receipt for that disposition.
*Owner ruling embedded:* FR-D8 ruled 2026-09-06; the module boundary ruled 2026-09-13 (read-back area 8); the provenance field ruled 2026-09-13 (stamp sitting item 8).
*Consumers:* C12 (owns the writer), C13 (calls it at certification and at every terminal), C8 (reads dispositions from the receipt), C14 (hand-landed and CLI-Claude results go through it).

**R6 — Dispositions.** A finding has exactly one of three dispositions: fixed with a verification receipt on the certified head; refuted with a recorded reason; or out of scope with a named follow-up item that exists at certification time and carries a revisit trigger (a condition, date, or milestone, never "documented") and a class-closure line. Minor and Nit findings may take the third disposition by the review's orchestrator; an Important finding may take it only for a very good reason stated on the disposition, and the receipt writer lists every such Important finding in the receipt's disclosures block; a Critical finding may not take it at all, by anyone: fixed or refuted, or the review is uncertified and the PR merges only as owner-accepted in its body.
*Owner ruling embedded:* the Critical prohibition and the two deferral rules ruled 2026-09-13 after the review-crew replay; the Important rider ruled 2026-09-13 at the stamp sitting (item 9).
*Consumers:* C12 (the writer refuses a follow-up with no trigger and reads severity), C13 (the loop records dispositions), C8 (the deferral disposition contract in calmer reviews).

**R7 — Engagement is telemetry.** Whenever the machine needs to know that a dispatched seat actually looked, the evidence is the runner's own record of the seat's tool calls and files opened, never a list the seat authored; for a dispatch-observed seat, absent or empty telemetry refuses. A hand-landed seat has no telemetry and is bound instead by FR-D2's execution-evidence binding on its landed envelope, labeled hand-landed on the receipt. The planted defect leaves every gate and becomes a sampled competence probe run after certification. The launcher's seat canary for builder sessions reads the session transcript's tool calls the same way.
*Owner ruling embedded:* Spec B FR-M2, ruled 2026-09-07 (walk item 9); the hand-landed path ruled 2026-09-13 (stamp sitting item 8).
*Consumers:* C12 (the unrun-review check), C13 (the canary as a gate leaves), C11 (the conformance probe reads engagement the same way), C14 (the seat canary).

**R8 — The dispatch shell's entry shape.** The shell has one documented entry shape: the seat bundle `{vendor, model, effort}` passes intact at every seam and effort is echoed with its source, never guessed; of the C1 annex's entry rows, the KEEP rows keep as declared, the MOVE rows that accept more shapes or default read tolerantly with a loud echo naming what was resolved, and the MOVE rows that are dropped read nothing, resolve no caller input, and echo nothing; every refusal names in its own text what would have been accepted; the entry doc is regenerated from the shell's declaration and the shipped skill texts point at it. There is no alias window: a dropped, renamed, or newly required argument is announced in the release note's transition note, the refusal a stale caller meets names the replacement, and the registered consumers are told once.
*Owner ruling embedded:* FR-S1, FR-S3, FR-S4, FR-M3, FR-M4; the alias window cut 2026-09-13 at the stamp sitting (item 7).
*Consumers:* C10 (owns), C11, C14 (extend the same entry shape), C7 and C4 (skill and doc texts point at the entry doc).

**R9 — Native result channels.** Each engine returns a typed result on its own structured channel, validated against a schema the shell declares per engine: codex on `--output-schema` with `-o`; cursor only after one two-dispatch trial (a review and a write on `--output-format json`, each validating within the declared wait) passes, else it stays on stream-json with the cap sized as an operating parameter; command-line Claude in print mode as the typed final response at exit under `--output-format json` with `--json-schema`, and in background mode as the session's final message retrieved after the idle notification, never the launch acknowledgement. Secret scrubbing and refuse-on-malformed are requirements on every channel; the marker parser and the salvage tiers retire for each engine that moves; the dispatch journal records the channel each run used; which engine is on which channel is recorded in the C1 values annex.
*Owner ruling embedded:* FR-S1's native-channel ruling 2026-09-06 sitting 1; per-mode contracts 2026-09-07 after Astra's second opinion (FR-S5).
*Consumers:* C11 (owns codex and the cursor trial), C14 (owns the Claude branch), C10 (the boundary rows stand).

**R10 — The conformance probe.** At each wave preflight one probe dispatch per engine on its channel must return a typed result that validates; a failing probe is loud, the preflight names the engine, what failed, and the lanes that depend on it, and the choice (launch without the engine, or hold the wave and troubleshoot) sits beside the launch word, so nothing launches on a failed probe without the owner's word. When the owner launches without the engine and it supplied a lane's second family, the preflight names the substitute family under the seat map's substitution rule or parks those full lanes. A drift or auth failure arriving mid-wave forfeits that dispatch with no salvage. The probe is invocable as one command with no argument beyond the engine name, its result is recorded in the preflight's existing record, and no engine CLI version is pinned.
*Owner ruling embedded:* FR-S5 ruled 2026-09-06; the owner's call on a failed probe ruled 2026-09-13 (read-back area 9).
*Consumers:* C11 (owns the probe), C14 (the Claude modes are probed separately), C7 (the launch doctrine section of the showrunner charter states the preflight step).

**R11 — The allowlist is hard shell.** An off-allowlist model cannot be dispatched on any path: the dispatch-guard check is wired into the dispatch surface as a safety refusal that names the allowlist, never a silent substitute; no tolerant entry reading and no cached liveness verdict resolves a model past it; the birth bite-proof attempts an off-allowlist dispatch on each path and every one refuses. The liveness cache keeps cell granularity with a one-hour value.
*Owner ruling embedded:* FR-S2 (#616 folded at the stamp), FR-S6 (one hour ruled 2026-09-13 at the stamp sitting).
*Consumers:* C10 (owns the wiring and the cache value), C11, C14 (every new path is a guarded path).

**R12 — The trust boundary.** The plugin reviews repos its owner trusts; review of code that could steer its reviewer is out of scope, disclaimed in PHILOSOPHY's threat-model paragraph and each project's configuration item 10, and on the declined registry with the one trigger "#40 goes live". The sanitized view keeps the neutral-cwd export, the exclusion of the twelve agent-configuration files and six directories from the tree the seats run in, and the containment family (the outward-symlink refusal, repo-root validation, the PR-body session pairing, the run-dir symlink refusal); a changed hunk on a stripped path is delivered beside the review patch as a plain review-only file whose name says it is data under review, which the seat prompt names as a context file; the hostile-repo defense set retires, listed by name in C16's first dated note before anything leaves. The security lens reads the project's declared threat model, and an accepted exposure is not a finding.
*Owner ruling embedded:* FR-F3 and FR-S8 ruled 2026-09-06 and 2026-09-07; the trigger narrowed 2026-09-13 (read-back area 10).
*Consumers:* C16 (owns the shrink and the review-only file), C1 (the PHILOSOPHY paragraph), C4 (the lens sentence), C17 (item 10's definition).

**R13 — The host position.** Claude Code is the supported host; Codex loads the skills and runs the discipline layer with no host guarantees; the doctrine and the skills stay host-neutral, the mechanical floors are host-specific, and a host that cannot carry one gets a disclosed absence, never a parity sentence. CONVENTIONS section 7.3 stays; sections 7.1, 7.2, and 7.4 narrow to the ruled claim, with the Codex absence stated in its true form (no session-start bootstrap, the covenant only through a project's own CLAUDE.md copy, no carrier in the out-of-repo storage mode). "The host model" means whatever runs the current session, read from the hook payload; "not the maker's family" replaces "non-Anthropic" at the micro-lane sites.
*Owner ruling embedded:* Part C R1, R2, R4 ruled 2026-09-05 (Spec A FR-F4).
*Consumers:* C4 (owns the committed-file edits), C7 (the charters' host wording), C1 (PHILOSOPHY's host line, if any).

**R14 — Command-line Claude is an engine.** CLI-spawned Claude (`claude -p` builders, `claude --bg` in the background-session trial, any subprocess-dispatched Claude seat) is registered in the engine adapter with the print and background result contracts of R9, exercised under the target `CLAUDE_CONFIG_DIR` at dispatch, with the same refusal tokens as codex and cursor and no quota leg; the launcher's hand-built argv retires into it. Native in-session seats are declared live and never probed, with a disclosure line on any certification resting on an unprobed native family and a LEDGERS section 3 residual recording it. Astra registers under the codex vendor as the top rung and takes the `reviewer-deep` cell at effort high, Sol moving to `reviewer` and `verifier`, only after one live security-lens dispatch names a planted fail-open by file, line, and severity; every attempt is recorded in the C1 values annex, three misses put the question to the owner, and until the pass Sol stays deep and every receipt resting on the seat discloses the pending registration.
*Owner ruling embedded:* Part C R5 and R6 ruled 2026-09-05; the gate ruled 2026-09-06; effort high ruled 2026-09-14 at the stamp sitting.
*Consumers:* C14 (owns), C15 (the launcher's argv retirement is sequenced with it), C4 (the LEDGERS residual), C9 (the trial exercises the background mode).

**R15 — The orchestration decommission scope.** The six orchestration pieces whose delete-when condition has been met are detached spawn in the launcher; wave-watch arming and the re-arm ritual; transcript-mtime liveness; the turn-end doctrine with its 540 and 600 second slice recipes; multi-account provisioning transport; and the launcher-enforced half of the own-worktree ruling (the prose stays for direct builders). They retire behind one receipt, the background-session trial's, and only if that receipt comes back clean; otherwise all six stay as plain keeps with their markers. On the same receipt, and only these wait on it: the six pieces and the semantic heartbeat, which retires with the watcher unless its record shows a stale-with-live-pid catch that idle or exit signals would have surfaced too late. Unconditionally, waiting on nothing: `harness_probe` retires or starts recording; the wave-watch auto-re-arm request is closed unbuilt because its subject is on the list; the usage poller is parked, not deleted; preflight check one, the launch doctrine's quota leg, the "suspect quota" recovery section, and their tests come out; and the plugin-root seam and its lint, whose delete-when condition is met (Codex aliases the variable), retire. The launch ledger and the launch doctrine's rulings block are doctrine, not workarounds, and stay. *Decide-by:* C9 (the trial's receipt names, piece by piece, what the harness now carries and what it still lacks).
*Owner ruling embedded:* Spec A FR-B6, the two records ruled 2026-09-05 (Part C R3) and the trial named 2026-09-12.
*Consumers:* C15 (owns the retirements), C9 (owns the receipt), C7 (the charters' launch and watch sections cite what remains, so C7 lands after C15), C14 (the launcher's spawn path is edited by C14 first, then C15).

**R16 — The keep-or-retire entry.** Every plugin component has one entry on the keep-or-retire list, kept by a person in the keep-or-retire reference doc: the component and its cost or annoyance; its condition (catch-based, citation-based, or usage-based, with the window in calendar days, plugin defaults 45, 45, and 60) and the one action on firing, a proposal to the owner at a gardening pass; its last demonstrated benefit with a receipt or "unknown"; consumer evidence as receipts, "unmeasured" where none; the owner's latest decision; and notes carrying one of four tags and, for a component guarding a model behavior, the engine family. The foundational set at the stamp is the covenant's hard lines and nothing else; the authority surfaces (the worktree guard, the circuit breaker and escalation, model governance, the verify gate, the vet-receipt spine with its owner half) get the owner's one look at C6's landing. The workaround-marker inventory is a section of the same doc; a new marker's entry lands in the PR that adds the workaround.
*Owner ruling embedded:* Spec A FR-B3 and FR-B6, ruled 2026-09-05, 2026-09-07, and 2026-09-12 (the entry shape, item 2 = d).
*Consumers:* C6 (owns the doc and the rollout, landed in wave 1 so entries can be added at birth), C2 (the gardening pass reads it), C7 (charters cite it and carry no entries themselves), C12 (adds the writer's and the four checks' entries in its own PR), C15 and C16 (their retirements are recorded there).

**R17 — The front door.** Intake is one door for machinery and product: the evidence bar (made to happen in the field or the lab; argued-could-happen fails), the grid of severity ladder and evidence tier with no third instrument, the tiers P0 (owner word at once), P1 (batched word at a walk), P2 (files on the advisor's authority with the grading recorded, the one carve-out from "no filing without the owner's word"), and declined (a registry line with a named trigger); every grading records band, tier, and evidence on the item; the misses log and the revisit-trigger registry live as one pinned comment on the collector; a trigger is read at the door and at the gardening pass and nowhere else; the size-consideration slot, the lane call, the presentation call, and the kind label are the four routing-time calls recorded together in the issue body; folded-in backlog items ride an epic as ratified scope and are listed one per line in the gardening record; every instrument filing carries a reader clause.
*Owner ruling embedded:* Spec A FR-A1 to FR-A9, ruled 2026-08-30 (CP3) and 2026-09-02 to 2026-09-12.
*Consumers:* C2 (owns owner-decisions.md and issue-contract.md), C7 (the showrunner charter maps to them), C17 (the configuration items the door reads).

**R18 — The gardening pass.** The pass is owed seven days after the last gardening record (the stamp date stands in until the first record) and is done at a decision walk the owner picks; at the start of any walk, if it is owed and no "gardening pass owed" item is on the collector, the advisor appends one. Its seven duties are the closed list of Spec A FR-A10: age P1s; sweep the registry's triggers, the elapsed retirement conditions, Spec B's tripwires as one outcome account, and the consumer reports, then draw about five proposals off the queue and ask before drawing more; read the misses log and propose recalibrations; read the forward share against the dial in one sentence, the decisions-asked count, the folded-in items, and the window's declines; report workaround markers whose delete-when condition came true; read guardian staleness and, when stale, run and triage the sweep in the same sitting; hand-count escapes by class. The record's contents and shape are FR-A10's; there is no meter on the pass, and "too long" from the owner means fewer and coarser components, never automation.
*Owner ruling embedded:* FR-A10 ruled 2026-09-05, 2026-09-07, and 2026-09-12.
*Consumers:* C2 (owns the duties' home), C7 (the showrunner charter's duty section points), C6 (duty 2 reads the list).

**R19 — The merge act.** Approval is never delegated: it is a scoped word in chat after the PRs are talked through, recorded by PR number beside the word and on each PR's owner half; the advisor executes merges inside the scope after the lane's review and verification evidence, CI green on the recorded head, and a current branch, and reports each merge at once in the thread that gave the word; a PR opened after the word or changed materially asks again, judged against the material-consequence line; a red merge train is fixed under the word when the fix is craft; force-push proceeds on a word after the reason is stated; releases and publishing stay the owner's click unless handed over in the moment; there is no mechanical merge floor.
*Owner ruling embedded:* Spec A FR-F1 ruled 2026-09-06 sitting 4, amended 2026-09-07 and 2026-09-12.
*Consumers:* C1 (owns all four homes amended together: PHILOSOPHY's promise 1, the covenant hard line, showrunner duty 6, and merge-train.md), C2 (the front door points at them), C5 (the gate it replaces).

**R20 — The gate retirement census.** The owner-authority gate family retires by census of every reference to its modules, hook, allowlist, and vocabulary, never by the spec's list alone: the hook, `lib/owner_authority.py`, the per-project allowlist override and its reference doc, the family's SAFETY_MACHINERY entries (with `mode_registry.py` judged on its own band-wide role, not retired on gate-family grounds), the review-discipline gate-family paragraph and its route-pin test, the configure and wave-watch mentions, the tests, and the per-project allow files of this repo and weekly-eats on the one owner word already given. "Run a workflow" becomes an ordinary command. The worktree guard, source_guard, the bootstrap, and the compaction hooks stay.
*Owner ruling embedded:* Spec A FR-F2 ruled 2026-09-06 sitting 4; the two allow files confirmed 2026-09-12.
*Consumers:* C5 (owns), C3 (the review-discipline paragraph removal lands in C5 after C3's Changes text, on the same file).

**R21 — Charters as maps.** Each charter is a map: one short section per duty stating the rule and its completion criterion, with the mechanism one hop away in a reference doc that loads when the duty fires; excuse tables move to per-skill reference pages or are cut; doctrine is cut where it is restructured, never measured, and the landing note lists what left; the per-role startup context before and after is recorded once in C7's landing note and is the reset's one grading; no line budget, no knob count, no citation tally.
*Owner ruling embedded:* Spec A FR-B6 ruled 2026-09-02 and 2026-09-06 sitting 3; the tally cut 2026-09-12.
*Consumers:* C7 (owns), C2 and C3 (their references are the one-hop targets).

**R22 — Calmer reviews.** The review loop takes a router-first lane call, the deferral disposition contract of R6 as its third disposition, round-count calming within the declared caps, and "fewer lens prompts with family diversity kept" as a design input; a review finding that proposes new standing machinery routes to the front door and never spawns a build from inside the review; "watch the guard" findings are answered by citing Change 1.
*Owner ruling embedded:* Spec A FR-B4, ruled 2026-08-30 (CP3-F item 8) and 2026-09-07.
*Consumers:* C8 (owns), C13 (the loop's caps and dispositions), C3 (the citation rule's home).

**R23 — The successor's three answers.** The certification child lands, as a design note and not as code, the three answers the loop's successor needs: how a stateless checker establishes a real run from journals and telemetry alone; how a re-run seat's qualifying result recovers coverage an earlier failed attempt left open; and how every abandoned or capped review still gets a receipt. Nothing of the successor is built or deleted until FR-D9's tripwire fires, and the owner rules the deletion on the coverage check. *Decide-by:* C13 (the note's content).
*Owner ruling embedded:* FR-D9 ruled 2026-09-06; the answers pulled forward 2026-09-13 (read-back area 8).
*Consumers:* C13 (owns the note), C12 (the writer's checks are the successor's checks).

**R24 — One version bump.** The certification changes ship behind one state-version bump under the driver's existing supported-versions mechanism; the receipt's new fields, the provenance field, and D1-029's shape change ride it; no stored loop-state field is removed at it or before the loop leaves; in-flight lanes complete on their recorded version; consumer-visible shape changes and the dispatch shell's argument changes are announced in the same release's transition note.
*Owner ruling embedded:* FR-D5 cut and FR-D8's fence ruled 2026-09-13.
*Consumers:* C12 (the bump), C13 (nothing removed), C10 and C11 (the transition note).

**R25 — What stays hard shell in the dispatch shell.** The journal, the run lock, and max-wait stay as hard shell; the secret scrub and refuse-on-malformed stay as requirements on every channel; the parse, scrub, and forfeit-grading boundary rows keep as declared; the stdout cap, the salvage paths, and the dirty-tree probe become keep-or-retire entries rather than contract rows; the forfeit ledger retires with no reader, and a preflight refusal returns its refusal to the caller like every other; C1's health condition is the refusal-streak trigger only.
*Owner ruling embedded:* FR-S1 supervision ruling 2026-09-06; FR-S7 cut and the ledger's retirement 2026-09-13.
*Consumers:* C10 (owns the retirement and the entries), C11 (the boundary on the native channel).

**R26 — The annexes close.** Each of C10, C11, C12, and C13 moves the live declaration of its surface into the implementation home (the envelope, the entry doc, and the echo set derive from there), re-verifies its rows against its build head with a dated note, and writes the table's last correction in its closing note; when the last of the four lands the derivation tables close as adjudication history and nothing maintains them.
*Owner ruling embedded:* FR-M1's closing rule ruled 2026-09-07.
*Consumers:* C10, C11, C12, C13.

**R27 — Birth duties for anything that detects.** From the stamp, a new detector, gate, watcher, census, or detector-shaped test ships only with all three birth duties: (i) a bite-proof that demonstrably fires on a planted defect and is deterministic by construction (no wall-clock assertions, no environment inference); (ii) a retirement condition and tag on its keep-or-retire entry; and (iii) by-construction coverage, its scope complete because of how it is built (one chokepoint, a closed enumeration), never a hand-maintained list chased case by case, so a hand-maintained parallel enumeration is itself rung-4 machinery that owes the same duty or does not ship. Every producer names its reader. Nothing watches other machinery.
*Owner ruling embedded:* Spec A FR-B1 and FR-B2 (rung 4's three birth duties and the widened third duty), ruled 2026-08-30 and 2026-09-06.
*Consumers:* C1, C2, C3, C4, C5, C6, C7, C8, C9, C10, C11, C12, C13, C14, C15, C16, C17; C3 owns the doctrine text.

**R28 — Delete on contact waits for the keep list.** A behavior test that goes red under a child's change is classified by the builder (a genuine regression means the behavior is fixed and the test kept; an obsolete expectation may be deleted under the standing authorization, unless the keep list names the file), with the classification and reason in the PR body. The rule activates only once the owner has stamped the seeded keep list; before that nothing is deleted and expectations are not rewritten. *Cross-epic seam:* the keep list is seeded by the verification-strategy spec's own children (#1105's set); recorded reciprocally on #1105.
*Owner ruling embedded:* Spec A FR-F6 change 3, ruled 2026-09-06 sitting 5, simplified 2026-09-13.
*Consumers:* every child (C1, C2, C3, C4, C5, C6, C7, C8, C9, C10, C11, C12, C13, C14, C15, C16, C17): each carries the at-contact rule for any test its change turns red, and C1 and C2 retire verbatim pins under it.

**R29 — Shipped surfaces carry no project provenance.** A surface that ships in the plugin or stands as a founding document (PHILOSOPHY, CONVENTIONS, README, the rubric, the glossary, the charters and their reference docs) states its rules and definitions without the record of how they were decided: no spec-section numbers, no sitting or checkpoint codes, no dated rulings or renames, no issue or PR numbers except a pointer the project's configuration supplies, and none of this project's own history (its measurements, its accounts, its epic's child names). The reason behind a counter-default rule may stay beside the rule; where it was decided may not. Provenance lives in the PR body that landed the text, the spec's Amendments log, and LEDGERS. The prose standard (`rubric/prose-standard.md`) carries the rule for every later edit.
*Owner ruling embedded:* 2026-09-15, at the owner's review of PR #1277; extends Spec A FR-B6's "FR numbers are provenance only: a surface cites the slug, never the FR number."
*Consumers:* every child that writes or edits a shipped surface: C1 (the founding texts and the prose standard, the first application), C2, C3, C4, C5, C6, C7, C8, C10, C11, C12, C13, C14, C15, C16, C17. The background-session trial child writes only LEDGERS, which is the project's record, and consumes nothing here.
