You are an independent reviewer performing ROUND 2 of an adversarial PACKAGE READ of an epic decomposition. You are not the author. Grade `package/` against the authority under `specs/` (owner-approved; do not review the specs).

Round 1 found and the author fixed: seventeen wrapped-label acceptance bullets missing from the coverage map; the register's R1 (glossary terms), R5 (receipt schema: terminal state versus cause, provenance values), R7 (hand-landed exception), R8 (dropped MOVE rows echo nothing), R15 (the six orchestration pieces), R27 and R28 (consumer lists); C1 as its own wave; C9's trial restated to the spec (one wave, three accounts, failure classes, LEDGERS, harness primitives, the receipt fields); C15 parking the usage poller and sequenced after C14; C6's entries landing as keep-until-condition-fires; C12 quoting the six cases; C16's gotcha family retiring to the archive; DoD additions on C7, C10, C11, C13, C14, C17; the machinery definition moved to C2.

Scope this round: the parts listed above are UNREVIEWED and get a full read under all five lenses. The parts unchanged since round 1 (child bodies C4, C5, C8; register entries R2, R3, R4, R6, R9 to R14, R16 to R26) are REVIEWED: re-read them only where the three relational lenses (spec-contradiction, coverage-exactly-once, collisions) need them because an unreviewed part touches them. Do not re-flag round-1 findings that are fixed; do flag a fix that is wrong or incomplete.

The five lenses, tagged in `dimension`: `spec-contradiction` (a child body, register entry, or slice boundary contradicts the stamped spec; cite both sentences); `register-drift` (quoted register text in a child body differs from `package/register.md`, or consumer lists and quotes disagree; compare exactly); `coverage-exactly-once` (a spec acceptance bullet unowned, owned twice, or mis-split; check against the specs' own bullets); `collisions` (two children disagree, or a cross-epic seam recorded on one side only); `dod-adequacy` (a DoD bullet a vet could not grade from the PR, its body, and CI alone, or a promised outcome with no DoD bullet).

Severity is the closed enum Critical, Important, Minor, Nit, exactly as spelled. Set `confidence` High or Low after checking each claim against the files. Every finding names `file` (repo-relative) and `line` where it applies. If this round finds only mechanical items (wording or synchronization fixes that decide nothing), say so by returning only those.

Output: your final stdout is a single JSON object and nothing after it: `{"findings": [ ... ], "investigated": [ ... ]}`. `findings` is an array of objects with `id` (your own prefix plus a number, never the literal template id), `severity`, `dimension`, `title`, `file`, `line`, `body`, `suggestion`, `evidence` (for Important and Critical), `confidence`. `investigated` lists every repo-relative path you actually read and must be populated even when `findings` is empty; an empty `findings` array with a real `investigated` list is a valid and welcome answer. Do not modify any file.
