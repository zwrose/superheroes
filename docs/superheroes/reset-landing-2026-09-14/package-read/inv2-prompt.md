You are an independent reviewer performing a bounded re-read (one round) of an epic decomposition package after its previous read parked at its ceiling. You are not the author. Grade `package/` against the authority under `specs/` (owner-approved; do not review the specs).

The previous round found and the author fixed: register R15 now gates only the six orchestration pieces and the heartbeat on the trial's receipt, with harness_probe, the re-arm closure, the quota pieces, and the plugin-root seam handled unconditionally; C7 lands after C15; C10 grades the cache-served failure disclosure, the release transition note, the stale-caller refusal, and the consumer tell; C1's DoD grades the four promise-1 homes amended together (merge-train.md replacing "the owner authorizes that edit"); C2 states the no-ladder rider on the P2 carve-out; C13's successor section quotes FR-D9's stay-and-go list and the coverage-replay rules, and the canary leaves the gate only with C12's canary-census receipt; C12 records the canary census, files post-shrink escapes to the misses log, and grades the version bump, the transition note, and the no-trigger refusal; C11 states the two-proposal tripwire with the annex rows as its readout; C6 consumes C10's three keep-or-retire entries by name.

Scope: those changed parts (register R15; child bodies C1, C2, C6, C7, C9, C10, C11, C12, C13, C15) are UNREVIEWED and get a full read under all five lenses. Everything else is REVIEWED and is re-read only where the three relational lenses (spec-contradiction, coverage-exactly-once, collisions) need it because an unreviewed part touches it. Do not re-flag fixed findings; do flag a fix that is wrong or incomplete.

The five lenses, tagged in `dimension`: `spec-contradiction` (cite the spec sentence and the package sentence), `register-drift` (compare quoted text exactly; consumer lists and quotes must agree), `coverage-exactly-once` (check the map against the specs' own acceptance bullets), `collisions` (two children disagree, or a one-sided seam), `dod-adequacy` (a DoD bullet a vet could not grade from the PR, its body, and CI alone, or a promised outcome with no DoD bullet).

Severity is the closed enum Critical, Important, Minor, Nit, exactly as spelled. Set `confidence` High or Low after checking each claim against the files. Every finding names `file` and `line` where it applies. If this round finds only mechanical items (wording or synchronization fixes that decide nothing), return only those; that is the convergence signal, and an empty findings array with a real investigated list is a valid and welcome answer.

Output: your final stdout is a single JSON object and nothing after it: `{"findings": [ ... ], "investigated": [ ... ]}`, findings with `id` (your own prefix plus a number, never the literal template id), `severity`, `dimension`, `title`, `file`, `line`, `body`, `suggestion`, `evidence` (for Important and Critical), `confidence`; `investigated` populated always. Do not modify any file.
