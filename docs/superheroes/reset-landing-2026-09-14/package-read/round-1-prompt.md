You are an independent reviewer performing an adversarial PACKAGE READ of an epic decomposition. You are not the author. Read everything under `package/` and grade it against the authority under `specs/`. Do not review the specs themselves; they are owner-approved and are the standard.

The package: `package/register.md` (28 numbered contract entries R1 to R28, each with a `*Consumers:*` line naming child tokens C1 to C17), `package/coverage-map.md` (every acceptance criterion of both specs allocated to exactly one owner), and `package/children/C1.md` to `C17.md` (the child issue bodies as they will be filed: Anchor, What, DoD, then the register entries the child consumes quoted verbatim).

Run all five lenses and tag every finding with its lens token in the `dimension` field:
1. `spec-contradiction`: a child body, register entry, or slice boundary contradicts the stamped spec text in `specs/`. Cite the spec sentence (file and section) and the package sentence.
2. `register-drift`: quoted register text in a child body differs from `package/register.md`, or a child is named as a consumer of an entry it does not quote, or quotes an entry that does not name it. Compare the text exactly.
3. `coverage-exactly-once`: the coverage map leaves a spec acceptance criterion unowned, owned twice, or mis-split (a split whose halves do not together cover the criterion, or whose declared owner cannot deliver its half). Check the map against the specs' acceptance bullets, not against the map's own counts.
4. `collisions`: two children disagree with each other (same surface, incompatible instructions; a sequencing claim one child makes that another contradicts), or a cross-epic seam is recorded on one side only (the verification-strategy seam R28 names #1105).
5. `dod-adequacy`: a child's DoD bullet is not gradable from the handback's artifacts alone (a vet reading only the PR, its body, and CI could not decide pass or fail), or a DoD omits the outcome the child's What promises.

Severity is the closed enum Critical, Important, Minor, Nit, exactly as spelled; Critical for a contradiction that would build the wrong thing or an uncovered criterion, Important for drift or a missing seam or an ungradable DoD, Minor for wording that decides nothing, Nit for typos. Set `confidence` High or Low after checking each claim against the files. Every finding names `file` (repo-relative) and, where it applies, `line`.

Output: your final stdout is a single JSON object and nothing after it: `{"findings": [ ... ], "investigated": [ ... ]}`. `findings` is an array of objects with fields `id` (your own prefix plus a number, for example `pkg-01`; never the literal template id), `severity`, `dimension` (the lens token), `title`, `file`, `line` (number or null), `body`, `suggestion`, `evidence` (for Important and Critical), `confidence`. `investigated` lists every repo-relative path you actually read; it must be populated even when `findings` is empty, and an empty `findings` array with a real `investigated` list is a valid and welcome answer. Do not modify any file.
