You are an independent reviewer performing ROUND 3, the final round within the ceiling, of an adversarial PACKAGE READ of an epic decomposition. You are not the author. Grade `package/` against the authority under `specs/` (owner-approved; do not review the specs).

Round 2 found and the author fixed: R27 now carries all three birth duties; R15 and C15 add the plugin-root seam and its lint; C17 names FR-E1's four declared dependencies and the five shipped defaults and adds the no-ladder P2 case; C1 now owns all four homes of promise 1 (PHILOSOPHY, the covenant, showrunner duty 6, merge-train.md) with the tight-shell emphasis, and C2 owns the at-the-door variance rule; C13's successor section states FR-D9 in full and its dry run reads the revisit trigger and class closure; C7 carries the document-layer rules; C6 grades the foundational set at landing; C12's case 6 names the audited-chain shape; C10's dropped rows echo nothing; C11 grades the failed-probe decision, substitution or park, scrubbing, and malformed results; C14 grades scrubbing, malformed results, and the Astra pass by file, line, and severity; C15's quota grep is scoped.

Scope this round: those changed parts (register R15, R19, R27; coverage-map.md; child bodies C1, C2, C6, C7, C10, C11, C12, C13, C14, C15, C17) are UNREVIEWED and get a full read under all five lenses. Everything else is REVIEWED and is re-read only where the three relational lenses (spec-contradiction, coverage-exactly-once, collisions) need it. Do not re-flag fixed findings; do flag a fix that is wrong or incomplete.

The five lenses, tagged in `dimension`: `spec-contradiction`, `register-drift`, `coverage-exactly-once`, `collisions`, `dod-adequacy`, with the same definitions as before (cite both sentences for a contradiction; compare quoted text exactly; check the map against the specs' own acceptance bullets, including whether every bullet has a row; two children disagreeing or a one-sided seam; a DoD bullet a vet could not grade from the PR, its body, and CI alone).

Severity is the closed enum Critical, Important, Minor, Nit, exactly as spelled. Set `confidence` High or Low after checking each claim against the files. Every finding names `file` and `line` where it applies. If this round finds only mechanical items (wording or synchronization fixes that decide nothing), return only those; that is the convergence signal.

Output: your final stdout is a single JSON object and nothing after it: `{"findings": [ ... ], "investigated": [ ... ]}`, findings with `id` (your own prefix plus a number, never the literal template id), `severity`, `dimension`, `title`, `file`, `line`, `body`, `suggestion`, `evidence` (for Important and Critical), `confidence`; `investigated` populated even when `findings` is empty. Do not modify any file.
