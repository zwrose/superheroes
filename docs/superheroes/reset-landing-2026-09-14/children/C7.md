Anchor (spec-section): part-b-spec-a-forward-doctrine · § Part 2 FR-B6 (the document layer, doctrine is cut where restructured, no line budget, the interim measurement); Spec B § FR-M2 (the check-runner's home); § Part 6 (the charter-restructure child) · as-of amendment #0 (owner-stamped 2026-09-14; record https://github.com/zwrose/superheroes/issues/695#issuecomment-5673081783)

What: The always-loaded set restructured as maps: the showrunner, workhorse, and detective charters each become one short section per duty stating the rule and its completion criterion with the mechanism one hop away, structured by the writing-for-agents rules (hierarchy with disclosure by branch; pointer wording that says when to follow the pointer; the leading words carry the decision; the no-op test, a sentence that changes no behavior goes; the cache rule, stable text first and volatile text last; a rule a script already enforces is not also written as prose); skill descriptions written as always-loaded pointers; excuse tables move to per-skill reference pages or are cut; doctrine is cut as it is restructured and the landing note lists what left; the check-runner as a plain shell task is homed in the workhorse charter; the showrunner charter's launch doctrine section states the conformance-probe preflight step with the owner's choice on a failed probe and cites what orchestration remains after C15. Shipped as a three-layer stack under the size-consideration slot (one charter per layer, bottom-up merge, gh-stack). The per-role startup context before and after is measured once and recorded in the landing note; it is the reset's grading. No tally, no line budget, no size target.

DoD:
- Each of the three charters reads as a map: every duty section states its rule, its completion criterion, and a link to its mechanism with pointer wording that says when to follow it, and the PR body lists the sections cut and, per charter, one example each of the leading-words rule, the no-op test, and the cache rule applied; no size measure is used as a pass condition, and no rule a script enforces appears as prose (the PR body names the scripts checked).
- The excuse tables are absent from the charters and present, where kept, on per-skill reference pages the charters link.
- Every skill description in the plugin manifest is written as an always-loaded pointer that says when to load its mechanism; the PR body lists each description before and after.
- The workhorse charter names the check-runner as a plain shell task in one section, and the showrunner charter's launch section names the conformance-probe preflight step and the owner's choice on a failed probe, each pointing at the dispatch-mechanics doc for the mechanism.
- The launch and watch sections name only the orchestration that remains after C15: the PR body lists each piece of register entry R15 as cited-as-remaining or absent, matching C15's recorded outcomes.
- The landing note records the per-role startup context before and after (files loaded and their sizes per role), measured by the same command both times, and lists every doctrine passage that left.
- The stack merges bottom-up with each layer green on its own; the size-consideration slot is recorded in this body.
- The four validators pass and CI is green on the final head; the register-quote check passes for this child.

**Epic:** #1259 (reset-landing-2026-09-14, the landing epic of the Part B specs; artifacts in PR #1258). **Child:** C7, charter restructure. **Sequencing:** Wave 3, after C2 and C3, and after C15 (the showrunner charter cites the orchestration that remains after C15's removals). **Size-consideration slot:** stated at routing. **Lane call:** full. **Kind:** reset (outside the dial per Spec A FR-A6; neither machinery nor product for the trend).

**Register entries this child consumes (quoted verbatim from docs/superheroes/reset-landing-2026-09-14/register.md; the register-quote check runs at filing, intake, and the package read):**

> **R1 — The vocabulary and its slugs.** The canonical glossary (one file, named by C1) carries one stable slug per term of Spec A FR-F9's seed, the whole seed and nothing less: ruling; decision walk; gardening pass, record, and window; condition window; keep-or-retire list; keep-or-retire entry; project configuration and configuration item; plugin component; retirement condition; catch-based, citation-based, and usage-based; real catch and noise; citation; consumer evidence; unmeasured; fired; proposal; retirement ruling; foundational component; authority surface; the priority tiers; declined registry, trigger, misses log, collector, and pending-words line; dial, machinery lane, product-forward lane, and N; craft call and owner call; material consequence; kind label; fold-in rules and folded-in items; workaround marker and delete-when condition; always-loaded set; keep list; rail; doctrine surface; foreign catch; the threat tags on the machinery map; and the four tags structural, capability-gap, harness-limit, and mixed; plus ground-truth sources. Every other surface cites a slug; no surface restates a definition. *(Amended 2026-09-15 with Spec A amendment #3: the background-session-trial and child-names entries are project vocabulary and stay out of the shipped glossary; every entry carries a definition of its own, never only a pointer; no project provenance, per R29.)*

> **R2 — One home per rule.** Every rule, schema, and contract has exactly one home, the one Spec A Part 6 names, and every other surface points at it; a drift-tested copy is not a second home, and a drift pin on a mirror is a delete signal for the mirror. The homes this epic lands: the front door in `skills/showrunner/reference/owner-decisions.md`; the tier vocabulary, size slot, kind labels, and the four routing-time calls in `skills/showrunner/reference/issue-contract.md`; Changes 1, 2, 4-doctrine, and 7 with the pin rule in `rubric/review-discipline.md`; the keep-or-retire list and the workaround-marker inventory in one new keep-or-retire reference doc; the prose standard in `rubric/prose-standard.md`; the glossary in one file; the forward share and dial principle in PHILOSOPHY with the numbers in the configure profile; promise 1's new shape in PHILOSOPHY, the covenant, showrunner duty 6, and `merge-train.md` amended together in one change set (C1); the trust boundary in PHILOSOPHY's threat-model paragraph, the configure profile's item 10, and one sentence in the security lens prompt; the host position in CONVENTIONS section 7, the README host line, and the dated `hosts/codex-tools.md`.

> **R3 — Pointer-only from the stamp.** From 2026-09-14 no new drift-tested copy of any rule is added anywhere; CONVENTIONS section 11 and `rubric/review-base.md` verification rule 6 become pointer-only, and the retirement of existing drift-tested copies completes when the last layer of C3 lands. In the interim an un-amended surface reads as a pointer-pending mirror, and the stamped spec is the rule's home.

> **R4 — The pin rule.** A test on a doctrine surface pins structure, never sentences: it may assert that a check id, a heading, or a column exists where code and doc both need it, and it never asserts verbatim prose. Existing verbatim-clause pins on surfaces this epic edits retire rather than move; a red pin on replaced text is expected and is closed by retiring the pin, and `lib/tests/test_disposition_flow.py`'s worth-it-gate pins across five files are reconciled by C2.

> **R8 — The dispatch shell's entry shape.** The shell has one documented entry shape: the seat bundle `{vendor, model, effort}` passes intact at every seam and effort is echoed with its source, never guessed; of the C1 annex's entry rows, the KEEP rows keep as declared, the MOVE rows that accept more shapes or default read tolerantly with a loud echo naming what was resolved, and the MOVE rows that are dropped read nothing, resolve no caller input, and echo nothing; every refusal names in its own text what would have been accepted; the entry doc is regenerated from the shell's declaration and the shipped skill texts point at it. There is no alias window: a dropped, renamed, or newly required argument is announced in the release note's transition note, the refusal a stale caller meets names the replacement, and the registered consumers are told once.

> **R10 — The conformance probe.** At each wave preflight one probe dispatch per engine on its channel must return a typed result that validates; a failing probe is loud, the preflight names the engine, what failed, and the lanes that depend on it, and the choice (launch without the engine, or hold the wave and troubleshoot) sits beside the launch word, so nothing launches on a failed probe without the owner's word. When the owner launches without the engine and it supplied a lane's second family, the preflight names the substitute family under the seat map's substitution rule or parks those full lanes. A drift or auth failure arriving mid-wave forfeits that dispatch with no salvage. The probe is invocable as one command with no argument beyond the engine name, its result is recorded in the preflight's existing record, and no engine CLI version is pinned.

> **R13 — The host position.** Claude Code is the supported host; Codex loads the skills and runs the discipline layer with no host guarantees; the doctrine and the skills stay host-neutral, the mechanical floors are host-specific, and a host that cannot carry one gets a disclosed absence, never a parity sentence. CONVENTIONS section 7.3 stays; sections 7.1, 7.2, and 7.4 narrow to the ruled claim, with the Codex absence stated in its true form (no session-start bootstrap, the covenant only through a project's own CLAUDE.md copy, no carrier in the out-of-repo storage mode). "The host model" means whatever runs the current session, read from the hook payload; "not the maker's family" replaces "non-Anthropic" at the micro-lane sites.

> **R15 — The orchestration decommission scope.** The six orchestration pieces whose delete-when condition has been met are detached spawn in the launcher; wave-watch arming and the re-arm ritual; transcript-mtime liveness; the turn-end doctrine with its 540 and 600 second slice recipes; multi-account provisioning transport; and the launcher-enforced half of the own-worktree ruling (the prose stays for direct builders). They retire behind one receipt, the background-session trial's, and only if that receipt comes back clean; otherwise all six stay as plain keeps with their markers. On the same receipt, and only these wait on it: the six pieces and the semantic heartbeat, which retires with the watcher unless its record shows a stale-with-live-pid catch that idle or exit signals would have surfaced too late. Unconditionally, waiting on nothing: `harness_probe` retires or starts recording; the wave-watch auto-re-arm request is closed unbuilt because its subject is on the list; the usage poller is parked, not deleted; preflight check one, the launch doctrine's quota leg, the "suspect quota" recovery section, and their tests come out; and the plugin-root seam and its lint, whose delete-when condition is met (Codex aliases the variable), retire. The launch ledger and the launch doctrine's rulings block are doctrine, not workarounds, and stay. *Decide-by:* C9 (the trial's receipt names, piece by piece, what the harness now carries and what it still lacks).

> **R16 — The keep-or-retire entry.** Every plugin component has one entry on the keep-or-retire list, kept by a person in the keep-or-retire record, which lives with the project's definition-docs wherever the project's doc-policy keeps them (`docs/superheroes/KEEP-OR-RETIRE.md` in this repository): the component and its cost or annoyance; its condition (catch-based, citation-based, or usage-based, with the window in calendar days, plugin defaults 45, 45, and 60) and the one action on firing, a proposal to the owner at a gardening pass; its last demonstrated benefit with a receipt or "unknown"; consumer evidence as receipts, "unmeasured" where none; the owner's latest decision; and notes carrying one of four tags and, for a component guarding a model behavior, the engine family. The foundational set at the stamp is the covenant's hard lines and nothing else; the authority surfaces (the worktree guard, the circuit breaker and escalation, model governance, the verify gate, the vet-receipt spine with its owner half) get the owner's one look at C6's landing. The workaround-marker inventory is a section of the same doc; a new marker's entry lands in the PR that adds the workaround. *(Amended 2026-09-16 with Spec A amendment #5: the record's home follows the project's doc-policy for definition-docs, committed here; it is project record of the same class as LEDGERS, so its entries keep receipts with locators and R29 does not bind it.)*

> **R17 — The front door.** Intake is one door for machinery and product: the evidence bar (made to happen in the field or the lab; argued-could-happen fails), the grid of severity ladder and evidence tier with no third instrument, the tiers P0 (owner word at once), P1 (batched word at a walk), P2 (files on the advisor's authority with the grading recorded, the one carve-out from "no filing without the owner's word"), and declined (a registry line with a named trigger); every grading records band, tier, and evidence on the item; the misses log and the revisit-trigger registry live as one pinned comment on the collector; a trigger is read at the door and at the gardening pass and nowhere else; the size-consideration slot, the lane call, the presentation call, and the kind label are the four routing-time calls recorded together in the issue body; folded-in backlog items ride an epic as ratified scope and are listed one per line in the gardening record; every instrument filing carries a reader clause.

> **R18 — The gardening pass.** The pass is owed seven days after the last gardening record (the stamp date stands in until the first record) and is done at a decision walk the owner picks; at the start of any walk, if it is owed and no "gardening pass owed" item is on the collector, the advisor appends one. Its seven duties are the closed list of Spec A FR-A10: age P1s; sweep the registry's triggers, the elapsed retirement conditions, Spec B's tripwires as one outcome account, and the consumer reports, then draw about five proposals off the queue and ask before drawing more; read the misses log and propose recalibrations; read the forward share against the dial in one sentence, the decisions-asked count, the folded-in items, and the window's declines; report workaround markers whose delete-when condition came true; read guardian staleness and, when stale, run and triage the sweep in the same sitting; hand-count escapes by class. The record's contents and shape are FR-A10's; there is no meter on the pass, and "too long" from the owner means fewer and coarser components, never automation.

> **R21 — Charters as maps.** Each charter is a map: one short section per duty stating the rule and its completion criterion, with the mechanism one hop away in a reference doc that loads when the duty fires; excuse tables move to per-skill reference pages or are cut; doctrine is cut where it is restructured, never measured, and the landing note lists what left; the per-role startup context before and after is recorded once in C7's landing note and is the reset's one grading; no line budget, no knob count, no citation tally.

> **R27 — Birth duties for anything that detects.** From the stamp, a new detector, gate, watcher, census, or detector-shaped test ships only with all three birth duties: (i) a bite-proof that demonstrably fires on a planted defect and is deterministic by construction (no wall-clock assertions, no environment inference); (ii) a retirement condition and tag on its keep-or-retire entry; and (iii) by-construction coverage, its scope complete because of how it is built (one chokepoint, a closed enumeration), never a hand-maintained list chased case by case, so a hand-maintained parallel enumeration is itself rung-4 machinery that owes the same duty or does not ship. Every producer names its reader. Nothing watches other machinery.

> **R28 — Delete on contact waits for the keep list.** A behavior test that goes red under a child's change is classified by the builder (a genuine regression means the behavior is fixed and the test kept; an obsolete expectation may be deleted under the standing authorization, unless the keep list names the file), with the classification and reason in the PR body. The rule activates only once the owner has stamped the seeded keep list; before that nothing is deleted and expectations are not rewritten. *Cross-epic seam:* the keep list is seeded by the verification-strategy spec's own children (#1105's set); recorded reciprocally on #1105.

> **R29 — Shipped surfaces carry no project provenance.** A surface that ships in the plugin or stands as a founding document (PHILOSOPHY, CONVENTIONS, README, the rubric, the glossary, the charters and their reference docs) states its rules and definitions without the record of how they were decided: no spec-section numbers, no sitting or checkpoint codes, no dated rulings or renames, no issue or PR numbers except a pointer the project's configuration supplies, and none of this project's own history (its measurements, its accounts, its epic's child names). The reason behind a counter-default rule may stay beside the rule; where it was decided may not. Provenance lives in the PR body that landed the text, the spec's Amendments log, and LEDGERS. The prose standard (`rubric/prose-standard.md`) carries the rule for every later edit.
