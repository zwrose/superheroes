Anchor (spec-section): part-b-spec-a-forward-doctrine · § Part 2 FR-B6 (acceptance 3, quota retired outright, ruled 2026-09-05; the same FR's sentence that `harness_probe` retires or starts recording and that the wave-watch auto-re-arm request is not built); § Part 6 item 11 (the orchestration-decommission child) · as-of amendment #5 (owner-stamped 2026-09-14; record https://github.com/zwrose/superheroes/issues/695#issuecomment-5673081783; spec file: `/Users/zwrose/superheroes/docs/strategic-assessment/part-b/spec-a-doctrine.md`, out-of-repo, local; the Amendments log is its last section). Filed as a split of C15 (#1274) on the owner's word of 2026-09-18 (the split pre-authorization and the overnight-wave sitting, walk record https://github.com/zwrose/superheroes/issues/695#issuecomment-5703107528).

What: The part of C15 that Spec A FR-B6 says lands whatever the background-session trial's receipt says, carved out so it lands now instead of waiting behind C14 and C9's receipt. Three retirements, all deletions: (1) the quota leg — preflight check one (`quota` in `PREFLIGHT_CHECKS` in `plugins/superheroes/lib/launch_doctrine.py` and the launcher's evaluation of it), the launch doctrine's quota line and its "Suspect quota before you suspect a defect" recovery section in `plugins/superheroes/rubric/launch-doctrine.md`, the matching line in `skills/showrunner/reference/dispatch-preflight.md`, and their tests; the usage poller was parked out of the plugin tree on 2026-09-05 (Spec A's record) and stays parked, not deleted, named in the PR body as the allowed residual beside the glossary's history note if one exists; (2) `harness_probe` — `plugins/superheroes/lib/harness_probe.py`, its reader in `session_context.py`, its test module, and its rows in the keep-or-retire record (B5's text and the workaround-marker inventory entry), retired rather than made to record, because its keep-or-retire entry already reads "retired and is not counted" and nothing reads a record it would write; (3) the wave-watch auto-re-arm request (#1098) closed unbuilt — the advisor closes it at vet with a receipt pointing at this PR; the PR body names the issue and the FR-B6 sentence that declines it, and no code changes for it. Nothing else of C15 moves: the six pieces of register R15, the semantic heartbeat, the launcher's spawn path, and the plugin-root seam with its two lints (some 200 seam citations across the skill prose, an edit that would collide with every live lane's skill files tonight) stay in C15 behind C14 and C9's receipt. No replacement detector, census or list ships; a retirement is a deletion.

DoD:
- `PREFLIGHT_CHECKS` carries no `quota` entry and the launcher evaluates none; the launch doctrine's check list and its recovery section carry no quota text; a grep for `quota` over `launch_doctrine.py`, `launcher.py`, `rubric/launch-doctrine.md` and `skills/showrunner/reference/dispatch-preflight.md` returns nothing; the tests that pinned the leg are gone; a launch preflight run on the head records the remaining checks and no quota row, shown in the PR body.
- `harness_probe.py` and `test_harness_probe.py` are gone, `session_context.py` no longer imports or calls it, and a grep for `harness_probe` over `plugins/superheroes/` returns nothing outside the keep-or-retire record's retirement note; a SessionStart bootstrap on the head still injects the charter and covenant context (one recorded run in the PR body).
- The keep-or-retire record: B5's entry no longer lists `harness_probe` as a covered component, and the workaround-marker inventory no longer lists its marker; a retirement note on the affected entry names this PR as the receipt and FR-B6 as the condition met; the marker-inventory grep rule (a grep for the tag over the tree, excluding the record, returns exactly the inventory's set) still holds on the head.
- Issue #1098 is named in the PR body as closed unbuilt under FR-B6, with the sentence quoted; the advisor closes it at vet with a receipt pointing at this PR.
- The launch ledger and the launch doctrine's rulings block are present after the change, shown by their paths in the PR body; `wave_watch.py`, the launcher's spawn path, and every R15 piece are untouched (the diff stat shows no edit to `wave_watch.py` and no edit to `launcher.py` beyond the quota leg).
- Tests that go red under these deletions are classified under R28 in the PR body (an obsolete expectation deleted; nothing behavioural weakened); the PR body's transition note states, in one plain line, that the preflight no longer records an account-headroom check.
- The four validators pass; CI is green on the exact head; the register-quote check passes for this child.

**Epic:** #1259 (reset-landing-2026-09-14). **Child:** C22, the unconditional orchestration retirements (split from C15 on the owner's word of 2026-09-18). **Sequencing:** independent of every open child; C15 keeps the conditional set and lands after C14 and C9's receipt as before. Live-lane overlap at filing: C21 (#1299) also edits `docs/superheroes/KEEP-OR-RETIRE.md`; the later lander merges main and takes a disclosed conflict round. Nothing waits on this child. **Size-consideration slot:** stated at routing: roughly 100 to 200 non-test lines, almost all deletions across two modules, two doc surfaces and the keep-or-retire record; test lines deleted in the low hundreds; the 2x tripwire is the builder's to watch, and a diff above 300 non-test lines is reported before it grows. **Lane call:** full. *Reasoning:* the change removes a preflight check and a session-start probe; an incomplete removal fails loud (an import error, a red test), but the surface is the launcher's preflight, whose wrong change fails quiet on the next wave; full at any size. **Presentation call:** say it. *Reasoning:* an advisor running a preflight sees one fewer check in the record; the PR body and the transition line state it; no spot-check. **Kind:** reset (deletion of machinery; outside the dial per Spec A FR-A6). **Rung line:** none; detectors are removed and nothing is born. **Route and vendors:** cursor `composer-2.5` implements through `dispatch-write` with `--expect-item`; the pre-code brief check runs cross-vendor on codex; review seats exclude the maker family; the builder runs opus at medium; every codex review seat is dispatched through the build checkout's own runner. **Consequence at filing:** after this lands, the plugin does not check account headroom before a launch and does not probe project-context injection at session start; the owner manages usage (ruled 2026-09-18), and the SessionStart bootstrap's own output is the check that context injection holds. Headless and interactive read the same preflight and bootstrap; no audience stays on old behaviour. **Mid-build rulings:** post on the PR and send by cross-session message to the session named `superheroes fable advisor`.

**Register entries this child consumes (quoted verbatim from docs/superheroes/reset-landing-2026-09-14/register.md; the register-quote check runs at filing, intake, and the package read):**

> **R16 — The keep-or-retire entry.** Every plugin component has one entry on the keep-or-retire list, kept by a person in the keep-or-retire record, which lives with the project's definition-docs wherever the project's doc-policy keeps them (`docs/superheroes/KEEP-OR-RETIRE.md` in this repository): the component and its cost or annoyance; its condition (catch-based, citation-based, or usage-based, with the window in calendar days, plugin defaults 45, 45, and 60) and the one action on firing, a proposal to the owner at a gardening pass; its last demonstrated benefit with a receipt or "unknown"; consumer evidence as receipts, "unmeasured" where none; the owner's latest decision; and notes carrying one of four tags and, for a component guarding a model behavior, the engine family. The foundational set at the stamp is the covenant's hard lines and nothing else; the authority surfaces (the worktree guard, the circuit breaker and escalation, model governance, the verify gate, the vet-receipt spine with its owner half) get the owner's one look at C6's landing. The workaround-marker inventory is a section of the same doc; a new marker's entry lands in the PR that adds the workaround. *(Amended 2026-09-16 with Spec A amendment #5: the record's home follows the project's doc-policy for definition-docs, committed here; it is project record of the same class as LEDGERS, so its entries keep receipts with locators and R29 does not bind it.)*

> **R28 — Delete on contact waits for the keep list.** A behavior test that goes red under a child's change is classified by the builder (a genuine regression means the behavior is fixed and the test kept; an obsolete expectation may be deleted under the standing authorization, unless the keep list names the file), with the classification and reason in the PR body. The rule activates only once the owner has stamped the seeded keep list; before that nothing is deleted and expectations are not rewritten. *Cross-epic seam:* the keep list is seeded by the verification-strategy spec's own children (#1105's set); recorded reciprocally on #1105.

> **R29 — Shipped surfaces carry no project provenance.** A surface that ships in the plugin or stands as a founding document (PHILOSOPHY, CONVENTIONS, README, the rubric, the glossary, the charters and their reference docs) states its rules and definitions without the record of how they were decided: no spec-section numbers, no sitting or checkpoint codes, no dated rulings or renames, no issue or PR numbers except a pointer the project's configuration supplies, and none of this project's own history (its measurements, its accounts, its epic's child names). The reason behind a counter-default rule may stay beside the rule; where it was decided may not. Provenance lives in the PR body that landed the text, the spec's Amendments log, and LEDGERS. The prose standard (`rubric/prose-standard.md`) carries the rule for every later edit.
