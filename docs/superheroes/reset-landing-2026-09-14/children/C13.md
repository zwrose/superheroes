Anchor (spec-section): part-b-spec-b-contract-re-derivation · § Part 2 FR-D4, FR-D8 (the loop's half), FR-D9 (the three answers); § Part 1 FR-M3, FR-M4, FR-M5, FR-M7 (the D1 annex's self-vouching families) on the round driver's side · as-of amendment #0 (owner-stamped 2026-09-14; record https://github.com/zwrose/superheroes/issues/695#issuecomment-5673081783)

What: The loop's side of bounded certification: rounds bounded by the declared caps with certification at any round where every finding is dispositioned; the three dispositions recorded in the loop record with the revisit trigger and class-closure lines; the severity keying with the Critical prohibition; the canary as a blocking gate leaves and the planted defect becomes a sampled competence probe; the receipt writer called at certification and at every terminal (cap and cannot-certify included); the round driver's refusals name the accepted shape and its echo set is complete by construction; its variance envelope declared in the round-driver reference; the guidance-channel row stands as ground-(b) KEEP with any change parking to the owner; no stored loop-state field removed; the D1 rows re-verified at the build head with a closing note and the D1 annex's self-vouching families assigned. The successor section of the round-driver reference states FR-D9 in full: what the successor is; the design note answering its three questions; the tripwire's three conditions read at gardening passes (a second driver-defect fix after this child lands, two of ten consecutive full lanes ending at a cap or cannot-certify for a loop reason, one consumer blocked by a loop reason) and that a firing routes the build to build-ready with the observed pattern and the inferred cause beside its alternative; the pre-deletion coverage replay of the four classes and the six cases; the sequencing rule (no deletion while FR-D3's window is open unless the owner closes it, the comparator's last reading recorded first); and the back-out (the prior plugin version runnable for one window, pinned at the deleting release). All as text, none as code.

DoD:
- A fixture review with one Minor finding dispositioned out of scope with a follow-up whose item shows a revisit trigger (a condition, date, or milestone) and a class-closure line certifies on the round it is dispositioned; the same review with the follow-up's trigger missing does not certify; the same review with a Critical set aside ends uncertified; all three recorded as dry runs in the PR body.
- The planted-defect canary no longer blocks certification: a review whose probe fizzled certifies when telemetry shows the seat engaged, recorded in a dry run, and the canary leaves the gate only with C12's canary census receipt (the record since 2026-08-30 showing no plant-caught case would have passed telemetry) attached to this PR.
- A review ending at a cap and a review ending cannot-certify each produce a receipt from the writer with the terminal state and cause set, recorded in dry runs.
- The guidance-channel row is unchanged (a diff of its rows and refusals against the head before the child shows no change), the round driver's refusals name the accepted shape in a recorded sample of each refusal token, and the echo set equals the annex's resolves-caller-input rows.
- The round-driver reference declares the loop's variance envelope, and its successor section states what the successor is (FR-D9's stay-and-go list quoted: the four-class check, the receipt writer, the seat map, the contracts, the grounding seat, and the dispatch core stay; the phases, gates, latches, resume machinery, the canary as a blocking gate, and the escalation ladder go), the tripwire's three conditions, the coverage replay with the rule that the unrun-review class's preventer is runner-side telemetry and that a class without a preventer or a six-case miss parks to the owner with nothing deleted, the sequencing rule, and the back-out, with the design note answering the three questions of register entry R23 in prose a builder could build from, marked as design, not code.
- The D1 annex carries a dated closing note per row this child touched, and no loop-state field is removed (a diff of the state schema shows additions only).
- The four validators pass and CI is green; the register-quote check passes for this child.

**Epic:** #1259 (reset-landing-2026-09-14, the landing epic of the Part B specs; artifacts in PR #1258). **Child:** C13, certification loop. **Sequencing:** Wave 2, after C12. **Size-consideration slot:** stated at routing. **Lane call:** full. **Kind:** reset (outside the dial per Spec A FR-A6; neither machinery nor product for the trend).

**Register entries this child consumes (quoted verbatim from docs/superheroes/reset-landing-2026-09-14/register.md; the register-quote check runs at filing, intake, and the package read):**

> **R1 — The vocabulary and its slugs.** The canonical glossary (one file, named by C1) carries one stable slug per term of Spec A FR-F9's seed, the whole seed and nothing less: ruling; decision walk; gardening pass, record, and window; condition window; keep-or-retire list; keep-or-retire entry; project configuration and configuration item; plugin component; retirement condition; catch-based, citation-based, and usage-based; real catch and noise; citation; background-session trial; consumer evidence; unmeasured; fired; proposal; retirement ruling; foundational component; authority surface; the priority tiers; declined registry, trigger, misses log, collector, and pending-words line; dial, machinery lane, product-forward lane, and N; the child names as reference vocabulary; craft call and owner call; material consequence; kind label; fold-in rules and folded-in items; workaround marker and delete-when condition; always-loaded set; keep list; rail; doctrine surface; foreign catch; the threat tags on the machinery map; and the four tags structural, capability-gap, harness-limit, and mixed; plus ground-truth sources (renamed 2026-09-13). Every other surface cites a slug; no surface restates a definition.

> **R5 — The receipt writer's boundary.** The certification receipt is derived by a receipt writer that is its own module: its only input is the review journal on disk, it imports nothing from the round driver, and its tests run with no driver. It emits the receipt or a refusal that names the class and the artifact. The receipt carries, beside today's fields: the terminal state, one of certified, cap, or cannot-certify; for a non-certified terminal its cause, either a loop reason from the closed set (latch, resume, state hash, detector, fixer cycle) or a non-loop reason (budget, open findings); each seat's provenance in its own field, dispatch-observed or hand-landed, kept separate from the certification shape; the disclosures block; and each finding's severity, disposition, and the receipt for that disposition.

> **R6 — Dispositions.** A finding has exactly one of three dispositions: fixed with a verification receipt on the certified head; refuted with a recorded reason; or out of scope with a named follow-up item that exists at certification time and carries a revisit trigger (a condition, date, or milestone, never "documented") and a class-closure line. Minor and Nit findings may take the third disposition by the review's orchestrator; an Important finding may take it only for a very good reason stated on the disposition, and the receipt writer lists every such Important finding in the receipt's disclosures block; a Critical finding may not take it at all, by anyone: fixed or refuted, or the review is uncertified and the PR merges only as owner-accepted in its body.

> **R7 — Engagement is telemetry.** Whenever the machine needs to know that a dispatched seat actually looked, the evidence is the runner's own record of the seat's tool calls and files opened, never a list the seat authored; for a dispatch-observed seat, absent or empty telemetry refuses. A hand-landed seat has no telemetry and is bound instead by FR-D2's execution-evidence binding on its landed envelope, labeled hand-landed on the receipt. The planted defect leaves every gate and becomes a sampled competence probe run after certification. The launcher's seat canary for builder sessions reads the session transcript's tool calls the same way.

> **R22 — Calmer reviews.** The review loop takes a router-first lane call, the deferral disposition contract of R6 as its third disposition, round-count calming within the declared caps, and "fewer lens prompts with family diversity kept" as a design input; a review finding that proposes new standing machinery routes to the front door and never spawns a build from inside the review; "watch the guard" findings are answered by citing Change 1.

> **R23 — The successor's three answers.** The certification child lands, as a design note and not as code, the three answers the loop's successor needs: how a stateless checker establishes a real run from journals and telemetry alone; how a re-run seat's qualifying result recovers coverage an earlier failed attempt left open; and how every abandoned or capped review still gets a receipt. Nothing of the successor is built or deleted until FR-D9's tripwire fires, and the owner rules the deletion on the coverage check. *Decide-by:* C13 (the note's content).

> **R24 — One version bump.** The certification changes ship behind one state-version bump under the driver's existing supported-versions mechanism; the receipt's new fields, the provenance field, and D1-029's shape change ride it; no stored loop-state field is removed at it or before the loop leaves; in-flight lanes complete on their recorded version; consumer-visible shape changes and the dispatch shell's argument changes are announced in the same release's transition note.

> **R26 — The annexes close.** Each of C10, C11, C12, and C13 moves the live declaration of its surface into the implementation home (the envelope, the entry doc, and the echo set derive from there), re-verifies its rows against its build head with a dated note, and writes the table's last correction in its closing note; when the last of the four lands the derivation tables close as adjudication history and nothing maintains them.

> **R27 — Birth duties for anything that detects.** From the stamp, a new detector, gate, watcher, census, or detector-shaped test ships only with all three birth duties: (i) a bite-proof that demonstrably fires on a planted defect and is deterministic by construction (no wall-clock assertions, no environment inference); (ii) a retirement condition and tag on its keep-or-retire entry; and (iii) by-construction coverage, its scope complete because of how it is built (one chokepoint, a closed enumeration), never a hand-maintained list chased case by case, so a hand-maintained parallel enumeration is itself rung-4 machinery that owes the same duty or does not ship. Every producer names its reader. Nothing watches other machinery.

> **R28 — Delete on contact waits for the keep list.** A behavior test that goes red under a child's change is classified by the builder (a genuine regression means the behavior is fixed and the test kept; an obsolete expectation may be deleted under the standing authorization, unless the keep list names the file), with the classification and reason in the PR body. The rule activates only once the owner has stamped the seeded keep list; before that nothing is deleted and expectations are not rewritten. *Cross-epic seam:* the keep list is seeded by the verification-strategy spec's own children (#1105's set); recorded reciprocally on #1105.
