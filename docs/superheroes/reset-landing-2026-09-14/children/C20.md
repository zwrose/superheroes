Anchor (receipt): https://github.com/zwrose/superheroes/pull/1283#issuecomment-5709682943 (C10's vet 224: three verified drifts on shipped prose that hand-lists vocabularies the code declares, and the prose review loop's park on the third-rework tripwire, PR #1283 comment 5709549661; stacked on C10 by the owner's ruling of 2026-09-17, walk item 1 = c, recorded on https://github.com/zwrose/superheroes/issues/695#issuecomment-5703107528)

What: The shipped reference prose stops hand-maintaining vocabularies the code owns, and instead derives from the one home or points at it. C10 (#1269, PR #1283) gave the dispatch shell's outcome vocabulary and entry vocabulary one home each and generates the entry doc from the shell's own declarations; three shipped passages still carry hand-typed copies, and three consecutive review rounds each found a new drifted member, which is what the tripwire stopped. The three, at C10's head `6105b2b4`: `plugins/superheroes/TRANSITION.md` lists the `<field>Source` marker set by hand and omits `seat-default`, which `seat_bundle.py` emits; `plugins/superheroes/skills/review-code/reference/auto-fix-loop.md` (line 835) lists the entry-refusal reasons by hand and names `engine-model-effort-conflict` where the shell emits `effort-token-conflict`; `docs/superheroes/KEEP-OR-RETIRE.md` (S10, two places) spells the entry-doc check command without its interpreter prefix. The same split exists one hop into the code: `model_registry.py` declares the `seat-default` constant and `seat_bundle.py` compares against the literal beside it. This child closes the class, not the three symptoms: the `<field>Source` marker vocabulary gets one closed enumeration in its implementation home, written by every producer from that home (the seat-bundle resolver, the legacy-journal synthesis, the journal-root resolution) with the chokepoint refusing a marker outside it; the entry-refusal reason set already has its home in `seat_bundle.ENTRY_REFUSAL_REASONS`; the entry-doc generator (`dispatch_entry_doc.py`, guarded by S10's existing `--check`) renders both sets, and the transition note and the auto-fix reference point at the generated entry doc rather than listing; the keep-or-retire command is spelled runnable once. No new detector is born if the work lands as ordered: the generated doc's existing determinism guard (S10, born with its birth duties in C10) covers the new sections, and the marker chokepoint is a refusal at the producer, not a watcher. If the builder finds a second detector is unavoidable, it ships with R27's three birth duties or the fork parks.

DoD:
- The `<field>Source` marker vocabulary has one declaring home; a grep for each marker literal outside that home and the generated doc returns nothing in shipped code or shipped prose, and the PR body carries that census as a one-time read.
- A marker outside the enumeration is refused at the producer's chokepoint; a bite-proof plants an undeclared marker at one producer and the refusal fires; reverted, green.
- The generated entry doc carries the marker set and the entry-refusal reason set rendered from their homes; `dispatch_entry_doc.py --check` refuses when either set changes without regeneration, shown by one planted change (bite-proof re-taken on S10, not a new guard).
- `TRANSITION.md` and `auto-fix-loop.md` carry no hand-typed copy of either set; each points at the generated entry doc, and the three findings vet 224 verified are gone by construction, not by edit.
- The two S10 command spellings in `docs/superheroes/KEEP-OR-RETIRE.md` are runnable as written (interpreter prefix present), and S10's entry names the new sections it now guards.
- The `seat-default` literal in `seat_bundle.py` reads the one constant; no second spelling remains.
- Keep-or-retire: no new entry unless a new detector ships; if one does, its entry carries the retirement condition and tag.
- The four validators pass; CI is green on the exact head; the register-quote check passes for this child; the PR is a native stacked PR on `build/1269-dispatch-entry-shrink` and lands only as one stack with C10.

**Epic:** #1259 (reset-landing-2026-09-14). **Child:** C20, the prose points at the code's vocabularies (C10's seam child; filed on the owner's word of 2026-09-17, walk item 1 = c). **Stack:** branch cut from `build/1269-dispatch-entry-shrink` at `6105b2b4` (C10's vetted head, PR #1283, draft); this child's PR has that branch as its base, the same shape as C18 on C2 and C12 layer 2 on layer 1; C10 stays draft and the two merge as one `gh stack merge` click after this child's vet. The advisor pushes the branch with one package-fix commit (register Consumers lines for R2, R27, R29 name C20; `children/C20.md`; the README child count) so the register-quote check passes at intake; the builder adopts that head. **Sequencing:** now, beside the live C12 layer 2 lane; both touch `plugins/superheroes/TRANSITION.md` and `docs/superheroes/KEEP-OR-RETIRE.md`, a recorded overlap: the later lander rebases and takes a disclosed conflict round. C11 (#1270) follows the stack. **Size-consideration slot:** stated at routing: one enumeration, one generator extension, three prose edits, one literal; roughly 100 to 200 non-test lines, tests and bite-proofs separate; the 2× tripwire is the builder's to watch. **Lane call:** full. *Reasoning:* a generated passage's drift guard that quietly stops firing is the quiet-failure shape, the work touches the dispatch shell's producers under the safety-machinery route, and R27's birth duties are graded at vet; full at any size. **Presentation call:** say it. *Reasoning:* a reader-visible correction to shipped reference text (one of them the transition note weekly-eats reads), a craft call the panel checks; the PR body states the change in plain language, no spot-check. **Kind:** reset (derivation replacing hand-maintained copies; no new machinery unless disclosed). **Rung line:** rung 1 for the one home and the derived prose (a copy that cannot exist); rung 2 for the chokepoint refusal at the producer; S10's existing guard extends, no detector is born. **Route and vendors:** safety-machinery route for `seat_bundle.py`, `engine_dispatch.py`, `model_registry.py`, `dispatch_entry_doc.py`; cursor `composer-2.5` implements through `dispatch-write` with `--expect-item`; the pre-code brief check runs cross-vendor on codex; review seats exclude the maker family; the builder runs opus at medium. **Mid-build rulings:** post on the PR and send by cross-session message to the session named `superheroes fable advisor`. **Consequence at filing:** C10's merge, and the transition note weekly-eats receives at that click, wait for this child's vet; no audience is left on old behavior, and headless and interactive read the same generated doc.

**Register entries this child consumes (quoted verbatim from docs/superheroes/reset-landing-2026-09-14/register.md; the register-quote check runs at filing, intake, and the package read):**

> **R2 — One home per rule.** Every rule, schema, and contract has exactly one home, the one Spec A Part 6 names, and every other surface points at it; a drift-tested copy is not a second home, and a drift pin on a mirror is a delete signal for the mirror. The homes this epic lands: the front door in `skills/showrunner/reference/owner-decisions.md`; the tier vocabulary, size slot, kind labels, and the four routing-time calls in `skills/showrunner/reference/issue-contract.md`; Changes 1, 2, 4-doctrine, and 7 with the pin rule in `rubric/review-discipline.md`; the keep-or-retire list and the workaround-marker inventory in one new keep-or-retire reference doc; the prose standard in `rubric/prose-standard.md`; the glossary in one file; the forward share and dial principle in PHILOSOPHY with the numbers in the configure profile; promise 1's new shape in PHILOSOPHY, the covenant, showrunner duty 6, and `merge-train.md` amended together in one change set (C1); the trust boundary in PHILOSOPHY's threat-model paragraph, the configure profile's item 10, and one sentence in the security lens prompt; the host position in CONVENTIONS section 7, the README host line, and the dated `hosts/codex-tools.md`.

> **R27 — Birth duties for anything that detects.** From the stamp, a new detector, gate, watcher, census, or detector-shaped test ships only with all three birth duties: (i) a bite-proof that demonstrably fires on a planted defect and is deterministic by construction (no wall-clock assertions, no environment inference); (ii) a retirement condition and tag on its keep-or-retire entry; and (iii) by-construction coverage, its scope complete because of how it is built (one chokepoint, a closed enumeration), never a hand-maintained list chased case by case, so a hand-maintained parallel enumeration is itself rung-4 machinery that owes the same duty or does not ship. Every producer names its reader. Nothing watches other machinery.

> **R29 — Shipped surfaces carry no project provenance.** A surface that ships in the plugin or stands as a founding document (PHILOSOPHY, CONVENTIONS, README, the rubric, the glossary, the charters and their reference docs) states its rules and definitions without the record of how they were decided: no spec-section numbers, no sitting or checkpoint codes, no dated rulings or renames, no issue or PR numbers except a pointer the project's configuration supplies, and none of this project's own history (its measurements, its accounts, its epic's child names). The reason behind a counter-default rule may stay beside the rule; where it was decided may not. Provenance lives in the PR body that landed the text, the spec's Amendments log, and LEDGERS. The prose standard (`rubric/prose-standard.md`) carries the rule for every later edit.
