Anchor (spec-section): part-b-spec-b-contract-re-derivation · § Part 3 FR-S5 (the two Claude populations), FR-S9; Spec A § FR-F5 · as-of amendment #0 (owner-stamped 2026-09-14; record https://github.com/zwrose/superheroes/issues/695#issuecomment-5673081783)

What: Command-line Claude registered as an engine in the adapter with the print and background result contracts, exercised under the target config dir at dispatch, with the same refusal tokens as codex and cursor and no quota leg; the launcher's hand-built argv retired into it (this child edits the launcher's spawn path; C15 follows it); the seat canary reading the session transcript's tool calls; the certification disclosure line for unprobed native seats; the conformance probe exercising result, completion, and progress separately for each mode. Astra registered under codex as the top rung at effort high, Sol moved to reviewer and verifier, landing only after the security-lens probe names a planted fail-open by location, every attempt recorded in the annex; until then Sol stays deep and receipts disclose it.

DoD:
- A print-mode dispatch and a background-mode dispatch of Claude each return a typed result through the adapter, and a launch acknowledgement alone is graded as no result, all three recorded in the PR body.
- A Claude dispatch runs under the target config dir (the recorded argv and environment show it), a wrong-config-dir dispatch refuses with the same token family codex and cursor use, and no quota check runs on the path (a grep for the quota leg in the adapter's Claude branch returns nothing).
- For each Claude mode, recorded tests show result production, completion detection, and progress telemetry each exercised separately.
- The launcher no longer builds a claude argv by hand: a grep for the old construction returns nothing, a builder launch goes through the adapter, and the seat canary reads the transcript's tool calls in a recorded run.
- A certification resting on a native in-session seat carries the unprobed-native disclosure line, shown in a recorded receipt.
- The Astra probe attempts are recorded in the annex, each with the dispatch's own result; a recorded pass quotes the file, line, and severity the dispatch returned for the planted fail-open, and only then does the registry show Astra at reviewer-deep at effort high with Sol at reviewer and verifier; on a miss Sol is unchanged and a receipt produced during the build carries the pending-registration disclosure; a third miss shows the owner proposal row in the annex.
- For both Claude modes, a malformed result refuses with the common refusal shape and a planted secret is scrubbed from the result and the journal, recorded.
- The four validators pass and CI is green; the register-quote check passes for this child.

**Epic:** #1259 (reset-landing-2026-09-14, the landing epic of the Part B specs; artifacts in PR #1258). **Child:** C14, CLI Claude engine and Astra. **Sequencing:** Wave 3, after C11. **Size-consideration slot:** stated at routing. **Lane call:** full. **Kind:** reset (outside the dial per Spec A FR-A6; neither machinery nor product for the trend).

**Register entries this child consumes (quoted verbatim from docs/superheroes/reset-landing-2026-09-14/register.md; the register-quote check runs at filing, intake, and the package read):**

> **R5 — The receipt writer's boundary.** The certification receipt is derived by a receipt writer that is its own module: its only input is the review journal on disk, it imports nothing from the round driver, and its tests run with no driver. It emits the receipt or a refusal that names the class and the artifact. The receipt carries, beside today's fields: the terminal state, one of certified, cap, or cannot-certify; for a non-certified terminal its cause, either a loop reason from the closed set (latch, resume, state hash, detector, fixer cycle) or a non-loop reason (budget, open findings); each seat's provenance in its own field, dispatch-observed or hand-landed, kept separate from the certification shape; the disclosures block; and each finding's severity, disposition, and the receipt for that disposition.

> **R7 — Engagement is telemetry.** Whenever the machine needs to know that a dispatched seat actually looked, the evidence is the runner's own record of the seat's tool calls and files opened, never a list the seat authored; for a dispatch-observed seat, absent or empty telemetry refuses. A hand-landed seat has no telemetry and is bound instead by FR-D2's execution-evidence binding on its landed envelope, labeled hand-landed on the receipt. The planted defect leaves every gate and becomes a sampled competence probe run after certification. The launcher's seat canary for builder sessions reads the session transcript's tool calls the same way.

> **R8 — The dispatch shell's entry shape.** The shell has one documented entry shape: the seat bundle `{vendor, model, effort}` passes intact at every seam and effort is echoed with its source, never guessed; of the C1 annex's entry rows, the KEEP rows keep as declared, the MOVE rows that accept more shapes or default read tolerantly with a loud echo naming what was resolved, and the MOVE rows that are dropped read nothing, resolve no caller input, and echo nothing; every refusal names in its own text what would have been accepted; the entry doc is regenerated from the shell's declaration and the shipped skill texts point at it. There is no alias window: a dropped, renamed, or newly required argument is announced in the release note's transition note, the refusal a stale caller meets names the replacement, and the registered consumers are told once.

> **R9 — Native result channels.** Each engine returns a typed result on its own structured channel, validated against a schema the shell declares per engine: codex on `--output-schema` with `-o`; cursor only after one two-dispatch trial (a review and a write on `--output-format json`, each validating within the declared wait) passes, else it stays on stream-json with the cap sized as an operating parameter; command-line Claude in print mode as the typed final response at exit under `--output-format json` with `--json-schema`, and in background mode as the session's final message retrieved after the idle notification, never the launch acknowledgement. Secret scrubbing and refuse-on-malformed are requirements on every channel; the marker parser and the salvage tiers retire for each engine that moves; the dispatch journal records the channel each run used; which engine is on which channel is recorded in the C1 values annex.

> **R10 — The conformance probe.** At each wave preflight one probe dispatch per engine on its channel must return a typed result that validates; a failing probe is loud, the preflight names the engine, what failed, and the lanes that depend on it, and the choice (launch without the engine, or hold the wave and troubleshoot) sits beside the launch word, so nothing launches on a failed probe without the owner's word. When the owner launches without the engine and it supplied a lane's second family, the preflight names the substitute family under the seat map's substitution rule or parks those full lanes. A drift or auth failure arriving mid-wave forfeits that dispatch with no salvage. The probe is invocable as one command with no argument beyond the engine name, its result is recorded in the preflight's existing record, and no engine CLI version is pinned.

> **R11 — The allowlist is hard shell.** An off-allowlist model cannot be dispatched on any path: the dispatch-guard check is wired into the dispatch surface as a safety refusal that names the allowlist, never a silent substitute; no tolerant entry reading and no cached liveness verdict resolves a model past it; the birth bite-proof attempts an off-allowlist dispatch on each path and every one refuses. The liveness cache keeps cell granularity with a one-hour value.

> **R14 — Command-line Claude is an engine.** CLI-spawned Claude (`claude -p` builders, `claude --bg` in the background-session trial, any subprocess-dispatched Claude seat) is registered in the engine adapter with the print and background result contracts of R9, exercised under the target `CLAUDE_CONFIG_DIR` at dispatch, with the same refusal tokens as codex and cursor and no quota leg; the launcher's hand-built argv retires into it. Native in-session seats are declared live and never probed, with a disclosure line on any certification resting on an unprobed native family and a LEDGERS section 3 residual recording it. Astra registers under the codex vendor as the top rung and takes the `reviewer-deep` cell at effort high, Sol moving to `reviewer` and `verifier`, only after one live security-lens dispatch names a planted fail-open by file, line, and severity; every attempt is recorded in the C1 values annex, three misses put the question to the owner, and until the pass Sol stays deep and every receipt resting on the seat discloses the pending registration.

> **R15 — The orchestration decommission scope.** The six orchestration pieces whose delete-when condition has been met are detached spawn in the launcher; wave-watch arming and the re-arm ritual; transcript-mtime liveness; the turn-end doctrine with its 540 and 600 second slice recipes; multi-account provisioning transport; and the launcher-enforced half of the own-worktree ruling (the prose stays for direct builders). They retire behind one receipt, the background-session trial's, and only if that receipt comes back clean; otherwise all six stay as plain keeps with their markers. On the same receipt, and only these wait on it: the six pieces and the semantic heartbeat, which retires with the watcher unless its record shows a stale-with-live-pid catch that idle or exit signals would have surfaced too late. Unconditionally, waiting on nothing: `harness_probe` retires or starts recording; the wave-watch auto-re-arm request is closed unbuilt because its subject is on the list; the usage poller is parked, not deleted; preflight check one, the launch doctrine's quota leg, the "suspect quota" recovery section, and their tests come out; and the plugin-root seam and its lint, whose delete-when condition is met (Codex aliases the variable), retire. The launch ledger and the launch doctrine's rulings block are doctrine, not workarounds, and stay. *Decide-by:* C9 (the trial's receipt names, piece by piece, what the harness now carries and what it still lacks).

> **R27 — Birth duties for anything that detects.** From the stamp, a new detector, gate, watcher, census, or detector-shaped test ships only with all three birth duties: (i) a bite-proof that demonstrably fires on a planted defect and is deterministic by construction (no wall-clock assertions, no environment inference); (ii) a retirement condition and tag on its keep-or-retire entry; and (iii) by-construction coverage, its scope complete because of how it is built (one chokepoint, a closed enumeration), never a hand-maintained list chased case by case, so a hand-maintained parallel enumeration is itself rung-4 machinery that owes the same duty or does not ship. Every producer names its reader. Nothing watches other machinery.

> **R28 — Delete on contact waits for the keep list.** A behavior test that goes red under a child's change is classified by the builder (a genuine regression means the behavior is fixed and the test kept; an obsolete expectation may be deleted under the standing authorization, unless the keep list names the file), with the classification and reason in the PR body. The rule activates only once the owner has stamped the seeded keep list; before that nothing is deleted and expectations are not rewritten. *Cross-epic seam:* the keep list is seeded by the verification-strategy spec's own children (#1105's set); recorded reciprocally on #1105.

> **R29 — Shipped surfaces carry no project provenance.** A surface that ships in the plugin or stands as a founding document (PHILOSOPHY, CONVENTIONS, README, the rubric, the glossary, the charters and their reference docs) states its rules and definitions without the record of how they were decided: no spec-section numbers, no sitting or checkpoint codes, no dated rulings or renames, no issue or PR numbers except a pointer the project's configuration supplies, and none of this project's own history (its measurements, its accounts, its epic's child names). The reason behind a counter-default rule may stay beside the rule; where it was decided may not. Provenance lives in the PR body that landed the text, the spec's Amendments log, and LEDGERS. The prose standard (`rubric/prose-standard.md`) carries the rule for every later edit.
