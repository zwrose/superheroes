Anchor (spec-section): part-b-spec-b-contract-re-derivation · § Part 3 FR-S8; Spec A § Part 7 FR-F3 (what changes; the machinery map's column) · as-of amendment #0 (owner-stamped 2026-09-14; record https://github.com/zwrose/superheroes/issues/695#issuecomment-5673081783)

What: The sanitized view shrinks to what trusted-repo pollution needs. Kept: the neutral-cwd export, the exclusion of the twelve configuration files and six directories, the containment family. Added: the review-only copy of stripped configuration changes as a plain file beside the review patch, named as data under review and named in the seat prompt as a context file. Retired, each listed by name in the child's first dated note before anything leaves: the hostile-repo defense set (the header-spoof check, the hostile diff-filter defenses, the forged-gitlink handling, the symlink-swap race guards, the ancestry scratch isolation, the partial-clone refusal) with their tests, and the untrusted-git gotcha family, which retires from the memory index to its archive under the owner's park-never-delete rule for memory files (the specs name the family as retiring; parking is how memory retires here). The machinery map's threat column selects the inventory; the advisor supplies it before the build.

DoD:
- The first dated note on this issue lists every retiring element by name with its file and test, and the gotcha family's members by name, posted before any deletion commit, and the owner's veto window is recorded.
- A review of a change that edits CLAUDE.md delivers the changed hunks in the review-only file and the seats cite it as the subject, recorded in a dry run; the file is absent from instruction discovery (no seat loads it as configuration).
- The containment refusals still fire on their fixtures (an outward symlink, a wrong repo root, a mismatched PR-body session, a symlinked run dir), each shown in the PR body.
- The retired elements and their tests are gone; the gotcha family's entries are moved to the memory archive index and gone from the active index, shown by the index diff in the PR body.
- The four validators pass and CI is green; the register-quote check passes for this child.

**Epic:** reset-landing-2026-09-14 (the landing epic of the Part B specs). **Child:** C16, sanitized-view shrink. **Sequencing:** Wave 1, after C1. **Size-consideration slot:** stated at routing. **Lane call:** full. **Kind:** reset (outside the dial per Spec A FR-A6; neither machinery nor product for the trend).

**Register entries this child consumes (quoted verbatim from docs/superheroes/reset-landing-2026-09-14/register.md; the register-quote check runs at filing, intake, and the package read):**

> **R12 — The trust boundary.** The plugin reviews repos its owner trusts; review of code that could steer its reviewer is out of scope, disclaimed in PHILOSOPHY's threat-model paragraph and each project's configuration item 10, and on the declined registry with the one trigger "#40 goes live". The sanitized view keeps the neutral-cwd export, the exclusion of the twelve agent-configuration files and six directories from the tree the seats run in, and the containment family (the outward-symlink refusal, repo-root validation, the PR-body session pairing, the run-dir symlink refusal); a changed hunk on a stripped path is delivered beside the review patch as a plain review-only file whose name says it is data under review, which the seat prompt names as a context file; the hostile-repo defense set retires, listed by name in C16's first dated note before anything leaves. The security lens reads the project's declared threat model, and an accepted exposure is not a finding.

> **R16 — The keep-or-retire entry.** Every plugin component has one entry on the keep-or-retire list, kept by a person in the keep-or-retire reference doc: the component and its cost or annoyance; its condition (catch-based, citation-based, or usage-based, with the window in calendar days, plugin defaults 45, 45, and 60) and the one action on firing, a proposal to the owner at a gardening pass; its last demonstrated benefit with a receipt or "unknown"; consumer evidence as receipts, "unmeasured" where none; the owner's latest decision; and notes carrying one of four tags and, for a component guarding a model behavior, the engine family. The foundational set at the stamp is the covenant's hard lines and nothing else; the authority surfaces (the worktree guard, the circuit breaker and escalation, model governance, the verify gate, the vet-receipt spine with its owner half) get the owner's one look at C6's landing. The workaround-marker inventory is a section of the same doc; a new marker's entry lands in the PR that adds the workaround.

> **R27 — Birth duties for anything that detects.** From the stamp, a new detector, gate, watcher, census, or detector-shaped test ships only with all three birth duties: (i) a bite-proof that demonstrably fires on a planted defect and is deterministic by construction (no wall-clock assertions, no environment inference); (ii) a retirement condition and tag on its keep-or-retire entry; and (iii) by-construction coverage, its scope complete because of how it is built (one chokepoint, a closed enumeration), never a hand-maintained list chased case by case, so a hand-maintained parallel enumeration is itself rung-4 machinery that owes the same duty or does not ship. Every producer names its reader. Nothing watches other machinery.

> **R28 — Delete on contact waits for the keep list.** A behavior test that goes red under a child's change is classified by the builder (a genuine regression means the behavior is fixed and the test kept; an obsolete expectation may be deleted under the standing authorization, unless the keep list names the file), with the classification and reason in the PR body. The rule activates only once the owner has stamped the seeded keep list; before that nothing is deleted and expectations are not rewritten. *Cross-epic seam:* the keep list is seeded by the verification-strategy spec's own children (#1105's set); recorded reciprocally on #1105.
