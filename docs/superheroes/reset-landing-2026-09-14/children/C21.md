Anchor (ruling): 2026-09-17 · owner ruling, decision walk item 10 = d ("not an appropriate threat model to be defending against; unnecessary machinery") · https://github.com/zwrose/superheroes/issues/695#issuecomment-5703107528 (the walk record carries the ruling and its date; collector @226-4 struck with it)

What: Retire the safety-machinery refusal that bars the certified loop's automated fixer from editing a named set of plugin files. Today `lib/escalation.py` carries a `SAFETY_MACHINERY` tuple and `is_safety_machinery`; the auto-fix loop's fixer runs a file-scope guard (`escalation.py guard --root --path`) before each edit and must not touch a file the guard refuses, reporting it as escalated; `escalation_resolve.py` reads the same predicate; and `rubric/review-discipline.md` carries the section "The safety-machinery route — the guard refuses the fixer" that tells a build what to do with the refused findings (implementer orders under the builder's authority). The owner ruled the threat this defends against (a fixer relaxing a gate on its own authority mid-round) not worth machinery: the independent fix audit and each gate's bite-proof are the checks that matter, and the guard's cost was measurable today (five implementer round trips across two lanes for fixes the panel had already specified). This child removes the guard entirely: the tuple, the predicate, the CLI `guard` verb and the fixer-prompt instruction go; the fixer edits any file in the diff like any other; the rubric section is retired and every pointer to it (the review-code references `auto-fix-loop.md` and `round-driver.md`, the order template `rubric/orders/dispatch-fixer.md`, the reviewer agent prompts `agents/code-reviewer.md` and `agents/premortem-reviewer.md`, the keep-or-retire entry D5 where it names the guard, and the `escalation-base.md` list item that names editing safety machinery at runtime) is deleted or rewritten so no shipped surface describes a refusal that no longer happens. The circuit breaker and the escalation resolver's other duties (rung routing, floor classification, the stuck-loop halt) are not touched. No replacement detector, census or list ships; a retirement is a deletion.

DoD:
- `lib/escalation.py` has no `SAFETY_MACHINERY` tuple, no `is_safety_machinery`, and no `guard` CLI verb; `escalation_resolve.py` no longer reads a safety predicate; a grep for `is_safety_machinery`, `SAFETY_MACHINERY`, "safety machinery" and "Escalation guard" across `plugins/superheroes/` returns nothing outside the keep-or-retire record and this child's own PR body.
- The auto-fix loop's fixer prompt and `skills/review-code/reference/auto-fix-loop.md` carry no file-scope guard step and no `allow`/`escalated` refusal path for guarded files; a fixer dispatched on a finding in `engine_dispatch.py` or `round_driver.py` edits the file and the fix audit grades it like any other (one test drives a fixer order on such a file through the loop's dispatch path and asserts no escalation is recorded).
- `rubric/review-discipline.md`'s section "The safety-machinery route — the guard refuses the fixer" is removed, and every citation of it in the plugin's charters and references is removed or repointed; `validate_skills.py` passes with no dangling citation.
- The keep-or-retire record: D5's entry no longer describes the fixer guard as a component; a retirement note on the affected entry names this PR as the receipt and the owner's ruling as the condition; no new entry is added.
- Tests that pinned the guard (`is_safety_machinery` membership, the guard CLI, the fixer's escalation on a guarded file) are classified under R28: deleted as obsolete expectations only where the keep list permits, otherwise re-pointed to the retired behaviour; the classification table is in the PR body; nothing behavioural is weakened.
- The four validators pass; CI is green on the exact head; the register-quote check passes for this child.

**Epic:** #1259 (reset-landing-2026-09-14). **Child:** C21, retire the safety-machinery fixer refusal (filed on the owner's word of 2026-09-17, walk item 10 = d). **Sequencing:** independent of every open child; it touches `escalation.py`, `escalation_resolve.py`, the auto-fix loop reference and `review-discipline.md`, so it lands after the C3 + C5 stack (which rewrites `review-discipline.md`) to avoid a conflict round; nothing waits on it. **Size-consideration slot:** stated at routing: a deletion of roughly 100 to 150 non-test lines across four modules and three prose surfaces, plus test reclassification; the 2× tripwire is the builder's to watch, and a diff above 300 non-test lines is reported before it grows. **Lane call:** full. *Reasoning:* a retirement whose incomplete form leaves a dangling guard call fails loud, but the change edits the certified loop's fixer path itself, a safety surface under the safety-machinery route it retires, and the vet grades that nothing else on that path changed; full at any size. **Presentation call:** say it. *Reasoning:* a consumer-visible change in what the auto-fix loop may edit; the PR body and the transition note state it in plain language; no spot-check. **Kind:** reset (deletion of machinery). **Rung line:** none; a detector is removed and nothing is born. **Route and vendors:** cursor `composer-2.5` implements through `dispatch-write` with `--expect-item`; the pre-code brief check runs cross-vendor on codex; review seats exclude the maker family; the builder runs opus at medium; every codex review seat is dispatched through the build checkout's own runner. **Consequence at filing:** after this lands, the certified loop's fixer may edit any file the panel names, including the dispatch shell and the driver; the independent fix audit and the gates' bite-proofs are the checks on those edits, and the owner accepted that trade. Headless and interactive read the same loop; no audience stays on old behaviour. **Mid-build rulings:** post on the PR and send by cross-session message to the session named `superheroes fable advisor`.

**Register entries this child consumes (quoted verbatim from docs/superheroes/reset-landing-2026-09-14/register.md; the register-quote check runs at filing, intake, and the package read):**

> **R16 — The keep-or-retire entry.** Every plugin component has one entry on the keep-or-retire list, kept by a person in the keep-or-retire record, which lives with the project's definition-docs wherever the project's doc-policy keeps them (`docs/superheroes/KEEP-OR-RETIRE.md` in this repository): the component and its cost or annoyance; its condition (catch-based, citation-based, or usage-based, with the window in calendar days, plugin defaults 45, 45, and 60) and the one action on firing, a proposal to the owner at a gardening pass; its last demonstrated benefit with a receipt or "unknown"; consumer evidence as receipts, "unmeasured" where none; the owner's latest decision; and notes carrying one of four tags and, for a component guarding a model behavior, the engine family. The foundational set at the stamp is the covenant's hard lines and nothing else; the authority surfaces (the worktree guard, the circuit breaker and escalation, model governance, the verify gate, the vet-receipt spine with its owner half) get the owner's one look at C6's landing. The workaround-marker inventory is a section of the same doc; a new marker's entry lands in the PR that adds the workaround. *(Amended 2026-09-16 with Spec A amendment #5: the record's home follows the project's doc-policy for definition-docs, committed here; it is project record of the same class as LEDGERS, so its entries keep receipts with locators and R29 does not bind it.)*

> **R27 — Birth duties for anything that detects.** From the stamp, a new detector, gate, watcher, census, or detector-shaped test ships only with all three birth duties: (i) a bite-proof that demonstrably fires on a planted defect and is deterministic by construction (no wall-clock assertions, no environment inference); (ii) a retirement condition and tag on its keep-or-retire entry; and (iii) by-construction coverage, its scope complete because of how it is built (one chokepoint, a closed enumeration), never a hand-maintained list chased case by case, so a hand-maintained parallel enumeration is itself rung-4 machinery that owes the same duty or does not ship. Every producer names its reader. Nothing watches other machinery.

> **R29 — Shipped surfaces carry no project provenance.** A surface that ships in the plugin or stands as a founding document (PHILOSOPHY, CONVENTIONS, README, the rubric, the glossary, the charters and their reference docs) states its rules and definitions without the record of how they were decided: no spec-section numbers, no sitting or checkpoint codes, no dated rulings or renames, no issue or PR numbers except a pointer the project's configuration supplies, and none of this project's own history (its measurements, its accounts, its epic's child names). The reason behind a counter-default rule may stay beside the rule; where it was decided may not. Provenance lives in the PR body that landed the text, the spec's Amendments log, and LEDGERS. The prose standard (`rubric/prose-standard.md`) carries the rule for every later edit.
