Anchor (spec-section): part-b-spec-b-contract-re-derivation · § Part 3 FR-S1 (the two populations, the boundary, supervision, the entry-surface rule), FR-S2, FR-S3, FR-S4, FR-S6, FR-S7; § Part 1 FR-M3, FR-M4, FR-M5, FR-M6, FR-M7 (the C1 annex's self-vouching families) · as-of amendment #0 (owner-stamped 2026-09-14; record https://github.com/zwrose/superheroes/issues/695#issuecomment-5673081783)

What: The dispatch shell's entry surface shrinks to one documented shape: the seat bundle passed intact with effort echoed and its source recorded; the C1 annex's KEEP rows kept, the accepting or defaulting MOVE rows read tolerantly with a loud echo, and the dropped MOVE rows reading nothing and echoing nothing; every refusal naming what would have been accepted; the entry doc regenerated from the shell's declaration with the skill texts pointing at it; the allowlist check wired into every dispatch path as a safety refusal with its birth bite-proof; the liveness cache at one hour, cell granularity, with a cache-served failure disclosed loudly (the vendor-loss degradation the shipped #1101 pattern uses, with the verdict labeled cache-served and its probe time); the forfeit ledger retired (no reader) with preflight refusals returned to the caller; the stdout cap, salvage paths, and dirty-tree probe moved to keep-or-retire entries; the refusal-streak condition stated in the mechanics doc; the dispatch-mechanics variance envelope declared; the C1 annex's self-vouching rows assigned by family (scrub and containment keep; the rest move: those that default or accept more shapes echo what they resolved, those that are dropped echo nothing). No alias window. The parse, scrub, and forfeit-grading boundary rows are not touched here.

DoD:
- An off-allowlist dispatch attempted on each dispatch path (review, write, brief-check, poll, and any other entry) refuses with the allowlist named; the attempts and their refusals are recorded in the PR body.
- Every MOVE row of the C1 annex either reads tolerantly with its echo in the result or is dropped and reads nothing, resolves no caller input, and echoes nothing (one recorded dropped-input case shows no resolved-input or echo field), and the annex carries a dated closing note per row; the count of rows moved equals the annex's MOVE count.
- A dispatch that omits effort records the resolved effort and its source in the dispatch record, and a dispatch with a wrong flag returns a refusal whose text names the accepted shape; both recorded in the PR body.
- The liveness cache's default is 3600 seconds at cell granularity, shown by a recorded probe-then-reprobe within the hour that serves the cached verdict labeled as cache-served; a recorded cached-failure case shows the loud vendor-loss degradation with the cache provenance and probe time in the disclosure.
- The entry doc is generated from the shell's declaration by a command named in the PR body, no shipped skill text carries an invocation example that does not point at it, and the doc declares the shell's variance envelope and states the refusal-streak condition.
- The release note's transition note lists every dropped, renamed, or newly required argument; a recorded stale-caller dispatch using a dropped flag refuses with the replacement named; the PR body records the one message to the registered consumers (weekly-eats' write path named).
- lib/forfeit_ledger.py and its tests are gone; a preflight refusal returns a result naming the refusal; the three keep-or-retire entries exist in C6's doc or are queued for it in the PR body.
- The four validators pass and CI is green; the register-quote check passes for this child.

**Epic:** #1259 (reset-landing-2026-09-14, the landing epic of the Part B specs; artifacts in PR #1258). **Child:** C10, dispatch entry shrink. **Sequencing:** Wave 1, after C1. **Size-consideration slot:** stated at routing. **Lane call:** full. **Kind:** reset (outside the dial per Spec A FR-A6; neither machinery nor product for the trend).

**Register entries this child consumes (quoted verbatim from docs/superheroes/reset-landing-2026-09-14/register.md; the register-quote check runs at filing, intake, and the package read):**

> **R1 — The vocabulary and its slugs.** The canonical glossary (one file, named by C1) carries one stable slug per term of Spec A FR-F9's seed, the whole seed and nothing less: ruling; decision walk; gardening pass, record, and window; condition window; keep-or-retire list; keep-or-retire entry; project configuration and configuration item; plugin component; retirement condition; catch-based, citation-based, and usage-based; real catch and noise; citation; background-session trial; consumer evidence; unmeasured; fired; proposal; retirement ruling; foundational component; authority surface; the priority tiers; declined registry, trigger, misses log, collector, and pending-words line; dial, machinery lane, product-forward lane, and N; the child names as reference vocabulary; craft call and owner call; material consequence; kind label; fold-in rules and folded-in items; workaround marker and delete-when condition; always-loaded set; keep list; rail; doctrine surface; foreign catch; the threat tags on the machinery map; and the four tags structural, capability-gap, harness-limit, and mixed; plus ground-truth sources (renamed 2026-09-13). Every other surface cites a slug; no surface restates a definition.

> **R8 — The dispatch shell's entry shape.** The shell has one documented entry shape: the seat bundle `{vendor, model, effort}` passes intact at every seam and effort is echoed with its source, never guessed; of the C1 annex's entry rows, the KEEP rows keep as declared, the MOVE rows that accept more shapes or default read tolerantly with a loud echo naming what was resolved, and the MOVE rows that are dropped read nothing, resolve no caller input, and echo nothing; every refusal names in its own text what would have been accepted; the entry doc is regenerated from the shell's declaration and the shipped skill texts point at it. There is no alias window: a dropped, renamed, or newly required argument is announced in the release note's transition note, the refusal a stale caller meets names the replacement, and the registered consumers are told once.

> **R9 — Native result channels.** Each engine returns a typed result on its own structured channel, validated against a schema the shell declares per engine: codex on `--output-schema` with `-o`; cursor only after one two-dispatch trial (a review and a write on `--output-format json`, each validating within the declared wait) passes, else it stays on stream-json with the cap sized as an operating parameter; command-line Claude in print mode as the typed final response at exit under `--output-format json` with `--json-schema`, and in background mode as the session's final message retrieved after the idle notification, never the launch acknowledgement. Secret scrubbing and refuse-on-malformed are requirements on every channel; the marker parser and the salvage tiers retire for each engine that moves; the dispatch journal records the channel each run used; which engine is on which channel is recorded in the C1 values annex.

> **R11 — The allowlist is hard shell.** An off-allowlist model cannot be dispatched on any path: the dispatch-guard check is wired into the dispatch surface as a safety refusal that names the allowlist, never a silent substitute; no tolerant entry reading and no cached liveness verdict resolves a model past it; the birth bite-proof attempts an off-allowlist dispatch on each path and every one refuses. The liveness cache keeps cell granularity with a one-hour value.

> **R24 — One version bump.** The certification changes ship behind one state-version bump under the driver's existing supported-versions mechanism; the receipt's new fields, the provenance field, and D1-029's shape change ride it; no stored loop-state field is removed at it or before the loop leaves; in-flight lanes complete on their recorded version; consumer-visible shape changes and the dispatch shell's argument changes are announced in the same release's transition note.

> **R25 — What stays hard shell in the dispatch shell.** The journal, the run lock, and max-wait stay as hard shell; the secret scrub and refuse-on-malformed stay as requirements on every channel; the parse, scrub, and forfeit-grading boundary rows keep as declared; the stdout cap, the salvage paths, and the dirty-tree probe become keep-or-retire entries rather than contract rows; the forfeit ledger retires with no reader, and a preflight refusal returns its refusal to the caller like every other; C1's health condition is the refusal-streak trigger only.

> **R26 — The annexes close.** Each of C10, C11, C12, and C13 moves the live declaration of its surface into the implementation home (the envelope, the entry doc, and the echo set derive from there), re-verifies its rows against its build head with a dated note, and writes the table's last correction in its closing note; when the last of the four lands the derivation tables close as adjudication history and nothing maintains them.

> **R27 — Birth duties for anything that detects.** From the stamp, a new detector, gate, watcher, census, or detector-shaped test ships only with all three birth duties: (i) a bite-proof that demonstrably fires on a planted defect and is deterministic by construction (no wall-clock assertions, no environment inference); (ii) a retirement condition and tag on its keep-or-retire entry; and (iii) by-construction coverage, its scope complete because of how it is built (one chokepoint, a closed enumeration), never a hand-maintained list chased case by case, so a hand-maintained parallel enumeration is itself rung-4 machinery that owes the same duty or does not ship. Every producer names its reader. Nothing watches other machinery.

> **R28 — Delete on contact waits for the keep list.** A behavior test that goes red under a child's change is classified by the builder (a genuine regression means the behavior is fixed and the test kept; an obsolete expectation may be deleted under the standing authorization, unless the keep list names the file), with the classification and reason in the PR body. The rule activates only once the owner has stamped the seeded keep list; before that nothing is deleted and expectations are not rewritten. *Cross-epic seam:* the keep list is seeded by the verification-strategy spec's own children (#1105's set); recorded reciprocally on #1105.
