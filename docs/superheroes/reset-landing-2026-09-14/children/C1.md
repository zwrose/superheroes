Anchor (spec-section): part-b-spec-a-forward-doctrine · § Part 7 FR-F1, FR-F3, FR-F9; § FR-B5; § FR-C1; § Part 6 · as-of amendment #0 (owner-stamped 2026-09-14; record https://github.com/zwrose/superheroes/issues/695#issuecomment-5673081783)

What: The shared seam every other child cites, landed first and alone. PHILOSOPHY.md: promise 1's new shape (a scoped word after the PRs are talked through; releases and publishing stay the owner's click; the never-merge line's merge act and force-push clause in their new form), the threat-model paragraph (the plugin reviews repos its owner trusts, and disclaims review of code that could steer its reviewer), the forward-share principle with the dial as a per-project number and the sentence citing the recorded baseline, the test-pilot-is-product convention, and Change 5's posture (shrink the interface or tolerate before documenting the trap) with its emphasis that the hard shell must stay tight, since an unwieldy shell recreates the problem inside the treatment for it. rubric/covenant.md: the hard lines in their new shape, none retired. skills/showrunner/reference/merge-train.md and the showrunner charter's duty 6: the executable rule of the same promise (the scoped word, the red-train craft rule, force-push on a stated reason, releases and publishing as the owner's click, no mechanical floor), landed in the same change set so the four homes of promise 1 are never out of step. One canonical glossary file (new; name it) seeded with the whole of Spec A FR-F9's term list, one stable slug each. No other surface is edited here; every later child points at these homes.

DoD:
- PHILOSOPHY.md carries promise 1's new shape, the threat-model paragraph, the forward-share principle with its baseline sentence, the test-pilot convention, and the Change 5 posture with the tight-shell emphasis, each in one place, with no restatement elsewhere in the file.
- rubric/covenant.md carries the hard lines in their new shape; a diff against the stamped Spec A FR-F1 text shows no rule added, removed, or weakened beyond the ruled changes.
- The four homes are amended together in one PR: PHILOSOPHY and the covenant carry the ruled FR-F1 shape; merge-train.md replaces its sentence "the owner authorizes that edit" with the red-train craft rule and states the scoped word; showrunner duty 6 carries the executable rule (the scoped word, force-push on a stated reason, releases as the owner's click, each merge reported at once, no mechanical floor); a diff of each home is in the PR body.
- The glossary file exists at the path the PR names and carries every term listed in register entry R1 (the whole FR-F9 seed plus ground-truth sources) with one slug each; a checklist in the PR body maps each R1 term to its slug, and every slug resolves as an in-file anchor.
- Every existing test that pinned verbatim sentences of the edited surfaces is retired under R4 rather than re-pointed, with the retirement listed in the PR body.
- The four validators pass locally and CI is green on the merged head; the register-quote check passes for this child.

**Epic:** #1259 (reset-landing-2026-09-14, the landing epic of the Part B specs; artifacts in PR #1258). **Child:** C1, founding texts. **Sequencing:** Wave 0, alone; the seam. **Size-consideration slot:** stated at routing. **Lane call:** full. **Kind:** reset (outside the dial per Spec A FR-A6; neither machinery nor product for the trend).

**Register entries this child consumes (quoted verbatim from docs/superheroes/reset-landing-2026-09-14/register.md; the register-quote check runs at filing, intake, and the package read):**

> **R1 — The vocabulary and its slugs.** The canonical glossary (one file, named by C1) carries one stable slug per term of Spec A FR-F9's seed, the whole seed and nothing less: ruling; decision walk; gardening pass, record, and window; condition window; keep-or-retire list; keep-or-retire entry; project configuration and configuration item; plugin component; retirement condition; catch-based, citation-based, and usage-based; real catch and noise; citation; background-session trial; consumer evidence; unmeasured; fired; proposal; retirement ruling; foundational component; authority surface; the priority tiers; declined registry, trigger, misses log, collector, and pending-words line; dial, machinery lane, product-forward lane, and N; the child names as reference vocabulary; craft call and owner call; material consequence; kind label; fold-in rules and folded-in items; workaround marker and delete-when condition; always-loaded set; keep list; rail; doctrine surface; foreign catch; the threat tags on the machinery map; and the four tags structural, capability-gap, harness-limit, and mixed; plus ground-truth sources (renamed 2026-09-13). Every other surface cites a slug; no surface restates a definition.

> **R2 — One home per rule.** Every rule, schema, and contract has exactly one home, the one Spec A Part 6 names, and every other surface points at it; a drift-tested copy is not a second home, and a drift pin on a mirror is a delete signal for the mirror. The homes this epic lands: the front door in `skills/showrunner/reference/owner-decisions.md`; the tier vocabulary, size slot, kind labels, and the four routing-time calls in `skills/showrunner/reference/issue-contract.md`; Changes 1, 2, 4-doctrine, and 7 with the pin rule in `rubric/review-discipline.md`; the keep-or-retire list and the workaround-marker inventory in one new keep-or-retire reference doc; the prose standard in `rubric/prose-standard.md`; the glossary in one file; the forward share and dial principle in PHILOSOPHY with the numbers in the configure profile; promise 1's new shape in PHILOSOPHY, the covenant, showrunner duty 6, and `merge-train.md` amended together in one change set (C1); the trust boundary in PHILOSOPHY's threat-model paragraph, the configure profile's item 10, and one sentence in the security lens prompt; the host position in CONVENTIONS section 7, the README host line, and the dated `hosts/codex-tools.md`.

> **R12 — The trust boundary.** The plugin reviews repos its owner trusts; review of code that could steer its reviewer is out of scope, disclaimed in PHILOSOPHY's threat-model paragraph and each project's configuration item 10, and on the declined registry with the one trigger "#40 goes live". The sanitized view keeps the neutral-cwd export, the exclusion of the twelve agent-configuration files and six directories from the tree the seats run in, and the containment family (the outward-symlink refusal, repo-root validation, the PR-body session pairing, the run-dir symlink refusal); a changed hunk on a stripped path is delivered beside the review patch as a plain review-only file whose name says it is data under review, which the seat prompt names as a context file; the hostile-repo defense set retires, listed by name in C16's first dated note before anything leaves. The security lens reads the project's declared threat model, and an accepted exposure is not a finding.

> **R13 — The host position.** Claude Code is the supported host; Codex loads the skills and runs the discipline layer with no host guarantees; the doctrine and the skills stay host-neutral, the mechanical floors are host-specific, and a host that cannot carry one gets a disclosed absence, never a parity sentence. CONVENTIONS section 7.3 stays; sections 7.1, 7.2, and 7.4 narrow to the ruled claim, with the Codex absence stated in its true form (no session-start bootstrap, the covenant only through a project's own CLAUDE.md copy, no carrier in the out-of-repo storage mode). "The host model" means whatever runs the current session, read from the hook payload; "not the maker's family" replaces "non-Anthropic" at the micro-lane sites.

> **R19 — The merge act.** Approval is never delegated: it is a scoped word in chat after the PRs are talked through, recorded by PR number beside the word and on each PR's owner half; the advisor executes merges inside the scope after the lane's review and verification evidence, CI green on the recorded head, and a current branch, and reports each merge at once in the thread that gave the word; a PR opened after the word or changed materially asks again, judged against the material-consequence line; a red merge train is fixed under the word when the fix is craft; force-push proceeds on a word after the reason is stated; releases and publishing stay the owner's click unless handed over in the moment; there is no mechanical merge floor.

> **R27 — Birth duties for anything that detects.** From the stamp, a new detector, gate, watcher, census, or detector-shaped test ships only with all three birth duties: (i) a bite-proof that demonstrably fires on a planted defect and is deterministic by construction (no wall-clock assertions, no environment inference); (ii) a retirement condition and tag on its keep-or-retire entry; and (iii) by-construction coverage, its scope complete because of how it is built (one chokepoint, a closed enumeration), never a hand-maintained list chased case by case, so a hand-maintained parallel enumeration is itself rung-4 machinery that owes the same duty or does not ship. Every producer names its reader. Nothing watches other machinery.

> **R28 — Delete on contact waits for the keep list.** A behavior test that goes red under a child's change is classified by the builder (a genuine regression means the behavior is fixed and the test kept; an obsolete expectation may be deleted under the standing authorization, unless the keep list names the file), with the classification and reason in the PR body. The rule activates only once the owner has stamped the seeded keep list; before that nothing is deleted and expectations are not rewritten. *Cross-epic seam:* the keep list is seeded by the verification-strategy spec's own children (#1105's set); recorded reciprocally on #1105.
